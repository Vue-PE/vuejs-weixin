/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId])
/******/ 			return installedModules[moduleId].exports;
/******/
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			exports: {},
/******/ 			id: moduleId,
/******/ 			loaded: false
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.loaded = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(0);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__(1);


/***/ },
/* 1 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	var _vue = __webpack_require__(2);
	
	var _vue2 = _interopRequireDefault(_vue);
	
	var _vueResource = __webpack_require__(4);
	
	var _vueResource2 = _interopRequireDefault(_vueResource);
	
	var _vueRouter = __webpack_require__(28);
	
	var _vueRouter2 = _interopRequireDefault(_vueRouter);
	
	var _router = __webpack_require__(29);
	
	var _router2 = _interopRequireDefault(_router);
	
	var _App = __webpack_require__(208);
	
	var _App2 = _interopRequireDefault(_App);
	
	__webpack_require__(222);
	
	var _local = __webpack_require__(223);
	
	var _local2 = _interopRequireDefault(_local);
	
	var _muiMin = __webpack_require__(51);
	
	var _muiMin2 = _interopRequireDefault(_muiMin);
	
	var _fastclick = __webpack_require__(224);
	
	var _fastclick2 = _interopRequireDefault(_fastclick);
	
	var _api = __webpack_require__(225);
	
	var _api2 = _interopRequireDefault(_api);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	// 公共组件
	// 系统文件
	window['LS'] = _local2.default;
	window['API'] = _api2.default;
	window['mui'] = _muiMin2.default;
	_fastclick2.default.attach(document.body);
	
	_vue2.default.use(_vueRouter2.default);
	_vue2.default.use(_vueResource2.default);
	_vue2.default.config.silent = false;
	_vue2.default.config.devtools = false;
	
	// 路由器配置
	var router = new _vueRouter2.default({
	    hashbang: false
	});
	(0, _router2.default)(router);
	
	//vue-resource设置
	// Vue.http.options.emulateHTTP = true
	_vue2.default.http.options.emulateJSON = true;
	_vue2.default.http.interceptors.push({
	    request: function request(_request) {
	        _request.data['token'] = this.$route.params.token;
	        return _request;
	    },
	    response: function response(_response) {
	        if (_response.data.errorInfo) {
	            this.$store.state.showTip({ type: 'error', content: _response.data.errorInfo });
	        }
	        return _response;
	    }
	});
	
	// 启动路由
	router.start(_App2.default, 'app');

/***/ },
/* 2 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(global, process) {/*!
	 * Vue.js v1.0.26
	 * (c) 2016 Evan You
	 * Released under the MIT License.
	 */
	'use strict';
	
	function set(obj, key, val) {
	  if (hasOwn(obj, key)) {
	    obj[key] = val;
	    return;
	  }
	  if (obj._isVue) {
	    set(obj._data, key, val);
	    return;
	  }
	  var ob = obj.__ob__;
	  if (!ob) {
	    obj[key] = val;
	    return;
	  }
	  ob.convert(key, val);
	  ob.dep.notify();
	  if (ob.vms) {
	    var i = ob.vms.length;
	    while (i--) {
	      var vm = ob.vms[i];
	      vm._proxy(key);
	      vm._digest();
	    }
	  }
	  return val;
	}
	
	/**
	 * Delete a property and trigger change if necessary.
	 *
	 * @param {Object} obj
	 * @param {String} key
	 */
	
	function del(obj, key) {
	  if (!hasOwn(obj, key)) {
	    return;
	  }
	  delete obj[key];
	  var ob = obj.__ob__;
	  if (!ob) {
	    if (obj._isVue) {
	      delete obj._data[key];
	      obj._digest();
	    }
	    return;
	  }
	  ob.dep.notify();
	  if (ob.vms) {
	    var i = ob.vms.length;
	    while (i--) {
	      var vm = ob.vms[i];
	      vm._unproxy(key);
	      vm._digest();
	    }
	  }
	}
	
	var hasOwnProperty = Object.prototype.hasOwnProperty;
	/**
	 * Check whether the object has the property.
	 *
	 * @param {Object} obj
	 * @param {String} key
	 * @return {Boolean}
	 */
	
	function hasOwn(obj, key) {
	  return hasOwnProperty.call(obj, key);
	}
	
	/**
	 * Check if an expression is a literal value.
	 *
	 * @param {String} exp
	 * @return {Boolean}
	 */
	
	var literalValueRE = /^\s?(true|false|-?[\d\.]+|'[^']*'|"[^"]*")\s?$/;
	
	function isLiteral(exp) {
	  return literalValueRE.test(exp);
	}
	
	/**
	 * Check if a string starts with $ or _
	 *
	 * @param {String} str
	 * @return {Boolean}
	 */
	
	function isReserved(str) {
	  var c = (str + '').charCodeAt(0);
	  return c === 0x24 || c === 0x5F;
	}
	
	/**
	 * Guard text output, make sure undefined outputs
	 * empty string
	 *
	 * @param {*} value
	 * @return {String}
	 */
	
	function _toString(value) {
	  return value == null ? '' : value.toString();
	}
	
	/**
	 * Check and convert possible numeric strings to numbers
	 * before setting back to data
	 *
	 * @param {*} value
	 * @return {*|Number}
	 */
	
	function toNumber(value) {
	  if (typeof value !== 'string') {
	    return value;
	  } else {
	    var parsed = Number(value);
	    return isNaN(parsed) ? value : parsed;
	  }
	}
	
	/**
	 * Convert string boolean literals into real booleans.
	 *
	 * @param {*} value
	 * @return {*|Boolean}
	 */
	
	function toBoolean(value) {
	  return value === 'true' ? true : value === 'false' ? false : value;
	}
	
	/**
	 * Strip quotes from a string
	 *
	 * @param {String} str
	 * @return {String | false}
	 */
	
	function stripQuotes(str) {
	  var a = str.charCodeAt(0);
	  var b = str.charCodeAt(str.length - 1);
	  return a === b && (a === 0x22 || a === 0x27) ? str.slice(1, -1) : str;
	}
	
	/**
	 * Camelize a hyphen-delmited string.
	 *
	 * @param {String} str
	 * @return {String}
	 */
	
	var camelizeRE = /-(\w)/g;
	
	function camelize(str) {
	  return str.replace(camelizeRE, toUpper);
	}
	
	function toUpper(_, c) {
	  return c ? c.toUpperCase() : '';
	}
	
	/**
	 * Hyphenate a camelCase string.
	 *
	 * @param {String} str
	 * @return {String}
	 */
	
	var hyphenateRE = /([a-z\d])([A-Z])/g;
	
	function hyphenate(str) {
	  return str.replace(hyphenateRE, '$1-$2').toLowerCase();
	}
	
	/**
	 * Converts hyphen/underscore/slash delimitered names into
	 * camelized classNames.
	 *
	 * e.g. my-component => MyComponent
	 *      some_else    => SomeElse
	 *      some/comp    => SomeComp
	 *
	 * @param {String} str
	 * @return {String}
	 */
	
	var classifyRE = /(?:^|[-_\/])(\w)/g;
	
	function classify(str) {
	  return str.replace(classifyRE, toUpper);
	}
	
	/**
	 * Simple bind, faster than native
	 *
	 * @param {Function} fn
	 * @param {Object} ctx
	 * @return {Function}
	 */
	
	function bind(fn, ctx) {
	  return function (a) {
	    var l = arguments.length;
	    return l ? l > 1 ? fn.apply(ctx, arguments) : fn.call(ctx, a) : fn.call(ctx);
	  };
	}
	
	/**
	 * Convert an Array-like object to a real Array.
	 *
	 * @param {Array-like} list
	 * @param {Number} [start] - start index
	 * @return {Array}
	 */
	
	function toArray(list, start) {
	  start = start || 0;
	  var i = list.length - start;
	  var ret = new Array(i);
	  while (i--) {
	    ret[i] = list[i + start];
	  }
	  return ret;
	}
	
	/**
	 * Mix properties into target object.
	 *
	 * @param {Object} to
	 * @param {Object} from
	 */
	
	function extend(to, from) {
	  var keys = Object.keys(from);
	  var i = keys.length;
	  while (i--) {
	    to[keys[i]] = from[keys[i]];
	  }
	  return to;
	}
	
	/**
	 * Quick object check - this is primarily used to tell
	 * Objects from primitive values when we know the value
	 * is a JSON-compliant type.
	 *
	 * @param {*} obj
	 * @return {Boolean}
	 */
	
	function isObject(obj) {
	  return obj !== null && typeof obj === 'object';
	}
	
	/**
	 * Strict object type check. Only returns true
	 * for plain JavaScript objects.
	 *
	 * @param {*} obj
	 * @return {Boolean}
	 */
	
	var toString = Object.prototype.toString;
	var OBJECT_STRING = '[object Object]';
	
	function isPlainObject(obj) {
	  return toString.call(obj) === OBJECT_STRING;
	}
	
	/**
	 * Array type check.
	 *
	 * @param {*} obj
	 * @return {Boolean}
	 */
	
	var isArray = Array.isArray;
	
	/**
	 * Define a property.
	 *
	 * @param {Object} obj
	 * @param {String} key
	 * @param {*} val
	 * @param {Boolean} [enumerable]
	 */
	
	function def(obj, key, val, enumerable) {
	  Object.defineProperty(obj, key, {
	    value: val,
	    enumerable: !!enumerable,
	    writable: true,
	    configurable: true
	  });
	}
	
	/**
	 * Debounce a function so it only gets called after the
	 * input stops arriving after the given wait period.
	 *
	 * @param {Function} func
	 * @param {Number} wait
	 * @return {Function} - the debounced function
	 */
	
	function _debounce(func, wait) {
	  var timeout, args, context, timestamp, result;
	  var later = function later() {
	    var last = Date.now() - timestamp;
	    if (last < wait && last >= 0) {
	      timeout = setTimeout(later, wait - last);
	    } else {
	      timeout = null;
	      result = func.apply(context, args);
	      if (!timeout) context = args = null;
	    }
	  };
	  return function () {
	    context = this;
	    args = arguments;
	    timestamp = Date.now();
	    if (!timeout) {
	      timeout = setTimeout(later, wait);
	    }
	    return result;
	  };
	}
	
	/**
	 * Manual indexOf because it's slightly faster than
	 * native.
	 *
	 * @param {Array} arr
	 * @param {*} obj
	 */
	
	function indexOf(arr, obj) {
	  var i = arr.length;
	  while (i--) {
	    if (arr[i] === obj) return i;
	  }
	  return -1;
	}
	
	/**
	 * Make a cancellable version of an async callback.
	 *
	 * @param {Function} fn
	 * @return {Function}
	 */
	
	function cancellable(fn) {
	  var cb = function cb() {
	    if (!cb.cancelled) {
	      return fn.apply(this, arguments);
	    }
	  };
	  cb.cancel = function () {
	    cb.cancelled = true;
	  };
	  return cb;
	}
	
	/**
	 * Check if two values are loosely equal - that is,
	 * if they are plain objects, do they have the same shape?
	 *
	 * @param {*} a
	 * @param {*} b
	 * @return {Boolean}
	 */
	
	function looseEqual(a, b) {
	  /* eslint-disable eqeqeq */
	  return a == b || (isObject(a) && isObject(b) ? JSON.stringify(a) === JSON.stringify(b) : false);
	  /* eslint-enable eqeqeq */
	}
	
	var hasProto = ('__proto__' in {});
	
	// Browser environment sniffing
	var inBrowser = typeof window !== 'undefined' && Object.prototype.toString.call(window) !== '[object Object]';
	
	// detect devtools
	var devtools = inBrowser && window.__VUE_DEVTOOLS_GLOBAL_HOOK__;
	
	// UA sniffing for working around browser-specific quirks
	var UA = inBrowser && window.navigator.userAgent.toLowerCase();
	var isIE = UA && UA.indexOf('trident') > 0;
	var isIE9 = UA && UA.indexOf('msie 9.0') > 0;
	var isAndroid = UA && UA.indexOf('android') > 0;
	var isIos = UA && /(iphone|ipad|ipod|ios)/i.test(UA);
	var iosVersionMatch = isIos && UA.match(/os ([\d_]+)/);
	var iosVersion = iosVersionMatch && iosVersionMatch[1].split('_');
	
	// detecting iOS UIWebView by indexedDB
	var hasMutationObserverBug = iosVersion && Number(iosVersion[0]) >= 9 && Number(iosVersion[1]) >= 3 && !window.indexedDB;
	
	var transitionProp = undefined;
	var transitionEndEvent = undefined;
	var animationProp = undefined;
	var animationEndEvent = undefined;
	
	// Transition property/event sniffing
	if (inBrowser && !isIE9) {
	  var isWebkitTrans = window.ontransitionend === undefined && window.onwebkittransitionend !== undefined;
	  var isWebkitAnim = window.onanimationend === undefined && window.onwebkitanimationend !== undefined;
	  transitionProp = isWebkitTrans ? 'WebkitTransition' : 'transition';
	  transitionEndEvent = isWebkitTrans ? 'webkitTransitionEnd' : 'transitionend';
	  animationProp = isWebkitAnim ? 'WebkitAnimation' : 'animation';
	  animationEndEvent = isWebkitAnim ? 'webkitAnimationEnd' : 'animationend';
	}
	
	/**
	 * Defer a task to execute it asynchronously. Ideally this
	 * should be executed as a microtask, so we leverage
	 * MutationObserver if it's available, and fallback to
	 * setTimeout(0).
	 *
	 * @param {Function} cb
	 * @param {Object} ctx
	 */
	
	var nextTick = (function () {
	  var callbacks = [];
	  var pending = false;
	  var timerFunc;
	  function nextTickHandler() {
	    pending = false;
	    var copies = callbacks.slice(0);
	    callbacks = [];
	    for (var i = 0; i < copies.length; i++) {
	      copies[i]();
	    }
	  }
	
	  /* istanbul ignore if */
	  if (typeof MutationObserver !== 'undefined' && !hasMutationObserverBug) {
	    var counter = 1;
	    var observer = new MutationObserver(nextTickHandler);
	    var textNode = document.createTextNode(counter);
	    observer.observe(textNode, {
	      characterData: true
	    });
	    timerFunc = function () {
	      counter = (counter + 1) % 2;
	      textNode.data = counter;
	    };
	  } else {
	    // webpack attempts to inject a shim for setImmediate
	    // if it is used as a global, so we have to work around that to
	    // avoid bundling unnecessary code.
	    var context = inBrowser ? window : typeof global !== 'undefined' ? global : {};
	    timerFunc = context.setImmediate || setTimeout;
	  }
	  return function (cb, ctx) {
	    var func = ctx ? function () {
	      cb.call(ctx);
	    } : cb;
	    callbacks.push(func);
	    if (pending) return;
	    pending = true;
	    timerFunc(nextTickHandler, 0);
	  };
	})();
	
	var _Set = undefined;
	/* istanbul ignore if */
	if (typeof Set !== 'undefined' && Set.toString().match(/native code/)) {
	  // use native Set when available.
	  _Set = Set;
	} else {
	  // a non-standard Set polyfill that only works with primitive keys.
	  _Set = function () {
	    this.set = Object.create(null);
	  };
	  _Set.prototype.has = function (key) {
	    return this.set[key] !== undefined;
	  };
	  _Set.prototype.add = function (key) {
	    this.set[key] = 1;
	  };
	  _Set.prototype.clear = function () {
	    this.set = Object.create(null);
	  };
	}
	
	function Cache(limit) {
	  this.size = 0;
	  this.limit = limit;
	  this.head = this.tail = undefined;
	  this._keymap = Object.create(null);
	}
	
	var p = Cache.prototype;
	
	/**
	 * Put <value> into the cache associated with <key>.
	 * Returns the entry which was removed to make room for
	 * the new entry. Otherwise undefined is returned.
	 * (i.e. if there was enough room already).
	 *
	 * @param {String} key
	 * @param {*} value
	 * @return {Entry|undefined}
	 */
	
	p.put = function (key, value) {
	  var removed;
	
	  var entry = this.get(key, true);
	  if (!entry) {
	    if (this.size === this.limit) {
	      removed = this.shift();
	    }
	    entry = {
	      key: key
	    };
	    this._keymap[key] = entry;
	    if (this.tail) {
	      this.tail.newer = entry;
	      entry.older = this.tail;
	    } else {
	      this.head = entry;
	    }
	    this.tail = entry;
	    this.size++;
	  }
	  entry.value = value;
	
	  return removed;
	};
	
	/**
	 * Purge the least recently used (oldest) entry from the
	 * cache. Returns the removed entry or undefined if the
	 * cache was empty.
	 */
	
	p.shift = function () {
	  var entry = this.head;
	  if (entry) {
	    this.head = this.head.newer;
	    this.head.older = undefined;
	    entry.newer = entry.older = undefined;
	    this._keymap[entry.key] = undefined;
	    this.size--;
	  }
	  return entry;
	};
	
	/**
	 * Get and register recent use of <key>. Returns the value
	 * associated with <key> or undefined if not in cache.
	 *
	 * @param {String} key
	 * @param {Boolean} returnEntry
	 * @return {Entry|*}
	 */
	
	p.get = function (key, returnEntry) {
	  var entry = this._keymap[key];
	  if (entry === undefined) return;
	  if (entry === this.tail) {
	    return returnEntry ? entry : entry.value;
	  }
	  // HEAD--------------TAIL
	  //   <.older   .newer>
	  //  <--- add direction --
	  //   A  B  C  <D>  E
	  if (entry.newer) {
	    if (entry === this.head) {
	      this.head = entry.newer;
	    }
	    entry.newer.older = entry.older; // C <-- E.
	  }
	  if (entry.older) {
	    entry.older.newer = entry.newer; // C. --> E
	  }
	  entry.newer = undefined; // D --x
	  entry.older = this.tail; // D. --> E
	  if (this.tail) {
	    this.tail.newer = entry; // E. <-- D
	  }
	  this.tail = entry;
	  return returnEntry ? entry : entry.value;
	};
	
	var cache$1 = new Cache(1000);
	var filterTokenRE = /[^\s'"]+|'[^']*'|"[^"]*"/g;
	var reservedArgRE = /^in$|^-?\d+/;
	
	/**
	 * Parser state
	 */
	
	var str;
	var dir;
	var c;
	var prev;
	var i;
	var l;
	var lastFilterIndex;
	var inSingle;
	var inDouble;
	var curly;
	var square;
	var paren;
	/**
	 * Push a filter to the current directive object
	 */
	
	function pushFilter() {
	  var exp = str.slice(lastFilterIndex, i).trim();
	  var filter;
	  if (exp) {
	    filter = {};
	    var tokens = exp.match(filterTokenRE);
	    filter.name = tokens[0];
	    if (tokens.length > 1) {
	      filter.args = tokens.slice(1).map(processFilterArg);
	    }
	  }
	  if (filter) {
	    (dir.filters = dir.filters || []).push(filter);
	  }
	  lastFilterIndex = i + 1;
	}
	
	/**
	 * Check if an argument is dynamic and strip quotes.
	 *
	 * @param {String} arg
	 * @return {Object}
	 */
	
	function processFilterArg(arg) {
	  if (reservedArgRE.test(arg)) {
	    return {
	      value: toNumber(arg),
	      dynamic: false
	    };
	  } else {
	    var stripped = stripQuotes(arg);
	    var dynamic = stripped === arg;
	    return {
	      value: dynamic ? arg : stripped,
	      dynamic: dynamic
	    };
	  }
	}
	
	/**
	 * Parse a directive value and extract the expression
	 * and its filters into a descriptor.
	 *
	 * Example:
	 *
	 * "a + 1 | uppercase" will yield:
	 * {
	 *   expression: 'a + 1',
	 *   filters: [
	 *     { name: 'uppercase', args: null }
	 *   ]
	 * }
	 *
	 * @param {String} s
	 * @return {Object}
	 */
	
	function parseDirective(s) {
	  var hit = cache$1.get(s);
	  if (hit) {
	    return hit;
	  }
	
	  // reset parser state
	  str = s;
	  inSingle = inDouble = false;
	  curly = square = paren = 0;
	  lastFilterIndex = 0;
	  dir = {};
	
	  for (i = 0, l = str.length; i < l; i++) {
	    prev = c;
	    c = str.charCodeAt(i);
	    if (inSingle) {
	      // check single quote
	      if (c === 0x27 && prev !== 0x5C) inSingle = !inSingle;
	    } else if (inDouble) {
	      // check double quote
	      if (c === 0x22 && prev !== 0x5C) inDouble = !inDouble;
	    } else if (c === 0x7C && // pipe
	    str.charCodeAt(i + 1) !== 0x7C && str.charCodeAt(i - 1) !== 0x7C) {
	      if (dir.expression == null) {
	        // first filter, end of expression
	        lastFilterIndex = i + 1;
	        dir.expression = str.slice(0, i).trim();
	      } else {
	        // already has filter
	        pushFilter();
	      }
	    } else {
	      switch (c) {
	        case 0x22:
	          inDouble = true;break; // "
	        case 0x27:
	          inSingle = true;break; // '
	        case 0x28:
	          paren++;break; // (
	        case 0x29:
	          paren--;break; // )
	        case 0x5B:
	          square++;break; // [
	        case 0x5D:
	          square--;break; // ]
	        case 0x7B:
	          curly++;break; // {
	        case 0x7D:
	          curly--;break; // }
	      }
	    }
	  }
	
	  if (dir.expression == null) {
	    dir.expression = str.slice(0, i).trim();
	  } else if (lastFilterIndex !== 0) {
	    pushFilter();
	  }
	
	  cache$1.put(s, dir);
	  return dir;
	}
	
	var directive = Object.freeze({
	  parseDirective: parseDirective
	});
	
	var regexEscapeRE = /[-.*+?^${}()|[\]\/\\]/g;
	var cache = undefined;
	var tagRE = undefined;
	var htmlRE = undefined;
	/**
	 * Escape a string so it can be used in a RegExp
	 * constructor.
	 *
	 * @param {String} str
	 */
	
	function escapeRegex(str) {
	  return str.replace(regexEscapeRE, '\\$&');
	}
	
	function compileRegex() {
	  var open = escapeRegex(config.delimiters[0]);
	  var close = escapeRegex(config.delimiters[1]);
	  var unsafeOpen = escapeRegex(config.unsafeDelimiters[0]);
	  var unsafeClose = escapeRegex(config.unsafeDelimiters[1]);
	  tagRE = new RegExp(unsafeOpen + '((?:.|\\n)+?)' + unsafeClose + '|' + open + '((?:.|\\n)+?)' + close, 'g');
	  htmlRE = new RegExp('^' + unsafeOpen + '((?:.|\\n)+?)' + unsafeClose + '$');
	  // reset cache
	  cache = new Cache(1000);
	}
	
	/**
	 * Parse a template text string into an array of tokens.
	 *
	 * @param {String} text
	 * @return {Array<Object> | null}
	 *               - {String} type
	 *               - {String} value
	 *               - {Boolean} [html]
	 *               - {Boolean} [oneTime]
	 */
	
	function parseText(text) {
	  if (!cache) {
	    compileRegex();
	  }
	  var hit = cache.get(text);
	  if (hit) {
	    return hit;
	  }
	  if (!tagRE.test(text)) {
	    return null;
	  }
	  var tokens = [];
	  var lastIndex = tagRE.lastIndex = 0;
	  var match, index, html, value, first, oneTime;
	  /* eslint-disable no-cond-assign */
	  while (match = tagRE.exec(text)) {
	    /* eslint-enable no-cond-assign */
	    index = match.index;
	    // push text token
	    if (index > lastIndex) {
	      tokens.push({
	        value: text.slice(lastIndex, index)
	      });
	    }
	    // tag token
	    html = htmlRE.test(match[0]);
	    value = html ? match[1] : match[2];
	    first = value.charCodeAt(0);
	    oneTime = first === 42; // *
	    value = oneTime ? value.slice(1) : value;
	    tokens.push({
	      tag: true,
	      value: value.trim(),
	      html: html,
	      oneTime: oneTime
	    });
	    lastIndex = index + match[0].length;
	  }
	  if (lastIndex < text.length) {
	    tokens.push({
	      value: text.slice(lastIndex)
	    });
	  }
	  cache.put(text, tokens);
	  return tokens;
	}
	
	/**
	 * Format a list of tokens into an expression.
	 * e.g. tokens parsed from 'a {{b}} c' can be serialized
	 * into one single expression as '"a " + b + " c"'.
	 *
	 * @param {Array} tokens
	 * @param {Vue} [vm]
	 * @return {String}
	 */
	
	function tokensToExp(tokens, vm) {
	  if (tokens.length > 1) {
	    return tokens.map(function (token) {
	      return formatToken(token, vm);
	    }).join('+');
	  } else {
	    return formatToken(tokens[0], vm, true);
	  }
	}
	
	/**
	 * Format a single token.
	 *
	 * @param {Object} token
	 * @param {Vue} [vm]
	 * @param {Boolean} [single]
	 * @return {String}
	 */
	
	function formatToken(token, vm, single) {
	  return token.tag ? token.oneTime && vm ? '"' + vm.$eval(token.value) + '"' : inlineFilters(token.value, single) : '"' + token.value + '"';
	}
	
	/**
	 * For an attribute with multiple interpolation tags,
	 * e.g. attr="some-{{thing | filter}}", in order to combine
	 * the whole thing into a single watchable expression, we
	 * have to inline those filters. This function does exactly
	 * that. This is a bit hacky but it avoids heavy changes
	 * to directive parser and watcher mechanism.
	 *
	 * @param {String} exp
	 * @param {Boolean} single
	 * @return {String}
	 */
	
	var filterRE = /[^|]\|[^|]/;
	function inlineFilters(exp, single) {
	  if (!filterRE.test(exp)) {
	    return single ? exp : '(' + exp + ')';
	  } else {
	    var dir = parseDirective(exp);
	    if (!dir.filters) {
	      return '(' + exp + ')';
	    } else {
	      return 'this._applyFilters(' + dir.expression + // value
	      ',null,' + // oldValue (null for read)
	      JSON.stringify(dir.filters) + // filter descriptors
	      ',false)'; // write?
	    }
	  }
	}
	
	var text = Object.freeze({
	  compileRegex: compileRegex,
	  parseText: parseText,
	  tokensToExp: tokensToExp
	});
	
	var delimiters = ['{{', '}}'];
	var unsafeDelimiters = ['{{{', '}}}'];
	
	var config = Object.defineProperties({
	
	  /**
	   * Whether to print debug messages.
	   * Also enables stack trace for warnings.
	   *
	   * @type {Boolean}
	   */
	
	  debug: false,
	
	  /**
	   * Whether to suppress warnings.
	   *
	   * @type {Boolean}
	   */
	
	  silent: false,
	
	  /**
	   * Whether to use async rendering.
	   */
	
	  async: true,
	
	  /**
	   * Whether to warn against errors caught when evaluating
	   * expressions.
	   */
	
	  warnExpressionErrors: true,
	
	  /**
	   * Whether to allow devtools inspection.
	   * Disabled by default in production builds.
	   */
	
	  devtools: process.env.NODE_ENV !== 'production',
	
	  /**
	   * Internal flag to indicate the delimiters have been
	   * changed.
	   *
	   * @type {Boolean}
	   */
	
	  _delimitersChanged: true,
	
	  /**
	   * List of asset types that a component can own.
	   *
	   * @type {Array}
	   */
	
	  _assetTypes: ['component', 'directive', 'elementDirective', 'filter', 'transition', 'partial'],
	
	  /**
	   * prop binding modes
	   */
	
	  _propBindingModes: {
	    ONE_WAY: 0,
	    TWO_WAY: 1,
	    ONE_TIME: 2
	  },
	
	  /**
	   * Max circular updates allowed in a batcher flush cycle.
	   */
	
	  _maxUpdateCount: 100
	
	}, {
	  delimiters: { /**
	                 * Interpolation delimiters. Changing these would trigger
	                 * the text parser to re-compile the regular expressions.
	                 *
	                 * @type {Array<String>}
	                 */
	
	    get: function get() {
	      return delimiters;
	    },
	    set: function set(val) {
	      delimiters = val;
	      compileRegex();
	    },
	    configurable: true,
	    enumerable: true
	  },
	  unsafeDelimiters: {
	    get: function get() {
	      return unsafeDelimiters;
	    },
	    set: function set(val) {
	      unsafeDelimiters = val;
	      compileRegex();
	    },
	    configurable: true,
	    enumerable: true
	  }
	});
	
	var warn = undefined;
	var formatComponentName = undefined;
	
	if (process.env.NODE_ENV !== 'production') {
	  (function () {
	    var hasConsole = typeof console !== 'undefined';
	
	    warn = function (msg, vm) {
	      if (hasConsole && !config.silent) {
	        console.error('[Vue warn]: ' + msg + (vm ? formatComponentName(vm) : ''));
	      }
	    };
	
	    formatComponentName = function (vm) {
	      var name = vm._isVue ? vm.$options.name : vm.name;
	      return name ? ' (found in component: <' + hyphenate(name) + '>)' : '';
	    };
	  })();
	}
	
	/**
	 * Append with transition.
	 *
	 * @param {Element} el
	 * @param {Element} target
	 * @param {Vue} vm
	 * @param {Function} [cb]
	 */
	
	function appendWithTransition(el, target, vm, cb) {
	  applyTransition(el, 1, function () {
	    target.appendChild(el);
	  }, vm, cb);
	}
	
	/**
	 * InsertBefore with transition.
	 *
	 * @param {Element} el
	 * @param {Element} target
	 * @param {Vue} vm
	 * @param {Function} [cb]
	 */
	
	function beforeWithTransition(el, target, vm, cb) {
	  applyTransition(el, 1, function () {
	    before(el, target);
	  }, vm, cb);
	}
	
	/**
	 * Remove with transition.
	 *
	 * @param {Element} el
	 * @param {Vue} vm
	 * @param {Function} [cb]
	 */
	
	function removeWithTransition(el, vm, cb) {
	  applyTransition(el, -1, function () {
	    remove(el);
	  }, vm, cb);
	}
	
	/**
	 * Apply transitions with an operation callback.
	 *
	 * @param {Element} el
	 * @param {Number} direction
	 *                  1: enter
	 *                 -1: leave
	 * @param {Function} op - the actual DOM operation
	 * @param {Vue} vm
	 * @param {Function} [cb]
	 */
	
	function applyTransition(el, direction, op, vm, cb) {
	  var transition = el.__v_trans;
	  if (!transition ||
	  // skip if there are no js hooks and CSS transition is
	  // not supported
	  !transition.hooks && !transitionEndEvent ||
	  // skip transitions for initial compile
	  !vm._isCompiled ||
	  // if the vm is being manipulated by a parent directive
	  // during the parent's compilation phase, skip the
	  // animation.
	  vm.$parent && !vm.$parent._isCompiled) {
	    op();
	    if (cb) cb();
	    return;
	  }
	  var action = direction > 0 ? 'enter' : 'leave';
	  transition[action](op, cb);
	}
	
	var transition = Object.freeze({
	  appendWithTransition: appendWithTransition,
	  beforeWithTransition: beforeWithTransition,
	  removeWithTransition: removeWithTransition,
	  applyTransition: applyTransition
	});
	
	/**
	 * Query an element selector if it's not an element already.
	 *
	 * @param {String|Element} el
	 * @return {Element}
	 */
	
	function query(el) {
	  if (typeof el === 'string') {
	    var selector = el;
	    el = document.querySelector(el);
	    if (!el) {
	      process.env.NODE_ENV !== 'production' && warn('Cannot find element: ' + selector);
	    }
	  }
	  return el;
	}
	
	/**
	 * Check if a node is in the document.
	 * Note: document.documentElement.contains should work here
	 * but always returns false for comment nodes in phantomjs,
	 * making unit tests difficult. This is fixed by doing the
	 * contains() check on the node's parentNode instead of
	 * the node itself.
	 *
	 * @param {Node} node
	 * @return {Boolean}
	 */
	
	function inDoc(node) {
	  if (!node) return false;
	  var doc = node.ownerDocument.documentElement;
	  var parent = node.parentNode;
	  return doc === node || doc === parent || !!(parent && parent.nodeType === 1 && doc.contains(parent));
	}
	
	/**
	 * Get and remove an attribute from a node.
	 *
	 * @param {Node} node
	 * @param {String} _attr
	 */
	
	function getAttr(node, _attr) {
	  var val = node.getAttribute(_attr);
	  if (val !== null) {
	    node.removeAttribute(_attr);
	  }
	  return val;
	}
	
	/**
	 * Get an attribute with colon or v-bind: prefix.
	 *
	 * @param {Node} node
	 * @param {String} name
	 * @return {String|null}
	 */
	
	function getBindAttr(node, name) {
	  var val = getAttr(node, ':' + name);
	  if (val === null) {
	    val = getAttr(node, 'v-bind:' + name);
	  }
	  return val;
	}
	
	/**
	 * Check the presence of a bind attribute.
	 *
	 * @param {Node} node
	 * @param {String} name
	 * @return {Boolean}
	 */
	
	function hasBindAttr(node, name) {
	  return node.hasAttribute(name) || node.hasAttribute(':' + name) || node.hasAttribute('v-bind:' + name);
	}
	
	/**
	 * Insert el before target
	 *
	 * @param {Element} el
	 * @param {Element} target
	 */
	
	function before(el, target) {
	  target.parentNode.insertBefore(el, target);
	}
	
	/**
	 * Insert el after target
	 *
	 * @param {Element} el
	 * @param {Element} target
	 */
	
	function after(el, target) {
	  if (target.nextSibling) {
	    before(el, target.nextSibling);
	  } else {
	    target.parentNode.appendChild(el);
	  }
	}
	
	/**
	 * Remove el from DOM
	 *
	 * @param {Element} el
	 */
	
	function remove(el) {
	  el.parentNode.removeChild(el);
	}
	
	/**
	 * Prepend el to target
	 *
	 * @param {Element} el
	 * @param {Element} target
	 */
	
	function prepend(el, target) {
	  if (target.firstChild) {
	    before(el, target.firstChild);
	  } else {
	    target.appendChild(el);
	  }
	}
	
	/**
	 * Replace target with el
	 *
	 * @param {Element} target
	 * @param {Element} el
	 */
	
	function replace(target, el) {
	  var parent = target.parentNode;
	  if (parent) {
	    parent.replaceChild(el, target);
	  }
	}
	
	/**
	 * Add event listener shorthand.
	 *
	 * @param {Element} el
	 * @param {String} event
	 * @param {Function} cb
	 * @param {Boolean} [useCapture]
	 */
	
	function on(el, event, cb, useCapture) {
	  el.addEventListener(event, cb, useCapture);
	}
	
	/**
	 * Remove event listener shorthand.
	 *
	 * @param {Element} el
	 * @param {String} event
	 * @param {Function} cb
	 */
	
	function off(el, event, cb) {
	  el.removeEventListener(event, cb);
	}
	
	/**
	 * For IE9 compat: when both class and :class are present
	 * getAttribute('class') returns wrong value...
	 *
	 * @param {Element} el
	 * @return {String}
	 */
	
	function getClass(el) {
	  var classname = el.className;
	  if (typeof classname === 'object') {
	    classname = classname.baseVal || '';
	  }
	  return classname;
	}
	
	/**
	 * In IE9, setAttribute('class') will result in empty class
	 * if the element also has the :class attribute; However in
	 * PhantomJS, setting `className` does not work on SVG elements...
	 * So we have to do a conditional check here.
	 *
	 * @param {Element} el
	 * @param {String} cls
	 */
	
	function setClass(el, cls) {
	  /* istanbul ignore if */
	  if (isIE9 && !/svg$/.test(el.namespaceURI)) {
	    el.className = cls;
	  } else {
	    el.setAttribute('class', cls);
	  }
	}
	
	/**
	 * Add class with compatibility for IE & SVG
	 *
	 * @param {Element} el
	 * @param {String} cls
	 */
	
	function addClass(el, cls) {
	  if (el.classList) {
	    el.classList.add(cls);
	  } else {
	    var cur = ' ' + getClass(el) + ' ';
	    if (cur.indexOf(' ' + cls + ' ') < 0) {
	      setClass(el, (cur + cls).trim());
	    }
	  }
	}
	
	/**
	 * Remove class with compatibility for IE & SVG
	 *
	 * @param {Element} el
	 * @param {String} cls
	 */
	
	function removeClass(el, cls) {
	  if (el.classList) {
	    el.classList.remove(cls);
	  } else {
	    var cur = ' ' + getClass(el) + ' ';
	    var tar = ' ' + cls + ' ';
	    while (cur.indexOf(tar) >= 0) {
	      cur = cur.replace(tar, ' ');
	    }
	    setClass(el, cur.trim());
	  }
	  if (!el.className) {
	    el.removeAttribute('class');
	  }
	}
	
	/**
	 * Extract raw content inside an element into a temporary
	 * container div
	 *
	 * @param {Element} el
	 * @param {Boolean} asFragment
	 * @return {Element|DocumentFragment}
	 */
	
	function extractContent(el, asFragment) {
	  var child;
	  var rawContent;
	  /* istanbul ignore if */
	  if (isTemplate(el) && isFragment(el.content)) {
	    el = el.content;
	  }
	  if (el.hasChildNodes()) {
	    trimNode(el);
	    rawContent = asFragment ? document.createDocumentFragment() : document.createElement('div');
	    /* eslint-disable no-cond-assign */
	    while (child = el.firstChild) {
	      /* eslint-enable no-cond-assign */
	      rawContent.appendChild(child);
	    }
	  }
	  return rawContent;
	}
	
	/**
	 * Trim possible empty head/tail text and comment
	 * nodes inside a parent.
	 *
	 * @param {Node} node
	 */
	
	function trimNode(node) {
	  var child;
	  /* eslint-disable no-sequences */
	  while ((child = node.firstChild, isTrimmable(child))) {
	    node.removeChild(child);
	  }
	  while ((child = node.lastChild, isTrimmable(child))) {
	    node.removeChild(child);
	  }
	  /* eslint-enable no-sequences */
	}
	
	function isTrimmable(node) {
	  return node && (node.nodeType === 3 && !node.data.trim() || node.nodeType === 8);
	}
	
	/**
	 * Check if an element is a template tag.
	 * Note if the template appears inside an SVG its tagName
	 * will be in lowercase.
	 *
	 * @param {Element} el
	 */
	
	function isTemplate(el) {
	  return el.tagName && el.tagName.toLowerCase() === 'template';
	}
	
	/**
	 * Create an "anchor" for performing dom insertion/removals.
	 * This is used in a number of scenarios:
	 * - fragment instance
	 * - v-html
	 * - v-if
	 * - v-for
	 * - component
	 *
	 * @param {String} content
	 * @param {Boolean} persist - IE trashes empty textNodes on
	 *                            cloneNode(true), so in certain
	 *                            cases the anchor needs to be
	 *                            non-empty to be persisted in
	 *                            templates.
	 * @return {Comment|Text}
	 */
	
	function createAnchor(content, persist) {
	  var anchor = config.debug ? document.createComment(content) : document.createTextNode(persist ? ' ' : '');
	  anchor.__v_anchor = true;
	  return anchor;
	}
	
	/**
	 * Find a component ref attribute that starts with $.
	 *
	 * @param {Element} node
	 * @return {String|undefined}
	 */
	
	var refRE = /^v-ref:/;
	
	function findRef(node) {
	  if (node.hasAttributes()) {
	    var attrs = node.attributes;
	    for (var i = 0, l = attrs.length; i < l; i++) {
	      var name = attrs[i].name;
	      if (refRE.test(name)) {
	        return camelize(name.replace(refRE, ''));
	      }
	    }
	  }
	}
	
	/**
	 * Map a function to a range of nodes .
	 *
	 * @param {Node} node
	 * @param {Node} end
	 * @param {Function} op
	 */
	
	function mapNodeRange(node, end, op) {
	  var next;
	  while (node !== end) {
	    next = node.nextSibling;
	    op(node);
	    node = next;
	  }
	  op(end);
	}
	
	/**
	 * Remove a range of nodes with transition, store
	 * the nodes in a fragment with correct ordering,
	 * and call callback when done.
	 *
	 * @param {Node} start
	 * @param {Node} end
	 * @param {Vue} vm
	 * @param {DocumentFragment} frag
	 * @param {Function} cb
	 */
	
	function removeNodeRange(start, end, vm, frag, cb) {
	  var done = false;
	  var removed = 0;
	  var nodes = [];
	  mapNodeRange(start, end, function (node) {
	    if (node === end) done = true;
	    nodes.push(node);
	    removeWithTransition(node, vm, onRemoved);
	  });
	  function onRemoved() {
	    removed++;
	    if (done && removed >= nodes.length) {
	      for (var i = 0; i < nodes.length; i++) {
	        frag.appendChild(nodes[i]);
	      }
	      cb && cb();
	    }
	  }
	}
	
	/**
	 * Check if a node is a DocumentFragment.
	 *
	 * @param {Node} node
	 * @return {Boolean}
	 */
	
	function isFragment(node) {
	  return node && node.nodeType === 11;
	}
	
	/**
	 * Get outerHTML of elements, taking care
	 * of SVG elements in IE as well.
	 *
	 * @param {Element} el
	 * @return {String}
	 */
	
	function getOuterHTML(el) {
	  if (el.outerHTML) {
	    return el.outerHTML;
	  } else {
	    var container = document.createElement('div');
	    container.appendChild(el.cloneNode(true));
	    return container.innerHTML;
	  }
	}
	
	var commonTagRE = /^(div|p|span|img|a|b|i|br|ul|ol|li|h1|h2|h3|h4|h5|h6|code|pre|table|th|td|tr|form|label|input|select|option|nav|article|section|header|footer)$/i;
	var reservedTagRE = /^(slot|partial|component)$/i;
	
	var isUnknownElement = undefined;
	if (process.env.NODE_ENV !== 'production') {
	  isUnknownElement = function (el, tag) {
	    if (tag.indexOf('-') > -1) {
	      // http://stackoverflow.com/a/28210364/1070244
	      return el.constructor === window.HTMLUnknownElement || el.constructor === window.HTMLElement;
	    } else {
	      return (/HTMLUnknownElement/.test(el.toString()) &&
	        // Chrome returns unknown for several HTML5 elements.
	        // https://code.google.com/p/chromium/issues/detail?id=540526
	        // Firefox returns unknown for some "Interactive elements."
	        !/^(data|time|rtc|rb|details|dialog|summary)$/.test(tag)
	      );
	    }
	  };
	}
	
	/**
	 * Check if an element is a component, if yes return its
	 * component id.
	 *
	 * @param {Element} el
	 * @param {Object} options
	 * @return {Object|undefined}
	 */
	
	function checkComponentAttr(el, options) {
	  var tag = el.tagName.toLowerCase();
	  var hasAttrs = el.hasAttributes();
	  if (!commonTagRE.test(tag) && !reservedTagRE.test(tag)) {
	    if (resolveAsset(options, 'components', tag)) {
	      return { id: tag };
	    } else {
	      var is = hasAttrs && getIsBinding(el, options);
	      if (is) {
	        return is;
	      } else if (process.env.NODE_ENV !== 'production') {
	        var expectedTag = options._componentNameMap && options._componentNameMap[tag];
	        if (expectedTag) {
	          warn('Unknown custom element: <' + tag + '> - ' + 'did you mean <' + expectedTag + '>? ' + 'HTML is case-insensitive, remember to use kebab-case in templates.');
	        } else if (isUnknownElement(el, tag)) {
	          warn('Unknown custom element: <' + tag + '> - did you ' + 'register the component correctly? For recursive components, ' + 'make sure to provide the "name" option.');
	        }
	      }
	    }
	  } else if (hasAttrs) {
	    return getIsBinding(el, options);
	  }
	}
	
	/**
	 * Get "is" binding from an element.
	 *
	 * @param {Element} el
	 * @param {Object} options
	 * @return {Object|undefined}
	 */
	
	function getIsBinding(el, options) {
	  // dynamic syntax
	  var exp = el.getAttribute('is');
	  if (exp != null) {
	    if (resolveAsset(options, 'components', exp)) {
	      el.removeAttribute('is');
	      return { id: exp };
	    }
	  } else {
	    exp = getBindAttr(el, 'is');
	    if (exp != null) {
	      return { id: exp, dynamic: true };
	    }
	  }
	}
	
	/**
	 * Option overwriting strategies are functions that handle
	 * how to merge a parent option value and a child option
	 * value into the final value.
	 *
	 * All strategy functions follow the same signature:
	 *
	 * @param {*} parentVal
	 * @param {*} childVal
	 * @param {Vue} [vm]
	 */
	
	var strats = config.optionMergeStrategies = Object.create(null);
	
	/**
	 * Helper that recursively merges two data objects together.
	 */
	
	function mergeData(to, from) {
	  var key, toVal, fromVal;
	  for (key in from) {
	    toVal = to[key];
	    fromVal = from[key];
	    if (!hasOwn(to, key)) {
	      set(to, key, fromVal);
	    } else if (isObject(toVal) && isObject(fromVal)) {
	      mergeData(toVal, fromVal);
	    }
	  }
	  return to;
	}
	
	/**
	 * Data
	 */
	
	strats.data = function (parentVal, childVal, vm) {
	  if (!vm) {
	    // in a Vue.extend merge, both should be functions
	    if (!childVal) {
	      return parentVal;
	    }
	    if (typeof childVal !== 'function') {
	      process.env.NODE_ENV !== 'production' && warn('The "data" option should be a function ' + 'that returns a per-instance value in component ' + 'definitions.', vm);
	      return parentVal;
	    }
	    if (!parentVal) {
	      return childVal;
	    }
	    // when parentVal & childVal are both present,
	    // we need to return a function that returns the
	    // merged result of both functions... no need to
	    // check if parentVal is a function here because
	    // it has to be a function to pass previous merges.
	    return function mergedDataFn() {
	      return mergeData(childVal.call(this), parentVal.call(this));
	    };
	  } else if (parentVal || childVal) {
	    return function mergedInstanceDataFn() {
	      // instance merge
	      var instanceData = typeof childVal === 'function' ? childVal.call(vm) : childVal;
	      var defaultData = typeof parentVal === 'function' ? parentVal.call(vm) : undefined;
	      if (instanceData) {
	        return mergeData(instanceData, defaultData);
	      } else {
	        return defaultData;
	      }
	    };
	  }
	};
	
	/**
	 * El
	 */
	
	strats.el = function (parentVal, childVal, vm) {
	  if (!vm && childVal && typeof childVal !== 'function') {
	    process.env.NODE_ENV !== 'production' && warn('The "el" option should be a function ' + 'that returns a per-instance value in component ' + 'definitions.', vm);
	    return;
	  }
	  var ret = childVal || parentVal;
	  // invoke the element factory if this is instance merge
	  return vm && typeof ret === 'function' ? ret.call(vm) : ret;
	};
	
	/**
	 * Hooks and param attributes are merged as arrays.
	 */
	
	strats.init = strats.created = strats.ready = strats.attached = strats.detached = strats.beforeCompile = strats.compiled = strats.beforeDestroy = strats.destroyed = strats.activate = function (parentVal, childVal) {
	  return childVal ? parentVal ? parentVal.concat(childVal) : isArray(childVal) ? childVal : [childVal] : parentVal;
	};
	
	/**
	 * Assets
	 *
	 * When a vm is present (instance creation), we need to do
	 * a three-way merge between constructor options, instance
	 * options and parent options.
	 */
	
	function mergeAssets(parentVal, childVal) {
	  var res = Object.create(parentVal || null);
	  return childVal ? extend(res, guardArrayAssets(childVal)) : res;
	}
	
	config._assetTypes.forEach(function (type) {
	  strats[type + 's'] = mergeAssets;
	});
	
	/**
	 * Events & Watchers.
	 *
	 * Events & watchers hashes should not overwrite one
	 * another, so we merge them as arrays.
	 */
	
	strats.watch = strats.events = function (parentVal, childVal) {
	  if (!childVal) return parentVal;
	  if (!parentVal) return childVal;
	  var ret = {};
	  extend(ret, parentVal);
	  for (var key in childVal) {
	    var parent = ret[key];
	    var child = childVal[key];
	    if (parent && !isArray(parent)) {
	      parent = [parent];
	    }
	    ret[key] = parent ? parent.concat(child) : [child];
	  }
	  return ret;
	};
	
	/**
	 * Other object hashes.
	 */
	
	strats.props = strats.methods = strats.computed = function (parentVal, childVal) {
	  if (!childVal) return parentVal;
	  if (!parentVal) return childVal;
	  var ret = Object.create(null);
	  extend(ret, parentVal);
	  extend(ret, childVal);
	  return ret;
	};
	
	/**
	 * Default strategy.
	 */
	
	var defaultStrat = function defaultStrat(parentVal, childVal) {
	  return childVal === undefined ? parentVal : childVal;
	};
	
	/**
	 * Make sure component options get converted to actual
	 * constructors.
	 *
	 * @param {Object} options
	 */
	
	function guardComponents(options) {
	  if (options.components) {
	    var components = options.components = guardArrayAssets(options.components);
	    var ids = Object.keys(components);
	    var def;
	    if (process.env.NODE_ENV !== 'production') {
	      var map = options._componentNameMap = {};
	    }
	    for (var i = 0, l = ids.length; i < l; i++) {
	      var key = ids[i];
	      if (commonTagRE.test(key) || reservedTagRE.test(key)) {
	        process.env.NODE_ENV !== 'production' && warn('Do not use built-in or reserved HTML elements as component ' + 'id: ' + key);
	        continue;
	      }
	      // record a all lowercase <-> kebab-case mapping for
	      // possible custom element case error warning
	      if (process.env.NODE_ENV !== 'production') {
	        map[key.replace(/-/g, '').toLowerCase()] = hyphenate(key);
	      }
	      def = components[key];
	      if (isPlainObject(def)) {
	        components[key] = Vue.extend(def);
	      }
	    }
	  }
	}
	
	/**
	 * Ensure all props option syntax are normalized into the
	 * Object-based format.
	 *
	 * @param {Object} options
	 */
	
	function guardProps(options) {
	  var props = options.props;
	  var i, val;
	  if (isArray(props)) {
	    options.props = {};
	    i = props.length;
	    while (i--) {
	      val = props[i];
	      if (typeof val === 'string') {
	        options.props[val] = null;
	      } else if (val.name) {
	        options.props[val.name] = val;
	      }
	    }
	  } else if (isPlainObject(props)) {
	    var keys = Object.keys(props);
	    i = keys.length;
	    while (i--) {
	      val = props[keys[i]];
	      if (typeof val === 'function') {
	        props[keys[i]] = { type: val };
	      }
	    }
	  }
	}
	
	/**
	 * Guard an Array-format assets option and converted it
	 * into the key-value Object format.
	 *
	 * @param {Object|Array} assets
	 * @return {Object}
	 */
	
	function guardArrayAssets(assets) {
	  if (isArray(assets)) {
	    var res = {};
	    var i = assets.length;
	    var asset;
	    while (i--) {
	      asset = assets[i];
	      var id = typeof asset === 'function' ? asset.options && asset.options.name || asset.id : asset.name || asset.id;
	      if (!id) {
	        process.env.NODE_ENV !== 'production' && warn('Array-syntax assets must provide a "name" or "id" field.');
	      } else {
	        res[id] = asset;
	      }
	    }
	    return res;
	  }
	  return assets;
	}
	
	/**
	 * Merge two option objects into a new one.
	 * Core utility used in both instantiation and inheritance.
	 *
	 * @param {Object} parent
	 * @param {Object} child
	 * @param {Vue} [vm] - if vm is present, indicates this is
	 *                     an instantiation merge.
	 */
	
	function mergeOptions(parent, child, vm) {
	  guardComponents(child);
	  guardProps(child);
	  if (process.env.NODE_ENV !== 'production') {
	    if (child.propsData && !vm) {
	      warn('propsData can only be used as an instantiation option.');
	    }
	  }
	  var options = {};
	  var key;
	  if (child['extends']) {
	    parent = typeof child['extends'] === 'function' ? mergeOptions(parent, child['extends'].options, vm) : mergeOptions(parent, child['extends'], vm);
	  }
	  if (child.mixins) {
	    for (var i = 0, l = child.mixins.length; i < l; i++) {
	      var mixin = child.mixins[i];
	      var mixinOptions = mixin.prototype instanceof Vue ? mixin.options : mixin;
	      parent = mergeOptions(parent, mixinOptions, vm);
	    }
	  }
	  for (key in parent) {
	    mergeField(key);
	  }
	  for (key in child) {
	    if (!hasOwn(parent, key)) {
	      mergeField(key);
	    }
	  }
	  function mergeField(key) {
	    var strat = strats[key] || defaultStrat;
	    options[key] = strat(parent[key], child[key], vm, key);
	  }
	  return options;
	}
	
	/**
	 * Resolve an asset.
	 * This function is used because child instances need access
	 * to assets defined in its ancestor chain.
	 *
	 * @param {Object} options
	 * @param {String} type
	 * @param {String} id
	 * @param {Boolean} warnMissing
	 * @return {Object|Function}
	 */
	
	function resolveAsset(options, type, id, warnMissing) {
	  /* istanbul ignore if */
	  if (typeof id !== 'string') {
	    return;
	  }
	  var assets = options[type];
	  var camelizedId;
	  var res = assets[id] ||
	  // camelCase ID
	  assets[camelizedId = camelize(id)] ||
	  // Pascal Case ID
	  assets[camelizedId.charAt(0).toUpperCase() + camelizedId.slice(1)];
	  if (process.env.NODE_ENV !== 'production' && warnMissing && !res) {
	    warn('Failed to resolve ' + type.slice(0, -1) + ': ' + id, options);
	  }
	  return res;
	}
	
	var uid$1 = 0;
	
	/**
	 * A dep is an observable that can have multiple
	 * directives subscribing to it.
	 *
	 * @constructor
	 */
	function Dep() {
	  this.id = uid$1++;
	  this.subs = [];
	}
	
	// the current target watcher being evaluated.
	// this is globally unique because there could be only one
	// watcher being evaluated at any time.
	Dep.target = null;
	
	/**
	 * Add a directive subscriber.
	 *
	 * @param {Directive} sub
	 */
	
	Dep.prototype.addSub = function (sub) {
	  this.subs.push(sub);
	};
	
	/**
	 * Remove a directive subscriber.
	 *
	 * @param {Directive} sub
	 */
	
	Dep.prototype.removeSub = function (sub) {
	  this.subs.$remove(sub);
	};
	
	/**
	 * Add self as a dependency to the target watcher.
	 */
	
	Dep.prototype.depend = function () {
	  Dep.target.addDep(this);
	};
	
	/**
	 * Notify all subscribers of a new value.
	 */
	
	Dep.prototype.notify = function () {
	  // stablize the subscriber list first
	  var subs = toArray(this.subs);
	  for (var i = 0, l = subs.length; i < l; i++) {
	    subs[i].update();
	  }
	};
	
	var arrayProto = Array.prototype;
	var arrayMethods = Object.create(arrayProto)
	
	/**
	 * Intercept mutating methods and emit events
	 */
	
	;['push', 'pop', 'shift', 'unshift', 'splice', 'sort', 'reverse'].forEach(function (method) {
	  // cache original method
	  var original = arrayProto[method];
	  def(arrayMethods, method, function mutator() {
	    // avoid leaking arguments:
	    // http://jsperf.com/closure-with-arguments
	    var i = arguments.length;
	    var args = new Array(i);
	    while (i--) {
	      args[i] = arguments[i];
	    }
	    var result = original.apply(this, args);
	    var ob = this.__ob__;
	    var inserted;
	    switch (method) {
	      case 'push':
	        inserted = args;
	        break;
	      case 'unshift':
	        inserted = args;
	        break;
	      case 'splice':
	        inserted = args.slice(2);
	        break;
	    }
	    if (inserted) ob.observeArray(inserted);
	    // notify change
	    ob.dep.notify();
	    return result;
	  });
	});
	
	/**
	 * Swap the element at the given index with a new value
	 * and emits corresponding event.
	 *
	 * @param {Number} index
	 * @param {*} val
	 * @return {*} - replaced element
	 */
	
	def(arrayProto, '$set', function $set(index, val) {
	  if (index >= this.length) {
	    this.length = Number(index) + 1;
	  }
	  return this.splice(index, 1, val)[0];
	});
	
	/**
	 * Convenience method to remove the element at given index or target element reference.
	 *
	 * @param {*} item
	 */
	
	def(arrayProto, '$remove', function $remove(item) {
	  /* istanbul ignore if */
	  if (!this.length) return;
	  var index = indexOf(this, item);
	  if (index > -1) {
	    return this.splice(index, 1);
	  }
	});
	
	var arrayKeys = Object.getOwnPropertyNames(arrayMethods);
	
	/**
	 * By default, when a reactive property is set, the new value is
	 * also converted to become reactive. However in certain cases, e.g.
	 * v-for scope alias and props, we don't want to force conversion
	 * because the value may be a nested value under a frozen data structure.
	 *
	 * So whenever we want to set a reactive property without forcing
	 * conversion on the new value, we wrap that call inside this function.
	 */
	
	var shouldConvert = true;
	
	function withoutConversion(fn) {
	  shouldConvert = false;
	  fn();
	  shouldConvert = true;
	}
	
	/**
	 * Observer class that are attached to each observed
	 * object. Once attached, the observer converts target
	 * object's property keys into getter/setters that
	 * collect dependencies and dispatches updates.
	 *
	 * @param {Array|Object} value
	 * @constructor
	 */
	
	function Observer(value) {
	  this.value = value;
	  this.dep = new Dep();
	  def(value, '__ob__', this);
	  if (isArray(value)) {
	    var augment = hasProto ? protoAugment : copyAugment;
	    augment(value, arrayMethods, arrayKeys);
	    this.observeArray(value);
	  } else {
	    this.walk(value);
	  }
	}
	
	// Instance methods
	
	/**
	 * Walk through each property and convert them into
	 * getter/setters. This method should only be called when
	 * value type is Object.
	 *
	 * @param {Object} obj
	 */
	
	Observer.prototype.walk = function (obj) {
	  var keys = Object.keys(obj);
	  for (var i = 0, l = keys.length; i < l; i++) {
	    this.convert(keys[i], obj[keys[i]]);
	  }
	};
	
	/**
	 * Observe a list of Array items.
	 *
	 * @param {Array} items
	 */
	
	Observer.prototype.observeArray = function (items) {
	  for (var i = 0, l = items.length; i < l; i++) {
	    observe(items[i]);
	  }
	};
	
	/**
	 * Convert a property into getter/setter so we can emit
	 * the events when the property is accessed/changed.
	 *
	 * @param {String} key
	 * @param {*} val
	 */
	
	Observer.prototype.convert = function (key, val) {
	  defineReactive(this.value, key, val);
	};
	
	/**
	 * Add an owner vm, so that when $set/$delete mutations
	 * happen we can notify owner vms to proxy the keys and
	 * digest the watchers. This is only called when the object
	 * is observed as an instance's root $data.
	 *
	 * @param {Vue} vm
	 */
	
	Observer.prototype.addVm = function (vm) {
	  (this.vms || (this.vms = [])).push(vm);
	};
	
	/**
	 * Remove an owner vm. This is called when the object is
	 * swapped out as an instance's $data object.
	 *
	 * @param {Vue} vm
	 */
	
	Observer.prototype.removeVm = function (vm) {
	  this.vms.$remove(vm);
	};
	
	// helpers
	
	/**
	 * Augment an target Object or Array by intercepting
	 * the prototype chain using __proto__
	 *
	 * @param {Object|Array} target
	 * @param {Object} src
	 */
	
	function protoAugment(target, src) {
	  /* eslint-disable no-proto */
	  target.__proto__ = src;
	  /* eslint-enable no-proto */
	}
	
	/**
	 * Augment an target Object or Array by defining
	 * hidden properties.
	 *
	 * @param {Object|Array} target
	 * @param {Object} proto
	 */
	
	function copyAugment(target, src, keys) {
	  for (var i = 0, l = keys.length; i < l; i++) {
	    var key = keys[i];
	    def(target, key, src[key]);
	  }
	}
	
	/**
	 * Attempt to create an observer instance for a value,
	 * returns the new observer if successfully observed,
	 * or the existing observer if the value already has one.
	 *
	 * @param {*} value
	 * @param {Vue} [vm]
	 * @return {Observer|undefined}
	 * @static
	 */
	
	function observe(value, vm) {
	  if (!value || typeof value !== 'object') {
	    return;
	  }
	  var ob;
	  if (hasOwn(value, '__ob__') && value.__ob__ instanceof Observer) {
	    ob = value.__ob__;
	  } else if (shouldConvert && (isArray(value) || isPlainObject(value)) && Object.isExtensible(value) && !value._isVue) {
	    ob = new Observer(value);
	  }
	  if (ob && vm) {
	    ob.addVm(vm);
	  }
	  return ob;
	}
	
	/**
	 * Define a reactive property on an Object.
	 *
	 * @param {Object} obj
	 * @param {String} key
	 * @param {*} val
	 */
	
	function defineReactive(obj, key, val) {
	  var dep = new Dep();
	
	  var property = Object.getOwnPropertyDescriptor(obj, key);
	  if (property && property.configurable === false) {
	    return;
	  }
	
	  // cater for pre-defined getter/setters
	  var getter = property && property.get;
	  var setter = property && property.set;
	
	  var childOb = observe(val);
	  Object.defineProperty(obj, key, {
	    enumerable: true,
	    configurable: true,
	    get: function reactiveGetter() {
	      var value = getter ? getter.call(obj) : val;
	      if (Dep.target) {
	        dep.depend();
	        if (childOb) {
	          childOb.dep.depend();
	        }
	        if (isArray(value)) {
	          for (var e, i = 0, l = value.length; i < l; i++) {
	            e = value[i];
	            e && e.__ob__ && e.__ob__.dep.depend();
	          }
	        }
	      }
	      return value;
	    },
	    set: function reactiveSetter(newVal) {
	      var value = getter ? getter.call(obj) : val;
	      if (newVal === value) {
	        return;
	      }
	      if (setter) {
	        setter.call(obj, newVal);
	      } else {
	        val = newVal;
	      }
	      childOb = observe(newVal);
	      dep.notify();
	    }
	  });
	}
	
	
	
	var util = Object.freeze({
		defineReactive: defineReactive,
		set: set,
		del: del,
		hasOwn: hasOwn,
		isLiteral: isLiteral,
		isReserved: isReserved,
		_toString: _toString,
		toNumber: toNumber,
		toBoolean: toBoolean,
		stripQuotes: stripQuotes,
		camelize: camelize,
		hyphenate: hyphenate,
		classify: classify,
		bind: bind,
		toArray: toArray,
		extend: extend,
		isObject: isObject,
		isPlainObject: isPlainObject,
		def: def,
		debounce: _debounce,
		indexOf: indexOf,
		cancellable: cancellable,
		looseEqual: looseEqual,
		isArray: isArray,
		hasProto: hasProto,
		inBrowser: inBrowser,
		devtools: devtools,
		isIE: isIE,
		isIE9: isIE9,
		isAndroid: isAndroid,
		isIos: isIos,
		iosVersionMatch: iosVersionMatch,
		iosVersion: iosVersion,
		hasMutationObserverBug: hasMutationObserverBug,
		get transitionProp () { return transitionProp; },
		get transitionEndEvent () { return transitionEndEvent; },
		get animationProp () { return animationProp; },
		get animationEndEvent () { return animationEndEvent; },
		nextTick: nextTick,
		get _Set () { return _Set; },
		query: query,
		inDoc: inDoc,
		getAttr: getAttr,
		getBindAttr: getBindAttr,
		hasBindAttr: hasBindAttr,
		before: before,
		after: after,
		remove: remove,
		prepend: prepend,
		replace: replace,
		on: on,
		off: off,
		setClass: setClass,
		addClass: addClass,
		removeClass: removeClass,
		extractContent: extractContent,
		trimNode: trimNode,
		isTemplate: isTemplate,
		createAnchor: createAnchor,
		findRef: findRef,
		mapNodeRange: mapNodeRange,
		removeNodeRange: removeNodeRange,
		isFragment: isFragment,
		getOuterHTML: getOuterHTML,
		mergeOptions: mergeOptions,
		resolveAsset: resolveAsset,
		checkComponentAttr: checkComponentAttr,
		commonTagRE: commonTagRE,
		reservedTagRE: reservedTagRE,
		get warn () { return warn; }
	});
	
	var uid = 0;
	
	function initMixin (Vue) {
	  /**
	   * The main init sequence. This is called for every
	   * instance, including ones that are created from extended
	   * constructors.
	   *
	   * @param {Object} options - this options object should be
	   *                           the result of merging class
	   *                           options and the options passed
	   *                           in to the constructor.
	   */
	
	  Vue.prototype._init = function (options) {
	    options = options || {};
	
	    this.$el = null;
	    this.$parent = options.parent;
	    this.$root = this.$parent ? this.$parent.$root : this;
	    this.$children = [];
	    this.$refs = {}; // child vm references
	    this.$els = {}; // element references
	    this._watchers = []; // all watchers as an array
	    this._directives = []; // all directives
	
	    // a uid
	    this._uid = uid++;
	
	    // a flag to avoid this being observed
	    this._isVue = true;
	
	    // events bookkeeping
	    this._events = {}; // registered callbacks
	    this._eventsCount = {}; // for $broadcast optimization
	
	    // fragment instance properties
	    this._isFragment = false;
	    this._fragment = // @type {DocumentFragment}
	    this._fragmentStart = // @type {Text|Comment}
	    this._fragmentEnd = null; // @type {Text|Comment}
	
	    // lifecycle state
	    this._isCompiled = this._isDestroyed = this._isReady = this._isAttached = this._isBeingDestroyed = this._vForRemoving = false;
	    this._unlinkFn = null;
	
	    // context:
	    // if this is a transcluded component, context
	    // will be the common parent vm of this instance
	    // and its host.
	    this._context = options._context || this.$parent;
	
	    // scope:
	    // if this is inside an inline v-for, the scope
	    // will be the intermediate scope created for this
	    // repeat fragment. this is used for linking props
	    // and container directives.
	    this._scope = options._scope;
	
	    // fragment:
	    // if this instance is compiled inside a Fragment, it
	    // needs to reigster itself as a child of that fragment
	    // for attach/detach to work properly.
	    this._frag = options._frag;
	    if (this._frag) {
	      this._frag.children.push(this);
	    }
	
	    // push self into parent / transclusion host
	    if (this.$parent) {
	      this.$parent.$children.push(this);
	    }
	
	    // merge options.
	    options = this.$options = mergeOptions(this.constructor.options, options, this);
	
	    // set ref
	    this._updateRef();
	
	    // initialize data as empty object.
	    // it will be filled up in _initData().
	    this._data = {};
	
	    // call init hook
	    this._callHook('init');
	
	    // initialize data observation and scope inheritance.
	    this._initState();
	
	    // setup event system and option events.
	    this._initEvents();
	
	    // call created hook
	    this._callHook('created');
	
	    // if `el` option is passed, start compilation.
	    if (options.el) {
	      this.$mount(options.el);
	    }
	  };
	}
	
	var pathCache = new Cache(1000);
	
	// actions
	var APPEND = 0;
	var PUSH = 1;
	var INC_SUB_PATH_DEPTH = 2;
	var PUSH_SUB_PATH = 3;
	
	// states
	var BEFORE_PATH = 0;
	var IN_PATH = 1;
	var BEFORE_IDENT = 2;
	var IN_IDENT = 3;
	var IN_SUB_PATH = 4;
	var IN_SINGLE_QUOTE = 5;
	var IN_DOUBLE_QUOTE = 6;
	var AFTER_PATH = 7;
	var ERROR = 8;
	
	var pathStateMachine = [];
	
	pathStateMachine[BEFORE_PATH] = {
	  'ws': [BEFORE_PATH],
	  'ident': [IN_IDENT, APPEND],
	  '[': [IN_SUB_PATH],
	  'eof': [AFTER_PATH]
	};
	
	pathStateMachine[IN_PATH] = {
	  'ws': [IN_PATH],
	  '.': [BEFORE_IDENT],
	  '[': [IN_SUB_PATH],
	  'eof': [AFTER_PATH]
	};
	
	pathStateMachine[BEFORE_IDENT] = {
	  'ws': [BEFORE_IDENT],
	  'ident': [IN_IDENT, APPEND]
	};
	
	pathStateMachine[IN_IDENT] = {
	  'ident': [IN_IDENT, APPEND],
	  '0': [IN_IDENT, APPEND],
	  'number': [IN_IDENT, APPEND],
	  'ws': [IN_PATH, PUSH],
	  '.': [BEFORE_IDENT, PUSH],
	  '[': [IN_SUB_PATH, PUSH],
	  'eof': [AFTER_PATH, PUSH]
	};
	
	pathStateMachine[IN_SUB_PATH] = {
	  "'": [IN_SINGLE_QUOTE, APPEND],
	  '"': [IN_DOUBLE_QUOTE, APPEND],
	  '[': [IN_SUB_PATH, INC_SUB_PATH_DEPTH],
	  ']': [IN_PATH, PUSH_SUB_PATH],
	  'eof': ERROR,
	  'else': [IN_SUB_PATH, APPEND]
	};
	
	pathStateMachine[IN_SINGLE_QUOTE] = {
	  "'": [IN_SUB_PATH, APPEND],
	  'eof': ERROR,
	  'else': [IN_SINGLE_QUOTE, APPEND]
	};
	
	pathStateMachine[IN_DOUBLE_QUOTE] = {
	  '"': [IN_SUB_PATH, APPEND],
	  'eof': ERROR,
	  'else': [IN_DOUBLE_QUOTE, APPEND]
	};
	
	/**
	 * Determine the type of a character in a keypath.
	 *
	 * @param {Char} ch
	 * @return {String} type
	 */
	
	function getPathCharType(ch) {
	  if (ch === undefined) {
	    return 'eof';
	  }
	
	  var code = ch.charCodeAt(0);
	
	  switch (code) {
	    case 0x5B: // [
	    case 0x5D: // ]
	    case 0x2E: // .
	    case 0x22: // "
	    case 0x27: // '
	    case 0x30:
	      // 0
	      return ch;
	
	    case 0x5F: // _
	    case 0x24:
	      // $
	      return 'ident';
	
	    case 0x20: // Space
	    case 0x09: // Tab
	    case 0x0A: // Newline
	    case 0x0D: // Return
	    case 0xA0: // No-break space
	    case 0xFEFF: // Byte Order Mark
	    case 0x2028: // Line Separator
	    case 0x2029:
	      // Paragraph Separator
	      return 'ws';
	  }
	
	  // a-z, A-Z
	  if (code >= 0x61 && code <= 0x7A || code >= 0x41 && code <= 0x5A) {
	    return 'ident';
	  }
	
	  // 1-9
	  if (code >= 0x31 && code <= 0x39) {
	    return 'number';
	  }
	
	  return 'else';
	}
	
	/**
	 * Format a subPath, return its plain form if it is
	 * a literal string or number. Otherwise prepend the
	 * dynamic indicator (*).
	 *
	 * @param {String} path
	 * @return {String}
	 */
	
	function formatSubPath(path) {
	  var trimmed = path.trim();
	  // invalid leading 0
	  if (path.charAt(0) === '0' && isNaN(path)) {
	    return false;
	  }
	  return isLiteral(trimmed) ? stripQuotes(trimmed) : '*' + trimmed;
	}
	
	/**
	 * Parse a string path into an array of segments
	 *
	 * @param {String} path
	 * @return {Array|undefined}
	 */
	
	function parse(path) {
	  var keys = [];
	  var index = -1;
	  var mode = BEFORE_PATH;
	  var subPathDepth = 0;
	  var c, newChar, key, type, transition, action, typeMap;
	
	  var actions = [];
	
	  actions[PUSH] = function () {
	    if (key !== undefined) {
	      keys.push(key);
	      key = undefined;
	    }
	  };
	
	  actions[APPEND] = function () {
	    if (key === undefined) {
	      key = newChar;
	    } else {
	      key += newChar;
	    }
	  };
	
	  actions[INC_SUB_PATH_DEPTH] = function () {
	    actions[APPEND]();
	    subPathDepth++;
	  };
	
	  actions[PUSH_SUB_PATH] = function () {
	    if (subPathDepth > 0) {
	      subPathDepth--;
	      mode = IN_SUB_PATH;
	      actions[APPEND]();
	    } else {
	      subPathDepth = 0;
	      key = formatSubPath(key);
	      if (key === false) {
	        return false;
	      } else {
	        actions[PUSH]();
	      }
	    }
	  };
	
	  function maybeUnescapeQuote() {
	    var nextChar = path[index + 1];
	    if (mode === IN_SINGLE_QUOTE && nextChar === "'" || mode === IN_DOUBLE_QUOTE && nextChar === '"') {
	      index++;
	      newChar = '\\' + nextChar;
	      actions[APPEND]();
	      return true;
	    }
	  }
	
	  while (mode != null) {
	    index++;
	    c = path[index];
	
	    if (c === '\\' && maybeUnescapeQuote()) {
	      continue;
	    }
	
	    type = getPathCharType(c);
	    typeMap = pathStateMachine[mode];
	    transition = typeMap[type] || typeMap['else'] || ERROR;
	
	    if (transition === ERROR) {
	      return; // parse error
	    }
	
	    mode = transition[0];
	    action = actions[transition[1]];
	    if (action) {
	      newChar = transition[2];
	      newChar = newChar === undefined ? c : newChar;
	      if (action() === false) {
	        return;
	      }
	    }
	
	    if (mode === AFTER_PATH) {
	      keys.raw = path;
	      return keys;
	    }
	  }
	}
	
	/**
	 * External parse that check for a cache hit first
	 *
	 * @param {String} path
	 * @return {Array|undefined}
	 */
	
	function parsePath(path) {
	  var hit = pathCache.get(path);
	  if (!hit) {
	    hit = parse(path);
	    if (hit) {
	      pathCache.put(path, hit);
	    }
	  }
	  return hit;
	}
	
	/**
	 * Get from an object from a path string
	 *
	 * @param {Object} obj
	 * @param {String} path
	 */
	
	function getPath(obj, path) {
	  return parseExpression(path).get(obj);
	}
	
	/**
	 * Warn against setting non-existent root path on a vm.
	 */
	
	var warnNonExistent;
	if (process.env.NODE_ENV !== 'production') {
	  warnNonExistent = function (path, vm) {
	    warn('You are setting a non-existent path "' + path.raw + '" ' + 'on a vm instance. Consider pre-initializing the property ' + 'with the "data" option for more reliable reactivity ' + 'and better performance.', vm);
	  };
	}
	
	/**
	 * Set on an object from a path
	 *
	 * @param {Object} obj
	 * @param {String | Array} path
	 * @param {*} val
	 */
	
	function setPath(obj, path, val) {
	  var original = obj;
	  if (typeof path === 'string') {
	    path = parse(path);
	  }
	  if (!path || !isObject(obj)) {
	    return false;
	  }
	  var last, key;
	  for (var i = 0, l = path.length; i < l; i++) {
	    last = obj;
	    key = path[i];
	    if (key.charAt(0) === '*') {
	      key = parseExpression(key.slice(1)).get.call(original, original);
	    }
	    if (i < l - 1) {
	      obj = obj[key];
	      if (!isObject(obj)) {
	        obj = {};
	        if (process.env.NODE_ENV !== 'production' && last._isVue) {
	          warnNonExistent(path, last);
	        }
	        set(last, key, obj);
	      }
	    } else {
	      if (isArray(obj)) {
	        obj.$set(key, val);
	      } else if (key in obj) {
	        obj[key] = val;
	      } else {
	        if (process.env.NODE_ENV !== 'production' && obj._isVue) {
	          warnNonExistent(path, obj);
	        }
	        set(obj, key, val);
	      }
	    }
	  }
	  return true;
	}
	
	var path = Object.freeze({
	  parsePath: parsePath,
	  getPath: getPath,
	  setPath: setPath
	});
	
	var expressionCache = new Cache(1000);
	
	var allowedKeywords = 'Math,Date,this,true,false,null,undefined,Infinity,NaN,' + 'isNaN,isFinite,decodeURI,decodeURIComponent,encodeURI,' + 'encodeURIComponent,parseInt,parseFloat';
	var allowedKeywordsRE = new RegExp('^(' + allowedKeywords.replace(/,/g, '\\b|') + '\\b)');
	
	// keywords that don't make sense inside expressions
	var improperKeywords = 'break,case,class,catch,const,continue,debugger,default,' + 'delete,do,else,export,extends,finally,for,function,if,' + 'import,in,instanceof,let,return,super,switch,throw,try,' + 'var,while,with,yield,enum,await,implements,package,' + 'protected,static,interface,private,public';
	var improperKeywordsRE = new RegExp('^(' + improperKeywords.replace(/,/g, '\\b|') + '\\b)');
	
	var wsRE = /\s/g;
	var newlineRE = /\n/g;
	var saveRE = /[\{,]\s*[\w\$_]+\s*:|('(?:[^'\\]|\\.)*'|"(?:[^"\\]|\\.)*"|`(?:[^`\\]|\\.)*\$\{|\}(?:[^`\\]|\\.)*`|`(?:[^`\\]|\\.)*`)|new |typeof |void /g;
	var restoreRE = /"(\d+)"/g;
	var pathTestRE = /^[A-Za-z_$][\w$]*(?:\.[A-Za-z_$][\w$]*|\['.*?'\]|\[".*?"\]|\[\d+\]|\[[A-Za-z_$][\w$]*\])*$/;
	var identRE = /[^\w$\.](?:[A-Za-z_$][\w$]*)/g;
	var literalValueRE$1 = /^(?:true|false|null|undefined|Infinity|NaN)$/;
	
	function noop() {}
	
	/**
	 * Save / Rewrite / Restore
	 *
	 * When rewriting paths found in an expression, it is
	 * possible for the same letter sequences to be found in
	 * strings and Object literal property keys. Therefore we
	 * remove and store these parts in a temporary array, and
	 * restore them after the path rewrite.
	 */
	
	var saved = [];
	
	/**
	 * Save replacer
	 *
	 * The save regex can match two possible cases:
	 * 1. An opening object literal
	 * 2. A string
	 * If matched as a plain string, we need to escape its
	 * newlines, since the string needs to be preserved when
	 * generating the function body.
	 *
	 * @param {String} str
	 * @param {String} isString - str if matched as a string
	 * @return {String} - placeholder with index
	 */
	
	function save(str, isString) {
	  var i = saved.length;
	  saved[i] = isString ? str.replace(newlineRE, '\\n') : str;
	  return '"' + i + '"';
	}
	
	/**
	 * Path rewrite replacer
	 *
	 * @param {String} raw
	 * @return {String}
	 */
	
	function rewrite(raw) {
	  var c = raw.charAt(0);
	  var path = raw.slice(1);
	  if (allowedKeywordsRE.test(path)) {
	    return raw;
	  } else {
	    path = path.indexOf('"') > -1 ? path.replace(restoreRE, restore) : path;
	    return c + 'scope.' + path;
	  }
	}
	
	/**
	 * Restore replacer
	 *
	 * @param {String} str
	 * @param {String} i - matched save index
	 * @return {String}
	 */
	
	function restore(str, i) {
	  return saved[i];
	}
	
	/**
	 * Rewrite an expression, prefixing all path accessors with
	 * `scope.` and generate getter/setter functions.
	 *
	 * @param {String} exp
	 * @return {Function}
	 */
	
	function compileGetter(exp) {
	  if (improperKeywordsRE.test(exp)) {
	    process.env.NODE_ENV !== 'production' && warn('Avoid using reserved keywords in expression: ' + exp);
	  }
	  // reset state
	  saved.length = 0;
	  // save strings and object literal keys
	  var body = exp.replace(saveRE, save).replace(wsRE, '');
	  // rewrite all paths
	  // pad 1 space here because the regex matches 1 extra char
	  body = (' ' + body).replace(identRE, rewrite).replace(restoreRE, restore);
	  return makeGetterFn(body);
	}
	
	/**
	 * Build a getter function. Requires eval.
	 *
	 * We isolate the try/catch so it doesn't affect the
	 * optimization of the parse function when it is not called.
	 *
	 * @param {String} body
	 * @return {Function|undefined}
	 */
	
	function makeGetterFn(body) {
	  try {
	    /* eslint-disable no-new-func */
	    return new Function('scope', 'return ' + body + ';');
	    /* eslint-enable no-new-func */
	  } catch (e) {
	    if (process.env.NODE_ENV !== 'production') {
	      /* istanbul ignore if */
	      if (e.toString().match(/unsafe-eval|CSP/)) {
	        warn('It seems you are using the default build of Vue.js in an environment ' + 'with Content Security Policy that prohibits unsafe-eval. ' + 'Use the CSP-compliant build instead: ' + 'http://vuejs.org/guide/installation.html#CSP-compliant-build');
	      } else {
	        warn('Invalid expression. ' + 'Generated function body: ' + body);
	      }
	    }
	    return noop;
	  }
	}
	
	/**
	 * Compile a setter function for the expression.
	 *
	 * @param {String} exp
	 * @return {Function|undefined}
	 */
	
	function compileSetter(exp) {
	  var path = parsePath(exp);
	  if (path) {
	    return function (scope, val) {
	      setPath(scope, path, val);
	    };
	  } else {
	    process.env.NODE_ENV !== 'production' && warn('Invalid setter expression: ' + exp);
	  }
	}
	
	/**
	 * Parse an expression into re-written getter/setters.
	 *
	 * @param {String} exp
	 * @param {Boolean} needSet
	 * @return {Function}
	 */
	
	function parseExpression(exp, needSet) {
	  exp = exp.trim();
	  // try cache
	  var hit = expressionCache.get(exp);
	  if (hit) {
	    if (needSet && !hit.set) {
	      hit.set = compileSetter(hit.exp);
	    }
	    return hit;
	  }
	  var res = { exp: exp };
	  res.get = isSimplePath(exp) && exp.indexOf('[') < 0
	  // optimized super simple getter
	  ? makeGetterFn('scope.' + exp)
	  // dynamic getter
	  : compileGetter(exp);
	  if (needSet) {
	    res.set = compileSetter(exp);
	  }
	  expressionCache.put(exp, res);
	  return res;
	}
	
	/**
	 * Check if an expression is a simple path.
	 *
	 * @param {String} exp
	 * @return {Boolean}
	 */
	
	function isSimplePath(exp) {
	  return pathTestRE.test(exp) &&
	  // don't treat literal values as paths
	  !literalValueRE$1.test(exp) &&
	  // Math constants e.g. Math.PI, Math.E etc.
	  exp.slice(0, 5) !== 'Math.';
	}
	
	var expression = Object.freeze({
	  parseExpression: parseExpression,
	  isSimplePath: isSimplePath
	});
	
	// we have two separate queues: one for directive updates
	// and one for user watcher registered via $watch().
	// we want to guarantee directive updates to be called
	// before user watchers so that when user watchers are
	// triggered, the DOM would have already been in updated
	// state.
	
	var queue = [];
	var userQueue = [];
	var has = {};
	var circular = {};
	var waiting = false;
	
	/**
	 * Reset the batcher's state.
	 */
	
	function resetBatcherState() {
	  queue.length = 0;
	  userQueue.length = 0;
	  has = {};
	  circular = {};
	  waiting = false;
	}
	
	/**
	 * Flush both queues and run the watchers.
	 */
	
	function flushBatcherQueue() {
	  var _again = true;
	
	  _function: while (_again) {
	    _again = false;
	
	    runBatcherQueue(queue);
	    runBatcherQueue(userQueue);
	    // user watchers triggered more watchers,
	    // keep flushing until it depletes
	    if (queue.length) {
	      _again = true;
	      continue _function;
	    }
	    // dev tool hook
	    /* istanbul ignore if */
	    if (devtools && config.devtools) {
	      devtools.emit('flush');
	    }
	    resetBatcherState();
	  }
	}
	
	/**
	 * Run the watchers in a single queue.
	 *
	 * @param {Array} queue
	 */
	
	function runBatcherQueue(queue) {
	  // do not cache length because more watchers might be pushed
	  // as we run existing watchers
	  for (var i = 0; i < queue.length; i++) {
	    var watcher = queue[i];
	    var id = watcher.id;
	    has[id] = null;
	    watcher.run();
	    // in dev build, check and stop circular updates.
	    if (process.env.NODE_ENV !== 'production' && has[id] != null) {
	      circular[id] = (circular[id] || 0) + 1;
	      if (circular[id] > config._maxUpdateCount) {
	        warn('You may have an infinite update loop for watcher ' + 'with expression "' + watcher.expression + '"', watcher.vm);
	        break;
	      }
	    }
	  }
	  queue.length = 0;
	}
	
	/**
	 * Push a watcher into the watcher queue.
	 * Jobs with duplicate IDs will be skipped unless it's
	 * pushed when the queue is being flushed.
	 *
	 * @param {Watcher} watcher
	 *   properties:
	 *   - {Number} id
	 *   - {Function} run
	 */
	
	function pushWatcher(watcher) {
	  var id = watcher.id;
	  if (has[id] == null) {
	    // push watcher into appropriate queue
	    var q = watcher.user ? userQueue : queue;
	    has[id] = q.length;
	    q.push(watcher);
	    // queue the flush
	    if (!waiting) {
	      waiting = true;
	      nextTick(flushBatcherQueue);
	    }
	  }
	}
	
	var uid$2 = 0;
	
	/**
	 * A watcher parses an expression, collects dependencies,
	 * and fires callback when the expression value changes.
	 * This is used for both the $watch() api and directives.
	 *
	 * @param {Vue} vm
	 * @param {String|Function} expOrFn
	 * @param {Function} cb
	 * @param {Object} options
	 *                 - {Array} filters
	 *                 - {Boolean} twoWay
	 *                 - {Boolean} deep
	 *                 - {Boolean} user
	 *                 - {Boolean} sync
	 *                 - {Boolean} lazy
	 *                 - {Function} [preProcess]
	 *                 - {Function} [postProcess]
	 * @constructor
	 */
	function Watcher(vm, expOrFn, cb, options) {
	  // mix in options
	  if (options) {
	    extend(this, options);
	  }
	  var isFn = typeof expOrFn === 'function';
	  this.vm = vm;
	  vm._watchers.push(this);
	  this.expression = expOrFn;
	  this.cb = cb;
	  this.id = ++uid$2; // uid for batching
	  this.active = true;
	  this.dirty = this.lazy; // for lazy watchers
	  this.deps = [];
	  this.newDeps = [];
	  this.depIds = new _Set();
	  this.newDepIds = new _Set();
	  this.prevError = null; // for async error stacks
	  // parse expression for getter/setter
	  if (isFn) {
	    this.getter = expOrFn;
	    this.setter = undefined;
	  } else {
	    var res = parseExpression(expOrFn, this.twoWay);
	    this.getter = res.get;
	    this.setter = res.set;
	  }
	  this.value = this.lazy ? undefined : this.get();
	  // state for avoiding false triggers for deep and Array
	  // watchers during vm._digest()
	  this.queued = this.shallow = false;
	}
	
	/**
	 * Evaluate the getter, and re-collect dependencies.
	 */
	
	Watcher.prototype.get = function () {
	  this.beforeGet();
	  var scope = this.scope || this.vm;
	  var value;
	  try {
	    value = this.getter.call(scope, scope);
	  } catch (e) {
	    if (process.env.NODE_ENV !== 'production' && config.warnExpressionErrors) {
	      warn('Error when evaluating expression ' + '"' + this.expression + '": ' + e.toString(), this.vm);
	    }
	  }
	  // "touch" every property so they are all tracked as
	  // dependencies for deep watching
	  if (this.deep) {
	    traverse(value);
	  }
	  if (this.preProcess) {
	    value = this.preProcess(value);
	  }
	  if (this.filters) {
	    value = scope._applyFilters(value, null, this.filters, false);
	  }
	  if (this.postProcess) {
	    value = this.postProcess(value);
	  }
	  this.afterGet();
	  return value;
	};
	
	/**
	 * Set the corresponding value with the setter.
	 *
	 * @param {*} value
	 */
	
	Watcher.prototype.set = function (value) {
	  var scope = this.scope || this.vm;
	  if (this.filters) {
	    value = scope._applyFilters(value, this.value, this.filters, true);
	  }
	  try {
	    this.setter.call(scope, scope, value);
	  } catch (e) {
	    if (process.env.NODE_ENV !== 'production' && config.warnExpressionErrors) {
	      warn('Error when evaluating setter ' + '"' + this.expression + '": ' + e.toString(), this.vm);
	    }
	  }
	  // two-way sync for v-for alias
	  var forContext = scope.$forContext;
	  if (forContext && forContext.alias === this.expression) {
	    if (forContext.filters) {
	      process.env.NODE_ENV !== 'production' && warn('It seems you are using two-way binding on ' + 'a v-for alias (' + this.expression + '), and the ' + 'v-for has filters. This will not work properly. ' + 'Either remove the filters or use an array of ' + 'objects and bind to object properties instead.', this.vm);
	      return;
	    }
	    forContext._withLock(function () {
	      if (scope.$key) {
	        // original is an object
	        forContext.rawValue[scope.$key] = value;
	      } else {
	        forContext.rawValue.$set(scope.$index, value);
	      }
	    });
	  }
	};
	
	/**
	 * Prepare for dependency collection.
	 */
	
	Watcher.prototype.beforeGet = function () {
	  Dep.target = this;
	};
	
	/**
	 * Add a dependency to this directive.
	 *
	 * @param {Dep} dep
	 */
	
	Watcher.prototype.addDep = function (dep) {
	  var id = dep.id;
	  if (!this.newDepIds.has(id)) {
	    this.newDepIds.add(id);
	    this.newDeps.push(dep);
	    if (!this.depIds.has(id)) {
	      dep.addSub(this);
	    }
	  }
	};
	
	/**
	 * Clean up for dependency collection.
	 */
	
	Watcher.prototype.afterGet = function () {
	  Dep.target = null;
	  var i = this.deps.length;
	  while (i--) {
	    var dep = this.deps[i];
	    if (!this.newDepIds.has(dep.id)) {
	      dep.removeSub(this);
	    }
	  }
	  var tmp = this.depIds;
	  this.depIds = this.newDepIds;
	  this.newDepIds = tmp;
	  this.newDepIds.clear();
	  tmp = this.deps;
	  this.deps = this.newDeps;
	  this.newDeps = tmp;
	  this.newDeps.length = 0;
	};
	
	/**
	 * Subscriber interface.
	 * Will be called when a dependency changes.
	 *
	 * @param {Boolean} shallow
	 */
	
	Watcher.prototype.update = function (shallow) {
	  if (this.lazy) {
	    this.dirty = true;
	  } else if (this.sync || !config.async) {
	    this.run();
	  } else {
	    // if queued, only overwrite shallow with non-shallow,
	    // but not the other way around.
	    this.shallow = this.queued ? shallow ? this.shallow : false : !!shallow;
	    this.queued = true;
	    // record before-push error stack in debug mode
	    /* istanbul ignore if */
	    if (process.env.NODE_ENV !== 'production' && config.debug) {
	      this.prevError = new Error('[vue] async stack trace');
	    }
	    pushWatcher(this);
	  }
	};
	
	/**
	 * Batcher job interface.
	 * Will be called by the batcher.
	 */
	
	Watcher.prototype.run = function () {
	  if (this.active) {
	    var value = this.get();
	    if (value !== this.value ||
	    // Deep watchers and watchers on Object/Arrays should fire even
	    // when the value is the same, because the value may
	    // have mutated; but only do so if this is a
	    // non-shallow update (caused by a vm digest).
	    (isObject(value) || this.deep) && !this.shallow) {
	      // set new value
	      var oldValue = this.value;
	      this.value = value;
	      // in debug + async mode, when a watcher callbacks
	      // throws, we also throw the saved before-push error
	      // so the full cross-tick stack trace is available.
	      var prevError = this.prevError;
	      /* istanbul ignore if */
	      if (process.env.NODE_ENV !== 'production' && config.debug && prevError) {
	        this.prevError = null;
	        try {
	          this.cb.call(this.vm, value, oldValue);
	        } catch (e) {
	          nextTick(function () {
	            throw prevError;
	          }, 0);
	          throw e;
	        }
	      } else {
	        this.cb.call(this.vm, value, oldValue);
	      }
	    }
	    this.queued = this.shallow = false;
	  }
	};
	
	/**
	 * Evaluate the value of the watcher.
	 * This only gets called for lazy watchers.
	 */
	
	Watcher.prototype.evaluate = function () {
	  // avoid overwriting another watcher that is being
	  // collected.
	  var current = Dep.target;
	  this.value = this.get();
	  this.dirty = false;
	  Dep.target = current;
	};
	
	/**
	 * Depend on all deps collected by this watcher.
	 */
	
	Watcher.prototype.depend = function () {
	  var i = this.deps.length;
	  while (i--) {
	    this.deps[i].depend();
	  }
	};
	
	/**
	 * Remove self from all dependencies' subcriber list.
	 */
	
	Watcher.prototype.teardown = function () {
	  if (this.active) {
	    // remove self from vm's watcher list
	    // this is a somewhat expensive operation so we skip it
	    // if the vm is being destroyed or is performing a v-for
	    // re-render (the watcher list is then filtered by v-for).
	    if (!this.vm._isBeingDestroyed && !this.vm._vForRemoving) {
	      this.vm._watchers.$remove(this);
	    }
	    var i = this.deps.length;
	    while (i--) {
	      this.deps[i].removeSub(this);
	    }
	    this.active = false;
	    this.vm = this.cb = this.value = null;
	  }
	};
	
	/**
	 * Recrusively traverse an object to evoke all converted
	 * getters, so that every nested property inside the object
	 * is collected as a "deep" dependency.
	 *
	 * @param {*} val
	 */
	
	var seenObjects = new _Set();
	function traverse(val, seen) {
	  var i = undefined,
	      keys = undefined;
	  if (!seen) {
	    seen = seenObjects;
	    seen.clear();
	  }
	  var isA = isArray(val);
	  var isO = isObject(val);
	  if ((isA || isO) && Object.isExtensible(val)) {
	    if (val.__ob__) {
	      var depId = val.__ob__.dep.id;
	      if (seen.has(depId)) {
	        return;
	      } else {
	        seen.add(depId);
	      }
	    }
	    if (isA) {
	      i = val.length;
	      while (i--) traverse(val[i], seen);
	    } else if (isO) {
	      keys = Object.keys(val);
	      i = keys.length;
	      while (i--) traverse(val[keys[i]], seen);
	    }
	  }
	}
	
	var text$1 = {
	
	  bind: function bind() {
	    this.attr = this.el.nodeType === 3 ? 'data' : 'textContent';
	  },
	
	  update: function update(value) {
	    this.el[this.attr] = _toString(value);
	  }
	};
	
	var templateCache = new Cache(1000);
	var idSelectorCache = new Cache(1000);
	
	var map = {
	  efault: [0, '', ''],
	  legend: [1, '<fieldset>', '</fieldset>'],
	  tr: [2, '<table><tbody>', '</tbody></table>'],
	  col: [2, '<table><tbody></tbody><colgroup>', '</colgroup></table>']
	};
	
	map.td = map.th = [3, '<table><tbody><tr>', '</tr></tbody></table>'];
	
	map.option = map.optgroup = [1, '<select multiple="multiple">', '</select>'];
	
	map.thead = map.tbody = map.colgroup = map.caption = map.tfoot = [1, '<table>', '</table>'];
	
	map.g = map.defs = map.symbol = map.use = map.image = map.text = map.circle = map.ellipse = map.line = map.path = map.polygon = map.polyline = map.rect = [1, '<svg ' + 'xmlns="http://www.w3.org/2000/svg" ' + 'xmlns:xlink="http://www.w3.org/1999/xlink" ' + 'xmlns:ev="http://www.w3.org/2001/xml-events"' + 'version="1.1">', '</svg>'];
	
	/**
	 * Check if a node is a supported template node with a
	 * DocumentFragment content.
	 *
	 * @param {Node} node
	 * @return {Boolean}
	 */
	
	function isRealTemplate(node) {
	  return isTemplate(node) && isFragment(node.content);
	}
	
	var tagRE$1 = /<([\w:-]+)/;
	var entityRE = /&#?\w+?;/;
	var commentRE = /<!--/;
	
	/**
	 * Convert a string template to a DocumentFragment.
	 * Determines correct wrapping by tag types. Wrapping
	 * strategy found in jQuery & component/domify.
	 *
	 * @param {String} templateString
	 * @param {Boolean} raw
	 * @return {DocumentFragment}
	 */
	
	function stringToFragment(templateString, raw) {
	  // try a cache hit first
	  var cacheKey = raw ? templateString : templateString.trim();
	  var hit = templateCache.get(cacheKey);
	  if (hit) {
	    return hit;
	  }
	
	  var frag = document.createDocumentFragment();
	  var tagMatch = templateString.match(tagRE$1);
	  var entityMatch = entityRE.test(templateString);
	  var commentMatch = commentRE.test(templateString);
	
	  if (!tagMatch && !entityMatch && !commentMatch) {
	    // text only, return a single text node.
	    frag.appendChild(document.createTextNode(templateString));
	  } else {
	    var tag = tagMatch && tagMatch[1];
	    var wrap = map[tag] || map.efault;
	    var depth = wrap[0];
	    var prefix = wrap[1];
	    var suffix = wrap[2];
	    var node = document.createElement('div');
	
	    node.innerHTML = prefix + templateString + suffix;
	    while (depth--) {
	      node = node.lastChild;
	    }
	
	    var child;
	    /* eslint-disable no-cond-assign */
	    while (child = node.firstChild) {
	      /* eslint-enable no-cond-assign */
	      frag.appendChild(child);
	    }
	  }
	  if (!raw) {
	    trimNode(frag);
	  }
	  templateCache.put(cacheKey, frag);
	  return frag;
	}
	
	/**
	 * Convert a template node to a DocumentFragment.
	 *
	 * @param {Node} node
	 * @return {DocumentFragment}
	 */
	
	function nodeToFragment(node) {
	  // if its a template tag and the browser supports it,
	  // its content is already a document fragment. However, iOS Safari has
	  // bug when using directly cloned template content with touch
	  // events and can cause crashes when the nodes are removed from DOM, so we
	  // have to treat template elements as string templates. (#2805)
	  /* istanbul ignore if */
	  if (isRealTemplate(node)) {
	    return stringToFragment(node.innerHTML);
	  }
	  // script template
	  if (node.tagName === 'SCRIPT') {
	    return stringToFragment(node.textContent);
	  }
	  // normal node, clone it to avoid mutating the original
	  var clonedNode = cloneNode(node);
	  var frag = document.createDocumentFragment();
	  var child;
	  /* eslint-disable no-cond-assign */
	  while (child = clonedNode.firstChild) {
	    /* eslint-enable no-cond-assign */
	    frag.appendChild(child);
	  }
	  trimNode(frag);
	  return frag;
	}
	
	// Test for the presence of the Safari template cloning bug
	// https://bugs.webkit.org/showug.cgi?id=137755
	var hasBrokenTemplate = (function () {
	  /* istanbul ignore else */
	  if (inBrowser) {
	    var a = document.createElement('div');
	    a.innerHTML = '<template>1</template>';
	    return !a.cloneNode(true).firstChild.innerHTML;
	  } else {
	    return false;
	  }
	})();
	
	// Test for IE10/11 textarea placeholder clone bug
	var hasTextareaCloneBug = (function () {
	  /* istanbul ignore else */
	  if (inBrowser) {
	    var t = document.createElement('textarea');
	    t.placeholder = 't';
	    return t.cloneNode(true).value === 't';
	  } else {
	    return false;
	  }
	})();
	
	/**
	 * 1. Deal with Safari cloning nested <template> bug by
	 *    manually cloning all template instances.
	 * 2. Deal with IE10/11 textarea placeholder bug by setting
	 *    the correct value after cloning.
	 *
	 * @param {Element|DocumentFragment} node
	 * @return {Element|DocumentFragment}
	 */
	
	function cloneNode(node) {
	  /* istanbul ignore if */
	  if (!node.querySelectorAll) {
	    return node.cloneNode();
	  }
	  var res = node.cloneNode(true);
	  var i, original, cloned;
	  /* istanbul ignore if */
	  if (hasBrokenTemplate) {
	    var tempClone = res;
	    if (isRealTemplate(node)) {
	      node = node.content;
	      tempClone = res.content;
	    }
	    original = node.querySelectorAll('template');
	    if (original.length) {
	      cloned = tempClone.querySelectorAll('template');
	      i = cloned.length;
	      while (i--) {
	        cloned[i].parentNode.replaceChild(cloneNode(original[i]), cloned[i]);
	      }
	    }
	  }
	  /* istanbul ignore if */
	  if (hasTextareaCloneBug) {
	    if (node.tagName === 'TEXTAREA') {
	      res.value = node.value;
	    } else {
	      original = node.querySelectorAll('textarea');
	      if (original.length) {
	        cloned = res.querySelectorAll('textarea');
	        i = cloned.length;
	        while (i--) {
	          cloned[i].value = original[i].value;
	        }
	      }
	    }
	  }
	  return res;
	}
	
	/**
	 * Process the template option and normalizes it into a
	 * a DocumentFragment that can be used as a partial or a
	 * instance template.
	 *
	 * @param {*} template
	 *        Possible values include:
	 *        - DocumentFragment object
	 *        - Node object of type Template
	 *        - id selector: '#some-template-id'
	 *        - template string: '<div><span>{{msg}}</span></div>'
	 * @param {Boolean} shouldClone
	 * @param {Boolean} raw
	 *        inline HTML interpolation. Do not check for id
	 *        selector and keep whitespace in the string.
	 * @return {DocumentFragment|undefined}
	 */
	
	function parseTemplate(template, shouldClone, raw) {
	  var node, frag;
	
	  // if the template is already a document fragment,
	  // do nothing
	  if (isFragment(template)) {
	    trimNode(template);
	    return shouldClone ? cloneNode(template) : template;
	  }
	
	  if (typeof template === 'string') {
	    // id selector
	    if (!raw && template.charAt(0) === '#') {
	      // id selector can be cached too
	      frag = idSelectorCache.get(template);
	      if (!frag) {
	        node = document.getElementById(template.slice(1));
	        if (node) {
	          frag = nodeToFragment(node);
	          // save selector to cache
	          idSelectorCache.put(template, frag);
	        }
	      }
	    } else {
	      // normal string template
	      frag = stringToFragment(template, raw);
	    }
	  } else if (template.nodeType) {
	    // a direct node
	    frag = nodeToFragment(template);
	  }
	
	  return frag && shouldClone ? cloneNode(frag) : frag;
	}
	
	var template = Object.freeze({
	  cloneNode: cloneNode,
	  parseTemplate: parseTemplate
	});
	
	var html = {
	
	  bind: function bind() {
	    // a comment node means this is a binding for
	    // {{{ inline unescaped html }}}
	    if (this.el.nodeType === 8) {
	      // hold nodes
	      this.nodes = [];
	      // replace the placeholder with proper anchor
	      this.anchor = createAnchor('v-html');
	      replace(this.el, this.anchor);
	    }
	  },
	
	  update: function update(value) {
	    value = _toString(value);
	    if (this.nodes) {
	      this.swap(value);
	    } else {
	      this.el.innerHTML = value;
	    }
	  },
	
	  swap: function swap(value) {
	    // remove old nodes
	    var i = this.nodes.length;
	    while (i--) {
	      remove(this.nodes[i]);
	    }
	    // convert new value to a fragment
	    // do not attempt to retrieve from id selector
	    var frag = parseTemplate(value, true, true);
	    // save a reference to these nodes so we can remove later
	    this.nodes = toArray(frag.childNodes);
	    before(frag, this.anchor);
	  }
	};
	
	/**
	 * Abstraction for a partially-compiled fragment.
	 * Can optionally compile content with a child scope.
	 *
	 * @param {Function} linker
	 * @param {Vue} vm
	 * @param {DocumentFragment} frag
	 * @param {Vue} [host]
	 * @param {Object} [scope]
	 * @param {Fragment} [parentFrag]
	 */
	function Fragment(linker, vm, frag, host, scope, parentFrag) {
	  this.children = [];
	  this.childFrags = [];
	  this.vm = vm;
	  this.scope = scope;
	  this.inserted = false;
	  this.parentFrag = parentFrag;
	  if (parentFrag) {
	    parentFrag.childFrags.push(this);
	  }
	  this.unlink = linker(vm, frag, host, scope, this);
	  var single = this.single = frag.childNodes.length === 1 &&
	  // do not go single mode if the only node is an anchor
	  !frag.childNodes[0].__v_anchor;
	  if (single) {
	    this.node = frag.childNodes[0];
	    this.before = singleBefore;
	    this.remove = singleRemove;
	  } else {
	    this.node = createAnchor('fragment-start');
	    this.end = createAnchor('fragment-end');
	    this.frag = frag;
	    prepend(this.node, frag);
	    frag.appendChild(this.end);
	    this.before = multiBefore;
	    this.remove = multiRemove;
	  }
	  this.node.__v_frag = this;
	}
	
	/**
	 * Call attach/detach for all components contained within
	 * this fragment. Also do so recursively for all child
	 * fragments.
	 *
	 * @param {Function} hook
	 */
	
	Fragment.prototype.callHook = function (hook) {
	  var i, l;
	  for (i = 0, l = this.childFrags.length; i < l; i++) {
	    this.childFrags[i].callHook(hook);
	  }
	  for (i = 0, l = this.children.length; i < l; i++) {
	    hook(this.children[i]);
	  }
	};
	
	/**
	 * Insert fragment before target, single node version
	 *
	 * @param {Node} target
	 * @param {Boolean} withTransition
	 */
	
	function singleBefore(target, withTransition) {
	  this.inserted = true;
	  var method = withTransition !== false ? beforeWithTransition : before;
	  method(this.node, target, this.vm);
	  if (inDoc(this.node)) {
	    this.callHook(attach);
	  }
	}
	
	/**
	 * Remove fragment, single node version
	 */
	
	function singleRemove() {
	  this.inserted = false;
	  var shouldCallRemove = inDoc(this.node);
	  var self = this;
	  this.beforeRemove();
	  removeWithTransition(this.node, this.vm, function () {
	    if (shouldCallRemove) {
	      self.callHook(detach);
	    }
	    self.destroy();
	  });
	}
	
	/**
	 * Insert fragment before target, multi-nodes version
	 *
	 * @param {Node} target
	 * @param {Boolean} withTransition
	 */
	
	function multiBefore(target, withTransition) {
	  this.inserted = true;
	  var vm = this.vm;
	  var method = withTransition !== false ? beforeWithTransition : before;
	  mapNodeRange(this.node, this.end, function (node) {
	    method(node, target, vm);
	  });
	  if (inDoc(this.node)) {
	    this.callHook(attach);
	  }
	}
	
	/**
	 * Remove fragment, multi-nodes version
	 */
	
	function multiRemove() {
	  this.inserted = false;
	  var self = this;
	  var shouldCallRemove = inDoc(this.node);
	  this.beforeRemove();
	  removeNodeRange(this.node, this.end, this.vm, this.frag, function () {
	    if (shouldCallRemove) {
	      self.callHook(detach);
	    }
	    self.destroy();
	  });
	}
	
	/**
	 * Prepare the fragment for removal.
	 */
	
	Fragment.prototype.beforeRemove = function () {
	  var i, l;
	  for (i = 0, l = this.childFrags.length; i < l; i++) {
	    // call the same method recursively on child
	    // fragments, depth-first
	    this.childFrags[i].beforeRemove(false);
	  }
	  for (i = 0, l = this.children.length; i < l; i++) {
	    // Call destroy for all contained instances,
	    // with remove:false and defer:true.
	    // Defer is necessary because we need to
	    // keep the children to call detach hooks
	    // on them.
	    this.children[i].$destroy(false, true);
	  }
	  var dirs = this.unlink.dirs;
	  for (i = 0, l = dirs.length; i < l; i++) {
	    // disable the watchers on all the directives
	    // so that the rendered content stays the same
	    // during removal.
	    dirs[i]._watcher && dirs[i]._watcher.teardown();
	  }
	};
	
	/**
	 * Destroy the fragment.
	 */
	
	Fragment.prototype.destroy = function () {
	  if (this.parentFrag) {
	    this.parentFrag.childFrags.$remove(this);
	  }
	  this.node.__v_frag = null;
	  this.unlink();
	};
	
	/**
	 * Call attach hook for a Vue instance.
	 *
	 * @param {Vue} child
	 */
	
	function attach(child) {
	  if (!child._isAttached && inDoc(child.$el)) {
	    child._callHook('attached');
	  }
	}
	
	/**
	 * Call detach hook for a Vue instance.
	 *
	 * @param {Vue} child
	 */
	
	function detach(child) {
	  if (child._isAttached && !inDoc(child.$el)) {
	    child._callHook('detached');
	  }
	}
	
	var linkerCache = new Cache(5000);
	
	/**
	 * A factory that can be used to create instances of a
	 * fragment. Caches the compiled linker if possible.
	 *
	 * @param {Vue} vm
	 * @param {Element|String} el
	 */
	function FragmentFactory(vm, el) {
	  this.vm = vm;
	  var template;
	  var isString = typeof el === 'string';
	  if (isString || isTemplate(el) && !el.hasAttribute('v-if')) {
	    template = parseTemplate(el, true);
	  } else {
	    template = document.createDocumentFragment();
	    template.appendChild(el);
	  }
	  this.template = template;
	  // linker can be cached, but only for components
	  var linker;
	  var cid = vm.constructor.cid;
	  if (cid > 0) {
	    var cacheId = cid + (isString ? el : getOuterHTML(el));
	    linker = linkerCache.get(cacheId);
	    if (!linker) {
	      linker = compile(template, vm.$options, true);
	      linkerCache.put(cacheId, linker);
	    }
	  } else {
	    linker = compile(template, vm.$options, true);
	  }
	  this.linker = linker;
	}
	
	/**
	 * Create a fragment instance with given host and scope.
	 *
	 * @param {Vue} host
	 * @param {Object} scope
	 * @param {Fragment} parentFrag
	 */
	
	FragmentFactory.prototype.create = function (host, scope, parentFrag) {
	  var frag = cloneNode(this.template);
	  return new Fragment(this.linker, this.vm, frag, host, scope, parentFrag);
	};
	
	var ON = 700;
	var MODEL = 800;
	var BIND = 850;
	var TRANSITION = 1100;
	var EL = 1500;
	var COMPONENT = 1500;
	var PARTIAL = 1750;
	var IF = 2100;
	var FOR = 2200;
	var SLOT = 2300;
	
	var uid$3 = 0;
	
	var vFor = {
	
	  priority: FOR,
	  terminal: true,
	
	  params: ['track-by', 'stagger', 'enter-stagger', 'leave-stagger'],
	
	  bind: function bind() {
	    // support "item in/of items" syntax
	    var inMatch = this.expression.match(/(.*) (?:in|of) (.*)/);
	    if (inMatch) {
	      var itMatch = inMatch[1].match(/\((.*),(.*)\)/);
	      if (itMatch) {
	        this.iterator = itMatch[1].trim();
	        this.alias = itMatch[2].trim();
	      } else {
	        this.alias = inMatch[1].trim();
	      }
	      this.expression = inMatch[2];
	    }
	
	    if (!this.alias) {
	      process.env.NODE_ENV !== 'production' && warn('Invalid v-for expression "' + this.descriptor.raw + '": ' + 'alias is required.', this.vm);
	      return;
	    }
	
	    // uid as a cache identifier
	    this.id = '__v-for__' + ++uid$3;
	
	    // check if this is an option list,
	    // so that we know if we need to update the <select>'s
	    // v-model when the option list has changed.
	    // because v-model has a lower priority than v-for,
	    // the v-model is not bound here yet, so we have to
	    // retrive it in the actual updateModel() function.
	    var tag = this.el.tagName;
	    this.isOption = (tag === 'OPTION' || tag === 'OPTGROUP') && this.el.parentNode.tagName === 'SELECT';
	
	    // setup anchor nodes
	    this.start = createAnchor('v-for-start');
	    this.end = createAnchor('v-for-end');
	    replace(this.el, this.end);
	    before(this.start, this.end);
	
	    // cache
	    this.cache = Object.create(null);
	
	    // fragment factory
	    this.factory = new FragmentFactory(this.vm, this.el);
	  },
	
	  update: function update(data) {
	    this.diff(data);
	    this.updateRef();
	    this.updateModel();
	  },
	
	  /**
	   * Diff, based on new data and old data, determine the
	   * minimum amount of DOM manipulations needed to make the
	   * DOM reflect the new data Array.
	   *
	   * The algorithm diffs the new data Array by storing a
	   * hidden reference to an owner vm instance on previously
	   * seen data. This allows us to achieve O(n) which is
	   * better than a levenshtein distance based algorithm,
	   * which is O(m * n).
	   *
	   * @param {Array} data
	   */
	
	  diff: function diff(data) {
	    // check if the Array was converted from an Object
	    var item = data[0];
	    var convertedFromObject = this.fromObject = isObject(item) && hasOwn(item, '$key') && hasOwn(item, '$value');
	
	    var trackByKey = this.params.trackBy;
	    var oldFrags = this.frags;
	    var frags = this.frags = new Array(data.length);
	    var alias = this.alias;
	    var iterator = this.iterator;
	    var start = this.start;
	    var end = this.end;
	    var inDocument = inDoc(start);
	    var init = !oldFrags;
	    var i, l, frag, key, value, primitive;
	
	    // First pass, go through the new Array and fill up
	    // the new frags array. If a piece of data has a cached
	    // instance for it, we reuse it. Otherwise build a new
	    // instance.
	    for (i = 0, l = data.length; i < l; i++) {
	      item = data[i];
	      key = convertedFromObject ? item.$key : null;
	      value = convertedFromObject ? item.$value : item;
	      primitive = !isObject(value);
	      frag = !init && this.getCachedFrag(value, i, key);
	      if (frag) {
	        // reusable fragment
	        frag.reused = true;
	        // update $index
	        frag.scope.$index = i;
	        // update $key
	        if (key) {
	          frag.scope.$key = key;
	        }
	        // update iterator
	        if (iterator) {
	          frag.scope[iterator] = key !== null ? key : i;
	        }
	        // update data for track-by, object repeat &
	        // primitive values.
	        if (trackByKey || convertedFromObject || primitive) {
	          withoutConversion(function () {
	            frag.scope[alias] = value;
	          });
	        }
	      } else {
	        // new isntance
	        frag = this.create(value, alias, i, key);
	        frag.fresh = !init;
	      }
	      frags[i] = frag;
	      if (init) {
	        frag.before(end);
	      }
	    }
	
	    // we're done for the initial render.
	    if (init) {
	      return;
	    }
	
	    // Second pass, go through the old fragments and
	    // destroy those who are not reused (and remove them
	    // from cache)
	    var removalIndex = 0;
	    var totalRemoved = oldFrags.length - frags.length;
	    // when removing a large number of fragments, watcher removal
	    // turns out to be a perf bottleneck, so we batch the watcher
	    // removals into a single filter call!
	    this.vm._vForRemoving = true;
	    for (i = 0, l = oldFrags.length; i < l; i++) {
	      frag = oldFrags[i];
	      if (!frag.reused) {
	        this.deleteCachedFrag(frag);
	        this.remove(frag, removalIndex++, totalRemoved, inDocument);
	      }
	    }
	    this.vm._vForRemoving = false;
	    if (removalIndex) {
	      this.vm._watchers = this.vm._watchers.filter(function (w) {
	        return w.active;
	      });
	    }
	
	    // Final pass, move/insert new fragments into the
	    // right place.
	    var targetPrev, prevEl, currentPrev;
	    var insertionIndex = 0;
	    for (i = 0, l = frags.length; i < l; i++) {
	      frag = frags[i];
	      // this is the frag that we should be after
	      targetPrev = frags[i - 1];
	      prevEl = targetPrev ? targetPrev.staggerCb ? targetPrev.staggerAnchor : targetPrev.end || targetPrev.node : start;
	      if (frag.reused && !frag.staggerCb) {
	        currentPrev = findPrevFrag(frag, start, this.id);
	        if (currentPrev !== targetPrev && (!currentPrev ||
	        // optimization for moving a single item.
	        // thanks to suggestions by @livoras in #1807
	        findPrevFrag(currentPrev, start, this.id) !== targetPrev)) {
	          this.move(frag, prevEl);
	        }
	      } else {
	        // new instance, or still in stagger.
	        // insert with updated stagger index.
	        this.insert(frag, insertionIndex++, prevEl, inDocument);
	      }
	      frag.reused = frag.fresh = false;
	    }
	  },
	
	  /**
	   * Create a new fragment instance.
	   *
	   * @param {*} value
	   * @param {String} alias
	   * @param {Number} index
	   * @param {String} [key]
	   * @return {Fragment}
	   */
	
	  create: function create(value, alias, index, key) {
	    var host = this._host;
	    // create iteration scope
	    var parentScope = this._scope || this.vm;
	    var scope = Object.create(parentScope);
	    // ref holder for the scope
	    scope.$refs = Object.create(parentScope.$refs);
	    scope.$els = Object.create(parentScope.$els);
	    // make sure point $parent to parent scope
	    scope.$parent = parentScope;
	    // for two-way binding on alias
	    scope.$forContext = this;
	    // define scope properties
	    // important: define the scope alias without forced conversion
	    // so that frozen data structures remain non-reactive.
	    withoutConversion(function () {
	      defineReactive(scope, alias, value);
	    });
	    defineReactive(scope, '$index', index);
	    if (key) {
	      defineReactive(scope, '$key', key);
	    } else if (scope.$key) {
	      // avoid accidental fallback
	      def(scope, '$key', null);
	    }
	    if (this.iterator) {
	      defineReactive(scope, this.iterator, key !== null ? key : index);
	    }
	    var frag = this.factory.create(host, scope, this._frag);
	    frag.forId = this.id;
	    this.cacheFrag(value, frag, index, key);
	    return frag;
	  },
	
	  /**
	   * Update the v-ref on owner vm.
	   */
	
	  updateRef: function updateRef() {
	    var ref = this.descriptor.ref;
	    if (!ref) return;
	    var hash = (this._scope || this.vm).$refs;
	    var refs;
	    if (!this.fromObject) {
	      refs = this.frags.map(findVmFromFrag);
	    } else {
	      refs = {};
	      this.frags.forEach(function (frag) {
	        refs[frag.scope.$key] = findVmFromFrag(frag);
	      });
	    }
	    hash[ref] = refs;
	  },
	
	  /**
	   * For option lists, update the containing v-model on
	   * parent <select>.
	   */
	
	  updateModel: function updateModel() {
	    if (this.isOption) {
	      var parent = this.start.parentNode;
	      var model = parent && parent.__v_model;
	      if (model) {
	        model.forceUpdate();
	      }
	    }
	  },
	
	  /**
	   * Insert a fragment. Handles staggering.
	   *
	   * @param {Fragment} frag
	   * @param {Number} index
	   * @param {Node} prevEl
	   * @param {Boolean} inDocument
	   */
	
	  insert: function insert(frag, index, prevEl, inDocument) {
	    if (frag.staggerCb) {
	      frag.staggerCb.cancel();
	      frag.staggerCb = null;
	    }
	    var staggerAmount = this.getStagger(frag, index, null, 'enter');
	    if (inDocument && staggerAmount) {
	      // create an anchor and insert it synchronously,
	      // so that we can resolve the correct order without
	      // worrying about some elements not inserted yet
	      var anchor = frag.staggerAnchor;
	      if (!anchor) {
	        anchor = frag.staggerAnchor = createAnchor('stagger-anchor');
	        anchor.__v_frag = frag;
	      }
	      after(anchor, prevEl);
	      var op = frag.staggerCb = cancellable(function () {
	        frag.staggerCb = null;
	        frag.before(anchor);
	        remove(anchor);
	      });
	      setTimeout(op, staggerAmount);
	    } else {
	      var target = prevEl.nextSibling;
	      /* istanbul ignore if */
	      if (!target) {
	        // reset end anchor position in case the position was messed up
	        // by an external drag-n-drop library.
	        after(this.end, prevEl);
	        target = this.end;
	      }
	      frag.before(target);
	    }
	  },
	
	  /**
	   * Remove a fragment. Handles staggering.
	   *
	   * @param {Fragment} frag
	   * @param {Number} index
	   * @param {Number} total
	   * @param {Boolean} inDocument
	   */
	
	  remove: function remove(frag, index, total, inDocument) {
	    if (frag.staggerCb) {
	      frag.staggerCb.cancel();
	      frag.staggerCb = null;
	      // it's not possible for the same frag to be removed
	      // twice, so if we have a pending stagger callback,
	      // it means this frag is queued for enter but removed
	      // before its transition started. Since it is already
	      // destroyed, we can just leave it in detached state.
	      return;
	    }
	    var staggerAmount = this.getStagger(frag, index, total, 'leave');
	    if (inDocument && staggerAmount) {
	      var op = frag.staggerCb = cancellable(function () {
	        frag.staggerCb = null;
	        frag.remove();
	      });
	      setTimeout(op, staggerAmount);
	    } else {
	      frag.remove();
	    }
	  },
	
	  /**
	   * Move a fragment to a new position.
	   * Force no transition.
	   *
	   * @param {Fragment} frag
	   * @param {Node} prevEl
	   */
	
	  move: function move(frag, prevEl) {
	    // fix a common issue with Sortable:
	    // if prevEl doesn't have nextSibling, this means it's
	    // been dragged after the end anchor. Just re-position
	    // the end anchor to the end of the container.
	    /* istanbul ignore if */
	    if (!prevEl.nextSibling) {
	      this.end.parentNode.appendChild(this.end);
	    }
	    frag.before(prevEl.nextSibling, false);
	  },
	
	  /**
	   * Cache a fragment using track-by or the object key.
	   *
	   * @param {*} value
	   * @param {Fragment} frag
	   * @param {Number} index
	   * @param {String} [key]
	   */
	
	  cacheFrag: function cacheFrag(value, frag, index, key) {
	    var trackByKey = this.params.trackBy;
	    var cache = this.cache;
	    var primitive = !isObject(value);
	    var id;
	    if (key || trackByKey || primitive) {
	      id = getTrackByKey(index, key, value, trackByKey);
	      if (!cache[id]) {
	        cache[id] = frag;
	      } else if (trackByKey !== '$index') {
	        process.env.NODE_ENV !== 'production' && this.warnDuplicate(value);
	      }
	    } else {
	      id = this.id;
	      if (hasOwn(value, id)) {
	        if (value[id] === null) {
	          value[id] = frag;
	        } else {
	          process.env.NODE_ENV !== 'production' && this.warnDuplicate(value);
	        }
	      } else if (Object.isExtensible(value)) {
	        def(value, id, frag);
	      } else if (process.env.NODE_ENV !== 'production') {
	        warn('Frozen v-for objects cannot be automatically tracked, make sure to ' + 'provide a track-by key.');
	      }
	    }
	    frag.raw = value;
	  },
	
	  /**
	   * Get a cached fragment from the value/index/key
	   *
	   * @param {*} value
	   * @param {Number} index
	   * @param {String} key
	   * @return {Fragment}
	   */
	
	  getCachedFrag: function getCachedFrag(value, index, key) {
	    var trackByKey = this.params.trackBy;
	    var primitive = !isObject(value);
	    var frag;
	    if (key || trackByKey || primitive) {
	      var id = getTrackByKey(index, key, value, trackByKey);
	      frag = this.cache[id];
	    } else {
	      frag = value[this.id];
	    }
	    if (frag && (frag.reused || frag.fresh)) {
	      process.env.NODE_ENV !== 'production' && this.warnDuplicate(value);
	    }
	    return frag;
	  },
	
	  /**
	   * Delete a fragment from cache.
	   *
	   * @param {Fragment} frag
	   */
	
	  deleteCachedFrag: function deleteCachedFrag(frag) {
	    var value = frag.raw;
	    var trackByKey = this.params.trackBy;
	    var scope = frag.scope;
	    var index = scope.$index;
	    // fix #948: avoid accidentally fall through to
	    // a parent repeater which happens to have $key.
	    var key = hasOwn(scope, '$key') && scope.$key;
	    var primitive = !isObject(value);
	    if (trackByKey || key || primitive) {
	      var id = getTrackByKey(index, key, value, trackByKey);
	      this.cache[id] = null;
	    } else {
	      value[this.id] = null;
	      frag.raw = null;
	    }
	  },
	
	  /**
	   * Get the stagger amount for an insertion/removal.
	   *
	   * @param {Fragment} frag
	   * @param {Number} index
	   * @param {Number} total
	   * @param {String} type
	   */
	
	  getStagger: function getStagger(frag, index, total, type) {
	    type = type + 'Stagger';
	    var trans = frag.node.__v_trans;
	    var hooks = trans && trans.hooks;
	    var hook = hooks && (hooks[type] || hooks.stagger);
	    return hook ? hook.call(frag, index, total) : index * parseInt(this.params[type] || this.params.stagger, 10);
	  },
	
	  /**
	   * Pre-process the value before piping it through the
	   * filters. This is passed to and called by the watcher.
	   */
	
	  _preProcess: function _preProcess(value) {
	    // regardless of type, store the un-filtered raw value.
	    this.rawValue = value;
	    return value;
	  },
	
	  /**
	   * Post-process the value after it has been piped through
	   * the filters. This is passed to and called by the watcher.
	   *
	   * It is necessary for this to be called during the
	   * watcher's dependency collection phase because we want
	   * the v-for to update when the source Object is mutated.
	   */
	
	  _postProcess: function _postProcess(value) {
	    if (isArray(value)) {
	      return value;
	    } else if (isPlainObject(value)) {
	      // convert plain object to array.
	      var keys = Object.keys(value);
	      var i = keys.length;
	      var res = new Array(i);
	      var key;
	      while (i--) {
	        key = keys[i];
	        res[i] = {
	          $key: key,
	          $value: value[key]
	        };
	      }
	      return res;
	    } else {
	      if (typeof value === 'number' && !isNaN(value)) {
	        value = range(value);
	      }
	      return value || [];
	    }
	  },
	
	  unbind: function unbind() {
	    if (this.descriptor.ref) {
	      (this._scope || this.vm).$refs[this.descriptor.ref] = null;
	    }
	    if (this.frags) {
	      var i = this.frags.length;
	      var frag;
	      while (i--) {
	        frag = this.frags[i];
	        this.deleteCachedFrag(frag);
	        frag.destroy();
	      }
	    }
	  }
	};
	
	/**
	 * Helper to find the previous element that is a fragment
	 * anchor. This is necessary because a destroyed frag's
	 * element could still be lingering in the DOM before its
	 * leaving transition finishes, but its inserted flag
	 * should have been set to false so we can skip them.
	 *
	 * If this is a block repeat, we want to make sure we only
	 * return frag that is bound to this v-for. (see #929)
	 *
	 * @param {Fragment} frag
	 * @param {Comment|Text} anchor
	 * @param {String} id
	 * @return {Fragment}
	 */
	
	function findPrevFrag(frag, anchor, id) {
	  var el = frag.node.previousSibling;
	  /* istanbul ignore if */
	  if (!el) return;
	  frag = el.__v_frag;
	  while ((!frag || frag.forId !== id || !frag.inserted) && el !== anchor) {
	    el = el.previousSibling;
	    /* istanbul ignore if */
	    if (!el) return;
	    frag = el.__v_frag;
	  }
	  return frag;
	}
	
	/**
	 * Find a vm from a fragment.
	 *
	 * @param {Fragment} frag
	 * @return {Vue|undefined}
	 */
	
	function findVmFromFrag(frag) {
	  var node = frag.node;
	  // handle multi-node frag
	  if (frag.end) {
	    while (!node.__vue__ && node !== frag.end && node.nextSibling) {
	      node = node.nextSibling;
	    }
	  }
	  return node.__vue__;
	}
	
	/**
	 * Create a range array from given number.
	 *
	 * @param {Number} n
	 * @return {Array}
	 */
	
	function range(n) {
	  var i = -1;
	  var ret = new Array(Math.floor(n));
	  while (++i < n) {
	    ret[i] = i;
	  }
	  return ret;
	}
	
	/**
	 * Get the track by key for an item.
	 *
	 * @param {Number} index
	 * @param {String} key
	 * @param {*} value
	 * @param {String} [trackByKey]
	 */
	
	function getTrackByKey(index, key, value, trackByKey) {
	  return trackByKey ? trackByKey === '$index' ? index : trackByKey.charAt(0).match(/\w/) ? getPath(value, trackByKey) : value[trackByKey] : key || value;
	}
	
	if (process.env.NODE_ENV !== 'production') {
	  vFor.warnDuplicate = function (value) {
	    warn('Duplicate value found in v-for="' + this.descriptor.raw + '": ' + JSON.stringify(value) + '. Use track-by="$index" if ' + 'you are expecting duplicate values.', this.vm);
	  };
	}
	
	var vIf = {
	
	  priority: IF,
	  terminal: true,
	
	  bind: function bind() {
	    var el = this.el;
	    if (!el.__vue__) {
	      // check else block
	      var next = el.nextElementSibling;
	      if (next && getAttr(next, 'v-else') !== null) {
	        remove(next);
	        this.elseEl = next;
	      }
	      // check main block
	      this.anchor = createAnchor('v-if');
	      replace(el, this.anchor);
	    } else {
	      process.env.NODE_ENV !== 'production' && warn('v-if="' + this.expression + '" cannot be ' + 'used on an instance root element.', this.vm);
	      this.invalid = true;
	    }
	  },
	
	  update: function update(value) {
	    if (this.invalid) return;
	    if (value) {
	      if (!this.frag) {
	        this.insert();
	      }
	    } else {
	      this.remove();
	    }
	  },
	
	  insert: function insert() {
	    if (this.elseFrag) {
	      this.elseFrag.remove();
	      this.elseFrag = null;
	    }
	    // lazy init factory
	    if (!this.factory) {
	      this.factory = new FragmentFactory(this.vm, this.el);
	    }
	    this.frag = this.factory.create(this._host, this._scope, this._frag);
	    this.frag.before(this.anchor);
	  },
	
	  remove: function remove() {
	    if (this.frag) {
	      this.frag.remove();
	      this.frag = null;
	    }
	    if (this.elseEl && !this.elseFrag) {
	      if (!this.elseFactory) {
	        this.elseFactory = new FragmentFactory(this.elseEl._context || this.vm, this.elseEl);
	      }
	      this.elseFrag = this.elseFactory.create(this._host, this._scope, this._frag);
	      this.elseFrag.before(this.anchor);
	    }
	  },
	
	  unbind: function unbind() {
	    if (this.frag) {
	      this.frag.destroy();
	    }
	    if (this.elseFrag) {
	      this.elseFrag.destroy();
	    }
	  }
	};
	
	var show = {
	
	  bind: function bind() {
	    // check else block
	    var next = this.el.nextElementSibling;
	    if (next && getAttr(next, 'v-else') !== null) {
	      this.elseEl = next;
	    }
	  },
	
	  update: function update(value) {
	    this.apply(this.el, value);
	    if (this.elseEl) {
	      this.apply(this.elseEl, !value);
	    }
	  },
	
	  apply: function apply(el, value) {
	    if (inDoc(el)) {
	      applyTransition(el, value ? 1 : -1, toggle, this.vm);
	    } else {
	      toggle();
	    }
	    function toggle() {
	      el.style.display = value ? '' : 'none';
	    }
	  }
	};
	
	var text$2 = {
	
	  bind: function bind() {
	    var self = this;
	    var el = this.el;
	    var isRange = el.type === 'range';
	    var lazy = this.params.lazy;
	    var number = this.params.number;
	    var debounce = this.params.debounce;
	
	    // handle composition events.
	    //   http://blog.evanyou.me/2014/01/03/composition-event/
	    // skip this for Android because it handles composition
	    // events quite differently. Android doesn't trigger
	    // composition events for language input methods e.g.
	    // Chinese, but instead triggers them for spelling
	    // suggestions... (see Discussion/#162)
	    var composing = false;
	    if (!isAndroid && !isRange) {
	      this.on('compositionstart', function () {
	        composing = true;
	      });
	      this.on('compositionend', function () {
	        composing = false;
	        // in IE11 the "compositionend" event fires AFTER
	        // the "input" event, so the input handler is blocked
	        // at the end... have to call it here.
	        //
	        // #1327: in lazy mode this is unecessary.
	        if (!lazy) {
	          self.listener();
	        }
	      });
	    }
	
	    // prevent messing with the input when user is typing,
	    // and force update on blur.
	    this.focused = false;
	    if (!isRange && !lazy) {
	      this.on('focus', function () {
	        self.focused = true;
	      });
	      this.on('blur', function () {
	        self.focused = false;
	        // do not sync value after fragment removal (#2017)
	        if (!self._frag || self._frag.inserted) {
	          self.rawListener();
	        }
	      });
	    }
	
	    // Now attach the main listener
	    this.listener = this.rawListener = function () {
	      if (composing || !self._bound) {
	        return;
	      }
	      var val = number || isRange ? toNumber(el.value) : el.value;
	      self.set(val);
	      // force update on next tick to avoid lock & same value
	      // also only update when user is not typing
	      nextTick(function () {
	        if (self._bound && !self.focused) {
	          self.update(self._watcher.value);
	        }
	      });
	    };
	
	    // apply debounce
	    if (debounce) {
	      this.listener = _debounce(this.listener, debounce);
	    }
	
	    // Support jQuery events, since jQuery.trigger() doesn't
	    // trigger native events in some cases and some plugins
	    // rely on $.trigger()
	    //
	    // We want to make sure if a listener is attached using
	    // jQuery, it is also removed with jQuery, that's why
	    // we do the check for each directive instance and
	    // store that check result on itself. This also allows
	    // easier test coverage control by unsetting the global
	    // jQuery variable in tests.
	    this.hasjQuery = typeof jQuery === 'function';
	    if (this.hasjQuery) {
	      var method = jQuery.fn.on ? 'on' : 'bind';
	      jQuery(el)[method]('change', this.rawListener);
	      if (!lazy) {
	        jQuery(el)[method]('input', this.listener);
	      }
	    } else {
	      this.on('change', this.rawListener);
	      if (!lazy) {
	        this.on('input', this.listener);
	      }
	    }
	
	    // IE9 doesn't fire input event on backspace/del/cut
	    if (!lazy && isIE9) {
	      this.on('cut', function () {
	        nextTick(self.listener);
	      });
	      this.on('keyup', function (e) {
	        if (e.keyCode === 46 || e.keyCode === 8) {
	          self.listener();
	        }
	      });
	    }
	
	    // set initial value if present
	    if (el.hasAttribute('value') || el.tagName === 'TEXTAREA' && el.value.trim()) {
	      this.afterBind = this.listener;
	    }
	  },
	
	  update: function update(value) {
	    // #3029 only update when the value changes. This prevent
	    // browsers from overwriting values like selectionStart
	    value = _toString(value);
	    if (value !== this.el.value) this.el.value = value;
	  },
	
	  unbind: function unbind() {
	    var el = this.el;
	    if (this.hasjQuery) {
	      var method = jQuery.fn.off ? 'off' : 'unbind';
	      jQuery(el)[method]('change', this.listener);
	      jQuery(el)[method]('input', this.listener);
	    }
	  }
	};
	
	var radio = {
	
	  bind: function bind() {
	    var self = this;
	    var el = this.el;
	
	    this.getValue = function () {
	      // value overwrite via v-bind:value
	      if (el.hasOwnProperty('_value')) {
	        return el._value;
	      }
	      var val = el.value;
	      if (self.params.number) {
	        val = toNumber(val);
	      }
	      return val;
	    };
	
	    this.listener = function () {
	      self.set(self.getValue());
	    };
	    this.on('change', this.listener);
	
	    if (el.hasAttribute('checked')) {
	      this.afterBind = this.listener;
	    }
	  },
	
	  update: function update(value) {
	    this.el.checked = looseEqual(value, this.getValue());
	  }
	};
	
	var select = {
	
	  bind: function bind() {
	    var _this = this;
	
	    var self = this;
	    var el = this.el;
	
	    // method to force update DOM using latest value.
	    this.forceUpdate = function () {
	      if (self._watcher) {
	        self.update(self._watcher.get());
	      }
	    };
	
	    // check if this is a multiple select
	    var multiple = this.multiple = el.hasAttribute('multiple');
	
	    // attach listener
	    this.listener = function () {
	      var value = getValue(el, multiple);
	      value = self.params.number ? isArray(value) ? value.map(toNumber) : toNumber(value) : value;
	      self.set(value);
	    };
	    this.on('change', this.listener);
	
	    // if has initial value, set afterBind
	    var initValue = getValue(el, multiple, true);
	    if (multiple && initValue.length || !multiple && initValue !== null) {
	      this.afterBind = this.listener;
	    }
	
	    // All major browsers except Firefox resets
	    // selectedIndex with value -1 to 0 when the element
	    // is appended to a new parent, therefore we have to
	    // force a DOM update whenever that happens...
	    this.vm.$on('hook:attached', function () {
	      nextTick(_this.forceUpdate);
	    });
	    if (!inDoc(el)) {
	      nextTick(this.forceUpdate);
	    }
	  },
	
	  update: function update(value) {
	    var el = this.el;
	    el.selectedIndex = -1;
	    var multi = this.multiple && isArray(value);
	    var options = el.options;
	    var i = options.length;
	    var op, val;
	    while (i--) {
	      op = options[i];
	      val = op.hasOwnProperty('_value') ? op._value : op.value;
	      /* eslint-disable eqeqeq */
	      op.selected = multi ? indexOf$1(value, val) > -1 : looseEqual(value, val);
	      /* eslint-enable eqeqeq */
	    }
	  },
	
	  unbind: function unbind() {
	    /* istanbul ignore next */
	    this.vm.$off('hook:attached', this.forceUpdate);
	  }
	};
	
	/**
	 * Get select value
	 *
	 * @param {SelectElement} el
	 * @param {Boolean} multi
	 * @param {Boolean} init
	 * @return {Array|*}
	 */
	
	function getValue(el, multi, init) {
	  var res = multi ? [] : null;
	  var op, val, selected;
	  for (var i = 0, l = el.options.length; i < l; i++) {
	    op = el.options[i];
	    selected = init ? op.hasAttribute('selected') : op.selected;
	    if (selected) {
	      val = op.hasOwnProperty('_value') ? op._value : op.value;
	      if (multi) {
	        res.push(val);
	      } else {
	        return val;
	      }
	    }
	  }
	  return res;
	}
	
	/**
	 * Native Array.indexOf uses strict equal, but in this
	 * case we need to match string/numbers with custom equal.
	 *
	 * @param {Array} arr
	 * @param {*} val
	 */
	
	function indexOf$1(arr, val) {
	  var i = arr.length;
	  while (i--) {
	    if (looseEqual(arr[i], val)) {
	      return i;
	    }
	  }
	  return -1;
	}
	
	var checkbox = {
	
	  bind: function bind() {
	    var self = this;
	    var el = this.el;
	
	    this.getValue = function () {
	      return el.hasOwnProperty('_value') ? el._value : self.params.number ? toNumber(el.value) : el.value;
	    };
	
	    function getBooleanValue() {
	      var val = el.checked;
	      if (val && el.hasOwnProperty('_trueValue')) {
	        return el._trueValue;
	      }
	      if (!val && el.hasOwnProperty('_falseValue')) {
	        return el._falseValue;
	      }
	      return val;
	    }
	
	    this.listener = function () {
	      var model = self._watcher.value;
	      if (isArray(model)) {
	        var val = self.getValue();
	        if (el.checked) {
	          if (indexOf(model, val) < 0) {
	            model.push(val);
	          }
	        } else {
	          model.$remove(val);
	        }
	      } else {
	        self.set(getBooleanValue());
	      }
	    };
	
	    this.on('change', this.listener);
	    if (el.hasAttribute('checked')) {
	      this.afterBind = this.listener;
	    }
	  },
	
	  update: function update(value) {
	    var el = this.el;
	    if (isArray(value)) {
	      el.checked = indexOf(value, this.getValue()) > -1;
	    } else {
	      if (el.hasOwnProperty('_trueValue')) {
	        el.checked = looseEqual(value, el._trueValue);
	      } else {
	        el.checked = !!value;
	      }
	    }
	  }
	};
	
	var handlers = {
	  text: text$2,
	  radio: radio,
	  select: select,
	  checkbox: checkbox
	};
	
	var model = {
	
	  priority: MODEL,
	  twoWay: true,
	  handlers: handlers,
	  params: ['lazy', 'number', 'debounce'],
	
	  /**
	   * Possible elements:
	   *   <select>
	   *   <textarea>
	   *   <input type="*">
	   *     - text
	   *     - checkbox
	   *     - radio
	   *     - number
	   */
	
	  bind: function bind() {
	    // friendly warning...
	    this.checkFilters();
	    if (this.hasRead && !this.hasWrite) {
	      process.env.NODE_ENV !== 'production' && warn('It seems you are using a read-only filter with ' + 'v-model="' + this.descriptor.raw + '". ' + 'You might want to use a two-way filter to ensure correct behavior.', this.vm);
	    }
	    var el = this.el;
	    var tag = el.tagName;
	    var handler;
	    if (tag === 'INPUT') {
	      handler = handlers[el.type] || handlers.text;
	    } else if (tag === 'SELECT') {
	      handler = handlers.select;
	    } else if (tag === 'TEXTAREA') {
	      handler = handlers.text;
	    } else {
	      process.env.NODE_ENV !== 'production' && warn('v-model does not support element type: ' + tag, this.vm);
	      return;
	    }
	    el.__v_model = this;
	    handler.bind.call(this);
	    this.update = handler.update;
	    this._unbind = handler.unbind;
	  },
	
	  /**
	   * Check read/write filter stats.
	   */
	
	  checkFilters: function checkFilters() {
	    var filters = this.filters;
	    if (!filters) return;
	    var i = filters.length;
	    while (i--) {
	      var filter = resolveAsset(this.vm.$options, 'filters', filters[i].name);
	      if (typeof filter === 'function' || filter.read) {
	        this.hasRead = true;
	      }
	      if (filter.write) {
	        this.hasWrite = true;
	      }
	    }
	  },
	
	  unbind: function unbind() {
	    this.el.__v_model = null;
	    this._unbind && this._unbind();
	  }
	};
	
	// keyCode aliases
	var keyCodes = {
	  esc: 27,
	  tab: 9,
	  enter: 13,
	  space: 32,
	  'delete': [8, 46],
	  up: 38,
	  left: 37,
	  right: 39,
	  down: 40
	};
	
	function keyFilter(handler, keys) {
	  var codes = keys.map(function (key) {
	    var charCode = key.charCodeAt(0);
	    if (charCode > 47 && charCode < 58) {
	      return parseInt(key, 10);
	    }
	    if (key.length === 1) {
	      charCode = key.toUpperCase().charCodeAt(0);
	      if (charCode > 64 && charCode < 91) {
	        return charCode;
	      }
	    }
	    return keyCodes[key];
	  });
	  codes = [].concat.apply([], codes);
	  return function keyHandler(e) {
	    if (codes.indexOf(e.keyCode) > -1) {
	      return handler.call(this, e);
	    }
	  };
	}
	
	function stopFilter(handler) {
	  return function stopHandler(e) {
	    e.stopPropagation();
	    return handler.call(this, e);
	  };
	}
	
	function preventFilter(handler) {
	  return function preventHandler(e) {
	    e.preventDefault();
	    return handler.call(this, e);
	  };
	}
	
	function selfFilter(handler) {
	  return function selfHandler(e) {
	    if (e.target === e.currentTarget) {
	      return handler.call(this, e);
	    }
	  };
	}
	
	var on$1 = {
	
	  priority: ON,
	  acceptStatement: true,
	  keyCodes: keyCodes,
	
	  bind: function bind() {
	    // deal with iframes
	    if (this.el.tagName === 'IFRAME' && this.arg !== 'load') {
	      var self = this;
	      this.iframeBind = function () {
	        on(self.el.contentWindow, self.arg, self.handler, self.modifiers.capture);
	      };
	      this.on('load', this.iframeBind);
	    }
	  },
	
	  update: function update(handler) {
	    // stub a noop for v-on with no value,
	    // e.g. @mousedown.prevent
	    if (!this.descriptor.raw) {
	      handler = function () {};
	    }
	
	    if (typeof handler !== 'function') {
	      process.env.NODE_ENV !== 'production' && warn('v-on:' + this.arg + '="' + this.expression + '" expects a function value, ' + 'got ' + handler, this.vm);
	      return;
	    }
	
	    // apply modifiers
	    if (this.modifiers.stop) {
	      handler = stopFilter(handler);
	    }
	    if (this.modifiers.prevent) {
	      handler = preventFilter(handler);
	    }
	    if (this.modifiers.self) {
	      handler = selfFilter(handler);
	    }
	    // key filter
	    var keys = Object.keys(this.modifiers).filter(function (key) {
	      return key !== 'stop' && key !== 'prevent' && key !== 'self' && key !== 'capture';
	    });
	    if (keys.length) {
	      handler = keyFilter(handler, keys);
	    }
	
	    this.reset();
	    this.handler = handler;
	
	    if (this.iframeBind) {
	      this.iframeBind();
	    } else {
	      on(this.el, this.arg, this.handler, this.modifiers.capture);
	    }
	  },
	
	  reset: function reset() {
	    var el = this.iframeBind ? this.el.contentWindow : this.el;
	    if (this.handler) {
	      off(el, this.arg, this.handler);
	    }
	  },
	
	  unbind: function unbind() {
	    this.reset();
	  }
	};
	
	var prefixes = ['-webkit-', '-moz-', '-ms-'];
	var camelPrefixes = ['Webkit', 'Moz', 'ms'];
	var importantRE = /!important;?$/;
	var propCache = Object.create(null);
	
	var testEl = null;
	
	var style = {
	
	  deep: true,
	
	  update: function update(value) {
	    if (typeof value === 'string') {
	      this.el.style.cssText = value;
	    } else if (isArray(value)) {
	      this.handleObject(value.reduce(extend, {}));
	    } else {
	      this.handleObject(value || {});
	    }
	  },
	
	  handleObject: function handleObject(value) {
	    // cache object styles so that only changed props
	    // are actually updated.
	    var cache = this.cache || (this.cache = {});
	    var name, val;
	    for (name in cache) {
	      if (!(name in value)) {
	        this.handleSingle(name, null);
	        delete cache[name];
	      }
	    }
	    for (name in value) {
	      val = value[name];
	      if (val !== cache[name]) {
	        cache[name] = val;
	        this.handleSingle(name, val);
	      }
	    }
	  },
	
	  handleSingle: function handleSingle(prop, value) {
	    prop = normalize(prop);
	    if (!prop) return; // unsupported prop
	    // cast possible numbers/booleans into strings
	    if (value != null) value += '';
	    if (value) {
	      var isImportant = importantRE.test(value) ? 'important' : '';
	      if (isImportant) {
	        /* istanbul ignore if */
	        if (process.env.NODE_ENV !== 'production') {
	          warn('It\'s probably a bad idea to use !important with inline rules. ' + 'This feature will be deprecated in a future version of Vue.');
	        }
	        value = value.replace(importantRE, '').trim();
	        this.el.style.setProperty(prop.kebab, value, isImportant);
	      } else {
	        this.el.style[prop.camel] = value;
	      }
	    } else {
	      this.el.style[prop.camel] = '';
	    }
	  }
	
	};
	
	/**
	 * Normalize a CSS property name.
	 * - cache result
	 * - auto prefix
	 * - camelCase -> dash-case
	 *
	 * @param {String} prop
	 * @return {String}
	 */
	
	function normalize(prop) {
	  if (propCache[prop]) {
	    return propCache[prop];
	  }
	  var res = prefix(prop);
	  propCache[prop] = propCache[res] = res;
	  return res;
	}
	
	/**
	 * Auto detect the appropriate prefix for a CSS property.
	 * https://gist.github.com/paulirish/523692
	 *
	 * @param {String} prop
	 * @return {String}
	 */
	
	function prefix(prop) {
	  prop = hyphenate(prop);
	  var camel = camelize(prop);
	  var upper = camel.charAt(0).toUpperCase() + camel.slice(1);
	  if (!testEl) {
	    testEl = document.createElement('div');
	  }
	  var i = prefixes.length;
	  var prefixed;
	  if (camel !== 'filter' && camel in testEl.style) {
	    return {
	      kebab: prop,
	      camel: camel
	    };
	  }
	  while (i--) {
	    prefixed = camelPrefixes[i] + upper;
	    if (prefixed in testEl.style) {
	      return {
	        kebab: prefixes[i] + prop,
	        camel: prefixed
	      };
	    }
	  }
	}
	
	// xlink
	var xlinkNS = 'http://www.w3.org/1999/xlink';
	var xlinkRE = /^xlink:/;
	
	// check for attributes that prohibit interpolations
	var disallowedInterpAttrRE = /^v-|^:|^@|^(?:is|transition|transition-mode|debounce|track-by|stagger|enter-stagger|leave-stagger)$/;
	// these attributes should also set their corresponding properties
	// because they only affect the initial state of the element
	var attrWithPropsRE = /^(?:value|checked|selected|muted)$/;
	// these attributes expect enumrated values of "true" or "false"
	// but are not boolean attributes
	var enumeratedAttrRE = /^(?:draggable|contenteditable|spellcheck)$/;
	
	// these attributes should set a hidden property for
	// binding v-model to object values
	var modelProps = {
	  value: '_value',
	  'true-value': '_trueValue',
	  'false-value': '_falseValue'
	};
	
	var bind$1 = {
	
	  priority: BIND,
	
	  bind: function bind() {
	    var attr = this.arg;
	    var tag = this.el.tagName;
	    // should be deep watch on object mode
	    if (!attr) {
	      this.deep = true;
	    }
	    // handle interpolation bindings
	    var descriptor = this.descriptor;
	    var tokens = descriptor.interp;
	    if (tokens) {
	      // handle interpolations with one-time tokens
	      if (descriptor.hasOneTime) {
	        this.expression = tokensToExp(tokens, this._scope || this.vm);
	      }
	
	      // only allow binding on native attributes
	      if (disallowedInterpAttrRE.test(attr) || attr === 'name' && (tag === 'PARTIAL' || tag === 'SLOT')) {
	        process.env.NODE_ENV !== 'production' && warn(attr + '="' + descriptor.raw + '": ' + 'attribute interpolation is not allowed in Vue.js ' + 'directives and special attributes.', this.vm);
	        this.el.removeAttribute(attr);
	        this.invalid = true;
	      }
	
	      /* istanbul ignore if */
	      if (process.env.NODE_ENV !== 'production') {
	        var raw = attr + '="' + descriptor.raw + '": ';
	        // warn src
	        if (attr === 'src') {
	          warn(raw + 'interpolation in "src" attribute will cause ' + 'a 404 request. Use v-bind:src instead.', this.vm);
	        }
	
	        // warn style
	        if (attr === 'style') {
	          warn(raw + 'interpolation in "style" attribute will cause ' + 'the attribute to be discarded in Internet Explorer. ' + 'Use v-bind:style instead.', this.vm);
	        }
	      }
	    }
	  },
	
	  update: function update(value) {
	    if (this.invalid) {
	      return;
	    }
	    var attr = this.arg;
	    if (this.arg) {
	      this.handleSingle(attr, value);
	    } else {
	      this.handleObject(value || {});
	    }
	  },
	
	  // share object handler with v-bind:class
	  handleObject: style.handleObject,
	
	  handleSingle: function handleSingle(attr, value) {
	    var el = this.el;
	    var interp = this.descriptor.interp;
	    if (this.modifiers.camel) {
	      attr = camelize(attr);
	    }
	    if (!interp && attrWithPropsRE.test(attr) && attr in el) {
	      var attrValue = attr === 'value' ? value == null // IE9 will set input.value to "null" for null...
	      ? '' : value : value;
	
	      if (el[attr] !== attrValue) {
	        el[attr] = attrValue;
	      }
	    }
	    // set model props
	    var modelProp = modelProps[attr];
	    if (!interp && modelProp) {
	      el[modelProp] = value;
	      // update v-model if present
	      var model = el.__v_model;
	      if (model) {
	        model.listener();
	      }
	    }
	    // do not set value attribute for textarea
	    if (attr === 'value' && el.tagName === 'TEXTAREA') {
	      el.removeAttribute(attr);
	      return;
	    }
	    // update attribute
	    if (enumeratedAttrRE.test(attr)) {
	      el.setAttribute(attr, value ? 'true' : 'false');
	    } else if (value != null && value !== false) {
	      if (attr === 'class') {
	        // handle edge case #1960:
	        // class interpolation should not overwrite Vue transition class
	        if (el.__v_trans) {
	          value += ' ' + el.__v_trans.id + '-transition';
	        }
	        setClass(el, value);
	      } else if (xlinkRE.test(attr)) {
	        el.setAttributeNS(xlinkNS, attr, value === true ? '' : value);
	      } else {
	        el.setAttribute(attr, value === true ? '' : value);
	      }
	    } else {
	      el.removeAttribute(attr);
	    }
	  }
	};
	
	var el = {
	
	  priority: EL,
	
	  bind: function bind() {
	    /* istanbul ignore if */
	    if (!this.arg) {
	      return;
	    }
	    var id = this.id = camelize(this.arg);
	    var refs = (this._scope || this.vm).$els;
	    if (hasOwn(refs, id)) {
	      refs[id] = this.el;
	    } else {
	      defineReactive(refs, id, this.el);
	    }
	  },
	
	  unbind: function unbind() {
	    var refs = (this._scope || this.vm).$els;
	    if (refs[this.id] === this.el) {
	      refs[this.id] = null;
	    }
	  }
	};
	
	var ref = {
	  bind: function bind() {
	    process.env.NODE_ENV !== 'production' && warn('v-ref:' + this.arg + ' must be used on a child ' + 'component. Found on <' + this.el.tagName.toLowerCase() + '>.', this.vm);
	  }
	};
	
	var cloak = {
	  bind: function bind() {
	    var el = this.el;
	    this.vm.$once('pre-hook:compiled', function () {
	      el.removeAttribute('v-cloak');
	    });
	  }
	};
	
	// must export plain object
	var directives = {
	  text: text$1,
	  html: html,
	  'for': vFor,
	  'if': vIf,
	  show: show,
	  model: model,
	  on: on$1,
	  bind: bind$1,
	  el: el,
	  ref: ref,
	  cloak: cloak
	};
	
	var vClass = {
	
	  deep: true,
	
	  update: function update(value) {
	    if (!value) {
	      this.cleanup();
	    } else if (typeof value === 'string') {
	      this.setClass(value.trim().split(/\s+/));
	    } else {
	      this.setClass(normalize$1(value));
	    }
	  },
	
	  setClass: function setClass(value) {
	    this.cleanup(value);
	    for (var i = 0, l = value.length; i < l; i++) {
	      var val = value[i];
	      if (val) {
	        apply(this.el, val, addClass);
	      }
	    }
	    this.prevKeys = value;
	  },
	
	  cleanup: function cleanup(value) {
	    var prevKeys = this.prevKeys;
	    if (!prevKeys) return;
	    var i = prevKeys.length;
	    while (i--) {
	      var key = prevKeys[i];
	      if (!value || value.indexOf(key) < 0) {
	        apply(this.el, key, removeClass);
	      }
	    }
	  }
	};
	
	/**
	 * Normalize objects and arrays (potentially containing objects)
	 * into array of strings.
	 *
	 * @param {Object|Array<String|Object>} value
	 * @return {Array<String>}
	 */
	
	function normalize$1(value) {
	  var res = [];
	  if (isArray(value)) {
	    for (var i = 0, l = value.length; i < l; i++) {
	      var _key = value[i];
	      if (_key) {
	        if (typeof _key === 'string') {
	          res.push(_key);
	        } else {
	          for (var k in _key) {
	            if (_key[k]) res.push(k);
	          }
	        }
	      }
	    }
	  } else if (isObject(value)) {
	    for (var key in value) {
	      if (value[key]) res.push(key);
	    }
	  }
	  return res;
	}
	
	/**
	 * Add or remove a class/classes on an element
	 *
	 * @param {Element} el
	 * @param {String} key The class name. This may or may not
	 *                     contain a space character, in such a
	 *                     case we'll deal with multiple class
	 *                     names at once.
	 * @param {Function} fn
	 */
	
	function apply(el, key, fn) {
	  key = key.trim();
	  if (key.indexOf(' ') === -1) {
	    fn(el, key);
	    return;
	  }
	  // The key contains one or more space characters.
	  // Since a class name doesn't accept such characters, we
	  // treat it as multiple classes.
	  var keys = key.split(/\s+/);
	  for (var i = 0, l = keys.length; i < l; i++) {
	    fn(el, keys[i]);
	  }
	}
	
	var component = {
	
	  priority: COMPONENT,
	
	  params: ['keep-alive', 'transition-mode', 'inline-template'],
	
	  /**
	   * Setup. Two possible usages:
	   *
	   * - static:
	   *   <comp> or <div v-component="comp">
	   *
	   * - dynamic:
	   *   <component :is="view">
	   */
	
	  bind: function bind() {
	    if (!this.el.__vue__) {
	      // keep-alive cache
	      this.keepAlive = this.params.keepAlive;
	      if (this.keepAlive) {
	        this.cache = {};
	      }
	      // check inline-template
	      if (this.params.inlineTemplate) {
	        // extract inline template as a DocumentFragment
	        this.inlineTemplate = extractContent(this.el, true);
	      }
	      // component resolution related state
	      this.pendingComponentCb = this.Component = null;
	      // transition related state
	      this.pendingRemovals = 0;
	      this.pendingRemovalCb = null;
	      // create a ref anchor
	      this.anchor = createAnchor('v-component');
	      replace(this.el, this.anchor);
	      // remove is attribute.
	      // this is removed during compilation, but because compilation is
	      // cached, when the component is used elsewhere this attribute
	      // will remain at link time.
	      this.el.removeAttribute('is');
	      this.el.removeAttribute(':is');
	      // remove ref, same as above
	      if (this.descriptor.ref) {
	        this.el.removeAttribute('v-ref:' + hyphenate(this.descriptor.ref));
	      }
	      // if static, build right now.
	      if (this.literal) {
	        this.setComponent(this.expression);
	      }
	    } else {
	      process.env.NODE_ENV !== 'production' && warn('cannot mount component "' + this.expression + '" ' + 'on already mounted element: ' + this.el);
	    }
	  },
	
	  /**
	   * Public update, called by the watcher in the dynamic
	   * literal scenario, e.g. <component :is="view">
	   */
	
	  update: function update(value) {
	    if (!this.literal) {
	      this.setComponent(value);
	    }
	  },
	
	  /**
	   * Switch dynamic components. May resolve the component
	   * asynchronously, and perform transition based on
	   * specified transition mode. Accepts a few additional
	   * arguments specifically for vue-router.
	   *
	   * The callback is called when the full transition is
	   * finished.
	   *
	   * @param {String} value
	   * @param {Function} [cb]
	   */
	
	  setComponent: function setComponent(value, cb) {
	    this.invalidatePending();
	    if (!value) {
	      // just remove current
	      this.unbuild(true);
	      this.remove(this.childVM, cb);
	      this.childVM = null;
	    } else {
	      var self = this;
	      this.resolveComponent(value, function () {
	        self.mountComponent(cb);
	      });
	    }
	  },
	
	  /**
	   * Resolve the component constructor to use when creating
	   * the child vm.
	   *
	   * @param {String|Function} value
	   * @param {Function} cb
	   */
	
	  resolveComponent: function resolveComponent(value, cb) {
	    var self = this;
	    this.pendingComponentCb = cancellable(function (Component) {
	      self.ComponentName = Component.options.name || (typeof value === 'string' ? value : null);
	      self.Component = Component;
	      cb();
	    });
	    this.vm._resolveComponent(value, this.pendingComponentCb);
	  },
	
	  /**
	   * Create a new instance using the current constructor and
	   * replace the existing instance. This method doesn't care
	   * whether the new component and the old one are actually
	   * the same.
	   *
	   * @param {Function} [cb]
	   */
	
	  mountComponent: function mountComponent(cb) {
	    // actual mount
	    this.unbuild(true);
	    var self = this;
	    var activateHooks = this.Component.options.activate;
	    var cached = this.getCached();
	    var newComponent = this.build();
	    if (activateHooks && !cached) {
	      this.waitingFor = newComponent;
	      callActivateHooks(activateHooks, newComponent, function () {
	        if (self.waitingFor !== newComponent) {
	          return;
	        }
	        self.waitingFor = null;
	        self.transition(newComponent, cb);
	      });
	    } else {
	      // update ref for kept-alive component
	      if (cached) {
	        newComponent._updateRef();
	      }
	      this.transition(newComponent, cb);
	    }
	  },
	
	  /**
	   * When the component changes or unbinds before an async
	   * constructor is resolved, we need to invalidate its
	   * pending callback.
	   */
	
	  invalidatePending: function invalidatePending() {
	    if (this.pendingComponentCb) {
	      this.pendingComponentCb.cancel();
	      this.pendingComponentCb = null;
	    }
	  },
	
	  /**
	   * Instantiate/insert a new child vm.
	   * If keep alive and has cached instance, insert that
	   * instance; otherwise build a new one and cache it.
	   *
	   * @param {Object} [extraOptions]
	   * @return {Vue} - the created instance
	   */
	
	  build: function build(extraOptions) {
	    var cached = this.getCached();
	    if (cached) {
	      return cached;
	    }
	    if (this.Component) {
	      // default options
	      var options = {
	        name: this.ComponentName,
	        el: cloneNode(this.el),
	        template: this.inlineTemplate,
	        // make sure to add the child with correct parent
	        // if this is a transcluded component, its parent
	        // should be the transclusion host.
	        parent: this._host || this.vm,
	        // if no inline-template, then the compiled
	        // linker can be cached for better performance.
	        _linkerCachable: !this.inlineTemplate,
	        _ref: this.descriptor.ref,
	        _asComponent: true,
	        _isRouterView: this._isRouterView,
	        // if this is a transcluded component, context
	        // will be the common parent vm of this instance
	        // and its host.
	        _context: this.vm,
	        // if this is inside an inline v-for, the scope
	        // will be the intermediate scope created for this
	        // repeat fragment. this is used for linking props
	        // and container directives.
	        _scope: this._scope,
	        // pass in the owner fragment of this component.
	        // this is necessary so that the fragment can keep
	        // track of its contained components in order to
	        // call attach/detach hooks for them.
	        _frag: this._frag
	      };
	      // extra options
	      // in 1.0.0 this is used by vue-router only
	      /* istanbul ignore if */
	      if (extraOptions) {
	        extend(options, extraOptions);
	      }
	      var child = new this.Component(options);
	      if (this.keepAlive) {
	        this.cache[this.Component.cid] = child;
	      }
	      /* istanbul ignore if */
	      if (process.env.NODE_ENV !== 'production' && this.el.hasAttribute('transition') && child._isFragment) {
	        warn('Transitions will not work on a fragment instance. ' + 'Template: ' + child.$options.template, child);
	      }
	      return child;
	    }
	  },
	
	  /**
	   * Try to get a cached instance of the current component.
	   *
	   * @return {Vue|undefined}
	   */
	
	  getCached: function getCached() {
	    return this.keepAlive && this.cache[this.Component.cid];
	  },
	
	  /**
	   * Teardown the current child, but defers cleanup so
	   * that we can separate the destroy and removal steps.
	   *
	   * @param {Boolean} defer
	   */
	
	  unbuild: function unbuild(defer) {
	    if (this.waitingFor) {
	      if (!this.keepAlive) {
	        this.waitingFor.$destroy();
	      }
	      this.waitingFor = null;
	    }
	    var child = this.childVM;
	    if (!child || this.keepAlive) {
	      if (child) {
	        // remove ref
	        child._inactive = true;
	        child._updateRef(true);
	      }
	      return;
	    }
	    // the sole purpose of `deferCleanup` is so that we can
	    // "deactivate" the vm right now and perform DOM removal
	    // later.
	    child.$destroy(false, defer);
	  },
	
	  /**
	   * Remove current destroyed child and manually do
	   * the cleanup after removal.
	   *
	   * @param {Function} cb
	   */
	
	  remove: function remove(child, cb) {
	    var keepAlive = this.keepAlive;
	    if (child) {
	      // we may have a component switch when a previous
	      // component is still being transitioned out.
	      // we want to trigger only one lastest insertion cb
	      // when the existing transition finishes. (#1119)
	      this.pendingRemovals++;
	      this.pendingRemovalCb = cb;
	      var self = this;
	      child.$remove(function () {
	        self.pendingRemovals--;
	        if (!keepAlive) child._cleanup();
	        if (!self.pendingRemovals && self.pendingRemovalCb) {
	          self.pendingRemovalCb();
	          self.pendingRemovalCb = null;
	        }
	      });
	    } else if (cb) {
	      cb();
	    }
	  },
	
	  /**
	   * Actually swap the components, depending on the
	   * transition mode. Defaults to simultaneous.
	   *
	   * @param {Vue} target
	   * @param {Function} [cb]
	   */
	
	  transition: function transition(target, cb) {
	    var self = this;
	    var current = this.childVM;
	    // for devtool inspection
	    if (current) current._inactive = true;
	    target._inactive = false;
	    this.childVM = target;
	    switch (self.params.transitionMode) {
	      case 'in-out':
	        target.$before(self.anchor, function () {
	          self.remove(current, cb);
	        });
	        break;
	      case 'out-in':
	        self.remove(current, function () {
	          target.$before(self.anchor, cb);
	        });
	        break;
	      default:
	        self.remove(current);
	        target.$before(self.anchor, cb);
	    }
	  },
	
	  /**
	   * Unbind.
	   */
	
	  unbind: function unbind() {
	    this.invalidatePending();
	    // Do not defer cleanup when unbinding
	    this.unbuild();
	    // destroy all keep-alive cached instances
	    if (this.cache) {
	      for (var key in this.cache) {
	        this.cache[key].$destroy();
	      }
	      this.cache = null;
	    }
	  }
	};
	
	/**
	 * Call activate hooks in order (asynchronous)
	 *
	 * @param {Array} hooks
	 * @param {Vue} vm
	 * @param {Function} cb
	 */
	
	function callActivateHooks(hooks, vm, cb) {
	  var total = hooks.length;
	  var called = 0;
	  hooks[0].call(vm, next);
	  function next() {
	    if (++called >= total) {
	      cb();
	    } else {
	      hooks[called].call(vm, next);
	    }
	  }
	}
	
	var propBindingModes = config._propBindingModes;
	var empty = {};
	
	// regexes
	var identRE$1 = /^[$_a-zA-Z]+[\w$]*$/;
	var settablePathRE = /^[A-Za-z_$][\w$]*(\.[A-Za-z_$][\w$]*|\[[^\[\]]+\])*$/;
	
	/**
	 * Compile props on a root element and return
	 * a props link function.
	 *
	 * @param {Element|DocumentFragment} el
	 * @param {Array} propOptions
	 * @param {Vue} vm
	 * @return {Function} propsLinkFn
	 */
	
	function compileProps(el, propOptions, vm) {
	  var props = [];
	  var names = Object.keys(propOptions);
	  var i = names.length;
	  var options, name, attr, value, path, parsed, prop;
	  while (i--) {
	    name = names[i];
	    options = propOptions[name] || empty;
	
	    if (process.env.NODE_ENV !== 'production' && name === '$data') {
	      warn('Do not use $data as prop.', vm);
	      continue;
	    }
	
	    // props could contain dashes, which will be
	    // interpreted as minus calculations by the parser
	    // so we need to camelize the path here
	    path = camelize(name);
	    if (!identRE$1.test(path)) {
	      process.env.NODE_ENV !== 'production' && warn('Invalid prop key: "' + name + '". Prop keys ' + 'must be valid identifiers.', vm);
	      continue;
	    }
	
	    prop = {
	      name: name,
	      path: path,
	      options: options,
	      mode: propBindingModes.ONE_WAY,
	      raw: null
	    };
	
	    attr = hyphenate(name);
	    // first check dynamic version
	    if ((value = getBindAttr(el, attr)) === null) {
	      if ((value = getBindAttr(el, attr + '.sync')) !== null) {
	        prop.mode = propBindingModes.TWO_WAY;
	      } else if ((value = getBindAttr(el, attr + '.once')) !== null) {
	        prop.mode = propBindingModes.ONE_TIME;
	      }
	    }
	    if (value !== null) {
	      // has dynamic binding!
	      prop.raw = value;
	      parsed = parseDirective(value);
	      value = parsed.expression;
	      prop.filters = parsed.filters;
	      // check binding type
	      if (isLiteral(value) && !parsed.filters) {
	        // for expressions containing literal numbers and
	        // booleans, there's no need to setup a prop binding,
	        // so we can optimize them as a one-time set.
	        prop.optimizedLiteral = true;
	      } else {
	        prop.dynamic = true;
	        // check non-settable path for two-way bindings
	        if (process.env.NODE_ENV !== 'production' && prop.mode === propBindingModes.TWO_WAY && !settablePathRE.test(value)) {
	          prop.mode = propBindingModes.ONE_WAY;
	          warn('Cannot bind two-way prop with non-settable ' + 'parent path: ' + value, vm);
	        }
	      }
	      prop.parentPath = value;
	
	      // warn required two-way
	      if (process.env.NODE_ENV !== 'production' && options.twoWay && prop.mode !== propBindingModes.TWO_WAY) {
	        warn('Prop "' + name + '" expects a two-way binding type.', vm);
	      }
	    } else if ((value = getAttr(el, attr)) !== null) {
	      // has literal binding!
	      prop.raw = value;
	    } else if (process.env.NODE_ENV !== 'production') {
	      // check possible camelCase prop usage
	      var lowerCaseName = path.toLowerCase();
	      value = /[A-Z\-]/.test(name) && (el.getAttribute(lowerCaseName) || el.getAttribute(':' + lowerCaseName) || el.getAttribute('v-bind:' + lowerCaseName) || el.getAttribute(':' + lowerCaseName + '.once') || el.getAttribute('v-bind:' + lowerCaseName + '.once') || el.getAttribute(':' + lowerCaseName + '.sync') || el.getAttribute('v-bind:' + lowerCaseName + '.sync'));
	      if (value) {
	        warn('Possible usage error for prop `' + lowerCaseName + '` - ' + 'did you mean `' + attr + '`? HTML is case-insensitive, remember to use ' + 'kebab-case for props in templates.', vm);
	      } else if (options.required) {
	        // warn missing required
	        warn('Missing required prop: ' + name, vm);
	      }
	    }
	    // push prop
	    props.push(prop);
	  }
	  return makePropsLinkFn(props);
	}
	
	/**
	 * Build a function that applies props to a vm.
	 *
	 * @param {Array} props
	 * @return {Function} propsLinkFn
	 */
	
	function makePropsLinkFn(props) {
	  return function propsLinkFn(vm, scope) {
	    // store resolved props info
	    vm._props = {};
	    var inlineProps = vm.$options.propsData;
	    var i = props.length;
	    var prop, path, options, value, raw;
	    while (i--) {
	      prop = props[i];
	      raw = prop.raw;
	      path = prop.path;
	      options = prop.options;
	      vm._props[path] = prop;
	      if (inlineProps && hasOwn(inlineProps, path)) {
	        initProp(vm, prop, inlineProps[path]);
	      }if (raw === null) {
	        // initialize absent prop
	        initProp(vm, prop, undefined);
	      } else if (prop.dynamic) {
	        // dynamic prop
	        if (prop.mode === propBindingModes.ONE_TIME) {
	          // one time binding
	          value = (scope || vm._context || vm).$get(prop.parentPath);
	          initProp(vm, prop, value);
	        } else {
	          if (vm._context) {
	            // dynamic binding
	            vm._bindDir({
	              name: 'prop',
	              def: propDef,
	              prop: prop
	            }, null, null, scope); // el, host, scope
	          } else {
	              // root instance
	              initProp(vm, prop, vm.$get(prop.parentPath));
	            }
	        }
	      } else if (prop.optimizedLiteral) {
	        // optimized literal, cast it and just set once
	        var stripped = stripQuotes(raw);
	        value = stripped === raw ? toBoolean(toNumber(raw)) : stripped;
	        initProp(vm, prop, value);
	      } else {
	        // string literal, but we need to cater for
	        // Boolean props with no value, or with same
	        // literal value (e.g. disabled="disabled")
	        // see https://github.com/vuejs/vue-loader/issues/182
	        value = options.type === Boolean && (raw === '' || raw === hyphenate(prop.name)) ? true : raw;
	        initProp(vm, prop, value);
	      }
	    }
	  };
	}
	
	/**
	 * Process a prop with a rawValue, applying necessary coersions,
	 * default values & assertions and call the given callback with
	 * processed value.
	 *
	 * @param {Vue} vm
	 * @param {Object} prop
	 * @param {*} rawValue
	 * @param {Function} fn
	 */
	
	function processPropValue(vm, prop, rawValue, fn) {
	  var isSimple = prop.dynamic && isSimplePath(prop.parentPath);
	  var value = rawValue;
	  if (value === undefined) {
	    value = getPropDefaultValue(vm, prop);
	  }
	  value = coerceProp(prop, value, vm);
	  var coerced = value !== rawValue;
	  if (!assertProp(prop, value, vm)) {
	    value = undefined;
	  }
	  if (isSimple && !coerced) {
	    withoutConversion(function () {
	      fn(value);
	    });
	  } else {
	    fn(value);
	  }
	}
	
	/**
	 * Set a prop's initial value on a vm and its data object.
	 *
	 * @param {Vue} vm
	 * @param {Object} prop
	 * @param {*} value
	 */
	
	function initProp(vm, prop, value) {
	  processPropValue(vm, prop, value, function (value) {
	    defineReactive(vm, prop.path, value);
	  });
	}
	
	/**
	 * Update a prop's value on a vm.
	 *
	 * @param {Vue} vm
	 * @param {Object} prop
	 * @param {*} value
	 */
	
	function updateProp(vm, prop, value) {
	  processPropValue(vm, prop, value, function (value) {
	    vm[prop.path] = value;
	  });
	}
	
	/**
	 * Get the default value of a prop.
	 *
	 * @param {Vue} vm
	 * @param {Object} prop
	 * @return {*}
	 */
	
	function getPropDefaultValue(vm, prop) {
	  // no default, return undefined
	  var options = prop.options;
	  if (!hasOwn(options, 'default')) {
	    // absent boolean value defaults to false
	    return options.type === Boolean ? false : undefined;
	  }
	  var def = options['default'];
	  // warn against non-factory defaults for Object & Array
	  if (isObject(def)) {
	    process.env.NODE_ENV !== 'production' && warn('Invalid default value for prop "' + prop.name + '": ' + 'Props with type Object/Array must use a factory function ' + 'to return the default value.', vm);
	  }
	  // call factory function for non-Function types
	  return typeof def === 'function' && options.type !== Function ? def.call(vm) : def;
	}
	
	/**
	 * Assert whether a prop is valid.
	 *
	 * @param {Object} prop
	 * @param {*} value
	 * @param {Vue} vm
	 */
	
	function assertProp(prop, value, vm) {
	  if (!prop.options.required && ( // non-required
	  prop.raw === null || // abscent
	  value == null) // null or undefined
	  ) {
	      return true;
	    }
	  var options = prop.options;
	  var type = options.type;
	  var valid = !type;
	  var expectedTypes = [];
	  if (type) {
	    if (!isArray(type)) {
	      type = [type];
	    }
	    for (var i = 0; i < type.length && !valid; i++) {
	      var assertedType = assertType(value, type[i]);
	      expectedTypes.push(assertedType.expectedType);
	      valid = assertedType.valid;
	    }
	  }
	  if (!valid) {
	    if (process.env.NODE_ENV !== 'production') {
	      warn('Invalid prop: type check failed for prop "' + prop.name + '".' + ' Expected ' + expectedTypes.map(formatType).join(', ') + ', got ' + formatValue(value) + '.', vm);
	    }
	    return false;
	  }
	  var validator = options.validator;
	  if (validator) {
	    if (!validator(value)) {
	      process.env.NODE_ENV !== 'production' && warn('Invalid prop: custom validator check failed for prop "' + prop.name + '".', vm);
	      return false;
	    }
	  }
	  return true;
	}
	
	/**
	 * Force parsing value with coerce option.
	 *
	 * @param {*} value
	 * @param {Object} options
	 * @return {*}
	 */
	
	function coerceProp(prop, value, vm) {
	  var coerce = prop.options.coerce;
	  if (!coerce) {
	    return value;
	  }
	  if (typeof coerce === 'function') {
	    return coerce(value);
	  } else {
	    process.env.NODE_ENV !== 'production' && warn('Invalid coerce for prop "' + prop.name + '": expected function, got ' + typeof coerce + '.', vm);
	    return value;
	  }
	}
	
	/**
	 * Assert the type of a value
	 *
	 * @param {*} value
	 * @param {Function} type
	 * @return {Object}
	 */
	
	function assertType(value, type) {
	  var valid;
	  var expectedType;
	  if (type === String) {
	    expectedType = 'string';
	    valid = typeof value === expectedType;
	  } else if (type === Number) {
	    expectedType = 'number';
	    valid = typeof value === expectedType;
	  } else if (type === Boolean) {
	    expectedType = 'boolean';
	    valid = typeof value === expectedType;
	  } else if (type === Function) {
	    expectedType = 'function';
	    valid = typeof value === expectedType;
	  } else if (type === Object) {
	    expectedType = 'object';
	    valid = isPlainObject(value);
	  } else if (type === Array) {
	    expectedType = 'array';
	    valid = isArray(value);
	  } else {
	    valid = value instanceof type;
	  }
	  return {
	    valid: valid,
	    expectedType: expectedType
	  };
	}
	
	/**
	 * Format type for output
	 *
	 * @param {String} type
	 * @return {String}
	 */
	
	function formatType(type) {
	  return type ? type.charAt(0).toUpperCase() + type.slice(1) : 'custom type';
	}
	
	/**
	 * Format value
	 *
	 * @param {*} value
	 * @return {String}
	 */
	
	function formatValue(val) {
	  return Object.prototype.toString.call(val).slice(8, -1);
	}
	
	var bindingModes = config._propBindingModes;
	
	var propDef = {
	
	  bind: function bind() {
	    var child = this.vm;
	    var parent = child._context;
	    // passed in from compiler directly
	    var prop = this.descriptor.prop;
	    var childKey = prop.path;
	    var parentKey = prop.parentPath;
	    var twoWay = prop.mode === bindingModes.TWO_WAY;
	
	    var parentWatcher = this.parentWatcher = new Watcher(parent, parentKey, function (val) {
	      updateProp(child, prop, val);
	    }, {
	      twoWay: twoWay,
	      filters: prop.filters,
	      // important: props need to be observed on the
	      // v-for scope if present
	      scope: this._scope
	    });
	
	    // set the child initial value.
	    initProp(child, prop, parentWatcher.value);
	
	    // setup two-way binding
	    if (twoWay) {
	      // important: defer the child watcher creation until
	      // the created hook (after data observation)
	      var self = this;
	      child.$once('pre-hook:created', function () {
	        self.childWatcher = new Watcher(child, childKey, function (val) {
	          parentWatcher.set(val);
	        }, {
	          // ensure sync upward before parent sync down.
	          // this is necessary in cases e.g. the child
	          // mutates a prop array, then replaces it. (#1683)
	          sync: true
	        });
	      });
	    }
	  },
	
	  unbind: function unbind() {
	    this.parentWatcher.teardown();
	    if (this.childWatcher) {
	      this.childWatcher.teardown();
	    }
	  }
	};
	
	var queue$1 = [];
	var queued = false;
	
	/**
	 * Push a job into the queue.
	 *
	 * @param {Function} job
	 */
	
	function pushJob(job) {
	  queue$1.push(job);
	  if (!queued) {
	    queued = true;
	    nextTick(flush);
	  }
	}
	
	/**
	 * Flush the queue, and do one forced reflow before
	 * triggering transitions.
	 */
	
	function flush() {
	  // Force layout
	  var f = document.documentElement.offsetHeight;
	  for (var i = 0; i < queue$1.length; i++) {
	    queue$1[i]();
	  }
	  queue$1 = [];
	  queued = false;
	  // dummy return, so js linters don't complain about
	  // unused variable f
	  return f;
	}
	
	var TYPE_TRANSITION = 'transition';
	var TYPE_ANIMATION = 'animation';
	var transDurationProp = transitionProp + 'Duration';
	var animDurationProp = animationProp + 'Duration';
	
	/**
	 * If a just-entered element is applied the
	 * leave class while its enter transition hasn't started yet,
	 * and the transitioned property has the same value for both
	 * enter/leave, then the leave transition will be skipped and
	 * the transitionend event never fires. This function ensures
	 * its callback to be called after a transition has started
	 * by waiting for double raf.
	 *
	 * It falls back to setTimeout on devices that support CSS
	 * transitions but not raf (e.g. Android 4.2 browser) - since
	 * these environments are usually slow, we are giving it a
	 * relatively large timeout.
	 */
	
	var raf = inBrowser && window.requestAnimationFrame;
	var waitForTransitionStart = raf
	/* istanbul ignore next */
	? function (fn) {
	  raf(function () {
	    raf(fn);
	  });
	} : function (fn) {
	  setTimeout(fn, 50);
	};
	
	/**
	 * A Transition object that encapsulates the state and logic
	 * of the transition.
	 *
	 * @param {Element} el
	 * @param {String} id
	 * @param {Object} hooks
	 * @param {Vue} vm
	 */
	function Transition(el, id, hooks, vm) {
	  this.id = id;
	  this.el = el;
	  this.enterClass = hooks && hooks.enterClass || id + '-enter';
	  this.leaveClass = hooks && hooks.leaveClass || id + '-leave';
	  this.hooks = hooks;
	  this.vm = vm;
	  // async state
	  this.pendingCssEvent = this.pendingCssCb = this.cancel = this.pendingJsCb = this.op = this.cb = null;
	  this.justEntered = false;
	  this.entered = this.left = false;
	  this.typeCache = {};
	  // check css transition type
	  this.type = hooks && hooks.type;
	  /* istanbul ignore if */
	  if (process.env.NODE_ENV !== 'production') {
	    if (this.type && this.type !== TYPE_TRANSITION && this.type !== TYPE_ANIMATION) {
	      warn('invalid CSS transition type for transition="' + this.id + '": ' + this.type, vm);
	    }
	  }
	  // bind
	  var self = this;['enterNextTick', 'enterDone', 'leaveNextTick', 'leaveDone'].forEach(function (m) {
	    self[m] = bind(self[m], self);
	  });
	}
	
	var p$1 = Transition.prototype;
	
	/**
	 * Start an entering transition.
	 *
	 * 1. enter transition triggered
	 * 2. call beforeEnter hook
	 * 3. add enter class
	 * 4. insert/show element
	 * 5. call enter hook (with possible explicit js callback)
	 * 6. reflow
	 * 7. based on transition type:
	 *    - transition:
	 *        remove class now, wait for transitionend,
	 *        then done if there's no explicit js callback.
	 *    - animation:
	 *        wait for animationend, remove class,
	 *        then done if there's no explicit js callback.
	 *    - no css transition:
	 *        done now if there's no explicit js callback.
	 * 8. wait for either done or js callback, then call
	 *    afterEnter hook.
	 *
	 * @param {Function} op - insert/show the element
	 * @param {Function} [cb]
	 */
	
	p$1.enter = function (op, cb) {
	  this.cancelPending();
	  this.callHook('beforeEnter');
	  this.cb = cb;
	  addClass(this.el, this.enterClass);
	  op();
	  this.entered = false;
	  this.callHookWithCb('enter');
	  if (this.entered) {
	    return; // user called done synchronously.
	  }
	  this.cancel = this.hooks && this.hooks.enterCancelled;
	  pushJob(this.enterNextTick);
	};
	
	/**
	 * The "nextTick" phase of an entering transition, which is
	 * to be pushed into a queue and executed after a reflow so
	 * that removing the class can trigger a CSS transition.
	 */
	
	p$1.enterNextTick = function () {
	  var _this = this;
	
	  // prevent transition skipping
	  this.justEntered = true;
	  waitForTransitionStart(function () {
	    _this.justEntered = false;
	  });
	  var enterDone = this.enterDone;
	  var type = this.getCssTransitionType(this.enterClass);
	  if (!this.pendingJsCb) {
	    if (type === TYPE_TRANSITION) {
	      // trigger transition by removing enter class now
	      removeClass(this.el, this.enterClass);
	      this.setupCssCb(transitionEndEvent, enterDone);
	    } else if (type === TYPE_ANIMATION) {
	      this.setupCssCb(animationEndEvent, enterDone);
	    } else {
	      enterDone();
	    }
	  } else if (type === TYPE_TRANSITION) {
	    removeClass(this.el, this.enterClass);
	  }
	};
	
	/**
	 * The "cleanup" phase of an entering transition.
	 */
	
	p$1.enterDone = function () {
	  this.entered = true;
	  this.cancel = this.pendingJsCb = null;
	  removeClass(this.el, this.enterClass);
	  this.callHook('afterEnter');
	  if (this.cb) this.cb();
	};
	
	/**
	 * Start a leaving transition.
	 *
	 * 1. leave transition triggered.
	 * 2. call beforeLeave hook
	 * 3. add leave class (trigger css transition)
	 * 4. call leave hook (with possible explicit js callback)
	 * 5. reflow if no explicit js callback is provided
	 * 6. based on transition type:
	 *    - transition or animation:
	 *        wait for end event, remove class, then done if
	 *        there's no explicit js callback.
	 *    - no css transition:
	 *        done if there's no explicit js callback.
	 * 7. wait for either done or js callback, then call
	 *    afterLeave hook.
	 *
	 * @param {Function} op - remove/hide the element
	 * @param {Function} [cb]
	 */
	
	p$1.leave = function (op, cb) {
	  this.cancelPending();
	  this.callHook('beforeLeave');
	  this.op = op;
	  this.cb = cb;
	  addClass(this.el, this.leaveClass);
	  this.left = false;
	  this.callHookWithCb('leave');
	  if (this.left) {
	    return; // user called done synchronously.
	  }
	  this.cancel = this.hooks && this.hooks.leaveCancelled;
	  // only need to handle leaveDone if
	  // 1. the transition is already done (synchronously called
	  //    by the user, which causes this.op set to null)
	  // 2. there's no explicit js callback
	  if (this.op && !this.pendingJsCb) {
	    // if a CSS transition leaves immediately after enter,
	    // the transitionend event never fires. therefore we
	    // detect such cases and end the leave immediately.
	    if (this.justEntered) {
	      this.leaveDone();
	    } else {
	      pushJob(this.leaveNextTick);
	    }
	  }
	};
	
	/**
	 * The "nextTick" phase of a leaving transition.
	 */
	
	p$1.leaveNextTick = function () {
	  var type = this.getCssTransitionType(this.leaveClass);
	  if (type) {
	    var event = type === TYPE_TRANSITION ? transitionEndEvent : animationEndEvent;
	    this.setupCssCb(event, this.leaveDone);
	  } else {
	    this.leaveDone();
	  }
	};
	
	/**
	 * The "cleanup" phase of a leaving transition.
	 */
	
	p$1.leaveDone = function () {
	  this.left = true;
	  this.cancel = this.pendingJsCb = null;
	  this.op();
	  removeClass(this.el, this.leaveClass);
	  this.callHook('afterLeave');
	  if (this.cb) this.cb();
	  this.op = null;
	};
	
	/**
	 * Cancel any pending callbacks from a previously running
	 * but not finished transition.
	 */
	
	p$1.cancelPending = function () {
	  this.op = this.cb = null;
	  var hasPending = false;
	  if (this.pendingCssCb) {
	    hasPending = true;
	    off(this.el, this.pendingCssEvent, this.pendingCssCb);
	    this.pendingCssEvent = this.pendingCssCb = null;
	  }
	  if (this.pendingJsCb) {
	    hasPending = true;
	    this.pendingJsCb.cancel();
	    this.pendingJsCb = null;
	  }
	  if (hasPending) {
	    removeClass(this.el, this.enterClass);
	    removeClass(this.el, this.leaveClass);
	  }
	  if (this.cancel) {
	    this.cancel.call(this.vm, this.el);
	    this.cancel = null;
	  }
	};
	
	/**
	 * Call a user-provided synchronous hook function.
	 *
	 * @param {String} type
	 */
	
	p$1.callHook = function (type) {
	  if (this.hooks && this.hooks[type]) {
	    this.hooks[type].call(this.vm, this.el);
	  }
	};
	
	/**
	 * Call a user-provided, potentially-async hook function.
	 * We check for the length of arguments to see if the hook
	 * expects a `done` callback. If true, the transition's end
	 * will be determined by when the user calls that callback;
	 * otherwise, the end is determined by the CSS transition or
	 * animation.
	 *
	 * @param {String} type
	 */
	
	p$1.callHookWithCb = function (type) {
	  var hook = this.hooks && this.hooks[type];
	  if (hook) {
	    if (hook.length > 1) {
	      this.pendingJsCb = cancellable(this[type + 'Done']);
	    }
	    hook.call(this.vm, this.el, this.pendingJsCb);
	  }
	};
	
	/**
	 * Get an element's transition type based on the
	 * calculated styles.
	 *
	 * @param {String} className
	 * @return {Number}
	 */
	
	p$1.getCssTransitionType = function (className) {
	  /* istanbul ignore if */
	  if (!transitionEndEvent ||
	  // skip CSS transitions if page is not visible -
	  // this solves the issue of transitionend events not
	  // firing until the page is visible again.
	  // pageVisibility API is supported in IE10+, same as
	  // CSS transitions.
	  document.hidden ||
	  // explicit js-only transition
	  this.hooks && this.hooks.css === false ||
	  // element is hidden
	  isHidden(this.el)) {
	    return;
	  }
	  var type = this.type || this.typeCache[className];
	  if (type) return type;
	  var inlineStyles = this.el.style;
	  var computedStyles = window.getComputedStyle(this.el);
	  var transDuration = inlineStyles[transDurationProp] || computedStyles[transDurationProp];
	  if (transDuration && transDuration !== '0s') {
	    type = TYPE_TRANSITION;
	  } else {
	    var animDuration = inlineStyles[animDurationProp] || computedStyles[animDurationProp];
	    if (animDuration && animDuration !== '0s') {
	      type = TYPE_ANIMATION;
	    }
	  }
	  if (type) {
	    this.typeCache[className] = type;
	  }
	  return type;
	};
	
	/**
	 * Setup a CSS transitionend/animationend callback.
	 *
	 * @param {String} event
	 * @param {Function} cb
	 */
	
	p$1.setupCssCb = function (event, cb) {
	  this.pendingCssEvent = event;
	  var self = this;
	  var el = this.el;
	  var onEnd = this.pendingCssCb = function (e) {
	    if (e.target === el) {
	      off(el, event, onEnd);
	      self.pendingCssEvent = self.pendingCssCb = null;
	      if (!self.pendingJsCb && cb) {
	        cb();
	      }
	    }
	  };
	  on(el, event, onEnd);
	};
	
	/**
	 * Check if an element is hidden - in that case we can just
	 * skip the transition alltogether.
	 *
	 * @param {Element} el
	 * @return {Boolean}
	 */
	
	function isHidden(el) {
	  if (/svg$/.test(el.namespaceURI)) {
	    // SVG elements do not have offset(Width|Height)
	    // so we need to check the client rect
	    var rect = el.getBoundingClientRect();
	    return !(rect.width || rect.height);
	  } else {
	    return !(el.offsetWidth || el.offsetHeight || el.getClientRects().length);
	  }
	}
	
	var transition$1 = {
	
	  priority: TRANSITION,
	
	  update: function update(id, oldId) {
	    var el = this.el;
	    // resolve on owner vm
	    var hooks = resolveAsset(this.vm.$options, 'transitions', id);
	    id = id || 'v';
	    oldId = oldId || 'v';
	    el.__v_trans = new Transition(el, id, hooks, this.vm);
	    removeClass(el, oldId + '-transition');
	    addClass(el, id + '-transition');
	  }
	};
	
	var internalDirectives = {
	  style: style,
	  'class': vClass,
	  component: component,
	  prop: propDef,
	  transition: transition$1
	};
	
	// special binding prefixes
	var bindRE = /^v-bind:|^:/;
	var onRE = /^v-on:|^@/;
	var dirAttrRE = /^v-([^:]+)(?:$|:(.*)$)/;
	var modifierRE = /\.[^\.]+/g;
	var transitionRE = /^(v-bind:|:)?transition$/;
	
	// default directive priority
	var DEFAULT_PRIORITY = 1000;
	var DEFAULT_TERMINAL_PRIORITY = 2000;
	
	/**
	 * Compile a template and return a reusable composite link
	 * function, which recursively contains more link functions
	 * inside. This top level compile function would normally
	 * be called on instance root nodes, but can also be used
	 * for partial compilation if the partial argument is true.
	 *
	 * The returned composite link function, when called, will
	 * return an unlink function that tearsdown all directives
	 * created during the linking phase.
	 *
	 * @param {Element|DocumentFragment} el
	 * @param {Object} options
	 * @param {Boolean} partial
	 * @return {Function}
	 */
	
	function compile(el, options, partial) {
	  // link function for the node itself.
	  var nodeLinkFn = partial || !options._asComponent ? compileNode(el, options) : null;
	  // link function for the childNodes
	  var childLinkFn = !(nodeLinkFn && nodeLinkFn.terminal) && !isScript(el) && el.hasChildNodes() ? compileNodeList(el.childNodes, options) : null;
	
	  /**
	   * A composite linker function to be called on a already
	   * compiled piece of DOM, which instantiates all directive
	   * instances.
	   *
	   * @param {Vue} vm
	   * @param {Element|DocumentFragment} el
	   * @param {Vue} [host] - host vm of transcluded content
	   * @param {Object} [scope] - v-for scope
	   * @param {Fragment} [frag] - link context fragment
	   * @return {Function|undefined}
	   */
	
	  return function compositeLinkFn(vm, el, host, scope, frag) {
	    // cache childNodes before linking parent, fix #657
	    var childNodes = toArray(el.childNodes);
	    // link
	    var dirs = linkAndCapture(function compositeLinkCapturer() {
	      if (nodeLinkFn) nodeLinkFn(vm, el, host, scope, frag);
	      if (childLinkFn) childLinkFn(vm, childNodes, host, scope, frag);
	    }, vm);
	    return makeUnlinkFn(vm, dirs);
	  };
	}
	
	/**
	 * Apply a linker to a vm/element pair and capture the
	 * directives created during the process.
	 *
	 * @param {Function} linker
	 * @param {Vue} vm
	 */
	
	function linkAndCapture(linker, vm) {
	  /* istanbul ignore if */
	  if (process.env.NODE_ENV === 'production') {
	    // reset directives before every capture in production
	    // mode, so that when unlinking we don't need to splice
	    // them out (which turns out to be a perf hit).
	    // they are kept in development mode because they are
	    // useful for Vue's own tests.
	    vm._directives = [];
	  }
	  var originalDirCount = vm._directives.length;
	  linker();
	  var dirs = vm._directives.slice(originalDirCount);
	  dirs.sort(directiveComparator);
	  for (var i = 0, l = dirs.length; i < l; i++) {
	    dirs[i]._bind();
	  }
	  return dirs;
	}
	
	/**
	 * Directive priority sort comparator
	 *
	 * @param {Object} a
	 * @param {Object} b
	 */
	
	function directiveComparator(a, b) {
	  a = a.descriptor.def.priority || DEFAULT_PRIORITY;
	  b = b.descriptor.def.priority || DEFAULT_PRIORITY;
	  return a > b ? -1 : a === b ? 0 : 1;
	}
	
	/**
	 * Linker functions return an unlink function that
	 * tearsdown all directives instances generated during
	 * the process.
	 *
	 * We create unlink functions with only the necessary
	 * information to avoid retaining additional closures.
	 *
	 * @param {Vue} vm
	 * @param {Array} dirs
	 * @param {Vue} [context]
	 * @param {Array} [contextDirs]
	 * @return {Function}
	 */
	
	function makeUnlinkFn(vm, dirs, context, contextDirs) {
	  function unlink(destroying) {
	    teardownDirs(vm, dirs, destroying);
	    if (context && contextDirs) {
	      teardownDirs(context, contextDirs);
	    }
	  }
	  // expose linked directives
	  unlink.dirs = dirs;
	  return unlink;
	}
	
	/**
	 * Teardown partial linked directives.
	 *
	 * @param {Vue} vm
	 * @param {Array} dirs
	 * @param {Boolean} destroying
	 */
	
	function teardownDirs(vm, dirs, destroying) {
	  var i = dirs.length;
	  while (i--) {
	    dirs[i]._teardown();
	    if (process.env.NODE_ENV !== 'production' && !destroying) {
	      vm._directives.$remove(dirs[i]);
	    }
	  }
	}
	
	/**
	 * Compile link props on an instance.
	 *
	 * @param {Vue} vm
	 * @param {Element} el
	 * @param {Object} props
	 * @param {Object} [scope]
	 * @return {Function}
	 */
	
	function compileAndLinkProps(vm, el, props, scope) {
	  var propsLinkFn = compileProps(el, props, vm);
	  var propDirs = linkAndCapture(function () {
	    propsLinkFn(vm, scope);
	  }, vm);
	  return makeUnlinkFn(vm, propDirs);
	}
	
	/**
	 * Compile the root element of an instance.
	 *
	 * 1. attrs on context container (context scope)
	 * 2. attrs on the component template root node, if
	 *    replace:true (child scope)
	 *
	 * If this is a fragment instance, we only need to compile 1.
	 *
	 * @param {Element} el
	 * @param {Object} options
	 * @param {Object} contextOptions
	 * @return {Function}
	 */
	
	function compileRoot(el, options, contextOptions) {
	  var containerAttrs = options._containerAttrs;
	  var replacerAttrs = options._replacerAttrs;
	  var contextLinkFn, replacerLinkFn;
	
	  // only need to compile other attributes for
	  // non-fragment instances
	  if (el.nodeType !== 11) {
	    // for components, container and replacer need to be
	    // compiled separately and linked in different scopes.
	    if (options._asComponent) {
	      // 2. container attributes
	      if (containerAttrs && contextOptions) {
	        contextLinkFn = compileDirectives(containerAttrs, contextOptions);
	      }
	      if (replacerAttrs) {
	        // 3. replacer attributes
	        replacerLinkFn = compileDirectives(replacerAttrs, options);
	      }
	    } else {
	      // non-component, just compile as a normal element.
	      replacerLinkFn = compileDirectives(el.attributes, options);
	    }
	  } else if (process.env.NODE_ENV !== 'production' && containerAttrs) {
	    // warn container directives for fragment instances
	    var names = containerAttrs.filter(function (attr) {
	      // allow vue-loader/vueify scoped css attributes
	      return attr.name.indexOf('_v-') < 0 &&
	      // allow event listeners
	      !onRE.test(attr.name) &&
	      // allow slots
	      attr.name !== 'slot';
	    }).map(function (attr) {
	      return '"' + attr.name + '"';
	    });
	    if (names.length) {
	      var plural = names.length > 1;
	      warn('Attribute' + (plural ? 's ' : ' ') + names.join(', ') + (plural ? ' are' : ' is') + ' ignored on component ' + '<' + options.el.tagName.toLowerCase() + '> because ' + 'the component is a fragment instance: ' + 'http://vuejs.org/guide/components.html#Fragment-Instance');
	    }
	  }
	
	  options._containerAttrs = options._replacerAttrs = null;
	  return function rootLinkFn(vm, el, scope) {
	    // link context scope dirs
	    var context = vm._context;
	    var contextDirs;
	    if (context && contextLinkFn) {
	      contextDirs = linkAndCapture(function () {
	        contextLinkFn(context, el, null, scope);
	      }, context);
	    }
	
	    // link self
	    var selfDirs = linkAndCapture(function () {
	      if (replacerLinkFn) replacerLinkFn(vm, el);
	    }, vm);
	
	    // return the unlink function that tearsdown context
	    // container directives.
	    return makeUnlinkFn(vm, selfDirs, context, contextDirs);
	  };
	}
	
	/**
	 * Compile a node and return a nodeLinkFn based on the
	 * node type.
	 *
	 * @param {Node} node
	 * @param {Object} options
	 * @return {Function|null}
	 */
	
	function compileNode(node, options) {
	  var type = node.nodeType;
	  if (type === 1 && !isScript(node)) {
	    return compileElement(node, options);
	  } else if (type === 3 && node.data.trim()) {
	    return compileTextNode(node, options);
	  } else {
	    return null;
	  }
	}
	
	/**
	 * Compile an element and return a nodeLinkFn.
	 *
	 * @param {Element} el
	 * @param {Object} options
	 * @return {Function|null}
	 */
	
	function compileElement(el, options) {
	  // preprocess textareas.
	  // textarea treats its text content as the initial value.
	  // just bind it as an attr directive for value.
	  if (el.tagName === 'TEXTAREA') {
	    var tokens = parseText(el.value);
	    if (tokens) {
	      el.setAttribute(':value', tokensToExp(tokens));
	      el.value = '';
	    }
	  }
	  var linkFn;
	  var hasAttrs = el.hasAttributes();
	  var attrs = hasAttrs && toArray(el.attributes);
	  // check terminal directives (for & if)
	  if (hasAttrs) {
	    linkFn = checkTerminalDirectives(el, attrs, options);
	  }
	  // check element directives
	  if (!linkFn) {
	    linkFn = checkElementDirectives(el, options);
	  }
	  // check component
	  if (!linkFn) {
	    linkFn = checkComponent(el, options);
	  }
	  // normal directives
	  if (!linkFn && hasAttrs) {
	    linkFn = compileDirectives(attrs, options);
	  }
	  return linkFn;
	}
	
	/**
	 * Compile a textNode and return a nodeLinkFn.
	 *
	 * @param {TextNode} node
	 * @param {Object} options
	 * @return {Function|null} textNodeLinkFn
	 */
	
	function compileTextNode(node, options) {
	  // skip marked text nodes
	  if (node._skip) {
	    return removeText;
	  }
	
	  var tokens = parseText(node.wholeText);
	  if (!tokens) {
	    return null;
	  }
	
	  // mark adjacent text nodes as skipped,
	  // because we are using node.wholeText to compile
	  // all adjacent text nodes together. This fixes
	  // issues in IE where sometimes it splits up a single
	  // text node into multiple ones.
	  var next = node.nextSibling;
	  while (next && next.nodeType === 3) {
	    next._skip = true;
	    next = next.nextSibling;
	  }
	
	  var frag = document.createDocumentFragment();
	  var el, token;
	  for (var i = 0, l = tokens.length; i < l; i++) {
	    token = tokens[i];
	    el = token.tag ? processTextToken(token, options) : document.createTextNode(token.value);
	    frag.appendChild(el);
	  }
	  return makeTextNodeLinkFn(tokens, frag, options);
	}
	
	/**
	 * Linker for an skipped text node.
	 *
	 * @param {Vue} vm
	 * @param {Text} node
	 */
	
	function removeText(vm, node) {
	  remove(node);
	}
	
	/**
	 * Process a single text token.
	 *
	 * @param {Object} token
	 * @param {Object} options
	 * @return {Node}
	 */
	
	function processTextToken(token, options) {
	  var el;
	  if (token.oneTime) {
	    el = document.createTextNode(token.value);
	  } else {
	    if (token.html) {
	      el = document.createComment('v-html');
	      setTokenType('html');
	    } else {
	      // IE will clean up empty textNodes during
	      // frag.cloneNode(true), so we have to give it
	      // something here...
	      el = document.createTextNode(' ');
	      setTokenType('text');
	    }
	  }
	  function setTokenType(type) {
	    if (token.descriptor) return;
	    var parsed = parseDirective(token.value);
	    token.descriptor = {
	      name: type,
	      def: directives[type],
	      expression: parsed.expression,
	      filters: parsed.filters
	    };
	  }
	  return el;
	}
	
	/**
	 * Build a function that processes a textNode.
	 *
	 * @param {Array<Object>} tokens
	 * @param {DocumentFragment} frag
	 */
	
	function makeTextNodeLinkFn(tokens, frag) {
	  return function textNodeLinkFn(vm, el, host, scope) {
	    var fragClone = frag.cloneNode(true);
	    var childNodes = toArray(fragClone.childNodes);
	    var token, value, node;
	    for (var i = 0, l = tokens.length; i < l; i++) {
	      token = tokens[i];
	      value = token.value;
	      if (token.tag) {
	        node = childNodes[i];
	        if (token.oneTime) {
	          value = (scope || vm).$eval(value);
	          if (token.html) {
	            replace(node, parseTemplate(value, true));
	          } else {
	            node.data = _toString(value);
	          }
	        } else {
	          vm._bindDir(token.descriptor, node, host, scope);
	        }
	      }
	    }
	    replace(el, fragClone);
	  };
	}
	
	/**
	 * Compile a node list and return a childLinkFn.
	 *
	 * @param {NodeList} nodeList
	 * @param {Object} options
	 * @return {Function|undefined}
	 */
	
	function compileNodeList(nodeList, options) {
	  var linkFns = [];
	  var nodeLinkFn, childLinkFn, node;
	  for (var i = 0, l = nodeList.length; i < l; i++) {
	    node = nodeList[i];
	    nodeLinkFn = compileNode(node, options);
	    childLinkFn = !(nodeLinkFn && nodeLinkFn.terminal) && node.tagName !== 'SCRIPT' && node.hasChildNodes() ? compileNodeList(node.childNodes, options) : null;
	    linkFns.push(nodeLinkFn, childLinkFn);
	  }
	  return linkFns.length ? makeChildLinkFn(linkFns) : null;
	}
	
	/**
	 * Make a child link function for a node's childNodes.
	 *
	 * @param {Array<Function>} linkFns
	 * @return {Function} childLinkFn
	 */
	
	function makeChildLinkFn(linkFns) {
	  return function childLinkFn(vm, nodes, host, scope, frag) {
	    var node, nodeLinkFn, childrenLinkFn;
	    for (var i = 0, n = 0, l = linkFns.length; i < l; n++) {
	      node = nodes[n];
	      nodeLinkFn = linkFns[i++];
	      childrenLinkFn = linkFns[i++];
	      // cache childNodes before linking parent, fix #657
	      var childNodes = toArray(node.childNodes);
	      if (nodeLinkFn) {
	        nodeLinkFn(vm, node, host, scope, frag);
	      }
	      if (childrenLinkFn) {
	        childrenLinkFn(vm, childNodes, host, scope, frag);
	      }
	    }
	  };
	}
	
	/**
	 * Check for element directives (custom elements that should
	 * be resovled as terminal directives).
	 *
	 * @param {Element} el
	 * @param {Object} options
	 */
	
	function checkElementDirectives(el, options) {
	  var tag = el.tagName.toLowerCase();
	  if (commonTagRE.test(tag)) {
	    return;
	  }
	  var def = resolveAsset(options, 'elementDirectives', tag);
	  if (def) {
	    return makeTerminalNodeLinkFn(el, tag, '', options, def);
	  }
	}
	
	/**
	 * Check if an element is a component. If yes, return
	 * a component link function.
	 *
	 * @param {Element} el
	 * @param {Object} options
	 * @return {Function|undefined}
	 */
	
	function checkComponent(el, options) {
	  var component = checkComponentAttr(el, options);
	  if (component) {
	    var ref = findRef(el);
	    var descriptor = {
	      name: 'component',
	      ref: ref,
	      expression: component.id,
	      def: internalDirectives.component,
	      modifiers: {
	        literal: !component.dynamic
	      }
	    };
	    var componentLinkFn = function componentLinkFn(vm, el, host, scope, frag) {
	      if (ref) {
	        defineReactive((scope || vm).$refs, ref, null);
	      }
	      vm._bindDir(descriptor, el, host, scope, frag);
	    };
	    componentLinkFn.terminal = true;
	    return componentLinkFn;
	  }
	}
	
	/**
	 * Check an element for terminal directives in fixed order.
	 * If it finds one, return a terminal link function.
	 *
	 * @param {Element} el
	 * @param {Array} attrs
	 * @param {Object} options
	 * @return {Function} terminalLinkFn
	 */
	
	function checkTerminalDirectives(el, attrs, options) {
	  // skip v-pre
	  if (getAttr(el, 'v-pre') !== null) {
	    return skip;
	  }
	  // skip v-else block, but only if following v-if
	  if (el.hasAttribute('v-else')) {
	    var prev = el.previousElementSibling;
	    if (prev && prev.hasAttribute('v-if')) {
	      return skip;
	    }
	  }
	
	  var attr, name, value, modifiers, matched, dirName, rawName, arg, def, termDef;
	  for (var i = 0, j = attrs.length; i < j; i++) {
	    attr = attrs[i];
	    name = attr.name.replace(modifierRE, '');
	    if (matched = name.match(dirAttrRE)) {
	      def = resolveAsset(options, 'directives', matched[1]);
	      if (def && def.terminal) {
	        if (!termDef || (def.priority || DEFAULT_TERMINAL_PRIORITY) > termDef.priority) {
	          termDef = def;
	          rawName = attr.name;
	          modifiers = parseModifiers(attr.name);
	          value = attr.value;
	          dirName = matched[1];
	          arg = matched[2];
	        }
	      }
	    }
	  }
	
	  if (termDef) {
	    return makeTerminalNodeLinkFn(el, dirName, value, options, termDef, rawName, arg, modifiers);
	  }
	}
	
	function skip() {}
	skip.terminal = true;
	
	/**
	 * Build a node link function for a terminal directive.
	 * A terminal link function terminates the current
	 * compilation recursion and handles compilation of the
	 * subtree in the directive.
	 *
	 * @param {Element} el
	 * @param {String} dirName
	 * @param {String} value
	 * @param {Object} options
	 * @param {Object} def
	 * @param {String} [rawName]
	 * @param {String} [arg]
	 * @param {Object} [modifiers]
	 * @return {Function} terminalLinkFn
	 */
	
	function makeTerminalNodeLinkFn(el, dirName, value, options, def, rawName, arg, modifiers) {
	  var parsed = parseDirective(value);
	  var descriptor = {
	    name: dirName,
	    arg: arg,
	    expression: parsed.expression,
	    filters: parsed.filters,
	    raw: value,
	    attr: rawName,
	    modifiers: modifiers,
	    def: def
	  };
	  // check ref for v-for and router-view
	  if (dirName === 'for' || dirName === 'router-view') {
	    descriptor.ref = findRef(el);
	  }
	  var fn = function terminalNodeLinkFn(vm, el, host, scope, frag) {
	    if (descriptor.ref) {
	      defineReactive((scope || vm).$refs, descriptor.ref, null);
	    }
	    vm._bindDir(descriptor, el, host, scope, frag);
	  };
	  fn.terminal = true;
	  return fn;
	}
	
	/**
	 * Compile the directives on an element and return a linker.
	 *
	 * @param {Array|NamedNodeMap} attrs
	 * @param {Object} options
	 * @return {Function}
	 */
	
	function compileDirectives(attrs, options) {
	  var i = attrs.length;
	  var dirs = [];
	  var attr, name, value, rawName, rawValue, dirName, arg, modifiers, dirDef, tokens, matched;
	  while (i--) {
	    attr = attrs[i];
	    name = rawName = attr.name;
	    value = rawValue = attr.value;
	    tokens = parseText(value);
	    // reset arg
	    arg = null;
	    // check modifiers
	    modifiers = parseModifiers(name);
	    name = name.replace(modifierRE, '');
	
	    // attribute interpolations
	    if (tokens) {
	      value = tokensToExp(tokens);
	      arg = name;
	      pushDir('bind', directives.bind, tokens);
	      // warn against mixing mustaches with v-bind
	      if (process.env.NODE_ENV !== 'production') {
	        if (name === 'class' && Array.prototype.some.call(attrs, function (attr) {
	          return attr.name === ':class' || attr.name === 'v-bind:class';
	        })) {
	          warn('class="' + rawValue + '": Do not mix mustache interpolation ' + 'and v-bind for "class" on the same element. Use one or the other.', options);
	        }
	      }
	    } else
	
	      // special attribute: transition
	      if (transitionRE.test(name)) {
	        modifiers.literal = !bindRE.test(name);
	        pushDir('transition', internalDirectives.transition);
	      } else
	
	        // event handlers
	        if (onRE.test(name)) {
	          arg = name.replace(onRE, '');
	          pushDir('on', directives.on);
	        } else
	
	          // attribute bindings
	          if (bindRE.test(name)) {
	            dirName = name.replace(bindRE, '');
	            if (dirName === 'style' || dirName === 'class') {
	              pushDir(dirName, internalDirectives[dirName]);
	            } else {
	              arg = dirName;
	              pushDir('bind', directives.bind);
	            }
	          } else
	
	            // normal directives
	            if (matched = name.match(dirAttrRE)) {
	              dirName = matched[1];
	              arg = matched[2];
	
	              // skip v-else (when used with v-show)
	              if (dirName === 'else') {
	                continue;
	              }
	
	              dirDef = resolveAsset(options, 'directives', dirName, true);
	              if (dirDef) {
	                pushDir(dirName, dirDef);
	              }
	            }
	  }
	
	  /**
	   * Push a directive.
	   *
	   * @param {String} dirName
	   * @param {Object|Function} def
	   * @param {Array} [interpTokens]
	   */
	
	  function pushDir(dirName, def, interpTokens) {
	    var hasOneTimeToken = interpTokens && hasOneTime(interpTokens);
	    var parsed = !hasOneTimeToken && parseDirective(value);
	    dirs.push({
	      name: dirName,
	      attr: rawName,
	      raw: rawValue,
	      def: def,
	      arg: arg,
	      modifiers: modifiers,
	      // conversion from interpolation strings with one-time token
	      // to expression is differed until directive bind time so that we
	      // have access to the actual vm context for one-time bindings.
	      expression: parsed && parsed.expression,
	      filters: parsed && parsed.filters,
	      interp: interpTokens,
	      hasOneTime: hasOneTimeToken
	    });
	  }
	
	  if (dirs.length) {
	    return makeNodeLinkFn(dirs);
	  }
	}
	
	/**
	 * Parse modifiers from directive attribute name.
	 *
	 * @param {String} name
	 * @return {Object}
	 */
	
	function parseModifiers(name) {
	  var res = Object.create(null);
	  var match = name.match(modifierRE);
	  if (match) {
	    var i = match.length;
	    while (i--) {
	      res[match[i].slice(1)] = true;
	    }
	  }
	  return res;
	}
	
	/**
	 * Build a link function for all directives on a single node.
	 *
	 * @param {Array} directives
	 * @return {Function} directivesLinkFn
	 */
	
	function makeNodeLinkFn(directives) {
	  return function nodeLinkFn(vm, el, host, scope, frag) {
	    // reverse apply because it's sorted low to high
	    var i = directives.length;
	    while (i--) {
	      vm._bindDir(directives[i], el, host, scope, frag);
	    }
	  };
	}
	
	/**
	 * Check if an interpolation string contains one-time tokens.
	 *
	 * @param {Array} tokens
	 * @return {Boolean}
	 */
	
	function hasOneTime(tokens) {
	  var i = tokens.length;
	  while (i--) {
	    if (tokens[i].oneTime) return true;
	  }
	}
	
	function isScript(el) {
	  return el.tagName === 'SCRIPT' && (!el.hasAttribute('type') || el.getAttribute('type') === 'text/javascript');
	}
	
	var specialCharRE = /[^\w\-:\.]/;
	
	/**
	 * Process an element or a DocumentFragment based on a
	 * instance option object. This allows us to transclude
	 * a template node/fragment before the instance is created,
	 * so the processed fragment can then be cloned and reused
	 * in v-for.
	 *
	 * @param {Element} el
	 * @param {Object} options
	 * @return {Element|DocumentFragment}
	 */
	
	function transclude(el, options) {
	  // extract container attributes to pass them down
	  // to compiler, because they need to be compiled in
	  // parent scope. we are mutating the options object here
	  // assuming the same object will be used for compile
	  // right after this.
	  if (options) {
	    options._containerAttrs = extractAttrs(el);
	  }
	  // for template tags, what we want is its content as
	  // a documentFragment (for fragment instances)
	  if (isTemplate(el)) {
	    el = parseTemplate(el);
	  }
	  if (options) {
	    if (options._asComponent && !options.template) {
	      options.template = '<slot></slot>';
	    }
	    if (options.template) {
	      options._content = extractContent(el);
	      el = transcludeTemplate(el, options);
	    }
	  }
	  if (isFragment(el)) {
	    // anchors for fragment instance
	    // passing in `persist: true` to avoid them being
	    // discarded by IE during template cloning
	    prepend(createAnchor('v-start', true), el);
	    el.appendChild(createAnchor('v-end', true));
	  }
	  return el;
	}
	
	/**
	 * Process the template option.
	 * If the replace option is true this will swap the $el.
	 *
	 * @param {Element} el
	 * @param {Object} options
	 * @return {Element|DocumentFragment}
	 */
	
	function transcludeTemplate(el, options) {
	  var template = options.template;
	  var frag = parseTemplate(template, true);
	  if (frag) {
	    var replacer = frag.firstChild;
	    var tag = replacer.tagName && replacer.tagName.toLowerCase();
	    if (options.replace) {
	      /* istanbul ignore if */
	      if (el === document.body) {
	        process.env.NODE_ENV !== 'production' && warn('You are mounting an instance with a template to ' + '<body>. This will replace <body> entirely. You ' + 'should probably use `replace: false` here.');
	      }
	      // there are many cases where the instance must
	      // become a fragment instance: basically anything that
	      // can create more than 1 root nodes.
	      if (
	      // multi-children template
	      frag.childNodes.length > 1 ||
	      // non-element template
	      replacer.nodeType !== 1 ||
	      // single nested component
	      tag === 'component' || resolveAsset(options, 'components', tag) || hasBindAttr(replacer, 'is') ||
	      // element directive
	      resolveAsset(options, 'elementDirectives', tag) ||
	      // for block
	      replacer.hasAttribute('v-for') ||
	      // if block
	      replacer.hasAttribute('v-if')) {
	        return frag;
	      } else {
	        options._replacerAttrs = extractAttrs(replacer);
	        mergeAttrs(el, replacer);
	        return replacer;
	      }
	    } else {
	      el.appendChild(frag);
	      return el;
	    }
	  } else {
	    process.env.NODE_ENV !== 'production' && warn('Invalid template option: ' + template);
	  }
	}
	
	/**
	 * Helper to extract a component container's attributes
	 * into a plain object array.
	 *
	 * @param {Element} el
	 * @return {Array}
	 */
	
	function extractAttrs(el) {
	  if (el.nodeType === 1 && el.hasAttributes()) {
	    return toArray(el.attributes);
	  }
	}
	
	/**
	 * Merge the attributes of two elements, and make sure
	 * the class names are merged properly.
	 *
	 * @param {Element} from
	 * @param {Element} to
	 */
	
	function mergeAttrs(from, to) {
	  var attrs = from.attributes;
	  var i = attrs.length;
	  var name, value;
	  while (i--) {
	    name = attrs[i].name;
	    value = attrs[i].value;
	    if (!to.hasAttribute(name) && !specialCharRE.test(name)) {
	      to.setAttribute(name, value);
	    } else if (name === 'class' && !parseText(value) && (value = value.trim())) {
	      value.split(/\s+/).forEach(function (cls) {
	        addClass(to, cls);
	      });
	    }
	  }
	}
	
	/**
	 * Scan and determine slot content distribution.
	 * We do this during transclusion instead at compile time so that
	 * the distribution is decoupled from the compilation order of
	 * the slots.
	 *
	 * @param {Element|DocumentFragment} template
	 * @param {Element} content
	 * @param {Vue} vm
	 */
	
	function resolveSlots(vm, content) {
	  if (!content) {
	    return;
	  }
	  var contents = vm._slotContents = Object.create(null);
	  var el, name;
	  for (var i = 0, l = content.children.length; i < l; i++) {
	    el = content.children[i];
	    /* eslint-disable no-cond-assign */
	    if (name = el.getAttribute('slot')) {
	      (contents[name] || (contents[name] = [])).push(el);
	    }
	    /* eslint-enable no-cond-assign */
	    if (process.env.NODE_ENV !== 'production' && getBindAttr(el, 'slot')) {
	      warn('The "slot" attribute must be static.', vm.$parent);
	    }
	  }
	  for (name in contents) {
	    contents[name] = extractFragment(contents[name], content);
	  }
	  if (content.hasChildNodes()) {
	    var nodes = content.childNodes;
	    if (nodes.length === 1 && nodes[0].nodeType === 3 && !nodes[0].data.trim()) {
	      return;
	    }
	    contents['default'] = extractFragment(content.childNodes, content);
	  }
	}
	
	/**
	 * Extract qualified content nodes from a node list.
	 *
	 * @param {NodeList} nodes
	 * @return {DocumentFragment}
	 */
	
	function extractFragment(nodes, parent) {
	  var frag = document.createDocumentFragment();
	  nodes = toArray(nodes);
	  for (var i = 0, l = nodes.length; i < l; i++) {
	    var node = nodes[i];
	    if (isTemplate(node) && !node.hasAttribute('v-if') && !node.hasAttribute('v-for')) {
	      parent.removeChild(node);
	      node = parseTemplate(node, true);
	    }
	    frag.appendChild(node);
	  }
	  return frag;
	}
	
	
	
	var compiler = Object.freeze({
		compile: compile,
		compileAndLinkProps: compileAndLinkProps,
		compileRoot: compileRoot,
		transclude: transclude,
		resolveSlots: resolveSlots
	});
	
	function stateMixin (Vue) {
	  /**
	   * Accessor for `$data` property, since setting $data
	   * requires observing the new object and updating
	   * proxied properties.
	   */
	
	  Object.defineProperty(Vue.prototype, '$data', {
	    get: function get() {
	      return this._data;
	    },
	    set: function set(newData) {
	      if (newData !== this._data) {
	        this._setData(newData);
	      }
	    }
	  });
	
	  /**
	   * Setup the scope of an instance, which contains:
	   * - observed data
	   * - computed properties
	   * - user methods
	   * - meta properties
	   */
	
	  Vue.prototype._initState = function () {
	    this._initProps();
	    this._initMeta();
	    this._initMethods();
	    this._initData();
	    this._initComputed();
	  };
	
	  /**
	   * Initialize props.
	   */
	
	  Vue.prototype._initProps = function () {
	    var options = this.$options;
	    var el = options.el;
	    var props = options.props;
	    if (props && !el) {
	      process.env.NODE_ENV !== 'production' && warn('Props will not be compiled if no `el` option is ' + 'provided at instantiation.', this);
	    }
	    // make sure to convert string selectors into element now
	    el = options.el = query(el);
	    this._propsUnlinkFn = el && el.nodeType === 1 && props
	    // props must be linked in proper scope if inside v-for
	    ? compileAndLinkProps(this, el, props, this._scope) : null;
	  };
	
	  /**
	   * Initialize the data.
	   */
	
	  Vue.prototype._initData = function () {
	    var dataFn = this.$options.data;
	    var data = this._data = dataFn ? dataFn() : {};
	    if (!isPlainObject(data)) {
	      data = {};
	      process.env.NODE_ENV !== 'production' && warn('data functions should return an object.', this);
	    }
	    var props = this._props;
	    // proxy data on instance
	    var keys = Object.keys(data);
	    var i, key;
	    i = keys.length;
	    while (i--) {
	      key = keys[i];
	      // there are two scenarios where we can proxy a data key:
	      // 1. it's not already defined as a prop
	      // 2. it's provided via a instantiation option AND there are no
	      //    template prop present
	      if (!props || !hasOwn(props, key)) {
	        this._proxy(key);
	      } else if (process.env.NODE_ENV !== 'production') {
	        warn('Data field "' + key + '" is already defined ' + 'as a prop. To provide default value for a prop, use the "default" ' + 'prop option; if you want to pass prop values to an instantiation ' + 'call, use the "propsData" option.', this);
	      }
	    }
	    // observe data
	    observe(data, this);
	  };
	
	  /**
	   * Swap the instance's $data. Called in $data's setter.
	   *
	   * @param {Object} newData
	   */
	
	  Vue.prototype._setData = function (newData) {
	    newData = newData || {};
	    var oldData = this._data;
	    this._data = newData;
	    var keys, key, i;
	    // unproxy keys not present in new data
	    keys = Object.keys(oldData);
	    i = keys.length;
	    while (i--) {
	      key = keys[i];
	      if (!(key in newData)) {
	        this._unproxy(key);
	      }
	    }
	    // proxy keys not already proxied,
	    // and trigger change for changed values
	    keys = Object.keys(newData);
	    i = keys.length;
	    while (i--) {
	      key = keys[i];
	      if (!hasOwn(this, key)) {
	        // new property
	        this._proxy(key);
	      }
	    }
	    oldData.__ob__.removeVm(this);
	    observe(newData, this);
	    this._digest();
	  };
	
	  /**
	   * Proxy a property, so that
	   * vm.prop === vm._data.prop
	   *
	   * @param {String} key
	   */
	
	  Vue.prototype._proxy = function (key) {
	    if (!isReserved(key)) {
	      // need to store ref to self here
	      // because these getter/setters might
	      // be called by child scopes via
	      // prototype inheritance.
	      var self = this;
	      Object.defineProperty(self, key, {
	        configurable: true,
	        enumerable: true,
	        get: function proxyGetter() {
	          return self._data[key];
	        },
	        set: function proxySetter(val) {
	          self._data[key] = val;
	        }
	      });
	    }
	  };
	
	  /**
	   * Unproxy a property.
	   *
	   * @param {String} key
	   */
	
	  Vue.prototype._unproxy = function (key) {
	    if (!isReserved(key)) {
	      delete this[key];
	    }
	  };
	
	  /**
	   * Force update on every watcher in scope.
	   */
	
	  Vue.prototype._digest = function () {
	    for (var i = 0, l = this._watchers.length; i < l; i++) {
	      this._watchers[i].update(true); // shallow updates
	    }
	  };
	
	  /**
	   * Setup computed properties. They are essentially
	   * special getter/setters
	   */
	
	  function noop() {}
	  Vue.prototype._initComputed = function () {
	    var computed = this.$options.computed;
	    if (computed) {
	      for (var key in computed) {
	        var userDef = computed[key];
	        var def = {
	          enumerable: true,
	          configurable: true
	        };
	        if (typeof userDef === 'function') {
	          def.get = makeComputedGetter(userDef, this);
	          def.set = noop;
	        } else {
	          def.get = userDef.get ? userDef.cache !== false ? makeComputedGetter(userDef.get, this) : bind(userDef.get, this) : noop;
	          def.set = userDef.set ? bind(userDef.set, this) : noop;
	        }
	        Object.defineProperty(this, key, def);
	      }
	    }
	  };
	
	  function makeComputedGetter(getter, owner) {
	    var watcher = new Watcher(owner, getter, null, {
	      lazy: true
	    });
	    return function computedGetter() {
	      if (watcher.dirty) {
	        watcher.evaluate();
	      }
	      if (Dep.target) {
	        watcher.depend();
	      }
	      return watcher.value;
	    };
	  }
	
	  /**
	   * Setup instance methods. Methods must be bound to the
	   * instance since they might be passed down as a prop to
	   * child components.
	   */
	
	  Vue.prototype._initMethods = function () {
	    var methods = this.$options.methods;
	    if (methods) {
	      for (var key in methods) {
	        this[key] = bind(methods[key], this);
	      }
	    }
	  };
	
	  /**
	   * Initialize meta information like $index, $key & $value.
	   */
	
	  Vue.prototype._initMeta = function () {
	    var metas = this.$options._meta;
	    if (metas) {
	      for (var key in metas) {
	        defineReactive(this, key, metas[key]);
	      }
	    }
	  };
	}
	
	var eventRE = /^v-on:|^@/;
	
	function eventsMixin (Vue) {
	  /**
	   * Setup the instance's option events & watchers.
	   * If the value is a string, we pull it from the
	   * instance's methods by name.
	   */
	
	  Vue.prototype._initEvents = function () {
	    var options = this.$options;
	    if (options._asComponent) {
	      registerComponentEvents(this, options.el);
	    }
	    registerCallbacks(this, '$on', options.events);
	    registerCallbacks(this, '$watch', options.watch);
	  };
	
	  /**
	   * Register v-on events on a child component
	   *
	   * @param {Vue} vm
	   * @param {Element} el
	   */
	
	  function registerComponentEvents(vm, el) {
	    var attrs = el.attributes;
	    var name, value, handler;
	    for (var i = 0, l = attrs.length; i < l; i++) {
	      name = attrs[i].name;
	      if (eventRE.test(name)) {
	        name = name.replace(eventRE, '');
	        // force the expression into a statement so that
	        // it always dynamically resolves the method to call (#2670)
	        // kinda ugly hack, but does the job.
	        value = attrs[i].value;
	        if (isSimplePath(value)) {
	          value += '.apply(this, $arguments)';
	        }
	        handler = (vm._scope || vm._context).$eval(value, true);
	        handler._fromParent = true;
	        vm.$on(name.replace(eventRE), handler);
	      }
	    }
	  }
	
	  /**
	   * Register callbacks for option events and watchers.
	   *
	   * @param {Vue} vm
	   * @param {String} action
	   * @param {Object} hash
	   */
	
	  function registerCallbacks(vm, action, hash) {
	    if (!hash) return;
	    var handlers, key, i, j;
	    for (key in hash) {
	      handlers = hash[key];
	      if (isArray(handlers)) {
	        for (i = 0, j = handlers.length; i < j; i++) {
	          register(vm, action, key, handlers[i]);
	        }
	      } else {
	        register(vm, action, key, handlers);
	      }
	    }
	  }
	
	  /**
	   * Helper to register an event/watch callback.
	   *
	   * @param {Vue} vm
	   * @param {String} action
	   * @param {String} key
	   * @param {Function|String|Object} handler
	   * @param {Object} [options]
	   */
	
	  function register(vm, action, key, handler, options) {
	    var type = typeof handler;
	    if (type === 'function') {
	      vm[action](key, handler, options);
	    } else if (type === 'string') {
	      var methods = vm.$options.methods;
	      var method = methods && methods[handler];
	      if (method) {
	        vm[action](key, method, options);
	      } else {
	        process.env.NODE_ENV !== 'production' && warn('Unknown method: "' + handler + '" when ' + 'registering callback for ' + action + ': "' + key + '".', vm);
	      }
	    } else if (handler && type === 'object') {
	      register(vm, action, key, handler.handler, handler);
	    }
	  }
	
	  /**
	   * Setup recursive attached/detached calls
	   */
	
	  Vue.prototype._initDOMHooks = function () {
	    this.$on('hook:attached', onAttached);
	    this.$on('hook:detached', onDetached);
	  };
	
	  /**
	   * Callback to recursively call attached hook on children
	   */
	
	  function onAttached() {
	    if (!this._isAttached) {
	      this._isAttached = true;
	      this.$children.forEach(callAttach);
	    }
	  }
	
	  /**
	   * Iterator to call attached hook
	   *
	   * @param {Vue} child
	   */
	
	  function callAttach(child) {
	    if (!child._isAttached && inDoc(child.$el)) {
	      child._callHook('attached');
	    }
	  }
	
	  /**
	   * Callback to recursively call detached hook on children
	   */
	
	  function onDetached() {
	    if (this._isAttached) {
	      this._isAttached = false;
	      this.$children.forEach(callDetach);
	    }
	  }
	
	  /**
	   * Iterator to call detached hook
	   *
	   * @param {Vue} child
	   */
	
	  function callDetach(child) {
	    if (child._isAttached && !inDoc(child.$el)) {
	      child._callHook('detached');
	    }
	  }
	
	  /**
	   * Trigger all handlers for a hook
	   *
	   * @param {String} hook
	   */
	
	  Vue.prototype._callHook = function (hook) {
	    this.$emit('pre-hook:' + hook);
	    var handlers = this.$options[hook];
	    if (handlers) {
	      for (var i = 0, j = handlers.length; i < j; i++) {
	        handlers[i].call(this);
	      }
	    }
	    this.$emit('hook:' + hook);
	  };
	}
	
	function noop$1() {}
	
	/**
	 * A directive links a DOM element with a piece of data,
	 * which is the result of evaluating an expression.
	 * It registers a watcher with the expression and calls
	 * the DOM update function when a change is triggered.
	 *
	 * @param {Object} descriptor
	 *                 - {String} name
	 *                 - {Object} def
	 *                 - {String} expression
	 *                 - {Array<Object>} [filters]
	 *                 - {Object} [modifiers]
	 *                 - {Boolean} literal
	 *                 - {String} attr
	 *                 - {String} arg
	 *                 - {String} raw
	 *                 - {String} [ref]
	 *                 - {Array<Object>} [interp]
	 *                 - {Boolean} [hasOneTime]
	 * @param {Vue} vm
	 * @param {Node} el
	 * @param {Vue} [host] - transclusion host component
	 * @param {Object} [scope] - v-for scope
	 * @param {Fragment} [frag] - owner fragment
	 * @constructor
	 */
	function Directive(descriptor, vm, el, host, scope, frag) {
	  this.vm = vm;
	  this.el = el;
	  // copy descriptor properties
	  this.descriptor = descriptor;
	  this.name = descriptor.name;
	  this.expression = descriptor.expression;
	  this.arg = descriptor.arg;
	  this.modifiers = descriptor.modifiers;
	  this.filters = descriptor.filters;
	  this.literal = this.modifiers && this.modifiers.literal;
	  // private
	  this._locked = false;
	  this._bound = false;
	  this._listeners = null;
	  // link context
	  this._host = host;
	  this._scope = scope;
	  this._frag = frag;
	  // store directives on node in dev mode
	  if (process.env.NODE_ENV !== 'production' && this.el) {
	    this.el._vue_directives = this.el._vue_directives || [];
	    this.el._vue_directives.push(this);
	  }
	}
	
	/**
	 * Initialize the directive, mixin definition properties,
	 * setup the watcher, call definition bind() and update()
	 * if present.
	 */
	
	Directive.prototype._bind = function () {
	  var name = this.name;
	  var descriptor = this.descriptor;
	
	  // remove attribute
	  if ((name !== 'cloak' || this.vm._isCompiled) && this.el && this.el.removeAttribute) {
	    var attr = descriptor.attr || 'v-' + name;
	    this.el.removeAttribute(attr);
	  }
	
	  // copy def properties
	  var def = descriptor.def;
	  if (typeof def === 'function') {
	    this.update = def;
	  } else {
	    extend(this, def);
	  }
	
	  // setup directive params
	  this._setupParams();
	
	  // initial bind
	  if (this.bind) {
	    this.bind();
	  }
	  this._bound = true;
	
	  if (this.literal) {
	    this.update && this.update(descriptor.raw);
	  } else if ((this.expression || this.modifiers) && (this.update || this.twoWay) && !this._checkStatement()) {
	    // wrapped updater for context
	    var dir = this;
	    if (this.update) {
	      this._update = function (val, oldVal) {
	        if (!dir._locked) {
	          dir.update(val, oldVal);
	        }
	      };
	    } else {
	      this._update = noop$1;
	    }
	    var preProcess = this._preProcess ? bind(this._preProcess, this) : null;
	    var postProcess = this._postProcess ? bind(this._postProcess, this) : null;
	    var watcher = this._watcher = new Watcher(this.vm, this.expression, this._update, // callback
	    {
	      filters: this.filters,
	      twoWay: this.twoWay,
	      deep: this.deep,
	      preProcess: preProcess,
	      postProcess: postProcess,
	      scope: this._scope
	    });
	    // v-model with inital inline value need to sync back to
	    // model instead of update to DOM on init. They would
	    // set the afterBind hook to indicate that.
	    if (this.afterBind) {
	      this.afterBind();
	    } else if (this.update) {
	      this.update(watcher.value);
	    }
	  }
	};
	
	/**
	 * Setup all param attributes, e.g. track-by,
	 * transition-mode, etc...
	 */
	
	Directive.prototype._setupParams = function () {
	  if (!this.params) {
	    return;
	  }
	  var params = this.params;
	  // swap the params array with a fresh object.
	  this.params = Object.create(null);
	  var i = params.length;
	  var key, val, mappedKey;
	  while (i--) {
	    key = hyphenate(params[i]);
	    mappedKey = camelize(key);
	    val = getBindAttr(this.el, key);
	    if (val != null) {
	      // dynamic
	      this._setupParamWatcher(mappedKey, val);
	    } else {
	      // static
	      val = getAttr(this.el, key);
	      if (val != null) {
	        this.params[mappedKey] = val === '' ? true : val;
	      }
	    }
	  }
	};
	
	/**
	 * Setup a watcher for a dynamic param.
	 *
	 * @param {String} key
	 * @param {String} expression
	 */
	
	Directive.prototype._setupParamWatcher = function (key, expression) {
	  var self = this;
	  var called = false;
	  var unwatch = (this._scope || this.vm).$watch(expression, function (val, oldVal) {
	    self.params[key] = val;
	    // since we are in immediate mode,
	    // only call the param change callbacks if this is not the first update.
	    if (called) {
	      var cb = self.paramWatchers && self.paramWatchers[key];
	      if (cb) {
	        cb.call(self, val, oldVal);
	      }
	    } else {
	      called = true;
	    }
	  }, {
	    immediate: true,
	    user: false
	  });(this._paramUnwatchFns || (this._paramUnwatchFns = [])).push(unwatch);
	};
	
	/**
	 * Check if the directive is a function caller
	 * and if the expression is a callable one. If both true,
	 * we wrap up the expression and use it as the event
	 * handler.
	 *
	 * e.g. on-click="a++"
	 *
	 * @return {Boolean}
	 */
	
	Directive.prototype._checkStatement = function () {
	  var expression = this.expression;
	  if (expression && this.acceptStatement && !isSimplePath(expression)) {
	    var fn = parseExpression(expression).get;
	    var scope = this._scope || this.vm;
	    var handler = function handler(e) {
	      scope.$event = e;
	      fn.call(scope, scope);
	      scope.$event = null;
	    };
	    if (this.filters) {
	      handler = scope._applyFilters(handler, null, this.filters);
	    }
	    this.update(handler);
	    return true;
	  }
	};
	
	/**
	 * Set the corresponding value with the setter.
	 * This should only be used in two-way directives
	 * e.g. v-model.
	 *
	 * @param {*} value
	 * @public
	 */
	
	Directive.prototype.set = function (value) {
	  /* istanbul ignore else */
	  if (this.twoWay) {
	    this._withLock(function () {
	      this._watcher.set(value);
	    });
	  } else if (process.env.NODE_ENV !== 'production') {
	    warn('Directive.set() can only be used inside twoWay' + 'directives.');
	  }
	};
	
	/**
	 * Execute a function while preventing that function from
	 * triggering updates on this directive instance.
	 *
	 * @param {Function} fn
	 */
	
	Directive.prototype._withLock = function (fn) {
	  var self = this;
	  self._locked = true;
	  fn.call(self);
	  nextTick(function () {
	    self._locked = false;
	  });
	};
	
	/**
	 * Convenience method that attaches a DOM event listener
	 * to the directive element and autometically tears it down
	 * during unbind.
	 *
	 * @param {String} event
	 * @param {Function} handler
	 * @param {Boolean} [useCapture]
	 */
	
	Directive.prototype.on = function (event, handler, useCapture) {
	  on(this.el, event, handler, useCapture);(this._listeners || (this._listeners = [])).push([event, handler]);
	};
	
	/**
	 * Teardown the watcher and call unbind.
	 */
	
	Directive.prototype._teardown = function () {
	  if (this._bound) {
	    this._bound = false;
	    if (this.unbind) {
	      this.unbind();
	    }
	    if (this._watcher) {
	      this._watcher.teardown();
	    }
	    var listeners = this._listeners;
	    var i;
	    if (listeners) {
	      i = listeners.length;
	      while (i--) {
	        off(this.el, listeners[i][0], listeners[i][1]);
	      }
	    }
	    var unwatchFns = this._paramUnwatchFns;
	    if (unwatchFns) {
	      i = unwatchFns.length;
	      while (i--) {
	        unwatchFns[i]();
	      }
	    }
	    if (process.env.NODE_ENV !== 'production' && this.el) {
	      this.el._vue_directives.$remove(this);
	    }
	    this.vm = this.el = this._watcher = this._listeners = null;
	  }
	};
	
	function lifecycleMixin (Vue) {
	  /**
	   * Update v-ref for component.
	   *
	   * @param {Boolean} remove
	   */
	
	  Vue.prototype._updateRef = function (remove) {
	    var ref = this.$options._ref;
	    if (ref) {
	      var refs = (this._scope || this._context).$refs;
	      if (remove) {
	        if (refs[ref] === this) {
	          refs[ref] = null;
	        }
	      } else {
	        refs[ref] = this;
	      }
	    }
	  };
	
	  /**
	   * Transclude, compile and link element.
	   *
	   * If a pre-compiled linker is available, that means the
	   * passed in element will be pre-transcluded and compiled
	   * as well - all we need to do is to call the linker.
	   *
	   * Otherwise we need to call transclude/compile/link here.
	   *
	   * @param {Element} el
	   */
	
	  Vue.prototype._compile = function (el) {
	    var options = this.$options;
	
	    // transclude and init element
	    // transclude can potentially replace original
	    // so we need to keep reference; this step also injects
	    // the template and caches the original attributes
	    // on the container node and replacer node.
	    var original = el;
	    el = transclude(el, options);
	    this._initElement(el);
	
	    // handle v-pre on root node (#2026)
	    if (el.nodeType === 1 && getAttr(el, 'v-pre') !== null) {
	      return;
	    }
	
	    // root is always compiled per-instance, because
	    // container attrs and props can be different every time.
	    var contextOptions = this._context && this._context.$options;
	    var rootLinker = compileRoot(el, options, contextOptions);
	
	    // resolve slot distribution
	    resolveSlots(this, options._content);
	
	    // compile and link the rest
	    var contentLinkFn;
	    var ctor = this.constructor;
	    // component compilation can be cached
	    // as long as it's not using inline-template
	    if (options._linkerCachable) {
	      contentLinkFn = ctor.linker;
	      if (!contentLinkFn) {
	        contentLinkFn = ctor.linker = compile(el, options);
	      }
	    }
	
	    // link phase
	    // make sure to link root with prop scope!
	    var rootUnlinkFn = rootLinker(this, el, this._scope);
	    var contentUnlinkFn = contentLinkFn ? contentLinkFn(this, el) : compile(el, options)(this, el);
	
	    // register composite unlink function
	    // to be called during instance destruction
	    this._unlinkFn = function () {
	      rootUnlinkFn();
	      // passing destroying: true to avoid searching and
	      // splicing the directives
	      contentUnlinkFn(true);
	    };
	
	    // finally replace original
	    if (options.replace) {
	      replace(original, el);
	    }
	
	    this._isCompiled = true;
	    this._callHook('compiled');
	  };
	
	  /**
	   * Initialize instance element. Called in the public
	   * $mount() method.
	   *
	   * @param {Element} el
	   */
	
	  Vue.prototype._initElement = function (el) {
	    if (isFragment(el)) {
	      this._isFragment = true;
	      this.$el = this._fragmentStart = el.firstChild;
	      this._fragmentEnd = el.lastChild;
	      // set persisted text anchors to empty
	      if (this._fragmentStart.nodeType === 3) {
	        this._fragmentStart.data = this._fragmentEnd.data = '';
	      }
	      this._fragment = el;
	    } else {
	      this.$el = el;
	    }
	    this.$el.__vue__ = this;
	    this._callHook('beforeCompile');
	  };
	
	  /**
	   * Create and bind a directive to an element.
	   *
	   * @param {Object} descriptor - parsed directive descriptor
	   * @param {Node} node   - target node
	   * @param {Vue} [host] - transclusion host component
	   * @param {Object} [scope] - v-for scope
	   * @param {Fragment} [frag] - owner fragment
	   */
	
	  Vue.prototype._bindDir = function (descriptor, node, host, scope, frag) {
	    this._directives.push(new Directive(descriptor, this, node, host, scope, frag));
	  };
	
	  /**
	   * Teardown an instance, unobserves the data, unbind all the
	   * directives, turn off all the event listeners, etc.
	   *
	   * @param {Boolean} remove - whether to remove the DOM node.
	   * @param {Boolean} deferCleanup - if true, defer cleanup to
	   *                                 be called later
	   */
	
	  Vue.prototype._destroy = function (remove, deferCleanup) {
	    if (this._isBeingDestroyed) {
	      if (!deferCleanup) {
	        this._cleanup();
	      }
	      return;
	    }
	
	    var destroyReady;
	    var pendingRemoval;
	
	    var self = this;
	    // Cleanup should be called either synchronously or asynchronoysly as
	    // callback of this.$remove(), or if remove and deferCleanup are false.
	    // In any case it should be called after all other removing, unbinding and
	    // turning of is done
	    var cleanupIfPossible = function cleanupIfPossible() {
	      if (destroyReady && !pendingRemoval && !deferCleanup) {
	        self._cleanup();
	      }
	    };
	
	    // remove DOM element
	    if (remove && this.$el) {
	      pendingRemoval = true;
	      this.$remove(function () {
	        pendingRemoval = false;
	        cleanupIfPossible();
	      });
	    }
	
	    this._callHook('beforeDestroy');
	    this._isBeingDestroyed = true;
	    var i;
	    // remove self from parent. only necessary
	    // if parent is not being destroyed as well.
	    var parent = this.$parent;
	    if (parent && !parent._isBeingDestroyed) {
	      parent.$children.$remove(this);
	      // unregister ref (remove: true)
	      this._updateRef(true);
	    }
	    // destroy all children.
	    i = this.$children.length;
	    while (i--) {
	      this.$children[i].$destroy();
	    }
	    // teardown props
	    if (this._propsUnlinkFn) {
	      this._propsUnlinkFn();
	    }
	    // teardown all directives. this also tearsdown all
	    // directive-owned watchers.
	    if (this._unlinkFn) {
	      this._unlinkFn();
	    }
	    i = this._watchers.length;
	    while (i--) {
	      this._watchers[i].teardown();
	    }
	    // remove reference to self on $el
	    if (this.$el) {
	      this.$el.__vue__ = null;
	    }
	
	    destroyReady = true;
	    cleanupIfPossible();
	  };
	
	  /**
	   * Clean up to ensure garbage collection.
	   * This is called after the leave transition if there
	   * is any.
	   */
	
	  Vue.prototype._cleanup = function () {
	    if (this._isDestroyed) {
	      return;
	    }
	    // remove self from owner fragment
	    // do it in cleanup so that we can call $destroy with
	    // defer right when a fragment is about to be removed.
	    if (this._frag) {
	      this._frag.children.$remove(this);
	    }
	    // remove reference from data ob
	    // frozen object may not have observer.
	    if (this._data && this._data.__ob__) {
	      this._data.__ob__.removeVm(this);
	    }
	    // Clean up references to private properties and other
	    // instances. preserve reference to _data so that proxy
	    // accessors still work. The only potential side effect
	    // here is that mutating the instance after it's destroyed
	    // may affect the state of other components that are still
	    // observing the same object, but that seems to be a
	    // reasonable responsibility for the user rather than
	    // always throwing an error on them.
	    this.$el = this.$parent = this.$root = this.$children = this._watchers = this._context = this._scope = this._directives = null;
	    // call the last hook...
	    this._isDestroyed = true;
	    this._callHook('destroyed');
	    // turn off all instance listeners.
	    this.$off();
	  };
	}
	
	function miscMixin (Vue) {
	  /**
	   * Apply a list of filter (descriptors) to a value.
	   * Using plain for loops here because this will be called in
	   * the getter of any watcher with filters so it is very
	   * performance sensitive.
	   *
	   * @param {*} value
	   * @param {*} [oldValue]
	   * @param {Array} filters
	   * @param {Boolean} write
	   * @return {*}
	   */
	
	  Vue.prototype._applyFilters = function (value, oldValue, filters, write) {
	    var filter, fn, args, arg, offset, i, l, j, k;
	    for (i = 0, l = filters.length; i < l; i++) {
	      filter = filters[write ? l - i - 1 : i];
	      fn = resolveAsset(this.$options, 'filters', filter.name, true);
	      if (!fn) continue;
	      fn = write ? fn.write : fn.read || fn;
	      if (typeof fn !== 'function') continue;
	      args = write ? [value, oldValue] : [value];
	      offset = write ? 2 : 1;
	      if (filter.args) {
	        for (j = 0, k = filter.args.length; j < k; j++) {
	          arg = filter.args[j];
	          args[j + offset] = arg.dynamic ? this.$get(arg.value) : arg.value;
	        }
	      }
	      value = fn.apply(this, args);
	    }
	    return value;
	  };
	
	  /**
	   * Resolve a component, depending on whether the component
	   * is defined normally or using an async factory function.
	   * Resolves synchronously if already resolved, otherwise
	   * resolves asynchronously and caches the resolved
	   * constructor on the factory.
	   *
	   * @param {String|Function} value
	   * @param {Function} cb
	   */
	
	  Vue.prototype._resolveComponent = function (value, cb) {
	    var factory;
	    if (typeof value === 'function') {
	      factory = value;
	    } else {
	      factory = resolveAsset(this.$options, 'components', value, true);
	    }
	    /* istanbul ignore if */
	    if (!factory) {
	      return;
	    }
	    // async component factory
	    if (!factory.options) {
	      if (factory.resolved) {
	        // cached
	        cb(factory.resolved);
	      } else if (factory.requested) {
	        // pool callbacks
	        factory.pendingCallbacks.push(cb);
	      } else {
	        factory.requested = true;
	        var cbs = factory.pendingCallbacks = [cb];
	        factory.call(this, function resolve(res) {
	          if (isPlainObject(res)) {
	            res = Vue.extend(res);
	          }
	          // cache resolved
	          factory.resolved = res;
	          // invoke callbacks
	          for (var i = 0, l = cbs.length; i < l; i++) {
	            cbs[i](res);
	          }
	        }, function reject(reason) {
	          process.env.NODE_ENV !== 'production' && warn('Failed to resolve async component' + (typeof value === 'string' ? ': ' + value : '') + '. ' + (reason ? '\nReason: ' + reason : ''));
	        });
	      }
	    } else {
	      // normal component
	      cb(factory);
	    }
	  };
	}
	
	var filterRE$1 = /[^|]\|[^|]/;
	
	function dataAPI (Vue) {
	  /**
	   * Get the value from an expression on this vm.
	   *
	   * @param {String} exp
	   * @param {Boolean} [asStatement]
	   * @return {*}
	   */
	
	  Vue.prototype.$get = function (exp, asStatement) {
	    var res = parseExpression(exp);
	    if (res) {
	      if (asStatement) {
	        var self = this;
	        return function statementHandler() {
	          self.$arguments = toArray(arguments);
	          var result = res.get.call(self, self);
	          self.$arguments = null;
	          return result;
	        };
	      } else {
	        try {
	          return res.get.call(this, this);
	        } catch (e) {}
	      }
	    }
	  };
	
	  /**
	   * Set the value from an expression on this vm.
	   * The expression must be a valid left-hand
	   * expression in an assignment.
	   *
	   * @param {String} exp
	   * @param {*} val
	   */
	
	  Vue.prototype.$set = function (exp, val) {
	    var res = parseExpression(exp, true);
	    if (res && res.set) {
	      res.set.call(this, this, val);
	    }
	  };
	
	  /**
	   * Delete a property on the VM
	   *
	   * @param {String} key
	   */
	
	  Vue.prototype.$delete = function (key) {
	    del(this._data, key);
	  };
	
	  /**
	   * Watch an expression, trigger callback when its
	   * value changes.
	   *
	   * @param {String|Function} expOrFn
	   * @param {Function} cb
	   * @param {Object} [options]
	   *                 - {Boolean} deep
	   *                 - {Boolean} immediate
	   * @return {Function} - unwatchFn
	   */
	
	  Vue.prototype.$watch = function (expOrFn, cb, options) {
	    var vm = this;
	    var parsed;
	    if (typeof expOrFn === 'string') {
	      parsed = parseDirective(expOrFn);
	      expOrFn = parsed.expression;
	    }
	    var watcher = new Watcher(vm, expOrFn, cb, {
	      deep: options && options.deep,
	      sync: options && options.sync,
	      filters: parsed && parsed.filters,
	      user: !options || options.user !== false
	    });
	    if (options && options.immediate) {
	      cb.call(vm, watcher.value);
	    }
	    return function unwatchFn() {
	      watcher.teardown();
	    };
	  };
	
	  /**
	   * Evaluate a text directive, including filters.
	   *
	   * @param {String} text
	   * @param {Boolean} [asStatement]
	   * @return {String}
	   */
	
	  Vue.prototype.$eval = function (text, asStatement) {
	    // check for filters.
	    if (filterRE$1.test(text)) {
	      var dir = parseDirective(text);
	      // the filter regex check might give false positive
	      // for pipes inside strings, so it's possible that
	      // we don't get any filters here
	      var val = this.$get(dir.expression, asStatement);
	      return dir.filters ? this._applyFilters(val, null, dir.filters) : val;
	    } else {
	      // no filter
	      return this.$get(text, asStatement);
	    }
	  };
	
	  /**
	   * Interpolate a piece of template text.
	   *
	   * @param {String} text
	   * @return {String}
	   */
	
	  Vue.prototype.$interpolate = function (text) {
	    var tokens = parseText(text);
	    var vm = this;
	    if (tokens) {
	      if (tokens.length === 1) {
	        return vm.$eval(tokens[0].value) + '';
	      } else {
	        return tokens.map(function (token) {
	          return token.tag ? vm.$eval(token.value) : token.value;
	        }).join('');
	      }
	    } else {
	      return text;
	    }
	  };
	
	  /**
	   * Log instance data as a plain JS object
	   * so that it is easier to inspect in console.
	   * This method assumes console is available.
	   *
	   * @param {String} [path]
	   */
	
	  Vue.prototype.$log = function (path) {
	    var data = path ? getPath(this._data, path) : this._data;
	    if (data) {
	      data = clean(data);
	    }
	    // include computed fields
	    if (!path) {
	      var key;
	      for (key in this.$options.computed) {
	        data[key] = clean(this[key]);
	      }
	      if (this._props) {
	        for (key in this._props) {
	          data[key] = clean(this[key]);
	        }
	      }
	    }
	    console.log(data);
	  };
	
	  /**
	   * "clean" a getter/setter converted object into a plain
	   * object copy.
	   *
	   * @param {Object} - obj
	   * @return {Object}
	   */
	
	  function clean(obj) {
	    return JSON.parse(JSON.stringify(obj));
	  }
	}
	
	function domAPI (Vue) {
	  /**
	   * Convenience on-instance nextTick. The callback is
	   * auto-bound to the instance, and this avoids component
	   * modules having to rely on the global Vue.
	   *
	   * @param {Function} fn
	   */
	
	  Vue.prototype.$nextTick = function (fn) {
	    nextTick(fn, this);
	  };
	
	  /**
	   * Append instance to target
	   *
	   * @param {Node} target
	   * @param {Function} [cb]
	   * @param {Boolean} [withTransition] - defaults to true
	   */
	
	  Vue.prototype.$appendTo = function (target, cb, withTransition) {
	    return insert(this, target, cb, withTransition, append, appendWithTransition);
	  };
	
	  /**
	   * Prepend instance to target
	   *
	   * @param {Node} target
	   * @param {Function} [cb]
	   * @param {Boolean} [withTransition] - defaults to true
	   */
	
	  Vue.prototype.$prependTo = function (target, cb, withTransition) {
	    target = query(target);
	    if (target.hasChildNodes()) {
	      this.$before(target.firstChild, cb, withTransition);
	    } else {
	      this.$appendTo(target, cb, withTransition);
	    }
	    return this;
	  };
	
	  /**
	   * Insert instance before target
	   *
	   * @param {Node} target
	   * @param {Function} [cb]
	   * @param {Boolean} [withTransition] - defaults to true
	   */
	
	  Vue.prototype.$before = function (target, cb, withTransition) {
	    return insert(this, target, cb, withTransition, beforeWithCb, beforeWithTransition);
	  };
	
	  /**
	   * Insert instance after target
	   *
	   * @param {Node} target
	   * @param {Function} [cb]
	   * @param {Boolean} [withTransition] - defaults to true
	   */
	
	  Vue.prototype.$after = function (target, cb, withTransition) {
	    target = query(target);
	    if (target.nextSibling) {
	      this.$before(target.nextSibling, cb, withTransition);
	    } else {
	      this.$appendTo(target.parentNode, cb, withTransition);
	    }
	    return this;
	  };
	
	  /**
	   * Remove instance from DOM
	   *
	   * @param {Function} [cb]
	   * @param {Boolean} [withTransition] - defaults to true
	   */
	
	  Vue.prototype.$remove = function (cb, withTransition) {
	    if (!this.$el.parentNode) {
	      return cb && cb();
	    }
	    var inDocument = this._isAttached && inDoc(this.$el);
	    // if we are not in document, no need to check
	    // for transitions
	    if (!inDocument) withTransition = false;
	    var self = this;
	    var realCb = function realCb() {
	      if (inDocument) self._callHook('detached');
	      if (cb) cb();
	    };
	    if (this._isFragment) {
	      removeNodeRange(this._fragmentStart, this._fragmentEnd, this, this._fragment, realCb);
	    } else {
	      var op = withTransition === false ? removeWithCb : removeWithTransition;
	      op(this.$el, this, realCb);
	    }
	    return this;
	  };
	
	  /**
	   * Shared DOM insertion function.
	   *
	   * @param {Vue} vm
	   * @param {Element} target
	   * @param {Function} [cb]
	   * @param {Boolean} [withTransition]
	   * @param {Function} op1 - op for non-transition insert
	   * @param {Function} op2 - op for transition insert
	   * @return vm
	   */
	
	  function insert(vm, target, cb, withTransition, op1, op2) {
	    target = query(target);
	    var targetIsDetached = !inDoc(target);
	    var op = withTransition === false || targetIsDetached ? op1 : op2;
	    var shouldCallHook = !targetIsDetached && !vm._isAttached && !inDoc(vm.$el);
	    if (vm._isFragment) {
	      mapNodeRange(vm._fragmentStart, vm._fragmentEnd, function (node) {
	        op(node, target, vm);
	      });
	      cb && cb();
	    } else {
	      op(vm.$el, target, vm, cb);
	    }
	    if (shouldCallHook) {
	      vm._callHook('attached');
	    }
	    return vm;
	  }
	
	  /**
	   * Check for selectors
	   *
	   * @param {String|Element} el
	   */
	
	  function query(el) {
	    return typeof el === 'string' ? document.querySelector(el) : el;
	  }
	
	  /**
	   * Append operation that takes a callback.
	   *
	   * @param {Node} el
	   * @param {Node} target
	   * @param {Vue} vm - unused
	   * @param {Function} [cb]
	   */
	
	  function append(el, target, vm, cb) {
	    target.appendChild(el);
	    if (cb) cb();
	  }
	
	  /**
	   * InsertBefore operation that takes a callback.
	   *
	   * @param {Node} el
	   * @param {Node} target
	   * @param {Vue} vm - unused
	   * @param {Function} [cb]
	   */
	
	  function beforeWithCb(el, target, vm, cb) {
	    before(el, target);
	    if (cb) cb();
	  }
	
	  /**
	   * Remove operation that takes a callback.
	   *
	   * @param {Node} el
	   * @param {Vue} vm - unused
	   * @param {Function} [cb]
	   */
	
	  function removeWithCb(el, vm, cb) {
	    remove(el);
	    if (cb) cb();
	  }
	}
	
	function eventsAPI (Vue) {
	  /**
	   * Listen on the given `event` with `fn`.
	   *
	   * @param {String} event
	   * @param {Function} fn
	   */
	
	  Vue.prototype.$on = function (event, fn) {
	    (this._events[event] || (this._events[event] = [])).push(fn);
	    modifyListenerCount(this, event, 1);
	    return this;
	  };
	
	  /**
	   * Adds an `event` listener that will be invoked a single
	   * time then automatically removed.
	   *
	   * @param {String} event
	   * @param {Function} fn
	   */
	
	  Vue.prototype.$once = function (event, fn) {
	    var self = this;
	    function on() {
	      self.$off(event, on);
	      fn.apply(this, arguments);
	    }
	    on.fn = fn;
	    this.$on(event, on);
	    return this;
	  };
	
	  /**
	   * Remove the given callback for `event` or all
	   * registered callbacks.
	   *
	   * @param {String} event
	   * @param {Function} fn
	   */
	
	  Vue.prototype.$off = function (event, fn) {
	    var cbs;
	    // all
	    if (!arguments.length) {
	      if (this.$parent) {
	        for (event in this._events) {
	          cbs = this._events[event];
	          if (cbs) {
	            modifyListenerCount(this, event, -cbs.length);
	          }
	        }
	      }
	      this._events = {};
	      return this;
	    }
	    // specific event
	    cbs = this._events[event];
	    if (!cbs) {
	      return this;
	    }
	    if (arguments.length === 1) {
	      modifyListenerCount(this, event, -cbs.length);
	      this._events[event] = null;
	      return this;
	    }
	    // specific handler
	    var cb;
	    var i = cbs.length;
	    while (i--) {
	      cb = cbs[i];
	      if (cb === fn || cb.fn === fn) {
	        modifyListenerCount(this, event, -1);
	        cbs.splice(i, 1);
	        break;
	      }
	    }
	    return this;
	  };
	
	  /**
	   * Trigger an event on self.
	   *
	   * @param {String|Object} event
	   * @return {Boolean} shouldPropagate
	   */
	
	  Vue.prototype.$emit = function (event) {
	    var isSource = typeof event === 'string';
	    event = isSource ? event : event.name;
	    var cbs = this._events[event];
	    var shouldPropagate = isSource || !cbs;
	    if (cbs) {
	      cbs = cbs.length > 1 ? toArray(cbs) : cbs;
	      // this is a somewhat hacky solution to the question raised
	      // in #2102: for an inline component listener like <comp @test="doThis">,
	      // the propagation handling is somewhat broken. Therefore we
	      // need to treat these inline callbacks differently.
	      var hasParentCbs = isSource && cbs.some(function (cb) {
	        return cb._fromParent;
	      });
	      if (hasParentCbs) {
	        shouldPropagate = false;
	      }
	      var args = toArray(arguments, 1);
	      for (var i = 0, l = cbs.length; i < l; i++) {
	        var cb = cbs[i];
	        var res = cb.apply(this, args);
	        if (res === true && (!hasParentCbs || cb._fromParent)) {
	          shouldPropagate = true;
	        }
	      }
	    }
	    return shouldPropagate;
	  };
	
	  /**
	   * Recursively broadcast an event to all children instances.
	   *
	   * @param {String|Object} event
	   * @param {...*} additional arguments
	   */
	
	  Vue.prototype.$broadcast = function (event) {
	    var isSource = typeof event === 'string';
	    event = isSource ? event : event.name;
	    // if no child has registered for this event,
	    // then there's no need to broadcast.
	    if (!this._eventsCount[event]) return;
	    var children = this.$children;
	    var args = toArray(arguments);
	    if (isSource) {
	      // use object event to indicate non-source emit
	      // on children
	      args[0] = { name: event, source: this };
	    }
	    for (var i = 0, l = children.length; i < l; i++) {
	      var child = children[i];
	      var shouldPropagate = child.$emit.apply(child, args);
	      if (shouldPropagate) {
	        child.$broadcast.apply(child, args);
	      }
	    }
	    return this;
	  };
	
	  /**
	   * Recursively propagate an event up the parent chain.
	   *
	   * @param {String} event
	   * @param {...*} additional arguments
	   */
	
	  Vue.prototype.$dispatch = function (event) {
	    var shouldPropagate = this.$emit.apply(this, arguments);
	    if (!shouldPropagate) return;
	    var parent = this.$parent;
	    var args = toArray(arguments);
	    // use object event to indicate non-source emit
	    // on parents
	    args[0] = { name: event, source: this };
	    while (parent) {
	      shouldPropagate = parent.$emit.apply(parent, args);
	      parent = shouldPropagate ? parent.$parent : null;
	    }
	    return this;
	  };
	
	  /**
	   * Modify the listener counts on all parents.
	   * This bookkeeping allows $broadcast to return early when
	   * no child has listened to a certain event.
	   *
	   * @param {Vue} vm
	   * @param {String} event
	   * @param {Number} count
	   */
	
	  var hookRE = /^hook:/;
	  function modifyListenerCount(vm, event, count) {
	    var parent = vm.$parent;
	    // hooks do not get broadcasted so no need
	    // to do bookkeeping for them
	    if (!parent || !count || hookRE.test(event)) return;
	    while (parent) {
	      parent._eventsCount[event] = (parent._eventsCount[event] || 0) + count;
	      parent = parent.$parent;
	    }
	  }
	}
	
	function lifecycleAPI (Vue) {
	  /**
	   * Set instance target element and kick off the compilation
	   * process. The passed in `el` can be a selector string, an
	   * existing Element, or a DocumentFragment (for block
	   * instances).
	   *
	   * @param {Element|DocumentFragment|string} el
	   * @public
	   */
	
	  Vue.prototype.$mount = function (el) {
	    if (this._isCompiled) {
	      process.env.NODE_ENV !== 'production' && warn('$mount() should be called only once.', this);
	      return;
	    }
	    el = query(el);
	    if (!el) {
	      el = document.createElement('div');
	    }
	    this._compile(el);
	    this._initDOMHooks();
	    if (inDoc(this.$el)) {
	      this._callHook('attached');
	      ready.call(this);
	    } else {
	      this.$once('hook:attached', ready);
	    }
	    return this;
	  };
	
	  /**
	   * Mark an instance as ready.
	   */
	
	  function ready() {
	    this._isAttached = true;
	    this._isReady = true;
	    this._callHook('ready');
	  }
	
	  /**
	   * Teardown the instance, simply delegate to the internal
	   * _destroy.
	   *
	   * @param {Boolean} remove
	   * @param {Boolean} deferCleanup
	   */
	
	  Vue.prototype.$destroy = function (remove, deferCleanup) {
	    this._destroy(remove, deferCleanup);
	  };
	
	  /**
	   * Partially compile a piece of DOM and return a
	   * decompile function.
	   *
	   * @param {Element|DocumentFragment} el
	   * @param {Vue} [host]
	   * @param {Object} [scope]
	   * @param {Fragment} [frag]
	   * @return {Function}
	   */
	
	  Vue.prototype.$compile = function (el, host, scope, frag) {
	    return compile(el, this.$options, true)(this, el, host, scope, frag);
	  };
	}
	
	/**
	 * The exposed Vue constructor.
	 *
	 * API conventions:
	 * - public API methods/properties are prefixed with `$`
	 * - internal methods/properties are prefixed with `_`
	 * - non-prefixed properties are assumed to be proxied user
	 *   data.
	 *
	 * @constructor
	 * @param {Object} [options]
	 * @public
	 */
	
	function Vue(options) {
	  this._init(options);
	}
	
	// install internals
	initMixin(Vue);
	stateMixin(Vue);
	eventsMixin(Vue);
	lifecycleMixin(Vue);
	miscMixin(Vue);
	
	// install instance APIs
	dataAPI(Vue);
	domAPI(Vue);
	eventsAPI(Vue);
	lifecycleAPI(Vue);
	
	var slot = {
	
	  priority: SLOT,
	  params: ['name'],
	
	  bind: function bind() {
	    // this was resolved during component transclusion
	    var name = this.params.name || 'default';
	    var content = this.vm._slotContents && this.vm._slotContents[name];
	    if (!content || !content.hasChildNodes()) {
	      this.fallback();
	    } else {
	      this.compile(content.cloneNode(true), this.vm._context, this.vm);
	    }
	  },
	
	  compile: function compile(content, context, host) {
	    if (content && context) {
	      if (this.el.hasChildNodes() && content.childNodes.length === 1 && content.childNodes[0].nodeType === 1 && content.childNodes[0].hasAttribute('v-if')) {
	        // if the inserted slot has v-if
	        // inject fallback content as the v-else
	        var elseBlock = document.createElement('template');
	        elseBlock.setAttribute('v-else', '');
	        elseBlock.innerHTML = this.el.innerHTML;
	        // the else block should be compiled in child scope
	        elseBlock._context = this.vm;
	        content.appendChild(elseBlock);
	      }
	      var scope = host ? host._scope : this._scope;
	      this.unlink = context.$compile(content, host, scope, this._frag);
	    }
	    if (content) {
	      replace(this.el, content);
	    } else {
	      remove(this.el);
	    }
	  },
	
	  fallback: function fallback() {
	    this.compile(extractContent(this.el, true), this.vm);
	  },
	
	  unbind: function unbind() {
	    if (this.unlink) {
	      this.unlink();
	    }
	  }
	};
	
	var partial = {
	
	  priority: PARTIAL,
	
	  params: ['name'],
	
	  // watch changes to name for dynamic partials
	  paramWatchers: {
	    name: function name(value) {
	      vIf.remove.call(this);
	      if (value) {
	        this.insert(value);
	      }
	    }
	  },
	
	  bind: function bind() {
	    this.anchor = createAnchor('v-partial');
	    replace(this.el, this.anchor);
	    this.insert(this.params.name);
	  },
	
	  insert: function insert(id) {
	    var partial = resolveAsset(this.vm.$options, 'partials', id, true);
	    if (partial) {
	      this.factory = new FragmentFactory(this.vm, partial);
	      vIf.insert.call(this);
	    }
	  },
	
	  unbind: function unbind() {
	    if (this.frag) {
	      this.frag.destroy();
	    }
	  }
	};
	
	var elementDirectives = {
	  slot: slot,
	  partial: partial
	};
	
	var convertArray = vFor._postProcess;
	
	/**
	 * Limit filter for arrays
	 *
	 * @param {Number} n
	 * @param {Number} offset (Decimal expected)
	 */
	
	function limitBy(arr, n, offset) {
	  offset = offset ? parseInt(offset, 10) : 0;
	  n = toNumber(n);
	  return typeof n === 'number' ? arr.slice(offset, offset + n) : arr;
	}
	
	/**
	 * Filter filter for arrays
	 *
	 * @param {String} search
	 * @param {String} [delimiter]
	 * @param {String} ...dataKeys
	 */
	
	function filterBy(arr, search, delimiter) {
	  arr = convertArray(arr);
	  if (search == null) {
	    return arr;
	  }
	  if (typeof search === 'function') {
	    return arr.filter(search);
	  }
	  // cast to lowercase string
	  search = ('' + search).toLowerCase();
	  // allow optional `in` delimiter
	  // because why not
	  var n = delimiter === 'in' ? 3 : 2;
	  // extract and flatten keys
	  var keys = Array.prototype.concat.apply([], toArray(arguments, n));
	  var res = [];
	  var item, key, val, j;
	  for (var i = 0, l = arr.length; i < l; i++) {
	    item = arr[i];
	    val = item && item.$value || item;
	    j = keys.length;
	    if (j) {
	      while (j--) {
	        key = keys[j];
	        if (key === '$key' && contains(item.$key, search) || contains(getPath(val, key), search)) {
	          res.push(item);
	          break;
	        }
	      }
	    } else if (contains(item, search)) {
	      res.push(item);
	    }
	  }
	  return res;
	}
	
	/**
	 * Filter filter for arrays
	 *
	 * @param {String|Array<String>|Function} ...sortKeys
	 * @param {Number} [order]
	 */
	
	function orderBy(arr) {
	  var comparator = null;
	  var sortKeys = undefined;
	  arr = convertArray(arr);
	
	  // determine order (last argument)
	  var args = toArray(arguments, 1);
	  var order = args[args.length - 1];
	  if (typeof order === 'number') {
	    order = order < 0 ? -1 : 1;
	    args = args.length > 1 ? args.slice(0, -1) : args;
	  } else {
	    order = 1;
	  }
	
	  // determine sortKeys & comparator
	  var firstArg = args[0];
	  if (!firstArg) {
	    return arr;
	  } else if (typeof firstArg === 'function') {
	    // custom comparator
	    comparator = function (a, b) {
	      return firstArg(a, b) * order;
	    };
	  } else {
	    // string keys. flatten first
	    sortKeys = Array.prototype.concat.apply([], args);
	    comparator = function (a, b, i) {
	      i = i || 0;
	      return i >= sortKeys.length - 1 ? baseCompare(a, b, i) : baseCompare(a, b, i) || comparator(a, b, i + 1);
	    };
	  }
	
	  function baseCompare(a, b, sortKeyIndex) {
	    var sortKey = sortKeys[sortKeyIndex];
	    if (sortKey) {
	      if (sortKey !== '$key') {
	        if (isObject(a) && '$value' in a) a = a.$value;
	        if (isObject(b) && '$value' in b) b = b.$value;
	      }
	      a = isObject(a) ? getPath(a, sortKey) : a;
	      b = isObject(b) ? getPath(b, sortKey) : b;
	    }
	    return a === b ? 0 : a > b ? order : -order;
	  }
	
	  // sort on a copy to avoid mutating original array
	  return arr.slice().sort(comparator);
	}
	
	/**
	 * String contain helper
	 *
	 * @param {*} val
	 * @param {String} search
	 */
	
	function contains(val, search) {
	  var i;
	  if (isPlainObject(val)) {
	    var keys = Object.keys(val);
	    i = keys.length;
	    while (i--) {
	      if (contains(val[keys[i]], search)) {
	        return true;
	      }
	    }
	  } else if (isArray(val)) {
	    i = val.length;
	    while (i--) {
	      if (contains(val[i], search)) {
	        return true;
	      }
	    }
	  } else if (val != null) {
	    return val.toString().toLowerCase().indexOf(search) > -1;
	  }
	}
	
	var digitsRE = /(\d{3})(?=\d)/g;
	
	// asset collections must be a plain object.
	var filters = {
	
	  orderBy: orderBy,
	  filterBy: filterBy,
	  limitBy: limitBy,
	
	  /**
	   * Stringify value.
	   *
	   * @param {Number} indent
	   */
	
	  json: {
	    read: function read(value, indent) {
	      return typeof value === 'string' ? value : JSON.stringify(value, null, arguments.length > 1 ? indent : 2);
	    },
	    write: function write(value) {
	      try {
	        return JSON.parse(value);
	      } catch (e) {
	        return value;
	      }
	    }
	  },
	
	  /**
	   * 'abc' => 'Abc'
	   */
	
	  capitalize: function capitalize(value) {
	    if (!value && value !== 0) return '';
	    value = value.toString();
	    return value.charAt(0).toUpperCase() + value.slice(1);
	  },
	
	  /**
	   * 'abc' => 'ABC'
	   */
	
	  uppercase: function uppercase(value) {
	    return value || value === 0 ? value.toString().toUpperCase() : '';
	  },
	
	  /**
	   * 'AbC' => 'abc'
	   */
	
	  lowercase: function lowercase(value) {
	    return value || value === 0 ? value.toString().toLowerCase() : '';
	  },
	
	  /**
	   * 12345 => $12,345.00
	   *
	   * @param {String} sign
	   * @param {Number} decimals Decimal places
	   */
	
	  currency: function currency(value, _currency, decimals) {
	    value = parseFloat(value);
	    if (!isFinite(value) || !value && value !== 0) return '';
	    _currency = _currency != null ? _currency : '$';
	    decimals = decimals != null ? decimals : 2;
	    var stringified = Math.abs(value).toFixed(decimals);
	    var _int = decimals ? stringified.slice(0, -1 - decimals) : stringified;
	    var i = _int.length % 3;
	    var head = i > 0 ? _int.slice(0, i) + (_int.length > 3 ? ',' : '') : '';
	    var _float = decimals ? stringified.slice(-1 - decimals) : '';
	    var sign = value < 0 ? '-' : '';
	    return sign + _currency + head + _int.slice(i).replace(digitsRE, '$1,') + _float;
	  },
	
	  /**
	   * 'item' => 'items'
	   *
	   * @params
	   *  an array of strings corresponding to
	   *  the single, double, triple ... forms of the word to
	   *  be pluralized. When the number to be pluralized
	   *  exceeds the length of the args, it will use the last
	   *  entry in the array.
	   *
	   *  e.g. ['single', 'double', 'triple', 'multiple']
	   */
	
	  pluralize: function pluralize(value) {
	    var args = toArray(arguments, 1);
	    var length = args.length;
	    if (length > 1) {
	      var index = value % 10 - 1;
	      return index in args ? args[index] : args[length - 1];
	    } else {
	      return args[0] + (value === 1 ? '' : 's');
	    }
	  },
	
	  /**
	   * Debounce a handler function.
	   *
	   * @param {Function} handler
	   * @param {Number} delay = 300
	   * @return {Function}
	   */
	
	  debounce: function debounce(handler, delay) {
	    if (!handler) return;
	    if (!delay) {
	      delay = 300;
	    }
	    return _debounce(handler, delay);
	  }
	};
	
	function installGlobalAPI (Vue) {
	  /**
	   * Vue and every constructor that extends Vue has an
	   * associated options object, which can be accessed during
	   * compilation steps as `this.constructor.options`.
	   *
	   * These can be seen as the default options of every
	   * Vue instance.
	   */
	
	  Vue.options = {
	    directives: directives,
	    elementDirectives: elementDirectives,
	    filters: filters,
	    transitions: {},
	    components: {},
	    partials: {},
	    replace: true
	  };
	
	  /**
	   * Expose useful internals
	   */
	
	  Vue.util = util;
	  Vue.config = config;
	  Vue.set = set;
	  Vue['delete'] = del;
	  Vue.nextTick = nextTick;
	
	  /**
	   * The following are exposed for advanced usage / plugins
	   */
	
	  Vue.compiler = compiler;
	  Vue.FragmentFactory = FragmentFactory;
	  Vue.internalDirectives = internalDirectives;
	  Vue.parsers = {
	    path: path,
	    text: text,
	    template: template,
	    directive: directive,
	    expression: expression
	  };
	
	  /**
	   * Each instance constructor, including Vue, has a unique
	   * cid. This enables us to create wrapped "child
	   * constructors" for prototypal inheritance and cache them.
	   */
	
	  Vue.cid = 0;
	  var cid = 1;
	
	  /**
	   * Class inheritance
	   *
	   * @param {Object} extendOptions
	   */
	
	  Vue.extend = function (extendOptions) {
	    extendOptions = extendOptions || {};
	    var Super = this;
	    var isFirstExtend = Super.cid === 0;
	    if (isFirstExtend && extendOptions._Ctor) {
	      return extendOptions._Ctor;
	    }
	    var name = extendOptions.name || Super.options.name;
	    if (process.env.NODE_ENV !== 'production') {
	      if (!/^[a-zA-Z][\w-]*$/.test(name)) {
	        warn('Invalid component name: "' + name + '". Component names ' + 'can only contain alphanumeric characaters and the hyphen.');
	        name = null;
	      }
	    }
	    var Sub = createClass(name || 'VueComponent');
	    Sub.prototype = Object.create(Super.prototype);
	    Sub.prototype.constructor = Sub;
	    Sub.cid = cid++;
	    Sub.options = mergeOptions(Super.options, extendOptions);
	    Sub['super'] = Super;
	    // allow further extension
	    Sub.extend = Super.extend;
	    // create asset registers, so extended classes
	    // can have their private assets too.
	    config._assetTypes.forEach(function (type) {
	      Sub[type] = Super[type];
	    });
	    // enable recursive self-lookup
	    if (name) {
	      Sub.options.components[name] = Sub;
	    }
	    // cache constructor
	    if (isFirstExtend) {
	      extendOptions._Ctor = Sub;
	    }
	    return Sub;
	  };
	
	  /**
	   * A function that returns a sub-class constructor with the
	   * given name. This gives us much nicer output when
	   * logging instances in the console.
	   *
	   * @param {String} name
	   * @return {Function}
	   */
	
	  function createClass(name) {
	    /* eslint-disable no-new-func */
	    return new Function('return function ' + classify(name) + ' (options) { this._init(options) }')();
	    /* eslint-enable no-new-func */
	  }
	
	  /**
	   * Plugin system
	   *
	   * @param {Object} plugin
	   */
	
	  Vue.use = function (plugin) {
	    /* istanbul ignore if */
	    if (plugin.installed) {
	      return;
	    }
	    // additional parameters
	    var args = toArray(arguments, 1);
	    args.unshift(this);
	    if (typeof plugin.install === 'function') {
	      plugin.install.apply(plugin, args);
	    } else {
	      plugin.apply(null, args);
	    }
	    plugin.installed = true;
	    return this;
	  };
	
	  /**
	   * Apply a global mixin by merging it into the default
	   * options.
	   */
	
	  Vue.mixin = function (mixin) {
	    Vue.options = mergeOptions(Vue.options, mixin);
	  };
	
	  /**
	   * Create asset registration methods with the following
	   * signature:
	   *
	   * @param {String} id
	   * @param {*} definition
	   */
	
	  config._assetTypes.forEach(function (type) {
	    Vue[type] = function (id, definition) {
	      if (!definition) {
	        return this.options[type + 's'][id];
	      } else {
	        /* istanbul ignore if */
	        if (process.env.NODE_ENV !== 'production') {
	          if (type === 'component' && (commonTagRE.test(id) || reservedTagRE.test(id))) {
	            warn('Do not use built-in or reserved HTML elements as component ' + 'id: ' + id);
	          }
	        }
	        if (type === 'component' && isPlainObject(definition)) {
	          if (!definition.name) {
	            definition.name = id;
	          }
	          definition = Vue.extend(definition);
	        }
	        this.options[type + 's'][id] = definition;
	        return definition;
	      }
	    };
	  });
	
	  // expose internal transition API
	  extend(Vue.transition, transition);
	}
	
	installGlobalAPI(Vue);
	
	Vue.version = '1.0.26';
	
	// devtools global hook
	/* istanbul ignore next */
	setTimeout(function () {
	  if (config.devtools) {
	    if (devtools) {
	      devtools.emit('init', Vue);
	    } else if (process.env.NODE_ENV !== 'production' && inBrowser && /Chrome\/\d+/.test(window.navigator.userAgent)) {
	      console.log('Download the Vue Devtools for a better development experience:\n' + 'https://github.com/vuejs/vue-devtools');
	    }
	  }
	}, 0);
	
	module.exports = Vue;
	/* WEBPACK VAR INJECTION */}.call(exports, (function() { return this; }()), __webpack_require__(3)))

/***/ },
/* 3 */
/***/ function(module, exports) {

	// shim for using process in browser
	
	var process = module.exports = {};
	
	// cached from whatever global is present so that test runners that stub it
	// don't break things.  But we need to wrap it in a try catch in case it is
	// wrapped in strict mode code which doesn't define any globals.  It's inside a
	// function because try/catches deoptimize in certain engines.
	
	var cachedSetTimeout;
	var cachedClearTimeout;
	
	(function () {
	  try {
	    cachedSetTimeout = setTimeout;
	  } catch (e) {
	    cachedSetTimeout = function () {
	      throw new Error('setTimeout is not defined');
	    }
	  }
	  try {
	    cachedClearTimeout = clearTimeout;
	  } catch (e) {
	    cachedClearTimeout = function () {
	      throw new Error('clearTimeout is not defined');
	    }
	  }
	} ())
	var queue = [];
	var draining = false;
	var currentQueue;
	var queueIndex = -1;
	
	function cleanUpNextTick() {
	    if (!draining || !currentQueue) {
	        return;
	    }
	    draining = false;
	    if (currentQueue.length) {
	        queue = currentQueue.concat(queue);
	    } else {
	        queueIndex = -1;
	    }
	    if (queue.length) {
	        drainQueue();
	    }
	}
	
	function drainQueue() {
	    if (draining) {
	        return;
	    }
	    var timeout = cachedSetTimeout(cleanUpNextTick);
	    draining = true;
	
	    var len = queue.length;
	    while(len) {
	        currentQueue = queue;
	        queue = [];
	        while (++queueIndex < len) {
	            if (currentQueue) {
	                currentQueue[queueIndex].run();
	            }
	        }
	        queueIndex = -1;
	        len = queue.length;
	    }
	    currentQueue = null;
	    draining = false;
	    cachedClearTimeout(timeout);
	}
	
	process.nextTick = function (fun) {
	    var args = new Array(arguments.length - 1);
	    if (arguments.length > 1) {
	        for (var i = 1; i < arguments.length; i++) {
	            args[i - 1] = arguments[i];
	        }
	    }
	    queue.push(new Item(fun, args));
	    if (queue.length === 1 && !draining) {
	        cachedSetTimeout(drainQueue, 0);
	    }
	};
	
	// v8 likes predictible objects
	function Item(fun, array) {
	    this.fun = fun;
	    this.array = array;
	}
	Item.prototype.run = function () {
	    this.fun.apply(null, this.array);
	};
	process.title = 'browser';
	process.browser = true;
	process.env = {};
	process.argv = [];
	process.version = ''; // empty string to avoid regexp issues
	process.versions = {};
	
	function noop() {}
	
	process.on = noop;
	process.addListener = noop;
	process.once = noop;
	process.off = noop;
	process.removeListener = noop;
	process.removeAllListeners = noop;
	process.emit = noop;
	
	process.binding = function (name) {
	    throw new Error('process.binding is not supported');
	};
	
	process.cwd = function () { return '/' };
	process.chdir = function (dir) {
	    throw new Error('process.chdir is not supported');
	};
	process.umask = function() { return 0; };


/***/ },
/* 4 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Install plugin.
	 */
	
	function install(Vue) {
	
	    var _ = __webpack_require__(5);
	
	    _.config = Vue.config;
	    _.warning = Vue.util.warn;
	    _.nextTick = Vue.util.nextTick;
	
	    Vue.url = __webpack_require__(6);
	    Vue.http = __webpack_require__(12);
	    Vue.resource = __webpack_require__(27);
	    Vue.Promise = __webpack_require__(14);
	
	    Object.defineProperties(Vue.prototype, {
	
	        $url: {
	            get: function () {
	                return _.options(Vue.url, this, this.$options.url);
	            }
	        },
	
	        $http: {
	            get: function () {
	                return _.options(Vue.http, this, this.$options.http);
	            }
	        },
	
	        $resource: {
	            get: function () {
	                return Vue.resource.bind(this);
	            }
	        },
	
	        $promise: {
	            get: function () {
	                return function (executor) {
	                    return new Vue.Promise(executor, this);
	                }.bind(this);
	            }
	        }
	
	    });
	}
	
	if (window.Vue) {
	    Vue.use(install);
	}
	
	module.exports = install;


/***/ },
/* 5 */
/***/ function(module, exports) {

	/**
	 * Utility functions.
	 */
	
	var _ = exports, array = [], console = window.console;
	
	_.warn = function (msg) {
	    if (console && _.warning && (!_.config.silent || _.config.debug)) {
	        console.warn('[VueResource warn]: ' + msg);
	    }
	};
	
	_.error = function (msg) {
	    if (console) {
	        console.error(msg);
	    }
	};
	
	_.trim = function (str) {
	    return str.replace(/^\s*|\s*$/g, '');
	};
	
	_.toLower = function (str) {
	    return str ? str.toLowerCase() : '';
	};
	
	_.isArray = Array.isArray;
	
	_.isString = function (val) {
	    return typeof val === 'string';
	};
	
	_.isFunction = function (val) {
	    return typeof val === 'function';
	};
	
	_.isObject = function (obj) {
	    return obj !== null && typeof obj === 'object';
	};
	
	_.isPlainObject = function (obj) {
	    return _.isObject(obj) && Object.getPrototypeOf(obj) == Object.prototype;
	};
	
	_.options = function (fn, obj, options) {
	
	    options = options || {};
	
	    if (_.isFunction(options)) {
	        options = options.call(obj);
	    }
	
	    return _.merge(fn.bind({$vm: obj, $options: options}), fn, {$options: options});
	};
	
	_.each = function (obj, iterator) {
	
	    var i, key;
	
	    if (typeof obj.length == 'number') {
	        for (i = 0; i < obj.length; i++) {
	            iterator.call(obj[i], obj[i], i);
	        }
	    } else if (_.isObject(obj)) {
	        for (key in obj) {
	            if (obj.hasOwnProperty(key)) {
	                iterator.call(obj[key], obj[key], key);
	            }
	        }
	    }
	
	    return obj;
	};
	
	_.defaults = function (target, source) {
	
	    for (var key in source) {
	        if (target[key] === undefined) {
	            target[key] = source[key];
	        }
	    }
	
	    return target;
	};
	
	_.extend = function (target) {
	
	    var args = array.slice.call(arguments, 1);
	
	    args.forEach(function (arg) {
	        merge(target, arg);
	    });
	
	    return target;
	};
	
	_.merge = function (target) {
	
	    var args = array.slice.call(arguments, 1);
	
	    args.forEach(function (arg) {
	        merge(target, arg, true);
	    });
	
	    return target;
	};
	
	function merge(target, source, deep) {
	    for (var key in source) {
	        if (deep && (_.isPlainObject(source[key]) || _.isArray(source[key]))) {
	            if (_.isPlainObject(source[key]) && !_.isPlainObject(target[key])) {
	                target[key] = {};
	            }
	            if (_.isArray(source[key]) && !_.isArray(target[key])) {
	                target[key] = [];
	            }
	            merge(target[key], source[key], deep);
	        } else if (source[key] !== undefined) {
	            target[key] = source[key];
	        }
	    }
	}


/***/ },
/* 6 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Service for URL templating.
	 */
	
	var _ = __webpack_require__(5);
	var ie = document.documentMode;
	var el = document.createElement('a');
	
	function Url(url, params) {
	
	    var options = url, transform;
	
	    if (_.isString(url)) {
	        options = {url: url, params: params};
	    }
	
	    options = _.merge({}, Url.options, this.$options, options);
	
	    Url.transforms.forEach(function (handler) {
	        transform = factory(handler, transform, this.$vm);
	    }, this);
	
	    return transform(options);
	};
	
	/**
	 * Url options.
	 */
	
	Url.options = {
	    url: '',
	    root: null,
	    params: {}
	};
	
	/**
	 * Url transforms.
	 */
	
	Url.transforms = [
	    __webpack_require__(7),
	    __webpack_require__(9),
	    __webpack_require__(10),
	    __webpack_require__(11)
	];
	
	/**
	 * Encodes a Url parameter string.
	 *
	 * @param {Object} obj
	 */
	
	Url.params = function (obj) {
	
	    var params = [], escape = encodeURIComponent;
	
	    params.add = function (key, value) {
	
	        if (_.isFunction(value)) {
	            value = value();
	        }
	
	        if (value === null) {
	            value = '';
	        }
	
	        this.push(escape(key) + '=' + escape(value));
	    };
	
	    serialize(params, obj);
	
	    return params.join('&').replace(/%20/g, '+');
	};
	
	/**
	 * Parse a URL and return its components.
	 *
	 * @param {String} url
	 */
	
	Url.parse = function (url) {
	
	    if (ie) {
	        el.href = url;
	        url = el.href;
	    }
	
	    el.href = url;
	
	    return {
	        href: el.href,
	        protocol: el.protocol ? el.protocol.replace(/:$/, '') : '',
	        port: el.port,
	        host: el.host,
	        hostname: el.hostname,
	        pathname: el.pathname.charAt(0) === '/' ? el.pathname : '/' + el.pathname,
	        search: el.search ? el.search.replace(/^\?/, '') : '',
	        hash: el.hash ? el.hash.replace(/^#/, '') : ''
	    };
	};
	
	function factory(handler, next, vm) {
	    return function (options) {
	        return handler.call(vm, options, next);
	    };
	}
	
	function serialize(params, obj, scope) {
	
	    var array = _.isArray(obj), plain = _.isPlainObject(obj), hash;
	
	    _.each(obj, function (value, key) {
	
	        hash = _.isObject(value) || _.isArray(value);
	
	        if (scope) {
	            key = scope + '[' + (plain || hash ? key : '') + ']';
	        }
	
	        if (!scope && array) {
	            params.add(value.name, value.value);
	        } else if (hash) {
	            serialize(params, value, key);
	        } else {
	            params.add(key, value);
	        }
	    });
	}
	
	module.exports = _.url = Url;


/***/ },
/* 7 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * URL Template (RFC 6570) Transform.
	 */
	
	var UrlTemplate = __webpack_require__(8);
	
	module.exports = function (options) {
	
	    var variables = [], url = UrlTemplate.expand(options.url, options.params, variables);
	
	    variables.forEach(function (key) {
	        delete options.params[key];
	    });
	
	    return url;
	};


/***/ },
/* 8 */
/***/ function(module, exports) {

	/**
	 * URL Template v2.0.6 (https://github.com/bramstein/url-template)
	 */
	
	exports.expand = function (url, params, variables) {
	
	    var tmpl = this.parse(url), expanded = tmpl.expand(params);
	
	    if (variables) {
	        variables.push.apply(variables, tmpl.vars);
	    }
	
	    return expanded;
	};
	
	exports.parse = function (template) {
	
	    var operators = ['+', '#', '.', '/', ';', '?', '&'], variables = [];
	
	    return {
	        vars: variables,
	        expand: function (context) {
	            return template.replace(/\{([^\{\}]+)\}|([^\{\}]+)/g, function (_, expression, literal) {
	                if (expression) {
	
	                    var operator = null, values = [];
	
	                    if (operators.indexOf(expression.charAt(0)) !== -1) {
	                        operator = expression.charAt(0);
	                        expression = expression.substr(1);
	                    }
	
	                    expression.split(/,/g).forEach(function (variable) {
	                        var tmp = /([^:\*]*)(?::(\d+)|(\*))?/.exec(variable);
	                        values.push.apply(values, exports.getValues(context, operator, tmp[1], tmp[2] || tmp[3]));
	                        variables.push(tmp[1]);
	                    });
	
	                    if (operator && operator !== '+') {
	
	                        var separator = ',';
	
	                        if (operator === '?') {
	                            separator = '&';
	                        } else if (operator !== '#') {
	                            separator = operator;
	                        }
	
	                        return (values.length !== 0 ? operator : '') + values.join(separator);
	                    } else {
	                        return values.join(',');
	                    }
	
	                } else {
	                    return exports.encodeReserved(literal);
	                }
	            });
	        }
	    };
	};
	
	exports.getValues = function (context, operator, key, modifier) {
	
	    var value = context[key], result = [];
	
	    if (this.isDefined(value) && value !== '') {
	        if (typeof value === 'string' || typeof value === 'number' || typeof value === 'boolean') {
	            value = value.toString();
	
	            if (modifier && modifier !== '*') {
	                value = value.substring(0, parseInt(modifier, 10));
	            }
	
	            result.push(this.encodeValue(operator, value, this.isKeyOperator(operator) ? key : null));
	        } else {
	            if (modifier === '*') {
	                if (Array.isArray(value)) {
	                    value.filter(this.isDefined).forEach(function (value) {
	                        result.push(this.encodeValue(operator, value, this.isKeyOperator(operator) ? key : null));
	                    }, this);
	                } else {
	                    Object.keys(value).forEach(function (k) {
	                        if (this.isDefined(value[k])) {
	                            result.push(this.encodeValue(operator, value[k], k));
	                        }
	                    }, this);
	                }
	            } else {
	                var tmp = [];
	
	                if (Array.isArray(value)) {
	                    value.filter(this.isDefined).forEach(function (value) {
	                        tmp.push(this.encodeValue(operator, value));
	                    }, this);
	                } else {
	                    Object.keys(value).forEach(function (k) {
	                        if (this.isDefined(value[k])) {
	                            tmp.push(encodeURIComponent(k));
	                            tmp.push(this.encodeValue(operator, value[k].toString()));
	                        }
	                    }, this);
	                }
	
	                if (this.isKeyOperator(operator)) {
	                    result.push(encodeURIComponent(key) + '=' + tmp.join(','));
	                } else if (tmp.length !== 0) {
	                    result.push(tmp.join(','));
	                }
	            }
	        }
	    } else {
	        if (operator === ';') {
	            result.push(encodeURIComponent(key));
	        } else if (value === '' && (operator === '&' || operator === '?')) {
	            result.push(encodeURIComponent(key) + '=');
	        } else if (value === '') {
	            result.push('');
	        }
	    }
	
	    return result;
	};
	
	exports.isDefined = function (value) {
	    return value !== undefined && value !== null;
	};
	
	exports.isKeyOperator = function (operator) {
	    return operator === ';' || operator === '&' || operator === '?';
	};
	
	exports.encodeValue = function (operator, value, key) {
	
	    value = (operator === '+' || operator === '#') ? this.encodeReserved(value) : encodeURIComponent(value);
	
	    if (key) {
	        return encodeURIComponent(key) + '=' + value;
	    } else {
	        return value;
	    }
	};
	
	exports.encodeReserved = function (str) {
	    return str.split(/(%[0-9A-Fa-f]{2})/g).map(function (part) {
	        if (!/%[0-9A-Fa-f]/.test(part)) {
	            part = encodeURI(part);
	        }
	        return part;
	    }).join('');
	};


/***/ },
/* 9 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Legacy Transform.
	 */
	
	var _ = __webpack_require__(5);
	
	module.exports = function (options, next) {
	
	    var variables = [], url = next(options);
	
	    url = url.replace(/(\/?):([a-z]\w*)/gi, function (match, slash, name) {
	
	        _.warn('The `:' + name + '` parameter syntax has been deprecated. Use the `{' + name + '}` syntax instead.');
	
	        if (options.params[name]) {
	            variables.push(name);
	            return slash + encodeUriSegment(options.params[name]);
	        }
	
	        return '';
	    });
	
	    variables.forEach(function (key) {
	        delete options.params[key];
	    });
	
	    return url;
	};
	
	function encodeUriSegment(value) {
	
	    return encodeUriQuery(value, true).
	        replace(/%26/gi, '&').
	        replace(/%3D/gi, '=').
	        replace(/%2B/gi, '+');
	}
	
	function encodeUriQuery(value, spaces) {
	
	    return encodeURIComponent(value).
	        replace(/%40/gi, '@').
	        replace(/%3A/gi, ':').
	        replace(/%24/g, '$').
	        replace(/%2C/gi, ',').
	        replace(/%20/g, (spaces ? '%20' : '+'));
	}


/***/ },
/* 10 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Query Parameter Transform.
	 */
	
	var _ = __webpack_require__(5);
	
	module.exports = function (options, next) {
	
	    var urlParams = Object.keys(_.url.options.params), query = {}, url = next(options);
	
	   _.each(options.params, function (value, key) {
	        if (urlParams.indexOf(key) === -1) {
	            query[key] = value;
	        }
	    });
	
	    query = _.url.params(query);
	
	    if (query) {
	        url += (url.indexOf('?') == -1 ? '?' : '&') + query;
	    }
	
	    return url;
	};


/***/ },
/* 11 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Root Prefix Transform.
	 */
	
	var _ = __webpack_require__(5);
	
	module.exports = function (options, next) {
	
	    var url = next(options);
	
	    if (_.isString(options.root) && !url.match(/^(https?:)?\//)) {
	        url = options.root + '/' + url;
	    }
	
	    return url;
	};


/***/ },
/* 12 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Service for sending network requests.
	 */
	
	var _ = __webpack_require__(5);
	var Client = __webpack_require__(13);
	var Promise = __webpack_require__(14);
	var interceptor = __webpack_require__(17);
	var jsonType = {'Content-Type': 'application/json'};
	
	function Http(url, options) {
	
	    var client = Client, request, promise;
	
	    Http.interceptors.forEach(function (handler) {
	        client = interceptor(handler, this.$vm)(client);
	    }, this);
	
	    options = _.isObject(url) ? url : _.extend({url: url}, options);
	    request = _.merge({}, Http.options, this.$options, options);
	    promise = client(request).bind(this.$vm).then(function (response) {
	
	        return response.ok ? response : Promise.reject(response);
	
	    }, function (response) {
	
	        if (response instanceof Error) {
	            _.error(response);
	        }
	
	        return Promise.reject(response);
	    });
	
	    if (request.success) {
	        promise.success(request.success);
	    }
	
	    if (request.error) {
	        promise.error(request.error);
	    }
	
	    return promise;
	}
	
	Http.options = {
	    method: 'get',
	    data: '',
	    params: {},
	    headers: {},
	    xhr: null,
	    jsonp: 'callback',
	    beforeSend: null,
	    crossOrigin: null,
	    emulateHTTP: false,
	    emulateJSON: false,
	    timeout: 0
	};
	
	Http.interceptors = [
	    __webpack_require__(18),
	    __webpack_require__(19),
	    __webpack_require__(20),
	    __webpack_require__(22),
	    __webpack_require__(23),
	    __webpack_require__(24),
	    __webpack_require__(25)
	];
	
	Http.headers = {
	    put: jsonType,
	    post: jsonType,
	    patch: jsonType,
	    delete: jsonType,
	    common: {'Accept': 'application/json, text/plain, */*'},
	    custom: {'X-Requested-With': 'XMLHttpRequest'}
	};
	
	['get', 'put', 'post', 'patch', 'delete', 'jsonp'].forEach(function (method) {
	
	    Http[method] = function (url, data, success, options) {
	
	        if (_.isFunction(data)) {
	            options = success;
	            success = data;
	            data = undefined;
	        }
	
	        if (_.isObject(success)) {
	            options = success;
	            success = undefined;
	        }
	
	        return this(url, _.extend({method: method, data: data, success: success}, options));
	    };
	});
	
	module.exports = _.http = Http;


/***/ },
/* 13 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Base client.
	 */
	
	var _ = __webpack_require__(5);
	var Promise = __webpack_require__(14);
	var xhrClient = __webpack_require__(16);
	
	module.exports = function (request) {
	
	    var response = (request.client || xhrClient)(request);
	
	    return Promise.resolve(response).then(function (response) {
	
	        if (response.headers) {
	
	            var headers = parseHeaders(response.headers);
	
	            response.headers = function (name) {
	
	                if (name) {
	                    return headers[_.toLower(name)];
	                }
	
	                return headers;
	            };
	
	        }
	
	        response.ok = response.status >= 200 && response.status < 300;
	
	        return response;
	    });
	
	};
	
	function parseHeaders(str) {
	
	    var headers = {}, value, name, i;
	
	    if (_.isString(str)) {
	        _.each(str.split('\n'), function (row) {
	
	            i = row.indexOf(':');
	            name = _.trim(_.toLower(row.slice(0, i)));
	            value = _.trim(row.slice(i + 1));
	
	            if (headers[name]) {
	
	                if (_.isArray(headers[name])) {
	                    headers[name].push(value);
	                } else {
	                    headers[name] = [headers[name], value];
	                }
	
	            } else {
	
	                headers[name] = value;
	            }
	
	        });
	    }
	
	    return headers;
	}


/***/ },
/* 14 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Promise adapter.
	 */
	
	var _ = __webpack_require__(5);
	var PromiseObj = window.Promise || __webpack_require__(15);
	
	function Promise(executor, context) {
	
	    if (executor instanceof PromiseObj) {
	        this.promise = executor;
	    } else {
	        this.promise = new PromiseObj(executor.bind(context));
	    }
	
	    this.context = context;
	}
	
	Promise.all = function (iterable, context) {
	    return new Promise(PromiseObj.all(iterable), context);
	};
	
	Promise.resolve = function (value, context) {
	    return new Promise(PromiseObj.resolve(value), context);
	};
	
	Promise.reject = function (reason, context) {
	    return new Promise(PromiseObj.reject(reason), context);
	};
	
	Promise.race = function (iterable, context) {
	    return new Promise(PromiseObj.race(iterable), context);
	};
	
	var p = Promise.prototype;
	
	p.bind = function (context) {
	    this.context = context;
	    return this;
	};
	
	p.then = function (fulfilled, rejected) {
	
	    if (fulfilled && fulfilled.bind && this.context) {
	        fulfilled = fulfilled.bind(this.context);
	    }
	
	    if (rejected && rejected.bind && this.context) {
	        rejected = rejected.bind(this.context);
	    }
	
	    this.promise = this.promise.then(fulfilled, rejected);
	
	    return this;
	};
	
	p.catch = function (rejected) {
	
	    if (rejected && rejected.bind && this.context) {
	        rejected = rejected.bind(this.context);
	    }
	
	    this.promise = this.promise.catch(rejected);
	
	    return this;
	};
	
	p.finally = function (callback) {
	
	    return this.then(function (value) {
	            callback.call(this);
	            return value;
	        }, function (reason) {
	            callback.call(this);
	            return PromiseObj.reject(reason);
	        }
	    );
	};
	
	p.success = function (callback) {
	
	    _.warn('The `success` method has been deprecated. Use the `then` method instead.');
	
	    return this.then(function (response) {
	        return callback.call(this, response.data, response.status, response) || response;
	    });
	};
	
	p.error = function (callback) {
	
	    _.warn('The `error` method has been deprecated. Use the `catch` method instead.');
	
	    return this.catch(function (response) {
	        return callback.call(this, response.data, response.status, response) || response;
	    });
	};
	
	p.always = function (callback) {
	
	    _.warn('The `always` method has been deprecated. Use the `finally` method instead.');
	
	    var cb = function (response) {
	        return callback.call(this, response.data, response.status, response) || response;
	    };
	
	    return this.then(cb, cb);
	};
	
	module.exports = Promise;


/***/ },
/* 15 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Promises/A+ polyfill v1.1.4 (https://github.com/bramstein/promis)
	 */
	
	var _ = __webpack_require__(5);
	
	var RESOLVED = 0;
	var REJECTED = 1;
	var PENDING  = 2;
	
	function Promise(executor) {
	
	    this.state = PENDING;
	    this.value = undefined;
	    this.deferred = [];
	
	    var promise = this;
	
	    try {
	        executor(function (x) {
	            promise.resolve(x);
	        }, function (r) {
	            promise.reject(r);
	        });
	    } catch (e) {
	        promise.reject(e);
	    }
	}
	
	Promise.reject = function (r) {
	    return new Promise(function (resolve, reject) {
	        reject(r);
	    });
	};
	
	Promise.resolve = function (x) {
	    return new Promise(function (resolve, reject) {
	        resolve(x);
	    });
	};
	
	Promise.all = function all(iterable) {
	    return new Promise(function (resolve, reject) {
	        var count = 0, result = [];
	
	        if (iterable.length === 0) {
	            resolve(result);
	        }
	
	        function resolver(i) {
	            return function (x) {
	                result[i] = x;
	                count += 1;
	
	                if (count === iterable.length) {
	                    resolve(result);
	                }
	            };
	        }
	
	        for (var i = 0; i < iterable.length; i += 1) {
	            Promise.resolve(iterable[i]).then(resolver(i), reject);
	        }
	    });
	};
	
	Promise.race = function race(iterable) {
	    return new Promise(function (resolve, reject) {
	        for (var i = 0; i < iterable.length; i += 1) {
	            Promise.resolve(iterable[i]).then(resolve, reject);
	        }
	    });
	};
	
	var p = Promise.prototype;
	
	p.resolve = function resolve(x) {
	    var promise = this;
	
	    if (promise.state === PENDING) {
	        if (x === promise) {
	            throw new TypeError('Promise settled with itself.');
	        }
	
	        var called = false;
	
	        try {
	            var then = x && x['then'];
	
	            if (x !== null && typeof x === 'object' && typeof then === 'function') {
	                then.call(x, function (x) {
	                    if (!called) {
	                        promise.resolve(x);
	                    }
	                    called = true;
	
	                }, function (r) {
	                    if (!called) {
	                        promise.reject(r);
	                    }
	                    called = true;
	                });
	                return;
	            }
	        } catch (e) {
	            if (!called) {
	                promise.reject(e);
	            }
	            return;
	        }
	
	        promise.state = RESOLVED;
	        promise.value = x;
	        promise.notify();
	    }
	};
	
	p.reject = function reject(reason) {
	    var promise = this;
	
	    if (promise.state === PENDING) {
	        if (reason === promise) {
	            throw new TypeError('Promise settled with itself.');
	        }
	
	        promise.state = REJECTED;
	        promise.value = reason;
	        promise.notify();
	    }
	};
	
	p.notify = function notify() {
	    var promise = this;
	
	    _.nextTick(function () {
	        if (promise.state !== PENDING) {
	            while (promise.deferred.length) {
	                var deferred = promise.deferred.shift(),
	                    onResolved = deferred[0],
	                    onRejected = deferred[1],
	                    resolve = deferred[2],
	                    reject = deferred[3];
	
	                try {
	                    if (promise.state === RESOLVED) {
	                        if (typeof onResolved === 'function') {
	                            resolve(onResolved.call(undefined, promise.value));
	                        } else {
	                            resolve(promise.value);
	                        }
	                    } else if (promise.state === REJECTED) {
	                        if (typeof onRejected === 'function') {
	                            resolve(onRejected.call(undefined, promise.value));
	                        } else {
	                            reject(promise.value);
	                        }
	                    }
	                } catch (e) {
	                    reject(e);
	                }
	            }
	        }
	    });
	};
	
	p.then = function then(onResolved, onRejected) {
	    var promise = this;
	
	    return new Promise(function (resolve, reject) {
	        promise.deferred.push([onResolved, onRejected, resolve, reject]);
	        promise.notify();
	    });
	};
	
	p.catch = function (onRejected) {
	    return this.then(undefined, onRejected);
	};
	
	module.exports = Promise;


/***/ },
/* 16 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * XMLHttp client.
	 */
	
	var _ = __webpack_require__(5);
	var Promise = __webpack_require__(14);
	
	module.exports = function (request) {
	    return new Promise(function (resolve) {
	
	        var xhr = new XMLHttpRequest(), response = {request: request}, handler;
	
	        request.cancel = function () {
	            xhr.abort();
	        };
	
	        xhr.open(request.method, _.url(request), true);
	
	        if (_.isPlainObject(request.xhr)) {
	            _.extend(xhr, request.xhr);
	        }
	
	        _.each(request.headers || {}, function (value, header) {
	            xhr.setRequestHeader(header, value);
	        });
	
	        handler = function (event) {
	
	            response.data = xhr.responseText;
	            response.status = xhr.status;
	            response.statusText = xhr.statusText;
	            response.headers = xhr.getAllResponseHeaders();
	
	            resolve(response);
	        };
	
	        xhr.onload = handler;
	        xhr.onabort = handler;
	        xhr.onerror = handler;
	
	        xhr.send(request.data);
	    });
	};


/***/ },
/* 17 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Interceptor factory.
	 */
	
	var _ = __webpack_require__(5);
	var Promise = __webpack_require__(14);
	
	module.exports = function (handler, vm) {
	
	    return function (client) {
	
	        if (_.isFunction(handler)) {
	            handler = handler.call(vm, Promise);
	        }
	
	        return function (request) {
	
	            if (_.isFunction(handler.request)) {
	                request = handler.request.call(vm, request);
	            }
	
	            return when(request, function (request) {
	                return when(client(request), function (response) {
	
	                    if (_.isFunction(handler.response)) {
	                        response = handler.response.call(vm, response);
	                    }
	
	                    return response;
	                });
	            });
	        };
	    };
	};
	
	function when(value, fulfilled, rejected) {
	
	    var promise = Promise.resolve(value);
	
	    if (arguments.length < 2) {
	        return promise;
	    }
	
	    return promise.then(fulfilled, rejected);
	}


/***/ },
/* 18 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Before Interceptor.
	 */
	
	var _ = __webpack_require__(5);
	
	module.exports = {
	
	    request: function (request) {
	
	        if (_.isFunction(request.beforeSend)) {
	            request.beforeSend.call(this, request);
	        }
	
	        return request;
	    }
	
	};


/***/ },
/* 19 */
/***/ function(module, exports) {

	/**
	 * Timeout Interceptor.
	 */
	
	module.exports = function () {
	
	    var timeout;
	
	    return {
	
	        request: function (request) {
	
	            if (request.timeout) {
	                timeout = setTimeout(function () {
	                    request.cancel();
	                }, request.timeout);
	            }
	
	            return request;
	        },
	
	        response: function (response) {
	
	            clearTimeout(timeout);
	
	            return response;
	        }
	
	    };
	};


/***/ },
/* 20 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * JSONP Interceptor.
	 */
	
	var jsonpClient = __webpack_require__(21);
	
	module.exports = {
	
	    request: function (request) {
	
	        if (request.method == 'JSONP') {
	            request.client = jsonpClient;
	        }
	
	        return request;
	    }
	
	};


/***/ },
/* 21 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * JSONP client.
	 */
	
	var _ = __webpack_require__(5);
	var Promise = __webpack_require__(14);
	
	module.exports = function (request) {
	    return new Promise(function (resolve) {
	
	        var callback = '_jsonp' + Math.random().toString(36).substr(2), response = {request: request, data: null}, handler, script;
	
	        request.params[request.jsonp] = callback;
	        request.cancel = function () {
	            handler({type: 'cancel'});
	        };
	
	        script = document.createElement('script');
	        script.src = _.url(request);
	        script.type = 'text/javascript';
	        script.async = true;
	
	        window[callback] = function (data) {
	            response.data = data;
	        };
	
	        handler = function (event) {
	
	            if (event.type === 'load' && response.data !== null) {
	                response.status = 200;
	            } else if (event.type === 'error') {
	                response.status = 404;
	            } else {
	                response.status = 0;
	            }
	
	            resolve(response);
	
	            delete window[callback];
	            document.body.removeChild(script);
	        };
	
	        script.onload = handler;
	        script.onerror = handler;
	
	        document.body.appendChild(script);
	    });
	};


/***/ },
/* 22 */
/***/ function(module, exports) {

	/**
	 * HTTP method override Interceptor.
	 */
	
	module.exports = {
	
	    request: function (request) {
	
	        if (request.emulateHTTP && /^(PUT|PATCH|DELETE)$/i.test(request.method)) {
	            request.headers['X-HTTP-Method-Override'] = request.method;
	            request.method = 'POST';
	        }
	
	        return request;
	    }
	
	};


/***/ },
/* 23 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Mime Interceptor.
	 */
	
	var _ = __webpack_require__(5);
	
	module.exports = {
	
	    request: function (request) {
	
	        if (request.emulateJSON && _.isPlainObject(request.data)) {
	            request.headers['Content-Type'] = 'application/x-www-form-urlencoded';
	            request.data = _.url.params(request.data);
	        }
	
	        if (_.isObject(request.data) && /FormData/i.test(request.data.toString())) {
	            delete request.headers['Content-Type'];
	        }
	
	        if (_.isPlainObject(request.data)) {
	            request.data = JSON.stringify(request.data);
	        }
	
	        return request;
	    },
	
	    response: function (response) {
	
	        try {
	            response.data = JSON.parse(response.data);
	        } catch (e) {}
	
	        return response;
	    }
	
	};


/***/ },
/* 24 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Header Interceptor.
	 */
	
	var _ = __webpack_require__(5);
	
	module.exports = {
	
	    request: function (request) {
	
	        request.method = request.method.toUpperCase();
	        request.headers = _.extend({}, _.http.headers.common,
	            !request.crossOrigin ? _.http.headers.custom : {},
	            _.http.headers[request.method.toLowerCase()],
	            request.headers
	        );
	
	        if (_.isPlainObject(request.data) && /^(GET|JSONP)$/i.test(request.method)) {
	            _.extend(request.params, request.data);
	            delete request.data;
	        }
	
	        return request;
	    }
	
	};


/***/ },
/* 25 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * CORS Interceptor.
	 */
	
	var _ = __webpack_require__(5);
	var xdrClient = __webpack_require__(26);
	var xhrCors = 'withCredentials' in new XMLHttpRequest();
	var originUrl = _.url.parse(location.href);
	
	module.exports = {
	
	    request: function (request) {
	
	        if (request.crossOrigin === null) {
	            request.crossOrigin = crossOrigin(request);
	        }
	
	        if (request.crossOrigin) {
	
	            if (!xhrCors) {
	                request.client = xdrClient;
	            }
	
	            request.emulateHTTP = false;
	        }
	
	        return request;
	    }
	
	};
	
	function crossOrigin(request) {
	
	    var requestUrl = _.url.parse(_.url(request));
	
	    return (requestUrl.protocol !== originUrl.protocol || requestUrl.host !== originUrl.host);
	}


/***/ },
/* 26 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * XDomain client (Internet Explorer).
	 */
	
	var _ = __webpack_require__(5);
	var Promise = __webpack_require__(14);
	
	module.exports = function (request) {
	    return new Promise(function (resolve) {
	
	        var xdr = new XDomainRequest(), response = {request: request}, handler;
	
	        request.cancel = function () {
	            xdr.abort();
	        };
	
	        xdr.open(request.method, _.url(request), true);
	
	        handler = function (event) {
	
	            response.data = xdr.responseText;
	            response.status = xdr.status;
	            response.statusText = xdr.statusText;
	
	            resolve(response);
	        };
	
	        xdr.timeout = 0;
	        xdr.onload = handler;
	        xdr.onabort = handler;
	        xdr.onerror = handler;
	        xdr.ontimeout = function () {};
	        xdr.onprogress = function () {};
	
	        xdr.send(request.data);
	    });
	};


/***/ },
/* 27 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Service for interacting with RESTful services.
	 */
	
	var _ = __webpack_require__(5);
	
	function Resource(url, params, actions, options) {
	
	    var self = this, resource = {};
	
	    actions = _.extend({},
	        Resource.actions,
	        actions
	    );
	
	    _.each(actions, function (action, name) {
	
	        action = _.merge({url: url, params: params || {}}, options, action);
	
	        resource[name] = function () {
	            return (self.$http || _.http)(opts(action, arguments));
	        };
	    });
	
	    return resource;
	}
	
	function opts(action, args) {
	
	    var options = _.extend({}, action), params = {}, data, success, error;
	
	    switch (args.length) {
	
	        case 4:
	
	            error = args[3];
	            success = args[2];
	
	        case 3:
	        case 2:
	
	            if (_.isFunction(args[1])) {
	
	                if (_.isFunction(args[0])) {
	
	                    success = args[0];
	                    error = args[1];
	
	                    break;
	                }
	
	                success = args[1];
	                error = args[2];
	
	            } else {
	
	                params = args[0];
	                data = args[1];
	                success = args[2];
	
	                break;
	            }
	
	        case 1:
	
	            if (_.isFunction(args[0])) {
	                success = args[0];
	            } else if (/^(POST|PUT|PATCH)$/i.test(options.method)) {
	                data = args[0];
	            } else {
	                params = args[0];
	            }
	
	            break;
	
	        case 0:
	
	            break;
	
	        default:
	
	            throw 'Expected up to 4 arguments [params, data, success, error], got ' + args.length + ' arguments';
	    }
	
	    options.data = data;
	    options.params = _.extend({}, options.params, params);
	
	    if (success) {
	        options.success = success;
	    }
	
	    if (error) {
	        options.error = error;
	    }
	
	    return options;
	}
	
	Resource.actions = {
	
	    get: {method: 'GET'},
	    save: {method: 'POST'},
	    query: {method: 'GET'},
	    update: {method: 'PUT'},
	    remove: {method: 'DELETE'},
	    delete: {method: 'DELETE'}
	
	};
	
	module.exports = _.resource = Resource;


/***/ },
/* 28 */
/***/ function(module, exports, __webpack_require__) {

	/*!
	 * vue-router v0.7.13
	 * (c) 2016 Evan You
	 * Released under the MIT License.
	 */
	(function (global, factory) {
	   true ? module.exports = factory() :
	  typeof define === 'function' && define.amd ? define(factory) :
	  global.VueRouter = factory();
	}(this, function () { 'use strict';
	
	  var babelHelpers = {};
	
	  babelHelpers.classCallCheck = function (instance, Constructor) {
	    if (!(instance instanceof Constructor)) {
	      throw new TypeError("Cannot call a class as a function");
	    }
	  };
	  function Target(path, matcher, delegate) {
	    this.path = path;
	    this.matcher = matcher;
	    this.delegate = delegate;
	  }
	
	  Target.prototype = {
	    to: function to(target, callback) {
	      var delegate = this.delegate;
	
	      if (delegate && delegate.willAddRoute) {
	        target = delegate.willAddRoute(this.matcher.target, target);
	      }
	
	      this.matcher.add(this.path, target);
	
	      if (callback) {
	        if (callback.length === 0) {
	          throw new Error("You must have an argument in the function passed to `to`");
	        }
	        this.matcher.addChild(this.path, target, callback, this.delegate);
	      }
	      return this;
	    }
	  };
	
	  function Matcher(target) {
	    this.routes = {};
	    this.children = {};
	    this.target = target;
	  }
	
	  Matcher.prototype = {
	    add: function add(path, handler) {
	      this.routes[path] = handler;
	    },
	
	    addChild: function addChild(path, target, callback, delegate) {
	      var matcher = new Matcher(target);
	      this.children[path] = matcher;
	
	      var match = generateMatch(path, matcher, delegate);
	
	      if (delegate && delegate.contextEntered) {
	        delegate.contextEntered(target, match);
	      }
	
	      callback(match);
	    }
	  };
	
	  function generateMatch(startingPath, matcher, delegate) {
	    return function (path, nestedCallback) {
	      var fullPath = startingPath + path;
	
	      if (nestedCallback) {
	        nestedCallback(generateMatch(fullPath, matcher, delegate));
	      } else {
	        return new Target(startingPath + path, matcher, delegate);
	      }
	    };
	  }
	
	  function addRoute(routeArray, path, handler) {
	    var len = 0;
	    for (var i = 0, l = routeArray.length; i < l; i++) {
	      len += routeArray[i].path.length;
	    }
	
	    path = path.substr(len);
	    var route = { path: path, handler: handler };
	    routeArray.push(route);
	  }
	
	  function eachRoute(baseRoute, matcher, callback, binding) {
	    var routes = matcher.routes;
	
	    for (var path in routes) {
	      if (routes.hasOwnProperty(path)) {
	        var routeArray = baseRoute.slice();
	        addRoute(routeArray, path, routes[path]);
	
	        if (matcher.children[path]) {
	          eachRoute(routeArray, matcher.children[path], callback, binding);
	        } else {
	          callback.call(binding, routeArray);
	        }
	      }
	    }
	  }
	
	  function map (callback, addRouteCallback) {
	    var matcher = new Matcher();
	
	    callback(generateMatch("", matcher, this.delegate));
	
	    eachRoute([], matcher, function (route) {
	      if (addRouteCallback) {
	        addRouteCallback(this, route);
	      } else {
	        this.add(route);
	      }
	    }, this);
	  }
	
	  var specials = ['/', '.', '*', '+', '?', '|', '(', ')', '[', ']', '{', '}', '\\'];
	
	  var escapeRegex = new RegExp('(\\' + specials.join('|\\') + ')', 'g');
	
	  var noWarning = false;
	  function warn(msg) {
	    if (!noWarning && typeof console !== 'undefined') {
	      console.error('[vue-router] ' + msg);
	    }
	  }
	
	  function tryDecode(uri, asComponent) {
	    try {
	      return asComponent ? decodeURIComponent(uri) : decodeURI(uri);
	    } catch (e) {
	      warn('malformed URI' + (asComponent ? ' component: ' : ': ') + uri);
	    }
	  }
	
	  function isArray(test) {
	    return Object.prototype.toString.call(test) === "[object Array]";
	  }
	
	  // A Segment represents a segment in the original route description.
	  // Each Segment type provides an `eachChar` and `regex` method.
	  //
	  // The `eachChar` method invokes the callback with one or more character
	  // specifications. A character specification consumes one or more input
	  // characters.
	  //
	  // The `regex` method returns a regex fragment for the segment. If the
	  // segment is a dynamic of star segment, the regex fragment also includes
	  // a capture.
	  //
	  // A character specification contains:
	  //
	  // * `validChars`: a String with a list of all valid characters, or
	  // * `invalidChars`: a String with a list of all invalid characters
	  // * `repeat`: true if the character specification can repeat
	
	  function StaticSegment(string) {
	    this.string = string;
	  }
	  StaticSegment.prototype = {
	    eachChar: function eachChar(callback) {
	      var string = this.string,
	          ch;
	
	      for (var i = 0, l = string.length; i < l; i++) {
	        ch = string.charAt(i);
	        callback({ validChars: ch });
	      }
	    },
	
	    regex: function regex() {
	      return this.string.replace(escapeRegex, '\\$1');
	    },
	
	    generate: function generate() {
	      return this.string;
	    }
	  };
	
	  function DynamicSegment(name) {
	    this.name = name;
	  }
	  DynamicSegment.prototype = {
	    eachChar: function eachChar(callback) {
	      callback({ invalidChars: "/", repeat: true });
	    },
	
	    regex: function regex() {
	      return "([^/]+)";
	    },
	
	    generate: function generate(params) {
	      var val = params[this.name];
	      return val == null ? ":" + this.name : val;
	    }
	  };
	
	  function StarSegment(name) {
	    this.name = name;
	  }
	  StarSegment.prototype = {
	    eachChar: function eachChar(callback) {
	      callback({ invalidChars: "", repeat: true });
	    },
	
	    regex: function regex() {
	      return "(.+)";
	    },
	
	    generate: function generate(params) {
	      var val = params[this.name];
	      return val == null ? ":" + this.name : val;
	    }
	  };
	
	  function EpsilonSegment() {}
	  EpsilonSegment.prototype = {
	    eachChar: function eachChar() {},
	    regex: function regex() {
	      return "";
	    },
	    generate: function generate() {
	      return "";
	    }
	  };
	
	  function parse(route, names, specificity) {
	    // normalize route as not starting with a "/". Recognition will
	    // also normalize.
	    if (route.charAt(0) === "/") {
	      route = route.substr(1);
	    }
	
	    var segments = route.split("/"),
	        results = [];
	
	    // A routes has specificity determined by the order that its different segments
	    // appear in. This system mirrors how the magnitude of numbers written as strings
	    // works.
	    // Consider a number written as: "abc". An example would be "200". Any other number written
	    // "xyz" will be smaller than "abc" so long as `a > z`. For instance, "199" is smaller
	    // then "200", even though "y" and "z" (which are both 9) are larger than "0" (the value
	    // of (`b` and `c`). This is because the leading symbol, "2", is larger than the other
	    // leading symbol, "1".
	    // The rule is that symbols to the left carry more weight than symbols to the right
	    // when a number is written out as a string. In the above strings, the leading digit
	    // represents how many 100's are in the number, and it carries more weight than the middle
	    // number which represents how many 10's are in the number.
	    // This system of number magnitude works well for route specificity, too. A route written as
	    // `a/b/c` will be more specific than `x/y/z` as long as `a` is more specific than
	    // `x`, irrespective of the other parts.
	    // Because of this similarity, we assign each type of segment a number value written as a
	    // string. We can find the specificity of compound routes by concatenating these strings
	    // together, from left to right. After we have looped through all of the segments,
	    // we convert the string to a number.
	    specificity.val = '';
	
	    for (var i = 0, l = segments.length; i < l; i++) {
	      var segment = segments[i],
	          match;
	
	      if (match = segment.match(/^:([^\/]+)$/)) {
	        results.push(new DynamicSegment(match[1]));
	        names.push(match[1]);
	        specificity.val += '3';
	      } else if (match = segment.match(/^\*([^\/]+)$/)) {
	        results.push(new StarSegment(match[1]));
	        specificity.val += '2';
	        names.push(match[1]);
	      } else if (segment === "") {
	        results.push(new EpsilonSegment());
	        specificity.val += '1';
	      } else {
	        results.push(new StaticSegment(segment));
	        specificity.val += '4';
	      }
	    }
	
	    specificity.val = +specificity.val;
	
	    return results;
	  }
	
	  // A State has a character specification and (`charSpec`) and a list of possible
	  // subsequent states (`nextStates`).
	  //
	  // If a State is an accepting state, it will also have several additional
	  // properties:
	  //
	  // * `regex`: A regular expression that is used to extract parameters from paths
	  //   that reached this accepting state.
	  // * `handlers`: Information on how to convert the list of captures into calls
	  //   to registered handlers with the specified parameters
	  // * `types`: How many static, dynamic or star segments in this route. Used to
	  //   decide which route to use if multiple registered routes match a path.
	  //
	  // Currently, State is implemented naively by looping over `nextStates` and
	  // comparing a character specification against a character. A more efficient
	  // implementation would use a hash of keys pointing at one or more next states.
	
	  function State(charSpec) {
	    this.charSpec = charSpec;
	    this.nextStates = [];
	  }
	
	  State.prototype = {
	    get: function get(charSpec) {
	      var nextStates = this.nextStates;
	
	      for (var i = 0, l = nextStates.length; i < l; i++) {
	        var child = nextStates[i];
	
	        var isEqual = child.charSpec.validChars === charSpec.validChars;
	        isEqual = isEqual && child.charSpec.invalidChars === charSpec.invalidChars;
	
	        if (isEqual) {
	          return child;
	        }
	      }
	    },
	
	    put: function put(charSpec) {
	      var state;
	
	      // If the character specification already exists in a child of the current
	      // state, just return that state.
	      if (state = this.get(charSpec)) {
	        return state;
	      }
	
	      // Make a new state for the character spec
	      state = new State(charSpec);
	
	      // Insert the new state as a child of the current state
	      this.nextStates.push(state);
	
	      // If this character specification repeats, insert the new state as a child
	      // of itself. Note that this will not trigger an infinite loop because each
	      // transition during recognition consumes a character.
	      if (charSpec.repeat) {
	        state.nextStates.push(state);
	      }
	
	      // Return the new state
	      return state;
	    },
	
	    // Find a list of child states matching the next character
	    match: function match(ch) {
	      // DEBUG "Processing `" + ch + "`:"
	      var nextStates = this.nextStates,
	          child,
	          charSpec,
	          chars;
	
	      // DEBUG "  " + debugState(this)
	      var returned = [];
	
	      for (var i = 0, l = nextStates.length; i < l; i++) {
	        child = nextStates[i];
	
	        charSpec = child.charSpec;
	
	        if (typeof (chars = charSpec.validChars) !== 'undefined') {
	          if (chars.indexOf(ch) !== -1) {
	            returned.push(child);
	          }
	        } else if (typeof (chars = charSpec.invalidChars) !== 'undefined') {
	          if (chars.indexOf(ch) === -1) {
	            returned.push(child);
	          }
	        }
	      }
	
	      return returned;
	    }
	
	    /** IF DEBUG
	    , debug: function() {
	      var charSpec = this.charSpec,
	          debug = "[",
	          chars = charSpec.validChars || charSpec.invalidChars;
	       if (charSpec.invalidChars) { debug += "^"; }
	      debug += chars;
	      debug += "]";
	       if (charSpec.repeat) { debug += "+"; }
	       return debug;
	    }
	    END IF **/
	  };
	
	  /** IF DEBUG
	  function debug(log) {
	    console.log(log);
	  }
	
	  function debugState(state) {
	    return state.nextStates.map(function(n) {
	      if (n.nextStates.length === 0) { return "( " + n.debug() + " [accepting] )"; }
	      return "( " + n.debug() + " <then> " + n.nextStates.map(function(s) { return s.debug() }).join(" or ") + " )";
	    }).join(", ")
	  }
	  END IF **/
	
	  // Sort the routes by specificity
	  function sortSolutions(states) {
	    return states.sort(function (a, b) {
	      return b.specificity.val - a.specificity.val;
	    });
	  }
	
	  function recognizeChar(states, ch) {
	    var nextStates = [];
	
	    for (var i = 0, l = states.length; i < l; i++) {
	      var state = states[i];
	
	      nextStates = nextStates.concat(state.match(ch));
	    }
	
	    return nextStates;
	  }
	
	  var oCreate = Object.create || function (proto) {
	    function F() {}
	    F.prototype = proto;
	    return new F();
	  };
	
	  function RecognizeResults(queryParams) {
	    this.queryParams = queryParams || {};
	  }
	  RecognizeResults.prototype = oCreate({
	    splice: Array.prototype.splice,
	    slice: Array.prototype.slice,
	    push: Array.prototype.push,
	    length: 0,
	    queryParams: null
	  });
	
	  function findHandler(state, path, queryParams) {
	    var handlers = state.handlers,
	        regex = state.regex;
	    var captures = path.match(regex),
	        currentCapture = 1;
	    var result = new RecognizeResults(queryParams);
	
	    for (var i = 0, l = handlers.length; i < l; i++) {
	      var handler = handlers[i],
	          names = handler.names,
	          params = {};
	
	      for (var j = 0, m = names.length; j < m; j++) {
	        params[names[j]] = captures[currentCapture++];
	      }
	
	      result.push({ handler: handler.handler, params: params, isDynamic: !!names.length });
	    }
	
	    return result;
	  }
	
	  function addSegment(currentState, segment) {
	    segment.eachChar(function (ch) {
	      var state;
	
	      currentState = currentState.put(ch);
	    });
	
	    return currentState;
	  }
	
	  function decodeQueryParamPart(part) {
	    // http://www.w3.org/TR/html401/interact/forms.html#h-17.13.4.1
	    part = part.replace(/\+/gm, '%20');
	    return tryDecode(part, true);
	  }
	
	  // The main interface
	
	  var RouteRecognizer = function RouteRecognizer() {
	    this.rootState = new State();
	    this.names = {};
	  };
	
	  RouteRecognizer.prototype = {
	    add: function add(routes, options) {
	      var currentState = this.rootState,
	          regex = "^",
	          specificity = {},
	          handlers = [],
	          allSegments = [],
	          name;
	
	      var isEmpty = true;
	
	      for (var i = 0, l = routes.length; i < l; i++) {
	        var route = routes[i],
	            names = [];
	
	        var segments = parse(route.path, names, specificity);
	
	        allSegments = allSegments.concat(segments);
	
	        for (var j = 0, m = segments.length; j < m; j++) {
	          var segment = segments[j];
	
	          if (segment instanceof EpsilonSegment) {
	            continue;
	          }
	
	          isEmpty = false;
	
	          // Add a "/" for the new segment
	          currentState = currentState.put({ validChars: "/" });
	          regex += "/";
	
	          // Add a representation of the segment to the NFA and regex
	          currentState = addSegment(currentState, segment);
	          regex += segment.regex();
	        }
	
	        var handler = { handler: route.handler, names: names };
	        handlers.push(handler);
	      }
	
	      if (isEmpty) {
	        currentState = currentState.put({ validChars: "/" });
	        regex += "/";
	      }
	
	      currentState.handlers = handlers;
	      currentState.regex = new RegExp(regex + "$");
	      currentState.specificity = specificity;
	
	      if (name = options && options.as) {
	        this.names[name] = {
	          segments: allSegments,
	          handlers: handlers
	        };
	      }
	    },
	
	    handlersFor: function handlersFor(name) {
	      var route = this.names[name],
	          result = [];
	      if (!route) {
	        throw new Error("There is no route named " + name);
	      }
	
	      for (var i = 0, l = route.handlers.length; i < l; i++) {
	        result.push(route.handlers[i]);
	      }
	
	      return result;
	    },
	
	    hasRoute: function hasRoute(name) {
	      return !!this.names[name];
	    },
	
	    generate: function generate(name, params) {
	      var route = this.names[name],
	          output = "";
	      if (!route) {
	        throw new Error("There is no route named " + name);
	      }
	
	      var segments = route.segments;
	
	      for (var i = 0, l = segments.length; i < l; i++) {
	        var segment = segments[i];
	
	        if (segment instanceof EpsilonSegment) {
	          continue;
	        }
	
	        output += "/";
	        output += segment.generate(params);
	      }
	
	      if (output.charAt(0) !== '/') {
	        output = '/' + output;
	      }
	
	      if (params && params.queryParams) {
	        output += this.generateQueryString(params.queryParams);
	      }
	
	      return output;
	    },
	
	    generateQueryString: function generateQueryString(params) {
	      var pairs = [];
	      var keys = [];
	      for (var key in params) {
	        if (params.hasOwnProperty(key)) {
	          keys.push(key);
	        }
	      }
	      keys.sort();
	      for (var i = 0, len = keys.length; i < len; i++) {
	        key = keys[i];
	        var value = params[key];
	        if (value == null) {
	          continue;
	        }
	        var pair = encodeURIComponent(key);
	        if (isArray(value)) {
	          for (var j = 0, l = value.length; j < l; j++) {
	            var arrayPair = key + '[]' + '=' + encodeURIComponent(value[j]);
	            pairs.push(arrayPair);
	          }
	        } else {
	          pair += "=" + encodeURIComponent(value);
	          pairs.push(pair);
	        }
	      }
	
	      if (pairs.length === 0) {
	        return '';
	      }
	
	      return "?" + pairs.join("&");
	    },
	
	    parseQueryString: function parseQueryString(queryString) {
	      var pairs = queryString.split("&"),
	          queryParams = {};
	      for (var i = 0; i < pairs.length; i++) {
	        var pair = pairs[i].split('='),
	            key = decodeQueryParamPart(pair[0]),
	            keyLength = key.length,
	            isArray = false,
	            value;
	        if (pair.length === 1) {
	          value = 'true';
	        } else {
	          //Handle arrays
	          if (keyLength > 2 && key.slice(keyLength - 2) === '[]') {
	            isArray = true;
	            key = key.slice(0, keyLength - 2);
	            if (!queryParams[key]) {
	              queryParams[key] = [];
	            }
	          }
	          value = pair[1] ? decodeQueryParamPart(pair[1]) : '';
	        }
	        if (isArray) {
	          queryParams[key].push(value);
	        } else {
	          queryParams[key] = value;
	        }
	      }
	      return queryParams;
	    },
	
	    recognize: function recognize(path, silent) {
	      noWarning = silent;
	      var states = [this.rootState],
	          pathLen,
	          i,
	          l,
	          queryStart,
	          queryParams = {},
	          isSlashDropped = false;
	
	      queryStart = path.indexOf('?');
	      if (queryStart !== -1) {
	        var queryString = path.substr(queryStart + 1, path.length);
	        path = path.substr(0, queryStart);
	        if (queryString) {
	          queryParams = this.parseQueryString(queryString);
	        }
	      }
	
	      path = tryDecode(path);
	      if (!path) return;
	
	      // DEBUG GROUP path
	
	      if (path.charAt(0) !== "/") {
	        path = "/" + path;
	      }
	
	      pathLen = path.length;
	      if (pathLen > 1 && path.charAt(pathLen - 1) === "/") {
	        path = path.substr(0, pathLen - 1);
	        isSlashDropped = true;
	      }
	
	      for (i = 0, l = path.length; i < l; i++) {
	        states = recognizeChar(states, path.charAt(i));
	        if (!states.length) {
	          break;
	        }
	      }
	
	      // END DEBUG GROUP
	
	      var solutions = [];
	      for (i = 0, l = states.length; i < l; i++) {
	        if (states[i].handlers) {
	          solutions.push(states[i]);
	        }
	      }
	
	      states = sortSolutions(solutions);
	
	      var state = solutions[0];
	
	      if (state && state.handlers) {
	        // if a trailing slash was dropped and a star segment is the last segment
	        // specified, put the trailing slash back
	        if (isSlashDropped && state.regex.source.slice(-5) === "(.+)$") {
	          path = path + "/";
	        }
	        return findHandler(state, path, queryParams);
	      }
	    }
	  };
	
	  RouteRecognizer.prototype.map = map;
	
	  var genQuery = RouteRecognizer.prototype.generateQueryString;
	
	  // export default for holding the Vue reference
	  var exports$1 = {};
	  /**
	   * Warn stuff.
	   *
	   * @param {String} msg
	   */
	
	  function warn$1(msg) {
	    /* istanbul ignore next */
	    if (typeof console !== 'undefined') {
	      console.error('[vue-router] ' + msg);
	    }
	  }
	
	  /**
	   * Resolve a relative path.
	   *
	   * @param {String} base
	   * @param {String} relative
	   * @param {Boolean} append
	   * @return {String}
	   */
	
	  function resolvePath(base, relative, append) {
	    var query = base.match(/(\?.*)$/);
	    if (query) {
	      query = query[1];
	      base = base.slice(0, -query.length);
	    }
	    // a query!
	    if (relative.charAt(0) === '?') {
	      return base + relative;
	    }
	    var stack = base.split('/');
	    // remove trailing segment if:
	    // - not appending
	    // - appending to trailing slash (last segment is empty)
	    if (!append || !stack[stack.length - 1]) {
	      stack.pop();
	    }
	    // resolve relative path
	    var segments = relative.replace(/^\//, '').split('/');
	    for (var i = 0; i < segments.length; i++) {
	      var segment = segments[i];
	      if (segment === '.') {
	        continue;
	      } else if (segment === '..') {
	        stack.pop();
	      } else {
	        stack.push(segment);
	      }
	    }
	    // ensure leading slash
	    if (stack[0] !== '') {
	      stack.unshift('');
	    }
	    return stack.join('/');
	  }
	
	  /**
	   * Forgiving check for a promise
	   *
	   * @param {Object} p
	   * @return {Boolean}
	   */
	
	  function isPromise(p) {
	    return p && typeof p.then === 'function';
	  }
	
	  /**
	   * Retrive a route config field from a component instance
	   * OR a component contructor.
	   *
	   * @param {Function|Vue} component
	   * @param {String} name
	   * @return {*}
	   */
	
	  function getRouteConfig(component, name) {
	    var options = component && (component.$options || component.options);
	    return options && options.route && options.route[name];
	  }
	
	  /**
	   * Resolve an async component factory. Have to do a dirty
	   * mock here because of Vue core's internal API depends on
	   * an ID check.
	   *
	   * @param {Object} handler
	   * @param {Function} cb
	   */
	
	  var resolver = undefined;
	
	  function resolveAsyncComponent(handler, cb) {
	    if (!resolver) {
	      resolver = {
	        resolve: exports$1.Vue.prototype._resolveComponent,
	        $options: {
	          components: {
	            _: handler.component
	          }
	        }
	      };
	    } else {
	      resolver.$options.components._ = handler.component;
	    }
	    resolver.resolve('_', function (Component) {
	      handler.component = Component;
	      cb(Component);
	    });
	  }
	
	  /**
	   * Map the dynamic segments in a path to params.
	   *
	   * @param {String} path
	   * @param {Object} params
	   * @param {Object} query
	   */
	
	  function mapParams(path, params, query) {
	    if (params === undefined) params = {};
	
	    path = path.replace(/:([^\/]+)/g, function (_, key) {
	      var val = params[key];
	      /* istanbul ignore if */
	      if (!val) {
	        warn$1('param "' + key + '" not found when generating ' + 'path for "' + path + '" with params ' + JSON.stringify(params));
	      }
	      return val || '';
	    });
	    if (query) {
	      path += genQuery(query);
	    }
	    return path;
	  }
	
	  var hashRE = /#.*$/;
	
	  var HTML5History = (function () {
	    function HTML5History(_ref) {
	      var root = _ref.root;
	      var onChange = _ref.onChange;
	      babelHelpers.classCallCheck(this, HTML5History);
	
	      if (root && root !== '/') {
	        // make sure there's the starting slash
	        if (root.charAt(0) !== '/') {
	          root = '/' + root;
	        }
	        // remove trailing slash
	        this.root = root.replace(/\/$/, '');
	        this.rootRE = new RegExp('^\\' + this.root);
	      } else {
	        this.root = null;
	      }
	      this.onChange = onChange;
	      // check base tag
	      var baseEl = document.querySelector('base');
	      this.base = baseEl && baseEl.getAttribute('href');
	    }
	
	    HTML5History.prototype.start = function start() {
	      var _this = this;
	
	      this.listener = function (e) {
	        var url = location.pathname + location.search;
	        if (_this.root) {
	          url = url.replace(_this.rootRE, '');
	        }
	        _this.onChange(url, e && e.state, location.hash);
	      };
	      window.addEventListener('popstate', this.listener);
	      this.listener();
	    };
	
	    HTML5History.prototype.stop = function stop() {
	      window.removeEventListener('popstate', this.listener);
	    };
	
	    HTML5History.prototype.go = function go(path, replace, append) {
	      var url = this.formatPath(path, append);
	      if (replace) {
	        history.replaceState({}, '', url);
	      } else {
	        // record scroll position by replacing current state
	        history.replaceState({
	          pos: {
	            x: window.pageXOffset,
	            y: window.pageYOffset
	          }
	        }, '', location.href);
	        // then push new state
	        history.pushState({}, '', url);
	      }
	      var hashMatch = path.match(hashRE);
	      var hash = hashMatch && hashMatch[0];
	      path = url
	      // strip hash so it doesn't mess up params
	      .replace(hashRE, '')
	      // remove root before matching
	      .replace(this.rootRE, '');
	      this.onChange(path, null, hash);
	    };
	
	    HTML5History.prototype.formatPath = function formatPath(path, append) {
	      return path.charAt(0) === '/'
	      // absolute path
	      ? this.root ? this.root + '/' + path.replace(/^\//, '') : path : resolvePath(this.base || location.pathname, path, append);
	    };
	
	    return HTML5History;
	  })();
	
	  var HashHistory = (function () {
	    function HashHistory(_ref) {
	      var hashbang = _ref.hashbang;
	      var onChange = _ref.onChange;
	      babelHelpers.classCallCheck(this, HashHistory);
	
	      this.hashbang = hashbang;
	      this.onChange = onChange;
	    }
	
	    HashHistory.prototype.start = function start() {
	      var self = this;
	      this.listener = function () {
	        var path = location.hash;
	        var raw = path.replace(/^#!?/, '');
	        // always
	        if (raw.charAt(0) !== '/') {
	          raw = '/' + raw;
	        }
	        var formattedPath = self.formatPath(raw);
	        if (formattedPath !== path) {
	          location.replace(formattedPath);
	          return;
	        }
	        // determine query
	        // note it's possible to have queries in both the actual URL
	        // and the hash fragment itself.
	        var query = location.search && path.indexOf('?') > -1 ? '&' + location.search.slice(1) : location.search;
	        self.onChange(path.replace(/^#!?/, '') + query);
	      };
	      window.addEventListener('hashchange', this.listener);
	      this.listener();
	    };
	
	    HashHistory.prototype.stop = function stop() {
	      window.removeEventListener('hashchange', this.listener);
	    };
	
	    HashHistory.prototype.go = function go(path, replace, append) {
	      path = this.formatPath(path, append);
	      if (replace) {
	        location.replace(path);
	      } else {
	        location.hash = path;
	      }
	    };
	
	    HashHistory.prototype.formatPath = function formatPath(path, append) {
	      var isAbsoloute = path.charAt(0) === '/';
	      var prefix = '#' + (this.hashbang ? '!' : '');
	      return isAbsoloute ? prefix + path : prefix + resolvePath(location.hash.replace(/^#!?/, ''), path, append);
	    };
	
	    return HashHistory;
	  })();
	
	  var AbstractHistory = (function () {
	    function AbstractHistory(_ref) {
	      var onChange = _ref.onChange;
	      babelHelpers.classCallCheck(this, AbstractHistory);
	
	      this.onChange = onChange;
	      this.currentPath = '/';
	    }
	
	    AbstractHistory.prototype.start = function start() {
	      this.onChange('/');
	    };
	
	    AbstractHistory.prototype.stop = function stop() {
	      // noop
	    };
	
	    AbstractHistory.prototype.go = function go(path, replace, append) {
	      path = this.currentPath = this.formatPath(path, append);
	      this.onChange(path);
	    };
	
	    AbstractHistory.prototype.formatPath = function formatPath(path, append) {
	      return path.charAt(0) === '/' ? path : resolvePath(this.currentPath, path, append);
	    };
	
	    return AbstractHistory;
	  })();
	
	  /**
	   * Determine the reusability of an existing router view.
	   *
	   * @param {Directive} view
	   * @param {Object} handler
	   * @param {Transition} transition
	   */
	
	  function canReuse(view, handler, transition) {
	    var component = view.childVM;
	    if (!component || !handler) {
	      return false;
	    }
	    // important: check view.Component here because it may
	    // have been changed in activate hook
	    if (view.Component !== handler.component) {
	      return false;
	    }
	    var canReuseFn = getRouteConfig(component, 'canReuse');
	    return typeof canReuseFn === 'boolean' ? canReuseFn : canReuseFn ? canReuseFn.call(component, {
	      to: transition.to,
	      from: transition.from
	    }) : true; // defaults to true
	  }
	
	  /**
	   * Check if a component can deactivate.
	   *
	   * @param {Directive} view
	   * @param {Transition} transition
	   * @param {Function} next
	   */
	
	  function canDeactivate(view, transition, next) {
	    var fromComponent = view.childVM;
	    var hook = getRouteConfig(fromComponent, 'canDeactivate');
	    if (!hook) {
	      next();
	    } else {
	      transition.callHook(hook, fromComponent, next, {
	        expectBoolean: true
	      });
	    }
	  }
	
	  /**
	   * Check if a component can activate.
	   *
	   * @param {Object} handler
	   * @param {Transition} transition
	   * @param {Function} next
	   */
	
	  function canActivate(handler, transition, next) {
	    resolveAsyncComponent(handler, function (Component) {
	      // have to check due to async-ness
	      if (transition.aborted) {
	        return;
	      }
	      // determine if this component can be activated
	      var hook = getRouteConfig(Component, 'canActivate');
	      if (!hook) {
	        next();
	      } else {
	        transition.callHook(hook, null, next, {
	          expectBoolean: true
	        });
	      }
	    });
	  }
	
	  /**
	   * Call deactivate hooks for existing router-views.
	   *
	   * @param {Directive} view
	   * @param {Transition} transition
	   * @param {Function} next
	   */
	
	  function deactivate(view, transition, next) {
	    var component = view.childVM;
	    var hook = getRouteConfig(component, 'deactivate');
	    if (!hook) {
	      next();
	    } else {
	      transition.callHooks(hook, component, next);
	    }
	  }
	
	  /**
	   * Activate / switch component for a router-view.
	   *
	   * @param {Directive} view
	   * @param {Transition} transition
	   * @param {Number} depth
	   * @param {Function} [cb]
	   */
	
	  function activate(view, transition, depth, cb, reuse) {
	    var handler = transition.activateQueue[depth];
	    if (!handler) {
	      saveChildView(view);
	      if (view._bound) {
	        view.setComponent(null);
	      }
	      cb && cb();
	      return;
	    }
	
	    var Component = view.Component = handler.component;
	    var activateHook = getRouteConfig(Component, 'activate');
	    var dataHook = getRouteConfig(Component, 'data');
	    var waitForData = getRouteConfig(Component, 'waitForData');
	
	    view.depth = depth;
	    view.activated = false;
	
	    var component = undefined;
	    var loading = !!(dataHook && !waitForData);
	
	    // "reuse" is a flag passed down when the parent view is
	    // either reused via keep-alive or as a child of a kept-alive view.
	    // of course we can only reuse if the current kept-alive instance
	    // is of the correct type.
	    reuse = reuse && view.childVM && view.childVM.constructor === Component;
	
	    if (reuse) {
	      // just reuse
	      component = view.childVM;
	      component.$loadingRouteData = loading;
	    } else {
	      saveChildView(view);
	
	      // unbuild current component. this step also destroys
	      // and removes all nested child views.
	      view.unbuild(true);
	
	      // build the new component. this will also create the
	      // direct child view of the current one. it will register
	      // itself as view.childView.
	      component = view.build({
	        _meta: {
	          $loadingRouteData: loading
	        },
	        created: function created() {
	          this._routerView = view;
	        }
	      });
	
	      // handle keep-alive.
	      // when a kept-alive child vm is restored, we need to
	      // add its cached child views into the router's view list,
	      // and also properly update current view's child view.
	      if (view.keepAlive) {
	        component.$loadingRouteData = loading;
	        var cachedChildView = component._keepAliveRouterView;
	        if (cachedChildView) {
	          view.childView = cachedChildView;
	          component._keepAliveRouterView = null;
	        }
	      }
	    }
	
	    // cleanup the component in case the transition is aborted
	    // before the component is ever inserted.
	    var cleanup = function cleanup() {
	      component.$destroy();
	    };
	
	    // actually insert the component and trigger transition
	    var insert = function insert() {
	      if (reuse) {
	        cb && cb();
	        return;
	      }
	      var router = transition.router;
	      if (router._rendered || router._transitionOnLoad) {
	        view.transition(component);
	      } else {
	        // no transition on first render, manual transition
	        /* istanbul ignore if */
	        if (view.setCurrent) {
	          // 0.12 compat
	          view.setCurrent(component);
	        } else {
	          // 1.0
	          view.childVM = component;
	        }
	        component.$before(view.anchor, null, false);
	      }
	      cb && cb();
	    };
	
	    var afterData = function afterData() {
	      // activate the child view
	      if (view.childView) {
	        activate(view.childView, transition, depth + 1, null, reuse || view.keepAlive);
	      }
	      insert();
	    };
	
	    // called after activation hook is resolved
	    var afterActivate = function afterActivate() {
	      view.activated = true;
	      if (dataHook && waitForData) {
	        // wait until data loaded to insert
	        loadData(component, transition, dataHook, afterData, cleanup);
	      } else {
	        // load data and insert at the same time
	        if (dataHook) {
	          loadData(component, transition, dataHook);
	        }
	        afterData();
	      }
	    };
	
	    if (activateHook) {
	      transition.callHooks(activateHook, component, afterActivate, {
	        cleanup: cleanup,
	        postActivate: true
	      });
	    } else {
	      afterActivate();
	    }
	  }
	
	  /**
	   * Reuse a view, just reload data if necessary.
	   *
	   * @param {Directive} view
	   * @param {Transition} transition
	   */
	
	  function reuse(view, transition) {
	    var component = view.childVM;
	    var dataHook = getRouteConfig(component, 'data');
	    if (dataHook) {
	      loadData(component, transition, dataHook);
	    }
	  }
	
	  /**
	   * Asynchronously load and apply data to component.
	   *
	   * @param {Vue} component
	   * @param {Transition} transition
	   * @param {Function} hook
	   * @param {Function} cb
	   * @param {Function} cleanup
	   */
	
	  function loadData(component, transition, hook, cb, cleanup) {
	    component.$loadingRouteData = true;
	    transition.callHooks(hook, component, function () {
	      component.$loadingRouteData = false;
	      component.$emit('route-data-loaded', component);
	      cb && cb();
	    }, {
	      cleanup: cleanup,
	      postActivate: true,
	      processData: function processData(data) {
	        // handle promise sugar syntax
	        var promises = [];
	        if (isPlainObject(data)) {
	          Object.keys(data).forEach(function (key) {
	            var val = data[key];
	            if (isPromise(val)) {
	              promises.push(val.then(function (resolvedVal) {
	                component.$set(key, resolvedVal);
	              }));
	            } else {
	              component.$set(key, val);
	            }
	          });
	        }
	        if (promises.length) {
	          return promises[0].constructor.all(promises);
	        }
	      }
	    });
	  }
	
	  /**
	   * Save the child view for a kept-alive view so that
	   * we can restore it when it is switched back to.
	   *
	   * @param {Directive} view
	   */
	
	  function saveChildView(view) {
	    if (view.keepAlive && view.childVM && view.childView) {
	      view.childVM._keepAliveRouterView = view.childView;
	    }
	    view.childView = null;
	  }
	
	  /**
	   * Check plain object.
	   *
	   * @param {*} val
	   */
	
	  function isPlainObject(val) {
	    return Object.prototype.toString.call(val) === '[object Object]';
	  }
	
	  /**
	   * A RouteTransition object manages the pipeline of a
	   * router-view switching process. This is also the object
	   * passed into user route hooks.
	   *
	   * @param {Router} router
	   * @param {Route} to
	   * @param {Route} from
	   */
	
	  var RouteTransition = (function () {
	    function RouteTransition(router, to, from) {
	      babelHelpers.classCallCheck(this, RouteTransition);
	
	      this.router = router;
	      this.to = to;
	      this.from = from;
	      this.next = null;
	      this.aborted = false;
	      this.done = false;
	    }
	
	    /**
	     * Abort current transition and return to previous location.
	     */
	
	    RouteTransition.prototype.abort = function abort() {
	      if (!this.aborted) {
	        this.aborted = true;
	        // if the root path throws an error during validation
	        // on initial load, it gets caught in an infinite loop.
	        var abortingOnLoad = !this.from.path && this.to.path === '/';
	        if (!abortingOnLoad) {
	          this.router.replace(this.from.path || '/');
	        }
	      }
	    };
	
	    /**
	     * Abort current transition and redirect to a new location.
	     *
	     * @param {String} path
	     */
	
	    RouteTransition.prototype.redirect = function redirect(path) {
	      if (!this.aborted) {
	        this.aborted = true;
	        if (typeof path === 'string') {
	          path = mapParams(path, this.to.params, this.to.query);
	        } else {
	          path.params = path.params || this.to.params;
	          path.query = path.query || this.to.query;
	        }
	        this.router.replace(path);
	      }
	    };
	
	    /**
	     * A router view transition's pipeline can be described as
	     * follows, assuming we are transitioning from an existing
	     * <router-view> chain [Component A, Component B] to a new
	     * chain [Component A, Component C]:
	     *
	     *  A    A
	     *  | => |
	     *  B    C
	     *
	     * 1. Reusablity phase:
	     *   -> canReuse(A, A)
	     *   -> canReuse(B, C)
	     *   -> determine new queues:
	     *      - deactivation: [B]
	     *      - activation: [C]
	     *
	     * 2. Validation phase:
	     *   -> canDeactivate(B)
	     *   -> canActivate(C)
	     *
	     * 3. Activation phase:
	     *   -> deactivate(B)
	     *   -> activate(C)
	     *
	     * Each of these steps can be asynchronous, and any
	     * step can potentially abort the transition.
	     *
	     * @param {Function} cb
	     */
	
	    RouteTransition.prototype.start = function start(cb) {
	      var transition = this;
	
	      // determine the queue of views to deactivate
	      var deactivateQueue = [];
	      var view = this.router._rootView;
	      while (view) {
	        deactivateQueue.unshift(view);
	        view = view.childView;
	      }
	      var reverseDeactivateQueue = deactivateQueue.slice().reverse();
	
	      // determine the queue of route handlers to activate
	      var activateQueue = this.activateQueue = toArray(this.to.matched).map(function (match) {
	        return match.handler;
	      });
	
	      // 1. Reusability phase
	      var i = undefined,
	          reuseQueue = undefined;
	      for (i = 0; i < reverseDeactivateQueue.length; i++) {
	        if (!canReuse(reverseDeactivateQueue[i], activateQueue[i], transition)) {
	          break;
	        }
	      }
	      if (i > 0) {
	        reuseQueue = reverseDeactivateQueue.slice(0, i);
	        deactivateQueue = reverseDeactivateQueue.slice(i).reverse();
	        activateQueue = activateQueue.slice(i);
	      }
	
	      // 2. Validation phase
	      transition.runQueue(deactivateQueue, canDeactivate, function () {
	        transition.runQueue(activateQueue, canActivate, function () {
	          transition.runQueue(deactivateQueue, deactivate, function () {
	            // 3. Activation phase
	
	            // Update router current route
	            transition.router._onTransitionValidated(transition);
	
	            // trigger reuse for all reused views
	            reuseQueue && reuseQueue.forEach(function (view) {
	              return reuse(view, transition);
	            });
	
	            // the root of the chain that needs to be replaced
	            // is the top-most non-reusable view.
	            if (deactivateQueue.length) {
	              var _view = deactivateQueue[deactivateQueue.length - 1];
	              var depth = reuseQueue ? reuseQueue.length : 0;
	              activate(_view, transition, depth, cb);
	            } else {
	              cb();
	            }
	          });
	        });
	      });
	    };
	
	    /**
	     * Asynchronously and sequentially apply a function to a
	     * queue.
	     *
	     * @param {Array} queue
	     * @param {Function} fn
	     * @param {Function} cb
	     */
	
	    RouteTransition.prototype.runQueue = function runQueue(queue, fn, cb) {
	      var transition = this;
	      step(0);
	      function step(index) {
	        if (index >= queue.length) {
	          cb();
	        } else {
	          fn(queue[index], transition, function () {
	            step(index + 1);
	          });
	        }
	      }
	    };
	
	    /**
	     * Call a user provided route transition hook and handle
	     * the response (e.g. if the user returns a promise).
	     *
	     * If the user neither expects an argument nor returns a
	     * promise, the hook is assumed to be synchronous.
	     *
	     * @param {Function} hook
	     * @param {*} [context]
	     * @param {Function} [cb]
	     * @param {Object} [options]
	     *                 - {Boolean} expectBoolean
	     *                 - {Boolean} postActive
	     *                 - {Function} processData
	     *                 - {Function} cleanup
	     */
	
	    RouteTransition.prototype.callHook = function callHook(hook, context, cb) {
	      var _ref = arguments.length <= 3 || arguments[3] === undefined ? {} : arguments[3];
	
	      var _ref$expectBoolean = _ref.expectBoolean;
	      var expectBoolean = _ref$expectBoolean === undefined ? false : _ref$expectBoolean;
	      var _ref$postActivate = _ref.postActivate;
	      var postActivate = _ref$postActivate === undefined ? false : _ref$postActivate;
	      var processData = _ref.processData;
	      var cleanup = _ref.cleanup;
	
	      var transition = this;
	      var nextCalled = false;
	
	      // abort the transition
	      var abort = function abort() {
	        cleanup && cleanup();
	        transition.abort();
	      };
	
	      // handle errors
	      var onError = function onError(err) {
	        postActivate ? next() : abort();
	        if (err && !transition.router._suppress) {
	          warn$1('Uncaught error during transition: ');
	          throw err instanceof Error ? err : new Error(err);
	        }
	      };
	
	      // since promise swallows errors, we have to
	      // throw it in the next tick...
	      var onPromiseError = function onPromiseError(err) {
	        try {
	          onError(err);
	        } catch (e) {
	          setTimeout(function () {
	            throw e;
	          }, 0);
	        }
	      };
	
	      // advance the transition to the next step
	      var next = function next() {
	        if (nextCalled) {
	          warn$1('transition.next() should be called only once.');
	          return;
	        }
	        nextCalled = true;
	        if (transition.aborted) {
	          cleanup && cleanup();
	          return;
	        }
	        cb && cb();
	      };
	
	      var nextWithBoolean = function nextWithBoolean(res) {
	        if (typeof res === 'boolean') {
	          res ? next() : abort();
	        } else if (isPromise(res)) {
	          res.then(function (ok) {
	            ok ? next() : abort();
	          }, onPromiseError);
	        } else if (!hook.length) {
	          next();
	        }
	      };
	
	      var nextWithData = function nextWithData(data) {
	        var res = undefined;
	        try {
	          res = processData(data);
	        } catch (err) {
	          return onError(err);
	        }
	        if (isPromise(res)) {
	          res.then(next, onPromiseError);
	        } else {
	          next();
	        }
	      };
	
	      // expose a clone of the transition object, so that each
	      // hook gets a clean copy and prevent the user from
	      // messing with the internals.
	      var exposed = {
	        to: transition.to,
	        from: transition.from,
	        abort: abort,
	        next: processData ? nextWithData : next,
	        redirect: function redirect() {
	          transition.redirect.apply(transition, arguments);
	        }
	      };
	
	      // actually call the hook
	      var res = undefined;
	      try {
	        res = hook.call(context, exposed);
	      } catch (err) {
	        return onError(err);
	      }
	
	      if (expectBoolean) {
	        // boolean hooks
	        nextWithBoolean(res);
	      } else if (isPromise(res)) {
	        // promise
	        if (processData) {
	          res.then(nextWithData, onPromiseError);
	        } else {
	          res.then(next, onPromiseError);
	        }
	      } else if (processData && isPlainOjbect(res)) {
	        // data promise sugar
	        nextWithData(res);
	      } else if (!hook.length) {
	        next();
	      }
	    };
	
	    /**
	     * Call a single hook or an array of async hooks in series.
	     *
	     * @param {Array} hooks
	     * @param {*} context
	     * @param {Function} cb
	     * @param {Object} [options]
	     */
	
	    RouteTransition.prototype.callHooks = function callHooks(hooks, context, cb, options) {
	      var _this = this;
	
	      if (Array.isArray(hooks)) {
	        this.runQueue(hooks, function (hook, _, next) {
	          if (!_this.aborted) {
	            _this.callHook(hook, context, next, options);
	          }
	        }, cb);
	      } else {
	        this.callHook(hooks, context, cb, options);
	      }
	    };
	
	    return RouteTransition;
	  })();
	
	  function isPlainOjbect(val) {
	    return Object.prototype.toString.call(val) === '[object Object]';
	  }
	
	  function toArray(val) {
	    return val ? Array.prototype.slice.call(val) : [];
	  }
	
	  var internalKeysRE = /^(component|subRoutes|fullPath)$/;
	
	  /**
	   * Route Context Object
	   *
	   * @param {String} path
	   * @param {Router} router
	   */
	
	  var Route = function Route(path, router) {
	    var _this = this;
	
	    babelHelpers.classCallCheck(this, Route);
	
	    var matched = router._recognizer.recognize(path);
	    if (matched) {
	      // copy all custom fields from route configs
	      [].forEach.call(matched, function (match) {
	        for (var key in match.handler) {
	          if (!internalKeysRE.test(key)) {
	            _this[key] = match.handler[key];
	          }
	        }
	      });
	      // set query and params
	      this.query = matched.queryParams;
	      this.params = [].reduce.call(matched, function (prev, cur) {
	        if (cur.params) {
	          for (var key in cur.params) {
	            prev[key] = cur.params[key];
	          }
	        }
	        return prev;
	      }, {});
	    }
	    // expose path and router
	    this.path = path;
	    // for internal use
	    this.matched = matched || router._notFoundHandler;
	    // internal reference to router
	    Object.defineProperty(this, 'router', {
	      enumerable: false,
	      value: router
	    });
	    // Important: freeze self to prevent observation
	    Object.freeze(this);
	  };
	
	  function applyOverride (Vue) {
	    var _Vue$util = Vue.util;
	    var extend = _Vue$util.extend;
	    var isArray = _Vue$util.isArray;
	    var defineReactive = _Vue$util.defineReactive;
	
	    // override Vue's init and destroy process to keep track of router instances
	    var init = Vue.prototype._init;
	    Vue.prototype._init = function (options) {
	      options = options || {};
	      var root = options._parent || options.parent || this;
	      var router = root.$router;
	      var route = root.$route;
	      if (router) {
	        // expose router
	        this.$router = router;
	        router._children.push(this);
	        /* istanbul ignore if */
	        if (this._defineMeta) {
	          // 0.12
	          this._defineMeta('$route', route);
	        } else {
	          // 1.0
	          defineReactive(this, '$route', route);
	        }
	      }
	      init.call(this, options);
	    };
	
	    var destroy = Vue.prototype._destroy;
	    Vue.prototype._destroy = function () {
	      if (!this._isBeingDestroyed && this.$router) {
	        this.$router._children.$remove(this);
	      }
	      destroy.apply(this, arguments);
	    };
	
	    // 1.0 only: enable route mixins
	    var strats = Vue.config.optionMergeStrategies;
	    var hooksToMergeRE = /^(data|activate|deactivate)$/;
	
	    if (strats) {
	      strats.route = function (parentVal, childVal) {
	        if (!childVal) return parentVal;
	        if (!parentVal) return childVal;
	        var ret = {};
	        extend(ret, parentVal);
	        for (var key in childVal) {
	          var a = ret[key];
	          var b = childVal[key];
	          // for data, activate and deactivate, we need to merge them into
	          // arrays similar to lifecycle hooks.
	          if (a && hooksToMergeRE.test(key)) {
	            ret[key] = (isArray(a) ? a : [a]).concat(b);
	          } else {
	            ret[key] = b;
	          }
	        }
	        return ret;
	      };
	    }
	  }
	
	  function View (Vue) {
	
	    var _ = Vue.util;
	    var componentDef =
	    // 0.12
	    Vue.directive('_component') ||
	    // 1.0
	    Vue.internalDirectives.component;
	    // <router-view> extends the internal component directive
	    var viewDef = _.extend({}, componentDef);
	
	    // with some overrides
	    _.extend(viewDef, {
	
	      _isRouterView: true,
	
	      bind: function bind() {
	        var route = this.vm.$route;
	        /* istanbul ignore if */
	        if (!route) {
	          warn$1('<router-view> can only be used inside a ' + 'router-enabled app.');
	          return;
	        }
	        // force dynamic directive so v-component doesn't
	        // attempt to build right now
	        this._isDynamicLiteral = true;
	        // finally, init by delegating to v-component
	        componentDef.bind.call(this);
	
	        // locate the parent view
	        var parentView = undefined;
	        var parent = this.vm;
	        while (parent) {
	          if (parent._routerView) {
	            parentView = parent._routerView;
	            break;
	          }
	          parent = parent.$parent;
	        }
	        if (parentView) {
	          // register self as a child of the parent view,
	          // instead of activating now. This is so that the
	          // child's activate hook is called after the
	          // parent's has resolved.
	          this.parentView = parentView;
	          parentView.childView = this;
	        } else {
	          // this is the root view!
	          var router = route.router;
	          router._rootView = this;
	        }
	
	        // handle late-rendered view
	        // two possibilities:
	        // 1. root view rendered after transition has been
	        //    validated;
	        // 2. child view rendered after parent view has been
	        //    activated.
	        var transition = route.router._currentTransition;
	        if (!parentView && transition.done || parentView && parentView.activated) {
	          var depth = parentView ? parentView.depth + 1 : 0;
	          activate(this, transition, depth);
	        }
	      },
	
	      unbind: function unbind() {
	        if (this.parentView) {
	          this.parentView.childView = null;
	        }
	        componentDef.unbind.call(this);
	      }
	    });
	
	    Vue.elementDirective('router-view', viewDef);
	  }
	
	  var trailingSlashRE = /\/$/;
	  var regexEscapeRE = /[-.*+?^${}()|[\]\/\\]/g;
	  var queryStringRE = /\?.*$/;
	
	  // install v-link, which provides navigation support for
	  // HTML5 history mode
	  function Link (Vue) {
	    var _Vue$util = Vue.util;
	    var _bind = _Vue$util.bind;
	    var isObject = _Vue$util.isObject;
	    var addClass = _Vue$util.addClass;
	    var removeClass = _Vue$util.removeClass;
	
	    var onPriority = Vue.directive('on').priority;
	    var LINK_UPDATE = '__vue-router-link-update__';
	
	    var activeId = 0;
	
	    Vue.directive('link-active', {
	      priority: 9999,
	      bind: function bind() {
	        var _this = this;
	
	        var id = String(activeId++);
	        // collect v-links contained within this element.
	        // we need do this here before the parent-child relationship
	        // gets messed up by terminal directives (if, for, components)
	        var childLinks = this.el.querySelectorAll('[v-link]');
	        for (var i = 0, l = childLinks.length; i < l; i++) {
	          var link = childLinks[i];
	          var existingId = link.getAttribute(LINK_UPDATE);
	          var value = existingId ? existingId + ',' + id : id;
	          // leave a mark on the link element which can be persisted
	          // through fragment clones.
	          link.setAttribute(LINK_UPDATE, value);
	        }
	        this.vm.$on(LINK_UPDATE, this.cb = function (link, path) {
	          if (link.activeIds.indexOf(id) > -1) {
	            link.updateClasses(path, _this.el);
	          }
	        });
	      },
	      unbind: function unbind() {
	        this.vm.$off(LINK_UPDATE, this.cb);
	      }
	    });
	
	    Vue.directive('link', {
	      priority: onPriority - 2,
	
	      bind: function bind() {
	        var vm = this.vm;
	        /* istanbul ignore if */
	        if (!vm.$route) {
	          warn$1('v-link can only be used inside a router-enabled app.');
	          return;
	        }
	        this.router = vm.$route.router;
	        // update things when the route changes
	        this.unwatch = vm.$watch('$route', _bind(this.onRouteUpdate, this));
	        // check v-link-active ids
	        var activeIds = this.el.getAttribute(LINK_UPDATE);
	        if (activeIds) {
	          this.el.removeAttribute(LINK_UPDATE);
	          this.activeIds = activeIds.split(',');
	        }
	        // no need to handle click if link expects to be opened
	        // in a new window/tab.
	        /* istanbul ignore if */
	        if (this.el.tagName === 'A' && this.el.getAttribute('target') === '_blank') {
	          return;
	        }
	        // handle click
	        this.handler = _bind(this.onClick, this);
	        this.el.addEventListener('click', this.handler);
	      },
	
	      update: function update(target) {
	        this.target = target;
	        if (isObject(target)) {
	          this.append = target.append;
	          this.exact = target.exact;
	          this.prevActiveClass = this.activeClass;
	          this.activeClass = target.activeClass;
	        }
	        this.onRouteUpdate(this.vm.$route);
	      },
	
	      onClick: function onClick(e) {
	        // don't redirect with control keys
	        /* istanbul ignore if */
	        if (e.metaKey || e.ctrlKey || e.shiftKey) return;
	        // don't redirect when preventDefault called
	        /* istanbul ignore if */
	        if (e.defaultPrevented) return;
	        // don't redirect on right click
	        /* istanbul ignore if */
	        if (e.button !== 0) return;
	
	        var target = this.target;
	        if (target) {
	          // v-link with expression, just go
	          e.preventDefault();
	          this.router.go(target);
	        } else {
	          // no expression, delegate for an <a> inside
	          var el = e.target;
	          while (el.tagName !== 'A' && el !== this.el) {
	            el = el.parentNode;
	          }
	          if (el.tagName === 'A' && sameOrigin(el)) {
	            e.preventDefault();
	            var path = el.pathname;
	            if (this.router.history.root) {
	              path = path.replace(this.router.history.rootRE, '');
	            }
	            this.router.go({
	              path: path,
	              replace: target && target.replace,
	              append: target && target.append
	            });
	          }
	        }
	      },
	
	      onRouteUpdate: function onRouteUpdate(route) {
	        // router.stringifyPath is dependent on current route
	        // and needs to be called again whenver route changes.
	        var newPath = this.router.stringifyPath(this.target);
	        if (this.path !== newPath) {
	          this.path = newPath;
	          this.updateActiveMatch();
	          this.updateHref();
	        }
	        if (this.activeIds) {
	          this.vm.$emit(LINK_UPDATE, this, route.path);
	        } else {
	          this.updateClasses(route.path, this.el);
	        }
	      },
	
	      updateActiveMatch: function updateActiveMatch() {
	        this.activeRE = this.path && !this.exact ? new RegExp('^' + this.path.replace(/\/$/, '').replace(queryStringRE, '').replace(regexEscapeRE, '\\$&') + '(\\/|$)') : null;
	      },
	
	      updateHref: function updateHref() {
	        if (this.el.tagName !== 'A') {
	          return;
	        }
	        var path = this.path;
	        var router = this.router;
	        var isAbsolute = path.charAt(0) === '/';
	        // do not format non-hash relative paths
	        var href = path && (router.mode === 'hash' || isAbsolute) ? router.history.formatPath(path, this.append) : path;
	        if (href) {
	          this.el.href = href;
	        } else {
	          this.el.removeAttribute('href');
	        }
	      },
	
	      updateClasses: function updateClasses(path, el) {
	        var activeClass = this.activeClass || this.router._linkActiveClass;
	        // clear old class
	        if (this.prevActiveClass && this.prevActiveClass !== activeClass) {
	          toggleClasses(el, this.prevActiveClass, removeClass);
	        }
	        // remove query string before matching
	        var dest = this.path.replace(queryStringRE, '');
	        path = path.replace(queryStringRE, '');
	        // add new class
	        if (this.exact) {
	          if (dest === path ||
	          // also allow additional trailing slash
	          dest.charAt(dest.length - 1) !== '/' && dest === path.replace(trailingSlashRE, '')) {
	            toggleClasses(el, activeClass, addClass);
	          } else {
	            toggleClasses(el, activeClass, removeClass);
	          }
	        } else {
	          if (this.activeRE && this.activeRE.test(path)) {
	            toggleClasses(el, activeClass, addClass);
	          } else {
	            toggleClasses(el, activeClass, removeClass);
	          }
	        }
	      },
	
	      unbind: function unbind() {
	        this.el.removeEventListener('click', this.handler);
	        this.unwatch && this.unwatch();
	      }
	    });
	
	    function sameOrigin(link) {
	      return link.protocol === location.protocol && link.hostname === location.hostname && link.port === location.port;
	    }
	
	    // this function is copied from v-bind:class implementation until
	    // we properly expose it...
	    function toggleClasses(el, key, fn) {
	      key = key.trim();
	      if (key.indexOf(' ') === -1) {
	        fn(el, key);
	        return;
	      }
	      var keys = key.split(/\s+/);
	      for (var i = 0, l = keys.length; i < l; i++) {
	        fn(el, keys[i]);
	      }
	    }
	  }
	
	  var historyBackends = {
	    abstract: AbstractHistory,
	    hash: HashHistory,
	    html5: HTML5History
	  };
	
	  // late bind during install
	  var Vue = undefined;
	
	  /**
	   * Router constructor
	   *
	   * @param {Object} [options]
	   */
	
	  var Router = (function () {
	    function Router() {
	      var _this = this;
	
	      var _ref = arguments.length <= 0 || arguments[0] === undefined ? {} : arguments[0];
	
	      var _ref$hashbang = _ref.hashbang;
	      var hashbang = _ref$hashbang === undefined ? true : _ref$hashbang;
	      var _ref$abstract = _ref.abstract;
	      var abstract = _ref$abstract === undefined ? false : _ref$abstract;
	      var _ref$history = _ref.history;
	      var history = _ref$history === undefined ? false : _ref$history;
	      var _ref$saveScrollPosition = _ref.saveScrollPosition;
	      var saveScrollPosition = _ref$saveScrollPosition === undefined ? false : _ref$saveScrollPosition;
	      var _ref$transitionOnLoad = _ref.transitionOnLoad;
	      var transitionOnLoad = _ref$transitionOnLoad === undefined ? false : _ref$transitionOnLoad;
	      var _ref$suppressTransitionError = _ref.suppressTransitionError;
	      var suppressTransitionError = _ref$suppressTransitionError === undefined ? false : _ref$suppressTransitionError;
	      var _ref$root = _ref.root;
	      var root = _ref$root === undefined ? null : _ref$root;
	      var _ref$linkActiveClass = _ref.linkActiveClass;
	      var linkActiveClass = _ref$linkActiveClass === undefined ? 'v-link-active' : _ref$linkActiveClass;
	      babelHelpers.classCallCheck(this, Router);
	
	      /* istanbul ignore if */
	      if (!Router.installed) {
	        throw new Error('Please install the Router with Vue.use() before ' + 'creating an instance.');
	      }
	
	      // Vue instances
	      this.app = null;
	      this._children = [];
	
	      // route recognizer
	      this._recognizer = new RouteRecognizer();
	      this._guardRecognizer = new RouteRecognizer();
	
	      // state
	      this._started = false;
	      this._startCb = null;
	      this._currentRoute = {};
	      this._currentTransition = null;
	      this._previousTransition = null;
	      this._notFoundHandler = null;
	      this._notFoundRedirect = null;
	      this._beforeEachHooks = [];
	      this._afterEachHooks = [];
	
	      // trigger transition on initial render?
	      this._rendered = false;
	      this._transitionOnLoad = transitionOnLoad;
	
	      // history mode
	      this._root = root;
	      this._abstract = abstract;
	      this._hashbang = hashbang;
	
	      // check if HTML5 history is available
	      var hasPushState = typeof window !== 'undefined' && window.history && window.history.pushState;
	      this._history = history && hasPushState;
	      this._historyFallback = history && !hasPushState;
	
	      // create history object
	      var inBrowser = Vue.util.inBrowser;
	      this.mode = !inBrowser || this._abstract ? 'abstract' : this._history ? 'html5' : 'hash';
	
	      var History = historyBackends[this.mode];
	      this.history = new History({
	        root: root,
	        hashbang: this._hashbang,
	        onChange: function onChange(path, state, anchor) {
	          _this._match(path, state, anchor);
	        }
	      });
	
	      // other options
	      this._saveScrollPosition = saveScrollPosition;
	      this._linkActiveClass = linkActiveClass;
	      this._suppress = suppressTransitionError;
	    }
	
	    /**
	     * Allow directly passing components to a route
	     * definition.
	     *
	     * @param {String} path
	     * @param {Object} handler
	     */
	
	    // API ===================================================
	
	    /**
	    * Register a map of top-level paths.
	    *
	    * @param {Object} map
	    */
	
	    Router.prototype.map = function map(_map) {
	      for (var route in _map) {
	        this.on(route, _map[route]);
	      }
	      return this;
	    };
	
	    /**
	     * Register a single root-level path
	     *
	     * @param {String} rootPath
	     * @param {Object} handler
	     *                 - {String} component
	     *                 - {Object} [subRoutes]
	     *                 - {Boolean} [forceRefresh]
	     *                 - {Function} [before]
	     *                 - {Function} [after]
	     */
	
	    Router.prototype.on = function on(rootPath, handler) {
	      if (rootPath === '*') {
	        this._notFound(handler);
	      } else {
	        this._addRoute(rootPath, handler, []);
	      }
	      return this;
	    };
	
	    /**
	     * Set redirects.
	     *
	     * @param {Object} map
	     */
	
	    Router.prototype.redirect = function redirect(map) {
	      for (var path in map) {
	        this._addRedirect(path, map[path]);
	      }
	      return this;
	    };
	
	    /**
	     * Set aliases.
	     *
	     * @param {Object} map
	     */
	
	    Router.prototype.alias = function alias(map) {
	      for (var path in map) {
	        this._addAlias(path, map[path]);
	      }
	      return this;
	    };
	
	    /**
	     * Set global before hook.
	     *
	     * @param {Function} fn
	     */
	
	    Router.prototype.beforeEach = function beforeEach(fn) {
	      this._beforeEachHooks.push(fn);
	      return this;
	    };
	
	    /**
	     * Set global after hook.
	     *
	     * @param {Function} fn
	     */
	
	    Router.prototype.afterEach = function afterEach(fn) {
	      this._afterEachHooks.push(fn);
	      return this;
	    };
	
	    /**
	     * Navigate to a given path.
	     * The path can be an object describing a named path in
	     * the format of { name: '...', params: {}, query: {}}
	     * The path is assumed to be already decoded, and will
	     * be resolved against root (if provided)
	     *
	     * @param {String|Object} path
	     * @param {Boolean} [replace]
	     */
	
	    Router.prototype.go = function go(path) {
	      var replace = false;
	      var append = false;
	      if (Vue.util.isObject(path)) {
	        replace = path.replace;
	        append = path.append;
	      }
	      path = this.stringifyPath(path);
	      if (path) {
	        this.history.go(path, replace, append);
	      }
	    };
	
	    /**
	     * Short hand for replacing current path
	     *
	     * @param {String} path
	     */
	
	    Router.prototype.replace = function replace(path) {
	      if (typeof path === 'string') {
	        path = { path: path };
	      }
	      path.replace = true;
	      this.go(path);
	    };
	
	    /**
	     * Start the router.
	     *
	     * @param {VueConstructor} App
	     * @param {String|Element} container
	     * @param {Function} [cb]
	     */
	
	    Router.prototype.start = function start(App, container, cb) {
	      /* istanbul ignore if */
	      if (this._started) {
	        warn$1('already started.');
	        return;
	      }
	      this._started = true;
	      this._startCb = cb;
	      if (!this.app) {
	        /* istanbul ignore if */
	        if (!App || !container) {
	          throw new Error('Must start vue-router with a component and a ' + 'root container.');
	        }
	        /* istanbul ignore if */
	        if (App instanceof Vue) {
	          throw new Error('Must start vue-router with a component, not a ' + 'Vue instance.');
	        }
	        this._appContainer = container;
	        var Ctor = this._appConstructor = typeof App === 'function' ? App : Vue.extend(App);
	        // give it a name for better debugging
	        Ctor.options.name = Ctor.options.name || 'RouterApp';
	      }
	
	      // handle history fallback in browsers that do not
	      // support HTML5 history API
	      if (this._historyFallback) {
	        var _location = window.location;
	        var _history = new HTML5History({ root: this._root });
	        var path = _history.root ? _location.pathname.replace(_history.rootRE, '') : _location.pathname;
	        if (path && path !== '/') {
	          _location.assign((_history.root || '') + '/' + this.history.formatPath(path) + _location.search);
	          return;
	        }
	      }
	
	      this.history.start();
	    };
	
	    /**
	     * Stop listening to route changes.
	     */
	
	    Router.prototype.stop = function stop() {
	      this.history.stop();
	      this._started = false;
	    };
	
	    /**
	     * Normalize named route object / string paths into
	     * a string.
	     *
	     * @param {Object|String|Number} path
	     * @return {String}
	     */
	
	    Router.prototype.stringifyPath = function stringifyPath(path) {
	      var generatedPath = '';
	      if (path && typeof path === 'object') {
	        if (path.name) {
	          var extend = Vue.util.extend;
	          var currentParams = this._currentTransition && this._currentTransition.to.params;
	          var targetParams = path.params || {};
	          var params = currentParams ? extend(extend({}, currentParams), targetParams) : targetParams;
	          generatedPath = encodeURI(this._recognizer.generate(path.name, params));
	        } else if (path.path) {
	          generatedPath = encodeURI(path.path);
	        }
	        if (path.query) {
	          // note: the generated query string is pre-URL-encoded by the recognizer
	          var query = this._recognizer.generateQueryString(path.query);
	          if (generatedPath.indexOf('?') > -1) {
	            generatedPath += '&' + query.slice(1);
	          } else {
	            generatedPath += query;
	          }
	        }
	      } else {
	        generatedPath = encodeURI(path ? path + '' : '');
	      }
	      return generatedPath;
	    };
	
	    // Internal methods ======================================
	
	    /**
	    * Add a route containing a list of segments to the internal
	    * route recognizer. Will be called recursively to add all
	    * possible sub-routes.
	    *
	    * @param {String} path
	    * @param {Object} handler
	    * @param {Array} segments
	    */
	
	    Router.prototype._addRoute = function _addRoute(path, handler, segments) {
	      guardComponent(path, handler);
	      handler.path = path;
	      handler.fullPath = (segments.reduce(function (path, segment) {
	        return path + segment.path;
	      }, '') + path).replace('//', '/');
	      segments.push({
	        path: path,
	        handler: handler
	      });
	      this._recognizer.add(segments, {
	        as: handler.name
	      });
	      // add sub routes
	      if (handler.subRoutes) {
	        for (var subPath in handler.subRoutes) {
	          // recursively walk all sub routes
	          this._addRoute(subPath, handler.subRoutes[subPath],
	          // pass a copy in recursion to avoid mutating
	          // across branches
	          segments.slice());
	        }
	      }
	    };
	
	    /**
	     * Set the notFound route handler.
	     *
	     * @param {Object} handler
	     */
	
	    Router.prototype._notFound = function _notFound(handler) {
	      guardComponent('*', handler);
	      this._notFoundHandler = [{ handler: handler }];
	    };
	
	    /**
	     * Add a redirect record.
	     *
	     * @param {String} path
	     * @param {String} redirectPath
	     */
	
	    Router.prototype._addRedirect = function _addRedirect(path, redirectPath) {
	      if (path === '*') {
	        this._notFoundRedirect = redirectPath;
	      } else {
	        this._addGuard(path, redirectPath, this.replace);
	      }
	    };
	
	    /**
	     * Add an alias record.
	     *
	     * @param {String} path
	     * @param {String} aliasPath
	     */
	
	    Router.prototype._addAlias = function _addAlias(path, aliasPath) {
	      this._addGuard(path, aliasPath, this._match);
	    };
	
	    /**
	     * Add a path guard.
	     *
	     * @param {String} path
	     * @param {String} mappedPath
	     * @param {Function} handler
	     */
	
	    Router.prototype._addGuard = function _addGuard(path, mappedPath, _handler) {
	      var _this2 = this;
	
	      this._guardRecognizer.add([{
	        path: path,
	        handler: function handler(match, query) {
	          var realPath = mapParams(mappedPath, match.params, query);
	          _handler.call(_this2, realPath);
	        }
	      }]);
	    };
	
	    /**
	     * Check if a path matches any redirect records.
	     *
	     * @param {String} path
	     * @return {Boolean} - if true, will skip normal match.
	     */
	
	    Router.prototype._checkGuard = function _checkGuard(path) {
	      var matched = this._guardRecognizer.recognize(path, true);
	      if (matched) {
	        matched[0].handler(matched[0], matched.queryParams);
	        return true;
	      } else if (this._notFoundRedirect) {
	        matched = this._recognizer.recognize(path);
	        if (!matched) {
	          this.replace(this._notFoundRedirect);
	          return true;
	        }
	      }
	    };
	
	    /**
	     * Match a URL path and set the route context on vm,
	     * triggering view updates.
	     *
	     * @param {String} path
	     * @param {Object} [state]
	     * @param {String} [anchor]
	     */
	
	    Router.prototype._match = function _match(path, state, anchor) {
	      var _this3 = this;
	
	      if (this._checkGuard(path)) {
	        return;
	      }
	
	      var currentRoute = this._currentRoute;
	      var currentTransition = this._currentTransition;
	
	      if (currentTransition) {
	        if (currentTransition.to.path === path) {
	          // do nothing if we have an active transition going to the same path
	          return;
	        } else if (currentRoute.path === path) {
	          // We are going to the same path, but we also have an ongoing but
	          // not-yet-validated transition. Abort that transition and reset to
	          // prev transition.
	          currentTransition.aborted = true;
	          this._currentTransition = this._prevTransition;
	          return;
	        } else {
	          // going to a totally different path. abort ongoing transition.
	          currentTransition.aborted = true;
	        }
	      }
	
	      // construct new route and transition context
	      var route = new Route(path, this);
	      var transition = new RouteTransition(this, route, currentRoute);
	
	      // current transition is updated right now.
	      // however, current route will only be updated after the transition has
	      // been validated.
	      this._prevTransition = currentTransition;
	      this._currentTransition = transition;
	
	      if (!this.app) {
	        (function () {
	          // initial render
	          var router = _this3;
	          _this3.app = new _this3._appConstructor({
	            el: _this3._appContainer,
	            created: function created() {
	              this.$router = router;
	            },
	            _meta: {
	              $route: route
	            }
	          });
	        })();
	      }
	
	      // check global before hook
	      var beforeHooks = this._beforeEachHooks;
	      var startTransition = function startTransition() {
	        transition.start(function () {
	          _this3._postTransition(route, state, anchor);
	        });
	      };
	
	      if (beforeHooks.length) {
	        transition.runQueue(beforeHooks, function (hook, _, next) {
	          if (transition === _this3._currentTransition) {
	            transition.callHook(hook, null, next, {
	              expectBoolean: true
	            });
	          }
	        }, startTransition);
	      } else {
	        startTransition();
	      }
	
	      if (!this._rendered && this._startCb) {
	        this._startCb.call(null);
	      }
	
	      // HACK:
	      // set rendered to true after the transition start, so
	      // that components that are acitvated synchronously know
	      // whether it is the initial render.
	      this._rendered = true;
	    };
	
	    /**
	     * Set current to the new transition.
	     * This is called by the transition object when the
	     * validation of a route has succeeded.
	     *
	     * @param {Transition} transition
	     */
	
	    Router.prototype._onTransitionValidated = function _onTransitionValidated(transition) {
	      // set current route
	      var route = this._currentRoute = transition.to;
	      // update route context for all children
	      if (this.app.$route !== route) {
	        this.app.$route = route;
	        this._children.forEach(function (child) {
	          child.$route = route;
	        });
	      }
	      // call global after hook
	      if (this._afterEachHooks.length) {
	        this._afterEachHooks.forEach(function (hook) {
	          return hook.call(null, {
	            to: transition.to,
	            from: transition.from
	          });
	        });
	      }
	      this._currentTransition.done = true;
	    };
	
	    /**
	     * Handle stuff after the transition.
	     *
	     * @param {Route} route
	     * @param {Object} [state]
	     * @param {String} [anchor]
	     */
	
	    Router.prototype._postTransition = function _postTransition(route, state, anchor) {
	      // handle scroll positions
	      // saved scroll positions take priority
	      // then we check if the path has an anchor
	      var pos = state && state.pos;
	      if (pos && this._saveScrollPosition) {
	        Vue.nextTick(function () {
	          window.scrollTo(pos.x, pos.y);
	        });
	      } else if (anchor) {
	        Vue.nextTick(function () {
	          var el = document.getElementById(anchor.slice(1));
	          if (el) {
	            window.scrollTo(window.scrollX, el.offsetTop);
	          }
	        });
	      }
	    };
	
	    return Router;
	  })();
	
	  function guardComponent(path, handler) {
	    var comp = handler.component;
	    if (Vue.util.isPlainObject(comp)) {
	      comp = handler.component = Vue.extend(comp);
	    }
	    /* istanbul ignore if */
	    if (typeof comp !== 'function') {
	      handler.component = null;
	      warn$1('invalid component for route "' + path + '".');
	    }
	  }
	
	  /* Installation */
	
	  Router.installed = false;
	
	  /**
	   * Installation interface.
	   * Install the necessary directives.
	   */
	
	  Router.install = function (externalVue) {
	    /* istanbul ignore if */
	    if (Router.installed) {
	      warn$1('already installed.');
	      return;
	    }
	    Vue = externalVue;
	    applyOverride(Vue);
	    View(Vue);
	    Link(Vue);
	    exports$1.Vue = Vue;
	    Router.installed = true;
	  };
	
	  // auto install
	  /* istanbul ignore if */
	  if (typeof window !== 'undefined' && window.Vue) {
	    window.Vue.use(Router);
	  }
	
	  return Router;
	
	}));

/***/ },
/* 29 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	
	exports.default = function (router) {
	    // 路由配置
	    router.map({
	
	        '/login/:token/:storeid': { name: 'login', component: _login2.default },
	
	        '/tech/:token': { name: 'tech', component: _tech2.default },
	
	        '/tech-my/:token': { name: 'techMy', component: _techMy2.default },
	
	        '/tech-my-head/:token': { name: 'techMyHead', component: _techMyHead2.default },
	
	        '/tech-comment/:token': { name: 'techComment', component: _techComment2.default },
	
	        '/tech-info/:token': { name: 'techInfo', component: _techInfo2.default },
	
	        '/index/:token/:storeid': { name: 'index', component: _index2.default },
	
	        '/consumer/:token/:storeid': { name: 'consumer', component: _consumer2.default },
	
	        '/consumer-pro/:token/:storeid/:id': { name: 'consumerPro', component: _consumerPro2.default },
	
	        '/consumer-tech/:token/:storeid/:id': { name: 'consumerTech', component: _consumerTech2.default },
	
	        '/consumer-order-sub/:token/:storeid/:pid/:tid': { name: 'consumerOrderSub', component: _consumerOrderSub2.default },
	
	        '/consumer-tech-comment/:token/:storeid/:id': { name: 'consumerTechCom', component: _consumerTechComment2.default },
	
	        '/consumer-shop-comment/:token/:storeid': { name: 'consumerShopCom', component: _consumerShopComment2.default },
	
	        '/consumer-vip/:token/:storeid': { name: 'consumerVip', component: _consumerVip2.default },
	
	        '/consumer-order-info/:token/:storeid/:id': { name: 'consumerOrderInfo', component: _consumerOrderInfo2.default },
	
	        '/consumer-order-list/:token/:storeid': { name: 'consumerOrderList', component: _consumerOrderList2.default },
	
	        '/consumer-my/:token/:storeid': { name: 'consumerMy', component: _consumerMy2.default },
	
	        '/consumer-post-comment/:token/:storeid/:id': { name: 'consumerPostComment', component: _consumerPostComment2.default },
	
	        '/consumer-pay/:token/:storeid/:id': { name: 'consumerPay', component: _consumerPay2.default },
	
	        '/manage-shop/:token': { name: 'manageShop', component: _shop2.default },
	
	        '/manage-shop-comment/:token': { name: 'manageShopComment', component: _shopComment2.default },
	
	        '/manage-shop-chart/:token': { name: 'manageShopChart', component: _shopChart2.default },
	
	        '/manage-tech/:token': { name: 'manageTech', component: _tech4.default },
	
	        '/manage-tech-info/:token/:id': { name: 'manageTechInfo', component: _techInfo4.default },
	
	        '/manage-project/:token': { name: 'manageProject', component: _project2.default },
	
	        '/manage-project-info/:token/:id/:switch': { name: 'manageProjectInfo', component: _projectInfo2.default },
	
	        '/consumer-images/:token': { name: 'consumerImages', component: _consumerImages2.default },
	
	        '/consumer-member/:token/:storeid': { name: 'consumerMember', component: _consumerMember2.default }
	
	    });
	
	    // router.beforeEach(function ({ to, next }) {
	    //     if (to.path === '/user') {
	    //         console.log('Winslow');
	    //     }
	    //     next();
	    // });
	
	    router.redirect({
	        '*': '/login/:token'
	    });
	};
	
	var _index = __webpack_require__(30);
	
	var _index2 = _interopRequireDefault(_index);
	
	var _login = __webpack_require__(33);
	
	var _login2 = _interopRequireDefault(_login);
	
	var _tech = __webpack_require__(36);
	
	var _tech2 = _interopRequireDefault(_tech);
	
	var _techMy = __webpack_require__(39);
	
	var _techMy2 = _interopRequireDefault(_techMy);
	
	var _techComment = __webpack_require__(42);
	
	var _techComment2 = _interopRequireDefault(_techComment);
	
	var _techInfo = __webpack_require__(48);
	
	var _techInfo2 = _interopRequireDefault(_techInfo);
	
	var _techMyHead = __webpack_require__(136);
	
	var _techMyHead2 = _interopRequireDefault(_techMyHead);
	
	var _consumer = __webpack_require__(144);
	
	var _consumer2 = _interopRequireDefault(_consumer);
	
	var _consumerPro = __webpack_require__(147);
	
	var _consumerPro2 = _interopRequireDefault(_consumerPro);
	
	var _consumerTech = __webpack_require__(150);
	
	var _consumerTech2 = _interopRequireDefault(_consumerTech);
	
	var _consumerOrderSub = __webpack_require__(153);
	
	var _consumerOrderSub2 = _interopRequireDefault(_consumerOrderSub);
	
	var _consumerTechComment = __webpack_require__(156);
	
	var _consumerTechComment2 = _interopRequireDefault(_consumerTechComment);
	
	var _consumerShopComment = __webpack_require__(159);
	
	var _consumerShopComment2 = _interopRequireDefault(_consumerShopComment);
	
	var _consumerVip = __webpack_require__(162);
	
	var _consumerVip2 = _interopRequireDefault(_consumerVip);
	
	var _consumerMember = __webpack_require__(165);
	
	var _consumerMember2 = _interopRequireDefault(_consumerMember);
	
	var _consumerOrderInfo = __webpack_require__(168);
	
	var _consumerOrderInfo2 = _interopRequireDefault(_consumerOrderInfo);
	
	var _consumerOrderList = __webpack_require__(171);
	
	var _consumerOrderList2 = _interopRequireDefault(_consumerOrderList);
	
	var _consumerMy = __webpack_require__(174);
	
	var _consumerMy2 = _interopRequireDefault(_consumerMy);
	
	var _consumerPostComment = __webpack_require__(177);
	
	var _consumerPostComment2 = _interopRequireDefault(_consumerPostComment);
	
	var _consumerPay = __webpack_require__(180);
	
	var _consumerPay2 = _interopRequireDefault(_consumerPay);
	
	var _consumerImages = __webpack_require__(183);
	
	var _consumerImages2 = _interopRequireDefault(_consumerImages);
	
	var _shop = __webpack_require__(186);
	
	var _shop2 = _interopRequireDefault(_shop);
	
	var _shopComment = __webpack_require__(189);
	
	var _shopComment2 = _interopRequireDefault(_shopComment);
	
	var _shopChart = __webpack_require__(192);
	
	var _shopChart2 = _interopRequireDefault(_shopChart);
	
	var _tech3 = __webpack_require__(196);
	
	var _tech4 = _interopRequireDefault(_tech3);
	
	var _techInfo3 = __webpack_require__(199);
	
	var _techInfo4 = _interopRequireDefault(_techInfo3);
	
	var _project = __webpack_require__(202);
	
	var _project2 = _interopRequireDefault(_project);
	
	var _projectInfo = __webpack_require__(205);
	
	var _projectInfo2 = _interopRequireDefault(_projectInfo);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/***/ },
/* 30 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(31)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/index.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(32)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-36e5a772/index.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 31 */
/***/ function(module, exports) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	// <template>
	// <div class="page" style="background: #EEE;"></div>
	// </template>
	//
	// <script>
	exports.default = {
	    name: 'index',
	    ready: function ready() {
	        var _this = this;
	
	        document.title = '养生汇';
	        this.$store.state.showTip({ type: 'loading', content: '请稍后...' });
	        this.$http.post(API.getUserInfo, {
	            token: this.$route.params.token
	        }).then(function (data) {
	            if (data.data.errorInfo) return;
	            _this.$store.state.hideTip();
	            // 没有选择身份
	            if (data.data.object.wechat_identity == 0) {
	                _this.$router.go('/login/' + _this.$route.params.token + '/' + _this.$route.params.storeid);
	                return;
	            }
	            // 消费者
	            if (data.data.object.wechat_identity == 1) {
	                _this.$router.go('/consumer/' + _this.$route.params.token + '/' + _this.$route.params.storeid);
	                return;
	            }
	            // 技师
	            if (data.data.object.wechat_identity == 2) {
	                _this.$router.go('/tech/' + _this.$route.params.token);
	                return;
	            }
	            // 经理
	            if (data.data.object.wechat_identity == 3 || data.data.object.wechat_identity == 4) {
	                _this.$router.go('/manage-shop/' + _this.$route.params.token);
	                return;
	            }
	        });
	    }
	};
	
	// </script>
	/* generated by vue-loader */

/***/ },
/* 32 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page\" style=\"background: #EEE;\"></div>\n";

/***/ },
/* 33 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(34)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/login.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(35)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-a3966144/login.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 34 */
/***/ function(module, exports) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	// <template>
	// <div class="page login">
	//     <header id="header" class="mui-bar mui-bar-nav">
	//         <h1 class="mui-title">选择身份</h1>
	//         <!-- <a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a> -->
	//     </header>
	//     <div class="mui-scroll-wrapper" style="bottom: 0px;">
	//     <div class="mui-scroll">
	//     <img class="login-tip" :src="'images/login/login-tip.png'" />
	//     <a class="btn-check" :class="{'active': identity==1}" @click="identity = 1">消费者</a>
	//     <a class="btn-check" :class="{'active': identity==2}" @click="identity = 2" >技师</a>
	//     <a class="btn-check" :class="{'active': identity==3}" @click="identity = 3">经理</a>
	//     <a class="btn-check" :class="{'active': identity==4}" @click="identity = 4">老板</a>
	//     <p class="text">提示：不同身份对应不同功能，一旦选择就不能更改！请据实选择！</p>
	//     <p class="password" v-if="identity!=0 && identity!=1"><input type="text" v-model="password"><br>请输入本店验证口令</p>
	//     <p class="number" v-if="identity==2"><input type="text" v-model="number"><br>请输入工号</p>
	//     <button type="button" class="mui-btn mui-btn-warning mui-btn-block" v-if="btnLogin" @click="login">登录</button>
	//     <button type="button" class="mui-btn mui-btn-outlined mui-btn-block" v-if="!btnLogin">登录</button>
	//     <p class="text gay">我们重视您的隐私，不会搜集手机号和位置信息！</p>
	//     </div></div>
	// </div>
	// </template>
	//
	// <script>
	exports.default = {
	    name: 'login',
	    data: function data() {
	        return {
	            identity: 0,
	            btnLogin: false,
	            password: '',
	            number: ''
	        };
	    },
	    ready: function ready() {
	        document.title = '选择身份';
	        mui('.mui-scroll-wrapper').scroll({ bounce: false });
	    },
	    watch: {
	        identity: function identity() {
	            this.isLogin();
	        },
	        password: function password() {
	            this.isLogin();
	        },
	        number: function number() {
	            this.isLogin();
	        }
	    },
	    methods: {
	        isLogin: function isLogin() {
	            if (this.identity == 1) this.btnLogin = true;else if (this.identity == 2) {
	                if (this.password == '' || this.number == '') this.btnLogin = false;else this.btnLogin = true;
	            } else {
	                if (this.password == '') this.btnLogin = false;else this.btnLogin = true;
	            }
	        },
	        login: function login() {
	            var _this = this;
	
	            var _data = {
	                identity: this.identity,
	                storeid: this.$route.params.storeid,
	                code: this.password,
	                num: this.number
	            };
	            this.$store.state.showTip({ type: 'loading', content: '请稍等...' });
	            this.$http.post(API.choiceIdentity, _data).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this.$store.state.hideTip();
	                // 消费者
	                if (_this.identity == 1) {
	                    _this.$router.go('/consumer/' + _this.$route.params.token + '/' + _this.$route.params.storeid);
	                    return;
	                }
	                if (_this.identity == 2) {
	                    _this.$router.go('/tech/' + _this.$route.params.token);
	                    return;
	                }
	                // 经理
	                if (_this.identity == 3 || _this.identity == 4) {
	                    _this.$router.go('/manage-shop/' + _this.$route.params.token);
	                    return;
	                }
	            });
	        }
	    }
	};
	
	// </script>
	/* generated by vue-loader */

/***/ },
/* 35 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page login\">\n    <header id=\"header\" class=\"mui-bar mui-bar-nav\">\n        <h1 class=\"mui-title\">选择身份</h1>\n        <!-- <a class=\"mui-action-back mui-icon mui-icon-left-nav mui-pull-left\"></a> -->\n    </header>\n    <div class=\"mui-scroll-wrapper\" style=\"bottom: 0px;\">\n    <div class=\"mui-scroll\">\n    <img class=\"login-tip\" :src=\"'images/login/login-tip.png'\" />\n    <a class=\"btn-check\" :class=\"{'active': identity==1}\" @click=\"identity = 1\">消费者</a>\n    <a class=\"btn-check\" :class=\"{'active': identity==2}\" @click=\"identity = 2\" >技师</a>\n    <a class=\"btn-check\" :class=\"{'active': identity==3}\" @click=\"identity = 3\">经理</a>\n    <a class=\"btn-check\" :class=\"{'active': identity==4}\" @click=\"identity = 4\">老板</a>\n    <p class=\"text\">提示：不同身份对应不同功能，一旦选择就不能更改！请据实选择！</p>\n    <p class=\"password\" v-if=\"identity!=0 && identity!=1\"><input type=\"text\" v-model=\"password\"><br>请输入本店验证口令</p>\n    <p class=\"number\" v-if=\"identity==2\"><input type=\"text\" v-model=\"number\"><br>请输入工号</p>\n    <button type=\"button\" class=\"mui-btn mui-btn-warning mui-btn-block\" v-if=\"btnLogin\" @click=\"login\">登录</button>\n    <button type=\"button\" class=\"mui-btn mui-btn-outlined mui-btn-block\" v-if=\"!btnLogin\">登录</button>\n    <p class=\"text gay\">我们重视您的隐私，不会搜集手机号和位置信息！</p>\n    </div></div>\n</div>\n";

/***/ },
/* 36 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(37)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/tech.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(38)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-55f680b1/tech.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 37 */
/***/ function(module, exports) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	// <template>
	// <div class="page tech">
	//     <header class="index">
	//         <div id="segmentedControl" class="mui-segmented-control">
	//             <a class="mui-control-item mui-active" href="#item1">当前</a>
	//             <a class="mui-control-item" href="#item2">项目</a>
	//         </div>
	//     </header>
	//
	//     <div class="mui-scroll-wrapper">
	//     <div class="mui-scroll">
	//     <div id="item1" class="mui-control-content fade-in-left mui-active" v-if="Ordering">
	//         <ul class="customers-container">
	//             <li>
	//                 <img v-if="Ordering.wechat_headimgurl" :src="Ordering.wechat_headimgurl"> 
	//                 {{Ordering.wechat_nickname ? Ordering.wechat_nickname : '前台开单'}}
	//                 <i v-if="Ordering.wechat_userlevel" class="lv lv{{Ordering.wechat_userlevel}}"></i>
	//             </li>
	//             <li>项目：<em>{{Ordering.item_name}}</em></li>
	//             <li>消费类型：{{Ordering.order_address == 0 ? '在店消费' : '技师上门'}}</li>
	//             <li>房间号：<span>{{Ordering.room_num}}</span></li>
	//             <li>备注：{{Ordering.order_info}}</li>
	//         </ul>
	//         <form class="mui-input-group select-working">
	//             <div class="mui-input-row mui-radio mui-left" v-if="!Ordering.order_addtime">
	//                 <label>上钟（开始计时）</label>
	//                 <input name="radio1" type="radio" checked>
	//             </div>
	//             <div class="mui-input-row mui-radio mui-left" v-if="Ordering.order_addtime">
	//                 <label>下钟（结束计时）</label>
	//                 <input name="radio1" type="radio" @click="workType = 'end'">
	//             </div>
	//             <div class="mui-input-row mui-radio mui-left" v-if="Ordering.order_addtime && Ordering.order_addtime==1">
	//                 <label>加钟（增加计时）</label>
	//                 <input name="radio1" type="radio" @click="workType = 'add'">
	//             </div>
	//         </form>
	//         <div class="timer" v-if="Ordering.order_addtime">{{jishi}}</div>
	//         <button type="button" class="mui-btn mui-btn-warning mui-btn-block" v-if="!Ordering.order_addtime || workType!='start'" @click="updateOrderStae()">确定</button>
	//         <a style="font-size: 12px;color: #ff7302;" @click="chooseGood(Ordering.order_id)">商品消费</a>
	//     </div>
	//
	//     <div id="item1" class="mui-control-content fade-in-left mui-active" v-if="!Ordering">
	//         <ul class="customers-container">
	//             <li class="nothing" v-show="massagist_status == 2">^_^ 订单马上来哟~</li>
	//             <li class="nothing" v-show="massagist_status == 3">^_^ 您目前正在休假~</li>
	//         </ul>
	//         <form class="mui-input-group select-working">
	//             <div class="mui-input-row mui-radio mui-left {{massagist_status==2 ? 'show-color' : ''}}">
	//                 <label>空闲（正在排班）</label>
	//                 <input name="radio1" type="radio" :checked="massagist_status == 2" value="2" v-model="massagist_status">
	//             </div>
	//             <div class="mui-input-row mui-radio mui-left {{massagist_status==3 ? 'show-color' : ''}}">
	//                 <label>休假（没有上班）</label>
	//                 <input name="radio1" type="radio" :checked="massagist_status == 3" value="3" v-model="massagist_status">
	//             </div>
	//         </form>
	//     </div>
	//
	//     <div id="item2" class="mui-control-content fade-in-right">
	//         <div class="title" @click="showHandlePro=!showHandlePro"><i class="ico-down {{!showHandlePro?'up':''}}"></i>项目调整申请（{{getHandleProject.length}}）</div>
	//         <ul class="mui-table-view item-container" v-show="showHandlePro">
	//             <li class="mui-table-view-cell mui-media" v-for="item in getHandleProject">
	//                 <a href="javascript:;">
	//                     <img class="mui-media-object mui-pull-left" :src="$store.state.URL + item.item_image">
	//                     <div class="mui-media-body">
	//                         {{item.item_name}}
	//                         <p class="mui-ellipsis">申请时间：{{(item.apply_time || '前台添加') | format 'yyyy-MM-dd hh:mm'}}</p>
	//                     </div>
	//                     <i class="status-text">申请{{item.handle == 'add' ? '添加' : '移除'}}</i>
	//                     <i class="status wait" v-if="item.apply_state == 0">等待审核</i>
	//                     <i class="status true" v-if="item.apply_state == 1">已同意</i>
	//                     <i class="status false" v-if="item.apply_state == 2">已拒绝</i>
	//                 </a>
	//             </li>
	//             <div class="no-data" v-if="getHandleProject.length<=0">
	//                 <h6>还没有该项目~</h6>
	//             </div>
	//         </ul>
	//
	//         <div class="title" @click="showHavePro=!showHavePro"><i class="ico-down {{!showHavePro?'up':''}}"></i>已添加项目（{{getHaveProject.length}}）</div>
	//         <ul class="mui-table-view item-container" v-show="showHavePro">
	//             <li class="mui-table-view-cell mui-media" v-for="item in getHaveProject">
	//                 <a href="javascript:;">
	//                     <img class="mui-media-object mui-pull-left" :src="$store.state.URL + item.item_image">
	//                     <div class="mui-media-body center">
	//                         {{item.item_name}}
	//                     </div>
	//                     <i class="status center remove" v-if="item.apply_state!=0" @click="apply(item.item_id, 'delete')">移除</i>
	//                     <i class="status center fail" v-if="item.apply_state==0">已申请</i>
	//                 </a>
	//             </li>
	//             <div class="no-data" v-if="getHaveProject.length<=0">
	//                 <h6>还没有该项目~</h6>
	//             </div>
	//         </ul>
	//
	//         <div class="title" @click="showAddPro=!showAddPro"><i class="ico-down {{!showAddPro?'up':''}}"></i>可申请项目（{{getAddProject.length}}）</div>
	//         <ul class="mui-table-view item-container" v-show="showAddPro">
	//             <li class="mui-table-view-cell mui-media" v-for="item in getAddProject">
	//                 <a href="javascript:;">
	//                     <img class="mui-media-object mui-pull-left" :src="$store.state.URL + item.item_image">
	//                     <div class="mui-media-body center">
	//                         {{item.item_name}}
	//                     </div>
	//                     <i class="status center add" v-if="item.apply_state!=0" @click="apply(item.item_id, 'add')">申请添加</i>
	//                     <i class="status center fail" v-if="item.apply_state==0">已申请</i>
	//                 </a>
	//             </li>
	//             <div class="no-data" v-if="getAddProject.length<=0">
	//                 <h6>还没有该项目~</h6>
	//             </div>
	//         </ul>
	//     </div>
	//     </div>
	//     </div>
	//     <nav class="mui-bar mui-bar-tab">
	//         <a class="winslow-tab-item mui-active">
	//             <span class="iconfont">&#xe67d;</span>
	//             <span class="mui-tab-label">首页</span>
	//         </a>
	//         <a class="winslow-tab-item" @click="local('tech-my')">
	//             <span class="iconfont">&#xe61e;</span>
	//             <span class="mui-tab-label">我的</span>
	//         </a>
	//     </nav>
	//
	//     <div class="choose-good" v-show="goodShow">
	//         <div class="container">
	//             <h1>选择商品</h1>
	//             <ul class="wrap">
	//                 <li v-for="item in goodsList" data-id="{{item.goods_id}}">
	//                     <img src="{{$store.state.URL+item.goods_head}}">
	//                     <span class="name">{{item.goods_name}}</span>
	//                     <span class="price">￥{{item.goods_price}}</span>
	//                     <div class="mui-numbox">
	//                       <button class="mui-btn mui-numbox-btn-minus" type="button">-</button>
	//                       <input class="mui-numbox-input" type="number" value="{{item.value}}" />
	//                       <button class="mui-btn mui-numbox-btn-plus" type="button">+</button>
	//                     </div>
	//                 </li>
	//                 <div v-if="goodsList.length<=0" class="no-data"><h6>店铺没有商品可选</h6></div>
	//             </ul>
	//             <footer>
	//                 <a @click="goodShow = false">取消</a><a @click="subGood()">确定</a>
	//             </footer>
	//         </div>
	//     </div>
	// </div>
	// </template>
	//
	// <script>
	exports.default = {
	    name: 'tech',
	    data: function data() {
	        return {
	            massagist_status: -1,
	            massagist_belongs: false,
	            getHaveProject: [],
	            getHandleProject: [],
	            getAddProject: [],
	            showHavePro: true,
	            showHandlePro: true,
	            showAddPro: true,
	            Ordering: null,
	            workType: 'start',
	            jishi: '',
	            isChangeStatu: 0,
	            goodShow: false,
	            goodsList: [],
	            goodsInfo: {
	                oid: null,
	                gid: []
	            }
	        };
	    },
	    ready: function ready() {
	        var _this = this;
	
	        document.title = '技师';
	        mui('.mui-scroll-wrapper').scroll({ bounce: false });
	        this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	        this.getOrderWork();
	        this.$http.post(API.getMassagistInfo, {}).then(function (data) {
	            if (data.data.errorInfo) return;
	            _this.$store.state.hideTip();
	            _this.massagist_status = data.data.object.massagist_status;
	            _this.massagist_belongs = data.data.object.massagist_belongs;
	        });
	        this.getProject();
	        var me = this;
	        $('.choose-good').on('click', '.mui-numbox-btn-plus', function () {
	            var _ = parseInt($(this).prev().val());
	            $(this).prev().val(_ + 1);
	            me.setGoodId();
	        });
	        $('.choose-good').on('click', '.mui-numbox-btn-minus', function () {
	            var _ = parseInt($(this).next().val());
	            if (_ <= 0) return;
	            $(this).next().val(_ - 1);
	            me.setGoodId();
	        });
	    },
	    watch: {
	        'massagist_status': function massagist_status(val) {
	            var _this2 = this;
	
	            if (this.isChangeStatu == 0) {
	                this.isChangeStatu++;
	                return;
	            }
	            this.$http.post(API.updateTechnicianState, {
	                action: val
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this2.$store.state.hideTip();
	            });
	        }
	    },
	    methods: {
	        local: function local(name) {
	            this.$router.go('/' + name + '/' + this.$route.params.token);
	        },
	        chooseGood: function chooseGood(id) {
	            var _this3 = this;
	
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            var me = this;
	            this.$http.post(API.user_get_this_order_goods, {
	                sid: this.massagist_belongs,
	                pid: id
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                var goodList = data.data.list ? data.data.list : [];
	                _this3.$http.post(API.user_get_goods_select, {
	                    sid: _this3.massagist_belongs
	                }).then(function (data) {
	                    if (data.data.errorInfo) return;
	                    _this3.$store.state.hideTip();
	                    _this3.goodsList = [];
	                    for (var i = 0; i < data.data.list.length; i++) {
	                        _this3.goodsList[i] = data.data.list[i];
	                        _this3.goodsList[i].value = 0;
	                        for (var j = 0; j < goodList.length; j++) {
	                            if (goodList[j].id == me.goodsList[i].goods_id) {
	                                me.goodsList[i].value = goodList[j].num;
	                            }
	                        }
	                    }
	                    _this3.goodShow = true;
	                    _this3.goodsInfo.oid = id;
	                });
	            });
	        },
	        setGoodId: function setGoodId() {
	            var me = this;
	            me.goodsInfo.gid = [];
	            for (var i = 0; i < $('.choose-good li').length; i++) {
	                for (var j = 0; j < $('.choose-good li:eq(' + i + ')').find('input').val(); j++) {
	                    me.goodsInfo.gid.push($('.choose-good li:eq(' + i + ')').attr('data-id'));
	                }
	            }
	        },
	        subGood: function subGood() {
	            var _this4 = this;
	
	            var me = this;
	            if (me.goodsInfo.gid.length == 0) {
	                me.$store.state.showTip({ type: 'error', content: '请添加或修改商品' });
	                return;
	            }
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.user_add_goods_this_order, {
	                sid: this.massagist_belongs,
	                id: this.goodsInfo.oid,
	                goods: this.goodsInfo.gid
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this4.$store.state.showTip({ type: 'success', content: '处理成功' });
	                var socket = new WebSocket("ws://121.43.163.207:9509");
	                var self = _this4;
	                socket.onopen = function (event) {
	                    socket.send('{"store": "' + self.$route.params.storeid + '","fun1":"1","fun2":"2","fun3":"3"}');
	                };
	                socket.onmessage = function (event) {
	                    console.log(event.data);
	                };
	                setTimeout(function () {
	                    location.reload();
	                }, 1500);
	            });
	        },
	        updateOrderStae: function updateOrderStae() {
	            var _this5 = this;
	
	            this.$store.state.showTip({ type: 'loading', content: '处理中...' });
	            this.$http.post(API.updateOrderStae, {
	                action: this.workType
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this5.$store.state.hideTip();
	                _this5.$store.state.showTip({ type: 'success', content: '处理成功' });
	                var socket = new WebSocket("ws://121.43.163.207:9509");
	                var self = _this5;
	                socket.onopen = function (event) {
	                    socket.send('{"store": "' + self.$route.params.storeid + '","fun1":"1","fun2":"2","fun3":"3"}');
	                };
	                socket.onmessage = function (event) {
	                    console.log(event.data);
	                };
	                setTimeout(function () {
	                    location.reload();
	                }, 1500);
	            });
	        },
	        getOrderWork: function getOrderWork() {
	            var _this6 = this;
	
	            // 获取工单
	            this.$http.post(API.getWorkOrder, {
	                working: 'w'
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this6.$store.state.hideTip();
	                if (data.data.object) {
	                    _this6.Ordering = data.data.object;
	                    if (_this6.Ordering.wechat_headimgurl.indexOf('http') < 0) {
	                        _this6.Ordering.wechat_headimgurl = _this6.$store.state.URL + _this6.Ordering.wechat_headimgurl;
	                    }
	                    if (_this6.Ordering.order_addtime) {
	                        if (_this6.Ordering.order_addtime == 1) {
	                            var miao = _this6.Ordering.order_item_starttime - Date.parse(new Date()) / 1000;
	                            _this6.showTime(miao);
	                        }
	                        if (_this6.Ordering.order_addtime == 2) {
	                            _this6.showTime(_this6.Ordering.order_item_starttime);
	                        }
	                    }
	                    if (!_this6.Ordering.wechat_user_viptime) {
	                        _this6.Ordering.wechat_userlevel = 0;
	                    } else if (_this6.Ordering.wechat_user_viptime == '1') {
	                        _this6.Ordering.wechat_userlevel = 7;
	                    } else if (_this6.Ordering.wechat_user_viptime - Date.parse(new Date()) / 1000 <= 0) {
	                        _this6.Ordering.wechat_userlevel = 0;
	                    } else {
	                        _this6.Ordering.wechat_userlevel = 1;
	                    }
	                }
	            });
	        },
	        showTime: function showTime(val) {
	            var _this7 = this;
	
	            var h = Math.floor(val / 60 / 60);
	            var m = Math.floor((val - h * 60 * 60) / 60);
	            var s = Math.floor(val - h * 60 * 60 - m * 60);
	            // document.getElementById("DD").innerHTML = parseInt(h/24);
	            this.jishi = h + ':' + m + ':' + s;
	            if (this.Ordering.order_addtime == 1) val--;else if (this.Ordering.order_addtime == 2) val++;
	            if (val < 0) {
	                this.jishi = '00:00:00';
	            } else {
	                setTimeout(function () {
	                    _this7.showTime(val);
	                }, 1000);
	            }
	        },
	
	        apply: function apply(id, type) {
	            var me = this;
	            var text = '';
	            type == 'add' ? text = '确定申请该项目吗？' : text = '确定移除该项目吗？';
	            mui.confirm(text, '提醒', ['取消', '确定'], function (e) {
	                if (e.index == 1) {
	                    me.$store.state.showTip({ type: 'loading', content: '请稍等...' });
	                    me.$http.post(API.applyProject, {
	                        pid: id,
	                        action: 'add'
	                    }).then(function (data) {
	                        if (data.data.errorInfo) return;
	                        me.$store.state.hideTip();
	                        me.$store.state.showTip({ type: 'success', content: '处理成功' });
	                        me.getProject();
	                    });
	                }
	            });
	        },
	        getProject: function getProject() {
	            var _this8 = this;
	
	            // 获取已有项目
	            this.$http.post(API.getHaveProject, {}).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this8.$store.state.hideTip();
	                _this8.getHaveProject = data.data.list;
	            });
	            // 获取已申请项目
	            this.$http.post(API.getHandleProject, {}).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this8.$store.state.hideTip();
	                _this8.getHandleProject = data.data.list;
	            });
	            // 获取可申请项目
	            this.$http.post(API.getAddProject, {}).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this8.$store.state.hideTip();
	                _this8.getAddProject = data.data.list;
	            });
	        }
	
	    }
	};
	
	// </script>
	/* generated by vue-loader */

/***/ },
/* 38 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page tech\">\n    <header class=\"index\">\n        <div id=\"segmentedControl\" class=\"mui-segmented-control\">\n            <a class=\"mui-control-item mui-active\" href=\"#item1\">当前</a>\n            <a class=\"mui-control-item\" href=\"#item2\">项目</a>\n        </div>\n    </header>\n\n    <div class=\"mui-scroll-wrapper\">\n    <div class=\"mui-scroll\">\n    <div id=\"item1\" class=\"mui-control-content fade-in-left mui-active\" v-if=\"Ordering\">\n        <ul class=\"customers-container\">\n            <li>\n                <img v-if=\"Ordering.wechat_headimgurl\" :src=\"Ordering.wechat_headimgurl\"> \n                {{Ordering.wechat_nickname ? Ordering.wechat_nickname : '前台开单'}}\n                <i v-if=\"Ordering.wechat_userlevel\" class=\"lv lv{{Ordering.wechat_userlevel}}\"></i>\n            </li>\n            <li>项目：<em>{{Ordering.item_name}}</em></li>\n            <li>消费类型：{{Ordering.order_address == 0 ? '在店消费' : '技师上门'}}</li>\n            <li>房间号：<span>{{Ordering.room_num}}</span></li>\n            <li>备注：{{Ordering.order_info}}</li>\n        </ul>\n        <form class=\"mui-input-group select-working\">\n            <div class=\"mui-input-row mui-radio mui-left\" v-if=\"!Ordering.order_addtime\">\n                <label>上钟（开始计时）</label>\n                <input name=\"radio1\" type=\"radio\" checked>\n            </div>\n            <div class=\"mui-input-row mui-radio mui-left\" v-if=\"Ordering.order_addtime\">\n                <label>下钟（结束计时）</label>\n                <input name=\"radio1\" type=\"radio\" @click=\"workType = 'end'\">\n            </div>\n            <div class=\"mui-input-row mui-radio mui-left\" v-if=\"Ordering.order_addtime && Ordering.order_addtime==1\">\n                <label>加钟（增加计时）</label>\n                <input name=\"radio1\" type=\"radio\" @click=\"workType = 'add'\">\n            </div>\n        </form>\n        <div class=\"timer\" v-if=\"Ordering.order_addtime\">{{jishi}}</div>\n        <button type=\"button\" class=\"mui-btn mui-btn-warning mui-btn-block\" v-if=\"!Ordering.order_addtime || workType!='start'\" @click=\"updateOrderStae()\">确定</button>\n        <a style=\"font-size: 12px;color: #ff7302;\" @click=\"chooseGood(Ordering.order_id)\">商品消费</a>\n    </div>\n\n    <div id=\"item1\" class=\"mui-control-content fade-in-left mui-active\" v-if=\"!Ordering\">\n        <ul class=\"customers-container\">\n            <li class=\"nothing\" v-show=\"massagist_status == 2\">^_^ 订单马上来哟~</li>\n            <li class=\"nothing\" v-show=\"massagist_status == 3\">^_^ 您目前正在休假~</li>\n        </ul>\n        <form class=\"mui-input-group select-working\">\n            <div class=\"mui-input-row mui-radio mui-left {{massagist_status==2 ? 'show-color' : ''}}\">\n                <label>空闲（正在排班）</label>\n                <input name=\"radio1\" type=\"radio\" :checked=\"massagist_status == 2\" value=\"2\" v-model=\"massagist_status\">\n            </div>\n            <div class=\"mui-input-row mui-radio mui-left {{massagist_status==3 ? 'show-color' : ''}}\">\n                <label>休假（没有上班）</label>\n                <input name=\"radio1\" type=\"radio\" :checked=\"massagist_status == 3\" value=\"3\" v-model=\"massagist_status\">\n            </div>\n        </form>\n    </div>\n\n    <div id=\"item2\" class=\"mui-control-content fade-in-right\">\n        <div class=\"title\" @click=\"showHandlePro=!showHandlePro\"><i class=\"ico-down {{!showHandlePro?'up':''}}\"></i>项目调整申请（{{getHandleProject.length}}）</div>\n        <ul class=\"mui-table-view item-container\" v-show=\"showHandlePro\">\n            <li class=\"mui-table-view-cell mui-media\" v-for=\"item in getHandleProject\">\n                <a href=\"javascript:;\">\n                    <img class=\"mui-media-object mui-pull-left\" :src=\"$store.state.URL + item.item_image\">\n                    <div class=\"mui-media-body\">\n                        {{item.item_name}}\n                        <p class=\"mui-ellipsis\">申请时间：{{(item.apply_time || '前台添加') | format 'yyyy-MM-dd hh:mm'}}</p>\n                    </div>\n                    <i class=\"status-text\">申请{{item.handle == 'add' ? '添加' : '移除'}}</i>\n                    <i class=\"status wait\" v-if=\"item.apply_state == 0\">等待审核</i>\n                    <i class=\"status true\" v-if=\"item.apply_state == 1\">已同意</i>\n                    <i class=\"status false\" v-if=\"item.apply_state == 2\">已拒绝</i>\n                </a>\n            </li>\n            <div class=\"no-data\" v-if=\"getHandleProject.length<=0\">\n                <h6>还没有该项目~</h6>\n            </div>\n        </ul>\n\n        <div class=\"title\" @click=\"showHavePro=!showHavePro\"><i class=\"ico-down {{!showHavePro?'up':''}}\"></i>已添加项目（{{getHaveProject.length}}）</div>\n        <ul class=\"mui-table-view item-container\" v-show=\"showHavePro\">\n            <li class=\"mui-table-view-cell mui-media\" v-for=\"item in getHaveProject\">\n                <a href=\"javascript:;\">\n                    <img class=\"mui-media-object mui-pull-left\" :src=\"$store.state.URL + item.item_image\">\n                    <div class=\"mui-media-body center\">\n                        {{item.item_name}}\n                    </div>\n                    <i class=\"status center remove\" v-if=\"item.apply_state!=0\" @click=\"apply(item.item_id, 'delete')\">移除</i>\n                    <i class=\"status center fail\" v-if=\"item.apply_state==0\">已申请</i>\n                </a>\n            </li>\n            <div class=\"no-data\" v-if=\"getHaveProject.length<=0\">\n                <h6>还没有该项目~</h6>\n            </div>\n        </ul>\n\n        <div class=\"title\" @click=\"showAddPro=!showAddPro\"><i class=\"ico-down {{!showAddPro?'up':''}}\"></i>可申请项目（{{getAddProject.length}}）</div>\n        <ul class=\"mui-table-view item-container\" v-show=\"showAddPro\">\n            <li class=\"mui-table-view-cell mui-media\" v-for=\"item in getAddProject\">\n                <a href=\"javascript:;\">\n                    <img class=\"mui-media-object mui-pull-left\" :src=\"$store.state.URL + item.item_image\">\n                    <div class=\"mui-media-body center\">\n                        {{item.item_name}}\n                    </div>\n                    <i class=\"status center add\" v-if=\"item.apply_state!=0\" @click=\"apply(item.item_id, 'add')\">申请添加</i>\n                    <i class=\"status center fail\" v-if=\"item.apply_state==0\">已申请</i>\n                </a>\n            </li>\n            <div class=\"no-data\" v-if=\"getAddProject.length<=0\">\n                <h6>还没有该项目~</h6>\n            </div>\n        </ul>\n    </div>\n    </div>\n    </div>\n    <nav class=\"mui-bar mui-bar-tab\">\n        <a class=\"winslow-tab-item mui-active\">\n            <span class=\"iconfont\">&#xe67d;</span>\n            <span class=\"mui-tab-label\">首页</span>\n        </a>\n        <a class=\"winslow-tab-item\" @click=\"local('tech-my')\">\n            <span class=\"iconfont\">&#xe61e;</span>\n            <span class=\"mui-tab-label\">我的</span>\n        </a>\n    </nav>\n\n    <div class=\"choose-good\" v-show=\"goodShow\">\n        <div class=\"container\">\n            <h1>选择商品</h1>\n            <ul class=\"wrap\">\n                <li v-for=\"item in goodsList\" data-id=\"{{item.goods_id}}\">\n                    <img src=\"{{$store.state.URL+item.goods_head}}\">\n                    <span class=\"name\">{{item.goods_name}}</span>\n                    <span class=\"price\">￥{{item.goods_price}}</span>\n                    <div class=\"mui-numbox\">\n                      <button class=\"mui-btn mui-numbox-btn-minus\" type=\"button\">-</button>\n                      <input class=\"mui-numbox-input\" type=\"number\" value=\"{{item.value}}\" />\n                      <button class=\"mui-btn mui-numbox-btn-plus\" type=\"button\">+</button>\n                    </div>\n                </li>\n                <div v-if=\"goodsList.length<=0\" class=\"no-data\"><h6>店铺没有商品可选</h6></div>\n            </ul>\n            <footer>\n                <a @click=\"goodShow = false\">取消</a><a @click=\"subGood()\">确定</a>\n            </footer>\n        </div>\n    </div>\n</div>\n";

/***/ },
/* 39 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(40)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/tech-my.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(41)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-54a943f8/tech-my.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 40 */
/***/ function(module, exports) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	// <template>
	// <div class="page tech" style="padding-top: 0px; background-color: #f1f1f1;">
	//     <header id="header" class="mui-bar mui-bar-nav">
	//         <h1 class="mui-title">我的工单</h1>
	//         <a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a>
	//     </header>
	//     <div class="mui-scroll-wrapper" style="top: 44px;">
	//     <div class="mui-scroll">
	//     <div id="sliderSegmentedControl" class="mui-slider-indicator mui-segmented-control mui-segmented-control-inverted" style=" background-color: #fff;">
	//         <a class="winslow-control-item mui-active" >工单</a>
	//         <a class="winslow-control-item" @click="local('tech-comment')">评价</a>
	//         <a class="winslow-control-item" @click="local('tech-info')">资料</a>
	//     </div>
	//
	//
	//     <div id="item1mobile" class="mui-control-content fade-in-left mui-active">
	//         <div class="title">
	//             <a class="{{period == '' ? 'active' : ''}}" @click="getOrder('')">当天</a>
	//             <a class="{{period == 'week' ? 'active' : ''}}" @click="getOrder('week')">一周内</a>
	//             <a class="{{period == 'month' ? 'active' : ''}}" @click="getOrder('month')">一月内</a>
	//         </div>
	//         <ul class="order-item">
	//             <li v-for="item in orderList">
	//                 <div class="order-code">订单号:{{item.order_num}}</div>
	//                 <div class="mui-table-view-cell mui-media">
	//                     <a href="javascript:;">
	//                         <img class="mui-media-object mui-pull-left" :src="$store.state.URL + item.item_image">
	//                         <div class="mui-media-body mui-ellipsis">
	//                             {{item.item_name}}
	//                             <p>完成时间：{{item.order_donetime | format 'yyyy-MM-dd hh:mm:ss'}}</p>
	//                             <p class="price">订单总价：<span>￥{{item.order_totalprice}}</span></p>
	//                         </div>
	//                     </a>
	//                 </div>
	//                 <div class="bg"></div>
	//             </li>
	//             <div class="no-data" v-if="orderList.length<=0" >
	//                 <h6>还没有订单哟~~~</h6>
	//             </div>
	//         </ul>
	//         <a class="btn-more" v-if="btnPage" @click="nextPage">查看更多</a>
	//     </div>
	//     </div>
	//     </div>
	//     <nav class="mui-bar mui-bar-tab">
	//         <a class="winslow-tab-item " @click="local('tech')">
	//             <span class="iconfont">&#xe67d;</span>
	//             <span class="mui-tab-label">首页</span>
	//         </a>
	//         <a class="winslow-tab-item mui-active">
	//             <span class="iconfont">&#xe61e;</span>
	//             <span class="mui-tab-label">我的</span>
	//         </a>
	//     </nav>
	// </div>
	// </template>
	//
	// <script>
	exports.default = {
	    name: 'techMy',
	    data: function data() {
	        return {
	            active: '',
	            period: '',
	            orderList: [],
	            page: 1,
	            btnPage: false
	        };
	    },
	    ready: function ready() {
	        document.title = '我的工单';
	        mui('.mui-scroll-wrapper').scroll({ bounce: false });
	        this.getOrder('');
	    },
	    methods: {
	        local: function local(name) {
	            this.$router.go('/' + name + '/' + this.$route.params.token);
	        },
	        getOrder: function getOrder(name) {
	            var _this = this;
	
	            this.period = this.active = name;
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.getWorkOrder, {
	                time: this.period,
	                page: this.page
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this.$store.state.hideTip();
	                if (!data.data.list) {
	                    _this.btnPage = false;
	                    return;
	                } else if (data.data.list.length < _this.$store.state.pageSize) {
	                    _this.btnPage = false;
	                } else {
	                    _this.btnPage = true;
	                };
	                _this.orderList = _this.orderList.concat(data.data.list);
	            });
	        },
	        nextPage: function nextPage() {
	            this.page++;
	            this.getOrder(this.active);
	        }
	    }
	};
	
	// </script>
	/* generated by vue-loader */

/***/ },
/* 41 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page tech\" style=\"padding-top: 0px; background-color: #f1f1f1;\">\n    <header id=\"header\" class=\"mui-bar mui-bar-nav\">\n        <h1 class=\"mui-title\">我的工单</h1>\n        <a class=\"mui-action-back mui-icon mui-icon-left-nav mui-pull-left\"></a>\n    </header>\n    <div class=\"mui-scroll-wrapper\" style=\"top: 44px;\">\n    <div class=\"mui-scroll\">\n    <div id=\"sliderSegmentedControl\" class=\"mui-slider-indicator mui-segmented-control mui-segmented-control-inverted\" style=\" background-color: #fff;\">\n        <a class=\"winslow-control-item mui-active\" >工单</a>\n        <a class=\"winslow-control-item\" @click=\"local('tech-comment')\">评价</a>\n        <a class=\"winslow-control-item\" @click=\"local('tech-info')\">资料</a>\n    </div>\n    \n    \n    <div id=\"item1mobile\" class=\"mui-control-content fade-in-left mui-active\">\n        <div class=\"title\">\n            <a class=\"{{period == '' ? 'active' : ''}}\" @click=\"getOrder('')\">当天</a>\n            <a class=\"{{period == 'week' ? 'active' : ''}}\" @click=\"getOrder('week')\">一周内</a>\n            <a class=\"{{period == 'month' ? 'active' : ''}}\" @click=\"getOrder('month')\">一月内</a>\n        </div>\n        <ul class=\"order-item\">\n            <li v-for=\"item in orderList\">\n                <div class=\"order-code\">订单号:{{item.order_num}}</div>\n                <div class=\"mui-table-view-cell mui-media\">\n                    <a href=\"javascript:;\">\n                        <img class=\"mui-media-object mui-pull-left\" :src=\"$store.state.URL + item.item_image\">\n                        <div class=\"mui-media-body mui-ellipsis\">\n                            {{item.item_name}}\n                            <p>完成时间：{{item.order_donetime | format 'yyyy-MM-dd hh:mm:ss'}}</p>\n                            <p class=\"price\">订单总价：<span>￥{{item.order_totalprice}}</span></p>\n                        </div>\n                    </a>\n                </div>\n                <div class=\"bg\"></div>\n            </li>\n            <div class=\"no-data\" v-if=\"orderList.length<=0\" >\n                <h6>还没有订单哟~~~</h6>\n            </div>\n        </ul>\n        <a class=\"btn-more\" v-if=\"btnPage\" @click=\"nextPage\">查看更多</a>\n    </div>\n    </div>\n    </div>\n    <nav class=\"mui-bar mui-bar-tab\">\n        <a class=\"winslow-tab-item \" @click=\"local('tech')\">\n            <span class=\"iconfont\">&#xe67d;</span>\n            <span class=\"mui-tab-label\">首页</span>\n        </a>\n        <a class=\"winslow-tab-item mui-active\">\n            <span class=\"iconfont\">&#xe61e;</span>\n            <span class=\"mui-tab-label\">我的</span>\n        </a>\n    </nav>\n</div>\n";

/***/ },
/* 42 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(43)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/tech-comment.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(47)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-6a6b1043/tech-comment.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 43 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	
	var _start = __webpack_require__(44);
	
	var _start2 = _interopRequireDefault(_start);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	exports.default = {
	    name: 'techComment',
	    data: function data() {
	        return {
	            active: 'all',
	            massagistInfo: {
	                massagist_image: []
	            },
	            info: {},
	            all: {
	                list: [],
	                page: 0,
	                btn: false
	            },
	            bad: {
	                list: [],
	                page: 0,
	                btn: false
	            },
	            good: {
	                list: [],
	                page: 0,
	                btn: false
	            },
	            midd: {
	                list: [],
	                page: 0,
	                btn: false
	            }
	        };
	    },
	    ready: function ready() {
	        var _this = this;
	
	        document.title = '我的评价';
	        mui('.mui-scroll-wrapper').scroll({ bounce: false });
	        this.$http.post(API.getMassagistInfo, {}).then(function (data) {
	            if (data.data.errorInfo) return;
	            _this.$store.state.hideTip();
	            _this.massagistInfo = data.data.object;
	            if (_this.massagistInfo.massagist_photo.indexOf('http') < 0) {
	                _this.massagistInfo.massagist_photo = _this.$store.state.URL + _this.massagistInfo.massagist_photo;
	            }
	        });
	        this.getAll();
	        this.getGood();
	        this.getMidd();
	        this.getBad();
	    },
	    methods: {
	        local: function local(name) {
	            this.$router.go('/' + name + '/' + this.$route.params.token);
	        },
	        getAll: function getAll() {
	            var _this2 = this;
	
	            this.all.page++;
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.getTechnicianEvaluate, {
	                page: this.all.page
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this2.$store.state.hideTip();
	                if (!data.data.list) {
	                    _this2.all.btn = false;
	                    return;
	                } else if (data.data.list.length < _this2.$store.state.pageSize) {
	                    _this2.all.btn = false;
	                } else {
	                    _this2.all.btn = true;
	                };
	                _this2.all.list = _this2.all.list.concat(data.data.list);
	                _this2.info = data.data.object;
	                for (var i = 0; i < _this2.all.list.length; i++) {
	                    if (!_this2.all.list[i].wechat_user_viptime) {
	                        _this2.all.list[i].wechat_userlevel = 0;
	                    } else if (_this2.all.list[i].wechat_user_viptime == '1') {
	                        _this2.all.list[i].wechat_userlevel = 7;
	                    } else if (_this2.all.list[i].wechat_user_viptime - Date.parse(new Date()) / 1000 <= 0) {
	                        _this2.all.list[i].wechat_userlevel = 0;
	                    } else {
	                        _this2.all.list[i].wechat_userlevel = 1;
	                    }
	                    if (_this2.all.list[i].wechat_headimgurl.indexOf('http') < 0) {
	                        _this2.all.list[i].wechat_headimgurl = _this2.$store.state.URL + _this2.all.list[i].wechat_headimgurl;
	                    }
	                }
	            });
	        },
	        getGood: function getGood() {
	            var _this3 = this;
	
	            this.good.page++;
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.getTechnicianEvaluate, {
	                page: this.good.page,
	                type: 1
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this3.$store.state.hideTip();
	                if (!data.data.list) {
	                    _this3.good.btn = false;
	                    return;
	                } else if (data.data.list.length < _this3.$store.state.pageSize) {
	                    _this3.good.btn = false;
	                } else {
	                    _this3.good.btn = true;
	                };
	                _this3.good.list = _this3.good.list.concat(data.data.list);
	                _this3.info = data.data.object;
	                for (var i = 0; i < _this3.good.list.length; i++) {
	                    if (!_this3.good.list[i].wechat_user_viptime) {
	                        _this3.good.list[i].wechat_userlevel = 0;
	                    } else if (_this3.good.list[i].wechat_user_viptime == '1') {
	                        _this3.good.list[i].wechat_userlevel = 7;
	                    } else if (_this3.good.list[i].wechat_user_viptime - Date.parse(new Date()) / 1000 <= 0) {
	                        _this3.good.list[i].wechat_userlevel = 0;
	                    } else {
	                        _this3.good.list[i].wechat_userlevel = 1;
	                    }
	                    if (_this3.good.list[i].wechat_headimgurl.indexOf('http') < 0) {
	                        _this3.good.list[i].wechat_headimgurl = _this3.$store.state.URL + _this3.good.list[i].wechat_headimgurl;
	                    }
	                }
	            });
	        },
	        getMidd: function getMidd() {
	            var _this4 = this;
	
	            this.midd.page++;
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.getTechnicianEvaluate, {
	                page: this.midd.page,
	                type: 2
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this4.$store.state.hideTip();
	                if (!data.data.list) {
	                    _this4.midd.btn = false;
	                    return;
	                } else if (data.data.list.length < _this4.$store.state.pageSize) {
	                    _this4.midd.btn = false;
	                } else {
	                    _this4.midd.btn = true;
	                };
	                _this4.midd.list = _this4.midd.list.concat(data.data.list);
	                _this4.info = data.data.object;
	                for (var i = 0; i < _this4.midd.list.length; i++) {
	                    if (!_this4.midd.list[i].wechat_user_viptime) {
	                        _this4.midd.list[i].wechat_userlevel = 0;
	                    } else if (_this4.midd.list[i].wechat_user_viptime == '1') {
	                        _this4.midd.list[i].wechat_userlevel = 7;
	                    } else if (_this4.midd.list[i].wechat_user_viptime - Date.parse(new Date()) / 1000 <= 0) {
	                        _this4.midd.list[i].wechat_userlevel = 0;
	                    } else {
	                        _this4.midd.list[i].wechat_userlevel = 1;
	                    }
	                    if (_this4.midd.list[i].wechat_headimgurl.indexOf('http') < 0) {
	                        _this4.midd.list[i].wechat_headimgurl = _this4.$store.state.URL + _this4.midd.list[i].wechat_headimgurl;
	                    }
	                }
	            });
	        },
	        getBad: function getBad() {
	            var _this5 = this;
	
	            this.bad.page++;
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.getTechnicianEvaluate, {
	                page: this.bad.page,
	                type: 3
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this5.$store.state.hideTip();
	                if (!data.data.list) {
	                    _this5.bad.btn = false;
	                    return;
	                } else if (data.data.list.length < _this5.$store.state.pageSize) {
	                    _this5.bad.btn = false;
	                } else {
	                    _this5.bad.btn = true;
	                };
	                _this5.bad.list = _this5.bad.list.concat(data.data.list);
	                _this5.info = data.data.object;
	                for (var i = 0; i < _this5.bad.list.length; i++) {
	                    if (!_this5.bad.list[i].wechat_user_viptime) {
	                        _this5.bad.list[i].wechat_userlevel = 0;
	                    } else if (_this5.bad.list[i].wechat_user_viptime == '1') {
	                        _this5.bad.list[i].wechat_userlevel = 7;
	                    } else if (_this5.bad.list[i].wechat_user_viptime - Date.parse(new Date()) / 1000 <= 0) {
	                        _this5.bad.list[i].wechat_userlevel = 0;
	                    } else {
	                        _this5.bad.list[i].wechat_userlevel = 1;
	                    }
	                    if (_this5.bad.list[i].wechat_headimgurl.indexOf('http') < 0) {
	                        _this5.bad.list[i].wechat_headimgurl = _this5.$store.state.URL + _this5.bad.list[i].wechat_headimgurl;
	                    }
	                }
	            });
	        }
	    },
	    components: {
	        Start: _start2.default
	    }
	};
	
	// </script>
	/* generated by vue-loader */
	// <template>
	// <div class="page tech" style="padding-top: 0px;">
	//     <header id="header" class="mui-bar mui-bar-nav">
	//         <h1 class="mui-title">我的评价</h1>
	//         <a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a>
	//     </header>
	//     <div class="mui-scroll-wrapper" style="top: 44px;">
	//     <div class="mui-scroll">
	//     <div id="sliderSegmentedControl" class="mui-slider-indicator mui-segmented-control mui-segmented-control-inverted">
	//         <a class="winslow-control-item " @click="local('tech-my')">工单</a>
	//         <a class="winslow-control-item mui-active" @click="local('tech-comment')">评价</a>
	//         <a class="winslow-control-item" @click="local('tech-info')">资料</a>
	//     </div>
	//
	//     <div class="my-info-container fade-in-left">
	//         <div class="my-info">
	//             <img :src="massagistInfo.massagist_photo" class="head">
	//             <div class="info mui-ellipsis">
	//                 <h1>工号：{{massagistInfo.massagist_no}}</h1>
	//                 <h2>年龄：{{massagistInfo.massagist_birth || '未设置' | years}}</h2>
	//                 <h3><start :value='parseInt(massagistInfo.massagist_store)'></start> {{parseInt(massagistInfo.massagist_store)}}分</h3>
	//             </div>
	//             <div class="info">
	//                 <h1>昵称：{{massagistInfo.massagist_name}}</h1>
	//                 <h2>籍贯：{{massagistInfo.c_name}}</h2>
	//             </div>
	//         </div>
	//     </div>
	//
	//     <div id="item1mobile" class="mui-control-content fade-in-left mui-active">
	//         <div id="sliderSegmentedControl" class="mui-segmented-control title comment sfiowhio">
	//             <a class="mui-control-item mui-active" href="#item1">全部({{info ? info.all : '0'}})</a>
	//             <a class="mui-control-item" href="#item2">好评({{info ? info.good : '0'}})</a>
	//             <a class="mui-control-item" href="#item3">中评({{info ? info.middle : '0'}})</a>
	//             <a class="mui-control-item" href="#item4">差评({{info ? info.bad : '0'}})</a>
	//         </div>
	//         <div id="item1" class="mui-control-content fade-in-left mui-active">
	//             <ul class="comment-item">
	//                 <li v-for="item in all.list">
	//                     <div class="item">
	//                         <img :src="item.wechat_headimgurl" class="head">
	//                         {{item.wechat_nickname}} <i class="lv lv{{item.wechat_userlevel}}"></i><br>
	//                         <div class="starts-container"><start :value="parseInt(item.comment_score_store)"><start></div>
	//                         <h6>
	//                             {{item.comment_time | format 'yyyy-MM-dd'}}
	//                         </h6>
	//                         <div class="tag">
	//                             <a v-for="tag in item.comment_store_label" v-if="tag!=''">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>
	//                         </div>
	//                         <p>{{item.comment}}</p>
	//                         <small v-for="reply in item.reply">
	//                             <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>
	//                             {{reply.comment}}
	//                             <br>
	//                         </small>
	//                     </div>
	//                 </li>
	//                 <div class="no-data" v-if="all.list.length<=0">
	//                     <h1>没有相关评论</h1>
	//                 </div>
	//                 <a class="btn-more" v-if="all.btn" @click="getAll">查看更多</a>
	//             </ul>
	//         </div>
	//
	//         <div id="item2" class="mui-control-content fade-in-left">
	//             <ul class="comment-item">
	//                 <li v-for="item in good.list" v-if="item.comment_level == '1'">
	//                     <div class="item">
	//                         <img :src="item.wechat_headimgurl" class="head">
	//                         {{item.wechat_nickname}} <i class="lv lv{{item.wechat_userlevel}}"></i><br>
	//                         <div class="starts-container"><start :value="parseInt(item.comment_score_store)"><start></div>
	//                         <h6>{{item.comment_time | format 'yyyy-MM-dd'}}</h6>
	//                         <div class="tag">
	//                             <a v-for="tag in item.comment_store_label" v-if="tag!=''">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>
	//                         </div>
	//                         <p>{{item.comment}}</p>
	//                         <small v-for="reply in item.reply">
	//                             <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>
	//                             {{reply.comment}}
	//                             <br>
	//                         </small>
	//                     </div>
	//                 </li>
	//                 <div class="no-data" v-if="good.list.length<=0">
	//                     <h1>没有相关评论</h1>
	//                 </div>
	//                 <a class="btn-more" v-if="good.btn" @click="getGood">查看更多</a>
	//             </ul>
	//         </div>
	//
	//         <div id="item3" class="mui-control-content fade-in-left">
	//             <ul class="comment-item">
	//                 <li v-for="item in midd.list" v-if="item.comment_level == '2'">
	//                     <div class="item">
	//                         <img :src="item.wechat_headimgurl" class="head">
	//                         {{item.wechat_nickname}} <i class="lv lv{{item.wechat_userlevel}}"></i><br>
	//                         <div class="starts-container"><start :value="parseInt(item.comment_score_store)"><start></div>
	//                         <h6>{{item.comment_time | format 'yyyy-MM-dd'}}</h6>
	//                         <div class="tag">
	//                             <a v-for="tag in item.comment_store_label" v-if="tag!=''">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>
	//                         </div>
	//                         <p>{{item.comment}}</p>
	//                         <small v-for="reply in item.reply">
	//                             <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>
	//                             {{reply.comment}}
	//                             <br>
	//                         </small>
	//                     </div>
	//                 </li>
	//                 <div class="no-data" v-if="midd.list.length<=0">
	//                     <h1>没有相关评论</h1>
	//                 </div>
	//                 <a class="btn-more" v-if="midd.btn" @click="getMidd">查看更多</a>
	//             </ul>
	//         </div>
	//
	//         <div id="item4" class="mui-control-content fade-in-left">
	//             <ul class="comment-item">
	//                 <li v-for="item in bad.list" v-if="item.comment_level == '3'">
	//                     <div class="item">
	//                         <img :src="item.wechat_headimgurl" class="head">
	//                         {{item.wechat_nickname}} <i class="lv lv{{item.wechat_userlevel}}"></i><br>
	//                         <div class="starts-container"><start :value="parseInt(item.comment_score_store)"><start></div>
	//                         <h6>{{item.comment_time | format 'yyyy-MM-dd'}}</h6>
	//                         <div class="tag">
	//                             <a v-for="tag in item.comment_store_label" v-if="tag!=''">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>
	//                         </div>
	//                         <p>{{item.comment}}</p>
	//                         <small v-for="reply in item.reply">
	//                             <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>
	//                             {{reply.comment}}
	//                             <br>
	//                         </small>
	//                     </div>
	//                 </li>
	//                 <div class="no-data" v-if="bad.list.length<=0">
	//                     <h1>没有相关评论</h1>
	//                 </div>
	//                 <a class="btn-more" v-if="bad.btn" @click="getBad">查看更多</a>
	//             </ul>
	//         </div>
	//     </div>
	//     </div>
	//     </div>
	//     <nav class="mui-bar mui-bar-tab">
	//         <a class="winslow-tab-item " @click="local('tech')">
	//             <span class="iconfont">&#xe67d;</span>
	//             <span class="mui-tab-label">首页</span>
	//         </a>
	//         <a class="winslow-tab-item mui-active">
	//             <span class="iconfont">&#xe61e;</span>
	//             <span class="mui-tab-label">我的</span>
	//         </a>
	//     </nav>
	//
	// </div>
	// </template>
	//
	// <script>

/***/ },
/* 44 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(45)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/components/start.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(46)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-322274c3/start.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 45 */
/***/ function(module, exports) {

	"use strict";
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	// <template>
	//     <a class="start-container" v-if="!big">
	//         <i class=" {{value>=1 ? 'active' : ''}} ">&#xe688;</i>
	//         <i class=" {{value>=2 ? 'active' : ''}} ">&#xe688;</i>
	//         <i class=" {{value>=3 ? 'active' : ''}} ">&#xe688;</i>
	//         <i class=" {{value>=4 ? 'active' : ''}} ">&#xe688;</i>
	//         <i class=" {{value>=5 ? 'active' : ''}} ">&#xe688;</i>
	//     </a>
	//     <a class="start-container-b" v-if="big">
	//         <i class=" {{value>=1 ? 'active' : ''}} " @click = "click(1)">&#xe688;</i>
	//         <i class=" {{value>=2 ? 'active' : ''}} " @click = "click(2)">&#xe688;</i>
	//         <i class=" {{value>=3 ? 'active' : ''}} " @click = "click(3)">&#xe688;</i>
	//         <i class=" {{value>=4 ? 'active' : ''}} " @click = "click(4)">&#xe688;</i>
	//         <i class=" {{value>=5 ? 'active' : ''}} " @click = "click(5)">&#xe688;</i>
	//     </a>
	// </template>
	// <script>
	exports.default = {
	    props: {
	        value: {
	            type: Number,
	            default: 5
	        },
	        ischange: {
	            type: Boolean,
	            default: false
	        },
	        big: {
	            type: Boolean,
	            default: false
	        }
	    },
	    methods: {
	        click: function click(val) {
	            if (this.ischange) this.value = val;
	        }
	    }
	};
	// </script>
	/* generated by vue-loader */

/***/ },
/* 46 */
/***/ function(module, exports) {

	module.exports = "\n<a class=\"start-container\" v-if=\"!big\">\n    <i class=\" {{value>=1 ? 'active' : ''}} \">&#xe688;</i>\n    <i class=\" {{value>=2 ? 'active' : ''}} \">&#xe688;</i>\n    <i class=\" {{value>=3 ? 'active' : ''}} \">&#xe688;</i>\n    <i class=\" {{value>=4 ? 'active' : ''}} \">&#xe688;</i>\n    <i class=\" {{value>=5 ? 'active' : ''}} \">&#xe688;</i>\n</a>\n<a class=\"start-container-b\" v-if=\"big\">\n    <i class=\" {{value>=1 ? 'active' : ''}} \" @click = \"click(1)\">&#xe688;</i>\n    <i class=\" {{value>=2 ? 'active' : ''}} \" @click = \"click(2)\">&#xe688;</i>\n    <i class=\" {{value>=3 ? 'active' : ''}} \" @click = \"click(3)\">&#xe688;</i>\n    <i class=\" {{value>=4 ? 'active' : ''}} \" @click = \"click(4)\">&#xe688;</i>\n    <i class=\" {{value>=5 ? 'active' : ''}} \" @click = \"click(5)\">&#xe688;</i>\n</a>\n";

/***/ },
/* 47 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page tech\" style=\"padding-top: 0px;\">\n    <header id=\"header\" class=\"mui-bar mui-bar-nav\">\n        <h1 class=\"mui-title\">我的评价</h1>\n        <a class=\"mui-action-back mui-icon mui-icon-left-nav mui-pull-left\"></a>\n    </header>\n    <div class=\"mui-scroll-wrapper\" style=\"top: 44px;\">\n    <div class=\"mui-scroll\">\n    <div id=\"sliderSegmentedControl\" class=\"mui-slider-indicator mui-segmented-control mui-segmented-control-inverted\">\n        <a class=\"winslow-control-item \" @click=\"local('tech-my')\">工单</a>\n        <a class=\"winslow-control-item mui-active\" @click=\"local('tech-comment')\">评价</a>\n        <a class=\"winslow-control-item\" @click=\"local('tech-info')\">资料</a>\n    </div>\n\n    <div class=\"my-info-container fade-in-left\">\n        <div class=\"my-info\">\n            <img :src=\"massagistInfo.massagist_photo\" class=\"head\">\n            <div class=\"info mui-ellipsis\">\n                <h1>工号：{{massagistInfo.massagist_no}}</h1>\n                <h2>年龄：{{massagistInfo.massagist_birth || '未设置' | years}}</h2>\n                <h3><start :value='parseInt(massagistInfo.massagist_store)'></start> {{parseInt(massagistInfo.massagist_store)}}分</h3>\n            </div>\n            <div class=\"info\">\n                <h1>昵称：{{massagistInfo.massagist_name}}</h1>\n                <h2>籍贯：{{massagistInfo.c_name}}</h2>\n            </div>\n        </div>\n    </div>\n    \n    <div id=\"item1mobile\" class=\"mui-control-content fade-in-left mui-active\">\n        <div id=\"sliderSegmentedControl\" class=\"mui-segmented-control title comment sfiowhio\">\n            <a class=\"mui-control-item mui-active\" href=\"#item1\">全部({{info ? info.all : '0'}})</a>\n            <a class=\"mui-control-item\" href=\"#item2\">好评({{info ? info.good : '0'}})</a>\n            <a class=\"mui-control-item\" href=\"#item3\">中评({{info ? info.middle : '0'}})</a>\n            <a class=\"mui-control-item\" href=\"#item4\">差评({{info ? info.bad : '0'}})</a>\n        </div>\n        <div id=\"item1\" class=\"mui-control-content fade-in-left mui-active\">\n            <ul class=\"comment-item\">\n                <li v-for=\"item in all.list\">\n                    <div class=\"item\">\n                        <img :src=\"item.wechat_headimgurl\" class=\"head\">\n                        {{item.wechat_nickname}} <i class=\"lv lv{{item.wechat_userlevel}}\"></i><br>\n                        <div class=\"starts-container\"><start :value=\"parseInt(item.comment_score_store)\"><start></div>\n                        <h6>\n                            {{item.comment_time | format 'yyyy-MM-dd'}}\n                        </h6>\n                        <div class=\"tag\">\n                            <a v-for=\"tag in item.comment_store_label\" v-if=\"tag!=''\">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>\n                        </div>\n                        <p>{{item.comment}}</p>\n                        <small v-for=\"reply in item.reply\">\n                            <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>\n                            {{reply.comment}}\n                            <br>\n                        </small>\n                    </div>\n                </li>\n                <div class=\"no-data\" v-if=\"all.list.length<=0\">\n                    <h1>没有相关评论</h1>\n                </div>\n                <a class=\"btn-more\" v-if=\"all.btn\" @click=\"getAll\">查看更多</a>\n            </ul>\n        </div>\n\n        <div id=\"item2\" class=\"mui-control-content fade-in-left\">\n            <ul class=\"comment-item\">\n                <li v-for=\"item in good.list\" v-if=\"item.comment_level == '1'\">\n                    <div class=\"item\">\n                        <img :src=\"item.wechat_headimgurl\" class=\"head\">\n                        {{item.wechat_nickname}} <i class=\"lv lv{{item.wechat_userlevel}}\"></i><br>\n                        <div class=\"starts-container\"><start :value=\"parseInt(item.comment_score_store)\"><start></div>\n                        <h6>{{item.comment_time | format 'yyyy-MM-dd'}}</h6>\n                        <div class=\"tag\">\n                            <a v-for=\"tag in item.comment_store_label\" v-if=\"tag!=''\">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>\n                        </div>\n                        <p>{{item.comment}}</p>\n                        <small v-for=\"reply in item.reply\">\n                            <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>\n                            {{reply.comment}}\n                            <br>\n                        </small>\n                    </div>\n                </li>\n                <div class=\"no-data\" v-if=\"good.list.length<=0\">\n                    <h1>没有相关评论</h1>\n                </div>\n                <a class=\"btn-more\" v-if=\"good.btn\" @click=\"getGood\">查看更多</a>\n            </ul>\n        </div>\n\n        <div id=\"item3\" class=\"mui-control-content fade-in-left\">\n            <ul class=\"comment-item\">\n                <li v-for=\"item in midd.list\" v-if=\"item.comment_level == '2'\">\n                    <div class=\"item\">\n                        <img :src=\"item.wechat_headimgurl\" class=\"head\">\n                        {{item.wechat_nickname}} <i class=\"lv lv{{item.wechat_userlevel}}\"></i><br>\n                        <div class=\"starts-container\"><start :value=\"parseInt(item.comment_score_store)\"><start></div>\n                        <h6>{{item.comment_time | format 'yyyy-MM-dd'}}</h6>\n                        <div class=\"tag\">\n                            <a v-for=\"tag in item.comment_store_label\" v-if=\"tag!=''\">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>\n                        </div>\n                        <p>{{item.comment}}</p>\n                        <small v-for=\"reply in item.reply\">\n                            <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>\n                            {{reply.comment}}\n                            <br>\n                        </small>\n                    </div>\n                </li>\n                <div class=\"no-data\" v-if=\"midd.list.length<=0\">\n                    <h1>没有相关评论</h1>\n                </div>\n                <a class=\"btn-more\" v-if=\"midd.btn\" @click=\"getMidd\">查看更多</a>\n            </ul>\n        </div>\n\n        <div id=\"item4\" class=\"mui-control-content fade-in-left\">\n            <ul class=\"comment-item\">\n                <li v-for=\"item in bad.list\" v-if=\"item.comment_level == '3'\">\n                    <div class=\"item\">\n                        <img :src=\"item.wechat_headimgurl\" class=\"head\">\n                        {{item.wechat_nickname}} <i class=\"lv lv{{item.wechat_userlevel}}\"></i><br>\n                        <div class=\"starts-container\"><start :value=\"parseInt(item.comment_score_store)\"><start></div>\n                        <h6>{{item.comment_time | format 'yyyy-MM-dd'}}</h6>\n                        <div class=\"tag\">\n                            <a v-for=\"tag in item.comment_store_label\" v-if=\"tag!=''\">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>\n                        </div>\n                        <p>{{item.comment}}</p>\n                        <small v-for=\"reply in item.reply\">\n                            <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>\n                            {{reply.comment}}\n                            <br>\n                        </small>\n                    </div>\n                </li>\n                <div class=\"no-data\" v-if=\"bad.list.length<=0\">\n                    <h1>没有相关评论</h1>\n                </div>\n                <a class=\"btn-more\" v-if=\"bad.btn\" @click=\"getBad\">查看更多</a>\n            </ul>\n        </div>\n    </div>\n    </div>\n    </div>\n    <nav class=\"mui-bar mui-bar-tab\">\n        <a class=\"winslow-tab-item \" @click=\"local('tech')\">\n            <span class=\"iconfont\">&#xe67d;</span>\n            <span class=\"mui-tab-label\">首页</span>\n        </a>\n        <a class=\"winslow-tab-item mui-active\">\n            <span class=\"iconfont\">&#xe61e;</span>\n            <span class=\"mui-tab-label\">我的</span>\n        </a>\n    </nav>\n\n</div>\n";

/***/ },
/* 48 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(49)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/tech-info.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(135)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-490bacfa/tech-info.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 49 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	
	__webpack_require__(50);
	
	__webpack_require__(134);
	
	// <template>
	// <div class="page tech" style="padding-top: 0px; background: #f9f9f9;">
	//     <header id="header" class="mui-bar mui-bar-nav">
	//         <h1 class="mui-title">个人资料</h1>
	//         <a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a>
	//     </header>
	//     <div class="mui-scroll-wrapper" style="top: 44px;">
	//     <div class="mui-scroll">
	//     <div id="sliderSegmentedControl" class="mui-slider-indicator mui-segmented-control mui-segmented-control-inverted">
	//         <a class="winslow-control-item " @click="local('tech-my')">工单</a>
	//         <a class="winslow-control-item " @click="local('tech-comment')">评价</a>
	//         <a class="winslow-control-item mui-active">资料</a>
	//     </div>
	//
	//     <div id="slider" class="mui-slider" >
	//         <a class="edit-bg" v-link="{ path: '/consumer-images/'+$route.params.token }"><i class="iconfont">&#xe670;</i> 编辑</a>
	//         <div class="mui-slider-group mui-slider-loop">
	//             <!-- 额外增加的一个节点(循环轮播：第一个节点是最后一张轮播) -->
	//             <div class="mui-slider-item mui-slider-item-duplicate">
	//                 <a>
	//                     <img :src=" $store.state.URL + massagistInfo.massagist_image[massagistInfo.massagist_image.length - 1].img_url ">
	//                 </a>
	//             </div>
	//
	//             <div class="mui-slider-item" v-for="item in massagistInfo.massagist_image">
	//                 <a>
	//                     <img :src=" $store.state.URL + item.img_url ">
	//                 </a>
	//             </div>
	//             <!-- 额外增加的一个节点(循环轮播：最后一个节点是第一张轮播) -->
	//             <div class="mui-slider-item mui-slider-item-duplicate">
	//                 <a>
	//                     <img :src=" $store.state.URL + massagistInfo.massagist_image[0].img_url ">
	//                 </a>
	//             </div>
	//
	//         </div>
	//         <div class="mui-slider-indicator">
	//             <div class="mui-indicator {{$index == 0 ? 'mui-active' : ''}}" v-for="item in massagistInfo.massagist_image"></div>
	//         </div>
	//     </div>
	//
	//     <ul class="mui-table-view user-info fade-in-left" id="head">
	//         <li class="mui-table-view-cell">
	//             <a class="mui-navigate-right" style="line-height: 50px;" v-link="{path: '/tech-my-head/' + $route.params.token}">
	//                 头像
	//                 <img :src="massagistInfo.massagist_photo" class="head">
	//             </a>
	//         </li>
	//         <li class="mui-table-view-cell" @click="updateName">
	//             昵称
	//             <span class="mui-pull-right">{{massagistInfo.massagist_name}}</span>
	//         </li>
	//         <li class="mui-table-view-cell">
	//             工号
	//             <span class="mui-pull-right">{{massagistInfo.massagist_no}}</span>
	//         </li>
	//         <li class="mui-table-view-cell" @click="showDate">
	//             出生日期
	//             <span class="mui-pull-right">{{massagistInfo.massagist_birth || '未设置' | format 'yyyy-MM-dd'}}</span>
	//         </li>
	//         <li class="mui-table-view-cell" @click="address">
	//             籍贯
	//             <span class="mui-pull-right">{{massagistInfo.c_name}}</span>
	//         </li>
	//         <li class="mui-table-view-cell" @click="sex">
	//             性别
	//             <span class="mui-pull-right">{{massagistInfo.massagist_sex | sex}}</span>
	//         </li>
	//     </ul>
	//
	//     <ul class="mui-table-view user-info fade-in-left">
	//         <li class="mui-table-view-cell">
	//             <a class="mui-navigate-right">
	//                 我的赏金
	//                 <span class="mui-pull-right navigate">￥0</span>
	//             </a>
	//         </li>
	//     </ul>
	//
	//     <p class="explain">
	//         说明：<br>
	//         · 请使用本人真实照片，虚假、色情、违禁照片会直接封号！<br>
	//         · 使用真实照片，可以增加点单量，并获得消费者打赏和系统奖励（功能近期上线）！<br>
	//         · 您的照片只有VIP才能浏览，其他人无法看到，敬请放心！
	//     </p>
	//
	//     </div></div>
	//
	//     <nav class="mui-bar mui-bar-tab">
	//         <a class="winslow-tab-item " @click="local('tech')">
	//             <span class="iconfont">&#xe67d;</span>
	//             <span class="mui-tab-label">首页</span>
	//         </a>
	//         <a class="winslow-tab-item mui-active">
	//             <span class="iconfont">&#xe61e;</span>
	//             <span class="mui-tab-label">我的</span>
	//         </a>
	//     </nav>
	//
	// </div>
	// </template>
	//
	// <script>
	exports.default = {
	    name: 'techInfo',
	    data: function data() {
	        return {
	            massagistInfo: {
	                massagist_image: []
	            },
	            province: []
	        };
	    },
	    ready: function ready() {
	        document.title = '个人资料';
	        mui("#slider").slider();
	        mui('.mui-scroll-wrapper').scroll({ bounce: false });
	        this.resload();
	    },
	    methods: {
	        local: function local(name) {
	            this.$router.go('/' + name + '/' + this.$route.params.token);
	        },
	        updateName: function updateName() {
	            var btnArray = ['取消', '确定'];
	            var me = this;
	            mui.prompt('', me.massagistInfo.massagist_name, '请输入您新的昵称', btnArray, function (e) {
	                if (e.index == 1) {
	                    me.$store.state.showTip({ type: 'loading', content: '修改中...' });
	                    me.$http.post(API.updateUserInfo, {
	                        name: e.value
	                    }).then(function (data) {
	                        if (data.data.errorInfo) return;
	                        me.$store.state.hideTip();
	                        me.$store.state.showTip({ type: 'success', content: '修改成功' });
	                        setTimeout(function () {
	                            me.resload();
	                        }, 2000);
	                    });
	                }
	            });
	        },
	        resload: function resload() {
	            var _this = this;
	
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.getMassagistInfo, {}).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this.$store.state.hideTip();
	                _this.massagistInfo = data.data.object;
	                if (_this.massagistInfo.massagist_photo.indexOf('http') < 0) {
	                    _this.massagistInfo.massagist_photo = _this.$store.state.URL + _this.massagistInfo.massagist_photo;
	                }
	            });
	        },
	        showDate: function showDate() {
	            var me = this;
	            var _data = {
	                type: "date",
	                beginYear: 1900,
	                endDate: new Date()
	            };
	            if (me.massagistInfo.massagist_birth) {
	                _data.value = new Date(me.massagistInfo.massagist_birth * 1000).format('yyyy-MM-dd');
	            }
	            var picker = new mui.DtPicker(_data);
	            picker.show(function (rs) {
	                me.$store.state.showTip({ type: 'loading', content: '修改中...' });
	                me.$http.post(API.updateUserInfo, {
	                    birthday: rs.text
	                }).then(function (data) {
	                    if (data.data.errorInfo) return;
	                    me.$store.state.hideTip();
	                    me.$store.state.showTip({ type: 'success', content: '修改成功' });
	                    setTimeout(function () {
	                        me.resload();
	                    }, 2000);
	                });
	                picker.dispose();
	            });
	        },
	        address: function address() {
	            var _this2 = this;
	
	            var me = this;
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.getProvince, {}).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this2.$store.state.hideTip();
	                _this2.province = data.data.list;
	                var addressPicker = new mui.PopPicker();
	                addressPicker.setData(me.province);
	                if (me.massagistInfo.massagist_native) addressPicker.pickers[0].setSelectedValue(me.massagistInfo.massagist_native);
	                addressPicker.show(function (items) {
	                    me.$store.state.showTip({ type: 'loading', content: '修改中...' });
	                    me.$http.post(API.updateUserInfo, {
	                        place: items[0].value
	                    }).then(function (data) {
	                        if (data.data.errorInfo) return;
	                        me.$store.state.hideTip();
	                        me.$store.state.showTip({ type: 'success', content: '修改成功' });
	                        setTimeout(function () {
	                            me.resload();
	                        }, 2000);
	                    });
	                });
	            });
	        },
	        sex: function sex() {
	            var sexPicker = new mui.PopPicker();
	            var me = this;
	            sexPicker.setData([{ value: 1, text: '男' }, { value: 2, text: '女' }]);
	            sexPicker.pickers[0].setSelectedValue(me.massagistInfo.massagist_sex);
	            sexPicker.show(function (items) {
	                me.$store.state.showTip({ type: 'loading', content: '修改中...' });
	                me.$http.post(API.updateUserInfo, {
	                    sex: items[0].value
	                }).then(function (data) {
	                    if (data.data.errorInfo) return;
	                    me.$store.state.hideTip();
	                    me.$store.state.showTip({ type: 'success', content: '修改成功' });
	                    setTimeout(function () {
	                        me.resload();
	                    }, 2000);
	                });
	            });
	        }
	    }
	};
	
	// </script>
	/* generated by vue-loader */

/***/ },
/* 50 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	var _muiMin = __webpack_require__(51);
	
	var _muiMin2 = _interopRequireDefault(_muiMin);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	window.mui = (0, _muiMin2.default)(function ($, window, document, undefined) {
	
		var MAX_EXCEED = 30;
		var VISIBLE_RANGE = 90;
		var DEFAULT_ITEM_HEIGHT = 40;
		var BLUR_WIDTH = 10;
	
		var rad2deg = $.rad2deg = function (rad) {
			return rad / (Math.PI / 180);
		};
	
		var deg2rad = $.deg2rad = function (deg) {
			return deg * (Math.PI / 180);
		};
	
		var platform = navigator.platform.toLowerCase();
		var userAgent = navigator.userAgent.toLowerCase();
		var isIos = (userAgent.indexOf('iphone') > -1 || userAgent.indexOf('ipad') > -1 || userAgent.indexOf('ipod') > -1) && (platform.indexOf('iphone') > -1 || platform.indexOf('ipad') > -1 || platform.indexOf('ipod') > -1);
		//alert(isIos);
	
		var Picker = $.Picker = function (holder, options) {
			var self = this;
			self.holder = holder;
			self.options = options || {};
			self.init();
			self.initInertiaParams();
			self.calcElementItemPostion(true);
			self.bindEvent();
		};
	
		Picker.prototype.findElementItems = function () {
			var self = this;
			self.elementItems = [].slice.call(self.holder.querySelectorAll('li'));
			return self.elementItems;
		};
	
		Picker.prototype.init = function () {
			var self = this;
			self.list = self.holder.querySelector('ul');
			self.findElementItems();
			self.height = self.holder.offsetHeight;
			self.r = self.height / 2 - BLUR_WIDTH;
			self.d = self.r * 2;
			self.itemHeight = self.elementItems.length > 0 ? self.elementItems[0].offsetHeight : DEFAULT_ITEM_HEIGHT;
			self.itemAngle = parseInt(self.calcAngle(self.itemHeight * 0.8));
			self.hightlightRange = self.itemAngle / 2;
			self.visibleRange = VISIBLE_RANGE;
			self.beginAngle = 0;
			self.beginExceed = self.beginAngle - MAX_EXCEED;
			self.list.angle = self.beginAngle;
			if (isIos) {
				self.list.style.webkitTransformOrigin = "center center " + self.r + "px";
			}
		};
	
		Picker.prototype.calcElementItemPostion = function (andGenerateItms) {
			var self = this;
			if (andGenerateItms) {
				self.items = [];
			}
			self.elementItems.forEach(function (item) {
				var index = self.elementItems.indexOf(item);
				self.endAngle = self.itemAngle * index;
				item.angle = self.endAngle;
				item.style.webkitTransformOrigin = "center center -" + self.r + "px";
				item.style.webkitTransform = "translateZ(" + self.r + "px) rotateX(" + -self.endAngle + "deg)";
				if (andGenerateItms) {
					var dataItem = {};
					dataItem.text = item.innerHTML || '';
					dataItem.value = item.getAttribute('data-value') || dataItem.text;
					self.items.push(dataItem);
				}
			});
			self.endExceed = self.endAngle + MAX_EXCEED;
			self.calcElementItemVisibility(self.beginAngle);
		};
	
		Picker.prototype.calcAngle = function (c) {
			var self = this;
			var a = parseFloat(self.r);
			var b = parseFloat(self.r);
			//直径的整倍数部分直接乘以 180
			c = Math.abs(c); //只算角度不关心正否值
			var intDeg = parseInt(c / self.d) * 180;
			c = c % self.d;
			//余弦
			var cosC = (a * a + b * b - c * c) / (2 * a * b);
			var angleC = intDeg + rad2deg(Math.acos(cosC));
			return angleC;
		};
	
		Picker.prototype.calcElementItemVisibility = function (angle) {
			var self = this;
			self.elementItems.forEach(function (item) {
				var difference = Math.abs(item.angle - angle);
				if (difference < self.hightlightRange) {
					item.classList.add('highlight');
				} else if (difference < self.visibleRange) {
					item.classList.add('visible');
					item.classList.remove('highlight');
				} else {
					item.classList.remove('highlight');
					item.classList.remove('visible');
				}
			});
		};
	
		Picker.prototype.setAngle = function (angle) {
			var self = this;
			self.list.angle = angle;
			self.list.style.webkitTransform = "perspective(1000px) rotateY(0deg) rotateX(" + angle + "deg)";
			self.calcElementItemVisibility(angle);
		};
	
		Picker.prototype.bindEvent = function () {
			var self = this;
			var lastAngle = 0;
			var startY = null;
			var isPicking = false;
			self.holder.addEventListener($.EVENT_START, function (event) {
				isPicking = true;
				event.preventDefault();
				self.list.style.webkitTransition = '';
				startY = (event.changedTouches ? event.changedTouches[0] : event).pageY;
				lastAngle = self.list.angle;
				self.updateInertiaParams(event, true);
			}, false);
			self.holder.addEventListener($.EVENT_END, function (event) {
				isPicking = false;
				event.preventDefault();
				self.startInertiaScroll(event);
			}, false);
			self.holder.addEventListener($.EVENT_CANCEL, function (event) {
				isPicking = false;
				event.preventDefault();
				self.startInertiaScroll(event);
			}, false);
			self.holder.addEventListener($.EVENT_MOVE, function (event) {
				if (!isPicking) {
					return;
				}
				event.preventDefault();
				var endY = (event.changedTouches ? event.changedTouches[0] : event).pageY;
				var dragRange = endY - startY;
				var dragAngle = self.calcAngle(dragRange);
				var newAngle = dragRange > 0 ? lastAngle - dragAngle : lastAngle + dragAngle;
				if (newAngle > self.endExceed) {
					newAngle = self.endExceed;
				}
				if (newAngle < self.beginExceed) {
					newAngle = self.beginExceed;
				}
				self.setAngle(newAngle);
				self.updateInertiaParams(event);
			}, false);
			//--
			self.list.addEventListener('tap', function (event) {
				var elementItem = event.target;
				if (elementItem.tagName == 'LI') {
					self.setSelectedIndex(self.elementItems.indexOf(elementItem), 200);
				}
			}, false);
		};
	
		Picker.prototype.initInertiaParams = function () {
			var self = this;
			self.lastMoveTime = 0;
			self.lastMoveStart = 0;
			self.stopInertiaMove = false;
		};
	
		Picker.prototype.updateInertiaParams = function (event, isStart) {
			var self = this;
			var point = event.changedTouches ? event.changedTouches[0] : event;
			if (isStart) {
				self.lastMoveStart = point.pageY;
				self.lastMoveTime = event.timeStamp || Date.now();
				self.startAngle = self.list.angle;
			} else {
				var nowTime = event.timeStamp || Date.now();
				if (nowTime - self.lastMoveTime > 300) {
					self.lastMoveTime = nowTime;
					self.lastMoveStart = point.pageY;
				}
			}
			self.stopInertiaMove = true;
		};
	
		Picker.prototype.startInertiaScroll = function (event) {
			var self = this;
			var point = event.changedTouches ? event.changedTouches[0] : event;
			/** 
	   * 缓动代码
	   */
			var nowTime = event.timeStamp || Date.now();
			var v = (point.pageY - self.lastMoveStart) / (nowTime - self.lastMoveTime); //最后一段时间手指划动速度  
			var dir = v > 0 ? -1 : 1; //加速度方向  
			var deceleration = dir * 0.0006 * -1;
			var duration = Math.abs(v / deceleration); // 速度消减至0所需时间  
			var dist = v * duration / 2; //最终移动多少 
			var startAngle = self.list.angle;
			var distAngle = self.calcAngle(dist) * dir;
			//----
			var srcDistAngle = distAngle;
			if (startAngle + distAngle < self.beginExceed) {
				distAngle = self.beginExceed - startAngle;
				duration = duration * (distAngle / srcDistAngle) * 0.6;
			}
			if (startAngle + distAngle > self.endExceed) {
				distAngle = self.endExceed - startAngle;
				duration = duration * (distAngle / srcDistAngle) * 0.6;
			}
			//----
			if (distAngle == 0) {
				self.endScroll();
				return;
			}
			self.scrollDistAngle(nowTime, startAngle, distAngle, duration);
		};
	
		Picker.prototype.scrollDistAngle = function (nowTime, startAngle, distAngle, duration) {
			var self = this;
			self.stopInertiaMove = false;
			(function (nowTime, startAngle, distAngle, duration) {
				var frameInterval = 13;
				var stepCount = duration / frameInterval;
				var stepIndex = 0;
				(function inertiaMove() {
					if (self.stopInertiaMove) return;
					var newAngle = self.quartEaseOut(stepIndex, startAngle, distAngle, stepCount);
					self.setAngle(newAngle);
					stepIndex++;
					if (stepIndex > stepCount - 1 || newAngle < self.beginExceed || newAngle > self.endExceed) {
						self.endScroll();
						return;
					}
					setTimeout(inertiaMove, frameInterval);
				})();
			})(nowTime, startAngle, distAngle, duration);
		};
	
		Picker.prototype.quartEaseOut = function (t, b, c, d) {
			return -c * ((t = t / d - 1) * t * t * t - 1) + b;
		};
	
		Picker.prototype.endScroll = function () {
			var self = this;
			if (self.list.angle < self.beginAngle) {
				self.list.style.webkitTransition = "150ms ease-out";
				self.setAngle(self.beginAngle);
			} else if (self.list.angle > self.endAngle) {
				self.list.style.webkitTransition = "150ms ease-out";
				self.setAngle(self.endAngle);
			} else {
				var index = parseInt((self.list.angle / self.itemAngle).toFixed(0));
				self.list.style.webkitTransition = "100ms ease-out";
				self.setAngle(self.itemAngle * index);
			}
			self.triggerChange();
		};
	
		Picker.prototype.triggerChange = function (force) {
			var self = this;
			setTimeout(function () {
				var index = self.getSelectedIndex();
				var item = self.items[index];
				if ($.trigger && (index != self.lastIndex || force === true)) {
					$.trigger(self.holder, 'change', {
						"index": index,
						"item": item
					});
					//console.log('change:' + index);
				}
				self.lastIndex = index;
				typeof force === 'function' && force();
			}, 0);
		};
	
		Picker.prototype.correctAngle = function (angle) {
			var self = this;
			if (angle < self.beginAngle) {
				return self.beginAngle;
			} else if (angle > self.endAngle) {
				return self.endAngle;
			} else {
				return angle;
			}
		};
	
		Picker.prototype.setItems = function (items) {
			var self = this;
			self.items = items || [];
			var buffer = [];
			self.items.forEach(function (item) {
				if (item !== null && item !== undefined) {
					buffer.push('<li>' + (item.text || item) + '</li>');
				}
			});
			self.list.innerHTML = buffer.join('');
			self.findElementItems();
			self.calcElementItemPostion();
			self.setAngle(self.correctAngle(self.list.angle));
			self.triggerChange(true);
		};
	
		Picker.prototype.getItems = function () {
			var self = this;
			return self.items;
		};
	
		Picker.prototype.getSelectedIndex = function () {
			var self = this;
			return parseInt((self.list.angle / self.itemAngle).toFixed(0));
		};
	
		Picker.prototype.setSelectedIndex = function (index, duration, callback) {
			var self = this;
			self.list.style.webkitTransition = '';
			var angle = self.correctAngle(self.itemAngle * index);
			if (duration && duration > 0) {
				var distAngle = angle - self.list.angle;
				self.scrollDistAngle(Date.now(), self.list.angle, distAngle, duration);
			} else {
				self.setAngle(angle);
			}
			self.triggerChange(callback);
		};
	
		Picker.prototype.getSelectedItem = function () {
			var self = this;
			return self.items[self.getSelectedIndex()];
		};
	
		Picker.prototype.getSelectedValue = function () {
			var self = this;
			return (self.items[self.getSelectedIndex()] || {}).value;
		};
	
		Picker.prototype.getSelectedText = function () {
			var self = this;
			return (self.items[self.getSelectedIndex()] || {}).text;
		};
	
		Picker.prototype.setSelectedValue = function (value, duration, callback) {
			var self = this;
			for (var index in self.items) {
				var item = self.items[index];
				if (item.value == value) {
					self.setSelectedIndex(index, duration, callback);
					return;
				}
			}
		};
	
		if ($.fn) {
			$.fn.picker = function (options) {
				//遍历选择的元素
				this.each(function (i, element) {
					if (element.picker) return;
					if (options) {
						element.picker = new Picker(element, options);
					} else {
						var optionsText = element.getAttribute('data-picker-options');
						var _options = optionsText ? JSON.parse(optionsText) : {};
						element.picker = new Picker(element, _options);
					}
				});
				return this[0] ? this[0].picker : null;
			};
	
			//自动初始化
			$.ready(function () {
				$('.mui-picker').picker();
			});
		}
	})(window.mui || window, window, document, undefined);
	//end
	
	/**
	 * 日期时间插件
	 * varstion 1.0.5
	 * by Houfeng
	 * Houfeng@DCloud.io
	 */
	
	/**
	 * 选择列表插件
	 * varstion 2.0.0
	 * by Houfeng
	 * Houfeng@DCloud.io
	 */
	(function ($, document) {
	
		//创建 DOM
		$.dom = function (str) {
			if (typeof str !== 'string') {
				if (str instanceof Array || str[0] && str.length) {
					return [].slice.call(str);
				} else {
					return [str];
				}
			}
			if (!$.__create_dom_div__) {
				$.__create_dom_div__ = document.createElement('div');
			}
			$.__create_dom_div__.innerHTML = str;
			return [].slice.call($.__create_dom_div__.childNodes);
		};
	
		var domBuffer = '<div class="mui-dtpicker" data-type="datetime">\
			<div class="mui-dtpicker-header">\
				<button data-id="btn-cancel" class="mui-btn">取消</button>\
				<button data-id="btn-ok" class="mui-btn mui-btn-blue">确定</button>\
			</div>\
			<div class="mui-dtpicker-title"><h5 data-id="title-y">年</h5><h5 data-id="title-m">月</h5><h5 data-id="title-d">日</h5><h5 data-id="title-h">时</h5><h5 data-id="title-i">分</h5></div>\
			<div class="mui-dtpicker-body">\
				<div data-id="picker-y" class="mui-picker">\
					<div class="mui-picker-inner">\
						<div class="mui-pciker-rule mui-pciker-rule-ft"></div>\
						<ul class="mui-pciker-list">\
						</ul>\
						<div class="mui-pciker-rule mui-pciker-rule-bg"></div>\
					</div>\
				</div>\
				<div data-id="picker-m" class="mui-picker">\
					<div class="mui-picker-inner">\
						<div class="mui-pciker-rule mui-pciker-rule-ft"></div>\
						<ul class="mui-pciker-list">\
						</ul>\
						<div class="mui-pciker-rule mui-pciker-rule-bg"></div>\
					</div>\
				</div>\
				<div data-id="picker-d" class="mui-picker">\
					<div class="mui-picker-inner">\
						<div class="mui-pciker-rule mui-pciker-rule-ft"></div>\
						<ul class="mui-pciker-list">\
						</ul>\
						<div class="mui-pciker-rule mui-pciker-rule-bg"></div>\
					</div>\
				</div>\
				<div data-id="picker-h" class="mui-picker">\
					<div class="mui-picker-inner">\
						<div class="mui-pciker-rule mui-pciker-rule-ft"></div>\
						<ul class="mui-pciker-list">\
						</ul>\
						<div class="mui-pciker-rule mui-pciker-rule-bg"></div>\
					</div>\
				</div>\
				<div data-id="picker-i" class="mui-picker">\
					<div class="mui-picker-inner">\
						<div class="mui-pciker-rule mui-pciker-rule-ft"></div>\
						<ul class="mui-pciker-list">\
						</ul>\
						<div class="mui-pciker-rule mui-pciker-rule-bg"></div>\
					</div>\
				</div>\
			</div>\
		</div>';
	
		//plugin
		var DtPicker = $.DtPicker = $.Class.extend({
			init: function init(options) {
				var self = this;
				var _picker = $.dom(domBuffer)[0];
				document.body.appendChild(_picker);
				$('[data-id*="picker"]', _picker).picker();
				var ui = self.ui = {
					picker: _picker,
					mask: $.createMask(),
					ok: $('[data-id="btn-ok"]', _picker)[0],
					cancel: $('[data-id="btn-cancel"]', _picker)[0],
					y: $('[data-id="picker-y"]', _picker)[0],
					m: $('[data-id="picker-m"]', _picker)[0],
					d: $('[data-id="picker-d"]', _picker)[0],
					h: $('[data-id="picker-h"]', _picker)[0],
					i: $('[data-id="picker-i"]', _picker)[0],
					labels: $('[data-id*="title-"]', _picker)
				};
				ui.cancel.addEventListener('tap', function () {
					self.hide();
				}, false);
				ui.ok.addEventListener('tap', function () {
					var rs = self.callback(self.getSelected());
					if (rs !== false) {
						self.hide();
					}
				}, false);
				ui.y.addEventListener('change', function (e) {
					//目前的change事件容易导致级联触发
					if (self.options.beginMonth || self.options.endMonth) {
						self._createMonth();
					} else {
						self._createDay();
					}
				}, false);
				ui.m.addEventListener('change', function (e) {
					self._createDay();
				}, false);
				ui.d.addEventListener('change', function (e) {
					if (self.options.beginMonth || self.options.endMonth) {
						//仅提供了beginDate时，触发day,hours,minutes的change
						self._createHours();
					}
				}, false);
				ui.h.addEventListener('change', function (e) {
					if (self.options.beginMonth || self.options.endMonth) {
						self._createMinutes();
					}
				}, false);
				ui.mask[0].addEventListener('tap', function () {
					self.hide();
				}, false);
				self._create(options);
				//防止滚动穿透
				self.ui.picker.addEventListener($.EVENT_START, function (event) {
					event.preventDefault();
				}, false);
				self.ui.picker.addEventListener($.EVENT_MOVE, function (event) {
					event.preventDefault();
				}, false);
			},
			getSelected: function getSelected() {
				var self = this;
				var ui = self.ui;
				var type = self.options.type;
				var selected = {
					type: type,
					y: ui.y.picker.getSelectedItem(),
					m: ui.m.picker.getSelectedItem(),
					d: ui.d.picker.getSelectedItem(),
					h: ui.h.picker.getSelectedItem(),
					i: ui.i.picker.getSelectedItem(),
					toString: function toString() {
						return this.value;
					}
				};
				switch (type) {
					case 'datetime':
						selected.value = selected.y.value + '-' + selected.m.value + '-' + selected.d.value + ' ' + selected.h.value + ':' + selected.i.value;
						selected.text = selected.y.text + '-' + selected.m.text + '-' + selected.d.text + ' ' + selected.h.text + ':' + selected.i.text;
						break;
					case 'date':
						selected.value = selected.y.value + '-' + selected.m.value + '-' + selected.d.value;
						selected.text = selected.y.text + '-' + selected.m.text + '-' + selected.d.text;
						break;
					case 'time':
						selected.value = selected.h.value + ':' + selected.i.value;
						selected.text = selected.h.text + ':' + selected.i.text;
						break;
					case 'month':
						selected.value = selected.y.value + '-' + selected.m.value;
						selected.text = selected.y.text + '-' + selected.m.text;
						break;
					case 'hour':
						selected.value = selected.y.value + '-' + selected.m.value + '-' + selected.d.value + ' ' + selected.h.value;
						selected.text = selected.y.text + '-' + selected.m.text + '-' + selected.d.text + ' ' + selected.h.text;
						break;
				}
				return selected;
			},
			setSelectedValue: function setSelectedValue(value) {
				var self = this;
				var ui = self.ui;
				var parsedValue = self._parseValue(value);
				//TODO 嵌套过多，因为picker的change时间是异步(考虑到性能)的，所以为了保证change之后再setSelected，目前使用回调处理
				ui.y.picker.setSelectedValue(parsedValue.y, 0, function () {
					ui.m.picker.setSelectedValue(parsedValue.m, 0, function () {
						ui.d.picker.setSelectedValue(parsedValue.d, 0, function () {
							ui.h.picker.setSelectedValue(parsedValue.h, 0, function () {
								ui.i.picker.setSelectedValue(parsedValue.i, 0);
							});
						});
					});
				});
			},
			isLeapYear: function isLeapYear(year) {
				return year % 4 == 0 && year % 100 != 0 || year % 400 == 0;
			},
			_inArray: function _inArray(array, item) {
				for (var index in array) {
					var _item = array[index];
					if (_item === item) return true;
				}
				return false;
			},
			getDayNum: function getDayNum(year, month) {
				var self = this;
				if (self._inArray([1, 3, 5, 7, 8, 10, 12], month)) {
					return 31;
				} else if (self._inArray([4, 6, 9, 11], month)) {
					return 30;
				} else if (self.isLeapYear(year)) {
					return 29;
				} else {
					return 28;
				}
			},
			_fill: function _fill(num) {
				num = num.toString();
				if (num.length < 2) {
					num = 0 + num;
				}
				return num;
			},
			_isBeginYear: function _isBeginYear() {
				return this.options.beginYear === parseInt(this.ui.y.picker.getSelectedValue());
			},
			_isBeginMonth: function _isBeginMonth() {
				return this.options.beginMonth && this._isBeginYear() && this.options.beginMonth === parseInt(this.ui.m.picker.getSelectedValue());
			},
			_isBeginDay: function _isBeginDay() {
				return this._isBeginMonth() && this.options.beginDay === parseInt(this.ui.d.picker.getSelectedValue());
			},
			_isBeginHours: function _isBeginHours() {
				return this._isBeginDay() && this.options.beginHours === parseInt(this.ui.h.picker.getSelectedValue());
			},
			_isEndYear: function _isEndYear() {
				return this.options.endYear === parseInt(this.ui.y.picker.getSelectedValue());
			},
			_isEndMonth: function _isEndMonth() {
				return this.options.endMonth && this._isEndYear() && this.options.endMonth === parseInt(this.ui.m.picker.getSelectedValue());
			},
			_isEndDay: function _isEndDay() {
				return this._isEndMonth() && this.options.endDay === parseInt(this.ui.d.picker.getSelectedValue());
			},
			_isEndHours: function _isEndHours() {
				return this._isEndDay() && this.options.endHours === parseInt(this.ui.h.picker.getSelectedValue());
			},
			_createYear: function _createYear(current) {
				var self = this;
				var options = self.options;
				var ui = self.ui;
				//生成年列表
				var yArray = [];
				if (options.customData.y) {
					yArray = options.customData.y;
				} else {
					var yBegin = options.beginYear;
					var yEnd = options.endYear;
					for (var y = yBegin; y <= yEnd; y++) {
						yArray.push({
							text: y + '',
							value: y
						});
					}
				}
				ui.y.picker.setItems(yArray);
				//ui.y.picker.setSelectedValue(current);
			},
			_createMonth: function _createMonth(current) {
				var self = this;
				var options = self.options;
				var ui = self.ui;
	
				//生成月列表
				var mArray = [];
				if (options.customData.m) {
					mArray = options.customData.m;
				} else {
					var m = options.beginMonth && self._isBeginYear() ? options.beginMonth : 1;
					var maxMonth = options.endMonth && self._isEndYear() ? options.endMonth : 12;
					for (; m <= maxMonth; m++) {
						var val = self._fill(m);
						mArray.push({
							text: val,
							value: val
						});
					}
				}
				ui.m.picker.setItems(mArray);
				//ui.m.picker.setSelectedValue(current);
			},
			_createDay: function _createDay(current) {
				var self = this;
				var options = self.options;
				var ui = self.ui;
	
				//生成日列表
				var dArray = [];
				if (options.customData.d) {
					dArray = options.customData.d;
				} else {
					var d = self._isBeginMonth() ? options.beginDay : 1;
					var maxDay = self._isEndMonth() ? options.endDay : self.getDayNum(parseInt(this.ui.y.picker.getSelectedValue()), parseInt(this.ui.m.picker.getSelectedValue()));
					for (; d <= maxDay; d++) {
						var val = self._fill(d);
						dArray.push({
							text: val,
							value: val
						});
					}
				}
				ui.d.picker.setItems(dArray);
				current = current || ui.d.picker.getSelectedValue();
				//ui.d.picker.setSelectedValue(current);
			},
			_createHours: function _createHours(current) {
				var self = this;
				var options = self.options;
				var ui = self.ui;
				//生成时列表
				var hArray = [];
				if (options.customData.h) {
					hArray = options.customData.h;
				} else {
					var h = self._isBeginDay() ? options.beginHours : 0;
					var maxHours = self._isEndDay() ? options.endHours : 23;
					for (; h <= maxHours; h++) {
						var val = self._fill(h);
						hArray.push({
							text: val,
							value: val
						});
					}
				}
				ui.h.picker.setItems(hArray);
				//ui.h.picker.setSelectedValue(current);
			},
			_createMinutes: function _createMinutes(current) {
				var self = this;
				var options = self.options;
				var ui = self.ui;
	
				//生成分列表
				var iArray = [];
				if (options.customData.i) {
					iArray = options.customData.i;
				} else {
					var i = self._isBeginHours() ? options.beginMinutes : 0;
					var maxMinutes = self._isEndHours() ? options.endMinutes : 59;
					for (; i <= maxMinutes; i++) {
						var val = self._fill(i);
						iArray.push({
							text: val,
							value: val
						});
					}
				}
				ui.i.picker.setItems(iArray);
				//ui.i.picker.setSelectedValue(current);
			},
			_setLabels: function _setLabels() {
				var self = this;
				var options = self.options;
				var ui = self.ui;
				ui.labels.each(function (i, label) {
					label.innerText = options.labels[i];
				});
			},
			_setButtons: function _setButtons() {
				var self = this;
				var options = self.options;
				var ui = self.ui;
				ui.cancel.innerText = options.buttons[0];
				ui.ok.innerText = options.buttons[1];
			},
			_parseValue: function _parseValue(value) {
				var self = this;
				var rs = {};
				if (value) {
					var parts = value.replace(":", "-").replace(" ", "-").split("-");
					rs.y = parts[0];
					rs.m = parts[1];
					rs.d = parts[2];
					rs.h = parts[3];
					rs.i = parts[4];
				} else {
					var now = new Date();
					rs.y = now.getFullYear();
					rs.m = now.getMonth() + 1;
					rs.d = now.getDate();
					rs.h = now.getHours();
					rs.i = now.getMinutes();
				}
				return rs;
			},
			_create: function _create(options) {
				var self = this;
				options = options || {};
				options.labels = options.labels || ['年', '月', '日', '时', '分'];
				options.buttons = options.buttons || ['取消', '确定'];
				options.type = options.type || 'datetime';
				options.customData = options.customData || {};
				self.options = options;
				var now = new Date();
				var beginDate = options.beginDate;
				if (beginDate instanceof Date && !isNaN(beginDate.valueOf())) {
					//设定了开始日期
					options.beginYear = beginDate.getFullYear();
					options.beginMonth = beginDate.getMonth() + 1;
					options.beginDay = beginDate.getDate();
					options.beginHours = beginDate.getHours();
					options.beginMinutes = beginDate.getMinutes();
				}
				var endDate = options.endDate;
				if (endDate instanceof Date && !isNaN(endDate.valueOf())) {
					//设定了结束日期
					options.endYear = endDate.getFullYear();
					options.endMonth = endDate.getMonth() + 1;
					options.endDay = endDate.getDate();
					options.endHours = endDate.getHours();
					options.endMinutes = endDate.getMinutes();
				}
				options.beginYear = options.beginYear || now.getFullYear() - 5;
				options.endYear = options.endYear || now.getFullYear() + 5;
				var ui = self.ui;
				//设定label
				self._setLabels();
				self._setButtons();
				//设定类型
				ui.picker.setAttribute('data-type', options.type);
				//生成
				self._createYear();
				self._createMonth();
				self._createDay();
				self._createHours();
				self._createMinutes();
				//设定默认值
				self.setSelectedValue(options.value);
			},
			//显示
			show: function show(callback) {
				var self = this;
				var ui = self.ui;
				self.callback = callback || $.noop;
				ui.mask.show();
				document.body.classList.add($.className('dtpicker-active-for-page'));
				ui.picker.classList.add($.className('active'));
				//处理物理返回键
				self.__back = $.back;
				$.back = function () {
					self.hide();
				};
			},
			hide: function hide() {
				var self = this;
				if (self.disposed) return;
				var ui = self.ui;
				ui.picker.classList.remove($.className('active'));
				ui.mask.close();
				document.body.classList.remove($.className('dtpicker-active-for-page'));
				//处理物理返回键
				$.back = self.__back;
			},
			dispose: function dispose() {
				var self = this;
				self.hide();
				setTimeout(function () {
					self.ui.picker.parentNode.removeChild(self.ui.picker);
					for (var name in self) {
						self[name] = null;
						delete self[name];
					};
					self.disposed = true;
				}, 300);
			}
		});
	})(_muiMin2.default, document);

/***/ },
/* 51 */
/***/ function(module, exports, __webpack_require__) {

	var __WEBPACK_AMD_DEFINE_ARRAY__, __WEBPACK_AMD_DEFINE_RESULT__;"use strict";var _stringify=__webpack_require__(52);var _stringify2=_interopRequireDefault(_stringify);var _defineProperty=__webpack_require__(55);var _defineProperty2=_interopRequireDefault(_defineProperty);var _getPrototypeOf=__webpack_require__(72);var _getPrototypeOf2=_interopRequireDefault(_getPrototypeOf);var _setPrototypeOf=__webpack_require__(83);var _setPrototypeOf2=_interopRequireDefault(_setPrototypeOf);var _typeof2=__webpack_require__(92);var _typeof3=_interopRequireDefault(_typeof2);function _interopRequireDefault(obj){return obj&&obj.__esModule?obj:{default:obj};}/*!
	 * =====================================================
	 * Mui v3.4.0 (http://dev.dcloud.net.cn/mui)
	 * =====================================================
	 */var mui=function(a,b){var c=/complete|loaded|interactive/,d=/^#([\w-]+)$/,e=/^\.([\w-]+)$/,f=/^[\w-]+$/,g=/translate(?:3d)?\((.+?)\)/,h=/matrix(3d)?\((.+?)\)/,i=function i(b,c){if(c=c||a,!b)return j();if("object"==(typeof b==="undefined"?"undefined":(0,_typeof3.default)(b)))return i.isArrayLike(b)?j(i.slice.call(b),null):j([b],null);if("function"==typeof b)return i.ready(b);if("string"==typeof b)try{if(b=b.trim(),d.test(b)){var e=a.getElementById(RegExp.$1);return j(e?[e]:[]);}return j(i.qsa(b,c),b);}catch(f){}return j();},j=function j(a,b){return a=a||[],(0,_setPrototypeOf2.default)(a,i.fn),a.selector=b||"",a;};i.uuid=0,i.data={},i.extend=function(){var a,c,d,e,f,g,h=arguments[0]||{},j=1,k=arguments.length,l=!1;for("boolean"==typeof h&&(l=h,h=arguments[j]||{},j++),"object"==(typeof h==="undefined"?"undefined":(0,_typeof3.default)(h))||i.isFunction(h)||(h={}),j===k&&(h=this,j--);k>j;j++){if(null!=(a=arguments[j]))for(c in a){d=h[c],e=a[c],h!==e&&(l&&e&&(i.isPlainObject(e)||(f=i.isArray(e)))?(f?(f=!1,g=d&&i.isArray(d)?d:[]):g=d&&i.isPlainObject(d)?d:{},h[c]=i.extend(l,g,e)):e!==b&&(h[c]=e));}}return h;},i.noop=function(){},i.slice=[].slice,i.filter=[].filter,i.type=function(a){return null==a?String(a):k[{}.toString.call(a)]||"object";},i.isArray=Array.isArray||function(a){return a instanceof Array;},i.isArrayLike=function(a){var b=!!a&&"length"in a&&a.length,c=i.type(a);return"function"===c||i.isWindow(a)?!1:"array"===c||0===b||"number"==typeof b&&b>0&&b-1 in a;},i.isWindow=function(a){return null!=a&&a===a.window;},i.isObject=function(a){return"object"===i.type(a);},i.isPlainObject=function(a){return i.isObject(a)&&!i.isWindow(a)&&(0,_getPrototypeOf2.default)(a)===Object.prototype;},i.isEmptyObject=function(a){for(var c in a){if(c!==b)return!1;}return!0;},i.isFunction=function(a){return"function"===i.type(a);},i.qsa=function(b,c){

},!1),this;},i.buffer=function(a,b,c){function d(){e&&(e.cancel(),e=0),f=i.now(),a.apply(c||this,arguments),g=i.now();}var e,f=0,g=0,b=b||150;return i.extend(function(){!f||g>=f&&i.now()-g>b||f>g&&i.now()-f>8*b?d():(e&&e.cancel(),e=i.later(d,b,null,arguments));},{stop:function stop(){e&&(e.cancel(),e=0);}});},i.each=function(a,b,c){if(!a)return this;if("number"==typeof a.length)[].every.call(a,function(a,c){return b.call(a,c,a)!==!1;});else for(var d in a){if(c){if(a.hasOwnProperty(d)&&b.call(a[d],d,a[d])===!1)return a;}else if(b.call(a[d],d,a[d])===!1)return a;}return this;},i.focus=function(a){i.os.ios?setTimeout(function(){a.focus();},10):a.focus();},i.trigger=function(a,b,c){return a.dispatchEvent(new CustomEvent(b,{detail:c,bubbles:!0,cancelable:!0})),this;},i.getStyles=function(a,b){var c=a.ownerDocument.defaultView.getComputedStyle(a,null);return b?c.getPropertyValue(b)||c[b]:c;},i.parseTranslate=function(a,b){var c=a.match(g||"");return c&&c[1]||(c=["","0,0,0"]),c=c[1].split(","),c={x:parseFloat(c[0]),y:parseFloat(c[1]),z:parseFloat(c[2])},b&&c.hasOwnProperty(b)?c[b]:c;},i.parseTranslateMatrix=function(a,b){var c=a.match(h),d=c&&c[1];c?(c=c[2].split(","),"3d"===d?c=c.slice(12,15):(c.push(0),c=c.slice(4,7))):c=[0,0,0];var e={x:parseFloat(c[0]),y:parseFloat(c[1]),z:parseFloat(c[2])};return b&&e.hasOwnProperty(b)?e[b]:e;},i.hooks={},i.addAction=function(a,b){var c=i.hooks[a];return c||(c=[]),b.index=b.index||1e3,c.push(b),c.sort(function(a,b){return a.index-b.index;}),i.hooks[a]=c,i.hooks[a];},i.doAction=function(a,b){i.isFunction(b)?i.each(i.hooks[a],b):i.each(i.hooks[a],function(a,b){return!b.handle();});},i.later=function(a,b,c,d){b=b||0;var e,f,g=a,h=d;return"string"==typeof a&&(g=c[a]),e=function e(){g.apply(c,i.isArray(h)?h:[h]);},f=setTimeout(e,b),{id:f,cancel:function cancel(){clearTimeout(f);}};},i.now=Date.now||function(){return+new Date();};var k={};return i.each(["Boolean","Number","String","Function","Array","Date","RegExp","Object","Error"],function(a,b){k["[object "+b+"]"]=b.toLowerCase();}),window.JSON&&(i.parseJSON=JSON.parse),i.fn={each:function each(a){return[].every.call(this,function(b,c){return a.call(b,c,b)!==!1;}),this;}},"function"=="function"&&__webpack_require__(133)&&!(__WEBPACK_AMD_DEFINE_ARRAY__ = [], __WEBPACK_AMD_DEFINE_RESULT__ = function(){return i;}.apply(exports, __WEBPACK_AMD_DEFINE_ARRAY__), __WEBPACK_AMD_DEFINE_RESULT__ !== undefined && (module.exports = __WEBPACK_AMD_DEFINE_RESULT__)),i;}(document);!function(a,b){function c(c){this.os={};var d=[function(){var a=c.match(/(MicroMessenger)\/([\d\.]+)/i);return a&&(this.os.wechat={version:a[2].replace(/_/g,".")}),!1;},function(){var a=c.match(/(Android);?[\s\/]+([\d.]+)?/);return a&&(this.os.android=!0,this.os.version=a[2],this.os.isBadAndroid=!/Chrome\/\d/.test(b.navigator.appVersion)),this.os.android===!0;},function(){var a=c.match(/(iPhone\sOS)\s([\d_]+)/);if(a)this.os.ios=this.os.iphone=!0,this.os.version=a[2].replace(/_/g,".");else{var b=c.match(/(iPad).*OS\s([\d_]+)/);b&&(this.os.ios=this.os.ipad=!0,this.os.version=b[2].replace(/_/g,"."));}return this.os.ios===!0;}];[].every.call(d,function(b){return!b.call(a);});}c.call(a,navigator.userAgent);}(mui,window),function(a,b){function c(c){this.os=this.os||{};var d=c.match(/Html5Plus/i);d&&(this.os.plus=!0,a(function(){b.body.classList.add("mui-plus");}),c.match(/StreamApp/i)&&(this.os.stream=!0,a(function(){b.body.classList.add("mui-plus-stream");})));}c.call(a,navigator.userAgent);}(mui,document),function(a){"ontouchstart"in window?(a.isTouchable=!0,a.EVENT_START="touchstart",a.EVENT_MOVE="touchmove",a.EVENT_END="touchend"):(a.isTouchable=!1,a.EVENT_START="mousedown",a.EVENT_MOVE="mousemove",a.EVENT_END="mouseup"),a.EVENT_CANCEL="touchcancel",a.EVENT_CLICK="click";var b=1,c={},d={preventDefault:"isDefaultPrevented",stopImmediatePropagation:"isImmediatePropagationStopped",stopPropagation:"isPropagationStopped"},e=function e(){return!0;},f=function f(){return!1;},g=function g(b,c){return b.detail?b.detail.currentTarget=c:b.detail={currentTarget:c},a.each(d,function(a,c){var d=b[a];b[a]=function(){return this[c]=e,d&&d.apply(b,arguments);},b[c]=f;},!0),b;},h=function h(a){return a&&(a._mid||(a._mid=b++));},i={},j=function j(b,d,e,f){return function(e){for(var f=c[b._mid][d],h=[],i=e.target,j={};i&&i!==document&&i!==b&&(!~["click","tap","doubletap","longtap","hold"].indexOf(d)||!i.disabled&&!i.classList.contains("mui-disabled"));i=i.parentNode){var k={};a.each(f,function(c,d){j[c]||(j[c]=a.qsa(c,b)),j[c]&&~j[c].indexOf(i)&&(k[c]||(k[c]=d));},!0),a.isEmptyObject(k)||h.push({element:i,handlers:k});}j=null,e=g(e),a.each(h,function(b,c){i=c.element;var f=i.tagName;return"tap"===d&&"INPUT"!==f&&"TEXTAREA"!==f&&"SELECT"!==f&&(e.preventDefault(),e.detail&&e.detail.gesture&&e.detail.gesture.preventDefault()),a.each(c.handlers,function(b,c){a.each(c,function(a,b){b.call(i,e)===!1&&(e.preventDefault(),e.stopPropagation());},!0);},!0),e.isPropagationStopped()?!1:void 0;},!0);};},k=function k(a,b){var c=i[h(a)],d=[];if(c){if(d=[],b){var e=function e(a){return a.type===b;};return c.filter(e);}d=c;}return d;},l=/^(INPUT|TEXTAREA|BUTTON|SELECT)$/;a.fn.on=function(b,d,e){return this.each(function(){var f=this;h(f),h(e);var g=!1,k=c[f._mid]||(c[f._mid]={}),m=k[b]||(k[b]={});a.isEmptyObject(m)&&(g=!0);var n=m[d]||(m[d]=[]);if(n.push(e),g){var o=i[h(f)];o||(o=[]);var p=j(f,b,d,e);o.push(p),p.i=o.length-1,p.type=b,i[h(f)]=o,f.addEventListener(b,p),"tap"===b&&f.addEventListener("click",function(a){if(a.target){var b=a.target.tagName;if(!l.test(b))if("A"===b){var c=a.target.href;c&&~c.indexOf("tel:")||a.preventDefault();}else a.preventDefault();}});}});},a.fn.off=function(b,d,e){return this.each(function(){var f=h(this);if(b){if(d){if(e){var g=c[f]&&c[f][b]&&c[f][b][d];a.each(g,function(a,b){return h(b)===h(e)?(g.splice(a,1),!1):void 0;},!0);}else c[f]&&c[f][b]&&delete c[f][b][d];}else c[f]&&delete c[f][b];}else c[f]&&delete c[f];c[f]?(!c[f][b]||a.isEmptyObject(c[f][b]))&&k(this,b).forEach(function(a){this.removeEventListener(a.type,a),delete i[f][a.i];}.bind(this)):k(this).forEach(function(a){this.removeEventListener(a.type,a),delete i[f][a.i];}.bind(this));});};}(mui),function(a,b,c){a.targets={},a.targetHandles=[],a.registerTarget=function(b){return b.index=b.index||1e3,a.targetHandles.push(b),a.targetHandles.sort(function(a,b){return a.index-b.index;}),a.targetHandles;},b.addEventListener(a.EVENT_START,function(b){for(var d=b.target,e={};d&&d!==c;d=d.parentNode){var f=!1;if(a.each(a.targetHandles,function(c,g){var h=g.name;f||e[h]||!g.hasOwnProperty("handle")?e[h]||g.isReset!==!1&&(a.targets[h]=!1):(a.targets[h]=g.handle(b,d),a.targets[h]&&(e[h]=!0,g.isContinue!==!0&&(f=!0)));}),f)break;}}),b.addEventListener("click",function(b){for(var d=b.target,e=!1;d&&d!==c&&("A"!==d.tagName||(a.each(a.targetHandles,function(a,c){c.name;return c.hasOwnProperty("handle")&&c.handle(b,d)?(e=!0,b.preventDefault(),!1):void 0;}),!e));d=d.parentNode){}});}(mui,window,document),function(a){String.prototype.trim===a&&(String.prototype.trim=function(){return this.replace(/^\s+|\s+$/g,"");}),Object.setPrototypeOf=_setPrototypeOf2.default||function(a,b){return a.__proto__=b,a;};}(),function(){function a(a,b){b=b||{bubbles:!1,cancelable:!1,detail:void 0};var c=document.createEvent("Events"),d=!0;for(var e in b){"bubbles"===e?d=!!b[e]:c[e]=b[e];}return c.initEvent(a,d,!0),c;}"undefined"==typeof window.CustomEvent&&(a.prototype=window.Event.prototype,window.CustomEvent=a);}(),Function.prototype.bind=Function.prototype.bind||function(a){var b=Array.prototype.splice.call(arguments,1),c=this,d=function d(){var e=b.concat(Array.prototype.splice.call(arguments,0));return this instanceof d?void c.apply(this,e):c.apply(a,e);};return d.prototype=c.prototype,d;},function(a){"classList"in a.documentElement||!_defineProperty2.default||"undefined"==typeof HTMLElement||Object.defineProperty(HTMLElement.prototype,"classList",{get:function get(){function a(a){return function(c){var d=b.className.split(/\s+/),e=d.indexOf(c);a(d,e,c),b.className=d.join(" ");};}var b=this,c={add:a(function(a,b,c){~b||a.push(c);}),remove:a(function(a,b){~b&&a.splice(b,1);}),toggle:a(function(a,b,c){~b?a.splice(b,1):a.push(c);}),contains:function contains(a){return!!~b.className.split(/\s+/).indexOf(a);},item:function item(a){return b.className.split(/\s+/)[a]||null;}};return Object.defineProperty(c,"length",{get:function get(){return b.className.split(/\s+/).length;}}),c;}});}(document),function(a){if(!a.requestAnimationFrame){var b=0;a.requestAnimationFrame=a.webkitRequestAnimationFrame||function(c,d){var e=new Date().getTime(),f=Math.max(0,16.7-(e-b)),g=a.setTimeout(function(){c(e+f);},f);return b=e+f,g;},a.cancelAnimationFrame=a.webkitCancelAnimationFrame||a.webkitCancelRequestAnimationFrame||function(a){clearTimeout(a);};}}(window),function(a,b,c){if((a.os.android||a.os.ios)&&!b.FastClick){var d=function d(a,b){return"LABEL"===b.tagName&&b.parentNode&&(b=b.parentNode.querySelector("input")),!b||"radio"!==b.type&&"checkbox"!==b.type||b.disabled?!1:b;};a.registerTarget({name:c,index:40,handle:d,target:!1});var e=function e(c){var d=a.targets.click;if(d){var e,f;document.activeElement&&document.activeElement!==d&&document.activeElement.blur(),f=c.detail.gesture.changedTouches[0],e=document.createEvent("MouseEvents"),e.initMouseEvent("click",!0,!0,b,1,f.screenX,f.screenY,f.clientX,f.clientY,!1,!1,!1,!1,0,null),e.forwardedTouchEvent=!0,d.dispatchEvent(e),c.detail&&c.detail.gesture.preventDefault();}};b.addEventListener("tap",e),b.addEventListener("doubletap",e),b.addEventListener("click",function(b){return a.targets.click&&!b.forwardedTouchEvent?(b.stopImmediatePropagation?b.stopImmediatePropagation():b.propagationStopped=!0,b.stopPropagation(),b.preventDefault(),!1):void 0;},!0);}}(mui,window,"click"),function(a,b){a(function(){if(a.os.ios){var c="mui-focusin",d="mui-bar-tab",e="mui-bar-footer",f="mui-bar-footer-secondary",g="mui-bar-footer-secondary-tab";b.addEventListener("focusin",function(h){if(!(a.os.plus&&window.plus&&plus.webview.currentWebview().children().length>0)){var i=h.target;if(i.tagName&&("TEXTAREA"===i.tagName||"INPUT"===i.tagName&&("text"===i.type||"search"===i.type||"number"===i.type))){if(i.disabled||i.readOnly)return;b.body.classList.add(c);for(var j=!1;i&&i!==b;i=i.parentNode){var k=i.classList;if(k&&k.contains(d)||k.contains(e)||k.contains(f)||k.contains(g)){j=!0;break;}}if(j){var l=b.body.scrollHeight,m=b.body.scrollLeft;setTimeout(function(){window.scrollTo(m,l);},20);}}}}),b.addEventListener("focusout",function(a){var d=b.body.classList;d.contains(c)&&(d.remove(c),setTimeout(function(){window.scrollTo(b.body.scrollLeft,b.body.scrollTop);},20));});}});}(mui,document),function(a){a.namespace="mui",a.classNamePrefix=a.namespace+"-",a.classSelectorPrefix="."+a.classNamePrefix,a.className=function(b){return a.classNamePrefix+b;},a.classSelector=function(b){return b.replace(/\./g,a.classSelectorPrefix);},a.eventName=function(b,c){return b+(a.namespace?"."+a.namespace:"")+(c?"."+c:"");};}(mui),function(a,b){a.gestures={session:{}},a.preventDefault=function(a){a.preventDefault();},a.stopPropagation=function(a){a.stopPropagation();},a.addGesture=function(b){return a.addAction("gestures",b);};var c=Math.round,d=Math.abs,e=Math.sqrt,f=(Math.atan,Math.atan2),g=function g(a,b,c){c||(c=["x","y"]);var d=b[c[0]]-a[c[0]],f=b[c[1]]-a[c[1]];return e(d*d+f*f);},h=function h(a,b){if(a.length>=2&&b.length>=2){var c=["pageX","pageY"];return g(b[1],b[0],c)/g(a[1],a[0],c);}return 1;},i=function i(a,b,c){c||(c=["x","y"]);var d=b[c[0]]-a[c[0]],e=b[c[1]]-a[c[1]];return 180*f(e,d)/Math.PI;},j=function j(a,b){return a===b?"":d(a)>=d(b)?a>0?"left":"right":b>0?"up":"down";},k=function k(a,b){var c=["pageX","pageY"];return i(b[1],b[0],c)-i(a[1],a[0],c);},l=function l(a,b,c){return{x:b/a||0,y:c/a||0};},m=function m(b,c){a.gestures.stoped||a.doAction("gestures",function(d,e){a.gestures.stoped||a.options.gestureConfig[e.name]!==!1&&e.handle(b,c);});},n=function n(a,b){for(;a;){if(a==b)return!0;a=a.parentNode;}return!1;},o=function o(a,b,c){for(var d=[],e=[],f=0;f<a.length;){var g=b?a[f][b]:a[f];e.indexOf(g)<0&&d.push(a[f]),e[f]=g,f++;}return c&&(d=b?d.sort(function(a,c){return a[b]>c[b];}):d.sort()),d;},p=function p(a){var b=a.length;if(1===b)return{x:c(a[0].pageX),y:c(a[0].pageY)};for(var d=0,e=0,f=0;b>f;){d+=a[f].pageX,e+=a[f].pageY,f++;}return{x:c(d/b),y:c(e/b)};},q=function q(){return a.options.gestureConfig.pinch;},r=function r(b){for(var d=[],e=0;e<b.touches.length;){d[e]={pageX:c(b.touches[e].pageX),pageY:c(b.touches[e].pageY)},e++;}return{timestamp:a.now(),gesture:b.gesture,touches:d,center:p(b.touches),deltaX:b.deltaX,deltaY:b.deltaY};},s=function s(b){var c=a.gestures.session,d=b.center,e=c.offsetDelta||{},f=c.prevDelta||{},g=c.prevTouch||{};(b.gesture.type===a.EVENT_START||b.gesture.type===a.EVENT_END)&&(f=c.prevDelta={x:g.deltaX||0,y:g.deltaY||0},e=c.offsetDelta={x:d.x,y:d.y}),b.deltaX=f.x+(d.x-e.x),b.deltaY=f.y+(d.y-e.y);},t=function t(b){var c=a.gestures.session,d=b.touches,e=d.length;c.firstTouch||(c.firstTouch=r(b)),q()&&e>1&&!c.firstMultiTouch?c.firstMultiTouch=r(b):1===e&&(c.firstMultiTouch=!1);var f=c.firstTouch,l=c.firstMultiTouch,m=l?l.center:f.center,n=b.center=p(d);b.timestamp=a.now(),b.deltaTime=b.timestamp-f.timestamp,b.angle=i(m,n),b.distance=g(m,n),s(b),b.offsetDirection=j(b.deltaX,b.deltaY),b.scale=l?h(l.touches,d):1,b.rotation=l?k(l.touches,d):0,v(b);},u=25,v=function v(b){var c,e,f,g,h=a.gestures.session,i=h.lastInterval||b,k=b.timestamp-i.timestamp;if(b.gesture.type!=a.EVENT_CANCEL&&(k>u||void 0===i.velocity)){var m=i.deltaX-b.deltaX,n=i.deltaY-b.deltaY,o=l(k,m,n);e=o.x,f=o.y,c=d(o.x)>d(o.y)?o.x:o.y,g=j(m,n)||i.direction,h.lastInterval=b;}else c=i.velocity,e=i.velocityX,f=i.velocityY,g=i.direction;b.velocity=c,b.velocityX=e,b.velocityY=f,b.direction=g;},w={},x=function x(a){for(var b=0;b<a.length;b++){!a.identifier&&(a.identifier=0);}return a;},y=function y(b,c){var d=x(a.slice.call(b.touches||[b])),e=b.type,f=[],g=[];if(e!==a.EVENT_START&&e!==a.EVENT_MOVE||1!==d.length){var h=0,f=[],g=[],i=x(a.slice.call(b.changedTouches||[b]));c.target=b.target;var j=a.gestures.session.target||b.target;if(f=d.filter(function(a){return n(a.target,j);}),e===a.EVENT_START)for(h=0;h<f.length;){w[f[h].identifier]=!0,h++;}for(h=0;h<i.length;){w[i[h].identifier]&&g.push(i[h]),(e===a.EVENT_END||e===a.EVENT_CANCEL)&&delete w[i[h].identifier],h++;}if(!g.length)return!1;}else w[d[0].identifier]=!0,f=d,g=d,c.target=b.target;f=o(f.concat(g),"identifier",!0);var k=f.length,l=g.length;return e===a.EVENT_START&&k-l===0&&(c.isFirst=!0,a.gestures.touch=a.gestures.session={target:b.target}),c.isFinal=(e===a.EVENT_END||e===a.EVENT_CANCEL)&&k-l===0,c.touches=f,c.changedTouches=g,!0;},z=function z(b){var c={gesture:b},d=y(b,c);d&&(t(c),m(b,c),a.gestures.session.prevTouch=c,b.type!==a.EVENT_END||a.isTouchable||(a.gestures.touch=a.gestures.session={}));};b.addEventListener(a.EVENT_START,z),b.addEventListener(a.EVENT_MOVE,z),b.addEventListener(a.EVENT_END,z),b.addEventListener(a.EVENT_CANCEL,z),b.addEventListener(a.EVENT_CLICK,function(b){(a.os.android||a.os.ios)&&(a.targets.popover&&b.target===a.targets.popover||a.targets.tab||a.targets.offcanvas||a.targets.modal)&&b.preventDefault();},!0),a.isScrolling=!1;var A=null;b.addEventListener("scroll",function(){a.isScrolling=!0,A&&clearTimeout(A),A=setTimeout(function(){a.isScrolling=!1;},250);});}(mui,window),function(a,b){var c=0,d=function d(_d,e){var f=a.gestures.session,g=this.options,h=a.now();switch(_d.type){case a.EVENT_MOVE:h-c>300&&(c=h,f.flickStart=e.center);break;case a.EVENT_END:case a.EVENT_CANCEL:e.flick=!1,f.flickStart&&g.flickMaxTime>h-c&&e.distance>g.flickMinDistince&&(e.flick=!0,e.flickTime=h-c,e.flickDistanceX=e.center.x-f.flickStart.x,e.flickDistanceY=e.center.y-f.flickStart.y,a.trigger(f.target,b,e),a.trigger(f.target,b+e.direction,e));}};a.addGesture({name:b,index:5,handle:d,options:{flickMaxTime:200,flickMinDistince:10}});}(mui,"flick"),function(a,b){var c=function c(_c,d){var e=a.gestures.session;if(_c.type===a.EVENT_END||_c.type===a.EVENT_CANCEL){var f=this.options;d.swipe=!1,d.direction&&f.swipeMaxTime>d.deltaTime&&d.distance>f.swipeMinDistince&&(d.swipe=!0,a.trigger(e.target,b,d),a.trigger(e.target,b+d.direction,d));}};a.addGesture({name:b,index:10,handle:c,options:{swipeMaxTime:300,swipeMinDistince:18}});}(mui,"swipe"),function(a,b){var c=function c(_c2,d){var e=a.gestures.session;switch(_c2.type){case a.EVENT_START:break;case a.EVENT_MOVE:if(!d.direction||!e.target)return;e.lockDirection&&e.startDirection&&e.startDirection&&e.startDirection!==d.direction&&("up"===e.startDirection||"down"===e.startDirection?d.direction=d.deltaY<0?"up":"down":d.direction=d.deltaX<0?"left":"right"),e.drag||(e.drag=!0,a.trigger(e.target,b+"start",d)),a.trigger(e.target,b,d),a.trigger(e.target,b+d.direction,d);break;case a.EVENT_END:case a.EVENT_CANCEL:e.drag&&d.isFinal&&a.trigger(e.target,b+"end",d);}};a.addGesture({name:b,index:20,handle:c,options:{fingers:1}});}(mui,"drag"),function(a,b){var c,d,e=function e(_e,f){var g=a.gestures.session,h=this.options;switch(_e.type){case a.EVENT_END:if(!f.isFinal)return;var i=g.target;if(!i||i.disabled||i.classList&&i.classList.contains("mui-disabled"))return;if(f.distance<h.tapMaxDistance&&f.deltaTime<h.tapMaxTime){if(a.options.gestureConfig.doubletap&&c&&c===i&&d&&f.timestamp-d<h.tapMaxInterval)return a.trigger(i,"doubletap",f),d=a.now(),void(c=i);a.trigger(i,b,f),d=a.now(),c=i;}}};a.addGesture({name:b,index:30,handle:e,options:{fingers:1,tapMaxInterval:300,tapMaxDistance:5,tapMaxTime:250}});}(mui,"tap"),function(a,b){var c,d=function d(_d2,e){var f=a.gestures.session,g=this.options;switch(_d2.type){case a.EVENT_START:clearTimeout(c),c=setTimeout(function(){a.trigger(f.target,b,e);},g.holdTimeout);break;case a.EVENT_MOVE:e.distance>g.holdThreshold&&clearTimeout(c);break;case a.EVENT_END:case a.EVENT_CANCEL:clearTimeout(c);}};a.addGesture({name:b,index:10,handle:d,options:{fingers:1,holdTimeout:500,holdThreshold:2}});}(mui,"longtap"),function(a,b){var c,d=function d(_d3,e){var f=a.gestures.session,g=this.options;switch(_d3.type){case a.EVENT_START:a.options.gestureConfig.hold&&(c&&clearTimeout(c),c=setTimeout(function(){e.hold=!0,a.trigger(f.target,b,e);},g.holdTimeout));break;case a.EVENT_MOVE:break;case a.EVENT_END:case a.EVENT_CANCEL:c&&(clearTimeout(c)&&(c=null),a.trigger(f.target,"release",e));}};a.addGesture({name:b,index:10,handle:d,options:{fingers:1,holdTimeout:0}});}(mui,"hold"),function(a,b){var c=function c(_c3,d){var e=this.options,f=a.gestures.session;switch(_c3.type){case a.EVENT_START:break;case a.EVENT_MOVE:if(a.options.gestureConfig.pinch){if(d.touches.length<2)return;f.pinch||(f.pinch=!0,a.trigger(f.target,b+"start",d)),a.trigger(f.target,b,d);var g=d.scale,h=d.rotation,i="undefined"==typeof d.lastScale?1:d.lastScale,j=1e-12;g>i?(i=g-j,a.trigger(f.target,b+"out",d)):i>g&&(i=g+j,a.trigger(f.target,b+"in",d)),Math.abs(h)>e.minRotationAngle&&a.trigger(f.target,"rotate",d);}break;case a.EVENT_END:case a.EVENT_CANCEL:a.options.gestureConfig.pinch&&f.pinch&&2===d.touches.length&&(f.pinch=!1,a.trigger(f.target,b+"end",d));}};a.addGesture({name:b,index:10,handle:c,options:{minRotationAngle:0}});}(mui,"pinch"),function(a){function b(a,b){var c="MUI_SCROLL_POSITION_"+document.location.href+"_"+b.src,d=parseFloat(localStorage.getItem(c))||0;d&&!function(a){b.onload=function(){window.scrollTo(0,a);};}(d),setInterval(function(){var a=window.scrollY;d!==a&&(localStorage.setItem(c,a+""),d=a);},100);}a.global=a.options={gestureConfig:{tap:!0,doubletap:!1,longtap:!1,hold:!1,flick:!0,swipe:!0,drag:!0,pinch:!1}},a.initGlobal=function(b){return a.options=a.extend(!0,a.global,b),this;};var c={},d=!1;a.init=function(b){return d=!0,a.options=a.extend(!0,a.global,b||{}),a.ready(function(){a.doAction("inits",function(b,d){var e=!(c[d.name]&&!d.repeat);e&&(d.handle.call(a),c[d.name]=!0);});}),this;},a.addInit=function(b){return a.addAction("inits",b);},a.addInit({name:"iframe",index:100,handle:function handle(){var b=a.options,c=b.subpages||[];!a.os.plus&&c.length&&e(c[0]);}});var e=function e(c){var d=document.createElement("div");d.className="mui-iframe-wrapper";var e=c.styles||{};"string"!=typeof e.top&&(e.top="0px"),"string"!=typeof e.bottom&&(e.bottom="0px"),d.style.top=e.top,d.style.bottom=e.bottom;var f=document.createElement("iframe");f.src=c.url,f.id=c.id||c.url,f.name=f.id,d.appendChild(f),document.body.appendChild(d),a.os.wechat&&b(d,f);};a(function(){var b=document.body.classList,c=[];a.os.ios?(c.push({os:"ios",version:a.os.version}),b.add("mui-ios")):a.os.android&&(c.push({os:"android",version:a.os.version}),b.add("mui-android")),a.os.wechat&&(c.push({os:"wechat",version:a.os.wechat.version}),b.add("mui-wechat")),c.length&&a.each(c,function(c,d){var e="";d.version&&a.each(d.version.split("."),function(c,f){e=e+(e?"-":"")+f,b.add(a.className(d.os+"-"+e));});});});}(mui),function(a){var b={swipeBack:!1,preloadPages:[],preloadLimit:10,keyEventBind:{backbutton:!0,menubutton:!0}},c={autoShow:!0,duration:a.os.ios?200:100,aniShow:"slide-in-right"};a.options.show&&(c=a.extend(!0,c,a.options.show)),a.currentWebview=null,a.isHomePage=!1,a.extend(!0,a.global,b),a.extend(!0,a.options,b),a.waitingOptions=function(b){return a.extend(!0,{},{autoShow:!0,title:"",modal:!1},b);},a.showOptions=function(b){return a.extend(!0,{},c,b);},a.windowOptions=function(b){return a.extend({scalable:!1,bounce:""},b);},a.plusReady=function(a){return window.plus?setTimeout(function(){a();},0):document.addEventListener("plusready",function(){a();},!1),this;},a.fire=function(b,c,d){b&&(""!==d&&(d=d||{},a.isPlainObject(d)&&(d=(0,_stringify2.default)(d||{}).replace(/\'/g,"\\u0027").replace(/\\/g,"\\u005c"))),b.evalJS("typeof mui!=='undefined'&&mui.receive('"+c+"','"+d+"')"));},a.receive=function(b,c){if(b){try{c&&(c=JSON.parse(c));}catch(d){}a.trigger(document,b,c);}};var d=function d(b){if(!b.preloaded){a.fire(b,"preload");for(var c=b.children(),d=0;d<c.length;d++){a.fire(c[d],"preload");}b.preloaded=!0;}},e=function e(b,c,d){if(d){if(!b[c+"ed"]){a.fire(b,c);for(var e=b.children(),f=0;f<e.length;f++){a.fire(e[f],c);}b[c+"ed"]=!0;}}else{a.fire(b,c);for(var e=b.children(),f=0;f<e.length;f++){a.fire(e[f],c);}}};a.openWindow=function(b,c,f){if("object"==(typeof b==="undefined"?"undefined":(0,_typeof3.default)(b))?(f=b,b=f.url,c=f.id||b):"object"==(typeof c==="undefined"?"undefined":(0,_typeof3.default)(c))?(f=c,c=b):c=c||b,!a.os.plus)return void(a.os.ios||a.os.android?window.top.location.href=b:window.parent.location.href=b);if(window.plus){f=f||{};var g,h,i=f.params||{},j=null,k=null;if(a.webviews[c]&&(k=a.webviews[c],plus.webview.getWebviewById(c)&&(j=k.webview)),k&&j)return g=k.show,g=f.show?a.extend(g,f.show):g,j.show(g.aniShow,g.duration,function(){d(j),e(j,"pagebeforeshow",!1);}),k.afterShowMethodName&&j.evalJS(k.afterShowMethodName+"('"+(0,_stringify2.default)(i)+"')"),j;if(f.createNew!==!0){if(j=plus.webview.getWebviewById(c))return g=a.showOptions(f.show),g.autoShow&&j.show(g.aniShow,g.duration,function(){d(j),e(j,"pagebeforeshow",!1);}),j;if(!b)throw new Error("webview["+c+"] does not exist");}var l=a.waitingOptions(f.waiting);if(l.autoShow&&(h=plus.nativeUI.showWaiting(l.title,l.options)),f=a.extend(f,{id:c,url:b}),j=a.createWindow(f),g=a.showOptions(f.show),g.autoShow){var m=function m(){h&&h.close(),j.show(g.aniShow,g.duration,function(){}),j.showed=!0,f.afterShowMethodName&&j.evalJS(f.afterShowMethodName+"('"+(0,_stringify2.default)(i)+"')");};b?(j.addEventListener("titleUpdate",m,!1),j.addEventListener("loaded",function(){d(j),e(j,"pagebeforeshow",!1);},!1)):m();}return j;}},a.createWindow=function(b,c){if(window.plus){var d,e=b.id||b.url;if(b.preload){a.webviews[e]&&a.webviews[e].webview.getURL()?d=a.webviews[e].webview:(b.createNew!==!0&&(d=plus.webview.getWebviewById(e)),d||(d=plus.webview.create(b.url,e,a.windowOptions(b.styles),a.extend({preload:!0},b.extras)),b.subpages&&a.each(b.subpages,function(b,c){var e=c.id||c.url;if(e){var f=plus.webview.getWebviewById(e);f||(f=plus.webview.create(c.url,e,a.windowOptions(c.styles),a.extend({preload:!0},c.extras))),d.append(f);}}))),a.webviews[e]={webview:d,preload:!0,show:a.showOptions(b.show),afterShowMethodName:b.afterShowMethodName};var f=a.data.preloads,g=f.indexOf(e);if(~g&&f.splice(g,1),f.push(e),f.length>a.options.preloadLimit){var h=a.data.preloads.shift(),i=a.webviews[h];i&&i.webview&&a.closeAll(i.webview),delete a.webviews[h];}}else c!==!1&&(d=plus.webview.create(b.url,e,a.windowOptions(b.styles),b.extras),b.subpages&&a.each(b.subpages,function(b,c){var e=c.id||c.url,f=plus.webview.getWebviewById(e);f||(f=plus.webview.create(c.url,e,a.windowOptions(c.styles),c.extras)),d.append(f);}));return d;}},a.preload=function(b){return b.preload||(b.preload=!0),a.createWindow(b);},a.closeOpened=function(b){var c=b.opened();if(c)for(var d=0,e=c.length;e>d;d++){var f=c[d],g=f.opened();g&&g.length>0?(a.closeOpened(f),f.close("none")):f.parent()!==b&&f.close("none");}},a.closeAll=function(b,c){a.closeOpened(b),c?b.close(c):b.close();},a.createWindows=function(b){a.each(b,function(b,c){a.createWindow(c,!1);});},a.appendWebview=function(b){if(window.plus){var c,d=b.id||b.url;return a.webviews[d]||(plus.webview.getWebviewById(d)||(c=plus.webview.create(b.url,d,b.styles,b.extras)),plus.webview.currentWebview().append(c),a.webviews[d]=b),c;}},a.webviews={},a.data.preloads=[],a.plusReady(function(){a.currentWebview=plus.webview.currentWebview();}),a.addInit({name:"5+",index:100,handle:function handle(){var b=a.options,c=b.subpages||[];a.os.plus&&a.plusReady(function(){a.each(c,function(b,c){a.appendWebview(c);}),plus.webview.currentWebview()===plus.webview.getWebviewById(plus.runtime.appid)&&(a.isHomePage=!0,setTimeout(function(){d(plus.webview.currentWebview());},300)),a.os.ios&&a.options.statusBarBackground&&plus.navigator.setStatusBarBackground(a.options.statusBarBackground),a.os.android&&parseFloat(a.os.version)<4.4&&null==plus.webview.currentWebview().parent()&&document.addEventListener("resume",function(){var a=document.body;a.style.display="none",setTimeout(function(){a.style.display="";},10);});});}}),window.addEventListener("preload",function(){var b=a.options.preloadPages||[];a.plusReady(function(){a.each(b,function(b,c){a.createWindow(a.extend(c,{preload:!0}));});});}),a.supportStatusbarOffset=function(){return a.os.plus&&a.os.ios&&parseFloat(a.os.version)>=7;},a.ready(function(){a.supportStatusbarOffset()&&document.body.classList.add("mui-statusbar");});}(mui),function(a,b){a.addBack=function(b){return a.addAction("backs",b);},a.addBack({name:"browser",index:100,handle:function handle(){return b.history.length>1?(b.history.back(),!0):!1;}}),a.back=function(){("function"!=typeof a.options.beforeback||a.options.beforeback()!==!1)&&a.doAction("backs");},b.addEventListener("tap",function(b){var c=a.targets.action;c&&c.classList.contains("mui-action-back")&&(a.back(),a.targets.action=!1);}),b.addEventListener("swiperight",function(b){var c=b.detail;a.options.swipeBack===!0&&Math.abs(c.angle)<3&&a.back();});}(mui,window),function(a,b){a.os.plus&&a.os.android&&a.addBack({name:"mui",index:5,handle:function handle(){if(a.targets._popover&&a.targets._popover.classList.contains("mui-active"))return a(a.targets._popover).popover("hide"),!0;var b=document.querySelector(".mui-off-canvas-wrap.mui-active");if(b)return a(b).offCanvas("close"),!0;var c=a.isFunction(a.getPreviewImage)&&a.getPreviewImage();return c&&c.isShown()?(c.close(),!0):a.closePopup();}}),a.__back__first=null,a.addBack({name:"5+",index:10,handle:function handle(){if(!b.plus)return!1;var c=plus.webview.currentWebview(),d=c.parent();return d?d.evalJS("mui&&mui.back();"):c.canBack(function(d){d.canBack?b.history.back():c.id===plus.runtime.appid?a.__back__first?new Date().getTime()-a.__back__first<2e3&&plus.runtime.quit():(a.__back__first=new Date().getTime(),mui.toast("再按一次退出应用"),setTimeout(function(){a.__back__first=null;},2e3)):c.preload?c.hide("auto"):a.closeAll(c);}),!0;}}),a.menu=function(){var c=document.querySelector(".mui-action-menu");if(c)a.trigger(c,a.EVENT_START),a.trigger(c,"tap");else if(b.plus){var d=a.currentWebview,e=d.parent();e&&e.evalJS("mui&&mui.menu();");}};var c=function c(){a.back();},d=function d(){a.menu();};a.plusReady(function(){a.options.keyEventBind.backbutton&&plus.key.addEventListener("backbutton",c,!1),a.options.keyEventBind.menubutton&&plus.key.addEventListener("menubutton",d,!1);}),a.addInit({name:"keyEventBind",index:1e3,handle:function handle(){a.plusReady(function(){a.options.keyEventBind.backbutton||plus.key.removeEventListener("backbutton",c),a.options.keyEventBind.menubutton||plus.key.removeEventListener("menubutton",d);});}});}(mui,window),function(a){a.addInit({name:"pullrefresh",index:1e3,handle:function handle(){var b=a.options,c=b.pullRefresh||{},d=c.down&&c.down.hasOwnProperty("callback"),e=c.up&&c.up.hasOwnProperty("callback");if(d||e){var f=c.container;if(f){var g=a(f);1===g.length&&(a.os.plus&&a.os.android?a.plusReady(function(){var b=plus.webview.currentWebview();if(e){var f={};f.up=c.up,f.webviewId=b.id||b.getURL(),g.pullRefresh(f);}if(d){var h=b.parent(),i=b.id||b.getURL();if(h){e||g.pullRefresh({webviewId:i});var j={webviewId:i};j.down=a.extend({},c.down),j.down.callback="_CALLBACK",h.evalJS("mui&&mui(document.querySelector('.mui-content')).pullRefresh('"+(0,_stringify2.default)(j)+"')");}}}):g.pullRefresh(c));}}}});}(mui),function(a,b,c){var d="application/json",e="text/html",f=/<script\b[^<]*(?:(?!<\/script>)<[^<]*)*<\/script>/gi,g=/^(?:text|application)\/javascript/i,h=/^(?:text|application)\/xml/i,i=/^\s*$/;a.ajaxSettings={type:"GET",beforeSend:a.noop,success:a.noop,error:a.noop,complete:a.noop,context:null,xhr:function xhr(a){return new b.XMLHttpRequest();},accepts:{script:"text/javascript, application/javascript, application/x-javascript",json:d,xml:"application/xml, text/xml",html:e,text:"text/plain"},timeout:0,processData:!0,cache:!0};var j=function j(a,b){var c=b.context;return b.beforeSend.call(c,a,b)===!1?!1:void 0;},k=function k(a,b,c){c.success.call(c.context,a,"success",b),m("success",b,c);},l=function l(a,b,c,d){d.error.call(d.context,c,b,a),m(b,c,d);},m=function m(a,b,c){c.complete.call(c.context,b,a);},n=function n(b,c,d,e){

});},o=function o(b){if(b.processData&&b.data&&"string"!=typeof b.data){var e=b.contentType;!e&&b.headers&&(e=b.headers["Content-Type"]),e&&~e.indexOf(d)?b.data=(0,_stringify2.default)(b.data):b.data=a.param(b.data,b.traditional);}!b.data||b.type&&"GET"!==b.type.toUpperCase()||(b.url=p(b.url,b.data),b.data=c);},p=function p(a,b){return""===b?a:(a+"&"+b).replace(/[&?]{1,2}/,"?");},q=function q(a){return a&&(a=a.split(";",2)[0]),a&&(a===e?"html":a===d?"json":g.test(a)?"script":h.test(a)&&"xml")||"text";},r=function r(b,d,e,f){return a.isFunction(d)&&(f=e,e=d,d=c),a.isFunction(e)||(f=e,e=c),{url:b,data:d,success:e,dataType:f};};a.ajax=function(d,e){"object"==(typeof d==="undefined"?"undefined":(0,_typeof3.default)(d))&&(e=d,d=c);var f=e||{};f.url=d||f.url;for(var g in a.ajaxSettings){f[g]===c&&(f[g]=a.ajaxSettings[g]);}o(f);var h=f.dataType;f.cache!==!1&&(e&&e.cache===!0||"script"!==h)||(f.url=p(f.url,"_="+a.now()));var m,n=f.accepts[h&&h.toLowerCase()],r={},s=function s(a,b){r[a.toLowerCase()]=[a,b];},t=/^([\w-]+:)\/\//.test(f.url)?RegExp.$1:b.location.protocol,u=f.xhr(f),v=u.setRequestHeader;

}if(u.setRequestHeader=s,u.onreadystatechange=function(){if(4===u.readyState){u.onreadystatechange=a.noop,clearTimeout(m);var b,c=!1,d="file:"===t;if(u.status>=200&&u.status<300||304===u.status||0===u.status&&d&&u.responseText){h=h||q(f.mimeType||u.getResponseHeader("content-type")),b=u.responseText;try{"script"===h?(1,eval)(b):"xml"===h?b=u.responseXML:"json"===h&&(b=i.test(b)?null:a.parseJSON(b));}catch(e){c=e;}c?l(c,"parsererror",u,f):k(b,u,f);}else{var g=u.status?"error":"abort",j=u.statusText||null;d&&(g="error",j="404"),l(j,g,u,f);}}},j(u,f)===!1)return u.abort(),l(null,"abort",u,f),u;if(f.xhrFields)for(var w in f.xhrFields){u[w]=f.xhrFields[w];}var x="async"in f?f.async:!0;u.open(f.type.toUpperCase(),f.url,x,f.username,f.password);for(var w in r){r.hasOwnProperty(w)&&v.apply(u,r[w]);}return f.timeout>0&&(m=setTimeout(function(){u.onreadystatechange=a.noop,u.abort(),l(null,"timeout",u,f);},f.timeout)),u.send(f.data?f.data:null),u;},a.param=function(a,b){var c=[];return c.add=function(a,b){this.push(encodeURIComponent(a)+"="+encodeURIComponent(b));},n(c,a,b),c.join("&").replace(/%20/g,"+");},a.get=function(){return a.ajax(r.apply(null,arguments));}

if(d&&d.length>0)for(var h=0,i=d.length;i>h;h++){c.appendChild(d[h]);}g[0].innerHTML=c.innerHTML;}else g[0].innerHTML=a;j&&j.apply(g,arguments);},a.ajax(i),this;};}(mui,window),function(a){var b=document.createElement("a");b.href=window.location.href,a.plusReady(function(){a.ajaxSettings=a.extend(a.ajaxSettings,{xhr:function xhr(a){if(a.crossDomain)return new plus.net.XMLHttpRequest();if("file:"!==b.protocol){var c=document.createElement("a");if(c.href=a.url,c.href=c.href,a.crossDomain=b.protocol+"//"+b.host!=c.protocol+"//"+c.host,a.crossDomain)return new plus.net.XMLHttpRequest();}return new window.XMLHttpRequest();}});});}(mui),function(a,b,c){a.offset=function(a){var d={top:0,left:0};return(0,_typeof3.default)(a.getBoundingClientRect)!==c&&(d=a.getBoundingClientRect()),{top:d.top+b.pageYOffset-a.clientTop,left:d.left+b.pageXOffset-a.clientLeft};};}(mui,window),function(a,b){a.scrollTo=function(a,c,d){c=c||1e3;var e=function e(c){if(0>=c)return b.scrollTo(0,a),void(d&&d());var f=a-b.scrollY;setTimeout(function(){b.scrollTo(0,b.scrollY+f/c*10),e(c-10);},16.7);};e(c);},a.animationFrame=function(a){var b,c,d;return function(){b=arguments,d=this,c||(c=!0,requestAnimationFrame(function(){a.apply(d,b),c=!1;}));};};}(mui,window),function(a){var b=!1,c=/xyz/.test(function(){xyz;})?/\b_super\b/:/.*/,d=function d(){};d.extend=function(a){function d(){!b&&this.init&&this.init.apply(this,arguments);}var e=this.prototype;b=!0;var f=new this();b=!1;for(var g in a){f[g]="function"==typeof a[g]&&"function"==typeof e[g]&&c.test(a[g])?function(a,b){return function(){var c=this._super;this._super=e[a];var d=b.apply(this,arguments);return this._super=c,d;};}(g,a[g]):a[g];}return d.prototype=f,d.prototype.constructor=d,d.extend=arguments.callee,d;},a.Class=d;}(mui),function(a,b,c){var d="mui-pull-top-pocket",e="mui-pull-bottom-pocket",f="mui-pull",g="mui-pull-loading",h="mui-pull-caption",i="mui-pull-caption-down",j="mui-pull-caption-refresh",k="mui-pull-caption-nomore",l="mui-icon",m="mui-spinner",n="mui-icon-pulldown",o="mui-block",p="mui-hidden",q="mui-visibility",r=g+" "+l+" "+n,s=g+" "+l+" "+n,t=g+" "+l+" "+m,u=['<div class="'+f+'">','<div class="{icon}"></div>','<div class="'+h+'">{contentrefresh}</div>',"</div>"].join(""),v={init:function init(b,c){this._super(b,a.extend(!0,{scrollY:!0,scrollX:!1,indicators:!0,deceleration:.003,down:{height:50,contentinit:"下拉可以刷新",contentdown:"下拉可以刷新",contentover:"释放立即刷新",contentrefresh:"正在刷新..."},up:{height:50,auto:!1,contentinit:"上拉显示更多",contentdown:"上拉显示更多",contentrefresh:"正在加载...",contentnomore:"没有更多数据了",duration:300}},c));},_init:function _init(){this._super(),this._initPocket();},_initPulldownRefresh:function _initPulldownRefresh(){this.pulldown=!0,this.pullPocket=this.topPocket,this.pullPocket.classList.add(o),this.pullPocket.classList.add(q),this.pullCaption=this.topCaption,this.pullLoading=this.topLoading;},_initPullupRefresh:function _initPullupRefresh(){this.pulldown=!1,this.pullPocket=this.bottomPocket,this.pullPocket.classList.add(o),this.pullPocket.classList.add(q),this.pullCaption=this.bottomCaption,this.pullLoading=this.bottomLoading;},_initPocket:function _initPocket(){var a=this.options;a.down&&a.down.hasOwnProperty("callback")&&(this.topPocket=this.scroller.querySelector("."+d),this.topPocket||(this.topPocket=this._createPocket(d,a.down,s),this.wrapper.insertBefore(this.topPocket,this.wrapper.firstChild)),this.topLoading=this.topPocket.querySelector("."+g),this.topCaption=this.topPocket.querySelector("."+h)),a.up&&a.up.hasOwnProperty("callback")&&(this.bottomPocket=this.scroller.querySelector("."+e),this.bottomPocket||(this.bottomPocket=this._createPocket(e,a.up,t),this.scroller.appendChild(this.bottomPocket)),this.bottomLoading=this.bottomPocket.querySelector("."+g),this.bottomCaption=this.bottomPocket.querySelector("."+h),this.wrapper.addEventListener("scrollbottom",this));},_createPocket:function _createPocket(a,c,d){var e=b.createElement("div");return e.className=a,e.innerHTML=u.replace("{contentrefresh}",c.contentinit).replace("{icon}",d),e;},_resetPullDownLoading:function _resetPullDownLoading(){var a=this.pullLoading;a&&(this.pullCaption.innerHTML=this.options.down.contentdown,a.style.webkitTransition="",a.style.webkitTransform="",a.style.webkitAnimation="",a.className=s);},_setCaptionClass:function _setCaptionClass(a,b,c){if(!a)switch(c){case this.options.up.contentdown:b.className=h+" "+i;break;case this.options.up.contentrefresh:b.className=h+" "+j;break;case this.options.up.contentnomore:b.className=h+" "+k;}},_setCaption:function _setCaption(a,b){if(!this.loading){var c=this.options,d=this.pullPocket,e=this.pullCaption,f=this.pullLoading,g=this.pulldown,h=this;d&&(b?setTimeout(function(){e.innerHTML=h.lastTitle=a,g?f.className=s:(h._setCaptionClass(!1,e,a),f.className=t),f.style.webkitAnimation="",f.style.webkitTransition="",f.style.webkitTransform="";},100):a!==this.lastTitle&&(e.innerHTML=a,g?a===c.down.contentrefresh?(f.className=t,f.style.webkitAnimation="spinner-spin 1s step-end infinite"):a===c.down.contentover?(f.className=r,f.style.webkitTransition="-webkit-transform 0.3s ease-in",f.style.webkitTransform="rotate(180deg)"):a===c.down.contentdown&&(f.className=s,f.style.webkitTransition="-webkit-transform 0.3s ease-in",f.style.webkitTransform="rotate(0deg)"):(a===c.up.contentrefresh?f.className=t+" "+q:f.className=t+" "+p,h._setCaptionClass(!1,e,a)),this.lastTitle=a));}}};a.PullRefresh=v;}(mui,document),function(a,b,c,d){var e="mui-scroll",f="mui-scrollbar",g="mui-scrollbar-indicator",h=f+"-vertical",i=f+"-horizontal",j="mui-active",k={quadratic:{style:"cubic-bezier(0.25, 0.46, 0.45, 0.94)",fn:function fn(a){return a*(2-a);}},circular:{style:"cubic-bezier(0.1, 0.57, 0.1, 1)",fn:function fn(a){return Math.sqrt(1- --a*a);}},outCirc:{style:"cubic-bezier(0.075, 0.82, 0.165, 1)"},outCubic:{style:"cubic-bezier(0.165, 0.84, 0.44, 1)"}},l=a.Class.extend({init:function init(b,c){this.wrapper=this.element=b,this.scroller=this.wrapper.children[0],this.scrollerStyle=this.scroller&&this.scroller.style,this.stopped=!1,this.options=a.extend(!0,{scrollY:!0,scrollX:!1,startX:0,startY:0,indicators:!0,stopPropagation:!1,hardwareAccelerated:!0,fixedBadAndorid:!1,preventDefaultException:{tagName:/^(INPUT|TEXTAREA|BUTTON|SELECT|VIDEO)$/},momentum:!0,snapX:.5,snap:!1,bounce:!0,bounceTime:500,bounceEasing:k.outCirc,scrollTime:500,scrollEasing:k.outCubic,directionLockThreshold:5,parallaxElement:!1,parallaxRatio:.5},c),this.x=0,this.y=0,this.translateZ=this.options.hardwareAccelerated?" translateZ(0)":"",this._init(),this.scroller&&(this.refresh(),this.scrollTo(this.options.startX,this.options.startY));},_init:function _init(){this._initParallax(),this._initIndicators(),this._initEvent();},_initParallax:function _initParallax(){this.options.parallaxElement&&(this.parallaxElement=c.querySelector(this.options.parallaxElement),this.parallaxStyle=this.parallaxElement.style,this.parallaxHeight=this.parallaxElement.offsetHeight,this.parallaxImgStyle=this.parallaxElement.querySelector("img").style);},_initIndicators:function _initIndicators(){var a=this;if(a.indicators=[],this.options.indicators){var b,c=[];a.options.scrollY&&(b={el:this._createScrollBar(h),listenX:!1},this.wrapper.appendChild(b.el),c.push(b)),this.options.scrollX&&(b={el:this._createScrollBar(i),listenY:!1},this.wrapper.appendChild(b.el),c.push(b));for(var d=c.length;d--;){this.indicators.push(new m(this,c[d]));}}},_initSnap:function _initSnap(){this.currentPage={},this.pages=[];for(var a=this.snaps,b=a.length,c=0,d=-1,e=0,f=0,g=0,h=0,i=0;b>i;i++){var k=a[i],l=k.offsetLeft,m=k.offsetWidth;(0===i||l<=a[i-1].offsetLeft)&&(c=0,d++),this.pages[c]||(this.pages[c]=[]),e=this._getSnapX(l),h=Math.round(m*this.options.snapX),f=e-h,g=e-m+h,this.pages[c][d]={x:e,leftX:f,rightX:g,pageX:c,element:k},k.classList.contains(j)&&(this.currentPage=this.pages[c][0]),e>=this.maxScrollX&&c++;}this.options.startX=this.currentPage.x||0;},_getSnapX:function _getSnapX(a){return Math.max(Math.min(0,-a+this.wrapperWidth/2),this.maxScrollX);},_gotoPage:function _gotoPage(a){this.currentPage=this.pages[Math.min(a,this.pages.length-1)][0];for(var b=0,c=this.snaps.length;c>b;b++){b===a?this.snaps[b].classList.add(j):this.snaps[b].classList.remove(j);}this.scrollTo(this.currentPage.x,0,this.options.scrollTime);},_nearestSnap:function _nearestSnap(a){if(!this.pages.length)return{x:0,pageX:0};var b=0,c=this.pages.length;for(a>0?a=0:a<this.maxScrollX&&(a=this.maxScrollX);c>b;b++){var d="left"===this.direction?this.pages[b][0].leftX:this.pages[b][0].rightX;if(a>=d)return this.pages[b][0];}return{x:0,pageX:0};},_initEvent:function _initEvent(c){var d=c?"removeEventListener":"addEventListener";b[d]("orientationchange",this),b[d]("resize",this),this.scroller[d]("webkitTransitionEnd",this),this.wrapper[d](a.EVENT_START,this),this.wrapper[d](a.EVENT_CANCEL,this),this.wrapper[d](a.EVENT_END,this),this.wrapper[d]("drag",this),this.wrapper[d]("dragend",this),this.wrapper[d]("flick",this),this.wrapper[d]("scrollend",this),this.options.scrollX&&this.wrapper[d]("swiperight",this);var e=this.wrapper.querySelector(".mui-segmented-control");e&&mui(e)[c?"off":"on"]("click","a",a.preventDefault),this.wrapper[d]("scrollstart",this),this.wrapper[d]("refresh",this);},_handleIndicatorScrollend:function _handleIndicatorScrollend(){this.indicators.map(function(a){a.fade();});},_handleIndicatorScrollstart:function _handleIndicatorScrollstart(){this.indicators.map(function(a){a.fade(1);});},_handleIndicatorRefresh:function _handleIndicatorRefresh(){this.indicators.map(function(a){a.refresh();});},handleEvent:function handleEvent(b){if(this.stopped)return void this.resetPosition();switch(b.type){case a.EVENT_START:this._start(b);break;case"drag":this.options.stopPropagation&&b.stopPropagation(),this._drag(b);break;case"dragend":case"flick":this.options.stopPropagation&&b.stopPropagation(),this._flick(b);break;case a.EVENT_CANCEL:case a.EVENT_END:this._end(b);break;case"webkitTransitionEnd":this.transitionTimer&&this.transitionTimer.cancel(),this._transitionEnd(b);break;case"scrollstart":this._handleIndicatorScrollstart(b);break;case"scrollend":this._handleIndicatorScrollend(b),this._scrollend(b),b.stopPropagation();break;case"orientationchange":case"resize":this._resize();break;case"swiperight":b.stopPropagation();break;case"refresh":this._handleIndicatorRefresh(b);}},_start:function _start(b){if(this.moved=this.needReset=!1,this._transitionTime(),this.isInTransition){this.needReset=!0,this.isInTransition=!1;var c=a.parseTranslateMatrix(a.getStyles(this.scroller,"webkitTransform"));this.setTranslate(Math.round(c.x),Math.round(c.y)),a.trigger(this.scroller,"scrollend",this),b.preventDefault();}this.reLayout(),a.trigger(this.scroller,"beforescrollstart",this);},_getDirectionByAngle:function _getDirectionByAngle(a){return-80>a&&a>-100?"up":a>=80&&100>a?"down":a>=170||-170>=a?"left":a>=-35&&10>=a?"right":null;},_drag:function _drag(c){var d=c.detail;if((this.options.scrollY||"up"===d.direction||"down"===d.direction)&&a.os.ios&&parseFloat(a.os.version)>=8){var e=d.gesture.touches[0].clientY;if(e+10>b.innerHeight||10>e)return void this.resetPosition(this.options.bounceTime);}var f=isReturn=!1;this._getDirectionByAngle(d.angle);if("left"===d.direction||"right"===d.direction?this.options.scrollX?(f=!0,this.moved||(a.gestures.session.lockDirection=!0,a.gestures.session.startDirection=d.direction)):this.options.scrollY&&!this.moved&&(isReturn=!0):"up"===d.direction||"down"===d.direction?this.options.scrollY?(f=!0,this.moved||(a.gestures.session.lockDirection=!0,a.gestures.session.startDirection=d.direction)):this.options.scrollX&&!this.moved&&(isReturn=!0):isReturn=!0,(this.moved||f)&&(c.stopPropagation(),d.gesture&&d.gesture.preventDefault()),!isReturn){this.moved?c.stopPropagation():a.trigger(this.scroller,"scrollstart",this);var g=0,h=0;this.moved?(g=d.deltaX-a.gestures.session.prevTouch.deltaX,h=d.deltaY-a.gestures.session.prevTouch.deltaY):(g=d.deltaX,h=d.deltaY);var i=Math.abs(d.deltaX),j=Math.abs(d.deltaY);i>j+this.options.directionLockThreshold?h=0:j>=i+this.options.directionLockThreshold&&(g=0),g=this.hasHorizontalScroll?g:0,h=this.hasVerticalScroll?h:0;

this.requestAnimationFrame||this._updateTranslate(),this.direction=d.deltaX>0?"right":"left",this.moved=!0,this.x=k,this.y=l,a.trigger(this.scroller,"scroll",this);}},_flick:function _flick(b){if(this.moved){b.stopPropagation();var c=b.detail;if(this._clearRequestAnimationFrame(),"dragend"!==b.type||!c.flick){var d=Math.round(this.x),e=Math.round(this.y);if(this.isInTransition=!1,!this.resetPosition(this.options.bounceTime)){if(this.scrollTo(d,e),"dragend"===b.type)return void a.trigger(this.scroller,"scrollend",this);var f=0,g="";return this.options.momentum&&c.flickTime<300&&(momentumX=this.hasHorizontalScroll?this._momentum(this.x,c.flickDistanceX,c.flickTime,this.maxScrollX,this.options.bounce?this.wrapperWidth:0,this.options.deceleration):{destination:d,duration:0},momentumY=this.hasVerticalScroll?this._momentum(this.y,c.flickDistanceY,c.flickTime,this.maxScrollY,this.options.bounce?this.wrapperHeight:0,this.options.deceleration):{destination:e,duration:0},d=momentumX.destination,e=momentumY.destination,f=Math.max(momentumX.duration,momentumY.duration),this.isInTransition=!0),d!=this.x||e!=this.y?((d>0||d<this.maxScrollX||e>0||e<this.maxScrollY)&&(g=k.quadratic),void this.scrollTo(d,e,f,g)):void a.trigger(this.scroller,"scrollend",this);}}}},_end:function _end(b){this.needReset=!1,(!this.moved&&this.needReset||b.type===a.EVENT_CANCEL)&&this.resetPosition();},_transitionEnd:function _transitionEnd(b){b.target==this.scroller&&this.isInTransition&&(this._transitionTime(),this.resetPosition(this.options.bounceTime)||(this.isInTransition=!1,a.trigger(this.scroller,"scrollend",this)));},_scrollend:function _scrollend(b){(0===this.y&&0===this.maxScrollY||Math.abs(this.y)>0&&this.y<=this.maxScrollY)&&a.trigger(this.scroller,"scrollbottom",this);},_resize:function _resize(){var a=this;clearTimeout(a.resizeTimeout),a.resizeTimeout=setTimeout(function(){a.refresh();},a.options.resizePolling);},_transitionTime:function _transitionTime(b){if(b=b||0,this.scrollerStyle.webkitTransitionDuration=b+"ms",this.parallaxElement&&this.options.scrollY&&(this.parallaxStyle.webkitTransitionDuration=b+"ms"),this.options.fixedBadAndorid&&!b&&a.os.isBadAndroid&&(this.scrollerStyle.webkitTransitionDuration="0.001s",this.parallaxElement&&this.options.scrollY&&(this.parallaxStyle.webkitTransitionDuration="0.001s")),this.indicators)for(var c=this.indicators.length;c--;){this.indicators[c].transitionTime(b);}b&&(this.transitionTimer&&this.transitionTimer.cancel(),this.transitionTimer=a.later(function(){a.trigger(this.scroller,"webkitTransitionEnd");},b+100,this));},_transitionTimingFunction:function _transitionTimingFunction(a){if(this.scrollerStyle.webkitTransitionTimingFunction=a,this.parallaxElement&&this.options.scrollY&&(this.parallaxStyle.webkitTransitionDuration=a),this.indicators)for(var b=this.indicators.length;b--;){this.indicators[b].transitionTimingFunction(a);}},_translate:function _translate(a,b){this.x=a,this.y=b;},_clearRequestAnimationFrame:function _clearRequestAnimationFrame(){this.requestAnimationFrame&&(cancelAnimationFrame(this.requestAnimationFrame),this.requestAnimationFrame=null);},_updateTranslate:function _updateTranslate(){var a=this;(a.x!==a.lastX||a.y!==a.lastY)&&a.setTranslate(a.x,a.y),a.requestAnimationFrame=requestAnimationFrame(function(){a._updateTranslate();});},_createScrollBar:function _createScrollBar(a){var b=c.createElement("div"),d=c.createElement("div");return b.className=f+" "+a,d.className=g,b.appendChild(d),a===h?(this.scrollbarY=b,this.scrollbarIndicatorY=d):a===i&&(this.scrollbarX=b,this.scrollbarIndicatorX=d),this.wrapper.appendChild(b),b;},_preventDefaultException:function _preventDefaultException(a,b){for(var c in b){if(b[c].test(a[c]))return!0;}return!1;},_reLayout:function _reLayout(){if(this.hasHorizontalScroll||(this.maxScrollX=0,this.scrollerWidth=this.wrapperWidth),this.hasVerticalScroll||(this.maxScrollY=0,this.scrollerHeight=this.wrapperHeight),this.indicators.map(function(a){a.refresh();}),this.options.snap&&"string"==typeof this.options.snap){var a=this.scroller.querySelectorAll(this.options.snap);this.itemLength=0,this.snaps=[];for(var b=0,c=a.length;c>b;b++){var d=a[b];d.parentNode===this.scroller&&(this.itemLength++,this.snaps.push(d));}this._initSnap();}},_momentum:function _momentum(a,b,c,e,f,g){var h,i,j=parseFloat(Math.abs(b)/c);return g=g===d?6e-4:g,h=a+j*j/(2*g)*(0>b?-1:1),i=j/g,e>h?(h=f?e-f/2.5*(j/8):e,b=Math.abs(h-a),i=b/j):h>0&&(h=f?f/2.5*(j/8):0,b=Math.abs(a)+h,i=b/j),{destination:Math.round(h),duration:i};},_getTranslateStr:function _getTranslateStr(a,b){return this.options.hardwareAccelerated?"translate3d("+a+"px,"+b+"px,0px) "+this.translateZ:"translate("+a+"px,"+b+"px) ";},setStopped:function setStopped(a){this.stopped=!!a;},setTranslate:function setTranslate(b,c){if(this.x=b,this.y=c,this.scrollerStyle.webkitTransform=this._getTranslateStr(b,c),this.parallaxElement&&this.options.scrollY){var d=c*this.options.parallaxRatio,e=1+d/((this.parallaxHeight-d)/2);e>1?(this.parallaxImgStyle.opacity=1-d/100*this.options.parallaxRatio,this.parallaxStyle.webkitTransform=this._getTranslateStr(0,-d)+" scale("+e+","+e+")"):(this.parallaxImgStyle.opacity=1,this.parallaxStyle.webkitTransform=this._getTranslateStr(0,-1)+" scale(1,1)");}if(this.indicators)for(var f=this.indicators.length;f--;){this.indicators[f].updatePosition();}this.lastX=this.x,this.lastY=this.y,a.trigger(this.scroller,"scroll",this);},reLayout:function reLayout(){this.wrapper.offsetHeight;var b=parseFloat(a.getStyles(this.wrapper,"padding-left"))||0,c=parseFloat(a.getStyles(this.wrapper,"padding-right"))||0,d=parseFloat(a.getStyles(this.wrapper,"padding-top"))||0,e=parseFloat(a.getStyles(this.wrapper,"padding-bottom"))||0,f=this.wrapper.clientWidth,g=this.wrapper.clientHeight;this.scrollerWidth=this.scroller.offsetWidth,this.scrollerHeight=this.scroller.offsetHeight,this.wrapperWidth=f-b-c,this.wrapperHeight=g-d-e,this.maxScrollX=Math.min(this.wrapperWidth-this.scrollerWidth,0),this.maxScrollY=Math.min(this.wrapperHeight-this.scrollerHeight,0),this.hasHorizontalScroll=this.options.scrollX&&this.maxScrollX<0,this.hasVerticalScroll=this.options.scrollY&&this.maxScrollY<0,this._reLayout();},resetPosition:function resetPosition(a){var b=this.x,c=this.y;

},_reInit:function _reInit(){for(var a=this.wrapper.querySelectorAll("."+e),b=0,c=a.length;c>b;b++){if(a[b].parentNode===this.wrapper){this.scroller=a[b];break;}}this.scrollerStyle=this.scroller&&this.scroller.style;},refresh:function refresh(){this._reInit(),this.reLayout(),a.trigger(this.scroller,"refresh",this),this.resetPosition();},scrollTo:function scrollTo(a,b,c,d){var d=d||k.circular;this.isInTransition=c>0,this.isInTransition?(this._clearRequestAnimationFrame(),this._transitionTimingFunction(d.style),this._transitionTime(c),this.setTranslate(a,b)):this.setTranslate(a,b);},scrollToBottom:function scrollToBottom(a,b){a=a||this.options.scrollTime,this.scrollTo(0,this.maxScrollY,a,b);},gotoPage:function gotoPage(a){this._gotoPage(a);},destroy:function destroy(){this._initEvent(!0),delete a.data[this.wrapper.getAttribute("data-scroll")],this.wrapper.setAttribute("data-scroll","");}}),m=function m(b,d){this.wrapper="string"==typeof d.el?c.querySelector(d.el):d.el,this.wrapperStyle=this.wrapper.style,this.indicator=this.wrapper.children[0],this.indicatorStyle=this.indicator.style,this.scroller=b,this.options=a.extend({listenX:!0,listenY:!0,fade:!1,speedRatioX:0,speedRatioY:0},d),this.sizeRatioX=1,this.sizeRatioY=1,this.maxPosX=0,this.maxPosY=0,this.options.fade&&(this.wrapperStyle.webkitTransform=this.scroller.translateZ,this.wrapperStyle.webkitTransitionDuration=this.options.fixedBadAndorid&&a.os.isBadAndroid?"0.001s":"0ms",this.wrapperStyle.opacity="0");};m.prototype={handleEvent:function handleEvent(a){},transitionTime:function transitionTime(b){b=b||0,this.indicatorStyle.webkitTransitionDuration=b+"ms",this.scroller.options.fixedBadAndorid&&!b&&a.os.isBadAndroid&&(this.indicatorStyle.webkitTransitionDuration="0.001s");},transitionTimingFunction:function transitionTimingFunction(a){this.indicatorStyle.webkitTransitionTimingFunction=a;},refresh:function refresh(){this.

wrapper.offsetHeight,this.options.listenX&&(this.wrapperWidth=this.wrapper.clientWidth,this.indicatorWidth=Math.max(Math.round(this.wrapperWidth*this.wrapperWidth/(this.scroller.scrollerWidth||this.wrapperWidth||1)),8),this.indicatorStyle.width=this.indicatorWidth+"px",this.maxPosX=this.wrapperWidth-this.indicatorWidth,this.minBoundaryX=0,this.maxBoundaryX=this.maxPosX,this.sizeRatioX=this.options.speedRatioX||this.scroller.maxScrollX&&this.maxPosX/this.scroller.maxScrollX),this.options.listenY&&(this.wrapperHeight=this.wrapper.clientHeight,this.indicatorHeight=Math.max(Math.round(this.wrapperHeight*this.wrapperHeight/(this.scroller.scrollerHeight||this.wrapperHeight||1)),8),this.indicatorStyle.height=this.indicatorHeight+"px",this.maxPosY=this.wrapperHeight-this.indicatorHeight,this.minBoundaryY=0,this.maxBoundaryY=this.maxPosY,this.sizeRatioY=this.options.speedRatioY||this.scroller.maxScrollY&&this.maxPosY/this.scroller.maxScrollY),this.updatePosition();},updatePosition:function updatePosition(){var a=this.options.listenX&&Math.round(this.sizeRatioX*this.scroller.x)||0,b=this.options.listenY&&Math.round(this.sizeRatioY*this.scroller.y)||0;a<this.minBoundaryX?(this.width=Math.max(this.indicatorWidth+a,8),this.indicatorStyle.width=this.width+"px",a=this.minBoundaryX):a>this.maxBoundaryX?(this.width=Math.max(

(this.height=Math.max(this.indicatorHeight+3*b,8),this.indicatorStyle.height=this.height+"px",b=this.minBoundaryY):b>this.maxBoundaryY?(this.height=Math.max(this.indicatorHeight-3*(b-this.maxPosY),8),this.indicatorStyle.height=this.height+"px",b=this.maxPosY+this.indicatorHeight-this.height):this.height!=this.indicatorHeight&&(this.height=this.indicatorHeight,this.indicatorStyle.height=this.height+"px"),this.x=a,this.y=b,this.indicatorStyle.webkitTransform=this.scroller._getTranslateStr(a,b);},fade:function fade(a,b){if(!b||this.visible){clearTimeout(this.fadeTimeout),this.fadeTimeout=null;var c=a?250:500,d=a?0:300;a=a?"1":"0",this.wrapperStyle.webkitTransitionDuration=c+"ms",this.fadeTimeout=setTimeout(function(a){this.wrapperStyle.opacity=a,this.visible=+a;}.bind(this,a),d);}}},a.Scroll=l,a.fn.scroll=function(b){var c=[];return this.each(function(){var d=null,e=this,f=e.getAttribute("data-scroll");if(f)d=a.data[f];else{f=++a.uuid;var g=a.extend({},b);e.classList.contains("mui-segmented-control")&&(g=a.extend(g,{scrollY:!1,scrollX:!0,indicators:!1,snap:".mui-control-item"})),a.data[f]=d=new l(e,g),e.setAttribute("data-scroll",f);}c.push(d);}),1===c.length?c[0]:c;};}(mui,window,document),function(a,b,c,d){var e="mui-visibility",f="mui-hidden",g=a.Scroll.extend(a.extend({handleEvent:function handleEvent(a){this._super(a),"scrollbottom"===a.type&&a.target===this.scroller&&this._scrollbottom();},_scrollbottom:function _scrollbottom(){this.pulldown||this.loading||(this.pulldown=!1,this._initPullupRefresh(),this.pullupLoading());},_start:function _start(a){a.touches&&a.touches.length&&a.touches[0].clientX>30&&a.target&&!this._preventDefaultException(a.target,this.options.preventDefaultException)&&a.preventDefault(),this.loading||(this.pulldown=this.pullPocket=this.pullCaption=this.pullLoading=!1),this._super(a);},_drag:function _drag(a){this._super(a),!this.pulldown&&!this.loading&&this.topPocket&&"down"===a.detail.direction&&this.y>=0&&this._initPulldownRefresh(),this.pulldown&&this._setCaption(this.y>this.options.down.height?this.options.down.contentover:this.options.down.contentdown);},_reLayout:function _reLayout(){this.hasVerticalScroll=!0,this._super();},resetPosition:function resetPosition(a){if(this.pulldown){if(this.y>=this.options.down.height)return this.pulldownLoading(d,a||0),!0;!this.loading&&this.topPocket.classList.remove(e);}return this._super(a);},pulldownLoading:function pulldownLoading(a,b){if("undefined"==typeof a&&(a=this.options.down.height),this.scrollTo(0,a,b,this.options.bounceEasing),!this.loading){this._initPulldownRefresh(),this._setCaption(this.options.down.contentrefresh),this.loading=!0,this.indicators.map(function(a){a.fade(0);});var c=this.options.down.callback;c&&c.call(this);}},endPulldownToRefresh:function endPulldownToRefresh(){var a=this;a.topPocket&&a.loading&&this.pulldown&&(a.scrollTo(0,0,a.options.bounceTime,a.options.bounceEasing),a.loading=!1,a._setCaption(a.options.down.contentdown,!0),setTimeout(function(){a.loading||a.topPocket.classList.remove(e);},350));},pullupLoading:function pullupLoading(a,b,c){b=b||0,this.scrollTo(b,this.maxScrollY,c,this.options.bounceEasing),this.loading||(this._initPullupRefresh(),this._setCaption(this.options.up.contentrefresh),this.indicators.map(function(a){a.fade(0);}),this.loading=!0,a=a||this.options.up.callback,a&&a.call(this));},endPullupToRefresh:function endPullupToRefresh(a){var b=this;b.bottomPocket&&(b.loading=!1,a?(this.finished=!0,b._setCaption(b.options.up.contentnomore),b.wrapper.removeEventListener("scrollbottom",b)):(b._setCaption(b.options.up.contentdown),b.loading||b.bottomPocket.classList.remove(e)));},disablePullupToRefresh:function disablePullupToRefresh(){this._initPullupRefresh(),this.bottomPocket.className="mui-pull-bottom-pocket "+f,this.wrapper.removeEventListener("scrollbottom",this);},enablePullupToRefresh:function enablePullupToRefresh(){this._initPullupRefresh(),this.bottomPocket.classList.remove(f),this._setCaption(this.options.up.contentdown),this.wrapper.addEventListener("scrollbottom",this);},refresh:function refresh(a){a&&this.finished&&(this.enablePullupToRefresh(),this.finished=!1),this._super();}},a.PullRefresh));a.fn.pullRefresh=function(b){if(1===this.length){var c=this[0],d=null;b=b||{};var e=c.getAttribute("data-pullrefresh");return e?d=a.data[e]:(e=++a.uuid,a.data[e]=d=new g(c,b),c.setAttribute("data-pullrefresh",e)),b.down&&b.down.auto?d.pulldownLoading(b.down.autoY):b.up&&b.up.auto&&d.pullupLoading(),d;}};}(mui,window,document),function(a,b){var c="mui-slider",d="mui-slider-group",e="mui-slider-loop",f="mui-action-previous",g="mui-action-next",h="mui-slider-item",i="mui-active",j="."+h,k=".mui-slider-progress-bar",l=a.Slider=a.Scroll.extend({init:function init(b,c){this._super(b,a.extend(!0,{fingers:1,interval:0,scrollY:!1,scrollX:!0,indicators:!1,scrollTime:1e3,startX:!1,slideTime:0,snap:j},c)),this.options.startX;},_init:function _init(){this._reInit(),this.scroller&&(this.scrollerStyle=this.scroller.style,this.progressBar=this.wrapper.querySelector(k),this.progressBar&&(this.progressBarWidth=this.progressBar.offsetWidth,this.progressBarStyle=this.progressBar.style),this._super(),this._initTimer());},_triggerSlide:function _triggerSlide(){var b=this;b.isInTransition=!1;b.currentPage;b.slideNumber=b._fixedSlideNumber(),b.loop&&(0===b.slideNumber?b.setTranslate(b.pages[1][0].x,0):b.slideNumber===b.itemLength-3&&b.setTranslate(b.pages[b.itemLength-2][0].x,0)),b.lastSlideNumber!=b.slideNumber&&(b.lastSlideNumber=b.slideNumber,b.lastPage=b.currentPage,a.trigger(b.wrapper,"slide",{slideNumber:b.slideNumber})),b._initTimer();},_handleSlide:function _handleSlide(b){var c=this;if(b.target===c.wrapper){var d=b.detail;d.slideNumber=d.slideNumber||0;for(var e=c.scroller.querySelectorAll(j),f=[],g=0,h=e.length;h>g;g++){var k=e[g];k.parentNode===c.scroller&&f.push(k);}var l=d.slideNumber;if(c.loop&&(l+=1),!c.wrapper.classList.contains("mui-segmented-control"))for(var g=0,h=f.length;h>g;g++){var k=f[g];k.parentNode===c.scroller&&(g===l?k.classList.add(i):k.classList.remove(i));}var m=c.wrapper.querySelector(".mui-slider-indicator");if(m){m.getAttribute("data-scroll")&&a(m).scroll().gotoPage(d.slideNumber);var n=m.querySelectorAll(".mui-indicator");if(n.length>0)for(var g=0,h=n.length;h>g;g++){n[g].classList[g===d.slideNumber?"add":"remove"](i);}else{var o=m.querySelector(".mui-number span");if(o)o.innerText=d.slideNumber+1;else for(var p=m.querySelectorAll(".mui-control-item"),g=0,h=p.length;h>g;g++){p[g].classList[g===d.slideNumber?"add":"remove"](i);}}}b.stopPropagation();}},_handleTabShow:function _handleTabShow(a){var b=this;b.gotoItem(a.detail.tabNumber||0,b.options.slideTime);},_handleIndicatorTap:function _handleIndicatorTap(a){var b=this,c=a.target;(c.classList.contains(f)||c.classList.contains(g))&&(b[c.classList.contains(f)?"prevItem":"nextItem"](),a.stopPropagation());},_initEvent:function _initEvent(b){var c=this;c._super(b);var d=b?"removeEventListener":"addEventListener";c.wrapper[d]("slide",this),c.wrapper[d](a.eventName("shown","tab"),this);},handleEvent:function handleEvent(b){switch(this._super(b),b.type){case"slide":this._handleSlide(b);break;case a.eventName("shown","tab"):~this.snaps.indexOf(b.target)&&this._handleTabShow(b);}},_scrollend:function _scrollend(a){this._super(a),this._triggerSlide(a);},_drag:function _drag(a){this._super(a);var c=a.detail.direction;if("left"===c||"right"===c){var d=this.wrapper.getAttribute("data-slidershowTimer");d&&b.clearTimeout(d),a.stopPropagation();}},_initTimer:function _initTimer(){var a=this,c=a.wrapper,d=a.options.interval,e=c.getAttribute("data-slidershowTimer");e&&b.clearTimeout(e),d&&(e=b.setTimeout(function(){c&&((c.offsetWidth||c.offsetHeight)&&a.nextItem(!0),a._initTimer());},d),c.setAttribute("data-slidershowTimer",e));},_fixedSlideNumber:function _fixedSlideNumber(a){a=a||this.currentPage;var b=a.pageX;return this.loop&&(b=0===a.pageX?this.itemLength-3:a.pageX===this.itemLength-1?0:a.pageX-1),b;},_reLayout:function _reLayout(){this.hasHorizontalScroll=!0,this.loop=this.scroller.classList.contains(e),this._super();},_getScroll:function _getScroll(){var b=a.parseTranslateMatrix(a.getStyles(this.scroller,"webkitTransform"));return b?b.x:0;},_transitionEnd:function _transitionEnd(b){b.target===this.scroller&&this.isInTransition&&(this._transitionTime(),this.isInTransition=!1,a.trigger(this.wrapper,"scrollend",this));},_flick:function _flick(a){if(this.moved){var b=a.detail,c=b.direction;this._clearRequestAnimationFrame(),this.isInTransition=!0,"flick"===a.type?(b.deltaTime<200&&(this.x=this._getPage(this.slideNumber+("right"===c?-1:1),!0).x),this.resetPosition(this.options.bounceTime)):"dragend"!==a.type||b.flick||this.resetPosition(this.options.bounceTime),a.stopPropagation();}},_initSnap:function _initSnap(){if(this.scrollerWidth=this.itemLength*this.scrollerWidth,this.maxScrollX=Math.min(this.wrapperWidth-this.scrollerWidth,0),this._super(),this.currentPage.x)this.slideNumber=this._fixedSlideNumber(),this.lastSlideNumber="undefined"==typeof this.lastSlideNumber?this.slideNumber:this.lastSlideNumber;else{var a=this.pages[this.loop?1:0];if(a=a||this.pages[0],!a)return;this.currentPage=a[0],this.slideNumber=0,this.lastSlideNumber="undefined"==typeof this.lastSlideNumber?0:this.lastSlideNumber;}this.options.startX=this.currentPage.x||0;},_getSnapX:function _getSnapX(a){return Math.max(-a,this.maxScrollX);},_getPage:function _getPage(a,b){return this.loop?a>this.itemLength-(b?2:3)?(a=1,time=0):(b?-1:0)>a?(a=this.itemLength-2,time=0):a+=1:(b||(a>this.itemLength-1?(a=0,time=0):0>a&&(a=this.itemLength-1,time=0)),a=Math.min(Math.max(0,a),this.itemLength-1)),this.pages[a][0];},_gotoItem:function _gotoItem(b,c){this.currentPage=this._getPage(b,!0),this.scrollTo(this.currentPage.x,0,c,this.options.scrollEasing),0===c&&a.trigger(this.wrapper,"scrollend",this);},setTranslate:function setTranslate(a,b){this._super(a,b);var c=this.progressBar;c&&(this.progressBarStyle.webkitTransform=this._getTranslateStr(-a*(this.progressBarWidth/this.wrapperWidth),0));},resetPosition:function resetPosition(a){return a=a||0,this.x>0?this.x=0:this.x<this.maxScrollX&&(this.x=this.maxScrollX),this.currentPage=this._nearestSnap(this.x),this.scrollTo(this.currentPage.x,0,a,this.options.scrollEasing),!0;},gotoItem:function gotoItem(a,b){this._gotoItem(a,"undefined"==typeof b?this.options.scrollTime:b);},nextItem:function nextItem(){this._gotoItem(this.slideNumber+1,this.options.scrollTime);},prevItem:function prevItem(){this._gotoItem(this.slideNumber-1,this.options.scrollTime);},getSlideNumber:function getSlideNumber(){return this.slideNumber||0;},_reInit:function _reInit(){for(var a=this.wrapper.querySelectorAll("."+d),b=0,c=a.length;c>b;b++){if(a[b].parentNode===this.wrapper){this.scroller=a[b];break;}}this.scrollerStyle=this.scroller&&this.scroller.style,this.progressBar&&(this.progressBarWidth=this.progressBar.offsetWidth,this.progressBarStyle=this.progressBar.style);},refresh:function refresh(b){b?(a.extend(this.options,b),this._super(),this._initTimer()):this._super();},destroy:function destroy(){this._initEvent(!0),delete a.data[this.wrapper.getAttribute("data-slider")],this.wrapper.setAttribute("data-slider","");}});a.fn.slider=function(b){var d=null;return this.each(function(){var e=this;if(this.classList.contains(c)||(e=this.querySelector("."+c)),e&&e.querySelector(j)){var f=e.getAttribute("data-slider");f?(d=a.data[f],d&&b&&d.refresh(b)):(f=++a.uuid,a.data[f]=d=new l(e,b),e.setAttribute("data-slider",f));}}),d;},a.ready(function(){a(".mui-slider").slider(),a(".mui-scroll-wrapper.mui-slider-indicator.mui-segmented-control").scroll({scrollY:!1,scrollX:!0,indicators:!1,snap:".mui-control-item"});});}(mui,window),function(a,b){a.os.plus&&a.os.android&&a.plusReady(function(){if(window.__NWin_Enable__!==!1){var c="mui-plus-pullrefresh",d="mui-visibility",e="mui-hidden",f="mui-block",g="mui-pull-caption",h="mui-pull-caption-down",i="mui-pull-caption-refresh",j="mui-pull-caption-nomore",k=a.Class.extend({init:function init(a,b){this.element=a,this.options=b,this.wrapper=this.scroller=a,this._init(),this._initPulldownRefreshEvent();},_init:function _init(){var a=this;window.addEventListener("dragup",a),b.addEventListener("plusscrollbottom",a),a.scrollInterval=window.setInterval(function(){a.isScroll&&!a.loading&&window.pageYOffset+window.innerHeight+10>=b.documentElement.scrollHeight&&(a.isScroll=!1,a.bottomPocket&&a.pullupLoading());},100);},_initPulldownRefreshEvent:function _initPulldownRefreshEvent(){var b=this;b.topPocket&&b.options.webviewId&&a.plusReady(function(){var a=plus.webview.getWebviewById(b.options.webviewId);if(a){b.options.webview=a;var c=b.options.down,d=c.height;a.addEventListener("close",function(){var a=b.options.webviewId&&b.options.webviewId.replace(/\//g,"_");b.element.removeAttribute("data-pullrefresh-plus-"+a);}),a.addEventListener("dragBounce",function(d){switch(b.pulldown?b.pullPocket.classList.add(f):b._initPulldownRefresh(),d.status){case"beforeChangeOffset":b._setCaption(c.contentdown);break;case"afterChangeOffset":b._setCaption(c.contentover);break;case"dragEndAfterChangeOffset":a.evalJS("mui&&mui.options.pullRefresh.down.callback()"),b._setCaption(c.contentrefresh);}},!1),a.setBounce({position:{top:2*d+"px"},changeoffset:{top:d+"px"}});}});},handleEvent:function handleEvent(a){var b=this;b.stopped||(b.isScroll=!1,("dragup"===a.type||"plusscrollbottom"===a.type)&&(b.isScroll=!0,setTimeout(function(){b.isScroll=!1;},1e3)));}}).extend(a.extend({setStopped:function setStopped(a){this.stopped=!!a;var b=plus.webview.currentWebview();if(this.stopped)b.setStyle({bounce:"none"}),b.setBounce({position:{top:"none"}});else{var c=this.options.down.height;b.setStyle({bounce:"vertical"}),b.setBounce({position:{top:2*c+"px"},changeoffset:{top:c+"px"}});}},pulldownLoading:function pulldownLoading(){a.plusReady(function(){plus.webview.currentWebview().setBounce({offset:{top:this.options.down.height+"px"}});}.bind(this));},_pulldownLoading:function _pulldownLoading(){var b=this;a.plusReady(function(){var a=plus.webview.getWebviewById(b.options.webviewId);a.setBounce({offset:{top:b.options.down.height+"px"}});});},endPulldownToRefresh:function endPulldownToRefresh(){var a=plus.webview.currentWebview();a.parent().evalJS("mui&&mui(document.querySelector('.mui-content')).pullRefresh('"+(0,_stringify2.default)({webviewId:a.id})+"')._endPulldownToRefresh()");},_endPulldownToRefresh:function _endPulldownToRefresh(){var a=this;a.topPocket&&a.options.webview&&(a.options.webview.endPullToRefresh(),a.loading=!1,a._setCaption(a.options.down.contentdown,!0),setTimeout(function(){a.loading||a.topPocket.classList.remove(f);},350));},pullupLoading:function pullupLoading(a){var b=this;b.isLoading||(b.isLoading=!0,b.pulldown!==!1?b._initPullupRefresh():this.pullPocket.classList.add(f),setTimeout(function(){b.pullLoading.classList.add(d),b.pullLoading.classList.remove(e),b.pullCaption.innerHTML="",b.pullCaption.className=g+" "+i,b.pullCaption.innerHTML=b.options.up.contentrefresh,a=a||b.options.up.callback,a&&a.call(b);},300));},endPullupToRefresh:function endPullupToRefresh(a){var c=this;c.pullLoading&&(c.pullLoading.classList.remove(d),c.pullLoading.classList.add(e),c.isLoading=!1,a?(c.finished=!0,c.pullCaption.className=g+" "+j,c.pullCaption.innerHTML=c.options.up.contentnomore,b.removeEventListener("plusscrollbottom",c),window.removeEventListener("dragup",c)):(c.pullCaption.className=g+" "+h,c.pullCaption.innerHTML=c.options.up.contentdown));},disablePullupToRefresh:function disablePullupToRefresh(){this._initPullupRefresh(),this.bottomPocket.className="mui-pull-bottom-pocket "+e,window.removeEventListener("dragup",this);},enablePullupToRefresh:function enablePullupToRefresh(){this._initPullupRefresh(),this.bottomPocket.classList.remove(e),this.pullCaption.className=g+" "+h,this.pullCaption.innerHTML=this.options.up.contentdown,b.addEventListener("plusscrollbottom",this),window.addEventListener("dragup",this);},scrollTo:function scrollTo(b,c,d){a.scrollTo(c,d);},scrollToBottom:function scrollToBottom(c){a.scrollTo(b.documentElement.scrollHeight,c);},refresh:function refresh(a){a&&this.finished&&(this.enablePullupToRefresh(),this.finished=!1);}},a.PullRefresh));a.fn.pullRefresh=function(d){var e;0===this.length?(e=b.createElement("div"),e.className="mui-content",b.body.appendChild(e)):e=this[0];var f=d;d=d||{},"string"==typeof d&&(d=a.parseJSON(d)),!d.webviewId&&(d.webviewId=plus.webview.currentWebview().id||plus.webview.currentWebview().getURL());var g=null,h=d.webviewId&&d.webviewId.replace(/\//g,"_"),i=e.getAttribute("data-pullrefresh-plus-"+h);return i||"undefined"!=typeof f?(i?g=a.data[i]:(i=++a.uuid,e.setAttribute("data-pullrefresh-plus-"+h,i),b.body.classList.add(c),a.data[i]=g=new k(e,d)),d.down&&d.down.auto?g._pulldownLoading():d.up&&d.up.auto&&g.pullupLoading(),g):!1;};}});}(mui,document),function(a,b,c,d){var e="mui-off-canvas-left",f="mui-off-canvas-right",g="mui-off-canvas-backdrop",h="mui-off-canvas-wrap",i="mui-slide-in",j="mui-active",k="mui-transitioning",l=".mui-inner-wrap",m=a.Class.extend({init:function init(b,d){this.wrapper=this.element=b,this.scroller=this.wrapper.querySelector(l),this.classList=this.wrapper.classList,this.scroller&&(this.options=a.extend(!0,{dragThresholdX:10,scale:.8,opacity:.1,preventDefaultException:{tagName:/^(INPUT|TEXTAREA|BUTTON|SELECT|VIDEO)$/}},d),c.body.classList.add("mui-fullscreen"),this.refresh(),this.initEvent());},_preventDefaultException:function _preventDefaultException(a,b){for(var c in b){if(b[c].test(a[c]))return!0;}return!1;},refresh:function refresh(a){this.slideIn=this.classList.contains(i),this.scalable=this.classList.contains("mui-scalable")&&!this.slideIn,this.scroller=this.wrapper.querySelector(l),this.offCanvasLefts=this.wrapper.querySelectorAll("."+e),this.offCanvasRights=this.wrapper.querySelectorAll("."+f),a?a.classList.contains(e)?this.offCanvasLeft=a:a.classList.contains(f)&&(this.offCanvasRight=a):(this.offCanvasRight=this.wrapper.querySelector("."+f),this.offCanvasLeft=this.wrapper.querySelector("."+e)),this.offCanvasRightWidth=this.offCanvasLeftWidth=0,this.offCanvasLeftSlideIn=this.offCanvasRightSlideIn=!1,this.offCanvasRight&&(this.offCanvasRightWidth=this.offCanvasRight.offsetWidth,this.offCanvasRightSlideIn=this.slideIn&&this.offCanvasRight.parentNode===this.wrapper),this.offCanvasLeft&&(this.offCanvasLeftWidth=this.offCanvasLeft.offsetWidth,this.offCanvasLeftSlideIn=this.slideIn&&this.offCanvasLeft.parentNode===this.wrapper),this.backdrop=this.scroller.querySelector("."+g),this.options.dragThresholdX=this.options.dragThresholdX||10,this.visible=!1,this.startX=null,this.lastX=null,this.offsetX=null,this.lastTranslateX=null;},handleEvent:function handleEvent(b){switch(b.type){case a.EVENT_START:b.target&&!this._preventDefaultException(b.target,this.options.preventDefaultException)&&b.preventDefault();break;case"webkitTransitionEnd":b.target===this.scroller&&this._dispatchEvent();break;case"drag":var c=b.detail;this.startX?this.lastX=c.center.x:(this.startX=c.center.x,this.lastX=this.startX),!this.isDragging&&Math.abs(this.lastX-this.startX)>this.options.dragThresholdX&&("left"===c.direction||"right"===c.direction)&&(this.slideIn?(this.scroller=this.wrapper.querySelector(l),this.classList.contains(j)?this.offCanvasRight&&this.offCanvasRight.classList.contains(j)?(this.offCanvas=this.offCanvasRight,this.offCanvasWidth=this.offCanvasRightWidth):(this.offCanvas=this.offCanvasLeft,this.offCanvasWidth=this.offCanvasLeftWidth):"left"===c.direction&&this.offCanvasRight?(this.offCanvas=this.offCanvasRight,this.offCanvasWidth=this.offCanvasRightWidth):"right"===c.direction&&this.offCanvasLeft?(this.offCanvas=this.offCanvasLeft,this.offCanvasWidth=this.offCanvasLeftWidth):this.scroller=null):this.classList.contains(j)?"left"===c.direction?(this.offCanvas=this.offCanvasLeft,this.offCanvasWidth=this.offCanvasLeftWidth):(this.offCanvas=this.offCanvasRight,this.offCanvasWidth=this.offCanvasRightWidth):"right"===c.direction?(this.offCanvas=this.offCanvasLeft,this.offCanvasWidth=this.offCanvasLeftWidth):(this.offCanvas=this.offCanvasRight,this.offCanvasWidth=this.offCanvasRightWidth),this.offCanvas&&this.scroller&&(this.startX=this.lastX,this.isDragging=!0,a.gestures.session.lockDirection=!0,a.gestures.session.startDirection=c.direction,this.offCanvas.classList.remove(k),this.scroller.classList.remove(k),this.offsetX=this.getTranslateX(),this._initOffCanvasVisible())),this.isDragging&&(this.updateTranslate(this.offsetX+(this.lastX-this.startX)),c.gesture.preventDefault(),b.stopPropagation());break;case"dragend":if(this.isDragging){var c=b.detail,d=c.direction;this.isDragging=!1,this.offCanvas.classList.add(k),this.scroller.classList.add(k);var e=0,f=this.getTranslateX();if(this.slideIn){if(e=f>=0?this.offCanvasRightWidth&&f/this.offCanvasRightWidth||0:this.offCanvasLeftWidth&&f/this.offCanvasLeftWidth||0,"right"===d&&0>=e&&(e>=-.5||c.swipe)?this.openPercentage(100):"right"===d&&e>0&&(e>=.5||c.swipe)?this.openPercentage(0):"right"===d&&-.5>=e?this.openPercentage(0):"right"===d&&e>0&&.5>=e?this.openPercentage(-100):"left"===d&&e>=0&&(.5>=e||c.swipe)?this.openPercentage(-100):"left"===d&&0>e&&(-.5>=e||c.swipe)?this.openPercentage(0):"left"===d&&e>=.5?this.openPercentage(0):"left"===d&&e>=-.5&&0>e?this.openPercentage(100):this.openPercentage(0),1===e||-1===e||0===e)return void this._dispatchEvent();}else{if(e=f>=0?this.offCanvasLeftWidth&&f/this.offCanvasLeftWidth||0:this.offCanvasRightWidth&&f/this.offCanvasRightWidth||0,0===e)return this.openPercentage(0),void this._dispatchEvent();"right"===d&&e>=0&&(e>=.5||c.swipe)?this.openPercentage(100):"right"===d&&0>e&&(e>-.5||c.swipe)?this.openPercentage(0):"right"===d&&e>0&&.5>e?this.openPercentage(0):"right"===d&&.5>e?this.openPercentage(-100):"left"===d&&0>=e&&(-.5>=e||c.swipe)?this.openPercentage(-100):"left"===d&&e>0&&(.5>=e||c.swipe)?this.openPercentage(0):"left"===d&&0>e&&e>=-.5?this.openPercentage(0):"left"===d&&e>.5?this.openPercentage(100):this.openPercentage(0),(1===e||-1===e)&&this._dispatchEvent();}}}},_dispatchEvent:function _dispatchEvent(){this.classList.contains(j)?a.trigger(this.wrapper,"shown",this):a.trigger(this.wrapper,"hidden",this);},_initOffCanvasVisible:function _initOffCanvasVisible(){this.visible||(this.visible=!0,this.offCanvasLeft&&(this.offCanvasLeft.style.visibility="visible"),this.offCanvasRight&&(this.offCanvasRight.style.visibility="visible"));},initEvent:function initEvent(){var b=this;b.backdrop&&b.backdrop.addEventListener("tap",function(a){b.close(),a.detail.gesture.preventDefault();}),this.classList.contains("mui-draggable")&&(this.wrapper.addEventListener(a.EVENT_START,this),this.wrapper.addEventListener("drag",this),this.wrapper.addEventListener("dragend",this)),this.wrapper.addEventListener("webkitTransitionEnd",this);},openPercentage:function openPercentage(a){var b=a/100;this.slideIn?(this.offCanvasLeft&&a>=0?(b=0===b?-1:0,this.updateTranslate(this.offCanvasLeftWidth*b),this.offCanvasLeft.classList[0!==a?"add":"remove"](j)):this.offCanvasRight&&0>=a&&(b=0===b?1:0,this.updateTranslate(this.offCanvasRightWidth*b),this.offCanvasRight.classList[0!==a?"add":"remove"](j)),this.classList[0!==a?"add":"remove"](j)):(this.offCanvasLeft&&a>=0?(this.updateTranslate(this.offCanvasLeftWidth*b),this.offCanvasLeft.classList[0!==b?"add":"remove"](j)):this.offCanvasRight&&0>=a&&(this.updateTranslate(this.offCanvasRightWidth*b),this.offCanvasRight.classList[0!==b?"add":"remove"](j)),this.classList[0!==b?"add":"remove"](j));},updateTranslate:function updateTranslate(b){if(b!==this.lastTranslateX){if(this.slideIn){if(this.offCanvas.classList.contains(f)){if(0>b)return void this.setTranslateX(0);if(b>this.offCanvasRightWidth)return void this.setTranslateX(this.offCanvasRightWidth);}else{if(b>0)return void this.setTranslateX(0);if(b<-this.offCanvasLeftWidth)return void this.setTranslateX(-this.offCanvasLeftWidth);}this.setTranslateX(b);}else{if(!this.offCanvasLeft&&b>0||!this.offCanvasRight&&0>b)return void this.setTranslateX(0);if(this.leftShowing&&b>this.offCanvasLeftWidth)return void this.setTranslateX(this.offCanvasLeftWidth);if(this.rightShowing&&b<-this.offCanvasRightWidth)return void this.setTranslateX(-this.offCanvasRightWidth);this.setTranslateX(b),b>=0?(this.leftShowing=!0,this.rightShowing=!1,b>0&&(this.offCanvasLeft&&a.each(this.offCanvasLefts,function(a,b){b===this.offCanvasLeft?this.offCanvasLeft.style.zIndex=0:b.style.zIndex=-1;}.bind(this)),this.offCanvasRight&&(this.offCanvasRight.style.zIndex=-1))):(this.rightShowing=!0,this.leftShowing=!1,this.offCanvasRight&&a.each(this.offCanvasRights,function(a,b){b===this.offCanvasRight?b.style.zIndex=0:b.style.zIndex=-1;}.bind(this)),this.offCanvasLeft&&(this.offCanvasLeft.style.zIndex=-1));}this.lastTranslateX=b;}},setTranslateX:a.animationFrame(function(a){if(this.scroller)if(this.scalable&&this.offCanvas.parentNode===this.wrapper){var b=Math.abs(a)/this.offCanvasWidth,c=1-(1-this.options.scale)*b,d=this.options.scale+(1-this.options.scale)*b,f=(1-(1-this.options.opacity)*b,this.options.opacity+(1-this.options.opacity)*b);this.offCanvas.classList.contains(e)?(this.offCanvas.style.webkitTransformOrigin="-100%",this.scroller.style.webkitTransformOrigin="left"):(this.offCanvas.style.webkitTransformOrigin="200%",this.scroller.style.webkitTransformOrigin="right"),this.offCanvas.style.opacity=f,this.offCanvas.style.webkitTransform="translate3d(0,0,0) scale("+d+")",this.scroller.style.webkitTransform="translate3d("+a+"px,0,0) scale("+c+")";}else this.slideIn?this.offCanvas.style.webkitTransform="translate3d("+a+"px,0,0)":this.scroller.style.webkitTransform="translate3d("+a+"px,0,0)";}),getTranslateX:function getTranslateX(){if(this.offCanvas){var b=this.slideIn?this.offCanvas:this.scroller,c=a.parseTranslateMatrix(a.getStyles(b,"webkitTransform"));return c&&c.x||0;}return 0;},isShown:function isShown(a){var b=!1;if(this.slideIn)b="left"===a?this.classList.contains(j)&&this.wrapper.querySelector("."+e+"."+j):"right"===a?this.classList.contains(j)&&this.wrapper.querySelector("."+f+"."+j):this.classList.contains(j)&&(this.wrapper.querySelector("."+e+"."+j)||this.wrapper.querySelector("."+f+"."+j));else{var c=this.getTranslateX();b="right"===a?this.classList.contains(j)&&0>c:"left"===a?this.classList.contains(j)&&c>0:this.classList.contains(j)&&0!==c;}return b;},close:function close(){this._initOffCanvasVisible(),this.offCanvas=this.wrapper.querySelector("."+f+"."+j)||this.wrapper.querySelector("."+e+"."+j),this.offCanvasWidth=this.offCanvas.offsetWidth,this.scroller&&(this.offCanvas.offsetHeight,this.offCanvas.classList.add(k),this.scroller.classList.add(k),this.openPercentage(0));},show:function show(a){return this._initOffCanvasVisible(),this.isShown(a)?!1:(a||(a=this.wrapper.querySelector("."+f)?"right":"left"),"right"===a?(this.offCanvas=this.offCanvasRight,this.offCanvasWidth=this.offCanvasRightWidth):(this.offCanvas=this.offCanvasLeft,this.offCanvasWidth=this.offCanvasLeftWidth),this.scroller&&(this.offCanvas.offsetHeight,this.offCanvas.classList.add(k),this.scroller.classList.add(k),this.openPercentage("left"===a?100:-100)),!0);},toggle:function toggle(a){var b=a;a&&a.classList&&(b=a.classList.contains(e)?"left":"right",this.refresh(a)),this.show(b)||this.close();}}),n=function n(a){if(parentNode=a.parentNode,parentNode){if(parentNode.classList.contains(h))return parentNode;if(parentNode=parentNode.parentNode,parentNode.classList.contains(h))return parentNode;}},o=function o(b,d){if("A"===d.tagName&&d.hash){var e=c.getElementById(d.hash.replace("#",""));if(e){var f=n(e);if(f)return a.targets._container=f,e;}}return!1;};a.registerTarget({name:d,index:60,handle:o,target:!1,isReset:!1,isContinue:!0}),b.addEventListener("tap",function(b){if(a.targets.offcanvas)for(var d=b.target;d&&d!==c;d=d.parentNode){if("A"===d.tagName&&d.hash&&d.hash==="#"+a.targets.offcanvas.id){b.detail&&b.detail.gesture&&b.detail.gesture.preventDefault(),a(a.targets._container).offCanvas().toggle(a.targets.offcanvas),a.targets.offcanvas=a.targets._container=null;break;}}}),a.fn.offCanvas=function(b){var c=[];return this.each(function(){var d=null,e=this;e.classList.contains(h)||(e=n(e));var f=e.getAttribute("data-offCanvas");f?d=a.data[f]:(f=++a.uuid,a.data[f]=d=new m(e,b),e.setAttribute("data-offCanvas",f)),("show"===b||"close"===b||"toggle"===b)&&d.toggle(),c.push(d);}),1===c.length?c[0]:c;},a.ready(function(){a(".mui-off-canvas-wrap").offCanvas();});}(mui,window,document,"offcanvas"),function(a,b){var c="mui-action",d=function d(a,b){var d=b.className||"";return"string"!=typeof d&&(d=""),d&&~d.indexOf(c)?(b.classList.contains("mui-action-back")&&a.preventDefault(),b):!1;};a.registerTarget({name:b,index:50,handle:d,target:!1,isContinue:!0});}(mui,"action"),function(a,b,c,d){var e="mui-modal",f=function f(a,b){if("A"===b.tagName&&b.hash){var d=c.getElementById(b.hash.replace("#",""));if(d&&d.classList.contains(e))return d;}return!1;};a.registerTarget({name:d,index:50,handle:f,target:!1,isReset:!1,isContinue:!0}),b.addEventListener("tap",function(b){a.targets.modal&&(b.detail.gesture.preventDefault(),a.targets.modal.classList.toggle("mui-active"));});}(mui,window,document,"modal"),function(a,b,c,d){var e="mui-popover",f="mui-popover-arrow",g="mui-popover-action",h="mui-backdrop",i="mui-bar-popover",j="mui-bar-backdrop",k="mui-backdrop-action",l="mui-active",m="mui-bottom",n=function n(b,d){if("A"===d.tagName&&d.hash){if(a.targets._popover=c.getElementById(d.hash.replace("#","")),a.targets._popover&&a.targets._popover.classList.contains(e))return d;a.targets._popover=null;}return!1;};a.registerTarget({name:d,index:60,handle:n,target:!1,isReset:!1,isContinue:!0});var o,p=function p(a){},q=function q(b){this.removeEventListener("webkitTransitionEnd",q),this.addEventListener(a.EVENT_MOVE,a.preventDefault),a.trigger(this,"shown",this);},r=function r(b){v(this,"none"),this.removeEventListener("webkitTransitionEnd",r),this.removeEventListener(a.EVENT_MOVE,a.preventDefault),p(!1),a.trigger(this,"hidden",this);},s=function(){var b=c.createElement("div");return b.classList.add(h),b.addEventListener(a.EVENT_MOVE,a.preventDefault),b.addEventListener("tap",function(b){var d=a.targets._popover;d&&(d.addEventListener("webkitTransitionEnd",r),d.classList.remove(l),t(d),c.body.setAttribute("style",""));}),b;}(),t=function t(b){s.setAttribute("style","opacity:0"),a.targets.popover=a.targets._popover=null,o=a.later(function(){!b.classList.contains(l)&&s.parentNode&&s.parentNode===c.body&&c.body.removeChild(s);},350);};b.addEventListener("tap",function(b){if(a.targets.popover){for(var d=!1,e=b.target;e&&e!==c;e=e.parentNode){e===a.targets.popover&&(d=!0);}d&&(b.detail.gesture.preventDefault(),u(a.targets._popover,a.targets.popover));}});var u=function u(a,b,d){if(!("show"===d&&a.classList.contains(l)||"hide"===d&&!a.classList.contains(l))){o&&o.cancel(),a.removeEventListener("webkitTransitionEnd",q),a.removeEventListener("webkitTransitionEnd",r),s.classList.remove(j),s.classList.remove(k);var e=c.querySelector(".mui-popover.mui-active");if(e&&(e.addEventListener("webkitTransitionEnd",r),e.classList.remove(l),a===e))return void t(e);var f=!1;(a.classList.contains(i)||a.classList.contains(g))&&(a.classList.contains(g)?(f=!0,s.classList.add(k)):s.classList.add(j)),v(a,"block"),a.offsetHeight,a.classList.add(l),s.setAttribute("style",""),c.body.appendChild(s),p(!0),w(a,b,f),s.classList.add(l),a.addEventListener("webkitTransitionEnd",q);}},v=function v(a,b,c,d){var e=a.style;"undefined"!=typeof b&&(e.display=b),"undefined"!=typeof c&&(e.top=c+"px"),"undefined"!=typeof d&&(e.left=d+"px");},w=function w(d,e,h){if(d&&e){if(h)return void v(d,"block");var i=b.innerWidth,j=b.innerHeight,k=d.offsetWidth,l=d.offsetHeight,n=e.offsetWidth,o=e.offsetHeight,p=a.offset(e),q=d.querySelector("."+f);q||(q=c.createElement("div"),q.className=f,d.appendChild(q));var r=q&&q.offsetWidth/2||0,s=0,t=0,u=0,w=0,x=d.classList.contains(g)?0:5,y="top";l+r<p.top-b.pageYOffset?s=p.top-l-r:l+r<j-(p.top-b.pageYOffset)-o?(y="bottom",s=p.top+o+r):(y="middle",s=Math.max((j-l)/2+b.pageYOffset,0),t=Math.max((i-k)/2+b.pageXOffset,0)),"top"===y||"bottom"===y?(t=n/2+p.left-k/2,u=t,x>t&&(t=x),t+k>i&&(t=i-k-x),q&&("top"===y?q.classList.add(m):q.classList.remove(m),u-=t,w=k/2-r/2+u,w=Math.max(Math.min(w,k-2*r-6),6),q.setAttribute("style","left:"+w+"px"))):"middle"===y&&q.setAttribute("style","display:none"),v(d,"block",s,t);}};a.createMask=function(b){var d=c.createElement("div");d.classList.add(h),d.addEventListener(a.EVENT_MOVE,a.preventDefault),d.addEventListener("tap",function(){e.close();});var e=[d];return e._show=!1,e.show=function(){return e._show=!0,d.setAttribute("style","opacity:1"),c.body.appendChild(d),e;},e._remove=function(){return e._show&&(e._show=!1,d.setAttribute("style","opacity:0"),a.later(function(){var a=c.body;d.parentNode===a&&a.removeChild(d);},350)),e;},e.close=function(){b?b()!==!1&&e._remove():e._remove();},e;},a.fn.popover=function(){var b=arguments;this.each(function(){a.targets._popover=this,("show"===b[0]||"hide"===b[0]||"toggle"===b[0])&&u(this,b[1],b[0]);});};}(mui,window,document,"popover"),function(a,b,c,d,e){var f="mui-control-item",g="mui-segmented-control",h="mui-segmented-control-vertical",i="mui-control-content",j="mui-bar-tab",k="mui-tab-item",l=function l(a,b){return b.classList&&(b.classList.contains(f)||b.classList.contains(k))?(b.parentNode&&b.parentNode.classList&&b.parentNode.classList.contains(h)||a.preventDefault(),b):!1;};a.registerTarget({name:d,index:80,handle:l,target:!1}),b.addEventListener("tap",function(b){var e=a.targets.tab;if(e){for(var h,l,m,n="mui-active",o="."+n,p=e.parentNode;p&&p!==c;p=p.parentNode){if(p.classList.contains(g)){h=p.querySelector(o+"."+f);break;}p.classList.contains(j)&&(h=p.querySelector(o+"."+k));}h&&h.classList.remove(n);var q=e===h;if(e&&e.classList.add(n),e.hash&&(m=c.getElementById(e.hash.replace("#","")))){if(!m.classList.contains(i))return void e.classList[q?"remove":"add"](n);if(!q){var r=m.parentNode;l=r.querySelectorAll("."+i+o);for(var s=0;s<l.length;s++){var t=l[s];t.parentNode===r&&t.classList.remove(n);}m.classList.add(n);for(var u=[],v=r.querySelectorAll("."+i),s=0;s<v.length;s++){v[s].parentNode===r&&u.push(v[s]);}a.trigger(m,a.eventName("shown",d),{tabNumber:Array.prototype.indexOf.call(u,m)}),b.detail&&b.detail.gesture.preventDefault();}}}});}(mui,window,document,"tab"),function(a,b,c){var d="mui-switch",e="mui-switch-handle",f="mui-active",g="mui-dragging",h="mui-disabled",i="."+e,j=function j(a,b){return b.classList&&b.classList.contains(d)?b:!1;};a.registerTarget({name:c,index:100,handle:j,target:!1});var k=function k(a){this.element=a,this.classList=this.element.classList,this.handle=this.element.querySelector(i),this.init(),this.initEvent();};k.prototype.init=function(){this.toggleWidth=this.element.offsetWidth,this.handleWidth=this.handle.offsetWidth,this.handleX=this.toggleWidth-this.handleWidth-3;},k.prototype.initEvent=function(){this.element.addEventListener(a.EVENT_START,this),this.element.addEventListener("drag",this),this.element.addEventListener("swiperight",this),this.element.addEventListener(a.EVENT_END,this),this.element.addEventListener(a.EVENT_CANCEL,this);},k.prototype.handleEvent=function(b){if(!this.classList.contains(h))switch(b.type){case a.EVENT_START:this.start(b);break;case"drag":this.drag(b);break;case"swiperight":this.swiperight();break;case a.EVENT_END:case a.EVENT_CANCEL:this.end(b);}},k.prototype.start=function(a){this.handle.style.webkitTransitionDuration=this.element.style.webkitTransitionDuration=".2s",this.classList.add(g),(0===this.toggleWidth||0===this.handleWidth)&&this.init();},k.prototype.drag=function(a){var b=a.detail;this.isDragging||("left"===b.direction||"right"===b.direction)&&(this.isDragging=!0,this.lastChanged=void 0,this.initialState=this.classList.contains(f)),this.isDragging&&(this.setTranslateX(b.deltaX),a.stopPropagation(),b.gesture.preventDefault());},k.prototype.swiperight=function(a){this.isDragging&&a.stopPropagation();},k.prototype.end=function(b){this.classList.remove(g),this.isDragging?(this.isDragging=!1,b.stopPropagation(),a.trigger(this.element,"toggle",{isActive:this.classList.contains(f)})):this.toggle();},k.prototype.toggle=function(b){var c=this.classList;b===!1?this.handle.style.webkitTransitionDuration=this.element.style.webkitTransitionDuration="0s":this.handle.style.webkitTransitionDuration=this.element.style.webkitTransitionDuration=".2s",c.contains(f)?(c.remove(f),this.handle.style.webkitTransform="translate(0,0)"):(c.add(f),this.handle.style.webkitTransform="translate("+this.handleX+"px,0)"),a.trigger(this.element,"toggle",{isActive:this.classList.contains(f)});},k.prototype.setTranslateX=a.animationFrame(function(a){if(this.isDragging){var b=!1;(this.initialState&&-a>this.handleX/2||!this.initialState&&a>this.handleX/2)&&(b=!0),this.lastChanged!==b&&(b?(this.handle.style.webkitTransform="translate("+(this.initialState?0:this.handleX)+"px,0)",this.classList[this.initialState?"remove":"add"](f)):(this.handle.style.webkitTransform="translate("+(this.initialState?this.handleX:0)+"px,0)",this.classList[this.initialState?"add":"remove"](f)),this.lastChanged=b);}}),a.fn["switch"]=function(b){var c=[];return this.each(function(){var b=null,d=this.getAttribute("data-switch");d?b=a.data[d]:(d=++a.uuid,a.data[d]=new k(this),this.setAttribute("data-switch",d)),c.push(b);}),c.length>1?c:c[0];},a.ready(function(){a("."+d)["switch"]();});}(mui,window,"toggle"),function(a,b,c){function d(a,b){var c=b?"removeEventListener":"addEventListener";a[c]("drag",F),a[c]("dragend",F),a[c]("swiperight",F),a[c]("swipeleft",F),a[c]("flick",F);}var e,f,g="mui-active",h="mui-selected",i="mui-grid-view",j="mui-table-view-radio",k="mui-table-view-cell",l="mui-collapse-content",m="mui-disabled",n="mui-switch",o="mui-btn",p="mui-slider-handle",q="mui-slider-left",r="mui-slider-right",s="mui-transitioning",t="."+p,u="."+q,v="."+r,w="."+h,x="."+o,y=.8,z=isOpened=openedActions=progress=!1,A=sliderActionLeft=sliderActionRight=buttonsLeft=buttonsRight=sliderDirection=sliderRequestAnimationFrame=!1,B=translateX=lastTranslateX=sliderActionLeftWidth=sliderActionRightWidth=0,C=function C(a){a?f?f.classList.add(g):e&&e.classList.add(g):(B&&B.cancel(),f?f.classList.remove(g):e&&e.classList.remove(g));},D=function D(){if(translateX!==lastTranslateX){if(buttonsRight&&buttonsRight.length>0){progress=translateX/sliderActionRightWidth,translateX<-sliderActionRightWidth&&(translateX=-sliderActionRightWidth-Math.pow(-translateX-sliderActionRightWidth,y));for(var a=0,b=buttonsRight.length;b>a;a++){var c=buttonsRight[a];"undefined"==typeof c._buttonOffset&&(c._buttonOffset=c.offsetLeft),buttonOffset=c._buttonOffset,E(c,translateX-buttonOffset*(1+Math.max(progress,-1)));}}if(buttonsLeft&&buttonsLeft.length>0){progress=translateX/sliderActionLeftWidth,translateX>sliderActionLeftWidth&&(translateX=sliderActionLeftWidth+Math.pow(translateX-sliderActionLeftWidth,y));for(var a=0,b=buttonsLeft.length;b>a;a++){var d=buttonsLeft[a];"undefined"==typeof d._buttonOffset&&(d._buttonOffset=sliderActionLeftWidth-d.offsetLeft-d.offsetWidth),buttonOffset=d._buttonOffset,buttonsLeft.length>1&&(d.style.zIndex=buttonsLeft.length-a),E(d,translateX+buttonOffset*(1-Math.min(progress,1)));}}E(A,translateX),lastTranslateX=translateX;}sliderRequestAnimationFrame=requestAnimationFrame(function(){D();});},E=function E(a,b){a&&(a.style.webkitTransform="translate("+b+"px,0)");};b.addEventListener(a.EVENT_START,function(b){e&&C(!1),e=f=!1,z=isOpened=openedActions=!1;for(var g=b.target,h=!1;g&&g!==c;g=g.parentNode){if(g.classList){var p=g.classList;if(("INPUT"===g.tagName&&"radio"!==g.type&&"checkbox"!==g.type||"BUTTON"===g.tagName||p.contains(n)||p.contains(o)||p.contains(m))&&(h=!0),p.contains(l))break;if(p.contains(k)){e=g;var q=e.parentNode.querySelector(w);if(!e.parentNode.classList.contains(j)&&q&&q!==e)return a.swipeoutClose(q),void(e=h=!1);if(!e.parentNode.classList.contains(i)){var r=e.querySelector("a");r&&r.parentNode===e&&(f=r);}var s=e.querySelector(t);s&&(d(e),b.stopPropagation()),h||(s?(B&&B.cancel(),B=a.later(function(){C(!0);},100)):C(!0));break;}}}}),b.addEventListener(a.EVENT_MOVE,function(a){C(!1);});var F={handleEvent:function handleEvent(a){switch(a.type){case"drag":this.drag(a);break;case"dragend":this.dragend(a);break;case"flick":this.flick(a);break;case"swiperight":this.swiperight(a);break;case"swipeleft":this.swipeleft(a);}},drag:function drag(a){if(e){z||(A=sliderActionLeft=sliderActionRight=buttonsLeft=buttonsRight=sliderDirection=sliderRequestAnimationFrame=!1,A=e.querySelector(t),A&&(sliderActionLeft=e.querySelector(u),sliderActionRight=e.querySelector(v),sliderActionLeft&&(sliderActionLeftWidth=sliderActionLeft.offsetWidth,buttonsLeft=sliderActionLeft.querySelectorAll(x)),sliderActionRight&&(sliderActionRightWidth=sliderActionRight.offsetWidth,buttonsRight=sliderActionRight.querySelectorAll(x)),e.classList.remove(s),isOpened=e.classList.contains(h),isOpened&&(openedActions=e.querySelector(u+w)?"left":"right")));var b=a.detail,c=b.direction,d=b.angle;if("left"===c&&(d>150||-150>d)?(buttonsRight||buttonsLeft&&isOpened)&&(z=!0):"right"===c&&d>-30&&30>d&&(buttonsLeft||buttonsRight&&isOpened)&&(z=!0),z){a.stopPropagation(),a.detail.gesture.preventDefault();var f=a.detail.deltaX;if(isOpened&&("right"===openedActions?f-=sliderActionRightWidth:f+=sliderActionLeftWidth),f>0&&!buttonsLeft||0>f&&!buttonsRight){if(!isOpened)return;f=0;}0>f?sliderDirection="toLeft":f>0?sliderDirection="toRight":sliderDirection||(sliderDirection="toLeft"),sliderRequestAnimationFrame||D(),translateX=f;}}},flick:function flick(a){z&&a.stopPropagation();},swipeleft:function swipeleft(a){z&&a.stopPropagation();},swiperight:function swiperight(a){z&&a.stopPropagation();},dragend:function dragend(b){if(z){b.stopPropagation(),sliderRequestAnimationFrame&&(cancelAnimationFrame(sliderRequestAnimationFrame),sliderRequestAnimationFrame=null);var c=b.detail;z=!1;var d="close",f="toLeft"===sliderDirection?sliderActionRightWidth:sliderActionLeftWidth,g=c.swipe||Math.abs(translateX)>f/2;g&&(isOpened?"left"===c.direction&&"right"===openedActions?d="open":"right"===c.direction&&"left"===openedActions&&(d="open"):d="open"),e.classList.add(s);var i;if("open"===d){var j="toLeft"===sliderDirection?-f:f;if(E(A,j),i="toLeft"===sliderDirection?buttonsRight:buttonsLeft,"undefined"!=typeof i){for(var k=null,l=0;l<i.length;l++){k=i[l],E(k,j);}k.parentNode.classList.add(h),e.classList.add(h),isOpened||a.trigger(e,"toLeft"===sliderDirection?"slideleft":"slideright");}}else E(A,0),sliderActionLeft&&sliderActionLeft.classList.remove(h),sliderActionRight&&sliderActionRight.classList.remove(h),e.classList.remove(h);var m;if(buttonsLeft&&buttonsLeft.length>0&&buttonsLeft!==i)for(var l=0,n=buttonsLeft.length;n>l;l++){var o=buttonsLeft[l];m=o._buttonOffset,"undefined"==typeof m&&(o._buttonOffset=sliderActionLeftWidth-o.offsetLeft-o.offsetWidth),E(o,m);}if(buttonsRight&&buttonsRight.length>0&&buttonsRight!==i)for(var l=0,n=buttonsRight.length;n>l;l++){var p=buttonsRight[l];m=p._buttonOffset,"undefined"==typeof m&&(p._buttonOffset=p.offsetLeft),E(p,-m);}}}};a.swipeoutOpen=function(b,c){if(b){var d=b.classList;if(!d.contains(h)){c||(c=b.querySelector(v)?"right":"left");var e=b.querySelector(a.classSelector(".slider-"+c));if(e){e.classList.add(h),d.add(h),d.remove(s);for(var f,g=e.querySelectorAll(x),i=e.offsetWidth,j="right"===c?-i:i,k=g.length,l=0;k>l;l++){f=g[l],"right"===c?E(f,-f.offsetLeft):E(f,i-f.offsetWidth-f.offsetLeft);}d.add(s);for(var l=0;k>l;l++){E(g[l],j);}E(b.querySelector(t),j);}}}},a.swipeoutClose=function(b){if(b){var c=b.classList;if(c.contains(h)){var d=b.querySelector(v+w)?"right":"left",e=b.querySelector(a.classSelector(".slider-"+d));if(e){e.classList.remove(h),c.remove(h),c.add(s);var f,g=e.querySelectorAll(x),i=e.offsetWidth,j=g.length;E(b.querySelector(t),0);for(var k=0;j>k;k++){f=g[k],"right"===d?E(f,-f.offsetLeft):E(f,i-f.offsetWidth-f.offsetLeft);}}}}},b.addEventListener(a.EVENT_END,function(a){e&&(C(!1),A&&d(e,!0));}),b.addEventListener(a.EVENT_CANCEL,function(a){e&&(C(!1),A&&d(e,!0));});var G=function G(b){var c=b.target&&b.target.type||"";if("radio"!==c&&"checkbox"!==c){var d=e.classList;if(d.contains("mui-radio")){var f=e.querySelector("input[type=radio]");f&&(f.disabled||f.readOnly||(f.checked=!f.checked,a.trigger(f,"change")));}else if(d.contains("mui-checkbox")){var f=e.querySelector("input[type=checkbox]");f&&(f.disabled||f.readOnly||(f.checked=!f.checked,a.trigger(f,"change")));}}};b.addEventListener(a.EVENT_CLICK,function(a){e&&e.classList.contains("mui-collapse")&&a.preventDefault();}),b.addEventListener("doubletap",function(a){e&&G(a);});var H=/^(INPUT|TEXTAREA|BUTTON|SELECT)$/;b.addEventListener("tap",function(b){if(e){var c=!1,d=e.classList,f=e.parentNode;if(f&&f.classList.contains(j)){if(d.contains(h))return;var i=f.querySelector("li"+w);return i&&i.classList.remove(h),d.add(h),void a.trigger(e,"selected",{el:e});}if(d.contains("mui-collapse")&&!e.parentNode.classList.contains("mui-unfold")){if(H.test(b.target.tagName)||b.detail.gesture.preventDefault(),!d.contains(g)){var k=e.parentNode.querySelector(".mui-collapse.mui-active");k&&k.classList.remove(g),c=!0;}d.toggle(g),c&&a.trigger(e,"expand");}else G(b);}});}(mui,window,document),function(a,b){a.alert=function(c,d,e,f){if(a.os.plus){if("undefined"==typeof c)return;"function"==typeof d?(f=d,d=null,e="确定"):"function"==typeof e&&(f=e,e=null),a.plusReady(function(){plus.nativeUI.alert(c,f,d,e);});}else b.alert(c);};}(mui,window),function(a,b){a.confirm=function(c,d,e,f){if(a.os.plus){if("undefined"==typeof c)return;"function"==typeof d?(f=d,d=null,e=null):"function"==typeof e&&(f=e,e=null),a.plusReady(function(){plus.nativeUI.confirm(c,f,d,e);});}else f(b.confirm(c)?{index:0}:{index:1});};}(mui,window),function(a,b){a.prompt=function(c,d,e,f,g){if(a.os.plus){if("undefined"==typeof message)return;"function"==typeof d?(g=d,d=null,e=null,f=null):"function"==typeof e?(g=e,e=null,f=null):"function"==typeof f&&(g=f,f=null),a.plusReady(function(){plus.nativeUI.prompt(c,g,e,d,f);});}else{var h=b.prompt(c);g(h?{index:0,value:h}:{index:1,value:""});}};}(mui,window),function(a,b){var c="mui-active";a.toast=function(b,d){var e={"long":3500,"short":2e3};if(d=a.extend({duration:"short"},d||{}),a.os.plus&&"div"!==d.type)a.plusReady(function(){plus.nativeUI.toast(b,{verticalAlign:"bottom",duration:d.duration});});else{"number"==typeof d.duration?duration=d.duration>0?d.duration:e["short"]:duration=e[d.duration],duration||(duration=e["short"]);var f=document.createElement("div");f.classList.add("mui-toast-container"),f.innerHTML='<div class="mui-toast-message">'+b+"</div>",f.addEventListener("webkitTransitionEnd",function(){f.classList.contains(c)||(f.parentNode.removeChild(f),f=null);}),f.addEventListener("click",function(){f.parentNode.removeChild(f),f=null;}),document.body.appendChild(f),f.offsetHeight,f.classList.add(c),setTimeout(function(){f&&f.classList.remove(c);},duration);}};}(mui,window),function(a,b,c){var d="mui-popup",e="mui-popup-backdrop",f="mui-popup-in",g="mui-popup-out",h="mui-popup-inner",i="mui-popup-title",j="mui-popup-text",k="mui-popup-input",l="mui-popup-buttons",m="mui-popup-button",n="mui-popup-button-bold",e="mui-popup-backdrop",o="mui-active",p=[],q=function(){var b=c.createElement("div");return b.classList.add(e),b.addEventListener(a.EVENT_MOVE,a.preventDefault),b.addEventListener("webkitTransitionEnd",function(){this.classList.contains(o)||b.parentNode&&b.parentNode.removeChild(b);}),b;}(),r=function r(a){return'<div class="'+k+'"><input type="text" autofocus placeholder="'+(a||"")+'"/></div>';},s=function s(a,b,c){return'<div class="'+h+'"><div class="'+i+'">'+b+'</div><div class="'+j+'">'+a.replace(/\r\n/g,"<br/>").replace(/\n/g,"<br/>")+"</div>"+(c||"")+"</div>";},t=function t(a){for(var b=a.length,c=[],d=0;b>d;d++){c.push('<span class="'+m+(d===b-1?" "+n:"")+'">'+a[d]+"</span>");}return'<div class="'+l+'">'+c.join("")+"</div>";},u=function u(b,e){var h=c.createElement("div");h.className=d,h.innerHTML=b;var i=function i(){h.parentNode&&h.parentNode.removeChild(h),h=null;};h.addEventListener(a.EVENT_MOVE,a.preventDefault),h.addEventListener("webkitTransitionEnd",function(a){h&&a.target===h&&h.classList.contains(g)&&i();}),h.style.display="block",c.body.appendChild(h),h.offsetHeight,h.classList.add(f),q.classList.contains(o)||(q.style.display="block",c.body.appendChild(q),q.offsetHeight,q.classList.add(o));var j=a.qsa("."+m,h),l=h.querySelector("."+k+" input"),n={element:h,close:function close(a,b){if(h){var c=e&&e({index:a||0,value:l&&l.value||""});if(c===!1)return;b!==!1?(h.classList.remove(f),h.classList.add(g)):i(),p.pop(),p.length?p[p.length-1].show(b):q.classList.remove(o);}}},r=function r(a){n.close(j.indexOf(a.target));};return a(h).on("tap","."+m,r),p.length&&p[p.length-1].hide(),p.push({close:n.close,show:function show(a){h.style.display="block",h.offsetHeight,h.classList.add(f);},hide:function hide(){h.style.display="none",h.classList.remove(f);}}),n;},v=function v(b,c,d,e,f){return"undefined"!=typeof b?("function"==typeof c?(e=c,f=d,c=null,d=null):"function"==typeof d&&(f=e,e=d,d=null),a.os.plus&&"div"!==f?plus.nativeUI.alert(b,e,c||"提示",d||"确定"):u(s(b,c||"提示")+t([d||"确定"]),e)):void 0;},w=function w(b,c,d,e,f){return"undefined"!=typeof b?("function"==typeof c?(e=c,f=d,c=null,d=null):"function"==typeof d&&(f=e,e=d,d=null),a.os.plus&&"div"!==f?plus.nativeUI.confirm(b,e,c,d||["取消","确认"]):u(s(b,c||"提示")+t(d||["取消","确认"]),e)):void 0;},x=function x(b,c,d,e,f,g){return"undefined"!=typeof b?("function"==typeof c?(f=c,g=d,c=null,d=null,e=null):"function"==typeof d?(f=d,g=e,d=null,e=null):"function"==typeof e&&(g=f,f=e,e=null),a.os.plus&&"div"!==g?plus.nativeUI.prompt(b,f,d||"提示",c,e||["取消","确认"]):u(s(b,d||"提示",r(c))+t(e||["取消","确认"]),f)):void 0;},y=function y(){return p.length?(p[p.length-1].close(),!0):!1;},z=function z(){for(;p.length;){p[p.length-1].close();}};a.closePopup=y,a.closePopups=z,a.alert=v,a.confirm=w,a.prompt=x;}(mui,window,document),function(a,b){var c="mui-progressbar",d="mui-progressbar-in",e="mui-progressbar-out",f="mui-progressbar-infinite",g=".mui-progressbar",h=function h(b){if(b=a(b||"body"),0!==b.length){if(b=b[0],b.classList.contains(c))return b;var d=b.querySelectorAll(g);if(d)for(var e=0,f=d.length;f>e;e++){var h=d[e];if(h.parentNode===b)return h;}}},i=function i(h,_i,j){if("number"==typeof h&&(j=_i,_i=h,h="body"),h=a(h||"body"),0!==h.length){h=h[0];var l;if(h.classList.contains(c))l=h;else{var m=h.querySelectorAll(g+":not(."+e+")");if(m)for(var n=0,o=m.length;o>n;n++){var p=m[n];if(p.parentNode===h){l=p;break;}}l?l.classList.add(d):(l=b.createElement("span"),l.className=c+" "+d+("undefined"!=typeof _i?"":" "+f)+(j?" "+c+"-"+j:""),"undefined"!=typeof _i&&(l.innerHTML="<span></span>"),h.appendChild(l));}return _i&&k(h,_i),l;}},j=function j(a){var b=h(a);if(b){var c=b.classList;c.contains(d)&&!c.contains(e)&&(c.remove(d),c.add(e),b.addEventListener("webkitAnimationEnd",function(){b.parentNode&&b.parentNode.removeChild(b),b=null;}));}},k=function k(a,b,c){"number"==typeof a&&(c=b,b=a,a=!1);var d=h(a);if(d&&!d.classList.contains(f)){b&&(b=Math.min(Math.max(b,0),100)),d.offsetHeight;var e=d.querySelector("span");if(e){var g=e.style;g.webkitTransform="translate3d("+(-100+b)+"%,0,0)","undefined"!=typeof c?g.webkitTransitionDuration=c+"ms":g.webkitTransitionDuration="";}return d;}};a.fn.progressbar=function(a){var b=[];return a=a||{},this.each(function(){var c=this,d=c.mui_plugin_progressbar;d?a&&d.setOptions(a):c.mui_plugin_progressbar=d={options:a,setOptions:function setOptions(a){this.options=a;},show:function show(){return i(c,this.options.progress,this.options.color);},setProgress:function setProgress(a){return k(c,a);},hide:function hide(){return j(c);}},b.push(d);}),1===b.length?b[0]:b;};}(mui,document),function(a,b,c){var d="mui-icon",e="mui-icon-clear",f="mui-icon-speech",g="mui-icon-search",h="mui-icon-eye",i="mui-input-row",j="mui-placeholder",k="mui-tooltip",l="mui-hidden",m="mui-focusin",n="."+e,o="."+f,p="."+h,q="."+j,r="."+k,s=function s(a){for(;a&&a!==c;a=a.parentNode){if(a.classList&&a.classList.contains(i))return a;}return null;},t=function t(a,b){this.element=a,this.options=b||{actions:"clear"},~this.options.actions.indexOf("slider")?(this.sliderActionClass=k+" "+l,this.sliderActionSelector=r):(~this.options.actions.indexOf("clear")&&(this.clearActionClass=d+" "+e+" "+l,this.clearActionSelector=n),~this.options.actions.indexOf("speech")&&(this.speechActionClass=d+" "+f,this.speechActionSelector=o),~this.options.actions.indexOf("search")&&(this.searchActionClass=j,this.searchActionSelector=q),~this.options.actions.indexOf("password")&&(this.passwordActionClass=d+" "+h,this.passwordActionSelector=p)),this.init();};t.prototype.init=function(){this.initAction(),this.initElementEvent();},t.prototype.initAction=function(){var b=this,c=b.element.parentNode;c&&(b.sliderActionClass?b.sliderAction=b.createAction(c,b.sliderActionClass,b.sliderActionSelector):(b.searchActionClass&&(b.searchAction=b.createAction(c,b.searchActionClass,b.searchActionSelector),b.searchAction.addEventListener("tap",function(c){a.focus(b.element),c.stopPropagation();})),b.speechActionClass&&(b.speechAction=b.createAction(c,b.speechActionClass,b.speechActionSelector),b.speechAction.addEventListener("click",a.stopPropagation),b.speechAction.addEventListener("tap",function(a){b.speechActionClick(a);})),b.clearActionClass&&(b.clearAction=b.createAction(c,b.clearActionClass,b.clearActionSelector),b.clearAction.addEventListener("tap",function(a){b.clearActionClick(a);})),b.passwordActionClass&&(b.passwordAction=b.createAction(c,b.passwordActionClass,b.passwordActionSelector),b.passwordAction.addEventListener("tap",function(a){b.passwordActionClick(a);}))));},t.prototype.createAction=function(a,b,e){var f=a.querySelector(e);if(!f){var f=c.createElement("span");f.className=b,b===this.searchActionClass&&(f.innerHTML='<span class="'+d+" "+g+'"></span><span>'+this.element.getAttribute("placeholder")+"</span>",this.element.setAttribute("placeholder",""),this.element.value.trim()&&a.classList.add("mui-active")),a.insertBefore(f,this.element.nextSibling);}return f;},t.prototype.initElementEvent=function(){var b=this.element;if(this.sliderActionClass){var c=this.sliderAction,d=null,e=function e(){c.classList.remove(l);var a=b.offsetLeft,e=b.offsetWidth-28,f=c.offsetWidth,g=Math.abs(b.max-b.min),h=e/g*Math.abs(b.value-b.min);c.style.left=14+a+h-f/2+"px",c.innerText=b.value,d&&clearTimeout(d),d=setTimeout(function(){c.classList.add(l);},1e3);};b.addEventListener("input",e),b.addEventListener("tap",e),b.addEventListener(a.EVENT_MOVE,function(a){a.stopPropagation();});}else{if(this.clearActionClass){var f=this.clearAction;if(!f)return;a.each(["keyup","change","input","focus","cut","paste"],function(a,c){!function(a){b.addEventListener(a,function(){f.classList[b.value.trim()?"remove":"add"](l);});}(c);}),b.addEventListener("blur",function(){f.classList.add(l);});}this.searchActionClass&&(b.addEventListener("focus",function(){b.parentNode.classList.add("mui-active");}),b.addEventListener("blur",function(){b.value.trim()||b.parentNode.classList.remove("mui-active");}));}},t.prototype.setPlaceholder=function(a){if(this.searchActionClass){var b=this.element.parentNode.querySelector(q);b&&(b.getElementsByTagName("span")[1].innerText=a);}else this.element.setAttribute("placeholder",a);},t.prototype.passwordActionClick=function(a){"text"===this.element.type?this.element.type="password":this.element.type="text",this.passwordAction.classList.toggle("mui-active"),a.preventDefault();},t.prototype.clearActionClick=function(b){var c=this;c.element.value="",a.focus(c.element),c.clearAction.classList.add(l),b.preventDefault();},t.prototype.speechActionClick=function(d){if(b.plus){var e=this,f=e.element.value;e.element.value="",c.body.classList.add(m),plus.speech.startRecognize({engine:"iFly"},function(b){e.element.value+=b,a.focus(e.element),plus.speech.stopRecognize(),a.trigger(e.element,"recognized",{value:e.element.value}),f!==e.element.value&&(a.trigger(e.element,"change"),a.trigger(e.element,"input"));},function(a){c.body.classList.remove(m);});}else alert("only for 5+");d.preventDefault();},a.fn.input=function(b){var c=[];return this.each(function(){var b=null,d=[],e=s(this.parentNode);if("range"===this.type&&e.classList.contains("mui-input-range"))d.push("slider");else{var f=this.classList;f.contains("mui-input-clear")&&d.push("clear"),a.os.android&&a.os.stream||!f.contains("mui-input-speech")||d.push("speech"),f.contains("mui-input-password")&&d.push("password"),"search"===this.type&&e.classList.contains("mui-search")&&d.push("search");}var g=this.getAttribute("data-input-"+d[0]);if(g)b=a.data[g];else{g=++a.uuid,b=a.data[g]=new t(this,{actions:d.join(",")});for(var h=0,i=d.length;i>h;h++){this.setAttribute("data-input-"+d[h],g);}}c.push(b);}),1===c.length?c[0]:c;},a.ready(function(){a(".mui-input-row input").input();});}(mui,window,document),function(a,b){var c=/^rgba\((\d{1,3}),\s*(\d{1,3}),\s*(\d{1,3}),\s*(\d{1,3})\)$/,d=function d(a){var b=a.match(c);return b&&5===b.length?[b[1],b[2],b[3],b[4]]:[];},e=function e(b,c){this.element=b,this.options=a.extend({top:0,offset:150,duration:16},c||{}),this._style=this.element.style,this._bgColor=this._style.backgroundColor;var e=d(mui.getStyles(this.element,"backgroundColor"));if(!e.length)throw new Error("元素背景颜色必须为RGBA");this._R=e[0],this._G=e[1],this._B=e[2],this._A=e[3],this._bufferFn=a.buffer(this.handleScroll,this.options.duration,this),this.initEvent();};e.prototype.initEvent=function(){b.addEventListener("scroll",this._bufferFn),b.addEventListener(a.EVENT_MOVE,this._bufferFn);},e.prototype.handleScroll=function(){this._style.backgroundColor="rgba("+this._R+","+this._G+","+this._B+","+(b.scrollY-this.options.top)/this.options.offset+")";},e.prototype.destory=function(){b.removeEventListener("scroll",this._bufferFn),b.removeEventListener(a.EVENT_MOVE,this._bufferFn),this.element.style.backgroundColor=this._bgColor,this.element.mui_plugin_transparent=null;},a.fn.transparent=function(a){a=a||{};var b=[];return this.each(function(){var c=this.mui_plugin_transparent;if(!c){var d=this.getAttribute("data-top"),f=this.getAttribute("data-offset"),g=this.getAttribute("data-duration");null!==d&&"undefined"==typeof a.top&&(a.top=d),null!==f&&"undefined"==typeof a.offset&&(a.offset=f),null!==g&&"undefined"==typeof a.duration&&(a.duration=g),c=this.mui_plugin_transparent=new e(this,a);}b.push(c);}),1===b.length?b[0]:b;},a.ready(function(){a(".mui-bar-transparent").transparent();});}(mui,window),function(a){var b="ontouchstart"in document,c=b?"tap":"click",d="change",e="mui-numbox",f=".mui-btn-numbox-plus,.mui-numbox-btn-plus",g=".mui-btn-numbox-minus,.mui-numbox-btn-minus",h=".mui-input-numbox,.mui-numbox-input",i=a.Numbox=a.Class.extend({init:function init(b,c){var d=this;if(!b)throw"构造 numbox 时缺少容器元素";d.holder=b,c=c||{},c.step=parseInt(c.step||1),d.options=c,d.input=a.qsa(h,d.holder)[0],d.plus=a.qsa(f,d.holder)[0],d.minus=a.qsa(g,d.holder)[0],d.checkValue(),d.initEvent();},initEvent:function initEvent(){var b=this;b.plus.addEventListener(c,function(c){var e=parseInt(b.input.value)+b.options.step;b.input.value=e.toString(),a.trigger(b.input,d,null);}),b.minus.addEventListener(c,function(c){var e=parseInt(b.input.value)-b.options.step;b.input.value=e.toString(),a.trigger(b.input,d,null);}),b.input.addEventListener(d,function(c){b.checkValue();var e=parseInt(b.input.value);a.trigger(b.holder,d,{value:e});});},getValue:function getValue(){var a=this;return parseInt(a.input.value);},checkValue:function checkValue(){var a=this,b=a.input.value;if(null==b||""==b||isNaN(b))a.input.value=a.options.min||0,a.minus.disabled=null!=a.options.min;else{var b=parseInt(b);null!=a.options.max&&!isNaN(a.options.max)&&b>=parseInt(a.options.max)?(b=a.options.max,a.plus.disabled=!0):a.plus.disabled=!1,null!=a.options.min&&!isNaN(a.options.min)&&b<=parseInt(a.options.min)?(b=a.options.min,a.minus.disabled=!0):a.minus.disabled=!1,a.input.value=b;}},setOption:function setOption(a,b){var c=this;c.options[a]=b;},setValue:function setValue(a){this.input.value=a,this.checkValue();}});a.fn.numbox=function(a){return this.each(function(a,b){if(!b.numbox)if(d)b.numbox=new i(b,d);else{var c=b.getAttribute("data-numbox-options"),d=c?JSON.parse(c):{};d.step=b.getAttribute("data-numbox-step")||d.step,d.min=b.getAttribute("data-numbox-min")||d.min,d.max=b.getAttribute("data-numbox-max")||d.max,b.numbox=new i(b,d);}}),this[0]?this[0].numbox:null;},a.ready(function(){a("."+e).numbox();});}(mui),function(a,b,c){var d="mui-disabled",e="reset",f="loading",g={loadingText:"Loading...",loadingIcon:"mui-spinner mui-spinner-white",loadingIconPosition:"left"},h=function h(b,c){this.element=b,this.options=a.extend({},g,c),this.options.loadingText||(this.options.loadingText=g.loadingText),null===this.options.loadingIcon&&(this.options.loadingIcon="mui-spinner","rgb(255, 255, 255)"===a.getStyles(this.element,"color")&&(this.options.loadingIcon+=" mui-spinner-white")),this.isInput="INPUT"===this.element.tagName,this.resetHTML=this.isInput?this.element.value:this.element.innerHTML,this.state="";};h.prototype.loading=function(){this.setState(f);},h.prototype.reset=function(){this.setState(e);},h.prototype.setState=function(a){if(this.state===a)return!1;if(this.state=a,a===e)this.element.disabled=!1,this.element.classList.remove(d),this.setHtml(this.resetHTML);else if(a===f){this.element.disabled=!0,this.element.classList.add(d);var b=this.isInput?this.options.loadingText:"<span>"+this.options.loadingText+"</span>";this.options.loadingIcon&&!this.isInput&&("right"===this.options.loadingIconPosition?b+='&nbsp;<span class="'+this.options.loadingIcon+'"></span>':b='<span class="'+this.options.loadingIcon+'"></span>&nbsp;'+b),this.setHtml(b);}},h.prototype.setHtml=function(a){this.isInput?this.element.value=a:this.element.innerHTML=a;},a.fn.button=function(a){var b=[];return this.each(function(){var c=this.mui_plugin_button;if(!c){var d=this.getAttribute("data-loading-text"),g=this.getAttribute("data-loading-icon"),i=this.getAttribute("data-loading-icon-position");this.mui_plugin_button=c=new h(this,{loadingText:d,loadingIcon:g,loadingIconPosition:i});}(a===f||a===e)&&c.setState(a),b.push(c);}),1===b.length?b[0]:b;};}(mui,window,document);

/***/ },
/* 52 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = { "default": __webpack_require__(53), __esModule: true };

/***/ },
/* 53 */
/***/ function(module, exports, __webpack_require__) {

	var core  = __webpack_require__(54)
	  , $JSON = core.JSON || (core.JSON = {stringify: JSON.stringify});
	module.exports = function stringify(it){ // eslint-disable-line no-unused-vars
	  return $JSON.stringify.apply($JSON, arguments);
	};

/***/ },
/* 54 */
/***/ function(module, exports) {

	var core = module.exports = {version: '2.4.0'};
	if(typeof __e == 'number')__e = core; // eslint-disable-line no-undef

/***/ },
/* 55 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = { "default": __webpack_require__(56), __esModule: true };

/***/ },
/* 56 */
/***/ function(module, exports, __webpack_require__) {

	__webpack_require__(57);
	var $Object = __webpack_require__(54).Object;
	module.exports = function defineProperty(it, key, desc){
	  return $Object.defineProperty(it, key, desc);
	};

/***/ },
/* 57 */
/***/ function(module, exports, __webpack_require__) {

	var $export = __webpack_require__(58);
	// 19.1.2.4 / 15.2.3.6 Object.defineProperty(O, P, Attributes)
	$export($export.S + $export.F * !__webpack_require__(67), 'Object', {defineProperty: __webpack_require__(63).f});

/***/ },
/* 58 */
/***/ function(module, exports, __webpack_require__) {

	var global    = __webpack_require__(59)
	  , core      = __webpack_require__(54)
	  , ctx       = __webpack_require__(60)
	  , hide      = __webpack_require__(62)
	  , PROTOTYPE = 'prototype';
	
	var $export = function(type, name, source){
	  var IS_FORCED = type & $export.F


	    , expProto  = exports[PROTOTYPE]
	    , target    = IS_GLOBAL ? global : IS_STATIC ? global[name] : (global[name] || {})[PROTOTYPE]
	    , key, own, out;
	  if(IS_GLOBAL)source = name;
	  for(key in source){
	    // contains in native
	    own = !IS_FORCED && target && target[key] !== undefined;
	    if(own && key in exports)continue;
	    // export native or passed
	    out = own ? target[key] : source[key];
	    // prevent global pollution for namespaces
	    exports[key] = IS_GLOBAL && typeof target[key] != 'function' ? source[key]
	    // bind timers to global for call from export context
	    : IS_BIND && own ? ctx(out, global)
	    // wrap global constructors for prevent change them in library
	    : IS_WRAP && target[key] == out ? (function(C){
	      var F = function(a, b, c){
	        if(this instanceof C){
	          switch(arguments.length){
	            case 0: return new C;
	            case 1: return new C(a);
	            case 2: return new C(a, b);
	          } return new C(a, b, c);
	        } return C.apply(this, arguments);
	      };
	      F[PROTOTYPE] = C[PROTOTYPE];
	      return F;
	    // make static versions for prototype methods
	    })(out) : IS_PROTO && typeof out == 'function' ? ctx(Function.call, out) : out;
	    // export proto methods to core.%CONSTRUCTOR%.methods.%NAME%
	    if(IS_PROTO){
	      (exports.virtual || (exports.virtual = {}))[key] = out;
	      // export proto methods to core.%CONSTRUCTOR%.prototype.%NAME%
	      if(type & $export.R && expProto && !expProto[key])hide(expProto, key, out);
	    }
	  }
	};
	// type bitmap
	$export.F = 1;   // forced
	$export.G = 2;   // global
	$export.S = 4;   // static
	$export.P = 8;   // proto
	$export.B = 16;  // bind
	$export.W = 32;  // wrap
	$export.U = 64;  // safe
	$export.R = 128; // real proto method for `library` 
	module.exports = $export;

/***/ },
/* 59 */
/***/ function(module, exports) {

	// https://github.com/zloirock/core-js/issues/86#issuecomment-115759028
	var global = module.exports = typeof window != 'undefined' && window.Math == Math
	  ? window : typeof self != 'undefined' && self.Math == Math ? self : Function('return this')();
	if(typeof __g == 'number')__g = global; // eslint-disable-line no-undef

/***/ },
/* 60 */
/***/ function(module, exports, __webpack_require__) {

	// optional / simple context binding
	var aFunction = __webpack_require__(61);
	module.exports = function(fn, that, length){
	  aFunction(fn);
	  if(that === undefined)return fn;
	  switch(length){
	    case 1: return function(a){
	      return fn.call(that, a);
	    };
	    case 2: return function(a, b){
	      return fn.call(that, a, b);
	    };
	    case 3: return function(a, b, c){
	      return fn.call(that, a, b, c);
	    };
	  }
	  return function(/* ...args */){
	    return fn.apply(that, arguments);
	  };
	};

/***/ },
/* 61 */
/***/ function(module, exports) {

	module.exports = function(it){
	  if(typeof it != 'function')throw TypeError(it + ' is not a function!');
	  return it;
	};

/***/ },
/* 62 */
/***/ function(module, exports, __webpack_require__) {

	var dP         = __webpack_require__(63)
	  , createDesc = __webpack_require__(71);
	module.exports = __webpack_require__(67) ? function(object, key, value){
	  return dP.f(object, key, createDesc(1, value));
	} : function(object, key, value){
	  object[key] = value;
	  return object;
	};

/***/ },
/* 63 */
/***/ function(module, exports, __webpack_require__) {

	var anObject       = __webpack_require__(64)


	  } catch(e){ /* empty */ }
	  if('get' in Attributes || 'set' in Attributes)throw TypeError('Accessors not supported!');
	  if('value' in Attributes)O[P] = Attributes.value;
	  return O;
	};

/***/ },
/* 64 */
/***/ function(module, exports, __webpack_require__) {

	var isObject = __webpack_require__(65);
	module.exports = function(it){
	  if(!isObject(it))throw TypeError(it + ' is not an object!');
	  return it;
	};

/***/ },
/* 65 */
/***/ function(module, exports) {

	module.exports = function(it){
	  return typeof it === 'object' ? it !== null : typeof it === 'function';
	};

/***/ },
/* 66 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = !__webpack_require__(67) && !__webpack_require__(68)(function(){
	  return Object.defineProperty(__webpack_require__(69)('div'), 'a', {get: function(){ return 7; }}).a != 7;
	});

/***/ },
/* 67 */
/***/ function(module, exports, __webpack_require__) {

	// Thank's IE8 for his funny defineProperty
	module.exports = !__webpack_require__(68)(function(){
	  return Object.defineProperty({}, 'a', {get: function(){ return 7; }}).a != 7;
	});

/***/ },
/* 68 */
/***/ function(module, exports) {

	module.exports = function(exec){
	  try {
	    return !!exec();
	  } catch(e){
	    return true;
	  }
	};

/***/ },
/* 69 */
/***/ function(module, exports, __webpack_require__) {

	var isObject = __webpack_require__(65)
	  , document = __webpack_require__(59).document
	  // in old IE typeof document.createElement is 'object'
	  , is = isObject(document) && isObject(document.createElement);
	module.exports = function(it){
	  return is ? document.createElement(it) : {};
	};

/***/ },
/* 70 */
/***/ function(module, exports, __webpack_require__) {

	// 7.1.1 ToPrimitive(input [, PreferredType])
	var isObject = __webpack_require__(65);
	// instead of the ES6 spec version, we didn't implement @@toPrimitive case
	// and the second argument - flag - preferred type is a string
	module.exports = function(it, S){
	  if(!isObject(it))return it;
	  var fn, val;
	  if(S && typeof (fn = it.toString) == 'function' && !isObject(val = fn.call(it)))return val;
	  if(typeof (fn = it.valueOf) == 'function' && !isObject(val = fn.call(it)))return val;
	  if(!S && typeof (fn = it.toString) == 'function' && !isObject(val = fn.call(it)))return val;
	  throw TypeError("Can't convert object to primitive value");
	};

/***/ },
/* 71 */
/***/ function(module, exports) {

	module.exports = function(bitmap, value){
	  return {
	    enumerable  : !(bitmap & 1),
	    configurable: !(bitmap & 2),
	    writable    : !(bitmap & 4),
	    value       : value
	  };
	};

/***/ },
/* 72 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = { "default": __webpack_require__(73), __esModule: true };

/***/ },
/* 73 */
/***/ function(module, exports, __webpack_require__) {

	__webpack_require__(74);
	module.exports = __webpack_require__(54).Object.getPrototypeOf;

/***/ },
/* 74 */
/***/ function(module, exports, __webpack_require__) {

	// 19.1.2.9 Object.getPrototypeOf(O)
	var toObject        = __webpack_require__(75)
	  , $getPrototypeOf = __webpack_require__(77);
	
	__webpack_require__(82)('getPrototypeOf', function(){
	  return function getPrototypeOf(it){
	    return $getPrototypeOf(toObject(it));
	  };
	});

/***/ },
/* 75 */
/***/ function(module, exports, __webpack_require__) {

	// 7.1.13 ToObject(argument)
	var defined = __webpack_require__(76);
	module.exports = function(it){
	  return Object(defined(it));
	};

/***/ },
/* 76 */
/***/ function(module, exports) {

	// 7.2.1 RequireObjectCoercible(argument)
	module.exports = function(it){
	  if(it == undefined)throw TypeError("Can't call method on  " + it);
	  return it;
	};

/***/ },
/* 77 */
/***/ function(module, exports, __webpack_require__) {

	// 19.1.2.9 / 15.2.3.2 Object.getPrototypeOf(O)
	var has         = __webpack_require__(78)
	  , toObject    = __webpack_require__(75)
	  , IE_PROTO    = __webpack_require__(79)('IE_PROTO')
	  , ObjectProto = Object.prototype;
	
	module.exports = Object.getPrototypeOf || function(O){
	  O = toObject(O);
	  if(has(O, IE_PROTO))return O[IE_PROTO];
	  if(typeof O.constructor == 'function' && O instanceof O.constructor){
	    return O.constructor.prototype;
	  } return O instanceof Object ? ObjectProto : null;
	};

/***/ },
/* 78 */
/***/ function(module, exports) {

	var hasOwnProperty = {}.hasOwnProperty;
	module.exports = function(it, key){
	  return hasOwnProperty.call(it, key);
	};

/***/ },
/* 79 */
/***/ function(module, exports, __webpack_require__) {

	var shared = __webpack_require__(80)('keys')
	  , uid    = __webpack_require__(81);
	module.exports = function(key){
	  return shared[key] || (shared[key] = uid(key));
	};

/***/ },
/* 80 */
/***/ function(module, exports, __webpack_require__) {

	var global = __webpack_require__(59)
	  , SHARED = '__core-js_shared__'
	  , store  = global[SHARED] || (global[SHARED] = {});
	module.exports = function(key){
	  return store[key] || (store[key] = {});
	};

/***/ },
/* 81 */
/***/ function(module, exports) {

	var id = 0
	  , px = Math.random();
	module.exports = function(key){
	  return 'Symbol('.concat(key === undefined ? '' : key, ')_', (++id + px).toString(36));
	};

/***/ },
/* 82 */
/***/ function(module, exports, __webpack_require__) {

	// most Object methods by ES6 should accept primitives
	var $export = __webpack_require__(58)
	  , core    = __webpack_require__(54)
	  , fails   = __webpack_require__(68);
	module.exports = function(KEY, exec){
	  var fn  = (core.Object || {})[KEY] || Object[KEY]
	    , exp = {};
	  exp[KEY] = exec(fn);
	  $export($export.S + $export.F * fails(function(){ fn(1); }), 'Object', exp);
	};

/***/ },
/* 83 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = { "default": __webpack_require__(84), __esModule: true };

/***/ },
/* 84 */
/***/ function(module, exports, __webpack_require__) {

	__webpack_require__(85);
	module.exports = __webpack_require__(54).Object.setPrototypeOf;

/***/ },
/* 85 */
/***/ function(module, exports, __webpack_require__) {

	// 19.1.3.19 Object.setPrototypeOf(O, proto)
	var $export = __webpack_require__(58);
	$export($export.S, 'Object', {setPrototypeOf: __webpack_require__(86).set});

/***/ },
/* 86 */
/***/ function(module, exports, __webpack_require__) {

	// Works with __proto__ only. Old v8 can't work with null proto objects.
	/* eslint-disable no-proto */
	var isObject = __webpack_require__(65)
	  , anObject = __webpack_require__(64);
	var check = function(O, proto){
	  anObject(O);
	  if(!isObject(proto) && proto !== null)throw TypeError(proto + ": can't set as prototype!");
	};
	module.exports = {
	  set: Object.setPrototypeOf || ('__proto__' in {} ? // eslint-disable-line
	    function(test, buggy, set){
	      try {
	        set = __webpack_require__(60)(Function.call, __webpack_require__(87).f(Object.prototype, '__proto__').set, 2);
	        set(test, []);
	        buggy = !(test instanceof Array);
	      } catch(e){ buggy = true; }
	      return function setPrototypeOf(O, proto){
	        check(O, proto);
	        if(buggy)O.__proto__ = proto;
	        else set(O, proto);
	        return O;
	      };
	    }({}, false) : undefined),
	  check: check
	};

/***/ },
/* 87 */
/***/ function(module, exports, __webpack_require__) {


	  O = toIObject(O);
	  P = toPrimitive(P, true);
	  if(IE8_DOM_DEFINE)try {
	    return gOPD(O, P);
	  } catch(e){ /* empty */ }
	  if(has(O, P))return createDesc(!pIE.f.call(O, P), O[P]);
	};

/***/ },
/* 88 */
/***/ function(module, exports) {

	exports.f = {}.propertyIsEnumerable;

/***/ },
/* 89 */
/***/ function(module, exports, __webpack_require__) {

	// to indexed object, toObject with fallback for non-array-like ES3 strings
	var IObject = __webpack_require__(90)
	  , defined = __webpack_require__(76);
	module.exports = function(it){
	  return IObject(defined(it));
	};

/***/ },
/* 90 */
/***/ function(module, exports, __webpack_require__) {

	// fallback for non-array-like ES3 and non-enumerable old V8 strings
	var cof = __webpack_require__(91);
	module.exports = Object('z').propertyIsEnumerable(0) ? Object : function(it){
	  return cof(it) == 'String' ? it.split('') : Object(it);
	};

/***/ },
/* 91 */
/***/ function(module, exports) {

	var toString = {}.toString;
	
	module.exports = function(it){
	  return toString.call(it).slice(8, -1);
	};

/***/ },
/* 92 */
/***/ function(module, exports, __webpack_require__) {

	"use strict";
	
	exports.__esModule = true;
	
	var _iterator = __webpack_require__(93);
	
	var _iterator2 = _interopRequireDefault(_iterator);
	
	var _symbol = __webpack_require__(119);
	
	var _symbol2 = _interopRequireDefault(_symbol);
	
	var _typeof = typeof _symbol2.default === "function" && typeof _iterator2.default === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof _symbol2.default === "function" && obj.constructor === _symbol2.default ? "symbol" : typeof obj; };
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	exports.default = typeof _symbol2.default === "function" && _typeof(_iterator2.default) === "symbol" ? function (obj) {
	  return typeof obj === "undefined" ? "undefined" : _typeof(obj);
	} : function (obj) {
	  return obj && typeof _symbol2.default === "function" && obj.constructor === _symbol2.default ? "symbol" : typeof obj === "undefined" ? "undefined" : _typeof(obj);
	};

/***/ },
/* 93 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = { "default": __webpack_require__(94), __esModule: true };

/***/ },
/* 94 */
/***/ function(module, exports, __webpack_require__) {

	__webpack_require__(95);
	__webpack_require__(114);
	module.exports = __webpack_require__(118).f('iterator');

/***/ },
/* 95 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	var $at  = __webpack_require__(96)(true);
	
	// 21.1.3.27 String.prototype[@@iterator]()
	__webpack_require__(98)(String, 'String', function(iterated){
	  this._t = String(iterated); // target
	  this._i = 0;                // next index
	// 21.1.5.2.1 %StringIteratorPrototype%.next()
	}, function(){
	  var O     = this._t
	    , index = this._i
	    , point;
	  if(index >= O.length)return {value: undefined, done: true};
	  point = $at(O, index);
	  this._i += point.length;
	  return {value: point, done: false};
	});

/***/ },
/* 96 */
/***/ function(module, exports, __webpack_require__) {

	var toInteger = __webpack_require__(97)
	  , defined   = __webpack_require__(76);
	// true  -> String#at
	// false -> String#codePointAt
	module.exports = function(TO_STRING){
	  return function(that, pos){
	    var s = String(defined(that))
	      , i = toInteger(pos)
	      , l = s.length
	      , a, b;
	    if(i < 0 || i >= l)return TO_STRING ? '' : undefined;
	    a = s.charCodeAt(i);
	    return a < 0xd800 || a > 0xdbff || i + 1 === l || (b = s.charCodeAt(i + 1)) < 0xdc00 || b > 0xdfff
	      ? TO_STRING ? s.charAt(i) : a
	      : TO_STRING ? s.slice(i, i + 2) : (a - 0xd800 << 10) + (b - 0xdc00) + 0x10000;
	  };
	};

/***/ },
/* 97 */
/***/ function(module, exports) {

	// 7.1.4 ToInteger
	var ceil  = Math.ceil


	  , BUGGY          = !([].keys && 'next' in [].keys()) // Safari has buggy iterators w/o `next`
	  , FF_ITERATOR    = '@@iterator'
	  , KEYS           = 'keys'
	  , VALUES         = 'values';
	
	var returnThis = function(){ return this; };
	
	module.exports = function(Base, NAME, Constructor, next, DEFAULT, IS_SET, FORCED){
	  $iterCreate(Constructor, NAME, next);
	  var getMethod = function(kind){
	    if(!BUGGY && kind in proto)return proto[kind];
	    switch(kind){
	      case KEYS: return function keys(){ return new Constructor(this, kind); };
	      case VALUES: return function values(){ return new Constructor(this, kind); };
	    } return function entries(){ return new Constructor(this, kind); };
	  };
	  var TAG        = NAME + ' Iterator'
	    , DEF_VALUES = DEFAULT == VALUES
	    , VALUES_BUG = false
	    , proto      = Base.prototype
	    , $native    = proto[ITERATOR] || proto[FF_ITERATOR] || DEFAULT && proto[DEFAULT]
	    , $default   = $native || getMethod(DEFAULT)
	    , $entries   = DEFAULT ? !DEF_VALUES ? $default : getMethod('entries') : undefined
	    , $anyNative = NAME == 'Array' ? proto.entries || $native : $native
	    , methods, key, IteratorPrototype;
	  // Fix native
	  if($anyNative){
	    IteratorPrototype = getPrototypeOf($anyNative.call(new Base));
	    if(IteratorPrototype !== Object.prototype){
	      // Set @@toStringTag to native iterators
	      setToStringTag(IteratorPrototype, TAG, true);
	      // fix for some old engines
	      if(!LIBRARY && !has(IteratorPrototype, ITERATOR))hide(IteratorPrototype, ITERATOR, returnThis);
	    }
	  }
	  // fix Array#{values, @@iterator}.name in V8 / FF
	  if(DEF_VALUES && $native && $native.name !== VALUES){
	    VALUES_BUG = true;
	    $default = function values(){ return $native.call(this); };
	  }
	  // Define iterator
	  if((!LIBRARY || FORCED) && (BUGGY || VALUES_BUG || !proto[ITERATOR])){
	    hide(proto, ITERATOR, $default);
	  }
	  // Plug for library
	  Iterators[NAME] = $default;
	  Iterators[TAG]  = returnThis;
	  if(DEFAULT){
	    methods = {
	      values:  DEF_VALUES ? $default : getMethod(VALUES),
	      keys:    IS_SET     ? $default : getMethod(KEYS),
	      entries: $entries
	    };
	    if(FORCED)for(key in methods){
	      if(!(key in proto))redefine(proto, key, methods[key]);
	    } else $export($export.P + $export.F * (BUGGY || VALUES_BUG), NAME, methods);
	  }
	  return methods;
	};

/***/ },
/* 99 */
/***/ function(module, exports) {

	module.exports = true;

/***/ },
/* 100 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__(62);

/***/ },
/* 101 */
/***/ function(module, exports) {

	module.exports = {};

/***/ },
/* 102 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	var create         = __webpack_require__(103)
	  , descriptor     = __webpack_require__(71)
	  , setToStringTag = __webpack_require__(112)
	  , IteratorPrototype = {};
	
	// 25.1.2.1.1 %IteratorPrototype%[@@iterator]()
	__webpack_require__(62)(IteratorPrototype, __webpack_require__(113)('iterator'), function(){ return this; });
	
	module.exports = function(Constructor, NAME, next){
	  Constructor.prototype = create(IteratorPrototype, {next: descriptor(1, next)});
	  setToStringTag(Constructor, NAME + ' Iterator');
	};

/***/ },
/* 103 */
/***/ function(module, exports, __webpack_require__) {

	// 19.1.2.2 / 15.2.3.5 Object.create(O [, Properties])
	var anObject    = __webpack_require__(64)
	  , dPs         = __webpack_require__(104)
	  , enumBugKeys = __webpack_require__(110)
	  , IE_PROTO    = __webpack_require__(79)('IE_PROTO')
	  , Empty       = function(){ /* empty */ }
	  , PROTOTYPE   = 'prototype';
	
	// Create object with fake `null` prototype: use iframe Object with cleared prototype
	var createDict = function(){
	  // Thrash, waste and sodomy: IE GC bug
	  var iframe = __webpack_require__(69)('iframe')
	    , i      = enumBugKeys.length
	    , lt     = '<'
	    , gt     = '>'
	    , iframeDocument;
	  iframe.style.display = 'none';
	  __webpack_require__(111).appendChild(iframe);
	  iframe.src = 'javascript:'; // eslint-disable-line no-script-url
	  // createDict = iframe.contentWindow.Object;
	  // html.removeChild(iframe);
	  iframeDocument = iframe.contentWindow.document;
	  iframeDocument.open();
	  iframeDocument.write(lt + 'script' + gt + 'document.F=Object' + lt + '/script' + gt);
	  iframeDocument.close();
	  createDict = iframeDocument.F;
	  while(i--)delete createDict[PROTOTYPE][enumBugKeys[i]];
	  return createDict();
	};
	
	module.exports = Object.create || function create(O, Properties){
	  var result;
	  if(O !== null){
	    Empty[PROTOTYPE] = anObject(O);
	    result = new Empty;
	    Empty[PROTOTYPE] = null;
	    // add "__proto__" for Object.getPrototypeOf polyfill
	    result[IE_PROTO] = O;
	  } else result = createDict();
	  return Properties === undefined ? result : dPs(result, Properties);
	};


/***/ },
/* 104 */
/***/ function(module, exports, __webpack_require__) {

	var dP       = __webpack_require__(63)
	  , anObject = __webpack_require__(64)
	  , getKeys  = __webpack_require__(105);
	
	module.exports = __webpack_require__(67) ? Object.defineProperties : function defineProperties(O, Properties){
	  anObject(O);
	  var keys   = getKeys(Properties)
	    , length = keys.length
	    , i = 0
	    , P;
	  while(length > i)dP.f(O, P = keys[i++], Properties[P]);
	  return O;
	};

/***/ },
/* 105 */
/***/ function(module, exports, __webpack_require__) {

	// 19.1.2.14 / 15.2.3.14 Object.keys(O)
	var $keys       = __webpack_require__(106)
	  , enumBugKeys = __webpack_require__(110);
	
	module.exports = Object.keys || function keys(O){
	  return $keys(O, enumBugKeys);
	};

/***/ },
/* 106 */
/***/ function(module, exports, __webpack_require__) {

	var has          = __webpack_require__(78)
	  , toIObject    = __webpack_require__(89)
	  , arrayIndexOf = __webpack_require__(107)(false)
	  , IE_PROTO     = __webpack_require__(79)('IE_PROTO');
	
	module.exports = function(object, names){
	  var O      = toIObject(object)
	    , i      = 0
	    , result = []
	    , key;
	  for(key in O)if(key != IE_PROTO)has(O, key) && result.push(key);
	  // Don't enum bug & hidden keys
	  while(names.length > i)if(has(O, key = names[i++])){
	    ~arrayIndexOf(result, key) || result.push(key);
	  }
	  return result;
	};

/***/ },
/* 107 */
/***/ function(module, exports, __webpack_require__) {

	// false -> Array#indexOf
	// true  -> Array#includes
	var toIObject = __webpack_require__(89)
	  , toLength  = __webpack_require__(108)
	  , toIndex   = __webpack_require__(109);
	module.exports = function(IS_INCLUDES){
	  return function($this, el, fromIndex){
	    var O      = toIObject($this)
	      , length = toLength(O.length)
	      , index  = toIndex(fromIndex, length)
	      , value;
	    // Array#includes uses SameValueZero equality algorithm
	    if(IS_INCLUDES && el != el)while(length > index){
	      value = O[index++];
	      if(value != value)return true;
	    // Array#toIndex ignores holes, Array#includes - not
	    } else for(;length > index; index++)if(IS_INCLUDES || index in O){
	      if(O[index] === el)return IS_INCLUDES || index || 0;
	    } return !IS_INCLUDES && -1;
	  };
	};

/***/ },
/* 108 */
/***/ function(module, exports, __webpack_require__) {

	// 7.1.15 ToLength
	var toInteger = __webpack_require__(97)
	  , min       = Math.min;
	module.exports = function(it){
	  return it > 0 ? min(toInteger(it), 0x1fffffffffffff) : 0; // pow(2, 53) - 1 == 9007199254740991
	};

/***/ },
/* 109 */
/***/ function(module, exports, __webpack_require__) {

	var toInteger = __webpack_require__(97)
	  , max       = Math.max
	  , min       = Math.min;
	module.exports = function(index, length){
	  index = toInteger(index);
	  return index < 0 ? max(index + length, 0) : min(index, length);
	};

/***/ },
/* 110 */
/***/ function(module, exports) {

	// IE 8- don't enum bug keys
	module.exports = (
	  'constructor,hasOwnProperty,isPrototypeOf,propertyIsEnumerable,toLocaleString,toString,valueOf'
	).split(',');

/***/ },
/* 111 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__(59).document && document.documentElement;

/***/ },
/* 112 */
/***/ function(module, exports, __webpack_require__) {

	var def = __webpack_require__(63).f
	  , has = __webpack_require__(78)
	  , TAG = __webpack_require__(113)('toStringTag');
	
	module.exports = function(it, tag, stat){
	  if(it && !has(it = stat ? it : it.prototype, TAG))def(it, TAG, {configurable: true, value: tag});
	};

/***/ },
/* 113 */
/***/ function(module, exports, __webpack_require__) {

	var store      = __webpack_require__(80)('wks')
	  , uid        = __webpack_require__(81)
	  , Symbol     = __webpack_require__(59).Symbol
	  , USE_SYMBOL = typeof Symbol == 'function';
	
	var $exports = module.exports = function(name){
	  return store[name] || (store[name] =
	    USE_SYMBOL && Symbol[name] || (USE_SYMBOL ? Symbol : uid)('Symbol.' + name));
	};
	
	$exports.store = store;

/***/ },
/* 114 */
/***/ function(module, exports, __webpack_require__) {

	__webpack_require__(115);
	var global        = __webpack_require__(59)
	  , hide          = __webpack_require__(62)
	  , Iterators     = __webpack_require__(101)
	  , TO_STRING_TAG = __webpack_require__(113)('toStringTag');
	
	for(var collections = ['NodeList', 'DOMTokenList', 'MediaList', 'StyleSheetList', 'CSSRuleList'], i = 0; i < 5; i++){
	  var NAME       = collections[i]
	    , Collection = global[NAME]
	    , proto      = Collection && Collection.prototype;
	  if(proto && !proto[TO_STRING_TAG])hide(proto, TO_STRING_TAG, NAME);
	  Iterators[NAME] = Iterators.Array;
	}

/***/ },
/* 115 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	var addToUnscopables = __webpack_require__(116)
	  , step             = __webpack_require__(117)
	  , Iterators        = __webpack_require__(101)
	  , toIObject        = __webpack_require__(89);
	
	// 22.1.3.4 Array.prototype.entries()
	// 22.1.3.13 Array.prototype.keys()
	// 22.1.3.29 Array.prototype.values()
	// 22.1.3.30 Array.prototype[@@iterator]()
	module.exports = __webpack_require__(98)(Array, 'Array', function(iterated, kind){
	  this._t = toIObject(iterated); // target
	  this._i = 0;                   // next index
	  this._k = kind;                // kind
	// 22.1.5.2.1 %ArrayIteratorPrototype%.next()
	}, function(){
	  var O     = this._t
	    , kind  = this._k
	    , index = this._i++;
	  if(!O || index >= O.length){
	    this._t = undefined;
	    return step(1);
	  }
	  if(kind == 'keys'  )return step(0, index);
	  if(kind == 'values')return step(0, O[index]);
	  return step(0, [index, O[index]]);
	}, 'values');
	
	// argumentsList[@@iterator] is %ArrayProto_values% (9.4.4.6, 9.4.4.7)
	Iterators.Arguments = Iterators.Array;
	
	addToUnscopables('keys');
	addToUnscopables('values');
	addToUnscopables('entries');

/***/ },
/* 116 */
/***/ function(module, exports) {

	module.exports = function(){ /* empty */ };

/***/ },
/* 117 */
/***/ function(module, exports) {

	module.exports = function(done, value){
	  return {value: value, done: !!done};
	};

/***/ },
/* 118 */
/***/ function(module, exports, __webpack_require__) {

	exports.f = __webpack_require__(113);

/***/ },
/* 119 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = { "default": __webpack_require__(120), __esModule: true };

/***/ },
/* 120 */
/***/ function(module, exports, __webpack_require__) {

	__webpack_require__(121);
	__webpack_require__(130);
	__webpack_require__(131);
	__webpack_require__(132);
	module.exports = __webpack_require__(54).Symbol;

/***/ },
/* 121 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	// ECMAScript 6 symbols shim
	var global         = __webpack_require__(59)


	  , PROTOTYPE      = 'prototype'
	  , HIDDEN         = wks('_hidden')
	  , TO_PRIMITIVE   = wks('toPrimitive')
	  , isEnum         = {}.propertyIsEnumerable
	  , SymbolRegistry = shared('symbol-registry')
	  , AllSymbols     = shared('symbols')
	  , OPSymbols      = shared('op-symbols')
	  , ObjectProto    = Object[PROTOTYPE]
	  , USE_NATIVE     = typeof $Symbol == 'function'
	  , QObject        = global.QObject;
	// Don't use setters in Qt Script, https://github.com/zloirock/core-js/issues/173
	var setter = !QObject || !QObject[PROTOTYPE] || !QObject[PROTOTYPE].findChild;
	
	// fallback for old Android, https://code.google.com/p/v8/issues/detail?id=687
	var setSymbolDesc = DESCRIPTORS && $fails(function(){
	  return _create(dP({}, 'a', {
	    get: function(){ return dP(this, 'a', {value: 7}).a; }
	  })).a != 7;
	}) ? function(it, key, D){
	  var protoDesc = gOPD(ObjectProto, key);
	  if(protoDesc)delete ObjectProto[key];
	  dP(it, key, D);
	  if(protoDesc && it !== ObjectProto)dP(ObjectProto, key, protoDesc);
	} : dP;
	
	var wrap = function(tag){
	  var sym = AllSymbols[tag] = _create($Symbol[PROTOTYPE]);
	  sym._k = tag;
	  return sym;
	};
	
	var isSymbol = USE_NATIVE && typeof $Symbol.iterator == 'symbol' ? function(it){
	  return typeof it == 'symbol';
	} : function(it){
	  return it instanceof $Symbol;
	};
	
	var $defineProperty = function defineProperty(it, key, D){
	  if(it === ObjectProto)$defineProperty(OPSymbols, key, D);
	  anObject(it);
	  key = toPrimitive(key, true);
	  anObject(D);
	  if(has(AllSymbols, key)){
	    if(!D.enumerable){
	      if(!has(it, HIDDEN))dP(it, HIDDEN, createDesc(1, {}));
	      it[HIDDEN][key] = true;
	    } else {
	      if(has(it, HIDDEN) && it[HIDDEN][key])it[HIDDEN][key] = false;
	      D = _create(D, {enumerable: createDesc(0, false)});
	    } return setSymbolDesc(it, key, D);
	  } return dP(it, key, D);
	};
	var $defineProperties = function defineProperties(it, P){
	  anObject(it);
	  var keys = enumKeys(P = toIObject(P))
	    , i    = 0
	    , l = keys.length
	    , key;
	  while(l > i)$defineProperty(it, key = keys[i++], P[key]);
	  return it;
	};
	var $create = function create(it, P){
	  return P === undefined ? _create(it) : $defineProperties(_create(it), P);
	};
	var $propertyIsEnumerable = function propertyIsEnumerable(key){
	  var E = isEnum.call(this, key = toPrimitive(key, true));
	  if(this === ObjectProto && has(AllSymbols, key) && !has(OPSymbols, key))return false;
	  return E || !has(this, key) || !has(AllSymbols, key) || has(this, HIDDEN) && this[HIDDEN][key] ? E : true;
	};
	var $getOwnPropertyDescriptor = function getOwnPropertyDescriptor(it, key){
	  it  = toIObject(it);
	  key = toPrimitive(key, true);
	  if(it === ObjectProto && has(AllSymbols, key) && !has(OPSymbols, key))return;
	  var D = gOPD(it, key);
	  if(D && has(AllSymbols, key) && !(has(it, HIDDEN) && it[HIDDEN][key]))D.enumerable = true;
	  return D;
	};
	var $getOwnPropertyNames = function getOwnPropertyNames(it){
	  var names  = gOPN(toIObject(it))
	    , result = []
	    , i      = 0
	    , key;
	  while(names.length > i){
	    if(!has(AllSymbols, key = names[i++]) && key != HIDDEN && key != META)result.push(key);
	  } return result;
	};
	var $getOwnPropertySymbols = function getOwnPropertySymbols(it){
	  var IS_OP  = it === ObjectProto
	    , names  = gOPN(IS_OP ? OPSymbols : toIObject(it))
	    , result = []
	    , i      = 0
	    , key;
	  while(names.length > i){
	    if(has(AllSymbols, key = names[i++]) && (IS_OP ? has(ObjectProto, key) : true))result.push(AllSymbols[key]);
	  } return result;
	};
	
	// 19.4.1.1 Symbol([description])
	if(!USE_NATIVE){
	  $Symbol = function Symbol(){
	    if(this instanceof $Symbol)throw TypeError('Symbol is not a constructor!');
	    var tag = uid(arguments.length > 0 ? arguments[0] : undefined);
	    var $set = function(value){
	      if(this === ObjectProto)$set.call(OPSymbols, value);
	      if(has(this, HIDDEN) && has(this[HIDDEN], tag))this[HIDDEN][tag] = false;
	      setSymbolDesc(this, tag, createDesc(1, value));
	    };
	    if(DESCRIPTORS && setter)setSymbolDesc(ObjectProto, tag, {configurable: true, set: $set});
	    return wrap(tag);
	  };
	  redefine($Symbol[PROTOTYPE], 'toString', function toString(){
	    return this._k;
	  });
	
	  $GOPD.f = $getOwnPropertyDescriptor;
	  $DP.f   = $defineProperty;
	  __webpack_require__(129).f = gOPNExt.f = $getOwnPropertyNames;
	  __webpack_require__(88).f  = $propertyIsEnumerable;
	  __webpack_require__(126).f = $getOwnPropertySymbols;
	
	  if(DESCRIPTORS && !__webpack_require__(99)){
	    redefine(ObjectProto, 'propertyIsEnumerable', $propertyIsEnumerable, true);
	  }
	
	  wksExt.f = function(name){
	    return wrap(wks(name));
	  }
	}
	
	$export($export.G + $export.W + $export.F * !USE_NATIVE, {Symbol: $Symbol});
	
	for(var symbols = (
	  // 19.4.2.2, 19.4.2.3, 19.4.2.4, 19.4.2.6, 19.4.2.8, 19.4.2.9, 19.4.2.10, 19.4.2.11, 19.4.2.12, 19.4.2.13, 19.4.2.14
	  'hasInstance,isConcatSpreadable,iterator,match,replace,search,species,split,toPrimitive,toStringTag,unscopables'
	).split(','), i = 0; symbols.length > i; )wks(symbols[i++]);
	
	for(var symbols = $keys(wks.store), i = 0; symbols.length > i; )wksDefine(symbols[i++]);
	
	$export($export.S + $export.F * !USE_NATIVE, 'Symbol', {
	  // 19.4.2.1 Symbol.for(key)
	  'for': function(key){
	    return has(SymbolRegistry, key += '')
	      ? SymbolRegistry[key]
	      : SymbolRegistry[key] = $Symbol(key);
	  },
	  // 19.4.2.5 Symbol.keyFor(sym)
	  keyFor: function keyFor(key){
	    if(isSymbol(key))return keyOf(SymbolRegistry, key);
	    throw TypeError(key + ' is not a symbol!');
	  },
	  useSetter: function(){ setter = true; },
	  useSimple: function(){ setter = false; }
	});
	
	$export($export.S + $export.F * !USE_NATIVE, 'Object', {
	  // 19.1.2.2 Object.create(O [, Properties])
	  create: $create,
	  // 19.1.2.4 Object.defineProperty(O, P, Attributes)
	  defineProperty: $defineProperty,
	  // 19.1.2.3 Object.defineProperties(O, Properties)
	  defineProperties: $defineProperties,
	  // 19.1.2.6 Object.getOwnPropertyDescriptor(O, P)
	  getOwnPropertyDescriptor: $getOwnPropertyDescriptor,
	  // 19.1.2.7 Object.getOwnPropertyNames(O)
	  getOwnPropertyNames: $getOwnPropertyNames,
	  // 19.1.2.8 Object.getOwnPropertySymbols(O)
	  getOwnPropertySymbols: $getOwnPropertySymbols
	});
	
	// 24.3.2 JSON.stringify(value [, replacer [, space]])
	$JSON && $export($export.S + $export.F * (!USE_NATIVE || $fails(function(){
	  var S = $Symbol();
	  // MS Edge converts symbol values to JSON as {}
	  // WebKit converts symbol values to JSON as null
	  // V8 throws on boxed symbols
	  return _stringify([S]) != '[null]' || _stringify({a: S}) != '{}' || _stringify(Object(S)) != '{}';
	})), 'JSON', {
	  stringify: function stringify(it){
	    if(it === undefined || isSymbol(it))return; // IE8 returns string on undefined
	    var args = [it]
	      , i    = 1
	      , replacer, $replacer;
	    while(arguments.length > i)args.push(arguments[i++]);
	    replacer = args[1];
	    if(typeof replacer == 'function')$replacer = replacer;
	    if($replacer || !isArray(replacer))replacer = function(key, value){
	      if($replacer)value = $replacer.call(this, key, value);
	      if(!isSymbol(value))return value;
	    };
	    args[1] = replacer;
	    return _stringify.apply($JSON, args);
	  }
	});
	
	// 19.4.3.4 Symbol.prototype[@@toPrimitive](hint)
	$Symbol[PROTOTYPE][TO_PRIMITIVE] || __webpack_require__(62)($Symbol[PROTOTYPE], TO_PRIMITIVE, $Symbol[PROTOTYPE].valueOf);
	// 19.4.3.5 Symbol.prototype[@@toStringTag]
	setToStringTag($Symbol, 'Symbol');
	// 20.2.1.9 Math[@@toStringTag]
	setToStringTag(Math, 'Math', true);
	// 24.3.3 JSON[@@toStringTag]
	setToStringTag(global.JSON, 'JSON', true);

/***/ },
/* 122 */
/***/ function(module, exports, __webpack_require__) {

	var META     = __webpack_require__(81)('meta')
	  , isObject = __webpack_require__(65)
	  , has      = __webpack_require__(78)
	  , setDesc  = __webpack_require__(63).f
	  , id       = 0;
	var isExtensible = Object.isExtensible || function(){
	  return true;
	};
	var FREEZE = !__webpack_require__(68)(function(){
	  return isExtensible(Object.preventExtensions({}));
	});
	var setMeta = function(it){
	  setDesc(it, META, {value: {
	    i: 'O' + ++id, // object ID
	    w: {}          // weak collections IDs
	  }});
	};
	var fastKey = function(it, create){
	  // return primitive with prefix
	  if(!isObject(it))return typeof it == 'symbol' ? it : (typeof it == 'string' ? 'S' : 'P') + it;
	  if(!has(it, META)){
	    // can't set metadata to uncaught frozen object
	    if(!isExtensible(it))return 'F';
	    // not necessary to add metadata
	    if(!create)return 'E';
	    // add missing metadata
	    setMeta(it);
	  // return object ID
	  } return it[META].i;
	};
	var getWeak = function(it, create){
	  if(!has(it, META)){
	    // can't set metadata to uncaught frozen object
	    if(!isExtensible(it))return true;
	    // not necessary to add metadata
	    if(!create)return false;
	    // add missing metadata
	    setMeta(it);
	  // return hash weak collections IDs
	  } return it[META].w;
	};
	// add metadata on freeze-family methods calling
	var onFreeze = function(it){
	  if(FREEZE && meta.NEED && isExtensible(it) && !has(it, META))setMeta(it);
	  return it;
	};
	var meta = module.exports = {
	  KEY:      META,
	  NEED:     false,
	  fastKey:  fastKey,
	  getWeak:  getWeak,
	  onFreeze: onFreeze
	};

/***/ },
/* 123 */
/***/ function(module, exports, __webpack_require__) {


	  if(name.charAt(0) != '_' && !(name in $Symbol))defineProperty($Symbol, name, {value: wksExt.f(name)});
	};

/***/ },
/* 124 */
/***/ function(module, exports, __webpack_require__) {

	var getKeys   = __webpack_require__(105)
	  , toIObject = __webpack_require__(89);
	module.exports = function(object, el){
	  var O      = toIObject(object)
	    , keys   = getKeys(O)
	    , length = keys.length
	    , index  = 0
	    , key;
	  while(length > index)if(O[key = keys[index++]] === el)return key;
	};

/***/ },
/* 125 */
/***/ function(module, exports, __webpack_require__) {

	// all enumerable object keys, includes symbols
	var getKeys = __webpack_require__(105)
	  , gOPS    = __webpack_require__(126)
	  , pIE     = __webpack_require__(88);
	module.exports = function(it){
	  var result     = getKeys(it)
	    , getSymbols = gOPS.f;
	  if(getSymbols){
	    var symbols = getSymbols(it)
	      , isEnum  = pIE.f
	      , i       = 0
	      , key;
	    while(symbols.length > i)if(isEnum.call(it, key = symbols[i++]))result.push(key);
	  } return result;
	};

/***/ },
/* 126 */
/***/ function(module, exports) {

	exports.f = Object.getOwnPropertySymbols;

/***/ },
/* 127 */
/***/ function(module, exports, __webpack_require__) {

	// 7.2.2 IsArray(argument)
	var cof = __webpack_require__(91);
	module.exports = Array.isArray || function isArray(arg){
	  return cof(arg) == 'Array';
	};

/***/ },
/* 128 */
/***/ function(module, exports, __webpack_require__) {

	// fallback for IE11 buggy Object.getOwnPropertyNames with iframe and window
	var toIObject = __webpack_require__(89)
	  , gOPN      = __webpack_require__(129).f
	  , toString  = {}.toString;
	
	var windowNames = typeof window == 'object' && window && Object.getOwnPropertyNames
	  ? Object.getOwnPropertyNames(window) : [];
	
	var getWindowNames = function(it){
	  try {
	    return gOPN(it);
	  } catch(e){
	    return windowNames.slice();
	  }
	};
	
	module.exports.f = function getOwnPropertyNames(it){
	  return windowNames && toString.call(it) == '[object Window]' ? getWindowNames(it) : gOPN(toIObject(it));
	};


/***/ },
/* 129 */
/***/ function(module, exports, __webpack_require__) {

	// 19.1.2.7 / 15.2.3.4 Object.getOwnPropertyNames(O)
	var $keys      = __webpack_require__(106)
	  , hiddenKeys = __webpack_require__(110).concat('length', 'prototype');
	
	exports.f = Object.getOwnPropertyNames || function getOwnPropertyNames(O){
	  return $keys(O, hiddenKeys);
	};

/***/ },
/* 130 */
/***/ function(module, exports) {



/***/ },
/* 131 */
/***/ function(module, exports, __webpack_require__) {

	__webpack_require__(123)('asyncIterator');

/***/ },
/* 132 */
/***/ function(module, exports, __webpack_require__) {

	__webpack_require__(123)('observable');

/***/ },
/* 133 */
/***/ function(module, exports) {

	/* WEBPACK VAR INJECTION */(function(__webpack_amd_options__) {module.exports = __webpack_amd_options__;
	
	/* WEBPACK VAR INJECTION */}.call(exports, {}))

/***/ },
/* 134 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	var _muiMin = __webpack_require__(51);
	
	var _muiMin2 = _interopRequireDefault(_muiMin);
	
	__webpack_require__(50);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	window.mui = _muiMin2.default; /**
	                                * 弹出选择列表插件
	                                * 此组件依赖 listpcker ，请在页面中先引入 mui.picker.css + mui.picker.js
	                                * varstion 1.0.1
	                                * by Houfeng
	                                * Houfeng@DCloud.io
	                                */
	
	(function ($, document) {
	
		//创建 DOM
		$.dom = function (str) {
			if (typeof str !== 'string') {
				if (str instanceof Array || str[0] && str.length) {
					return [].slice.call(str);
				} else {
					return [str];
				}
			}
			if (!$.__create_dom_div__) {
				$.__create_dom_div__ = document.createElement('div');
			}
			$.__create_dom_div__.innerHTML = str;
			return [].slice.call($.__create_dom_div__.childNodes);
		};
	
		var panelBuffer = '<div class="mui-poppicker">\
			<div class="mui-poppicker-header">\
				<button class="mui-btn mui-poppicker-btn-cancel">取消</button>\
				<button class="mui-btn mui-btn-blue mui-poppicker-btn-ok">确定</button>\
				<div class="mui-poppicker-clear"></div>\
			</div>\
			<div class="mui-poppicker-body">\
			</div>\
		</div>';
	
		var pickerBuffer = '<div class="mui-picker">\
			<div class="mui-picker-inner">\
				<div class="mui-pciker-rule mui-pciker-rule-ft"></div>\
				<ul class="mui-pciker-list">\
				</ul>\
				<div class="mui-pciker-rule mui-pciker-rule-bg"></div>\
			</div>\
		</div>';
	
		//定义弹出选择器类
		var PopPicker = $.PopPicker = $.Class.extend({
			//构造函数
			init: function init(options) {
				var self = this;
				self.options = options || {};
				self.options.buttons = self.options.buttons || ['取消', '确定'];
				self.panel = $.dom(panelBuffer)[0];
				document.body.appendChild(self.panel);
				self.ok = self.panel.querySelector('.mui-poppicker-btn-ok');
				self.cancel = self.panel.querySelector('.mui-poppicker-btn-cancel');
				self.body = self.panel.querySelector('.mui-poppicker-body');
				self.mask = $.createMask();
				self.cancel.innerText = self.options.buttons[0];
				self.ok.innerText = self.options.buttons[1];
				self.cancel.addEventListener('tap', function (event) {
					self.hide();
				}, false);
				self.ok.addEventListener('tap', function (event) {
					if (self.callback) {
						var rs = self.callback(self.getSelectedItems());
						if (rs !== false) {
							self.hide();
						}
					}
				}, false);
				self.mask[0].addEventListener('tap', function () {
					self.hide();
				}, false);
				self._createPicker();
				//防止滚动穿透
				self.panel.addEventListener($.EVENT_START, function (event) {
					event.preventDefault();
				}, false);
				self.panel.addEventListener($.EVENT_MOVE, function (event) {
					event.preventDefault();
				}, false);
			},
			_createPicker: function _createPicker() {
				var self = this;
				var layer = self.options.layer || 1;
				var width = 100 / layer + '%';
				self.pickers = [];
				for (var i = 1; i <= layer; i++) {
					var pickerElement = $.dom(pickerBuffer)[0];
					pickerElement.style.width = width;
					self.body.appendChild(pickerElement);
					var picker = $(pickerElement).picker();
					self.pickers.push(picker);
					pickerElement.addEventListener('change', function (event) {
						var nextPickerElement = this.nextSibling;
						if (nextPickerElement && nextPickerElement.picker) {
							var eventData = event.detail || {};
							var preItem = eventData.item || {};
							nextPickerElement.picker.setItems(preItem.children);
						}
					}, false);
				}
			},
			//填充数据
			setData: function setData(data) {
				var self = this;
				data = data || [];
				self.pickers[0].setItems(data);
			},
			//获取选中的项（数组）
			getSelectedItems: function getSelectedItems() {
				var self = this;
				var items = [];
				for (var i in self.pickers) {
					var picker = self.pickers[i];
					items.push(picker.getSelectedItem() || {});
				}
				return items;
			},
			//显示
			show: function show(callback) {
				var self = this;
				self.callback = callback;
				self.mask.show();
				document.body.classList.add($.className('poppicker-active-for-page'));
				self.panel.classList.add($.className('active'));
				//处理物理返回键
				self.__back = $.back;
				$.back = function () {
					self.hide();
				};
			},
			//隐藏
			hide: function hide() {
				var self = this;
				if (self.disposed) return;
				self.panel.classList.remove($.className('active'));
				self.mask.close();
				document.body.classList.remove($.className('poppicker-active-for-page'));
				//处理物理返回键
				$.back = self.__back;
			},
			dispose: function dispose() {
				var self = this;
				self.hide();
				setTimeout(function () {
					self.panel.parentNode.removeChild(self.panel);
					for (var name in self) {
						self[name] = null;
						delete self[name];
					};
					self.disposed = true;
				}, 300);
			}
		});
	})(_muiMin2.default, document);

/***/ },
/* 135 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page tech\" style=\"padding-top: 0px; background: #f9f9f9;\">\n    <header id=\"header\" class=\"mui-bar mui-bar-nav\">\n        <h1 class=\"mui-title\">个人资料</h1>\n        <a class=\"mui-action-back mui-icon mui-icon-left-nav mui-pull-left\"></a>\n    </header>\n    <div class=\"mui-scroll-wrapper\" style=\"top: 44px;\">\n    <div class=\"mui-scroll\">\n    <div id=\"sliderSegmentedControl\" class=\"mui-slider-indicator mui-segmented-control mui-segmented-control-inverted\">\n        <a class=\"winslow-control-item \" @click=\"local('tech-my')\">工单</a>\n        <a class=\"winslow-control-item \" @click=\"local('tech-comment')\">评价</a>\n        <a class=\"winslow-control-item mui-active\">资料</a>\n    </div>\n\n    <div id=\"slider\" class=\"mui-slider\" >\n        <a class=\"edit-bg\" v-link=\"{ path: '/consumer-images/'+$route.params.token }\"><i class=\"iconfont\">&#xe670;</i> 编辑</a>\n        <div class=\"mui-slider-group mui-slider-loop\">\n            <!-- 额外增加的一个节点(循环轮播：第一个节点是最后一张轮播) -->\n            <div class=\"mui-slider-item mui-slider-item-duplicate\">\n                <a>\n                    <img :src=\" $store.state.URL + massagistInfo.massagist_image[massagistInfo.massagist_image.length - 1].img_url \">\n                </a>\n            </div>\n\n            <div class=\"mui-slider-item\" v-for=\"item in massagistInfo.massagist_image\">\n                <a>\n                    <img :src=\" $store.state.URL + item.img_url \">\n                </a>\n            </div>\n            <!-- 额外增加的一个节点(循环轮播：最后一个节点是第一张轮播) -->\n            <div class=\"mui-slider-item mui-slider-item-duplicate\">\n                <a>\n                    <img :src=\" $store.state.URL + massagistInfo.massagist_image[0].img_url \">\n                </a>\n            </div>\n\n        </div>\n        <div class=\"mui-slider-indicator\">\n            <div class=\"mui-indicator {{$index == 0 ? 'mui-active' : ''}}\" v-for=\"item in massagistInfo.massagist_image\"></div>\n        </div>\n    </div>\n\n    <ul class=\"mui-table-view user-info fade-in-left\" id=\"head\">\n        <li class=\"mui-table-view-cell\">\n            <a class=\"mui-navigate-right\" style=\"line-height: 50px;\" v-link=\"{path: '/tech-my-head/' + $route.params.token}\">\n                头像\n                <img :src=\"massagistInfo.massagist_photo\" class=\"head\">\n            </a>\n        </li>\n        <li class=\"mui-table-view-cell\" @click=\"updateName\">\n            昵称\n            <span class=\"mui-pull-right\">{{massagistInfo.massagist_name}}</span>\n        </li>\n        <li class=\"mui-table-view-cell\">\n            工号\n            <span class=\"mui-pull-right\">{{massagistInfo.massagist_no}}</span>\n        </li>\n        <li class=\"mui-table-view-cell\" @click=\"showDate\">\n            出生日期\n            <span class=\"mui-pull-right\">{{massagistInfo.massagist_birth || '未设置' | format 'yyyy-MM-dd'}}</span>\n        </li>\n        <li class=\"mui-table-view-cell\" @click=\"address\">\n            籍贯\n            <span class=\"mui-pull-right\">{{massagistInfo.c_name}}</span>\n        </li>\n        <li class=\"mui-table-view-cell\" @click=\"sex\">\n            性别\n            <span class=\"mui-pull-right\">{{massagistInfo.massagist_sex | sex}}</span>\n        </li>\n    </ul>\n\n    <ul class=\"mui-table-view user-info fade-in-left\">\n        <li class=\"mui-table-view-cell\">\n            <a class=\"mui-navigate-right\">\n                我的赏金\n                <span class=\"mui-pull-right navigate\">￥0</span>\n            </a>\n        </li>\n    </ul>\n\n    <p class=\"explain\">\n        说明：<br>\n        · 请使用本人真实照片，虚假、色情、违禁照片会直接封号！<br>\n        · 使用真实照片，可以增加点单量，并获得消费者打赏和系统奖励（功能近期上线）！<br>\n        · 您的照片只有VIP才能浏览，其他人无法看到，敬请放心！\n    </p>\n\n    </div></div>\n\n    <nav class=\"mui-bar mui-bar-tab\">\n        <a class=\"winslow-tab-item \" @click=\"local('tech')\">\n            <span class=\"iconfont\">&#xe67d;</span>\n            <span class=\"mui-tab-label\">首页</span>\n        </a>\n        <a class=\"winslow-tab-item mui-active\">\n            <span class=\"iconfont\">&#xe61e;</span>\n            <span class=\"mui-tab-label\">我的</span>\n        </a>\n    </nav>\n    \n</div>\n";

/***/ },
/* 136 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(137)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/tech-my-head.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(143)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-d2b34376/tech-my-head.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 137 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	
	var _assign = __webpack_require__(138);
	
	var _assign2 = _interopRequireDefault(_assign);
	
	var _exif = __webpack_require__(142);
	
	var _exif2 = _interopRequireDefault(_exif);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	exports.default = {
	    name: 'techMyHead',
	    data: function data() {
	        return {
	            showCrop: false,
	            cropImg: '',
	            massagistInfo: {
	                massagist_image: []
	            },
	            changeId: 0
	        };
	    },
	    ready: function ready() {
	        var _this = this;
	
	        document.title = '我的头像';
	        mui('.mui-scroll-wrapper').scroll({ bounce: false });
	        this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	        this.$http.post(API.getMassagistInfo, {}).then(function (data) {
	            if (data.data.errorInfo) return;
	            _this.$store.state.hideTip();
	            _this.massagistInfo = data.data.object;
	            if (_this.massagistInfo.massagist_photo.indexOf('http') < 0) {
	                _this.massagistInfo.massagist_photo = _this.$store.state.URL + _this.massagistInfo.massagist_photo;
	            }
	        });
	    },
	    methods: {
	        local: function local(name) {
	            this.$router.go('/' + name + '/' + this.$route.params.token);
	        },
	        file: function file() {
	            var u = navigator.userAgent;
	            var isAndroid = u.indexOf('Android') > -1 || u.indexOf('Adr') > -1;
	            var isiOS = !!u.match(/\(i[^;]+;( U;)? CPU.+Mac OS X/);
	
	            if (isAndroid) mui('#fileCrop')[0].click();else if (isiOS) {
	                mui('#fileCrop')[0].click();
	                mui('#fileCrop')[0].click();
	            }
	        },
	        change: function change(e) {
	            this.$store.state.showTip({ type: 'loading', content: '努力加载图像中...' });
	            this.showCrop = true;
	            var orientation = null;
	            var FILE = e.target.files[0];
	
	            _exif2.default.getData(FILE, function () {
	                var obj = (0, _assign2.default)({}, _exif2.default.getAllTags(this));
	                orientation = obj.Orientation;
	            });
	
	            var reader = new FileReader();
	            var self = this;
	
	            reader.onload = function (e) {
	                self.$store.state.__getImgData(e.target.result, orientation, function (data) {
	                    var pic = new Image();
	                    pic.src = data;
	                    pic.onload = function () {
	                        self.cropImg = data;
	                        setTimeout(function () {
	                            $(".cropper").cropper({ aspectRatio: 1 / 1 });
	                            self.$store.state.hideTip();
	                        }, 100);
	                    };
	                });
	            };
	            reader.readAsDataURL(FILE);
	        },
	        saveCrop: function saveCrop() {
	            var _this2 = this;
	
	            this.$store.state.showTip({ type: 'loading', content: '上传中...' });
	            setTimeout(function () {
	                var dataURL = $(".cropper").cropper("getDataURL");
	                var picURL = dataURL.replace(/^.*?,/, '');
	                _this2.$http.post(API.uploadImg, {
	                    type: 'head',
	                    base64: picURL
	                }).then(function (data) {
	                    if (data.data.errorInfo) return;
	                    _this2.$store.state.hideTip();
	                    _this2.$store.state.locationReload();
	                });
	            }, 100);
	        }
	    }
	};
	
	// </script>
	/* generated by vue-loader */
	// <template>
	// <div class="page consumer" style="background-color: #444;">
	//     <header id="header" class="mui-bar mui-bar-nav bar-images">
	//         <h1 class="mui-title">我的头像</h1>
	//         <a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a>
	//     </header>
	//     <div class="mui-scroll-wrapper" style="bottom: 0px;">
	//     <div class="mui-scroll">
	//     <ul class="images-container">
	//         <img :src="massagistInfo.massagist_photo" class="head">
	//         <a class="change-head-img" @click="file">更换头像</a>
	//         <input id="fileCrop" style="opacity: 0;" type="file" accept="image/*" v-on:change="change">
	//     </ul>
	//     </div></div>
	//
	//     <div class="crop-container" v-show="showCrop">
	//         <div class="wrap">
	//             <img class="cropper" :src="cropImg">
	//         </div>
	//         <div class="btn">
	//             <div><button type="button" class="mui-btn mui-btn-block" @click="$store.state.locationReload()">取消</button></div>
	//             <div><button type="button" class="mui-btn mui-btn-primary mui-btn-block" @click="saveCrop">保存</button></div>
	//         </div>
	//     </div>
	// </div>
	// </template>
	//
	// <script>

/***/ },
/* 138 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = { "default": __webpack_require__(139), __esModule: true };

/***/ },
/* 139 */
/***/ function(module, exports, __webpack_require__) {

	__webpack_require__(140);
	module.exports = __webpack_require__(54).Object.assign;

/***/ },
/* 140 */
/***/ function(module, exports, __webpack_require__) {

	// 19.1.3.1 Object.assign(target, source)
	var $export = __webpack_require__(58);
	
	$export($export.S + $export.F, 'Object', {assign: __webpack_require__(141)});

/***/ },
/* 141 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	// 19.1.2.1 Object.assign(target, source, ...)
	var getKeys  = __webpack_require__(105)
	  , gOPS     = __webpack_require__(126)
	  , pIE      = __webpack_require__(88)
	  , toObject = __webpack_require__(75)
	  , IObject  = __webpack_require__(90)
	  , $assign  = Object.assign;
	
	// should work with symbols and should have deterministic property order (V8 bug)
	module.exports = !$assign || __webpack_require__(68)(function(){
	  var A = {}
	    , B = {}
	    , S = Symbol()
	    , K = 'abcdefghijklmnopqrst';
	  A[S] = 7;
	  K.split('').forEach(function(k){ B[k] = k; });
	  return $assign({}, A)[S] != 7 || Object.keys($assign({}, B)).join('') != K;
	}) ? function assign(target, source){ // eslint-disable-line no-unused-vars
	  var T     = toObject(target)
	    , aLen  = arguments.length
	    , index = 1
	    , getSymbols = gOPS.f
	    , isEnum     = pIE.f;
	  while(aLen > index){
	    var S      = IObject(arguments[index++])
	      , keys   = getSymbols ? getKeys(S).concat(getSymbols(S)) : getKeys(S)
	      , length = keys.length
	      , j      = 0
	      , key;
	    while(length > j)if(isEnum.call(S, key = keys[j++]))T[key] = S[key];
	  } return T;
	} : $assign;

/***/ },
/* 142 */
/***/ function(module, exports, __webpack_require__) {

	var __WEBPACK_AMD_DEFINE_ARRAY__, __WEBPACK_AMD_DEFINE_RESULT__;'use strict';
	
	var _typeof2 = __webpack_require__(92);
	
	var _typeof3 = _interopRequireDefault(_typeof2);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	(function () {
	
	    var debug = false;
	
	    var root = this;
	
	    var EXIF = function EXIF(obj) {
	        if (obj instanceof EXIF) return obj;
	        if (!(this instanceof EXIF)) return new EXIF(obj);
	        this.EXIFwrapped = obj;
	    };
	
	    if (true) {
	        if (typeof module !== 'undefined' && module.exports) {
	            exports = module.exports = EXIF;
	        }
	        exports.EXIF = EXIF;
	    } else {
	        root.EXIF = EXIF;
	    }
	
	    var ExifTags = EXIF.Tags = {
	
	        // version tags
	        0x9000: "ExifVersion", // EXIF version
	        0xA000: "FlashpixVersion", // Flashpix format version
	
	        // colorspace tags
	        0xA001: "ColorSpace", // Color space information tag
	
	        // image configuration
	        0xA002: "PixelXDimension", // Valid width of meaningful image
	        0xA003: "PixelYDimension", // Valid height of meaningful image
	        0x9101: "ComponentsConfiguration", // Information about channels
	        0x9102: "CompressedBitsPerPixel", // Compressed bits per pixel
	
	        // user information
	        0x927C: "MakerNote", // Any desired information written by the manufacturer
	        0x9286: "UserComment", // Comments by user
	
	        // related file
	        0xA004: "RelatedSoundFile", // Name of related sound file
	
	        // date and time
	        0x9003: "DateTimeOriginal", // Date and time when the original image was generated
	        0x9004: "DateTimeDigitized", // Date and time when the image was stored digitally
	        0x9290: "SubsecTime", // Fractions of seconds for DateTime
	        0x9291: "SubsecTimeOriginal", // Fractions of seconds for DateTimeOriginal
	        0x9292: "SubsecTimeDigitized", // Fractions of seconds for DateTimeDigitized
	
	        // picture-taking conditions
	        0x829A: "ExposureTime", // Exposure time (in seconds)
	        0x829D: "FNumber", // F number
	        0x8822: "ExposureProgram", // Exposure program
	        0x8824: "SpectralSensitivity", // Spectral sensitivity
	        0x8827: "ISOSpeedRatings", // ISO speed rating
	        0x8828: "OECF", // Optoelectric conversion factor
	        0x9201: "ShutterSpeedValue", // Shutter speed
	        0x9202: "ApertureValue", // Lens aperture
	        0x9203: "BrightnessValue", // Value of brightness
	        0x9204: "ExposureBias", // Exposure bias
	        0x9205: "MaxApertureValue", // Smallest F number of lens
	        0x9206: "SubjectDistance", // Distance to subject in meters
	        0x9207: "MeteringMode", // Metering mode
	        0x9208: "LightSource", // Kind of light source
	        0x9209: "Flash", // Flash status
	        0x9214: "SubjectArea", // Location and area of main subject
	        0x920A: "FocalLength", // Focal length of the lens in mm
	        0xA20B: "FlashEnergy", // Strobe energy in BCPS
	        0xA20C: "SpatialFrequencyResponse", //
	        0xA20E: "FocalPlaneXResolution", // Number of pixels in width direction per FocalPlaneResolutionUnit
	        0xA20F: "FocalPlaneYResolution", // Number of pixels in height direction per FocalPlaneResolutionUnit
	        0xA210: "FocalPlaneResolutionUnit", // Unit for measuring FocalPlaneXResolution and FocalPlaneYResolution
	        0xA214: "SubjectLocation", // Location of subject in image
	        0xA215: "ExposureIndex", // Exposure index selected on camera
	        0xA217: "SensingMethod", // Image sensor type
	        0xA300: "FileSource", // Image source (3 == DSC)
	        0xA301: "SceneType", // Scene type (1 == directly photographed)
	        0xA302: "CFAPattern", // Color filter array geometric pattern
	        0xA401: "CustomRendered", // Special processing
	        0xA402: "ExposureMode", // Exposure mode
	        0xA403: "WhiteBalance", // 1 = auto white balance, 2 = manual
	        0xA404: "DigitalZoomRation", // Digital zoom ratio
	        0xA405: "FocalLengthIn35mmFilm", // Equivalent foacl length assuming 35mm film camera (in mm)
	        0xA406: "SceneCaptureType", // Type of scene
	        0xA407: "GainControl", // Degree of overall image gain adjustment
	        0xA408: "Contrast", // Direction of contrast processing applied by camera
	        0xA409: "Saturation", // Direction of saturation processing applied by camera
	        0xA40A: "Sharpness", // Direction of sharpness processing applied by camera
	        0xA40B: "DeviceSettingDescription", //
	        0xA40C: "SubjectDistanceRange", // Distance to subject
	
	        // other tags
	        0xA005: "InteroperabilityIFDPointer",
	        0xA420: "ImageUniqueID" // Identifier assigned uniquely to each image
	    };
	
	    var TiffTags = EXIF.TiffTags = {
	        0x0100: "ImageWidth",


	            3: "Cr",
	            4: "R",
	            5: "G",
	            6: "B"
	        }
	    };
	
	    function addEvent(element, event, handler) {
	        if (element.addEventListener) {
	            element.addEventListener(event, handler, false);
	        } else if (element.attachEvent) {
	            element.attachEvent("on" + event, handler);
	        }
	    }
	
	    function imageHasData(img) {
	        return !!img.exifdata;
	    }
	
	    function base64ToArrayBuffer(base64, contentType) {
	        contentType = contentType || base64.match(/^data\:([^\;]+)\;base64,/mi)[1] || ''; // e.g. 'data:image/jpeg;base64,...' => 'image/jpeg'
	        base64 = base64.replace(/^data\:([^\;]+)\;base64,/gmi, '');
	        var binary = atob(base64);
	        var len = binary.length;
	        var buffer = new ArrayBuffer(len);
	        var view = new Uint8Array(buffer);
	        for (var i = 0; i < len; i++) {
	            view[i] = binary.charCodeAt(i);
	        }
	        return buffer;
	    }
	
	    function objectURLToBlob(url, callback) {
	        var http = new XMLHttpRequest();
	        http.open("GET", url, true);
	        http.responseType = "blob";
	        http.onload = function (e) {
	            if (this.status == 200 || this.status === 0) {
	                callback(this.response);
	            }
	        };
	        http.send();
	    }
	
	    function getImageData(img, callback) {
	        function handleBinaryFile(binFile) {
	            var data = findEXIFinJPEG(binFile);
	            var iptcdata = findIPTCinJPEG(binFile);
	            img.exifdata = data || {};
	            img.iptcdata = iptcdata || {};
	            if (callback) {
	                callback.call(img);
	            }
	        }
	
	        if (img.src) {
	            if (/^data\:/i.test(img.src)) {
	                // Data URI
	                var arrayBuffer = base64ToArrayBuffer(img.src);
	                handleBinaryFile(arrayBuffer);
	            } else if (/^blob\:/i.test(img.src)) {
	                // Object URL
	                var fileReader = new FileReader();
	                fileReader.onload = function (e) {
	                    handleBinaryFile(e.target.result);
	                };
	                objectURLToBlob(img.src, function (blob) {
	                    fileReader.readAsArrayBuffer(blob);
	                });
	            } else {
	                var http = new XMLHttpRequest();
	                http.onload = function () {
	                    if (this.status == 200 || this.status === 0) {
	                        handleBinaryFile(http.response);
	                    } else {
	                        throw "Could not load image";
	                    }
	                    http = null;
	                };
	                http.open("GET", img.src, true);
	                http.responseType = "arraybuffer";
	                http.send(null);
	            }
	        } else if (window.FileReader && (img instanceof window.Blob || img instanceof window.File)) {
	            var fileReader = new FileReader();
	            fileReader.onload = function (e) {
	                if (debug) console.log("Got file of length " + e.target.result.byteLength);
	                handleBinaryFile(e.target.result);
	            };
	
	            fileReader.readAsArrayBuffer(img);
	        }
	    }
	
	    function findEXIFinJPEG(file) {
	        var dataView = new DataView(file);
	
	        if (debug) console.log("Got file of length " + file.byteLength);
	        if (dataView.getUint8(0) != 0xFF || dataView.getUint8(1) != 0xD8) {
	            if (debug) console.log("Not a valid JPEG");
	            return false; // not a valid jpeg
	        }
	
	        var offset = 2,
	            length = file.byteLength,
	            marker;
	
	        while (offset < length) {
	            if (dataView.getUint8(offset) != 0xFF) {
	                if (debug) console.log("Not a valid marker at offset " + offset + ", found: " + dataView.getUint8(offset));
	                return false; // not a valid marker, something is wrong
	            }
	
	            marker = dataView.getUint8(offset + 1);
	            if (debug) console.log(marker);
	
	            // we could implement handling for other markers here,
	            // but we're only looking for 0xFFE1 for EXIF data
	
	            if (marker == 225) {
	                if (debug) console.log("Found 0xFFE1 marker");
	
	                return readEXIFData(dataView, offset + 4, dataView.getUint16(offset + 2) - 2);
	
	                // offset += 2 + file.getShortAt(offset+2, true);
	            } else {
	                offset += 2 + dataView.getUint16(offset + 2);
	            }
	        }
	    }
	
	    function findIPTCinJPEG(file) {
	        var dataView = new DataView(file);
	
	        if (debug) console.log("Got file of length " + file.byteLength);
	        if (dataView.getUint8(0) != 0xFF || dataView.getUint8(1) != 0xD8) {
	            if (debug) console.log("Not a valid JPEG");
	            return false; // not a valid jpeg
	        }
	
	        var offset = 2,
	            length = file.byteLength;
	
	        var isFieldSegmentStart = function isFieldSegmentStart(dataView, offset) {
	            return dataView.getUint8(offset) === 0x38 && dataView.getUint8(offset + 1) === 0x42 && dataView.getUint8(offset + 2) === 0x49 && dataView.getUint8(offset + 3) === 0x4D && dataView.getUint8(offset + 4) === 0x04 && dataView.getUint8(offset + 5) === 0x04;
	        };
	
	        while (offset < length) {
	
	            if (isFieldSegmentStart(dataView, offset)) {
	
	                // Get the length of the name header (which is padded to an even number of bytes)
	                var nameHeaderLength = dataView.getUint8(offset + 7);
	                if (nameHeaderLength % 2 !== 0) nameHeaderLength += 1;
	                // Check for pre photoshop 6 format
	                if (nameHeaderLength === 0) {
	                    // Always 4
	                    nameHeaderLength = 4;
	                }
	
	                var startOffset = offset + 8 + nameHeaderLength;
	                var sectionLength = dataView.getUint16(offset + 6 + nameHeaderLength);
	
	                return readIPTCData(file, startOffset, sectionLength);
	
	                break;
	            }
	
	            // Not the marker, continue searching
	            offset++;
	        }
	    }
	    var IptcFieldMap = {
	        0x78: 'caption',
	        0x6E: 'credit',
	        0x19: 'keywords',
	        0x37: 'dateCreated',
	        0x50: 'byline',
	        0x55: 'bylineTitle',
	        0x7A: 'captionWriter',
	        0x69: 'headline',
	        0x74: 'copyright',
	        0x0F: 'category'
	    };
	    function readIPTCData(file, startOffset, sectionLength) {
	        var dataView = new DataView(file);
	        var data = {};
	        var fieldValue, fieldName, dataSize, segmentType, segmentSize;
	        var segmentStartPos = startOffset;
	        while (segmentStartPos < startOffset + sectionLength) {
	            if (dataView.getUint8(segmentStartPos) === 0x1C && dataView.getUint8(segmentStartPos + 1) === 0x02) {
	                segmentType = dataView.getUint8(segmentStartPos + 2);
	                if (segmentType in IptcFieldMap) {
	                    dataSize = dataView.getInt16(segmentStartPos + 3);
	                    segmentSize = dataSize + 5;
	                    fieldName = IptcFieldMap[segmentType];
	                    fieldValue = getStringFromDB(dataView, segmentStartPos + 5, dataSize);
	                    // Check if we already stored a value with this name
	                    if (data.hasOwnProperty(fieldName)) {
	                        // Value already stored with this name, create multivalue field
	                        if (data[fieldName] instanceof Array) {
	                            data[fieldName].push(fieldValue);
	                        } else {
	                            data[fieldName] = [data[fieldName], fieldValue];
	                        }
	                    } else {
	                        data[fieldName] = fieldValue;
	                    }
	                }
	            }
	            segmentStartPos++;
	        }
	        return data;
	    }
	
	    function readTags(file, tiffStart, dirStart, strings, bigEnd) {
	        var entries = file.getUint16(dirStart, !bigEnd),
	            tags = {},
	            entryOffset,
	            tag,
	            i;
	
	        for (i = 0; i < entries; i++) {
	            entryOffset = dirStart + i * 12 + 2;
	            tag = strings[file.getUint16(entryOffset, !bigEnd)];
	            if (!tag && debug) console.log("Unknown tag: " + file.getUint16(entryOffset, !bigEnd));
	            tags[tag] = readTagValue(file, entryOffset, tiffStart, dirStart, bigEnd);
	        }
	        return tags;
	    }
	
	    function readTagValue(file, entryOffset, tiffStart, dirStart, bigEnd) {
	        var type = file.getUint16(entryOffset + 2, !bigEnd),
	            numValues = file.getUint32(entryOffset + 4, !bigEnd),
	            valueOffset = file.getUint32(entryOffset + 8, !bigEnd) + tiffStart,
	            offset,
	            vals,
	            val,
	            n,
	            numerator,
	            denominator;
	
	        switch (type) {
	            case 1: // byte, 8-bit unsigned int
	            case 7:
	                // undefined, 8-bit byte, value depending on field
	                if (numValues == 1) {
	                    return file.getUint8(entryOffset + 8, !bigEnd);
	                } else {
	                    offset = numValues > 4 ? valueOffset : entryOffset + 8;
	                    vals = [];
	                    for (n = 0; n < numValues; n++) {
	                        vals[n] = file.getUint8(offset + n);
	                    }
	                    return vals;
	                }
	
	            case 2:
	                // ascii, 8-bit byte
	                offset = numValues > 4 ? valueOffset : entryOffset + 8;
	                return getStringFromDB(file, offset, numValues - 1);
	
	            case 3:
	                // short, 16 bit int
	                if (numValues == 1) {
	                    return file.getUint16(entryOffset + 8, !bigEnd);
	                } else {
	                    offset = numValues > 2 ? valueOffset : entryOffset + 8;
	                    vals = [];
	                    for (n = 0; n < numValues; n++) {
	                        vals[n] = file.getUint16(offset + 2 * n, !bigEnd);
	                    }
	                    return vals;
	                }
	
	            case 4:
	                // long, 32 bit int
	                if (numValues == 1) {
	                    return file.getUint32(entryOffset + 8, !bigEnd);
	                } else {
	                    vals = [];
	                    for (n = 0; n < numValues; n++) {
	                        vals[n] = file.getUint32(valueOffset + 4 * n, !bigEnd);
	                    }
	                    return vals;
	                }
	
	            case 5:
	                // rational = two long values, first is numerator, second is denominator
	                if (numValues == 1) {
	                    numerator = file.getUint32(valueOffset, !bigEnd);
	                    denominator = file.getUint32(valueOffset + 4, !bigEnd);
	                    val = new Number(numerator / denominator);
	                    val.numerator = numerator;
	                    val.denominator = denominator;
	                    return val;
	                } else {
	                    vals = [];
	                    for (n = 0; n < numValues; n++) {
	                        numerator = file.getUint32(valueOffset + 8 * n, !bigEnd);
	                        denominator = file.getUint32(valueOffset + 4 + 8 * n, !bigEnd);
	                        vals[n] = new Number(numerator / denominator);
	                        vals[n].numerator = numerator;
	                        vals[n].denominator = denominator;
	                    }
	                    return vals;
	                }
	
	            case 9:
	                // slong, 32 bit signed int
	                if (numValues == 1) {
	                    return file.getInt32(entryOffset + 8, !bigEnd);
	                } else {
	                    vals = [];
	                    for (n = 0; n < numValues; n++) {
	                        vals[n] = file.getInt32(valueOffset + 4 * n, !bigEnd);
	                    }
	                    return vals;
	                }
	
	            case 10:
	                // signed rational, two slongs, first is numerator, second is denominator
	                if (numValues == 1) {
	                    return file.getInt32(valueOffset, !bigEnd) / file.getInt32(valueOffset + 4, !bigEnd);
	                } else {
	                    vals = [];
	                    for (n = 0; n < numValues; n++) {
	                        vals[n] = file.getInt32(valueOffset + 8 * n, !bigEnd) / file.getInt32(valueOffset + 4 + 8 * n, !bigEnd);
	                    }
	                    return vals;
	                }
	        }
	    }
	
	    function getStringFromDB(buffer, start, length) {
	        var outstr = "";
	        for (n = start; n < start + length; n++) {
	            outstr += String.fromCharCode(buffer.getUint8(n));
	        }
	        return outstr;
	    }
	
	    function readEXIFData(file, start) {
	        if (getStringFromDB(file, start, 4) != "Exif") {
	            if (debug) console.log("Not valid EXIF data! " + getStringFromDB(file, start, 4));
	            return false;
	        }
	
	        var bigEnd,
	            tags,
	            tag,
	            exifData,
	            gpsData,
	            tiffOffset = start + 6;
	
	        // test for TIFF validity and endianness
	        if (file.getUint16(tiffOffset) == 0x4949) {
	            bigEnd = false;
	        } else if (file.getUint16(tiffOffset) == 0x4D4D) {
	            bigEnd = true;
	        } else {
	            if (debug) console.log("Not valid TIFF data! (no 0x4949 or 0x4D4D)");
	            return false;
	        }
	
	        if (file.getUint16(tiffOffset + 2, !bigEnd) != 0x002A) {
	            if (debug) console.log("Not valid TIFF data! (no 0x002A)");
	            return false;
	        }
	
	        var firstIFDOffset = file.getUint32(tiffOffset + 4, !bigEnd);
	
	        if (firstIFDOffset < 0x00000008) {
	            if (debug) console.log("Not valid TIFF data! (First offset less than 8)", file.getUint32(tiffOffset + 4, !bigEnd));
	            return false;
	        }
	
	        tags = readTags(file, tiffOffset, tiffOffset + firstIFDOffset, TiffTags, bigEnd);
	
	        if (tags.ExifIFDPointer) {
	            exifData = readTags(file, tiffOffset, tiffOffset + tags.ExifIFDPointer, ExifTags, bigEnd);
	            for (tag in exifData) {
	                switch (tag) {


	                        exifData[tag] = StringValues[tag][exifData[tag]];
	                        break;
	
	                    case "ExifVersion":
	                    case "FlashpixVersion":
	                        exifData[tag] = String.fromCharCode(exifData[tag][0], exifData[tag][1], exifData[tag][2], exifData[tag][3]);
	                        break;
	
	                    case "ComponentsConfiguration":
	                        exifData[tag] = StringValues.Components[exifData[tag][0]] + StringValues.Components[exifData[tag][1]] + StringValues.Components[exifData[tag][2]] + StringValues.Components[exifData[tag][3]];
	                        break;
	                }
	                tags[tag] = exifData[tag];
	            }
	        }
	
	        if (tags.GPSInfoIFDPointer) {
	            gpsData = readTags(file, tiffOffset, tiffOffset + tags.GPSInfoIFDPointer, GPSTags, bigEnd);
	            for (tag in gpsData) {
	                switch (tag) {
	                    case "GPSVersionID":
	                        gpsData[tag] = gpsData[tag][0] + "." + gpsData[tag][1] + "." + gpsData[tag][2] + "." + gpsData[tag][3];
	                        break;
	                }
	                tags[tag] = gpsData[tag];
	            }
	        }
	
	        return tags;
	    }
	
	    EXIF.getData = function (img, callback) {
	        if ((img instanceof Image || img instanceof HTMLImageElement) && !img.complete) return false;
	
	        if (!imageHasData(img)) {
	            getImageData(img, callback);
	        } else {
	            if (callback) {
	                callback.call(img);
	            }
	        }
	        return true;
	    };
	
	    EXIF.getTag = function (img, tag) {
	        if (!imageHasData(img)) return;
	        return img.exifdata[tag];
	    };
	
	    EXIF.getAllTags = function (img) {
	        if (!imageHasData(img)) return {};
	        var a,
	            data = img.exifdata,
	            tags = {};
	        for (a in data) {
	            if (data.hasOwnProperty(a)) {
	                tags[a] = data[a];
	            }
	        }
	        return tags;
	    };
	
	    EXIF.pretty = function (img) {
	        if (!imageHasData(img)) return "";
	        var a,
	            data = img.exifdata,
	            strPretty = "";
	        for (a in data) {
	            if (data.hasOwnProperty(a)) {
	                if ((0, _typeof3.default)(data[a]) == "object") {
	                    if (data[a] instanceof Number) {
	                        strPretty += a + " : " + data[a] + " [" + data[a].numerator + "/" + data[a].denominator + "]\r\n";
	                    } else {
	                        strPretty += a + " : [" + data[a].length + " values]\r\n";
	                    }
	                } else {
	                    strPretty += a + " : " + data[a] + "\r\n";
	                }
	            }
	        }
	        return strPretty;
	    };
	
	    EXIF.readFromBinaryFile = function (file) {
	        return findEXIFinJPEG(file);
	    };
	
	    if (true) {
	        !(__WEBPACK_AMD_DEFINE_ARRAY__ = [], __WEBPACK_AMD_DEFINE_RESULT__ = function () {
	            return EXIF;
	        }.apply(exports, __WEBPACK_AMD_DEFINE_ARRAY__), __WEBPACK_AMD_DEFINE_RESULT__ !== undefined && (module.exports = __WEBPACK_AMD_DEFINE_RESULT__));
	    }
	}).call(undefined);

/***/ },
/* 143 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page consumer\" style=\"background-color: #444;\">\n    <header id=\"header\" class=\"mui-bar mui-bar-nav bar-images\">\n        <h1 class=\"mui-title\">我的头像</h1>\n        <a class=\"mui-action-back mui-icon mui-icon-left-nav mui-pull-left\"></a>\n    </header>\n    <div class=\"mui-scroll-wrapper\" style=\"bottom: 0px;\">\n    <div class=\"mui-scroll\">\n    <ul class=\"images-container\">\n        <img :src=\"massagistInfo.massagist_photo\" class=\"head\">\n        <a class=\"change-head-img\" @click=\"file\">更换头像</a>\n        <input id=\"fileCrop\" style=\"opacity: 0;\" type=\"file\" accept=\"image/*\" v-on:change=\"change\">\n    </ul>\n    </div></div>\n\n    <div class=\"crop-container\" v-show=\"showCrop\">\n        <div class=\"wrap\">\n            <img class=\"cropper\" :src=\"cropImg\">\n        </div>\n        <div class=\"btn\">\n            <div><button type=\"button\" class=\"mui-btn mui-btn-block\" @click=\"$store.state.locationReload()\">取消</button></div>\n            <div><button type=\"button\" class=\"mui-btn mui-btn-primary mui-btn-block\" @click=\"saveCrop\">保存</button></div>\n        </div>\n    </div>\n</div>\n";

/***/ },
/* 144 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(145)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/consumer.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(146)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-f4e7cede/consumer.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 145 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	
	var _start = __webpack_require__(44);
	
	var _start2 = _interopRequireDefault(_start);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	exports.default = {
	    name: 'consumer',
	    data: function data() {
	        return {
	            projectList: [],
	            massagistInfo: {
	                imgs: [],
	                info: {}
	            }
	        };
	    },
	    ready: function ready() {
	        var _this = this;
	
	        document.title = '店铺首页';
	        mui("#slider").slider();
	        mui('.mui-scroll-wrapper').scroll({ bounce: false });
	        this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	        this.$http.post(API.getStoreContents, { store_id: this.$route.params.storeid }).then(function (data) {
	            if (data.data.errorInfo) return;
	            _this.$store.state.hideTip();
	            _this.massagistInfo = data.data.list;
	        });
	        this.$http.post(API.getOnlineProject, { sid: this.$route.params.storeid }).then(function (data) {
	            if (data.data.errorInfo) return;
	            _this.$store.state.hideTip();
	            _this.projectList = data.data.object;
	        });
	    },
	    methods: {
	        local: function local(name) {
	            this.$router.go('/' + name + '/' + this.$route.params.token);
	        }
	    },
	    components: {
	        Start: _start2.default
	    }
	};
	
	// </script>
	/* generated by vue-loader */
	// <template>
	// <div class="page consumer">
	//
	//     <header id="header" class="mui-bar mui-bar-nav">
	//         <h1 class="mui-title">店铺首页</h1>
	//         <!-- <a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a> -->
	//     </header>
	//
	//     <div class="mui-scroll-wrapper">
	//     <div class="mui-scroll">
	//     <div id="slider" class="mui-slider" >
	//         <div class="mui-slider-group mui-slider-loop">
	//             <div class="mui-slider-item mui-slider-item-duplicate">
	//                 <a>
	//                     <img :src=" $store.state.URL + massagistInfo.imgs[massagistInfo.imgs.length - 1].image_address ">
	//                 </a>
	//             </div>
	//
	//             <div class="mui-slider-item" v-for="item in massagistInfo.imgs">
	//                 <a>
	//                     <img :src="$store.state.URL + item.image_address">
	//                 </a>
	//             </div>
	//
	//             <div class="mui-slider-item mui-slider-item-duplicate">
	//                 <a>
	//                     <img :src=" $store.state.URL + massagistInfo.imgs[0].image_address ">
	//                 </a>
	//             </div>
	//         </div>
	//         <div class="mui-slider-indicator">
	//             <div class="mui-indicator {{$index == 0 ? 'mui-active' : ''}}" v-for="item in massagistInfo.imgs"></div>
	//         </div>
	//     </div>
	//
	//     <div class="shop-title-container">
	//         <h1>{{massagistInfo.info.store_name}}</h1>
	//         <h5>
	//             <span class="start"><start :value='massagistInfo.score'></start>{{parseInt(massagistInfo.score)}}分</span>
	//             <a class="pj" v-link="{path: '/consumer-shop-comment/' + this.$route.params.token + '/' + this.$route.params.storeid}">店铺评价 ({{massagistInfo.comment_total}}) </a>
	//         </h5>
	//     </div>
	//     <div class="position-container mui-ellipsis">
	//         <i class="iconfont">&#xe603;</i>{{massagistInfo.info.store_address}}
	//         <a class="iconfont tel" href="tel:{{massagistInfo.info.store_tel}}">&#xe604;</a>
	//     </div>
	//
	//     <ul class="mui-table-view item-container">
	//         <li class="mui-table-view-cell mui-media" v-for="item in projectList">
	//             <a v-link="{path: '/consumer-pro/' + this.$route.params.token + '/' + this.$route.params.storeid + '/' + item.item_id}">
	//                 <div class="mui-media-object mui-pull-left image-size" style="background-image: url({{$store.state.URL + item.item_image}});"></div>
	//                 <!-- <img class="" :src="$store.state.URL + item.item_image"> -->
	//                 <div class="mui-media-body">
	//                     {{item.item_name}}
	//                     <p class="mui-ellipsis">套餐详情：{{item.item_intro}}</p>
	//                     <p>项目时长：{{item.item_time}}分钟</p>
	//                     <p><em class="price">￥{{item.item_vipprice}}</em> <em class="disguise">非会员：￥{{item.item_price}}</em></p>
	//                 </div>
	//             </a>
	//             <a class="xuanze" v-link="{path: '/consumer-pro/' + this.$route.params.token + '/' + this.$route.params.storeid + '/' + item.item_id}">选择</a>
	//         </li>
	//         <div class="no-data" v-if="projectList.length<=0">
	//             <h1>店铺还没有相关项目</h1>
	//         </div>
	//     </ul>
	//     </div>
	//     </div>
	//
	//     <nav class="mui-bar mui-bar-tab">
	//         <a class="winslow-tab-item mui-active">
	//             <span class="iconfont">&#xe67d;</span>
	//             <span class="mui-tab-label">首页</span>
	//         </a>
	//         <a class="winslow-tab-item" v-link="{path: '/consumer-order-list/' + this.$route.params.token + '/' + this.$route.params.storeid}">
	//             <span class="iconfont">&#xe61e;</span>
	//             <span class="mui-tab-label">我的</span>
	//         </a>
	//     </nav>
	//
	//
	// </div>
	// </template>
	//
	// <script>

/***/ },
/* 146 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page consumer\">\n\n    <header id=\"header\" class=\"mui-bar mui-bar-nav\">\n        <h1 class=\"mui-title\">店铺首页</h1>\n        <!-- <a class=\"mui-action-back mui-icon mui-icon-left-nav mui-pull-left\"></a> -->\n    </header>\n\n    <div class=\"mui-scroll-wrapper\">\n    <div class=\"mui-scroll\">\n    <div id=\"slider\" class=\"mui-slider\" >\n        <div class=\"mui-slider-group mui-slider-loop\">\n            <div class=\"mui-slider-item mui-slider-item-duplicate\">\n                <a>\n                    <img :src=\" $store.state.URL + massagistInfo.imgs[massagistInfo.imgs.length - 1].image_address \">\n                </a>\n            </div>\n\n            <div class=\"mui-slider-item\" v-for=\"item in massagistInfo.imgs\">\n                <a>\n                    <img :src=\"$store.state.URL + item.image_address\">\n                </a>\n            </div>\n\n            <div class=\"mui-slider-item mui-slider-item-duplicate\">\n                <a>\n                    <img :src=\" $store.state.URL + massagistInfo.imgs[0].image_address \">\n                </a>\n            </div>\n        </div>\n        <div class=\"mui-slider-indicator\">\n            <div class=\"mui-indicator {{$index == 0 ? 'mui-active' : ''}}\" v-for=\"item in massagistInfo.imgs\"></div>\n        </div>\n    </div>\n\n    <div class=\"shop-title-container\">\n        <h1>{{massagistInfo.info.store_name}}</h1>\n        <h5>\n            <span class=\"start\"><start :value='massagistInfo.score'></start>{{parseInt(massagistInfo.score)}}分</span>\n            <a class=\"pj\" v-link=\"{path: '/consumer-shop-comment/' + this.$route.params.token + '/' + this.$route.params.storeid}\">店铺评价 ({{massagistInfo.comment_total}}) </a>\n        </h5>\n    </div>\n    <div class=\"position-container mui-ellipsis\">\n        <i class=\"iconfont\">&#xe603;</i>{{massagistInfo.info.store_address}}\n        <a class=\"iconfont tel\" href=\"tel:{{massagistInfo.info.store_tel}}\">&#xe604;</a>\n    </div>\n\n    <ul class=\"mui-table-view item-container\">\n        <li class=\"mui-table-view-cell mui-media\" v-for=\"item in projectList\">\n            <a v-link=\"{path: '/consumer-pro/' + this.$route.params.token + '/' + this.$route.params.storeid + '/' + item.item_id}\">\n                <div class=\"mui-media-object mui-pull-left image-size\" style=\"background-image: url({{$store.state.URL + item.item_image}});\"></div>\n                <!-- <img class=\"\" :src=\"$store.state.URL + item.item_image\"> -->\n                <div class=\"mui-media-body\">\n                    {{item.item_name}}\n                    <p class=\"mui-ellipsis\">套餐详情：{{item.item_intro}}</p>\n                    <p>项目时长：{{item.item_time}}分钟</p>\n                    <p><em class=\"price\">￥{{item.item_vipprice}}</em> <em class=\"disguise\">非会员：￥{{item.item_price}}</em></p>\n                </div>\n            </a>\n            <a class=\"xuanze\" v-link=\"{path: '/consumer-pro/' + this.$route.params.token + '/' + this.$route.params.storeid + '/' + item.item_id}\">选择</a>\n        </li>\n        <div class=\"no-data\" v-if=\"projectList.length<=0\">\n            <h1>店铺还没有相关项目</h1>\n        </div>\n    </ul>\n    </div>\n    </div>\n\n    <nav class=\"mui-bar mui-bar-tab\">\n        <a class=\"winslow-tab-item mui-active\">\n            <span class=\"iconfont\">&#xe67d;</span>\n            <span class=\"mui-tab-label\">首页</span>\n        </a>\n        <a class=\"winslow-tab-item\" v-link=\"{path: '/consumer-order-list/' + this.$route.params.token + '/' + this.$route.params.storeid}\">\n            <span class=\"iconfont\">&#xe61e;</span>\n            <span class=\"mui-tab-label\">我的</span>\n        </a>\n    </nav>\n    \n\n</div>\n";

/***/ },
/* 147 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(148)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/consumer-pro.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(149)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-e00c291e/consumer-pro.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 148 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	
	var _start = __webpack_require__(44);
	
	var _start2 = _interopRequireDefault(_start);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	exports.default = {
	    name: 'consumerPro',
	    data: function data() {
	        return {
	            techList: [],
	            proInfo: {}
	        };
	    },
	    ready: function ready() {
	        document.title = '项目详情';
	        mui('.mui-scroll-wrapper').scroll({ bounce: false });
	        this.resload();
	    },
	    methods: {
	        local: function local(name) {
	            this.$router.go('/' + name + '/' + this.$route.params.token);
	        },
	        resload: function resload() {
	            var _this = this;
	
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.getProjectTechnician, {
	                sid: this.$route.params.storeid,
	                pid: this.$route.params.id
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this.$store.state.hideTip();
	                _this.techList = data.data.list;
	                _this.proInfo = data.data.object;
	                for (var i = 0; i < _this.techList.length; i++) {
	                    _this.techList[i].massagist_store = parseInt(_this.techList[i].massagist_store);
	                    if (_this.techList[i].massagist_photo.indexOf('http') < 0) {
	                        _this.techList[i].massagist_photo = _this.$store.state.URL + _this.techList[i].massagist_photo;
	                    }
	                }
	            });
	        }
	    },
	    components: {
	        Start: _start2.default
	    }
	};
	
	// </script>
	/* generated by vue-loader */
	// <template>
	// <div class="page consumer" style="background: #fff; padding-bottom: 10px;">
	//     <header id="header" class="mui-bar mui-bar-nav">
	//         <h1 class="mui-title">项目详情</h1>
	//         <a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a>
	//     </header>
	//     <div class="mui-scroll-wrapper" style="bottom: 0px;">
	//     <div class="mui-scroll">
	//     <div class="banner-main" style="background-image: url({{$store.state.URL + proInfo.item_image}})"></div>
	//
	//     <div class="shop-title-container" style="padding-right: 95px;">
	//         <h1>{{proInfo.item_name}}</h1>
	//         <h5>套餐详情：{{proInfo.item_intro}}</h5>
	//         <h4>项目时长：{{proInfo.item_time}}分钟</h4>
	//         <div class="price">
	//             <em>￥{{proInfo.item_vipprice}}</em>
	//             <span>非会员:￥{{proInfo.item_price}}</span>
	//         </div>
	//     </div>
	//     <div class="title">为保护技师隐私，真实图片仅VIP可见 <a v-link="{path: '/consumer-vip/' + this.$route.params.token + '/' + this.$route.params.storeid}" class="no">加入VIP</a></div>
	//     <ul class="mui-table-view item-container">
	//         <li class="mui-table-view-cell mui-media jishi" v-for="item in techList">
	//             <a v-link="{path: '/consumer-tech/' + this.$route.params.token + '/' + this.$route.params.storeid + '/' + item.massagist_id}">
	//                 <img class="mui-media-object mui-pull-left" :src="item.massagist_photo">
	//                 <div class="mui-media-body">
	//                     <div class="info mui-ellipsis">
	//                         <h1>工号：{{item.massagist_no}}</h1>
	//                         <h2>年龄：{{item.massagist_birth || '未设置' | years}}</h2>
	//                         <h3><start :value='parseInt(item.massagist_store)'></start> {{parseInt(item.massagist_store)}}分</h3>
	//                     </div>
	//                     <div class="info">
	//                         <h1>昵称：{{item.massagist_name}}</h1>
	//                         <h2>籍贯：{{item.c_name}}</h2>
	//                     </div>
	//                 </div>
	//             </a>
	//             <a class="xuanze" v-if="item.massagist_status=='2'" v-link="{path: '/consumer-order-sub/' + this.$route.params.token + '/' + this.$route.params.storeid + '/' + this.$route.params.id + '/' + item.massagist_id}">选择</a>
	//             <a class="xuanze sleep" v-if="item.massagist_status!='2'" @click="$store.state.showTip({type: 'error', content: '选择的技师休息或正在工作中' })">选择</a>
	//         </li>
	//         <div class="no-data" v-if="techList.length<=0">
	//             <h1>项目还没有相关技师</h1>
	//         </div>
	//     </ul>
	//
	//     <!-- <div class="footer">
	//         <a>生成订单</a>
	//     </div> -->
	//     </div></div>
	//
	// </div>
	// </template>
	//
	// <script>

/***/ },
/* 149 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page consumer\" style=\"background: #fff; padding-bottom: 10px;\">\n    <header id=\"header\" class=\"mui-bar mui-bar-nav\">\n        <h1 class=\"mui-title\">项目详情</h1>\n        <a class=\"mui-action-back mui-icon mui-icon-left-nav mui-pull-left\"></a>\n    </header>\n    <div class=\"mui-scroll-wrapper\" style=\"bottom: 0px;\">\n    <div class=\"mui-scroll\">\n    <div class=\"banner-main\" style=\"background-image: url({{$store.state.URL + proInfo.item_image}})\"></div>\n\n    <div class=\"shop-title-container\" style=\"padding-right: 95px;\">\n        <h1>{{proInfo.item_name}}</h1>\n        <h5>套餐详情：{{proInfo.item_intro}}</h5>\n        <h4>项目时长：{{proInfo.item_time}}分钟</h4>\n        <div class=\"price\">\n            <em>￥{{proInfo.item_vipprice}}</em>\n            <span>非会员:￥{{proInfo.item_price}}</span>\n        </div>\n    </div>\n    <div class=\"title\">为保护技师隐私，真实图片仅VIP可见 <a v-link=\"{path: '/consumer-vip/' + this.$route.params.token + '/' + this.$route.params.storeid}\" class=\"no\">加入VIP</a></div>\n    <ul class=\"mui-table-view item-container\">\n        <li class=\"mui-table-view-cell mui-media jishi\" v-for=\"item in techList\">\n            <a v-link=\"{path: '/consumer-tech/' + this.$route.params.token + '/' + this.$route.params.storeid + '/' + item.massagist_id}\">\n                <img class=\"mui-media-object mui-pull-left\" :src=\"item.massagist_photo\">\n                <div class=\"mui-media-body\">\n                    <div class=\"info mui-ellipsis\">\n                        <h1>工号：{{item.massagist_no}}</h1>\n                        <h2>年龄：{{item.massagist_birth || '未设置' | years}}</h2>\n                        <h3><start :value='parseInt(item.massagist_store)'></start> {{parseInt(item.massagist_store)}}分</h3>\n                    </div>\n                    <div class=\"info\">\n                        <h1>昵称：{{item.massagist_name}}</h1>\n                        <h2>籍贯：{{item.c_name}}</h2>\n                    </div>\n                </div>\n            </a>\n            <a class=\"xuanze\" v-if=\"item.massagist_status=='2'\" v-link=\"{path: '/consumer-order-sub/' + this.$route.params.token + '/' + this.$route.params.storeid + '/' + this.$route.params.id + '/' + item.massagist_id}\">选择</a>\n            <a class=\"xuanze sleep\" v-if=\"item.massagist_status!='2'\" @click=\"$store.state.showTip({type: 'error', content: '选择的技师休息或正在工作中' })\">选择</a>\n        </li>\n        <div class=\"no-data\" v-if=\"techList.length<=0\">\n            <h1>项目还没有相关技师</h1>\n        </div>\n    </ul>\n\n    <!-- <div class=\"footer\">\n        <a>生成订单</a>\n    </div> -->\n    </div></div>\n    \n</div>\n";

/***/ },
/* 150 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(151)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/consumer-tech.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(152)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-4dadab02/consumer-tech.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 151 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	
	var _start = __webpack_require__(44);
	
	var _start2 = _interopRequireDefault(_start);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	exports.default = {
	    name: 'consumerTech',
	    data: function data() {
	        return {
	            proList: [],
	            techInfo: {}
	        };
	    },
	    ready: function ready() {
	        document.title = '技师详情';
	        mui("#slider").slider();
	        mui('.mui-scroll-wrapper').scroll({ bounce: false });
	        this.resload();
	    },
	    methods: {
	        local: function local(name) {
	            this.$router.go('/' + name + '/' + this.$route.params.token);
	        },
	        resload: function resload() {
	            var _this = this;
	
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.getMassagistInfo, {
	                store_id: this.$route.params.storeid,
	                tid: this.$route.params.id
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this.$store.state.hideTip();
	                _this.proList = data.data.list;
	                _this.techInfo = data.data.object;
	                _this.techInfo.massagist_store = parseInt(_this.techInfo.massagist_store);
	                if (_this.techInfo.massagist_photo.indexOf('http') < 0) {
	                    _this.techInfo.massagist_photo = _this.$store.state.URL + _this.techInfo.massagist_photo;
	                }
	            });
	        }
	    },
	    components: {
	        Start: _start2.default
	    }
	};
	
	// </script>
	/* generated by vue-loader */
	// <template>
	// <div class="page consumer" style="padding-bottom: 10px;">
	//     <header id="header" class="mui-bar mui-bar-nav">
	//         <h1 class="mui-title">技师详情</h1>
	//         <a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a>
	//     </header>
	//     <div class="mui-scroll-wrapper" style="bottom: 0px;">
	//     <div class="mui-scroll">
	//     <div id="slider" class="mui-slider" >
	//         <div class="mui-slider-group mui-slider-loop">
	//             <!-- 额外增加的一个节点(循环轮播：第一个节点是最后一张轮播) -->
	//             <div class="mui-slider-item mui-slider-item-duplicate">
	//                 <a>
	//                     <img :src=" $store.state.URL + techInfo.massagist_image[techInfo.massagist_image.length - 1].img_url ">
	//                 </a>
	//             </div>
	//
	//             <div class="mui-slider-item" v-for="item in techInfo.massagist_image">
	//                 <a>
	//                     <img :src=" $store.state.URL + item.img_url ">
	//                 </a>
	//             </div>
	//             <!-- 额外增加的一个节点(循环轮播：最后一个节点是第一张轮播) -->
	//             <div class="mui-slider-item mui-slider-item-duplicate">
	//                 <a>
	//                     <img :src=" $store.state.URL + techInfo.massagist_image[0].img_url ">
	//                 </a>
	//             </div>
	//         </div>
	//         <div class="mui-slider-indicator">
	//             <div class="mui-indicator {{$index == 0 ? 'mui-active' : ''}}" v-for="item in techInfo.massagist_image"></div>
	//         </div>
	//     </div>
	//
	//     <div class="my-info-container">
	//         <div class="my-info">
	//             <img :src="techInfo.massagist_photo" class="head">
	//             <div class="info mui-ellipsis">
	//                 <h1>工号：{{techInfo.massagist_no}}</h1>
	//                 <h2>年龄：{{techInfo.massagist_birth || '未设置' | years}}</h2>
	//                 <h3>
	//                     <start :value='techInfo.massagist_store'></start>{{techInfo.massagist_store}}分
	//                 </h3>
	//             </div>
	//             <div class="info">
	//                 <h1>昵称：{{techInfo.massagist_name}}</h1>
	//                 <h2>籍贯：{{techInfo.c_name}}</h2>
	//                 <h3><a class="pj" v-link="{path: '/consumer-tech-comment/' + this.$route.params.token + '/' + this.$route.params.storeid + '/' + techInfo.massagist_id}">评价 ({{techInfo.comment_total}})</a></h3>
	//             </div>
	//         </div>
	//     </div>
	//
	//     <ul class="mui-table-view item-container">
	//         <li class="mui-table-view-cell mui-media" v-for="item in proList">
	//             <a v-link="{path: '/consumer-pro/' + this.$route.params.token + '/' + this.$route.params.storeid + '/' + item.item_id}">
	//                 <div class="mui-media-object mui-pull-left image-size" style="background-image: url({{$store.state.URL + item.item_image}});"></div>
	//                 <!-- <img class="mui-media-object mui-pull-left" :src="$store.state.URL + item.item_image"> -->
	//                 <div class="mui-media-body">
	//                     {{item.item_name}}
	//                     <p class="mui-ellipsis">套餐详情：{{item.item_intro}}</p>
	//                     <p>项目时长：{{item.item_time}}分钟</p>
	//                     <p><em class="price">￥{{item.item_vipprice}}</em> <em class="disguise">非会员：￥{{item.item_price}}</em></p>
	//                 </div>
	//             </a>
	//             <a class="xuanze" v-if="techInfo.massagist_status == '2'" v-link="{path: '/consumer-order-sub/' + this.$route.params.token + '/' + this.$route.params.storeid + '/' + item.item_id + '/' + this.$route.params.id}">选择</a>
	//             <a class="xuanze sleep" v-if="techInfo.massagist_status != '2'" @click="$store.state.showTip({type: 'error', content: '选择的技师休息或正在工作中' })">选择</a>
	//         </li>
	//         <div class="no-data" v-if="techList.proList<=0">
	//             <h1>技师还没有相关项目</h1>
	//         </div>
	//     </ul>
	//     </div></div>
	//     <!-- <div class="footer">
	//         <a>生成订单</a>
	//     </div> -->
	//
	//
	// </div>
	// </template>
	//
	// <script>

/***/ },
/* 152 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page consumer\" style=\"padding-bottom: 10px;\">\n    <header id=\"header\" class=\"mui-bar mui-bar-nav\">\n        <h1 class=\"mui-title\">技师详情</h1>\n        <a class=\"mui-action-back mui-icon mui-icon-left-nav mui-pull-left\"></a>\n    </header>\n    <div class=\"mui-scroll-wrapper\" style=\"bottom: 0px;\">\n    <div class=\"mui-scroll\">\n    <div id=\"slider\" class=\"mui-slider\" >\n        <div class=\"mui-slider-group mui-slider-loop\">\n            <!-- 额外增加的一个节点(循环轮播：第一个节点是最后一张轮播) -->\n            <div class=\"mui-slider-item mui-slider-item-duplicate\">\n                <a>\n                    <img :src=\" $store.state.URL + techInfo.massagist_image[techInfo.massagist_image.length - 1].img_url \">\n                </a>\n            </div>\n\n            <div class=\"mui-slider-item\" v-for=\"item in techInfo.massagist_image\">\n                <a>\n                    <img :src=\" $store.state.URL + item.img_url \">\n                </a>\n            </div>\n            <!-- 额外增加的一个节点(循环轮播：最后一个节点是第一张轮播) -->\n            <div class=\"mui-slider-item mui-slider-item-duplicate\">\n                <a>\n                    <img :src=\" $store.state.URL + techInfo.massagist_image[0].img_url \">\n                </a>\n            </div>\n        </div>\n        <div class=\"mui-slider-indicator\">\n            <div class=\"mui-indicator {{$index == 0 ? 'mui-active' : ''}}\" v-for=\"item in techInfo.massagist_image\"></div>\n        </div>\n    </div>\n\n    <div class=\"my-info-container\">\n        <div class=\"my-info\">\n            <img :src=\"techInfo.massagist_photo\" class=\"head\">\n            <div class=\"info mui-ellipsis\">\n                <h1>工号：{{techInfo.massagist_no}}</h1>\n                <h2>年龄：{{techInfo.massagist_birth || '未设置' | years}}</h2>\n                <h3>\n                    <start :value='techInfo.massagist_store'></start>{{techInfo.massagist_store}}分\n                </h3>\n            </div>\n            <div class=\"info\">\n                <h1>昵称：{{techInfo.massagist_name}}</h1>\n                <h2>籍贯：{{techInfo.c_name}}</h2>\n                <h3><a class=\"pj\" v-link=\"{path: '/consumer-tech-comment/' + this.$route.params.token + '/' + this.$route.params.storeid + '/' + techInfo.massagist_id}\">评价 ({{techInfo.comment_total}})</a></h3>\n            </div>\n        </div>\n    </div>\n\n    <ul class=\"mui-table-view item-container\">\n        <li class=\"mui-table-view-cell mui-media\" v-for=\"item in proList\">\n            <a v-link=\"{path: '/consumer-pro/' + this.$route.params.token + '/' + this.$route.params.storeid + '/' + item.item_id}\">\n                <div class=\"mui-media-object mui-pull-left image-size\" style=\"background-image: url({{$store.state.URL + item.item_image}});\"></div>\n                <!-- <img class=\"mui-media-object mui-pull-left\" :src=\"$store.state.URL + item.item_image\"> -->\n                <div class=\"mui-media-body\">\n                    {{item.item_name}}\n                    <p class=\"mui-ellipsis\">套餐详情：{{item.item_intro}}</p>\n                    <p>项目时长：{{item.item_time}}分钟</p>\n                    <p><em class=\"price\">￥{{item.item_vipprice}}</em> <em class=\"disguise\">非会员：￥{{item.item_price}}</em></p>\n                </div>\n            </a>\n            <a class=\"xuanze\" v-if=\"techInfo.massagist_status == '2'\" v-link=\"{path: '/consumer-order-sub/' + this.$route.params.token + '/' + this.$route.params.storeid + '/' + item.item_id + '/' + this.$route.params.id}\">选择</a>\n            <a class=\"xuanze sleep\" v-if=\"techInfo.massagist_status != '2'\" @click=\"$store.state.showTip({type: 'error', content: '选择的技师休息或正在工作中' })\">选择</a>\n        </li>\n        <div class=\"no-data\" v-if=\"techList.proList<=0\">\n            <h1>技师还没有相关项目</h1>\n        </div>\n    </ul>\n    </div></div>\n    <!-- <div class=\"footer\">\n        <a>生成订单</a>\n    </div> -->\n    \n    \n</div>\n";

/***/ },
/* 153 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(154)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/consumer-order-sub.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(155)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-17fbfdf6/consumer-order-sub.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 154 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	
	var _start = __webpack_require__(44);
	
	var _start2 = _interopRequireDefault(_start);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	exports.default = {
	    name: 'consumerOrderSub',
	    data: function data() {
	        return {
	            htmlInfo: {},
	            serType: 0,
	            roomStorey: 0,
	            roomNo: 0,
	            roomInfo: '',
	            roomNoList: [],
	            handList: [],
	            handId: 0,
	            info: ''
	        };
	    },
	    watch: {
	        'roomStorey': function roomStorey() {
	            var _this = this;
	
	            if (this.roomStorey == 0) return;
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.getRoomNum, {
	                sid: this.$route.params.storeid,
	                floor: this.roomStorey
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this.$store.state.hideTip();
	                _this.roomNoList = data.data.list;
	            });
	        }
	    },
	    ready: function ready() {
	        var _this2 = this;
	
	        document.title = '提交订单';
	        mui('.mui-scroll-wrapper').scroll({ bounce: false });
	        this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	        this.$http.post(API.generateOrderHtml, {
	            sid: this.$route.params.storeid,
	            pid: this.$route.params.pid,
	            tid: this.$route.params.tid
	        }).then(function (data) {
	            if (data.data.errorInfo) return;
	            _this2.$store.state.hideTip();
	            _this2.htmlInfo = data.data.list;
	            if (_this2.htmlInfo.technician_info.massagist_photo.indexOf('http') < 0) {
	                _this2.htmlInfo.technician_info.massagist_photo = _this2.$store.state.URL + _this2.htmlInfo.technician_info.massagist_photo;
	            }
	        });
	        this.$http.post(API.userGetHandSelect, {
	            sid: this.$route.params.storeid
	        }).then(function (data) {
	            if (data.data.errorInfo) return;
	            _this2.$store.state.hideTip();
	            _this2.handList = data.data.list;
	        });
	    },
	    methods: {
	        local: function local(name) {
	            this.$router.go('/' + name + '/' + this.$route.params.token);
	        },
	        submit: function submit() {
	            var _this3 = this;
	
	            if (this.roomNo == 0 && this.roomInfo.length <= 0) {
	                this.$store.state.showTip({ type: 'error', content: '请输入正确的房间号' });
	                return;
	            }
	            this.$store.state.showTip({ type: 'loading', content: '提交中...' });
	            var _data = {
	                belongs: this.$route.params.storeid,
	                projectid: this.$route.params.pid,
	                massagist: this.$route.params.tid,
	                address: this.serType,
	                info: this.info
	            };
	            this.serType == 0 ? _data.room = this.roomNo : _data.room_num = this.roomInfo;
	            if (this.handId != 0) _data.hand = this.handId;
	            this.$http.post(API.userSubmitOrder, _data).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this3.$store.state.hideTip();
	                _this3.$store.state.showTip({ type: 'success', content: '提交成功' });
	                var socket = new WebSocket("ws://121.43.163.207:9509");
	                var self = _this3;
	                socket.onopen = function (event) {
	                    socket.send('{"store": "' + self.$route.params.storeid + '","fun1":"1","fun2":"2","fun3":"3"}');
	                };
	                socket.onmessage = function (event) {
	                    console.log(event.data);
	                };
	                setTimeout(function () {
	                    _this3.$router.go('/consumer-order-list/' + _this3.$route.params.token + '/' + _this3.$route.params.storeid);
	                }, 1500);
	            });
	        }
	    },
	    components: {
	        Start: _start2.default
	    }
	};
	
	// </script>
	/* generated by vue-loader */
	// <template>
	// <div class="page consumer">
	//     <header id="header" class="mui-bar mui-bar-nav haed2 ">
	//         <h1 class="mui-title">提交订单</h1>
	//         <a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a>
	//     </header>
	//     <div class="mui-scroll-wrapper" style="bottom: 0px;">
	//     <div class="mui-scroll">
	//     <div class="shop-title-container order">
	//         <img :src="$store.state.URL + htmlInfo.project_info.item_image">
	//         <h1 style="width: 80%;">{{htmlInfo.project_info.item_name}}</h1>
	//         <h5>套餐详情：{{htmlInfo.project_info.item_intro}}</h5>
	//         <div class="price">
	//             <em>￥{{htmlInfo.project_info.item_price}}</em>
	//         </div>
	//     </div>
	//     <div class="order-tech">
	//         <img :src="htmlInfo.technician_info.massagist_photo">
	//         <h1>工号：{{htmlInfo.technician_info.massagist_no}}&nbsp;&nbsp;&nbsp;&nbsp;昵称：{{htmlInfo.technician_info.massagist_name}}</h1>
	//     </div>
	//
	//     <form class="order-form select-working">
	//         <div class="mui-input-row mui-radio mui-left {{serType==0 ? 'show-color' : ''}}">
	//             <label>在店消费</label>
	//             <input name="radio1" type="radio" v-model="serType" value="0">
	//         </div>
	//         <div class="mui-input-row mui-radio mui-left {{serType==1 ? 'show-color' : ''}}">
	//             <label>技师上门</label>
	//             <input name="radio1" type="radio" v-model="serType" value="1">
	//         </div>
	//         <section v-show="serType == 0">
	//             手牌号：
	//             <select v-model="handId" style="color: #{{handId == 0? '999' : '333'}};">
	//                 <option value="0" disabled selected style='display:none;'>手牌</option>
	//                 <option v-for="item in handList" value="{{item.value}}" :selected="$index == 0">{{item.text}}</option>
	//             </select>
	//             <br>
	//             房间号：
	//             <select v-model="roomStorey" style="color: #{{roomStorey == 0? '999' : '333'}};">
	//                 <option value="0" disabled selected style='display:none;'>楼层</option>
	//                 <option v-for="item in htmlInfo.room_info" value="{{item}}" :selected="$index == 0">{{item}}楼</option>
	//             </select>
	//             <select v-model="roomNo" style="color: #{{roomNo == 0? '999' : '333'}};">
	//                 <option value="0" disabled selected style='display:none;'>房间号</option>
	//                 <option v-for="item in roomNoList" value="{{item.id}}">{{item.value}}</option>
	//             </select>
	//         </section>
	//         <section v-show="serType == 1">
	//             房间号：
	//             <input type="text" style="width: 150px;" v-model="roomInfo">
	//         </section>
	//         <section>　备注：<input type="text" placeholder="请输入您要备注的信息" v-model="info"></section>
	//     </form>
	//
	//     <div class="order-foot">
	//         <h3>订单总价：<span>￥{{htmlInfo.total_price}}</span></h3>
	//         <button type="button" class="mui-btn mui-btn-warning mui-btn-block" @click="submit">提交订单</button>
	//     </div>
	//     </div></div>
	// </div>
	// </template>
	//
	// <script>

/***/ },
/* 155 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page consumer\">\n    <header id=\"header\" class=\"mui-bar mui-bar-nav haed2 \">\n        <h1 class=\"mui-title\">提交订单</h1>\n        <a class=\"mui-action-back mui-icon mui-icon-left-nav mui-pull-left\"></a>\n    </header>\n    <div class=\"mui-scroll-wrapper\" style=\"bottom: 0px;\">\n    <div class=\"mui-scroll\">\n    <div class=\"shop-title-container order\">\n        <img :src=\"$store.state.URL + htmlInfo.project_info.item_image\">\n        <h1 style=\"width: 80%;\">{{htmlInfo.project_info.item_name}}</h1>\n        <h5>套餐详情：{{htmlInfo.project_info.item_intro}}</h5>\n        <div class=\"price\">\n            <em>￥{{htmlInfo.project_info.item_price}}</em>\n        </div>\n    </div>\n    <div class=\"order-tech\">\n        <img :src=\"htmlInfo.technician_info.massagist_photo\">\n        <h1>工号：{{htmlInfo.technician_info.massagist_no}}&nbsp;&nbsp;&nbsp;&nbsp;昵称：{{htmlInfo.technician_info.massagist_name}}</h1>\n    </div>\n\n    <form class=\"order-form select-working\">\n        <div class=\"mui-input-row mui-radio mui-left {{serType==0 ? 'show-color' : ''}}\">\n            <label>在店消费</label>\n            <input name=\"radio1\" type=\"radio\" v-model=\"serType\" value=\"0\">\n        </div>\n        <div class=\"mui-input-row mui-radio mui-left {{serType==1 ? 'show-color' : ''}}\">\n            <label>技师上门</label>\n            <input name=\"radio1\" type=\"radio\" v-model=\"serType\" value=\"1\">\n        </div>\n        <section v-show=\"serType == 0\">\n            手牌号：\n            <select v-model=\"handId\" style=\"color: #{{handId == 0? '999' : '333'}};\">\n                <option value=\"0\" disabled selected style='display:none;'>手牌</option>\n                <option v-for=\"item in handList\" value=\"{{item.value}}\" :selected=\"$index == 0\">{{item.text}}</option>\n            </select>\n            <br>\n            房间号：\n            <select v-model=\"roomStorey\" style=\"color: #{{roomStorey == 0? '999' : '333'}};\">\n                <option value=\"0\" disabled selected style='display:none;'>楼层</option>\n                <option v-for=\"item in htmlInfo.room_info\" value=\"{{item}}\" :selected=\"$index == 0\">{{item}}楼</option>\n            </select>\n            <select v-model=\"roomNo\" style=\"color: #{{roomNo == 0? '999' : '333'}};\">\n                <option value=\"0\" disabled selected style='display:none;'>房间号</option>\n                <option v-for=\"item in roomNoList\" value=\"{{item.id}}\">{{item.value}}</option>\n            </select>\n        </section>\n        <section v-show=\"serType == 1\">\n            房间号：\n            <input type=\"text\" style=\"width: 150px;\" v-model=\"roomInfo\">\n        </section>\n        <section>　备注：<input type=\"text\" placeholder=\"请输入您要备注的信息\" v-model=\"info\"></section>\n    </form>\n    \n    <div class=\"order-foot\">\n        <h3>订单总价：<span>￥{{htmlInfo.total_price}}</span></h3>\n        <button type=\"button\" class=\"mui-btn mui-btn-warning mui-btn-block\" @click=\"submit\">提交订单</button>\n    </div>\n    </div></div>\n</div>\n";

/***/ },
/* 156 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(157)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/consumer-tech-comment.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(158)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-42014cd8/consumer-tech-comment.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 157 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	
	var _start = __webpack_require__(44);
	
	var _start2 = _interopRequireDefault(_start);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	exports.default = {
	    name: 'consumerTechCom',
	    data: function data() {
	        return {
	            techInfo: {},
	            active: 'all',
	            info: {},
	            all: {
	                list: [],
	                page: 0,
	                btn: false
	            },
	            bad: {
	                list: [],
	                page: 0,
	                btn: false
	            },
	            good: {
	                list: [],
	                page: 0,
	                btn: false
	            },
	            midd: {
	                list: [],
	                page: 0,
	                btn: false
	            }
	        };
	    },
	    ready: function ready() {
	        var _this = this;
	
	        document.title = '技师评价';
	        mui('.mui-scroll-wrapper').scroll({ bounce: false });
	        this.$http.post(API.getMassagistInfo, {
	            store_id: this.$route.params.storeid,
	            tid: this.$route.params.id
	
	        }).then(function (data) {
	            if (data.data.errorInfo) return;
	            _this.$store.state.hideTip();
	            _this.techInfo = data.data.object;
	            _this.techInfo.massagist_store = parseInt(_this.techInfo.massagist_store);
	            if (_this.techInfo.massagist_photo.indexOf('http') < 0) {
	                _this.techInfo.massagist_photo = _this.$store.state.URL + _this.techInfo.massagist_photo;
	            }
	        });
	        this.getAll();
	        this.getGood();
	        this.getMidd();
	        this.getBad();
	    },
	    methods: {
	        local: function local(name) {
	            this.$router.go('/' + name + '/' + this.$route.params.token);
	        },
	        getAll: function getAll() {
	            var _this2 = this;
	
	            this.all.page++;
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.getTechnicianEvaluate, {
	                store: this.$route.params.storeid,
	                tid: this.$route.params.id,
	                page: this.all.page
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this2.$store.state.hideTip();
	                if (!data.data.list) {
	                    _this2.all.btn = false;
	                    return;
	                } else if (data.data.list.length < _this2.$store.state.pageSize) {
	                    _this2.all.btn = false;
	                } else {
	                    _this2.all.btn = true;
	                };
	                _this2.all.list = _this2.all.list.concat(data.data.list);
	                _this2.info = data.data.object;
	                for (var i = 0; i < _this2.all.list.length; i++) {
	                    if (!_this2.all.list[i].wechat_user_viptime) {
	                        _this2.all.list[i].wechat_userlevel = 0;
	                    } else if (_this2.all.list[i].wechat_user_viptime == '1') {
	                        _this2.all.list[i].wechat_userlevel = 7;
	                    } else if (_this2.all.list[i].wechat_user_viptime - Date.parse(new Date()) / 1000 <= 0) {
	                        _this2.all.list[i].wechat_userlevel = 0;
	                    } else {
	                        _this2.all.list[i].wechat_userlevel = 1;
	                    }
	                    if (_this2.all.list[i].wechat_headimgurl.indexOf('http') < 0) {
	                        _this2.all.list[i].wechat_headimgurl = _this2.$store.state.URL + _this2.all.list[i].wechat_headimgurl;
	                    }
	                }
	            });
	        },
	        getGood: function getGood() {
	            var _this3 = this;
	
	            this.good.page++;
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.getTechnicianEvaluate, {
	                store: this.$route.params.storeid,
	                tid: this.$route.params.id,
	                page: this.good.page,
	                type: 1
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this3.$store.state.hideTip();
	                if (!data.data.list) {
	                    _this3.good.btn = false;
	                    return;
	                } else if (data.data.list.length < _this3.$store.state.pageSize) {
	                    _this3.good.btn = false;
	                } else {
	                    _this3.good.btn = true;
	                };
	                _this3.good.list = _this3.good.list.concat(data.data.list);
	                _this3.info = data.data.object;
	                for (var i = 0; i < _this3.good.list.length; i++) {
	                    if (!_this3.good.list[i].wechat_user_viptime) {
	                        _this3.good.list[i].wechat_userlevel = 0;
	                    } else if (_this3.good.list[i].wechat_user_viptime == '1') {
	                        _this3.good.list[i].wechat_userlevel = 7;
	                    } else if (_this3.good.list[i].wechat_user_viptime - Date.parse(new Date()) / 1000 <= 0) {
	                        _this3.good.list[i].wechat_userlevel = 0;
	                    } else {
	                        _this3.good.list[i].wechat_userlevel = 1;
	                    }
	                    if (_this3.good.list[i].wechat_headimgurl.indexOf('http') < 0) {
	                        _this3.good.list[i].wechat_headimgurl = _this3.$store.state.URL + _this3.good.list[i].wechat_headimgurl;
	                    }
	                }
	            });
	        },
	        getMidd: function getMidd() {
	            var _this4 = this;
	
	            this.midd.page++;
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.getTechnicianEvaluate, {
	                store: this.$route.params.storeid,
	                tid: this.$route.params.id,
	                page: this.midd.page,
	                type: 2
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this4.$store.state.hideTip();
	                if (!data.data.list) {
	                    _this4.midd.btn = false;
	                    return;
	                } else if (data.data.list.length < _this4.$store.state.pageSize) {
	                    _this4.midd.btn = false;
	                } else {
	                    _this4.midd.btn = true;
	                };
	                _this4.midd.list = _this4.midd.list.concat(data.data.list);
	                _this4.info = data.data.object;
	                for (var i = 0; i < _this4.midd.list.length; i++) {
	                    if (!_this4.midd.list[i].wechat_user_viptime) {
	                        _this4.midd.list[i].wechat_userlevel = 0;
	                    } else if (_this4.midd.list[i].wechat_user_viptime == '1') {
	                        _this4.midd.list[i].wechat_userlevel = 7;
	                    } else if (_this4.midd.list[i].wechat_user_viptime - Date.parse(new Date()) / 1000 <= 0) {
	                        _this4.midd.list[i].wechat_userlevel = 0;
	                    } else {
	                        _this4.midd.list[i].wechat_userlevel = 1;
	                    }
	                    if (_this4.midd.list[i].wechat_headimgurl.indexOf('http') < 0) {
	                        _this4.midd.list[i].wechat_headimgurl = _this4.$store.state.URL + _this4.midd.list[i].wechat_headimgurl;
	                    }
	                }
	            });
	        },
	        getBad: function getBad() {
	            var _this5 = this;
	
	            this.bad.page++;
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.getTechnicianEvaluate, {
	                store: this.$route.params.storeid,
	                tid: this.$route.params.id,
	                page: this.bad.page,
	                type: 3
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this5.$store.state.hideTip();
	                if (!data.data.list) {
	                    _this5.bad.btn = false;
	                    return;
	                } else if (data.data.list.length < _this5.$store.state.pageSize) {
	                    _this5.bad.btn = false;
	                } else {
	                    _this5.bad.btn = true;
	                };
	                _this5.bad.list = _this5.bad.list.concat(data.data.list);
	                _this5.info = data.data.object;
	                for (var i = 0; i < _this5.bad.list.length; i++) {
	                    if (!_this5.bad.list[i].wechat_user_viptime) {
	                        _this5.bad.list[i].wechat_userlevel = 0;
	                    } else if (_this5.bad.list[i].wechat_user_viptime == '1') {
	                        _this5.bad.list[i].wechat_userlevel = 7;
	                    } else if (_this5.bad.list[i].wechat_user_viptime - Date.parse(new Date()) / 1000 <= 0) {
	                        _this5.bad.list[i].wechat_userlevel = 0;
	                    } else {
	                        _this5.bad.list[i].wechat_userlevel = 1;
	                    }
	                    if (_this5.bad.list[i].wechat_headimgurl.indexOf('http') < 0) {
	                        _this5.bad.list[i].wechat_headimgurl = _this5.$store.state.URL + _this5.bad.list[i].wechat_headimgurl;
	                    }
	                }
	            });
	        }
	    },
	    components: {
	        Start: _start2.default
	    }
	};
	
	// </script>
	/* generated by vue-loader */
	// <template>
	// <div class="page consumer">
	//     <header id="header" class="mui-bar mui-bar-nav haed2 ">
	//         <h1 class="mui-title">技师评价</h1>
	//         <a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a>
	//     </header>
	//     <div class="mui-scroll-wrapper" style="bottom: 0px;">
	//     <div class="mui-scroll">
	//     <div class="my-info-container fade-in-left no-border">
	//         <div class="my-info">
	//             <img :src="techInfo.massagist_photo" class="head">
	//             <div class="info mui-ellipsis">
	//                 <h1>工号：{{techInfo.massagist_no}}</h1>
	//                 <h2>年龄：{{techInfo.massagist_birth || '未设置' | years}}</h2>
	//                 <h3>
	//                     <start :value='parseInt(techInfo.massagist_store)'></start>{{parseInt(techInfo.massagist_store)}}分
	//                 </h3>
	//             </div>
	//             <div class="info">
	//                 <h1>昵称：{{techInfo.massagist_name}}</h1>
	//                 <h2>籍贯：{{techInfo.c_name}}</h2>
	//             </div>
	//         </div>
	//     </div>
	//
	//     <div id="sliderSegmentedControl" class="mui-slider-indicator mui-segmented-control mui-segmented-control-inverted">
	//         <a class="mui-control-item mui-active" href="#item1">全部({{info ? info.all : '0'}})</a>
	//         <a class="mui-control-item" href="#item2">好评({{info ? info.good : '0'}})</a>
	//         <a class="mui-control-item" href="#item3">中评({{info ? info.middle : '0'}})</a>
	//         <a class="mui-control-item" href="#item4">差评({{info ? info.bad : '0'}})</a>
	//     </div>
	//
	//     <div id="item1" class="mui-control-content fade-in-left mui-active">
	//         <ul class="comment-item">
	//             <li v-for="item in all.list">
	//                 <div class="item">
	//                     <img :src="item.wechat_headimgurl" class="head">
	//                     {{item.wechat_nickname}} <i class="lv lv{{item.wechat_userlevel}}"></i><br>
	//                     <div class="starts-container"><start :value="parseInt(item.comment_score_store)"><start></div>
	//                     <h6>
	//                         {{item.comment_time | format 'yyyy-MM-dd'}}
	//                     </h6>
	//                     <div class="tag">
	//                         <a v-for="tag in item.comment_store_label" v-if="tag!=''">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>
	//                     </div>
	//                     <p>{{item.comment}}</p>
	//                     <small v-for="reply in item.reply">
	//                         <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>
	//                         {{reply.comment}}
	//                         <br>
	//                     </small>
	//                 </div>
	//             </li>
	//             <div class="no-data" v-if="all.list.length<=0">
	//                 <h1>没有相关评论</h1>
	//             </div>
	//             <a class="btn-more" v-if="all.btn" @click="getAll">查看更多</a>
	//         </ul>
	//     </div>
	//
	//     <div id="item2" class="mui-control-content fade-in-left">
	//         <ul class="comment-item">
	//             <li v-for="item in good.list" v-if="item.comment_level == '1'">
	//                 <div class="item">
	//                     <img :src="item.wechat_headimgurl" class="head">
	//                     {{item.wechat_nickname}} <i class="lv lv{{item.wechat_userlevel}}"></i><br>
	//                     <div class="starts-container"><start :value="parseInt(item.comment_score_store)"><start></div>
	//                     <h6>{{item.comment_time | format 'yyyy-MM-dd'}}</h6>
	//                     <div class="tag">
	//                         <a v-for="tag in item.comment_store_label" v-if="tag!=''">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>
	//                     </div>
	//                     <p>{{item.comment}}</p>
	//                     <small v-for="reply in item.reply">
	//                         <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>
	//                         {{reply.comment}}
	//                         <br>
	//                     </small>
	//                 </div>
	//             </li>
	//             <div class="no-data" v-if="good.list.length<=0">
	//                 <h1>没有相关评论</h1>
	//             </div>
	//             <a class="btn-more" v-if="good.btn" @click="getGood">查看更多</a>
	//         </ul>
	//     </div>
	//
	//     <div id="item3" class="mui-control-content fade-in-left">
	//         <ul class="comment-item">
	//             <li v-for="item in midd.list" v-if="item.comment_level == '2'">
	//                 <div class="item">
	//                     <img :src="item.wechat_headimgurl" class="head">
	//                     {{item.wechat_nickname}} <i class="lv lv{{item.wechat_userlevel}}"></i><br>
	//                     <div class="starts-container"><start :value="parseInt(item.comment_score_store)"><start></div>
	//                     <h6>{{item.comment_time | format 'yyyy-MM-dd'}}</h6>
	//                     <div class="tag">
	//                         <a v-for="tag in item.comment_store_label" v-if="tag!=''">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>
	//                     </div>
	//                     <p>{{item.comment}}</p>
	//                     <small v-for="reply in item.reply">
	//                         <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>
	//                         {{reply.comment}}
	//                         <br>
	//                     </small>
	//                 </div>
	//             </li>
	//             <div class="no-data" v-if="midd.list.length<=0">
	//                 <h1>没有相关评论</h1>
	//             </div>
	//             <a class="btn-more" v-if="midd.btn" @click="getMidd">查看更多</a>
	//         </ul>
	//     </div>
	//
	//     <div id="item4" class="mui-control-content fade-in-left">
	//         <ul class="comment-item">
	//             <li v-for="item in bad.list" v-if="item.comment_level == '3'">
	//                 <div class="item">
	//                     <img :src="item.wechat_headimgurl" class="head">
	//                     {{item.wechat_nickname}} <i class="lv lv{{item.wechat_userlevel}}"></i><br>
	//                     <div class="starts-container"><start :value="parseInt(item.comment_score_store)"><start></div>
	//                     <h6>{{item.comment_time | format 'yyyy-MM-dd'}}</h6>
	//                     <div class="tag">
	//                         <a v-for="tag in item.comment_store_label" v-if="tag!=''">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>
	//                     </div>
	//                     <p>{{item.comment}}</p>
	//                     <small v-for="reply in item.reply">
	//                         <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>
	//                         {{reply.comment}}
	//                         <br>
	//                     </small>
	//                 </div>
	//             </li>
	//             <div class="no-data" v-if="bad.list.length<=0">
	//                 <h1>没有相关评论</h1>
	//             </div>
	//             <a class="btn-more" v-if="bad.btn" @click="getBad">查看更多</a>
	//         </ul>
	//     </div>
	//
	//     </div></div>
	//
	// </div>
	// </template>
	//
	// <script>

/***/ },
/* 158 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page consumer\">\n    <header id=\"header\" class=\"mui-bar mui-bar-nav haed2 \">\n        <h1 class=\"mui-title\">技师评价</h1>\n        <a class=\"mui-action-back mui-icon mui-icon-left-nav mui-pull-left\"></a>\n    </header>\n    <div class=\"mui-scroll-wrapper\" style=\"bottom: 0px;\">\n    <div class=\"mui-scroll\">\n    <div class=\"my-info-container fade-in-left no-border\">\n        <div class=\"my-info\">\n            <img :src=\"techInfo.massagist_photo\" class=\"head\">\n            <div class=\"info mui-ellipsis\">\n                <h1>工号：{{techInfo.massagist_no}}</h1>\n                <h2>年龄：{{techInfo.massagist_birth || '未设置' | years}}</h2>\n                <h3>\n                    <start :value='parseInt(techInfo.massagist_store)'></start>{{parseInt(techInfo.massagist_store)}}分\n                </h3>\n            </div>\n            <div class=\"info\">\n                <h1>昵称：{{techInfo.massagist_name}}</h1>\n                <h2>籍贯：{{techInfo.c_name}}</h2>\n            </div>\n        </div>\n    </div>\n    \n    <div id=\"sliderSegmentedControl\" class=\"mui-slider-indicator mui-segmented-control mui-segmented-control-inverted\">\n        <a class=\"mui-control-item mui-active\" href=\"#item1\">全部({{info ? info.all : '0'}})</a>\n        <a class=\"mui-control-item\" href=\"#item2\">好评({{info ? info.good : '0'}})</a>\n        <a class=\"mui-control-item\" href=\"#item3\">中评({{info ? info.middle : '0'}})</a>\n        <a class=\"mui-control-item\" href=\"#item4\">差评({{info ? info.bad : '0'}})</a>\n    </div>\n    \n    <div id=\"item1\" class=\"mui-control-content fade-in-left mui-active\">\n        <ul class=\"comment-item\">\n            <li v-for=\"item in all.list\">\n                <div class=\"item\">\n                    <img :src=\"item.wechat_headimgurl\" class=\"head\">\n                    {{item.wechat_nickname}} <i class=\"lv lv{{item.wechat_userlevel}}\"></i><br>\n                    <div class=\"starts-container\"><start :value=\"parseInt(item.comment_score_store)\"><start></div>\n                    <h6>\n                        {{item.comment_time | format 'yyyy-MM-dd'}}\n                    </h6>\n                    <div class=\"tag\">\n                        <a v-for=\"tag in item.comment_store_label\" v-if=\"tag!=''\">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>\n                    </div>\n                    <p>{{item.comment}}</p>\n                    <small v-for=\"reply in item.reply\">\n                        <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>\n                        {{reply.comment}}\n                        <br>\n                    </small>\n                </div>\n            </li>\n            <div class=\"no-data\" v-if=\"all.list.length<=0\">\n                <h1>没有相关评论</h1>\n            </div>\n            <a class=\"btn-more\" v-if=\"all.btn\" @click=\"getAll\">查看更多</a>\n        </ul>\n    </div>\n\n    <div id=\"item2\" class=\"mui-control-content fade-in-left\">\n        <ul class=\"comment-item\">\n            <li v-for=\"item in good.list\" v-if=\"item.comment_level == '1'\">\n                <div class=\"item\">\n                    <img :src=\"item.wechat_headimgurl\" class=\"head\">\n                    {{item.wechat_nickname}} <i class=\"lv lv{{item.wechat_userlevel}}\"></i><br>\n                    <div class=\"starts-container\"><start :value=\"parseInt(item.comment_score_store)\"><start></div>\n                    <h6>{{item.comment_time | format 'yyyy-MM-dd'}}</h6>\n                    <div class=\"tag\">\n                        <a v-for=\"tag in item.comment_store_label\" v-if=\"tag!=''\">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>\n                    </div>\n                    <p>{{item.comment}}</p>\n                    <small v-for=\"reply in item.reply\">\n                        <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>\n                        {{reply.comment}}\n                        <br>\n                    </small>\n                </div>\n            </li>\n            <div class=\"no-data\" v-if=\"good.list.length<=0\">\n                <h1>没有相关评论</h1>\n            </div>\n            <a class=\"btn-more\" v-if=\"good.btn\" @click=\"getGood\">查看更多</a>\n        </ul>\n    </div>\n\n    <div id=\"item3\" class=\"mui-control-content fade-in-left\">\n        <ul class=\"comment-item\">\n            <li v-for=\"item in midd.list\" v-if=\"item.comment_level == '2'\">\n                <div class=\"item\">\n                    <img :src=\"item.wechat_headimgurl\" class=\"head\">\n                    {{item.wechat_nickname}} <i class=\"lv lv{{item.wechat_userlevel}}\"></i><br>\n                    <div class=\"starts-container\"><start :value=\"parseInt(item.comment_score_store)\"><start></div>\n                    <h6>{{item.comment_time | format 'yyyy-MM-dd'}}</h6>\n                    <div class=\"tag\">\n                        <a v-for=\"tag in item.comment_store_label\" v-if=\"tag!=''\">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>\n                    </div>\n                    <p>{{item.comment}}</p>\n                    <small v-for=\"reply in item.reply\">\n                        <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>\n                        {{reply.comment}}\n                        <br>\n                    </small>\n                </div>\n            </li>\n            <div class=\"no-data\" v-if=\"midd.list.length<=0\">\n                <h1>没有相关评论</h1>\n            </div>\n            <a class=\"btn-more\" v-if=\"midd.btn\" @click=\"getMidd\">查看更多</a>\n        </ul>\n    </div>\n\n    <div id=\"item4\" class=\"mui-control-content fade-in-left\">\n        <ul class=\"comment-item\">\n            <li v-for=\"item in bad.list\" v-if=\"item.comment_level == '3'\">\n                <div class=\"item\">\n                    <img :src=\"item.wechat_headimgurl\" class=\"head\">\n                    {{item.wechat_nickname}} <i class=\"lv lv{{item.wechat_userlevel}}\"></i><br>\n                    <div class=\"starts-container\"><start :value=\"parseInt(item.comment_score_store)\"><start></div>\n                    <h6>{{item.comment_time | format 'yyyy-MM-dd'}}</h6>\n                    <div class=\"tag\">\n                        <a v-for=\"tag in item.comment_store_label\" v-if=\"tag!=''\">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>\n                    </div>\n                    <p>{{item.comment}}</p>\n                    <small v-for=\"reply in item.reply\">\n                        <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>\n                        {{reply.comment}}\n                        <br>\n                    </small>\n                </div>\n            </li>\n            <div class=\"no-data\" v-if=\"bad.list.length<=0\">\n                <h1>没有相关评论</h1>\n            </div>\n            <a class=\"btn-more\" v-if=\"bad.btn\" @click=\"getBad\">查看更多</a>\n        </ul>\n    </div>\n\n    </div></div>\n    \n</div>\n";

/***/ },
/* 159 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(160)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/consumer-shop-comment.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(161)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-157d61f4/consumer-shop-comment.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 160 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	
	var _start = __webpack_require__(44);
	
	var _start2 = _interopRequireDefault(_start);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	exports.default = {
	    name: 'consumerShopCom',
	    data: function data() {
	        return {
	            info: {},
	            all: {
	                list: [],
	                page: 0,
	                btn: false
	            },
	            bad: {
	                list: [],
	                page: 0,
	                btn: false
	            },
	            good: {
	                list: [],
	                page: 0,
	                btn: false
	            },
	            midd: {
	                list: [],
	                page: 0,
	                btn: false
	            }
	        };
	    },
	    ready: function ready() {
	        document.title = '店铺评价';
	        mui('.mui-scroll-wrapper').scroll({ bounce: false });
	        this.getAll();
	        this.getGood();
	        this.getMidd();
	        this.getBad();
	    },
	    methods: {
	        local: function local(name) {
	            this.$router.go('/' + name + '/' + this.$route.params.token);
	        },
	        getAll: function getAll() {
	            var _this = this;
	
	            this.all.page++;
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.getTechnicianEvaluate, {
	                store: this.$route.params.storeid,
	                page: this.all.page
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this.$store.state.hideTip();
	                if (!data.data.list) {
	                    _this.all.btn = false;
	                    return;
	                } else if (data.data.list.length < _this.$store.state.pageSize) {
	                    _this.all.btn = false;
	                } else {
	                    _this.all.btn = true;
	                };
	                _this.all.list = _this.all.list.concat(data.data.list);
	                _this.info = data.data.object;
	                for (var i = 0; i < _this.all.list.length; i++) {
	                    if (!_this.all.list[i].wechat_user_viptime) {
	                        _this.all.list[i].wechat_userlevel = 0;
	                    } else if (_this.all.list[i].wechat_user_viptime == '1') {
	                        _this.all.list[i].wechat_userlevel = 7;
	                    } else if (_this.all.list[i].wechat_user_viptime - Date.parse(new Date()) / 1000 <= 0) {
	                        _this.all.list[i].wechat_userlevel = 0;
	                    } else {
	                        _this.all.list[i].wechat_userlevel = 1;
	                    }
	                    if (_this.all.list[i].wechat_headimgurl.indexOf('http') < 0) {
	                        _this.all.list[i].wechat_headimgurl = _this.$store.state.URL + _this.all.list[i].wechat_headimgurl;
	                    }
	                }
	            });
	        },
	        getGood: function getGood() {
	            var _this2 = this;
	
	            this.good.page++;
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.getTechnicianEvaluate, {
	                store: this.$route.params.storeid,
	                page: this.good.page,
	                type: 1
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this2.$store.state.hideTip();
	                if (!data.data.list) {
	                    _this2.good.btn = false;
	                    return;
	                } else if (data.data.list.length < _this2.$store.state.pageSize) {
	                    _this2.good.btn = false;
	                } else {
	                    _this2.good.btn = true;
	                };
	                _this2.good.list = _this2.good.list.concat(data.data.list);
	                _this2.info = data.data.object;
	                for (var i = 0; i < _this2.good.list.length; i++) {
	                    if (!_this2.good.list[i].wechat_user_viptime) {
	                        _this2.good.list[i].wechat_userlevel = 0;
	                    } else if (_this2.good.list[i].wechat_user_viptime == '1') {
	                        _this2.good.list[i].wechat_userlevel = 7;
	                    } else if (_this2.good.list[i].wechat_user_viptime - Date.parse(new Date()) / 1000 <= 0) {
	                        _this2.good.list[i].wechat_userlevel = 0;
	                    } else {
	                        _this2.good.list[i].wechat_userlevel = 1;
	                    }
	                    if (_this2.good.list[i].wechat_headimgurl.indexOf('http') < 0) {
	                        _this2.good.list[i].wechat_headimgurl = _this2.$store.state.URL + _this2.good.list[i].wechat_headimgurl;
	                    }
	                }
	            });
	        },
	        getMidd: function getMidd() {
	            var _this3 = this;
	
	            this.midd.page++;
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.getTechnicianEvaluate, {
	                store: this.$route.params.storeid,
	                page: this.midd.page,
	                type: 2
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this3.$store.state.hideTip();
	                if (!data.data.list) {
	                    _this3.midd.btn = false;
	                    return;
	                } else if (data.data.list.length < _this3.$store.state.pageSize) {
	                    _this3.midd.btn = false;
	                } else {
	                    _this3.midd.btn = true;
	                };
	                _this3.midd.list = _this3.midd.list.concat(data.data.list);
	                _this3.info = data.data.object;
	                for (var i = 0; i < _this3.midd.list.length; i++) {
	                    if (!_this3.midd.list[i].wechat_user_viptime) {
	                        _this3.midd.list[i].wechat_userlevel = 0;
	                    } else if (_this3.midd.list[i].wechat_user_viptime == '1') {
	                        _this3.midd.list[i].wechat_userlevel = 7;
	                    } else if (_this3.midd.list[i].wechat_user_viptime - Date.parse(new Date()) / 1000 <= 0) {
	                        _this3.midd.list[i].wechat_userlevel = 0;
	                    } else {
	                        _this3.midd.list[i].wechat_userlevel = 1;
	                    }
	                    if (_this3.midd.list[i].wechat_headimgurl.indexOf('http') < 0) {
	                        _this3.midd.list[i].wechat_headimgurl = _this3.$store.state.URL + _this3.midd.list[i].wechat_headimgurl;
	                    }
	                }
	            });
	        },
	        getBad: function getBad() {
	            var _this4 = this;
	
	            this.bad.page++;
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.getTechnicianEvaluate, {
	                store: this.$route.params.storeid,
	                page: this.bad.page,
	                type: 3
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this4.$store.state.hideTip();
	                if (!data.data.list) {
	                    _this4.bad.btn = false;
	                    return;
	                } else if (data.data.list.length < _this4.$store.state.pageSize) {
	                    _this4.bad.btn = false;
	                } else {
	                    _this4.bad.btn = true;
	                };
	                _this4.bad.list = _this4.bad.list.concat(data.data.list);
	                _this4.info = data.data.object;
	                for (var i = 0; i < _this4.bad.list.length; i++) {
	                    if (!_this4.bad.list[i].wechat_user_viptime) {
	                        _this4.bad.list[i].wechat_userlevel = 0;
	                    } else if (_this4.bad.list[i].wechat_user_viptime == '1') {
	                        _this4.bad.list[i].wechat_userlevel = 7;
	                    } else if (_this4.bad.list[i].wechat_user_viptime - Date.parse(new Date()) / 1000 <= 0) {
	                        _this4.bad.list[i].wechat_userlevel = 0;
	                    } else {
	                        _this4.bad.list[i].wechat_userlevel = 1;
	                    }
	                    if (_this4.bad.list[i].wechat_headimgurl.indexOf('http') < 0) {
	                        _this4.bad.list[i].wechat_headimgurl = _this4.$store.state.URL + _this4.bad.list[i].wechat_headimgurl;
	                    }
	                }
	            });
	        }
	    },
	    components: {
	        Start: _start2.default
	    }
	};
	
	// </script>
	/* generated by vue-loader */
	// <template>
	// <div class="page consumer">
	//     <header id="header" class="mui-bar mui-bar-nav haed2 ">
	//         <h1 class="mui-title">店铺评价</h1>
	//         <a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a>
	//     </header>
	//     <div class="mui-scroll-wrapper" style="bottom: 0px;">
	//     <div class="mui-scroll">
	//     <div id="sliderSegmentedControl" class="mui-slider-indicator mui-segmented-control mui-segmented-control-inverted">
	//         <a class="mui-control-item mui-active" href="#item1">全部({{info ? info.all : '0'}})</a>
	//         <a class="mui-control-item" href="#item2">好评({{info ? info.good : '0'}})</a>
	//         <a class="mui-control-item" href="#item3">中评({{info ? info.middle : '0'}})</a>
	//         <a class="mui-control-item" href="#item4">差评({{info ? info.bad : '0'}})</a>
	//     </div>
	//
	//     <div id="item1" class="mui-control-content fade-in-left mui-active">
	//         <ul class="comment-item">
	//             <li v-for="item in all.list">
	//                 <div class="item">
	//                     <img :src="item.wechat_headimgurl" class="head">
	//                     {{item.wechat_nickname}} <i class="lv lv{{item.wechat_userlevel}}"></i><br>
	//                     <div class="starts-container"><start :value="parseInt(item.comment_score_store)"><start></div>
	//                     <h6>
	//                         {{item.comment_time | format 'yyyy-MM-dd'}}
	//                     </h6>
	//                     <div class="tag">
	//                         <a v-for="tag in item.comment_store_label" v-if="tag!=''">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>
	//                     </div>
	//                     <p>{{item.comment}}</p>
	//                     <small v-for="reply in item.reply">
	//                         <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>
	//                         {{reply.comment}}
	//                         <br>
	//                     </small>
	//                 </div>
	//             </li>
	//             <div class="no-data" v-if="all.list.length<=0">
	//                 <h1>没有相关评论</h1>
	//             </div>
	//             <a class="btn-more" v-if="all.btn" @click="getAll">查看更多</a>
	//         </ul>
	//     </div>
	//
	//     <div id="item2" class="mui-control-content fade-in-left">
	//         <ul class="comment-item">
	//             <li v-for="item in good.list" v-if="item.comment_level == '1'">
	//                 <div class="item">
	//                     <img :src="item.wechat_headimgurl" class="head">
	//                     {{item.wechat_nickname}} <i class="lv lv{{item.wechat_userlevel}}"></i><br>
	//                     <div class="starts-container"><start :value="parseInt(item.comment_score_store)"><start></div>
	//                     <h6>{{item.comment_time | format 'yyyy-MM-dd'}}</h6>
	//                     <div class="tag">
	//                         <a v-for="tag in item.comment_store_label" v-if="tag!=''">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>
	//                     </div>
	//                     <p>{{item.comment}}</p>
	//                     <small v-for="reply in item.reply">
	//                         <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>
	//                         {{reply.comment}}
	//                         <br>
	//                     </small>
	//                 </div>
	//             </li>
	//             <div class="no-data" v-if="good.list.length<=0">
	//                 <h1>没有相关评论</h1>
	//             </div>
	//             <a class="btn-more" v-if="good.btn" @click="getGood">查看更多</a>
	//         </ul>
	//     </div>
	//
	//     <div id="item3" class="mui-control-content fade-in-left">
	//         <ul class="comment-item">
	//             <li v-for="item in midd.list" v-if="item.comment_level == '2'">
	//                 <div class="item">
	//                     <img :src="item.wechat_headimgurl" class="head">
	//                     {{item.wechat_nickname}} <i class="lv lv{{item.wechat_userlevel}}"></i><br>
	//                     <div class="starts-container"><start :value="parseInt(item.comment_score_store)"><start></div>
	//                     <h6>{{item.comment_time | format 'yyyy-MM-dd'}}</h6>
	//                     <div class="tag">
	//                         <a v-for="tag in item.comment_store_label" v-if="tag!=''">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>
	//                     </div>
	//                     <p>{{item.comment}}</p>
	//                     <small v-for="reply in item.reply">
	//                         <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>
	//                         {{reply.comment}}
	//                         <br>
	//                     </small>
	//                 </div>
	//             </li>
	//             <div class="no-data" v-if="midd.list.length<=0">
	//                 <h1>没有相关评论</h1>
	//             </div>
	//             <a class="btn-more" v-if="midd.btn" @click="getMidd">查看更多</a>
	//         </ul>
	//     </div>
	//
	//     <div id="item4" class="mui-control-content fade-in-left">
	//         <ul class="comment-item">
	//             <li v-for="item in bad.list" v-if="item.comment_level == '3'">
	//                 <div class="item">
	//                     <img :src="item.wechat_headimgurl" class="head">
	//                     {{item.wechat_nickname}} <i class="lv lv{{item.wechat_userlevel}}"></i><br>
	//                     <div class="starts-container"><start :value="parseInt(item.comment_score_store)"><start></div>
	//                     <h6>{{item.comment_time | format 'yyyy-MM-dd'}}</h6>
	//                     <div class="tag">
	//                         <a v-for="tag in item.comment_store_label" v-if="tag!=''">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>
	//                     </div>
	//                     <p>{{item.comment}}</p>
	//                     <small v-for="reply in item.reply">
	//                         <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>
	//                         {{reply.comment}}
	//                         <br>
	//                     </small>
	//                 </div>
	//             </li>
	//             <div class="no-data" v-if="bad.list.length<=0">
	//                 <h1>没有相关评论</h1>
	//             </div>
	//             <a class="btn-more" v-if="bad.btn" @click="getBad">查看更多</a>
	//         </ul>
	//     </div>
	//
	//     </div>
	//     </div>
	//
	// </div>
	// </template>
	//
	// <script>

/***/ },
/* 161 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page consumer\">\n    <header id=\"header\" class=\"mui-bar mui-bar-nav haed2 \">\n        <h1 class=\"mui-title\">店铺评价</h1>\n        <a class=\"mui-action-back mui-icon mui-icon-left-nav mui-pull-left\"></a>\n    </header>\n    <div class=\"mui-scroll-wrapper\" style=\"bottom: 0px;\">\n    <div class=\"mui-scroll\">\n    <div id=\"sliderSegmentedControl\" class=\"mui-slider-indicator mui-segmented-control mui-segmented-control-inverted\">\n        <a class=\"mui-control-item mui-active\" href=\"#item1\">全部({{info ? info.all : '0'}})</a>\n        <a class=\"mui-control-item\" href=\"#item2\">好评({{info ? info.good : '0'}})</a>\n        <a class=\"mui-control-item\" href=\"#item3\">中评({{info ? info.middle : '0'}})</a>\n        <a class=\"mui-control-item\" href=\"#item4\">差评({{info ? info.bad : '0'}})</a>\n    </div>\n    \n    <div id=\"item1\" class=\"mui-control-content fade-in-left mui-active\">\n        <ul class=\"comment-item\">\n            <li v-for=\"item in all.list\">\n                <div class=\"item\">\n                    <img :src=\"item.wechat_headimgurl\" class=\"head\">\n                    {{item.wechat_nickname}} <i class=\"lv lv{{item.wechat_userlevel}}\"></i><br>\n                    <div class=\"starts-container\"><start :value=\"parseInt(item.comment_score_store)\"><start></div>\n                    <h6>\n                        {{item.comment_time | format 'yyyy-MM-dd'}}\n                    </h6>\n                    <div class=\"tag\">\n                        <a v-for=\"tag in item.comment_store_label\" v-if=\"tag!=''\">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>\n                    </div>\n                    <p>{{item.comment}}</p>\n                    <small v-for=\"reply in item.reply\">\n                        <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>\n                        {{reply.comment}}\n                        <br>\n                    </small>\n                </div>\n            </li>\n            <div class=\"no-data\" v-if=\"all.list.length<=0\">\n                <h1>没有相关评论</h1>\n            </div>\n            <a class=\"btn-more\" v-if=\"all.btn\" @click=\"getAll\">查看更多</a>\n        </ul>\n    </div>\n\n    <div id=\"item2\" class=\"mui-control-content fade-in-left\">\n        <ul class=\"comment-item\">\n            <li v-for=\"item in good.list\" v-if=\"item.comment_level == '1'\">\n                <div class=\"item\">\n                    <img :src=\"item.wechat_headimgurl\" class=\"head\">\n                    {{item.wechat_nickname}} <i class=\"lv lv{{item.wechat_userlevel}}\"></i><br>\n                    <div class=\"starts-container\"><start :value=\"parseInt(item.comment_score_store)\"><start></div>\n                    <h6>{{item.comment_time | format 'yyyy-MM-dd'}}</h6>\n                    <div class=\"tag\">\n                        <a v-for=\"tag in item.comment_store_label\" v-if=\"tag!=''\">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>\n                    </div>\n                    <p>{{item.comment}}</p>\n                    <small v-for=\"reply in item.reply\">\n                        <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>\n                        {{reply.comment}}\n                        <br>\n                    </small>\n                </div>\n            </li>\n            <div class=\"no-data\" v-if=\"good.list.length<=0\">\n                <h1>没有相关评论</h1>\n            </div>\n            <a class=\"btn-more\" v-if=\"good.btn\" @click=\"getGood\">查看更多</a>\n        </ul>\n    </div>\n\n    <div id=\"item3\" class=\"mui-control-content fade-in-left\">\n        <ul class=\"comment-item\">\n            <li v-for=\"item in midd.list\" v-if=\"item.comment_level == '2'\">\n                <div class=\"item\">\n                    <img :src=\"item.wechat_headimgurl\" class=\"head\">\n                    {{item.wechat_nickname}} <i class=\"lv lv{{item.wechat_userlevel}}\"></i><br>\n                    <div class=\"starts-container\"><start :value=\"parseInt(item.comment_score_store)\"><start></div>\n                    <h6>{{item.comment_time | format 'yyyy-MM-dd'}}</h6>\n                    <div class=\"tag\">\n                        <a v-for=\"tag in item.comment_store_label\" v-if=\"tag!=''\">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>\n                    </div>\n                    <p>{{item.comment}}</p>\n                    <small v-for=\"reply in item.reply\">\n                        <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>\n                        {{reply.comment}}\n                        <br>\n                    </small>\n                </div>\n            </li>\n            <div class=\"no-data\" v-if=\"midd.list.length<=0\">\n                <h1>没有相关评论</h1>\n            </div>\n            <a class=\"btn-more\" v-if=\"midd.btn\" @click=\"getMidd\">查看更多</a>\n        </ul>\n    </div>\n\n    <div id=\"item4\" class=\"mui-control-content fade-in-left\">\n        <ul class=\"comment-item\">\n            <li v-for=\"item in bad.list\" v-if=\"item.comment_level == '3'\">\n                <div class=\"item\">\n                    <img :src=\"item.wechat_headimgurl\" class=\"head\">\n                    {{item.wechat_nickname}} <i class=\"lv lv{{item.wechat_userlevel}}\"></i><br>\n                    <div class=\"starts-container\"><start :value=\"parseInt(item.comment_score_store)\"><start></div>\n                    <h6>{{item.comment_time | format 'yyyy-MM-dd'}}</h6>\n                    <div class=\"tag\">\n                        <a v-for=\"tag in item.comment_store_label\" v-if=\"tag!=''\">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>\n                    </div>\n                    <p>{{item.comment}}</p>\n                    <small v-for=\"reply in item.reply\">\n                        <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>\n                        {{reply.comment}}\n                        <br>\n                    </small>\n                </div>\n            </li>\n            <div class=\"no-data\" v-if=\"bad.list.length<=0\">\n                <h1>没有相关评论</h1>\n            </div>\n            <a class=\"btn-more\" v-if=\"bad.btn\" @click=\"getBad\">查看更多</a>\n        </ul>\n    </div>\n    \n    </div>\n    </div>\n    \n</div>\n";

/***/ },
/* 162 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(163)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/consumer-vip.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(164)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-83dc6e3e/consumer-vip.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 163 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	
	var _start = __webpack_require__(44);
	
	var _start2 = _interopRequireDefault(_start);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	exports.default = {
	    name: 'consumerVip',
	    data: function data() {
	        return {
	            userInfo: { wechat_userlevel: 0 }
	        };
	    },
	    ready: function ready() {
	        var _this = this;
	
	        document.title = 'VIP中心';
	        mui('.mui-scroll-wrapper').scroll({ bounce: false });
	        this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	        this.$http.post(API.getUserInfo, {}).then(function (data) {
	            if (data.data.errorInfo) return;
	            _this.$store.state.hideTip();
	            _this.userInfo = data.data.object;
	            if (_this.userInfo.wechat_headimgurl.indexOf('http') < 0) {
	                _this.userInfo.wechat_headimgurl = _this.$store.state.URL + _this.userInfo.wechat_headimgurl;
	            }
	            if (!_this.userInfo.wechat_user_viptime) {
	                _this.userInfo.wechat_userlevel = 0;
	            } else if (_this.userInfo.wechat_user_viptime == '1') {
	                _this.userInfo.wechat_userlevel = 7;
	            } else if (_this.userInfo.wechat_user_viptime - Date.parse(new Date()) / 1000 <= 0) {
	                _this.userInfo.wechat_userlevel = 0;
	            } else {
	                _this.userInfo.wechat_userlevel = 1;
	            }
	        });
	    },
	    methods: {
	        local: function local(name) {
	            this.$router.go('/' + name + '/' + this.$route.params.token);
	        },
	        pay: function pay(type) {
	            if (type == 2 && this.userInfo.wechat_user_viptime) {
	                this.$store.state.showTip({ type: 'error', content: '您已经开通过体验会员了，不能第二次开通，请去开通永久会员吧' });
	                return;
	            }
	            window.location.href = API.wxPayIndex + '?token=' + this.$route.params.token + '&type=' + type;
	        }
	    },
	    components: {
	        Start: _start2.default
	    }
	};
	
	// </script>
	/* generated by vue-loader */
	// <template>
	// <div class="page consumer">
	//     <header id="header" class="mui-bar mui-bar-nav haed2 ">
	//         <h1 class="mui-title">VIP中心</h1>
	//         <a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a>
	//     </header>
	//     <div class="mui-scroll-wrapper" style="bottom: 0px;">
	//     <div class="mui-scroll">
	//     <div class="vip-info">
	//         <img :src="userInfo.wechat_headimgurl">
	//         <h1>{{userInfo.wechat_nickname}} <i class="lv lv{{userInfo.wechat_userlevel}}"></i></h1>
	//         <p v-if="userInfo.wechat_userlevel<=0">您还不是VIP</p>
	//         <p v-if="userInfo.wechat_userlevel>0">您已开通VIP</p>
	//     </div>
	//
	//     <div class="vip-wrap">
	//         <div id="sliderSegmentedControl" class="mui-slider-indicator mui-segmented-control mui-segmented-control-inverted">
	//             <a class="mui-control-item mui-active" href="#item1"><i class="iconfont">&#xe60c;</i> 终身VIP</a>
	//             <a class="mui-control-item" href="#item2">体验VIP</a>
	//         </div>
	//         <div id="item1" class="mui-slider-item mui-control-content mui-active">
	//             <p>
	//                 畅想VIP特权，查看技师真实图片，一次充值，终身有效。<br>
	//                 现推广期内加入终身VIP，可永享V9最高等级！
	//             </p>
	//             <input type="text" value="￥100" readonly="readonly" class="{{userInfo.wechat_userlevel>0 ? 'vip' : ''}}">
	//             <button class="vip" v-if="userInfo.wechat_userlevel>0">立即开通</button>
	//             <button v-if="userInfo.wechat_userlevel<=0" @click="pay(1)">立即开通</button>
	//         </div>
	//         <div id="item2" class="mui-slider-item mui-control-content">
	//             <p>
	//                 体验VIP特权，可查看技师真实图片，体验期三天
	//             </p>
	//             <input type="text" value="￥5" readonly="readonly" class="{{userInfo.wechat_userlevel>0 ? 'vip' : ''}}">
	//             <button class="vip" v-if="userInfo.wechat_userlevel>0">立即开通</button>
	//             <button v-if="userInfo.wechat_userlevel<=0" @click="pay(2)">立即开通</button>
	//         </div>
	//     </div>
	//
	//     <div class="vip-foot">
	//         <p>VIP使用说明：</p>
	//         <p>· VIP服务一经开通后不支持退款；</p>
	//         <p>· 如遇到支付问题，请拨打客服电话：<a href="tel:021-67652958">021-67652958；</a></p>
	//         <p>· 自购买之时起5分钟内生效；</p>
	//         <p>· 系统VIP不同于商家会员，VIP可以在本系统全国所有商家享受VIP 特权； </p>
	//     </div>
	//     </div></div>
	// </div>
	// </template>
	//
	// <script>

/***/ },
/* 164 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page consumer\">\n    <header id=\"header\" class=\"mui-bar mui-bar-nav haed2 \">\n        <h1 class=\"mui-title\">VIP中心</h1>\n        <a class=\"mui-action-back mui-icon mui-icon-left-nav mui-pull-left\"></a>\n    </header>\n    <div class=\"mui-scroll-wrapper\" style=\"bottom: 0px;\">\n    <div class=\"mui-scroll\">\n    <div class=\"vip-info\">\n        <img :src=\"userInfo.wechat_headimgurl\">\n        <h1>{{userInfo.wechat_nickname}} <i class=\"lv lv{{userInfo.wechat_userlevel}}\"></i></h1>\n        <p v-if=\"userInfo.wechat_userlevel<=0\">您还不是VIP</p>\n        <p v-if=\"userInfo.wechat_userlevel>0\">您已开通VIP</p>\n    </div>\n\n    <div class=\"vip-wrap\">\n        <div id=\"sliderSegmentedControl\" class=\"mui-slider-indicator mui-segmented-control mui-segmented-control-inverted\">\n            <a class=\"mui-control-item mui-active\" href=\"#item1\"><i class=\"iconfont\">&#xe60c;</i> 终身VIP</a>\n            <a class=\"mui-control-item\" href=\"#item2\">体验VIP</a>\n        </div>\n        <div id=\"item1\" class=\"mui-slider-item mui-control-content mui-active\">\n            <p>\n                畅想VIP特权，查看技师真实图片，一次充值，终身有效。<br>\n                现推广期内加入终身VIP，可永享V9最高等级！\n            </p>\n            <input type=\"text\" value=\"￥100\" readonly=\"readonly\" class=\"{{userInfo.wechat_userlevel>0 ? 'vip' : ''}}\">\n            <button class=\"vip\" v-if=\"userInfo.wechat_userlevel>0\">立即开通</button>\n            <button v-if=\"userInfo.wechat_userlevel<=0\" @click=\"pay(1)\">立即开通</button>\n        </div>\n        <div id=\"item2\" class=\"mui-slider-item mui-control-content\">\n            <p>\n                体验VIP特权，可查看技师真实图片，体验期三天\n            </p>\n            <input type=\"text\" value=\"￥5\" readonly=\"readonly\" class=\"{{userInfo.wechat_userlevel>0 ? 'vip' : ''}}\">\n            <button class=\"vip\" v-if=\"userInfo.wechat_userlevel>0\">立即开通</button>\n            <button v-if=\"userInfo.wechat_userlevel<=0\" @click=\"pay(2)\">立即开通</button>\n        </div>\n    </div>\n    \n    <div class=\"vip-foot\">\n        <p>VIP使用说明：</p>\n        <p>· VIP服务一经开通后不支持退款；</p>\n        <p>· 如遇到支付问题，请拨打客服电话：<a href=\"tel:021-67652958\">021-67652958；</a></p>\n        <p>· 自购买之时起5分钟内生效；</p>\n        <p>· 系统VIP不同于商家会员，VIP可以在本系统全国所有商家享受VIP 特权； </p>\n    </div>\n    </div></div>\n</div>\n";

/***/ },
/* 165 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(166)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/consumer-member.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(167)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-19d56c06/consumer-member.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 166 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	
	var _start = __webpack_require__(44);
	
	var _start2 = _interopRequireDefault(_start);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	exports.default = {
	    name: 'consumerVip',
	    data: function data() {
	        return {
	            userInfo: { wechat_userlevel: 0 },
	            memberInfo: {}
	        };
	    },
	    ready: function ready() {
	        var _this = this;
	
	        document.title = 'VIP中心';
	        mui('.mui-scroll-wrapper').scroll({ bounce: false });
	        this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	        this.$http.post(API.getUserInfo, {}).then(function (data) {
	            if (data.data.errorInfo) return;
	            _this.$store.state.hideTip();
	            _this.userInfo = data.data.object;
	            if (_this.userInfo.wechat_headimgurl.indexOf('http') < 0) {
	                _this.userInfo.wechat_headimgurl = _this.$store.state.URL + _this.userInfo.wechat_headimgurl;
	            }
	            if (!_this.userInfo.wechat_user_viptime) {
	                _this.userInfo.wechat_userlevel = 0;
	            } else if (_this.userInfo.wechat_user_viptime == '1') {
	                _this.userInfo.wechat_userlevel = 7;
	            } else if (_this.userInfo.wechat_user_viptime - Date.parse(new Date()) / 1000 <= 0) {
	                _this.userInfo.wechat_userlevel = 0;
	            } else {
	                _this.userInfo.wechat_userlevel = 1;
	            }
	        });
	        this.$http.post(API.user_get_member_info, {
	            sid: this.$route.params.storeid
	        }).then(function (data) {
	            if (data.data.errorInfo) return;
	            _this.$store.state.hideTip();
	            _this.memberInfo = data.data.object;
	        });
	    },
	    methods: {
	        local: function local(name) {
	            this.$router.go('/' + name + '/' + this.$route.params.token);
	        },
	        bindVip: function bindVip() {
	            var btnArray = ['取消', '确定'];
	            var me = this;
	            mui.prompt('', me.userInfo.wechat_member, '请输入会员账号', btnArray, function (e) {
	                if (e.index == 1) {
	                    me.$store.state.showTip({ type: 'loading', content: '绑定中...' });
	                    me.$http.post(API.Binding_member, {
	                        sid: me.$route.params.storeid,
	                        num: e.value
	                    }).then(function (data) {
	                        if (data.data.errorInfo) return;
	                        me.$store.state.hideTip();
	                        me.$store.state.showTip({ type: 'success', content: '绑定成功' });
	                        setTimeout(function () {
	                            me.resload();
	                        }, 2000);
	                    });
	                }
	            });
	        },
	        pay: function pay(type) {
	            if (type == 2 && this.userInfo.wechat_user_viptime) {
	                this.$store.state.showTip({ type: 'error', content: '您已经开通过体验会员了，不能第二次开通，请去开通永久会员吧' });
	                return;
	            }
	            window.location.href = API.wxPayIndex + '?token=' + this.$route.params.token + '&type=' + type;
	        }
	    },
	    components: {
	        Start: _start2.default
	    }
	};
	
	// </script>
	/* generated by vue-loader */
	// <template>
	// <div class="page consumer">
	//     <header id="header" class="mui-bar mui-bar-nav haed2 ">
	//         <h1 class="mui-title">会员中心</h1>
	//         <a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a>
	//     </header>
	//     <div class="mui-scroll-wrapper" style="bottom: 0px;">
	//     <div class="mui-scroll">
	//     <div class="vip-info">
	//         <img :src="userInfo.wechat_headimgurl">
	//         <h1>{{userInfo.wechat_nickname}} <i class="lv lv{{userInfo.wechat_userlevel}}"></i></h1>
	//         <p v-if="userInfo.wechat_userlevel<=0">您还不是VIP</p>
	//         <p v-if="userInfo.wechat_userlevel>0">您已开通VIP</p>
	//     </div>
	//
	//     <div class="vip-wrap" v-if="memberInfo" style="font-size: 14px;text-align: left;color: #666;padding: 15px;">
	//         会员账号：{{memberInfo.member_num}} <br>
	//         会员等级：{{memberInfo.member_level}} <br>
	//         联系方式：{{memberInfo.member_tel}} <br>
	//         账户余额：{{memberInfo.member_balance}} <br>
	//         会员积分：{{memberInfo.member_integral}} 
	//     </div>
	//     <button v-if="!memberInfo" type="button" class="mui-btn mui-btn-warning mui-btn-block" @click="bindVip" style="width: 90%;margin: 10px 5%;">绑定会员</button>
	//
	//     <div class="vip-foot">
	//         <p>尊敬的会员：</p>
	//         <p>如果您发现账户余额与会员积分有误，请及时联系商户说明情况！祝您消费愉快！</p>
	//     </div>
	//     </div></div>
	// </div>
	// </template>
	//
	// <script>

/***/ },
/* 167 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page consumer\">\n    <header id=\"header\" class=\"mui-bar mui-bar-nav haed2 \">\n        <h1 class=\"mui-title\">会员中心</h1>\n        <a class=\"mui-action-back mui-icon mui-icon-left-nav mui-pull-left\"></a>\n    </header>\n    <div class=\"mui-scroll-wrapper\" style=\"bottom: 0px;\">\n    <div class=\"mui-scroll\">\n    <div class=\"vip-info\">\n        <img :src=\"userInfo.wechat_headimgurl\">\n        <h1>{{userInfo.wechat_nickname}} <i class=\"lv lv{{userInfo.wechat_userlevel}}\"></i></h1>\n        <p v-if=\"userInfo.wechat_userlevel<=0\">您还不是VIP</p>\n        <p v-if=\"userInfo.wechat_userlevel>0\">您已开通VIP</p>\n    </div>\n\n    <div class=\"vip-wrap\" v-if=\"memberInfo\" style=\"font-size: 14px;text-align: left;color: #666;padding: 15px;\">\n        会员账号：{{memberInfo.member_num}} <br>\n        会员等级：{{memberInfo.member_level}} <br>\n        联系方式：{{memberInfo.member_tel}} <br>\n        账户余额：{{memberInfo.member_balance}} <br>\n        会员积分：{{memberInfo.member_integral}} \n    </div>\n    <button v-if=\"!memberInfo\" type=\"button\" class=\"mui-btn mui-btn-warning mui-btn-block\" @click=\"bindVip\" style=\"width: 90%;margin: 10px 5%;\">绑定会员</button>\n    \n    <div class=\"vip-foot\">\n        <p>尊敬的会员：</p>\n        <p>如果您发现账户余额与会员积分有误，请及时联系商户说明情况！祝您消费愉快！</p>\n    </div>\n    </div></div>\n</div>\n";

/***/ },
/* 168 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(169)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/consumer-order-info.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(170)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-156d65b9/consumer-order-info.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 169 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	
	var _start = __webpack_require__(44);
	
	var _start2 = _interopRequireDefault(_start);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	exports.default = {
	    name: 'consumerOrderInfo',
	    data: function data() {
	        return {
	            userOrderInfo: {},
	            status: '',
	            obj: false
	        };
	    },
	    ready: function ready() {
	        var _this = this;
	
	        document.title = '订单详情';
	        mui('.mui-scroll-wrapper').scroll({ bounce: false });
	        this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	        this.$http.post(API.user_get_this_order_goods, {
	            sid: this.$route.params.storeid,
	            pid: this.$route.params.id
	        }).then(function (data) {
	            if (data.data.errorInfo) return;
	            _this.obj = data.data.object;
	        });
	        this.$http.post(API.getUserOrderInfo, {
	            order_id: this.$route.params.id,
	            sid: this.$route.params.storeid
	        }).then(function (data) {
	            if (data.data.errorInfo) return;
	            _this.$store.state.hideTip();
	            _this.userOrderInfo = data.data.list;
	            if (_this.userOrderInfo.massagist_photo.indexOf('http') < 0) {
	                _this.userOrderInfo.massagist_photo = _this.$store.state.URL + _this.userOrderInfo.massagist_photo;
	            }
	            if (_this.userOrderInfo.order_status == 1 && _this.userOrderInfo.order_pay == 0) _this.status = '待付款';
	            if (_this.userOrderInfo.order_status == 1 && _this.userOrderInfo.order_pay == 1) _this.status = '已完成';
	            if (_this.userOrderInfo.order_status == 2) _this.status = '已取消';
	            if (_this.userOrderInfo.order_status == 3) _this.status = '未开始';
	            if (_this.userOrderInfo.order_status == 4) _this.status = '消费中';
	            if (_this.userOrderInfo.order_status == 6) _this.status = '待确认';
	        });
	    },
	    methods: {
	        local: function local(name) {
	            this.$router.go('/' + name + '/' + this.$route.params.token);
	        }
	    },
	    components: {
	        Start: _start2.default
	    }
	};
	
	// </script>
	/* generated by vue-loader */
	// <template>
	// <div class="page consumer" style="padding-bottom: 10px;">
	//     <header id="header" class="mui-bar mui-bar-nav haed2 ">
	//         <h1 class="mui-title">订单详情</h1>
	//         <a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a>
	//     </header>
	//
	//     <div class="mui-scroll-wrapper" style="bottom: 0px;">
	//     <div class="mui-scroll">
	//     <div class="title">订单详情</div>
	//     <div class="shop-title-container order">
	//         <div class="image-size-list" style="background-image: url({{$store.state.URL + userOrderInfo.item_image}});"></div>
	//         <!-- <img :src="$store.state.URL + userOrderInfo.item_image"> -->
	//         <h1 style="width: 80%;">{{userOrderInfo.item_name}}</h1>
	//         <h5>套餐详情：{{userOrderInfo.item_intro}}</h5>
	//         <div class="price">
	//             <em>￥{{userOrderInfo.item_price}}</em>
	//         </div>
	//     </div>
	//     <div class="order-tech">
	//         <img :src="userOrderInfo.massagist_photo">
	//         <h1>工号：{{userOrderInfo.massagist_no}} 昵称：{{userOrderInfo.massagist_name}}</h1>
	//     </div>
	//
	//     <ul class="mui-table-view order-info">
	//         <li class="mui-table-view-cell">
	//             房间号<span class="mui-pull-right">{{userOrderInfo.order_address == 0 ? userOrderInfo.room_num : userOrderInfo.order_room_num}}</span>
	//         </li>
	//         <li class="mui-table-view-cell">
	//             服务类型<span class="mui-pull-right">{{userOrderInfo.order_address == 0 ? '在店消费' : '技师上门'}}</span>
	//         </li>
	//         <li class="mui-table-view-cell">
	//             订单总价<span class="mui-pull-right">￥{{userOrderInfo.item_price}}</span>
	//         </li>
	//         <li class="mui-table-view-cell" v-if="obj">
	//             商品消费<span class="mui-pull-right">￥{{obj}}</span>
	//         </li>
	//         <li class="mui-table-view-cell show-color">
	//             需付款<span class="mui-pull-right show-color">￥{{userOrderInfo.order_totalprice}}</span>
	//         </li>
	//     </ul>
	//     <div class="title">订单信息</div>
	//     <ul class="mui-table-view order-info">
	//         <li class="mui-table-view-cell">
	//             订单备注<span class="mui-pull-right">{{userOrderInfo.order_info || '无'}}</span>
	//         </li>
	//         <li class="mui-table-view-cell">
	//             订单编号<span class="mui-pull-right">{{userOrderInfo.order_num}}</span>
	//         </li>
	//         <li class="mui-table-view-cell">
	//             创建时间<span class="mui-pull-right">{{userOrderInfo.order_createtime || '' | format 'yyyy-MM-dd hh:mm:ss'}}</span>
	//         </li>
	//         <li class="mui-table-view-cell">
	//             完成时间<span class="mui-pull-right">{{userOrderInfo.order_donetime || status | format 'yyyy-MM-dd hh:mm:ss'}}</span>
	//         </li>
	//         <li class="mui-table-view-cell show-color">
	//             订单状态<span class="mui-pull-right show-color">{{status}}</span>
	//         </li>
	//     </ul>
	//     <div style="width: 90%;margin: 15px auto;" v-if="userOrderInfo.order_status == 1 && userOrderInfo.order_pay == 0">
	//         <button type="button" class="mui-btn mui-btn-warning mui-btn-block" v-link="{path: '/consumer-pay/' + $route.params.token + '/' + $route.params.storeid + '/' + $route.params.id}">确认付款</button>
	//     </div>
	//     </div></div>
	//
	// </div>
	// </template>
	//
	// <script>

/***/ },
/* 170 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page consumer\" style=\"padding-bottom: 10px;\">\n    <header id=\"header\" class=\"mui-bar mui-bar-nav haed2 \">\n        <h1 class=\"mui-title\">订单详情</h1>\n        <a class=\"mui-action-back mui-icon mui-icon-left-nav mui-pull-left\"></a>\n    </header>\n\n    <div class=\"mui-scroll-wrapper\" style=\"bottom: 0px;\">\n    <div class=\"mui-scroll\">\n    <div class=\"title\">订单详情</div>\n    <div class=\"shop-title-container order\">\n        <div class=\"image-size-list\" style=\"background-image: url({{$store.state.URL + userOrderInfo.item_image}});\"></div>\n        <!-- <img :src=\"$store.state.URL + userOrderInfo.item_image\"> -->\n        <h1 style=\"width: 80%;\">{{userOrderInfo.item_name}}</h1>\n        <h5>套餐详情：{{userOrderInfo.item_intro}}</h5>\n        <div class=\"price\">\n            <em>￥{{userOrderInfo.item_price}}</em>\n        </div>\n    </div>\n    <div class=\"order-tech\">\n        <img :src=\"userOrderInfo.massagist_photo\">\n        <h1>工号：{{userOrderInfo.massagist_no}} 昵称：{{userOrderInfo.massagist_name}}</h1>\n    </div>\n\n    <ul class=\"mui-table-view order-info\">\n        <li class=\"mui-table-view-cell\">\n            房间号<span class=\"mui-pull-right\">{{userOrderInfo.order_address == 0 ? userOrderInfo.room_num : userOrderInfo.order_room_num}}</span>\n        </li>\n        <li class=\"mui-table-view-cell\">\n            服务类型<span class=\"mui-pull-right\">{{userOrderInfo.order_address == 0 ? '在店消费' : '技师上门'}}</span>\n        </li>\n        <li class=\"mui-table-view-cell\">\n            订单总价<span class=\"mui-pull-right\">￥{{userOrderInfo.item_price}}</span>\n        </li>\n        <li class=\"mui-table-view-cell\" v-if=\"obj\">\n            商品消费<span class=\"mui-pull-right\">￥{{obj}}</span>\n        </li>\n        <li class=\"mui-table-view-cell show-color\">\n            需付款<span class=\"mui-pull-right show-color\">￥{{userOrderInfo.order_totalprice}}</span>\n        </li>\n    </ul>\n    <div class=\"title\">订单信息</div>\n    <ul class=\"mui-table-view order-info\">\n        <li class=\"mui-table-view-cell\">\n            订单备注<span class=\"mui-pull-right\">{{userOrderInfo.order_info || '无'}}</span>\n        </li>\n        <li class=\"mui-table-view-cell\">\n            订单编号<span class=\"mui-pull-right\">{{userOrderInfo.order_num}}</span>\n        </li>\n        <li class=\"mui-table-view-cell\">\n            创建时间<span class=\"mui-pull-right\">{{userOrderInfo.order_createtime || '' | format 'yyyy-MM-dd hh:mm:ss'}}</span>\n        </li>\n        <li class=\"mui-table-view-cell\">\n            完成时间<span class=\"mui-pull-right\">{{userOrderInfo.order_donetime || status | format 'yyyy-MM-dd hh:mm:ss'}}</span>\n        </li>\n        <li class=\"mui-table-view-cell show-color\">\n            订单状态<span class=\"mui-pull-right show-color\">{{status}}</span>\n        </li>\n    </ul>\n    <div style=\"width: 90%;margin: 15px auto;\" v-if=\"userOrderInfo.order_status == 1 && userOrderInfo.order_pay == 0\">\n        <button type=\"button\" class=\"mui-btn mui-btn-warning mui-btn-block\" v-link=\"{path: '/consumer-pay/' + $route.params.token + '/' + $route.params.storeid + '/' + $route.params.id}\">确认付款</button>\n    </div>\n    </div></div>\n    \n</div>\n";

/***/ },
/* 171 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(172)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/consumer-order-list.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(173)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-5b0739a9/consumer-order-list.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 172 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	
	var _start = __webpack_require__(44);
	
	var _start2 = _interopRequireDefault(_start);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	exports.default = {
	    name: 'consumerOrderList',
	    data: function data() {
	        return {
	            userOrderlist: [],
	            page: 1,
	            btnPage: false,
	            goodShow: false,
	            goodsList: [],
	            goodsInfo: {
	                oid: null,
	                gid: []
	            }
	        };
	    },
	    ready: function ready() {
	        var _this = this;
	
	        document.title = '我的订单';
	        mui('.mui-scroll-wrapper').scroll({ bounce: false });
	        this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	        this.$http.post(API.getUserOrderlist, {
	            page: this.page,
	            sid: this.$route.params.storeid
	        }).then(function (data) {
	            if (data.data.errorInfo) return;
	            _this.$store.state.hideTip();
	            if (!data.data.list) {
	                _this.btnPage = false;
	                return;
	            } else if (data.data.list.length < _this.$store.state.pageSize) {
	                _this.btnPage = false;
	            } else {
	                _this.btnPage = true;
	            };
	            _this.userOrderlist = data.data.list;
	        });
	        var me = this;
	        $('.choose-good').on('click', '.mui-numbox-btn-plus', function () {
	            var _ = parseInt($(this).prev().val());
	            $(this).prev().val(_ + 1);
	            me.setGoodId();
	        });
	        $('.choose-good').on('click', '.mui-numbox-btn-minus', function () {
	            var _ = parseInt($(this).next().val());
	            if (_ <= 0) return;
	            $(this).next().val(_ - 1);
	            me.setGoodId();
	        });
	    },
	    methods: {
	        local: function local(name) {
	            this.$router.go('/' + name + '/' + this.$route.params.token);
	        },
	        setGoodId: function setGoodId() {
	            var me = this;
	            me.goodsInfo.gid = [];
	            for (var i = 0; i < $('.choose-good li').length; i++) {
	                for (var j = 0; j < $('.choose-good li:eq(' + i + ')').find('input').val(); j++) {
	                    me.goodsInfo.gid.push($('.choose-good li:eq(' + i + ')').attr('data-id'));
	                }
	            }
	        },
	        subGood: function subGood() {
	            var _this2 = this;
	
	            var me = this;
	            if (me.goodsInfo.gid.length == 0) {
	                me.$store.state.showTip({ type: 'error', content: '请添加或修改商品' });
	                return;
	            }
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.user_add_goods_this_order, {
	                sid: this.$route.params.storeid,
	                id: this.goodsInfo.oid,
	                goods: this.goodsInfo.gid
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this2.$store.state.showTip({ type: 'success', content: '处理成功' });
	                var socket = new WebSocket("ws://121.43.163.207:9509");
	                var self = _this2;
	                socket.onopen = function (event) {
	                    socket.send('{"store": "' + self.$route.params.storeid + '","fun1":"1","fun2":"2","fun3":"3"}');
	                };
	                socket.onmessage = function (event) {
	                    console.log(event.data);
	                };
	                setTimeout(function () {
	                    location.reload();
	                }, 1500);
	            });
	        },
	        chooseGood: function chooseGood(id) {
	            var _this3 = this;
	
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            var me = this;
	            this.$http.post(API.user_get_this_order_goods, {
	                sid: this.$route.params.storeid,
	                pid: id
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                var goodList = data.data.list ? data.data.list : [];
	                _this3.$http.post(API.user_get_goods_select, {
	                    sid: _this3.$route.params.storeid
	                }).then(function (data) {
	                    if (data.data.errorInfo) return;
	                    _this3.$store.state.hideTip();
	                    _this3.goodsList = [];
	                    for (var i = 0; i < data.data.list.length; i++) {
	                        _this3.goodsList[i] = data.data.list[i];
	                        _this3.goodsList[i].value = 0;
	                        for (var j = 0; j < goodList.length; j++) {
	                            if (goodList[j].id == me.goodsList[i].goods_id) {
	                                me.goodsList[i].value = goodList[j].num;
	                            }
	                        }
	                    }
	                    _this3.goodShow = true;
	                    _this3.goodsInfo.oid = id;
	                });
	            });
	        },
	        nextPage: function nextPage() {
	            var _this4 = this;
	
	            this.page++;
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.getUserOrderlist, {
	                page: this.page,
	                sid: this.$route.params.storeid
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this4.$store.state.hideTip();
	                if (!data.data.list) {
	                    _this4.btnPage = false;
	                    return;
	                } else if (data.data.list.length < _this4.$store.state.pageSize) {
	                    _this4.btnPage = false;
	                } else {
	                    _this4.btnPage = true;
	                };
	                _this4.userOrderlist = _this4.userOrderlist.concat(data.data.list);
	            });
	        },
	        cancelOrder: function cancelOrder(id) {
	            var me = this;
	            mui.confirm('确定取消吗？', '提醒', ['取消', '确定'], function (e) {
	                var _this5 = this;
	
	                if (e.index == 1) {
	                    me.$store.state.showTip({ type: 'loading', content: '取消中...' });
	                    me.$http.post(API.deleteOrderList, {
	                        order_id: id,
	                        sid: me.$route.params.storeid,
	                        action: 'cancel'
	                    }).then(function (data) {
	                        if (data.data.errorInfo) return;
	                        me.$store.state.hideTip();
	                        me.$store.state.showTip({ type: 'success', content: '取消成功' });
	                        var socket = new WebSocket("ws://121.43.163.207:9509");
	                        var self = _this5;
	                        socket.onopen = function (event) {
	                            socket.send('{"store": "' + self.$route.params.storeid + '","fun1":"1","fun2":"2","fun3":"3"}');
	                        };
	                        socket.onmessage = function (event) {
	                            console.log(event.data);
	                        };
	                        setTimeout(function () {
	                            location.reload();
	                        }, 1500);
	                    });
	                }
	            });
	        },
	        delOrder: function delOrder(id) {
	            var me = this;
	            mui.confirm('确定删除吗？', '提醒', ['取消', '确定'], function (e) {
	                if (e.index == 1) {
	                    me.$store.state.showTip({ type: 'loading', content: '删除中...' });
	                    me.$http.post(API.deleteOrderList, {
	                        order_id: id,
	                        sid: me.$route.params.storeid
	                    }).then(function (data) {
	                        if (data.data.errorInfo) return;
	                        me.$store.state.hideTip();
	                        me.$store.state.showTip({ type: 'success', content: '删除成功' });
	                        setTimeout(function () {
	                            location.reload();
	                        }, 1500);
	                    });
	                }
	            });
	        }
	    },
	    components: {
	        Start: _start2.default
	    }
	};
	
	// </script>
	/* generated by vue-loader */
	// <template>
	// <div class="page consumer">
	//     <header id="header" class="mui-bar mui-bar-nav">
	//         <h1 class="mui-title">我的订单</h1>
	//         <!-- <a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a> -->
	//     </header>
	//
	//     <div class="mui-scroll-wrapper">
	//     <div class="mui-scroll">
	//     <div id="sliderSegmentedControl" class="mui-slider-indicator mui-segmented-control mui-segmented-control-inverted">
	//         <a class="winslow-control-item mui-active" >我的订单</a>
	//         <a class="winslow-control-item" v-link="{path: '/consumer-my/' + $route.params.token + '/' + $route.params.storeid}">我的资料</a>
	//     </div>
	//
	//     <ul class="order-container">
	//         <li v-for="item in userOrderlist">
	//             <div class="order-text">
	//                 <div class="image-size-list" style="background-image: url({{$store.state.URL + item.item_image}});"></div>
	//                 <!-- <img :src="$store.state.URL + item.item_image"> -->
	//                 <h1 style="width: 80%;">{{item.item_name}}</h1>
	//                 <h6>下单时间：{{item.order_createtime | format 'yyyy-MM-dd hh:mm'}}</h6>
	//                 <p>总价：￥{{item.order_totalprice}}</p>
	//                 <div class="price">￥{{item.order_totalprice}}<br>
	//                     <em class="suc" v-if="item.order_status == 1 && item.order_pay == 1">完成</em>
	//                     <em class="cancel" v-if="item.order_status == 2">已取消</em>
	//                     <em v-if="item.order_status == 3">未开始</em>
	//                     <em class="fin" v-if="item.order_status == 4">消费中</em>
	//                     <em class="fin" v-if="item.order_status == 6">待确认</em>
	//                     <em class="fin" v-if="item.order_status == 1 && item.order_pay == 0">待付款</em>
	//                 </div>
	//             </div>
	//             <div class="order-btn">
	//                 <a v-if="item.order_status != 1 && item.order_status != 2" @click="chooseGood(item.order_id)">商品消费</a>
	//                 <a v-if="item.order_status == 3" @click="cancelOrder(item.order_id)">取消订单</a>
	//                 <a v-link="{path: '/consumer-order-info/' + this.$route.params.token + '/' + this.$route.params.storeid + '/' + item.order_id}">订单详情</a>
	//                 <a class="red" v-if="item.order_status == 1 && item.order_pay == 0" v-link="{path: '/consumer-pay/' + this.$route.params.token + '/' + this.$route.params.storeid + '/' + item.order_id}">确认付款</a>
	//                 <a class="red" v-if="item.order_status == 6" @click="$store.state.showTip({type: 'error', content: '订单金额不详，请与前台联系' })">待确认</a>
	//                 <a class="red" v-if="item.order_status == 1 && !item.order_comment && item.order_pay == 1" v-link="{path: '/consumer-post-comment/' + this.$route.params.token + '/' + this.$route.params.storeid + '/' + item.order_id}">发表评价</a>
	//                 <a class="red" v-if="item.order_status == 1 && item.comment && item.order_pay == 1">已评价</a>
	//                 <a class="red" v-if="item.order_status == 2" @click="delOrder(item.order_id)">删除订单</a>
	//             </div>
	//         </li>
	//     </ul>
	//
	//     <div class="no-data" v-if="userOrderlist.length<=0">
	//         <h1>您还没有相关订单</h1>
	//         <h6>去下一单试试吧</h6>
	//         <a v-link="{path: '/consumer/' + this.$route.params.token + '/' + this.$route.params.storeid}">去店铺逛逛</a>
	//     </div>
	//
	//     <a class="btn-more" v-if="btnPage" @click="nextPage">查看更多</a>
	//
	//     </div>
	//     </div>
	//
	//     <nav class="mui-bar mui-bar-tab">
	//         <a class="winslow-tab-item" v-link="{path: '/consumer/' + this.$route.params.token + '/' + this.$route.params.storeid}">
	//             <span class="iconfont">&#xe67d;</span>
	//             <span class="mui-tab-label">首页</span>
	//         </a>
	//         <a class="winslow-tab-item mui-active">
	//             <span class="iconfont">&#xe61e;</span>
	//             <span class="mui-tab-label">我的</span>
	//         </a>
	//     </nav>
	//
	//     <div class="choose-good" v-show="goodShow">
	//         <div class="container">
	//             <h1>选择商品</h1>
	//             <ul class="wrap">
	//                 <li v-for="item in goodsList" data-id="{{item.goods_id}}">
	//                     <img src="{{$store.state.URL+item.goods_head}}">
	//                     <span class="name">{{item.goods_name}}</span>
	//                     <span class="price">￥{{item.goods_price}}</span>
	//                     <div class="mui-numbox">
	//                       <button class="mui-btn mui-numbox-btn-minus" type="button">-</button>
	//                       <input class="mui-numbox-input" type="number" value="{{item.value}}" />
	//                       <button class="mui-btn mui-numbox-btn-plus" type="button">+</button>
	//                     </div>
	//                 </li>
	//                 <div v-if="goodsList.length<=0" class="no-data"><h6>店铺没有商品可选</h6></div>
	//             </ul>
	//             <footer>
	//                 <a @click="goodShow = false">取消</a><a @click="subGood()">确定</a>
	//             </footer>
	//         </div>
	//     </div>
	//
	//
	// </div>
	// </template>
	//
	// <script>

/***/ },
/* 173 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page consumer\">\n    <header id=\"header\" class=\"mui-bar mui-bar-nav\">\n        <h1 class=\"mui-title\">我的订单</h1>\n        <!-- <a class=\"mui-action-back mui-icon mui-icon-left-nav mui-pull-left\"></a> -->\n    </header>\n    \n    <div class=\"mui-scroll-wrapper\">\n    <div class=\"mui-scroll\">\n    <div id=\"sliderSegmentedControl\" class=\"mui-slider-indicator mui-segmented-control mui-segmented-control-inverted\">\n        <a class=\"winslow-control-item mui-active\" >我的订单</a>\n        <a class=\"winslow-control-item\" v-link=\"{path: '/consumer-my/' + $route.params.token + '/' + $route.params.storeid}\">我的资料</a>\n    </div>\n\n    <ul class=\"order-container\">\n        <li v-for=\"item in userOrderlist\">\n            <div class=\"order-text\">\n                <div class=\"image-size-list\" style=\"background-image: url({{$store.state.URL + item.item_image}});\"></div>\n                <!-- <img :src=\"$store.state.URL + item.item_image\"> -->\n                <h1 style=\"width: 80%;\">{{item.item_name}}</h1>\n                <h6>下单时间：{{item.order_createtime | format 'yyyy-MM-dd hh:mm'}}</h6>\n                <p>总价：￥{{item.order_totalprice}}</p>\n                <div class=\"price\">￥{{item.order_totalprice}}<br>\n                    <em class=\"suc\" v-if=\"item.order_status == 1 && item.order_pay == 1\">完成</em>\n                    <em class=\"cancel\" v-if=\"item.order_status == 2\">已取消</em>\n                    <em v-if=\"item.order_status == 3\">未开始</em>\n                    <em class=\"fin\" v-if=\"item.order_status == 4\">消费中</em>\n                    <em class=\"fin\" v-if=\"item.order_status == 6\">待确认</em>\n                    <em class=\"fin\" v-if=\"item.order_status == 1 && item.order_pay == 0\">待付款</em>\n                </div>\n            </div>\n            <div class=\"order-btn\">\n                <a v-if=\"item.order_status != 1 && item.order_status != 2\" @click=\"chooseGood(item.order_id)\">商品消费</a>\n                <a v-if=\"item.order_status == 3\" @click=\"cancelOrder(item.order_id)\">取消订单</a>\n                <a v-link=\"{path: '/consumer-order-info/' + this.$route.params.token + '/' + this.$route.params.storeid + '/' + item.order_id}\">订单详情</a>\n                <a class=\"red\" v-if=\"item.order_status == 1 && item.order_pay == 0\" v-link=\"{path: '/consumer-pay/' + this.$route.params.token + '/' + this.$route.params.storeid + '/' + item.order_id}\">确认付款</a>\n                <a class=\"red\" v-if=\"item.order_status == 6\" @click=\"$store.state.showTip({type: 'error', content: '订单金额不详，请与前台联系' })\">待确认</a>\n                <a class=\"red\" v-if=\"item.order_status == 1 && !item.order_comment && item.order_pay == 1\" v-link=\"{path: '/consumer-post-comment/' + this.$route.params.token + '/' + this.$route.params.storeid + '/' + item.order_id}\">发表评价</a>\n                <a class=\"red\" v-if=\"item.order_status == 1 && item.comment && item.order_pay == 1\">已评价</a>\n                <a class=\"red\" v-if=\"item.order_status == 2\" @click=\"delOrder(item.order_id)\">删除订单</a>\n            </div>\n        </li>\n    </ul>\n\n    <div class=\"no-data\" v-if=\"userOrderlist.length<=0\">\n        <h1>您还没有相关订单</h1>\n        <h6>去下一单试试吧</h6>\n        <a v-link=\"{path: '/consumer/' + this.$route.params.token + '/' + this.$route.params.storeid}\">去店铺逛逛</a>\n    </div>\n\n    <a class=\"btn-more\" v-if=\"btnPage\" @click=\"nextPage\">查看更多</a>\n\n    </div>\n    </div>\n\n    <nav class=\"mui-bar mui-bar-tab\">\n        <a class=\"winslow-tab-item\" v-link=\"{path: '/consumer/' + this.$route.params.token + '/' + this.$route.params.storeid}\">\n            <span class=\"iconfont\">&#xe67d;</span>\n            <span class=\"mui-tab-label\">首页</span>\n        </a>\n        <a class=\"winslow-tab-item mui-active\">\n            <span class=\"iconfont\">&#xe61e;</span>\n            <span class=\"mui-tab-label\">我的</span>\n        </a>\n    </nav>\n\n    <div class=\"choose-good\" v-show=\"goodShow\">\n        <div class=\"container\">\n            <h1>选择商品</h1>\n            <ul class=\"wrap\">\n                <li v-for=\"item in goodsList\" data-id=\"{{item.goods_id}}\">\n                    <img src=\"{{$store.state.URL+item.goods_head}}\">\n                    <span class=\"name\">{{item.goods_name}}</span>\n                    <span class=\"price\">￥{{item.goods_price}}</span>\n                    <div class=\"mui-numbox\">\n                      <button class=\"mui-btn mui-numbox-btn-minus\" type=\"button\">-</button>\n                      <input class=\"mui-numbox-input\" type=\"number\" value=\"{{item.value}}\" />\n                      <button class=\"mui-btn mui-numbox-btn-plus\" type=\"button\">+</button>\n                    </div>\n                </li>\n                <div v-if=\"goodsList.length<=0\" class=\"no-data\"><h6>店铺没有商品可选</h6></div>\n            </ul>\n            <footer>\n                <a @click=\"goodShow = false\">取消</a><a @click=\"subGood()\">确定</a>\n            </footer>\n        </div>\n    </div>\n    \n    \n</div>\n";

/***/ },
/* 174 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(175)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/consumer-my.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(176)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-7403bfd0/consumer-my.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 175 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	
	var _assign = __webpack_require__(138);
	
	var _assign2 = _interopRequireDefault(_assign);
	
	var _exif = __webpack_require__(142);
	
	var _exif2 = _interopRequireDefault(_exif);
	
	var _start = __webpack_require__(44);
	
	var _start2 = _interopRequireDefault(_start);
	
	__webpack_require__(50);
	
	__webpack_require__(134);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	// <template>
	// <div class="page consumer">
	//     <header id="header" class="mui-bar mui-bar-nav">
	//         <h1 class="mui-title">我的资料</h1>
	//         <!-- <a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a> -->
	//     </header>
	//
	//     <div class="mui-scroll-wrapper">
	//     <div class="mui-scroll">
	//     <div id="sliderSegmentedControl" class="mui-slider-indicator mui-segmented-control mui-segmented-control-inverted">
	//         <a class="winslow-control-item" v-link="{path: '/consumer-order-list/' + this.$route.params.token + '/' + this.$route.params.storeid}">我的订单</a>
	//         <a class="winslow-control-item mui-active">我的资料</a>
	//     </div>
	//
	//     <ul class="mui-table-view user-info fade-in-left">
	//         <li class="mui-table-view-cell" @click="file" id="fileStart">
	//             <a class="mui-navigate-right" style="line-height: 50px;">
	//                 头像
	//                 <img :src="userInfo.wechat_headimgurl" class="head">
	//             </a>
	//         </li>
	//         <li class="mui-table-view-cell" @click="updateName">
	//             昵称
	//             <span class="mui-pull-right">{{userInfo.wechat_nickname}}</span>
	//         </li>
	//         <li class="mui-table-view-cell" v-link="{path: '/consumer-vip/' + this.$route.params.token + '/' + this.$route.params.storeid}">
	//             VIP等级
	//             <span class="mui-pull-right"><i class="lv lv{{userInfo.wechat_userlevel}}"></i></span>
	//         </li>
	//         <li class="mui-table-view-cell" @click="sex">
	//             性别
	//             <span class="mui-pull-right">{{userInfo.wechat_sex | sex}}</span>
	//         </li>
	//         <li class="mui-table-view-cell" v-link="{path: '/consumer-member/' + this.$route.params.token + '/' + this.$route.params.storeid}">
	//             会员信息
	//             <span class="mui-pull-right">{{userInfo.wechat_member || '没有绑定'}}</span>
	//         </li>
	//     </ul>
	//     </div>
	//     </div>
	//
	//     <nav class="mui-bar mui-bar-tab">
	//         <a class="winslow-tab-item" v-link="{path: '/consumer/' + this.$route.params.token + '/' + this.$route.params.storeid}">
	//             <span class="iconfont">&#xe67d;</span>
	//             <span class="mui-tab-label">首页</span>
	//         </a>
	//         <a class="winslow-tab-item mui-active">
	//             <span class="iconfont">&#xe61e;</span>
	//             <span class="mui-tab-label">我的</span>
	//         </a>
	//     </nav>
	//     <input id="fileCrop" style="opacity: 0;" type="file" accept="image/*" v-on:change="change">
	//     <div class="crop-container" v-show="showCrop">
	//         <div class="wrap">
	//             <img class="cropper" :src="cropImg">
	//         </div>
	//         <div class="btn">
	//             <div><button type="button" class="mui-btn mui-btn-block" @click="$store.state.locationReload()">取消</button></div>
	//             <div><button type="button" class="mui-btn mui-btn-primary mui-btn-block" @click="saveCrop">保存</button></div>
	//         </div>
	//     </div> 
	//
	// </div>
	// </template>
	//
	// <script>
	exports.default = {
	    name: 'consumerMy',
	    data: function data() {
	        return {
	            showCrop: false,
	            cropImg: '',
	            userInfo: {}
	        };
	    },
	    ready: function ready() {
	        document.title = '我的资料';
	        this.resload();
	        mui('.mui-scroll-wrapper').scroll({ bounce: false });
	    },
	    methods: {
	        file: function file() {
	            var u = navigator.userAgent;
	            var isAndroid = u.indexOf('Android') > -1 || u.indexOf('Adr') > -1;
	            var isiOS = !!u.match(/\(i[^;]+;( U;)? CPU.+Mac OS X/);
	
	            if (isAndroid) mui('#fileCrop')[0].click();else if (isiOS) {
	                mui('#fileCrop')[0].click();
	                mui('#fileCrop')[0].click();
	            }
	        },
	        change: function change(e) {
	            this.$store.state.showTip({ type: 'loading', content: '努力加载图像中...' });
	            this.showCrop = true;
	            var orientation = null;
	            var FILE = e.target.files[0];
	
	            _exif2.default.getData(FILE, function () {
	                var obj = (0, _assign2.default)({}, _exif2.default.getAllTags(this));
	                orientation = obj.Orientation;
	            });
	
	            var reader = new FileReader();
	            var self = this;
	
	            reader.onload = function (e) {
	                self.$store.state.__getImgData(e.target.result, orientation, function (data) {
	                    var pic = new Image();
	                    pic.src = data;
	                    pic.onload = function () {
	                        self.cropImg = data;
	                        setTimeout(function () {
	                            $(".cropper").cropper({ aspectRatio: 1 / 1 });
	                            self.$store.state.hideTip();
	                        }, 100);
	                    };
	                });
	            };
	            reader.readAsDataURL(FILE);
	        },
	        saveCrop: function saveCrop() {
	            var _this = this;
	
	            this.$store.state.showTip({ type: 'loading', content: '上传中...' });
	            setTimeout(function () {
	                var dataURL = $(".cropper").cropper("getDataURL");
	                var picURL = dataURL.replace(/^.*?,/, '');
	                _this.$http.post(API.uploadImg, {
	                    type: 'head',
	                    base64: picURL
	                }).then(function (data) {
	                    if (data.data.errorInfo) return;
	                    _this.$store.state.hideTip();
	                    _this.$store.state.locationReload();
	                });
	            }, 100);
	        },
	        updateName: function updateName() {
	            var btnArray = ['取消', '确定'];
	            var me = this;
	            mui.prompt('', me.userInfo.wechat_nickname, '请输入您新的昵称', btnArray, function (e) {
	                if (e.index == 1) {
	                    me.$store.state.showTip({ type: 'loading', content: '修改中...' });
	                    me.$http.post(API.updateUserInfo, {
	                        name: e.value
	                    }).then(function (data) {
	                        if (data.data.errorInfo) return;
	                        me.$store.state.hideTip();
	                        me.$store.state.showTip({ type: 'success', content: '修改成功' });
	                        setTimeout(function () {
	                            me.resload();
	                        }, 2000);
	                    });
	                }
	            });
	        },
	        resload: function resload() {
	            var _this2 = this;
	
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.getUserInfo, {}).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this2.$store.state.hideTip();
	                _this2.userInfo = data.data.object;
	                if (_this2.userInfo.wechat_headimgurl.indexOf('http') < 0) {
	                    _this2.userInfo.wechat_headimgurl = _this2.$store.state.URL + _this2.userInfo.wechat_headimgurl;
	                }
	                if (!_this2.userInfo.wechat_user_viptime) {
	                    _this2.userInfo.wechat_userlevel = 0;
	                } else if (_this2.userInfo.wechat_user_viptime == '1') {
	                    _this2.userInfo.wechat_userlevel = 7;
	                } else if (_this2.userInfo.wechat_user_viptime - Date.parse(new Date()) / 1000 <= 0) {
	                    _this2.userInfo.wechat_userlevel = 0;
	                } else {
	                    _this2.userInfo.wechat_userlevel = 1;
	                }
	            });
	        },
	        sex: function sex() {
	            var sexPicker = new mui.PopPicker();
	            var me = this;
	            sexPicker.setData([{ value: 1, text: '男' }, { value: 2, text: '女' }]);
	            sexPicker.pickers[0].setSelectedValue(me.userInfo.wechat_sex);
	            sexPicker.show(function (items) {
	                me.$store.state.showTip({ type: 'loading', content: '修改中...' });
	                me.$http.post(API.updateUserInfo, {
	                    sex: items[0].value
	                }).then(function (data) {
	                    if (data.data.errorInfo) return;
	                    me.$store.state.hideTip();
	                    me.$store.state.showTip({ type: 'success', content: '修改成功' });
	                    setTimeout(function () {
	                        me.resload();
	                    }, 2000);
	                });
	            });
	        },
	
	        local: function local(name) {
	            this.$router.go('/' + name + '/' + this.$route.params.token);
	        }
	    },
	    components: {
	        Start: _start2.default
	    }
	};
	
	// </script>
	/* generated by vue-loader */

/***/ },
/* 176 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page consumer\">\n    <header id=\"header\" class=\"mui-bar mui-bar-nav\">\n        <h1 class=\"mui-title\">我的资料</h1>\n        <!-- <a class=\"mui-action-back mui-icon mui-icon-left-nav mui-pull-left\"></a> -->\n    </header>\n    \n    <div class=\"mui-scroll-wrapper\">\n    <div class=\"mui-scroll\">\n    <div id=\"sliderSegmentedControl\" class=\"mui-slider-indicator mui-segmented-control mui-segmented-control-inverted\">\n        <a class=\"winslow-control-item\" v-link=\"{path: '/consumer-order-list/' + this.$route.params.token + '/' + this.$route.params.storeid}\">我的订单</a>\n        <a class=\"winslow-control-item mui-active\">我的资料</a>\n    </div>\n\n    <ul class=\"mui-table-view user-info fade-in-left\">\n        <li class=\"mui-table-view-cell\" @click=\"file\" id=\"fileStart\">\n            <a class=\"mui-navigate-right\" style=\"line-height: 50px;\">\n                头像\n                <img :src=\"userInfo.wechat_headimgurl\" class=\"head\">\n            </a>\n        </li>\n        <li class=\"mui-table-view-cell\" @click=\"updateName\">\n            昵称\n            <span class=\"mui-pull-right\">{{userInfo.wechat_nickname}}</span>\n        </li>\n        <li class=\"mui-table-view-cell\" v-link=\"{path: '/consumer-vip/' + this.$route.params.token + '/' + this.$route.params.storeid}\">\n            VIP等级\n            <span class=\"mui-pull-right\"><i class=\"lv lv{{userInfo.wechat_userlevel}}\"></i></span>\n        </li>\n        <li class=\"mui-table-view-cell\" @click=\"sex\">\n            性别\n            <span class=\"mui-pull-right\">{{userInfo.wechat_sex | sex}}</span>\n        </li>\n        <li class=\"mui-table-view-cell\" v-link=\"{path: '/consumer-member/' + this.$route.params.token + '/' + this.$route.params.storeid}\">\n            会员信息\n            <span class=\"mui-pull-right\">{{userInfo.wechat_member || '没有绑定'}}</span>\n        </li>\n    </ul>\n    </div>\n    </div>\n\n    <nav class=\"mui-bar mui-bar-tab\">\n        <a class=\"winslow-tab-item\" v-link=\"{path: '/consumer/' + this.$route.params.token + '/' + this.$route.params.storeid}\">\n            <span class=\"iconfont\">&#xe67d;</span>\n            <span class=\"mui-tab-label\">首页</span>\n        </a>\n        <a class=\"winslow-tab-item mui-active\">\n            <span class=\"iconfont\">&#xe61e;</span>\n            <span class=\"mui-tab-label\">我的</span>\n        </a>\n    </nav>\n    <input id=\"fileCrop\" style=\"opacity: 0;\" type=\"file\" accept=\"image/*\" v-on:change=\"change\">\n    <div class=\"crop-container\" v-show=\"showCrop\">\n        <div class=\"wrap\">\n            <img class=\"cropper\" :src=\"cropImg\">\n        </div>\n        <div class=\"btn\">\n            <div><button type=\"button\" class=\"mui-btn mui-btn-block\" @click=\"$store.state.locationReload()\">取消</button></div>\n            <div><button type=\"button\" class=\"mui-btn mui-btn-primary mui-btn-block\" @click=\"saveCrop\">保存</button></div>\n        </div>\n    </div> \n    \n</div>\n";

/***/ },
/* 177 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(178)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/consumer-post-comment.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(179)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-274af09e/consumer-post-comment.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 178 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	
	var _start = __webpack_require__(44);
	
	var _start2 = _interopRequireDefault(_start);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	exports.default = {
	    name: 'consumerPostComment',
	    data: function data() {
	        return {
	            commentHtml: {},
	            level: 1,
	            comment: '',
	            storeLabel: [],
	            tecLabel: [],
	            scoreStore: 5,
	            scoreMassagist: 5
	        };
	    },
	    watch: {
	        'comment': function comment(newval, oldval) {
	            if (newval.length > 200) this.comment = oldval;
	        }
	    },
	    ready: function ready() {
	        var _this = this;
	
	        document.title = '发表评价';
	        mui('.mui-scroll-wrapper').scroll({ bounce: false });
	        // Array
	        Array.prototype.contains = function (obj) {
	            var i = this.length;
	            while (i--) {
	                if (this[i] === obj) {
	                    return true;
	                }
	            }
	            return false;
	        };
	        Array.prototype.remove = function (val) {
	            var index = this.indexOf(val);
	            if (index > -1) {
	                this.splice(index, 1);
	            }
	        };
	        this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	        this.$http.post(API.getCommentHtml, {
	            sid: this.$route.params.storeid,
	            oid: this.$route.params.id
	        }).then(function (data) {
	            if (data.data.errorInfo) return;
	            _this.$store.state.hideTip();
	            _this.commentHtml = data.data.list;
	            if (_this.commentHtml.massagist_photo.indexOf('http') < 0) {
	                _this.commentHtml.massagist_photo = _this.$store.state.URL + _this.commentHtml.massagist_photo;
	            }
	        });
	    },
	    methods: {
	        local: function local(name) {
	            this.$router.go('/' + name + '/' + this.$route.params.token);
	        },
	        storePush: function storePush(id, rid1, rid2) {
	            if (this.storeLabel.indexOf(id) > -1) {
	                this.storeLabel.remove(id);
	            } else {
	                this.storeLabel.push(id);
	                this.storeLabel.remove(rid1);
	                this.storeLabel.remove(rid2);
	            }
	        },
	        tecPush: function tecPush(id, rid1, rid2) {
	            if (this.tecLabel.indexOf(id) > -1) {
	                this.tecLabel.remove(id);
	            } else {
	                this.tecLabel.push(id);
	                this.tecLabel.remove(rid1);
	                this.tecLabel.remove(rid2);
	            }
	        },
	        submit: function submit() {
	            var _this2 = this;
	
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.releaseComment, {
	                sid: this.$route.params.storeid,
	                oid: this.$route.params.id,
	                comment: this.comment,
	                comment_level: this.level,
	                comment_store_label: this.storeLabel,
	                comment_t_label: this.tecLabel,
	                comment_score_store: this.scoreStore,
	                comment_score_massagist: this.scoreMassagist
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this2.$store.state.hideTip();
	                _this2.$store.state.showTip({ type: 'success', content: '发布成功' });
	                setTimeout(function () {
	                    window.history.go(-1);
	                }, 1500);
	            });
	        }
	    },
	    components: {
	        Start: _start2.default
	    }
	};
	
	// </script>
	/* generated by vue-loader */
	// <template>
	// <div class="page consumer" style="padding-bottom: 0px;">
	//     <header id="header" class="mui-bar mui-bar-nav haed2 ">
	//         <h1 class="mui-title">发表评价</h1>
	//         <a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a>
	//     </header>
	//     <div class="mui-scroll-wrapper" style="bottom: 0px;">
	//     <div class="mui-scroll">
	//     <div class="imgs">
	//         <img :src="commentHtml.massagist_photo">
	//         <img :src="$store.state.URL + commentHtml.item_image">
	//     </div>
	//     <div class="comment-btn">
	//         <a class="good {{level==1 ? 'active' : ''}}" @click="level=1">好评</a>
	//         <a class="so {{level==2 ? 'active' : ''}}" @click="level=2">中评</a>
	//         <a class="bad {{level==3 ? 'active' : ''}}" @click="level=3">差评</a>
	//     </div>
	//
	//     <div class="title">店铺</div>
	//     <ul class="comment-li-container">
	//         <li><a @click="storePush(1, 5, 9)" class="{{storeLabel.contains(1) ? 'active' : ''}}">环境优雅</a></li>
	//         <li><a @click="storePush(2, 6, 10)" class="{{storeLabel.contains(2) ? 'active' : ''}}">设备完善</a></li>
	//         <li><a @click="storePush(3, 7, 11)" class="{{storeLabel.contains(3) ? 'active' : ''}}">价格合理</a></li>
	//         <li><a @click="storePush(4, 8, 12)" class="{{storeLabel.contains(4) ? 'active' : ''}}">下次还来</a></li>
	//         <li><a @click="storePush(5, 1, 9)" class="{{storeLabel.contains(5) ? 'active' : ''}}">环境一般</a></li>
	//         <li><a @click="storePush(6, 2, 10)" class="{{storeLabel.contains(6) ? 'active' : ''}}">设施一般</a></li>
	//         <li><a @click="storePush(7, 3, 11)" class="{{storeLabel.contains(7) ? 'active' : ''}}">性价比低</a></li>
	//         <li><a @click="storePush(8, 4, 12)" class="{{storeLabel.contains(8) ? 'active' : ''}}">有待提高</a></li>
	//         <li><a @click="storePush(9, 1, 5)" class="{{storeLabel.contains(9) ? 'active' : ''}}">环境恶劣</a></li>
	//         <li><a @click="storePush(10, 2, 6)" class="{{storeLabel.contains(10) ? 'active' : ''}}">设施陈旧</a></li>
	//         <li><a @click="storePush(11, 3, 7)" class="{{storeLabel.contains(11) ? 'active' : ''}}">价格坑爹</a></li>
	//         <li><a @click="storePush(12, 4, 8)" class="{{storeLabel.contains(12) ? 'active' : ''}}">绝不再来</a></li>
	//         <div class="clear"></div>
	//     </ul>
	//
	//     <div class="title">技师</div>
	//     <ul class="comment-li-container">
	//         <li><a @click="tecPush(1, 5, 9)" class="{{tecLabel.contains(1) ? 'active' : ''}}">服务热情</a></li>
	//         <li><a @click="tecPush(2, 6, 10)" class="{{tecLabel.contains(2) ? 'active' : ''}}">手法专业</a></li>
	//         <li><a @click="tecPush(3, 7, 11)" class="{{tecLabel.contains(3) ? 'active' : ''}}">态度很好</a></li>
	//         <li><a @click="tecPush(4, 8, 12)" class="{{tecLabel.contains(4) ? 'active' : ''}}">颜值爆表</a></li>
	//         <li><a @click="tecPush(5, 1, 9)" class="{{tecLabel.contains(5) ? 'active' : ''}}">服务一般</a></li>
	//         <li><a @click="tecPush(6, 2, 10)" class="{{tecLabel.contains(6) ? 'active' : ''}}">手法一般</a></li>
	//         <li><a @click="tecPush(7, 3, 11)" class="{{tecLabel.contains(7) ? 'active' : ''}}">态度一般</a></li>
	//         <li><a @click="tecPush(8, 4, 12)" class="{{tecLabel.contains(8) ? 'active' : ''}}">看的过去</a></li>
	//         <li><a @click="tecPush(9, 1, 5)" class="{{tecLabel.contains(9) ? 'active' : ''}}">服务太差</a></li>
	//         <li><a @click="tecPush(10, 2, 6)" class="{{tecLabel.contains(10) ? 'active' : ''}}">手法太差</a></li>
	//         <li><a @click="tecPush(11, 3, 7)" class="{{tecLabel.contains(11) ? 'active' : ''}}">态度恶劣</a></li>
	//         <li><a @click="tecPush(12, 4, 8)" class="{{tecLabel.contains(12) ? 'active' : ''}}">惨不忍睹</a></li>
	//         <div class="clear"></div>
	//         <textarea placeholder="亲！可以再次输入对店铺及技师的服务评价" v-model="comment"></textarea>
	//         <p>{{comment.length}}/200</p>
	//     </ul>
	//
	//     <div class="title">店铺及技师评分(满意给5星哦)</div>
	//     <div class="winslow-container">
	//         <p class="pingfen">店铺评分 <start :value.sync="scoreStore" :ischange='true' :big='true'></start></p>
	//         <p class="pingfen">技师评分 <start :value.sync="scoreMassagist" :ischange='true' :big='true'></start></p>
	//     </div>
	//
	//     <button type="button" class="mui-btn mui-btn-warning mui-btn-block" style="width: 70%;margin: 20px auto;" @click="submit">发表评价</button>
	//     </div></div>
	// </div>
	// </template>
	//
	// <script>

/***/ },
/* 179 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page consumer\" style=\"padding-bottom: 0px;\">\n    <header id=\"header\" class=\"mui-bar mui-bar-nav haed2 \">\n        <h1 class=\"mui-title\">发表评价</h1>\n        <a class=\"mui-action-back mui-icon mui-icon-left-nav mui-pull-left\"></a>\n    </header>\n    <div class=\"mui-scroll-wrapper\" style=\"bottom: 0px;\">\n    <div class=\"mui-scroll\">\n    <div class=\"imgs\">\n        <img :src=\"commentHtml.massagist_photo\">\n        <img :src=\"$store.state.URL + commentHtml.item_image\">\n    </div>\n    <div class=\"comment-btn\">\n        <a class=\"good {{level==1 ? 'active' : ''}}\" @click=\"level=1\">好评</a>\n        <a class=\"so {{level==2 ? 'active' : ''}}\" @click=\"level=2\">中评</a>\n        <a class=\"bad {{level==3 ? 'active' : ''}}\" @click=\"level=3\">差评</a>\n    </div>\n    \n    <div class=\"title\">店铺</div>\n    <ul class=\"comment-li-container\">\n        <li><a @click=\"storePush(1, 5, 9)\" class=\"{{storeLabel.contains(1) ? 'active' : ''}}\">环境优雅</a></li>\n        <li><a @click=\"storePush(2, 6, 10)\" class=\"{{storeLabel.contains(2) ? 'active' : ''}}\">设备完善</a></li>\n        <li><a @click=\"storePush(3, 7, 11)\" class=\"{{storeLabel.contains(3) ? 'active' : ''}}\">价格合理</a></li>\n        <li><a @click=\"storePush(4, 8, 12)\" class=\"{{storeLabel.contains(4) ? 'active' : ''}}\">下次还来</a></li>\n        <li><a @click=\"storePush(5, 1, 9)\" class=\"{{storeLabel.contains(5) ? 'active' : ''}}\">环境一般</a></li>\n        <li><a @click=\"storePush(6, 2, 10)\" class=\"{{storeLabel.contains(6) ? 'active' : ''}}\">设施一般</a></li>\n        <li><a @click=\"storePush(7, 3, 11)\" class=\"{{storeLabel.contains(7) ? 'active' : ''}}\">性价比低</a></li>\n        <li><a @click=\"storePush(8, 4, 12)\" class=\"{{storeLabel.contains(8) ? 'active' : ''}}\">有待提高</a></li>\n        <li><a @click=\"storePush(9, 1, 5)\" class=\"{{storeLabel.contains(9) ? 'active' : ''}}\">环境恶劣</a></li>\n        <li><a @click=\"storePush(10, 2, 6)\" class=\"{{storeLabel.contains(10) ? 'active' : ''}}\">设施陈旧</a></li>\n        <li><a @click=\"storePush(11, 3, 7)\" class=\"{{storeLabel.contains(11) ? 'active' : ''}}\">价格坑爹</a></li>\n        <li><a @click=\"storePush(12, 4, 8)\" class=\"{{storeLabel.contains(12) ? 'active' : ''}}\">绝不再来</a></li>\n        <div class=\"clear\"></div>\n    </ul>\n\n    <div class=\"title\">技师</div>\n    <ul class=\"comment-li-container\">\n        <li><a @click=\"tecPush(1, 5, 9)\" class=\"{{tecLabel.contains(1) ? 'active' : ''}}\">服务热情</a></li>\n        <li><a @click=\"tecPush(2, 6, 10)\" class=\"{{tecLabel.contains(2) ? 'active' : ''}}\">手法专业</a></li>\n        <li><a @click=\"tecPush(3, 7, 11)\" class=\"{{tecLabel.contains(3) ? 'active' : ''}}\">态度很好</a></li>\n        <li><a @click=\"tecPush(4, 8, 12)\" class=\"{{tecLabel.contains(4) ? 'active' : ''}}\">颜值爆表</a></li>\n        <li><a @click=\"tecPush(5, 1, 9)\" class=\"{{tecLabel.contains(5) ? 'active' : ''}}\">服务一般</a></li>\n        <li><a @click=\"tecPush(6, 2, 10)\" class=\"{{tecLabel.contains(6) ? 'active' : ''}}\">手法一般</a></li>\n        <li><a @click=\"tecPush(7, 3, 11)\" class=\"{{tecLabel.contains(7) ? 'active' : ''}}\">态度一般</a></li>\n        <li><a @click=\"tecPush(8, 4, 12)\" class=\"{{tecLabel.contains(8) ? 'active' : ''}}\">看的过去</a></li>\n        <li><a @click=\"tecPush(9, 1, 5)\" class=\"{{tecLabel.contains(9) ? 'active' : ''}}\">服务太差</a></li>\n        <li><a @click=\"tecPush(10, 2, 6)\" class=\"{{tecLabel.contains(10) ? 'active' : ''}}\">手法太差</a></li>\n        <li><a @click=\"tecPush(11, 3, 7)\" class=\"{{tecLabel.contains(11) ? 'active' : ''}}\">态度恶劣</a></li>\n        <li><a @click=\"tecPush(12, 4, 8)\" class=\"{{tecLabel.contains(12) ? 'active' : ''}}\">惨不忍睹</a></li>\n        <div class=\"clear\"></div>\n        <textarea placeholder=\"亲！可以再次输入对店铺及技师的服务评价\" v-model=\"comment\"></textarea>\n        <p>{{comment.length}}/200</p>\n    </ul>\n\n    <div class=\"title\">店铺及技师评分(满意给5星哦)</div>\n    <div class=\"winslow-container\">\n        <p class=\"pingfen\">店铺评分 <start :value.sync=\"scoreStore\" :ischange='true' :big='true'></start></p>\n        <p class=\"pingfen\">技师评分 <start :value.sync=\"scoreMassagist\" :ischange='true' :big='true'></start></p>\n    </div>\n\n    <button type=\"button\" class=\"mui-btn mui-btn-warning mui-btn-block\" style=\"width: 70%;margin: 20px auto;\" @click=\"submit\">发表评价</button>\n    </div></div>\n</div>\n";

/***/ },
/* 180 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(181)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/consumer-pay.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(182)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-738473ec/consumer-pay.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 181 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	
	var _start = __webpack_require__(44);
	
	var _start2 = _interopRequireDefault(_start);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	exports.default = {
	    name: 'consumerPay',
	    data: function data() {
	        return {
	            userOrderInfo: {}
	        };
	    },
	    ready: function ready() {
	        var _this = this;
	
	        document.title = '付款详情';
	        mui('.mui-scroll-wrapper').scroll({ bounce: false });
	        this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	        this.$http.post(API.getStoreWx, {
	            oid: this.$route.params.id,
	            sid: this.$route.params.storeid
	        }).then(function (data) {
	            if (data.data.errorInfo) return;
	            _this.$store.state.hideTip();
	            _this.userOrderInfo = data.data.object;
	        });
	    },
	    methods: {
	        local: function local(name) {
	            this.$router.go('/' + name + '/' + this.$route.params.token);
	        },
	        pay: function pay() {
	            window.location.href = API.wxPayOrder + '?token=' + this.$route.params.token + '&sid=' + this.$route.params.storeid + '&num=' + this.userOrderInfo.order_num;
	        }
	    },
	    components: {
	        Start: _start2.default
	    }
	};
	
	// </script>
	/* generated by vue-loader */
	// <template>
	// <div class="page consumer">
	//     <header id="header" class="mui-bar mui-bar-nav haed2 ">
	//         <h1 class="mui-title">付款详情</h1>
	//         <a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a>
	//     </header>
	//     <div class="mui-scroll-wrapper" style="bottom: 0px;">
	//     <div class="mui-scroll">
	//     <div class="imgs">
	//         订单信息：{{userOrderInfo.item_name}} 技师：{{userOrderInfo.massagist_name}}
	//     </div>
	//     <div class="imgs">
	//         支付方式：
	//         <a class="active">微信在现在支付</a>
	//         <a>线下支付</a>
	//     </div>
	//     <div class="imgs">
	//         需付款：<em>￥{{userOrderInfo.order_totalprice}}</em>
	//     </div>
	//
	//     <button type="button" class="mui-btn mui-btn-warning mui-btn-block" style="width: 90%;margin: 20px auto;" @click="pay">付款</button>
	//     </div></div>
	// </div>
	// </template>
	//
	// <script>

/***/ },
/* 182 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page consumer\">\n    <header id=\"header\" class=\"mui-bar mui-bar-nav haed2 \">\n        <h1 class=\"mui-title\">付款详情</h1>\n        <a class=\"mui-action-back mui-icon mui-icon-left-nav mui-pull-left\"></a>\n    </header>\n    <div class=\"mui-scroll-wrapper\" style=\"bottom: 0px;\">\n    <div class=\"mui-scroll\">\n    <div class=\"imgs\">\n        订单信息：{{userOrderInfo.item_name}} 技师：{{userOrderInfo.massagist_name}}\n    </div>\n    <div class=\"imgs\">\n        支付方式：\n        <a class=\"active\">微信在现在支付</a>\n        <a>线下支付</a>\n    </div>\n    <div class=\"imgs\">\n        需付款：<em>￥{{userOrderInfo.order_totalprice}}</em>\n    </div>\n\n    <button type=\"button\" class=\"mui-btn mui-btn-warning mui-btn-block\" style=\"width: 90%;margin: 20px auto;\" @click=\"pay\">付款</button>\n    </div></div>\n</div>\n";

/***/ },
/* 183 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(184)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/consumer-images.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(185)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-14d244e4/consumer-images.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 184 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	
	var _assign = __webpack_require__(138);
	
	var _assign2 = _interopRequireDefault(_assign);
	
	var _exif = __webpack_require__(142);
	
	var _exif2 = _interopRequireDefault(_exif);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	exports.default = {
	    name: 'consumerImages',
	    data: function data() {
	        return {
	            showCrop: false,
	            cropImg: '',
	            massagistInfo: {
	                massagist_image: []
	            },
	            changeId: 0
	        };
	    },
	    ready: function ready() {
	        var _this = this;
	
	        document.title = '个人封面';
	        mui('.mui-scroll-wrapper').scroll({ bounce: false });
	        this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	        this.$http.post(API.getMassagistInfo, {}).then(function (data) {
	            if (data.data.errorInfo) return;
	            _this.$store.state.hideTip();
	            _this.massagistInfo = data.data.object;
	            if (_this.massagistInfo.massagist_photo.indexOf('http') < 0) {
	                _this.massagistInfo.massagist_photo = _this.$store.state.URL + _this.massagistInfo.massagist_photo;
	            }
	        });
	    },
	    methods: {
	        local: function local(name) {
	            this.$router.go('/' + name + '/' + this.$route.params.token);
	        },
	        del: function del(id) {
	            var me = this;
	            if (me.massagistInfo.massagist_image.length <= 1) {
	                me.$store.state.showTip({ type: 'error', content: '至少保留一张封面图片' });
	                return;
	            }
	            mui.confirm('确定删除这张封面吗？', '提醒', ['取消', '确定'], function (e) {
	                if (e.index == 1) {
	                    me.$store.state.showTip({ type: 'loading', content: '删除中...' });
	                    me.$http.post(API.deleteTechnicianImg, {
	                        img_id: id
	                    }).then(function (data) {
	                        if (data.data.errorInfo) return;
	                        me.$store.state.hideTip();
	                        me.$store.state.showTip({ type: 'success', content: '删除成功' });
	                        location.reload();
	                    });
	                }
	            });
	        },
	        changePic: function changePic(id) {
	            this.changeId = parseInt(id);
	            this.file();
	        },
	        add: function add() {
	            this.changeId = 0;
	            this.file();
	        },
	        file: function file() {
	            var u = navigator.userAgent;
	            var isAndroid = u.indexOf('Android') > -1 || u.indexOf('Adr') > -1;
	            var isiOS = !!u.match(/\(i[^;]+;( U;)? CPU.+Mac OS X/);
	
	            if (isAndroid) mui('#fileCrop')[0].click();else if (isiOS) {
	                mui('#fileCrop')[0].click();
	                mui('#fileCrop')[0].click();
	            }
	        },
	        change: function change(e) {
	            this.$store.state.showTip({ type: 'loading', content: '努力加载图像中...' });
	            this.showCrop = true;
	            var orientation = null;
	            var FILE = e.target.files[0];
	
	            _exif2.default.getData(FILE, function () {
	                var obj = (0, _assign2.default)({}, _exif2.default.getAllTags(this));
	                orientation = obj.Orientation;
	            });
	
	            var reader = new FileReader();
	            var self = this;
	
	            reader.onload = function (e) {
	                self.$store.state.__getImgData(e.target.result, orientation, function (data) {
	                    var pic = new Image();
	                    pic.src = data;
	                    pic.onload = function () {
	                        self.cropImg = data;
	                        setTimeout(function () {
	                            $(".cropper").cropper({ aspectRatio: 1 / 0.466 });
	                            self.$store.state.hideTip();
	                        }, 100);
	                    };
	                });
	            };
	            reader.readAsDataURL(FILE);
	        },
	        saveCrop: function saveCrop() {
	            var _this2 = this;
	
	            this.$store.state.showTip({ type: 'loading', content: '上传中...' });
	            setTimeout(function () {
	                var dataURL = $(".cropper").cropper("getDataURL");
	                var picURL = dataURL.replace(/^.*?,/, '');
	                _this2.$http.post(API.uploadImg, {
	                    img_id: _this2.changeId,
	                    base64: picURL
	                }).then(function (data) {
	                    if (data.data.errorInfo) return;
	                    _this2.$store.state.hideTip();
	                    _this2.$store.state.locationReload();
	                });
	            }, 100);
	        }
	    }
	};
	
	// </script>
	/* generated by vue-loader */
	// <template>
	// <div class="page consumer" style="background-color: #444;">
	//     <header id="header" class="mui-bar mui-bar-nav bar-images">
	//         <h1 class="mui-title">个人封面</h1>
	//         <a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a>
	//     </header>
	//
	//     <div class="mui-scroll-wrapper" style="bottom: 0px;">
	//     <div class="mui-scroll">
	//     <ul class="images-container">
	//         <li v-for="item in massagistInfo.massagist_image">
	//             <img :src=" $store.state.URL + item.img_url ">
	//             <a @click="del(item.img_id)">删除</a><a @click="changePic(item.img_id)">更换</a>
	//         </li>
	//         <li class="notitle" v-if="3-massagistInfo.massagist_image.length>2" @click="add"><img :src="'images/add.jpg'"></li>
	//         <li class="notitle" v-if="3-massagistInfo.massagist_image.length>1" @click="add"><img :src="'images/add.jpg'"></li>
	//         <li class="notitle" v-if="3-massagistInfo.massagist_image.length>0" @click="add"><img :src="'images/add.jpg'"></li>
	//     </ul>
	//     </div></div>
	//     <input id="fileCrop" style="opacity: 0;" type="file" accept="image/*" v-on:change="change">
	//     <div class="crop-container" v-show="showCrop">
	//         <div class="wrap">
	//             <img class="cropper" :src="cropImg">
	//         </div>
	//         <div class="btn">
	//             <div><button type="button" class="mui-btn mui-btn-block" @click="$store.state.locationReload()">取消</button></div>
	//             <div><button type="button" class="mui-btn mui-btn-primary mui-btn-block" @click="saveCrop">保存</button></div>
	//         </div>
	//     </div>
	//
	// </div>
	// </template>
	//
	// <script>

/***/ },
/* 185 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page consumer\" style=\"background-color: #444;\">\n    <header id=\"header\" class=\"mui-bar mui-bar-nav bar-images\">\n        <h1 class=\"mui-title\">个人封面</h1>\n        <a class=\"mui-action-back mui-icon mui-icon-left-nav mui-pull-left\"></a>\n    </header>\n\n    <div class=\"mui-scroll-wrapper\" style=\"bottom: 0px;\">\n    <div class=\"mui-scroll\">\n    <ul class=\"images-container\">\n        <li v-for=\"item in massagistInfo.massagist_image\">\n            <img :src=\" $store.state.URL + item.img_url \">\n            <a @click=\"del(item.img_id)\">删除</a><a @click=\"changePic(item.img_id)\">更换</a>\n        </li>\n        <li class=\"notitle\" v-if=\"3-massagistInfo.massagist_image.length>2\" @click=\"add\"><img :src=\"'images/add.jpg'\"></li>\n        <li class=\"notitle\" v-if=\"3-massagistInfo.massagist_image.length>1\" @click=\"add\"><img :src=\"'images/add.jpg'\"></li>\n        <li class=\"notitle\" v-if=\"3-massagistInfo.massagist_image.length>0\" @click=\"add\"><img :src=\"'images/add.jpg'\"></li>\n    </ul>\n    </div></div>\n    <input id=\"fileCrop\" style=\"opacity: 0;\" type=\"file\" accept=\"image/*\" v-on:change=\"change\">\n    <div class=\"crop-container\" v-show=\"showCrop\">\n        <div class=\"wrap\">\n            <img class=\"cropper\" :src=\"cropImg\">\n        </div>\n        <div class=\"btn\">\n            <div><button type=\"button\" class=\"mui-btn mui-btn-block\" @click=\"$store.state.locationReload()\">取消</button></div>\n            <div><button type=\"button\" class=\"mui-btn mui-btn-primary mui-btn-block\" @click=\"saveCrop\">保存</button></div>\n        </div>\n    </div>\n\n</div>\n";

/***/ },
/* 186 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(187)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/manage/shop.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(188)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-1adecfd6/shop.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 187 */
/***/ function(module, exports) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	// <template>
	// <div class="page manage">
	//     <header id="header" class="mui-bar mui-bar-nav">
	//         <h1 class="mui-title">店铺信息</h1>
	//         <!-- <a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a> -->
	//     </header>
	//
	//     <div class="mui-scroll-wrapper">
	//     <div class="mui-scroll">
	//
	//     <div id="sliderSegmentedControl" class="mui-slider-indicator mui-segmented-control mui-segmented-control-inverted">
	//         <a class="winslow-control-item mui-active" @click="local('manage-shop')" >店铺信息</a>
	//         <a class="winslow-control-item" @click="local('manage-project')">项目管理</a>
	//         <a class="winslow-control-item" @click="local('manage-tech')">技师管理</a>
	//     </div>
	//
	//     <div id="slider" class="mui-slider" >
	//         <div class="mui-slider-group mui-slider-loop">
	//             <div class="mui-slider-item mui-slider-item-duplicate">
	//                 <a>
	//                     <img :src=" $store.state.URL + massagistInfo.imgs[massagistInfo.imgs.length - 1].image_address ">
	//                 </a>
	//             </div>
	//
	//             <div class="mui-slider-item" v-for="item in massagistInfo.imgs">
	//                 <a>
	//                     <img :src="$store.state.URL + item.image_address">
	//                 </a>
	//             </div>
	//
	//             <div class="mui-slider-item mui-slider-item-duplicate">
	//                 <a>
	//                     <img :src=" $store.state.URL + massagistInfo.imgs[0].image_address ">
	//                 </a>
	//             </div>
	//         </div>
	//         <div class="mui-slider-indicator">
	//             <div class="mui-indicator {{$index == 0 ? 'mui-active' : ''}}" v-for="item in massagistInfo.imgs"></div>
	//         </div>
	//     </div>
	//
	//     <div class="winslow-container">
	//         <h1>{{massagistInfo.info.store_name}}</h1>
	//         <p><i class="iconfont">&#xe603;</i>{{massagistInfo.info.store_address}}</p>
	//         <p><a href="tel:{{massagistInfo.info.store_tel}}"><i class="iconfont">&#xe608;</i>{{massagistInfo.info.store_tel}}</a></p>
	//     </div>
	//     </div>
	//     </div>
	//
	//     <nav class="mui-bar mui-bar-tab">
	//         <a class="winslow-tab-item mui-active">
	//             <span class="iconfont">&#xe67d;</span>
	//             <span class="mui-tab-label">管理</span>
	//         </a>
	//         <a class="winslow-tab-item" @click="local('manage-shop-chart')">
	//             <span class="iconfont">&#xe671;</span>
	//             <span class="mui-tab-label">报表</span>
	//         </a>
	//         <a class="winslow-tab-item" @click="local('manage-shop-comment')">
	//             <span class="iconfont">&#xe706;</span>
	//             <span class="mui-tab-label">店铺评价</span>
	//         </a>
	//     </nav>
	//
	//
	// </div>
	// </template>
	//
	// <script>
	exports.default = {
	    name: 'manageShop',
	    data: function data() {
	        return {
	            massagistInfo: {
	                imgs: [],
	                info: {}
	            }
	        };
	    },
	    ready: function ready() {
	        var _this = this;
	
	        document.title = '店铺信息';
	        mui('.mui-scroll-wrapper').scroll({ bounce: false });
	        mui("#slider").slider();
	        this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	        this.$http.post(API.getStoreContents, {}).then(function (data) {
	            if (data.data.errorInfo) return;
	            _this.$store.state.hideTip();
	            _this.massagistInfo = data.data.list;
	        });
	    },
	    methods: {
	        local: function local(name) {
	            this.$router.go('/' + name + '/' + this.$route.params.token);
	        }
	    }
	};
	
	// </script>
	/* generated by vue-loader */

/***/ },
/* 188 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page manage\">\n    <header id=\"header\" class=\"mui-bar mui-bar-nav\">\n        <h1 class=\"mui-title\">店铺信息</h1>\n        <!-- <a class=\"mui-action-back mui-icon mui-icon-left-nav mui-pull-left\"></a> -->\n    </header>\n\n    <div class=\"mui-scroll-wrapper\">\n    <div class=\"mui-scroll\">\n    \n    <div id=\"sliderSegmentedControl\" class=\"mui-slider-indicator mui-segmented-control mui-segmented-control-inverted\">\n        <a class=\"winslow-control-item mui-active\" @click=\"local('manage-shop')\" >店铺信息</a>\n        <a class=\"winslow-control-item\" @click=\"local('manage-project')\">项目管理</a>\n        <a class=\"winslow-control-item\" @click=\"local('manage-tech')\">技师管理</a>\n    </div>\n\n    <div id=\"slider\" class=\"mui-slider\" >\n        <div class=\"mui-slider-group mui-slider-loop\">\n            <div class=\"mui-slider-item mui-slider-item-duplicate\">\n                <a>\n                    <img :src=\" $store.state.URL + massagistInfo.imgs[massagistInfo.imgs.length - 1].image_address \">\n                </a>\n            </div>\n\n            <div class=\"mui-slider-item\" v-for=\"item in massagistInfo.imgs\">\n                <a>\n                    <img :src=\"$store.state.URL + item.image_address\">\n                </a>\n            </div>\n\n            <div class=\"mui-slider-item mui-slider-item-duplicate\">\n                <a>\n                    <img :src=\" $store.state.URL + massagistInfo.imgs[0].image_address \">\n                </a>\n            </div>\n        </div>\n        <div class=\"mui-slider-indicator\">\n            <div class=\"mui-indicator {{$index == 0 ? 'mui-active' : ''}}\" v-for=\"item in massagistInfo.imgs\"></div>\n        </div>\n    </div>\n\n    <div class=\"winslow-container\">\n        <h1>{{massagistInfo.info.store_name}}</h1>\n        <p><i class=\"iconfont\">&#xe603;</i>{{massagistInfo.info.store_address}}</p>\n        <p><a href=\"tel:{{massagistInfo.info.store_tel}}\"><i class=\"iconfont\">&#xe608;</i>{{massagistInfo.info.store_tel}}</a></p>\n    </div>\n    </div>\n    </div>\n\n    <nav class=\"mui-bar mui-bar-tab\">\n        <a class=\"winslow-tab-item mui-active\">\n            <span class=\"iconfont\">&#xe67d;</span>\n            <span class=\"mui-tab-label\">管理</span>\n        </a>\n        <a class=\"winslow-tab-item\" @click=\"local('manage-shop-chart')\">\n            <span class=\"iconfont\">&#xe671;</span>\n            <span class=\"mui-tab-label\">报表</span>\n        </a>\n        <a class=\"winslow-tab-item\" @click=\"local('manage-shop-comment')\">\n            <span class=\"iconfont\">&#xe706;</span>\n            <span class=\"mui-tab-label\">店铺评价</span>\n        </a>\n    </nav>\n    \n    \n</div>\n";

/***/ },
/* 189 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(190)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/manage/shop-comment.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(191)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-448cf8b2/shop-comment.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 190 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	
	var _start = __webpack_require__(44);
	
	var _start2 = _interopRequireDefault(_start);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	exports.default = {
	    name: 'manageShopComment',
	    data: function data() {
	        return {
	            info: {},
	            all: {
	                list: [],
	                page: 0,
	                btn: false
	            },
	            bad: {
	                list: [],
	                page: 0,
	                btn: false
	            },
	            good: {
	                list: [],
	                page: 0,
	                btn: false
	            },
	            midd: {
	                list: [],
	                page: 0,
	                btn: false
	            }
	        };
	    },
	    ready: function ready() {
	        document.title = '店铺评价';
	        mui('.mui-scroll-wrapper').scroll({ bounce: false });
	        this.getAll();
	        this.getGood();
	        this.getMidd();
	        this.getBad();
	    },
	    methods: {
	        local: function local(name) {
	            this.$router.go('/' + name + '/' + this.$route.params.token);
	        },
	        getAll: function getAll() {
	            var _this = this;
	
	            this.all.page++;
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.getTechnicianEvaluate, {
	                page: this.all.page
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this.$store.state.hideTip();
	                if (!data.data.list) {
	                    _this.all.btn = false;
	                    return;
	                } else if (data.data.list.length < _this.$store.state.pageSize) {
	                    _this.all.btn = false;
	                } else {
	                    _this.all.btn = true;
	                };
	                _this.all.list = _this.all.list.concat(data.data.list);
	                _this.info = data.data.object;
	                for (var i = 0; i < _this.all.list.length; i++) {
	                    if (!_this.all.list[i].wechat_user_viptime) {
	                        _this.all.list[i].wechat_userlevel = 0;
	                    } else if (_this.all.list[i].wechat_user_viptime == '1') {
	                        _this.all.list[i].wechat_userlevel = 7;
	                    } else if (_this.all.list[i].wechat_user_viptime - Date.parse(new Date()) / 1000 <= 0) {
	                        _this.all.list[i].wechat_userlevel = 0;
	                    } else {
	                        _this.all.list[i].wechat_userlevel = 1;
	                    }
	                    if (_this.all.list[i].wechat_headimgurl.indexOf('http') < 0) {
	                        _this.all.list[i].wechat_headimgurl = _this.$store.state.URL + _this.all.list[i].wechat_headimgurl;
	                    }
	                }
	            });
	        },
	        getGood: function getGood() {
	            var _this2 = this;
	
	            this.good.page++;
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.getTechnicianEvaluate, {
	                page: this.good.page,
	                type: 1
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this2.$store.state.hideTip();
	                if (!data.data.list) {
	                    _this2.good.btn = false;
	                    return;
	                } else if (data.data.list.length < _this2.$store.state.pageSize) {
	                    _this2.good.btn = false;
	                } else {
	                    _this2.good.btn = true;
	                };
	                _this2.good.list = _this2.good.list.concat(data.data.list);
	                _this2.info = data.data.object;
	                for (var i = 0; i < _this2.good.list.length; i++) {
	                    if (!_this2.good.list[i].wechat_user_viptime) {
	                        _this2.good.list[i].wechat_userlevel = 0;
	                    } else if (_this2.good.list[i].wechat_user_viptime == '1') {
	                        _this2.good.list[i].wechat_userlevel = 7;
	                    } else if (_this2.good.list[i].wechat_user_viptime - Date.parse(new Date()) / 1000 <= 0) {
	                        _this2.good.list[i].wechat_userlevel = 0;
	                    } else {
	                        _this2.good.list[i].wechat_userlevel = 1;
	                    }
	                    if (_this2.good.list[i].wechat_headimgurl.indexOf('http') < 0) {
	                        _this2.good.list[i].wechat_headimgurl = _this2.$store.state.URL + _this2.good.list[i].wechat_headimgurl;
	                    }
	                }
	            });
	        },
	        getMidd: function getMidd() {
	            var _this3 = this;
	
	            this.midd.page++;
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.getTechnicianEvaluate, {
	                page: this.midd.page,
	                type: 2
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this3.$store.state.hideTip();
	                if (!data.data.list) {
	                    _this3.midd.btn = false;
	                    return;
	                } else if (data.data.list.length < _this3.$store.state.pageSize) {
	                    _this3.midd.btn = false;
	                } else {
	                    _this3.midd.btn = true;
	                };
	                _this3.midd.list = _this3.midd.list.concat(data.data.list);
	                _this3.info = data.data.object;
	                for (var i = 0; i < _this3.midd.list.length; i++) {
	                    if (!_this3.midd.list[i].wechat_user_viptime) {
	                        _this3.midd.list[i].wechat_userlevel = 0;
	                    } else if (_this3.midd.list[i].wechat_user_viptime == '1') {
	                        _this3.midd.list[i].wechat_userlevel = 7;
	                    } else if (_this3.midd.list[i].wechat_user_viptime - Date.parse(new Date()) / 1000 <= 0) {
	                        _this3.midd.list[i].wechat_userlevel = 0;
	                    } else {
	                        _this3.midd.list[i].wechat_userlevel = 1;
	                    }
	                    if (_this3.midd.list[i].wechat_headimgurl.indexOf('http') < 0) {
	                        _this3.midd.list[i].wechat_headimgurl = _this3.$store.state.URL + _this3.midd.list[i].wechat_headimgurl;
	                    }
	                }
	            });
	        },
	        getBad: function getBad() {
	            var _this4 = this;
	
	            this.bad.page++;
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.getTechnicianEvaluate, {
	                page: this.bad.page,
	                type: 3
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this4.$store.state.hideTip();
	                if (!data.data.list) {
	                    _this4.bad.btn = false;
	                    return;
	                } else if (data.data.list.length < _this4.$store.state.pageSize) {
	                    _this4.bad.btn = false;
	                } else {
	                    _this4.bad.btn = true;
	                };
	                _this4.bad.list = _this4.bad.list.concat(data.data.list);
	                _this4.info = data.data.object;
	                for (var i = 0; i < _this4.bad.list.length; i++) {
	                    if (!_this4.bad.list[i].wechat_user_viptime) {
	                        _this4.bad.list[i].wechat_userlevel = 0;
	                    } else if (_this4.bad.list[i].wechat_user_viptime == '1') {
	                        _this4.bad.list[i].wechat_userlevel = 7;
	                    } else if (_this4.bad.list[i].wechat_user_viptime - Date.parse(new Date()) / 1000 <= 0) {
	                        _this4.bad.list[i].wechat_userlevel = 0;
	                    } else {
	                        _this4.bad.list[i].wechat_userlevel = 1;
	                    }
	                    if (_this4.bad.list[i].wechat_headimgurl.indexOf('http') < 0) {
	                        _this4.bad.list[i].wechat_headimgurl = _this4.$store.state.URL + _this4.bad.list[i].wechat_headimgurl;
	                    }
	                }
	            });
	        }
	    },
	    components: {
	        Start: _start2.default
	    }
	};
	
	// </script>
	/* generated by vue-loader */
	// <template>
	// <div class="page manage">
	//     <header id="header" class="mui-bar mui-bar-nav">
	//         <h1 class="mui-title">店铺评价</h1>
	//         <!-- <a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a> -->
	//     </header>
	//     <div class="mui-scroll-wrapper">
	//     <div class="mui-scroll">
	//     <div id="sliderSegmentedControl" class="mui-slider-indicator mui-segmented-control mui-segmented-control-inverted">
	//         <a class="mui-control-item mui-active" href="#item1">全部({{info ? info.all : '0'}})</a>
	//         <a class="mui-control-item" href="#item2">好评({{info ? info.good : '0'}})</a>
	//         <a class="mui-control-item" href="#item3">中评({{info ? info.middle : '0'}})</a>
	//         <a class="mui-control-item" href="#item4">差评({{info ? info.bad : '0'}})</a>
	//     </div>
	//
	//     <div id="item1" class="mui-control-content fade-in-left mui-active">
	//         <ul class="comment-item">
	//             <li v-for="item in all.list">
	//                 <div class="item">
	//                     <img :src="item.wechat_headimgurl" class="head">
	//                     {{item.wechat_nickname}} <i class="lv lv{{item.wechat_userlevel}}"></i><br>
	//                     <div class="starts-container"><start :value="parseInt(item.comment_score_store)"><start></div>
	//                     <h6>
	//                         {{item.comment_time | format 'yyyy-MM-dd'}}
	//                     </h6>
	//                     <div class="tag">
	//                         <a v-for="tag in item.comment_store_label" v-if="tag!=''">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>
	//                     </div>
	//                     <p>{{item.comment}}</p>
	//                     <small v-for="reply in item.reply">
	//                         <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>
	//                         {{reply.comment}}
	//                         <br>
	//                     </small>
	//                 </div>
	//             </li>
	//             <div class="no-data" v-if="all.list.length<=0">
	//                 <h1>没有相关评论</h1>
	//             </div>
	//             <a class="btn-more" v-if="all.btn" @click="getAll">查看更多</a>
	//         </ul>
	//     </div>
	//
	//     <div id="item2" class="mui-control-content fade-in-left">
	//         <ul class="comment-item">
	//             <li v-for="item in good.list" v-if="item.comment_level == '1'">
	//                 <div class="item">
	//                     <img :src="item.wechat_headimgurl" class="head">
	//                     {{item.wechat_nickname}} <i class="lv lv{{item.wechat_userlevel}}"></i><br>
	//                     <div class="starts-container"><start :value="parseInt(item.comment_score_store)"><start></div>
	//                     <h6>{{item.comment_time | format 'yyyy-MM-dd'}}</h6>
	//                     <div class="tag">
	//                         <a v-for="tag in item.comment_store_label" v-if="tag!=''">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>
	//                     </div>
	//                     <p>{{item.comment}}</p>
	//                     <small v-for="reply in item.reply">
	//                         <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>
	//                         {{reply.comment}}
	//                         <br>
	//                     </small>
	//                 </div>
	//             </li>
	//             <div class="no-data" v-if="good.list.length<=0">
	//                 <h1>没有相关评论</h1>
	//             </div>
	//             <a class="btn-more" v-if="good.btn" @click="getGood">查看更多</a>
	//         </ul>
	//     </div>
	//
	//     <div id="item3" class="mui-control-content fade-in-left">
	//         <ul class="comment-item">
	//             <li v-for="item in midd.list" v-if="item.comment_level == '2'">
	//                 <div class="item">
	//                     <img :src="item.wechat_headimgurl" class="head">
	//                     {{item.wechat_nickname}} <i class="lv lv{{item.wechat_userlevel}}"></i><br>
	//                     <div class="starts-container"><start :value="parseInt(item.comment_score_store)"><start></div>
	//                     <h6>{{item.comment_time | format 'yyyy-MM-dd'}}</h6>
	//                     <div class="tag">
	//                         <a v-for="tag in item.comment_store_label" v-if="tag!=''">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>
	//                     </div>
	//                     <p>{{item.comment}}</p>
	//                     <small v-for="reply in item.reply">
	//                         <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>
	//                         {{reply.comment}}
	//                         <br>
	//                     </small>
	//                 </div>
	//             </li>
	//             <div class="no-data" v-if="midd.list.length<=0">
	//                 <h1>没有相关评论</h1>
	//             </div>
	//             <a class="btn-more" v-if="midd.btn" @click="getMidd">查看更多</a>
	//         </ul>
	//     </div>
	//
	//     <div id="item4" class="mui-control-content fade-in-left">
	//         <ul class="comment-item">
	//             <li v-for="item in bad.list" v-if="item.comment_level == '3'">
	//                 <div class="item">
	//                     <img :src="item.wechat_headimgurl" class="head">
	//                     {{item.wechat_nickname}} <i class="lv lv{{item.wechat_userlevel}}"></i><br>
	//                     <div class="starts-container"><start :value="parseInt(item.comment_score_store)"><start></div>
	//                     <h6>{{item.comment_time | format 'yyyy-MM-dd'}}</h6>
	//                     <div class="tag">
	//                         <a v-for="tag in item.comment_store_label" v-if="tag!=''">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>
	//                     </div>
	//                     <p>{{item.comment}}</p>
	//                     <small v-for="reply in item.reply">
	//                         <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>
	//                         {{reply.comment}}
	//                         <br>
	//                     </small>
	//                 </div>
	//             </li>
	//             <div class="no-data" v-if="bad.list.length<=0">
	//                 <h1>没有相关评论</h1>
	//             </div>
	//             <a class="btn-more" v-if="bad.btn" @click="getBad">查看更多</a>
	//         </ul>
	//     </div>
	//     </div>
	//     </div>
	//
	//     <nav class="mui-bar mui-bar-tab">
	//         <a class="winslow-tab-item" @click="local('manage-shop')">
	//             <span class="iconfont">&#xe67d;</span>
	//             <span class="mui-tab-label">管理</span>
	//         </a>
	//         <a class="winslow-tab-item" @click="local('manage-shop-chart')">
	//             <span class="iconfont">&#xe671;</span>
	//             <span class="mui-tab-label">报表</span>
	//         </a>
	//         <a class="winslow-tab-item mui-active">
	//             <span class="iconfont">&#xe706;</span>
	//             <span class="mui-tab-label">店铺评价</span>
	//         </a>
	//     </nav>
	//
	//
	// </div>
	// </template>
	//
	// <script>

/***/ },
/* 191 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page manage\">\n    <header id=\"header\" class=\"mui-bar mui-bar-nav\">\n        <h1 class=\"mui-title\">店铺评价</h1>\n        <!-- <a class=\"mui-action-back mui-icon mui-icon-left-nav mui-pull-left\"></a> -->\n    </header>\n    <div class=\"mui-scroll-wrapper\">\n    <div class=\"mui-scroll\">\n    <div id=\"sliderSegmentedControl\" class=\"mui-slider-indicator mui-segmented-control mui-segmented-control-inverted\">\n        <a class=\"mui-control-item mui-active\" href=\"#item1\">全部({{info ? info.all : '0'}})</a>\n        <a class=\"mui-control-item\" href=\"#item2\">好评({{info ? info.good : '0'}})</a>\n        <a class=\"mui-control-item\" href=\"#item3\">中评({{info ? info.middle : '0'}})</a>\n        <a class=\"mui-control-item\" href=\"#item4\">差评({{info ? info.bad : '0'}})</a>\n    </div>\n    \n    <div id=\"item1\" class=\"mui-control-content fade-in-left mui-active\">\n        <ul class=\"comment-item\">\n            <li v-for=\"item in all.list\">\n                <div class=\"item\">\n                    <img :src=\"item.wechat_headimgurl\" class=\"head\">\n                    {{item.wechat_nickname}} <i class=\"lv lv{{item.wechat_userlevel}}\"></i><br>\n                    <div class=\"starts-container\"><start :value=\"parseInt(item.comment_score_store)\"><start></div>\n                    <h6>\n                        {{item.comment_time | format 'yyyy-MM-dd'}}\n                    </h6>\n                    <div class=\"tag\">\n                        <a v-for=\"tag in item.comment_store_label\" v-if=\"tag!=''\">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>\n                    </div>\n                    <p>{{item.comment}}</p>\n                    <small v-for=\"reply in item.reply\">\n                        <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>\n                        {{reply.comment}}\n                        <br>\n                    </small>\n                </div>\n            </li>\n            <div class=\"no-data\" v-if=\"all.list.length<=0\">\n                <h1>没有相关评论</h1>\n            </div>\n            <a class=\"btn-more\" v-if=\"all.btn\" @click=\"getAll\">查看更多</a>\n        </ul>\n    </div>\n\n    <div id=\"item2\" class=\"mui-control-content fade-in-left\">\n        <ul class=\"comment-item\">\n            <li v-for=\"item in good.list\" v-if=\"item.comment_level == '1'\">\n                <div class=\"item\">\n                    <img :src=\"item.wechat_headimgurl\" class=\"head\">\n                    {{item.wechat_nickname}} <i class=\"lv lv{{item.wechat_userlevel}}\"></i><br>\n                    <div class=\"starts-container\"><start :value=\"parseInt(item.comment_score_store)\"><start></div>\n                    <h6>{{item.comment_time | format 'yyyy-MM-dd'}}</h6>\n                    <div class=\"tag\">\n                        <a v-for=\"tag in item.comment_store_label\" v-if=\"tag!=''\">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>\n                    </div>\n                    <p>{{item.comment}}</p>\n                    <small v-for=\"reply in item.reply\">\n                        <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>\n                        {{reply.comment}}\n                        <br>\n                    </small>\n                </div>\n            </li>\n            <div class=\"no-data\" v-if=\"good.list.length<=0\">\n                <h1>没有相关评论</h1>\n            </div>\n            <a class=\"btn-more\" v-if=\"good.btn\" @click=\"getGood\">查看更多</a>\n        </ul>\n    </div>\n\n    <div id=\"item3\" class=\"mui-control-content fade-in-left\">\n        <ul class=\"comment-item\">\n            <li v-for=\"item in midd.list\" v-if=\"item.comment_level == '2'\">\n                <div class=\"item\">\n                    <img :src=\"item.wechat_headimgurl\" class=\"head\">\n                    {{item.wechat_nickname}} <i class=\"lv lv{{item.wechat_userlevel}}\"></i><br>\n                    <div class=\"starts-container\"><start :value=\"parseInt(item.comment_score_store)\"><start></div>\n                    <h6>{{item.comment_time | format 'yyyy-MM-dd'}}</h6>\n                    <div class=\"tag\">\n                        <a v-for=\"tag in item.comment_store_label\" v-if=\"tag!=''\">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>\n                    </div>\n                    <p>{{item.comment}}</p>\n                    <small v-for=\"reply in item.reply\">\n                        <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>\n                        {{reply.comment}}\n                        <br>\n                    </small>\n                </div>\n            </li>\n            <div class=\"no-data\" v-if=\"midd.list.length<=0\">\n                <h1>没有相关评论</h1>\n            </div>\n            <a class=\"btn-more\" v-if=\"midd.btn\" @click=\"getMidd\">查看更多</a>\n        </ul>\n    </div>\n\n    <div id=\"item4\" class=\"mui-control-content fade-in-left\">\n        <ul class=\"comment-item\">\n            <li v-for=\"item in bad.list\" v-if=\"item.comment_level == '3'\">\n                <div class=\"item\">\n                    <img :src=\"item.wechat_headimgurl\" class=\"head\">\n                    {{item.wechat_nickname}} <i class=\"lv lv{{item.wechat_userlevel}}\"></i><br>\n                    <div class=\"starts-container\"><start :value=\"parseInt(item.comment_score_store)\"><start></div>\n                    <h6>{{item.comment_time | format 'yyyy-MM-dd'}}</h6>\n                    <div class=\"tag\">\n                        <a v-for=\"tag in item.comment_store_label\" v-if=\"tag!=''\">{{$store.state.storeLabel[parseInt(tag)-1]}}</a>\n                    </div>\n                    <p>{{item.comment}}</p>\n                    <small v-for=\"reply in item.reply\">\n                        <span>{{reply.comment_userid ? '用户回复' : '店铺回复'}}: </span>\n                        {{reply.comment}}\n                        <br>\n                    </small>\n                </div>\n            </li>\n            <div class=\"no-data\" v-if=\"bad.list.length<=0\">\n                <h1>没有相关评论</h1>\n            </div>\n            <a class=\"btn-more\" v-if=\"bad.btn\" @click=\"getBad\">查看更多</a>\n        </ul>\n    </div>\n    </div>\n    </div>\n\n    <nav class=\"mui-bar mui-bar-tab\">\n        <a class=\"winslow-tab-item\" @click=\"local('manage-shop')\">\n            <span class=\"iconfont\">&#xe67d;</span>\n            <span class=\"mui-tab-label\">管理</span>\n        </a>\n        <a class=\"winslow-tab-item\" @click=\"local('manage-shop-chart')\">\n            <span class=\"iconfont\">&#xe671;</span>\n            <span class=\"mui-tab-label\">报表</span>\n        </a>\n        <a class=\"winslow-tab-item mui-active\">\n            <span class=\"iconfont\">&#xe706;</span>\n            <span class=\"mui-tab-label\">店铺评价</span>\n        </a>\n    </nav>\n    \n    \n</div>\n";

/***/ },
/* 192 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(193)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/manage/shop-chart.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(195)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-eb853cb4/shop-chart.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 193 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	
	var _echarts = __webpack_require__(194);
	
	var _echarts2 = _interopRequireDefault(_echarts);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	exports.default = {
	    name: 'manageShopChart',
	    data: function data() {
	        return {
	            activeChart: '',
	            sohru: 0,
	            chartInfo: {
	                'today': {
	                    title: '今日营业额'
	                },
	                'week': {
	                    title: '一周营业额'
	                },
	                'month': {
	                    title: '一月营业额'
	                }
	            },
	            lineDate: {
	                key: [],
	                value: []
	            },
	            pieDate: {
	                key: [],
	                object: []
	            }
	        };
	    },
	    watch: {
	        'activeChart': function activeChart() {
	            var _this = this;
	
	            this.lineDate = {
	                key: [],
	                value: []
	            };
	            this.pieDate = {
	                key: [],
	                object: []
	            };
	            this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	            this.$http.post(API.getDataTable, { type: this.activeChart }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this.$store.state.hideTip();
	                var tips = '';
	                if (_this.activeChart == 'month') tips = '日';
	                if (_this.activeChart == 'today') tips = '点';
	                for (var i = 0; i < data.data.list.length; i++) {
	                    _this.lineDate.key.push(data.data.list[i].key + tips);
	                    _this.lineDate.value.push(data.data.list[i].value);
	                }
	                setTimeout(function () {
	                    if (_this.lineDate.key.length > 0) _this.showLine();
	                }, 500);
	            });
	            this.$http.post(API.getProjectProportion, { type: this.activeChart }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this.$store.state.hideTip();
	                _this.sohru = data.data.object;
	                for (var i = 0; i < data.data.list.length; i++) {
	                    _this.pieDate.key.push(data.data.list[i].key);
	                    _this.pieDate.object.push({
	                        value: data.data.list[i].value,
	                        name: data.data.list[i].key
	                    });
	                }
	                setTimeout(function () {
	                    if (_this.pieDate.key.length > 0) _this.showPie();
	                }, 500);
	            });
	        }
	    },
	    ready: function ready() {
	        document.title = '报表';
	        mui('.mui-scroll-wrapper').scroll({ bounce: false });
	        this.activeChart = 'today';
	    },
	    methods: {
	        local: function local(name) {
	            this.$router.go('/' + name + '/' + this.$route.params.token);
	        },
	        showLine: function showLine() {
	            var me = this;
	            var myChart = _echarts2.default.init(document.getElementById('lineChart'));
	            var option = {
	                title: {
	                    show: true,
	                    text: '营\n业\n额',
	                    subtext: '单\n位\n:\n元\n',
	                    top: '30px'
	                },
	                legend: {
	                    data: ['价格']
	                },
	                tooltip: {
	                    show: true,
	                    trigger: 'axis',
	                    formatter: "{a} <br/>{b}: {c}元",
	                    axisPointer: {
	                        lineStyle: {
	                            color: '#6d3a3a',
	                            width: 0
	                        }
	                    }
	                },
	                toolbox: {
	                    show: true
	                },
	                // 相对位置
	                grid: {
	                    left: '45px',
	                    right: '10px',
	                    bottom: '10px',
	                    top: '10px',
	                    containLabel: true
	                },
	                xAxis: [{
	                    type: 'category',
	                    boundaryGap: false,
	                    // X轴
	                    axisLine: {
	                        show: true,
	                        lineStyle: {
	                            color: '#000'
	                        }
	                    },
	                    // X轴标签
	                    axisLabel: {
	                        textStyle: {
	                            color: '#666',
	                            fontStyle: 'normal',
	                            fontFamily: 'sans-serif',
	                            fontSize: 12
	                        }
	                    },
	                    // X轴竖线
	                    splitLine: {
	                        show: false,
	                        lineStyle: {
	                            color: '#000'
	                        }
	                    },
	                    data: me.lineDate.key
	                }],
	                yAxis: [{
	                    type: 'value',
	                    axisLine: {
	                        show: true
	                    },
	                    axisTick: {
	                        show: true
	                    },
	                    axisLabel: {
	                        show: true,
	                        textStyle: {
	                            color: '#666'
	                        }
	                    },
	                    splitLine: {
	                        show: false
	                    }
	
	                }],
	                series: [{
	                    name: '营业额',
	                    type: 'line',
	                    stack: '总量',
	                    areaStyle: { normal: {
	                            color: new _echarts2.default.graphic.LinearGradient(252, 235, 218, 1, [{
	                                offset: 0, color: 'rgba(252, 235, 218, 1)'
	                            }, {
	                                offset: 1, color: 'rgba(252, 235, 218, .5)'
	                            }], true)
	                        } },
	                    lineStyle: { normal: { color: 'rgb(255, 115, 2)', width: 1 } },
	                    itemStyle: { normal: { color: 'rgb(255, 115, 2)', opacity: 0.8 } },
	                    data: me.lineDate.value
	                }]
	            };
	            myChart.setOption(option);
	        },
	        showPie: function showPie() {
	            var me = this;
	            var myPieChart = _echarts2.default.init(document.getElementById('pieChart'));
	            var pieoption = {
	                title: {
	                    show: false
	                },
	                tooltip: {
	                    trigger: 'item',
	                    position: 'right',
	                    textStyle: {
	                        fontSize: 12
	                    },
	                    formatter: "{b} : <br/>{c}元({d}%)"
	                },
	                legend: {
	                    orient: 'vertical',
	                    right: '0%',
	                    top: '10%',
	                    itemWidth: 14,
	                    formatter: function formatter(name) {
	                        return _echarts2.default.format.truncateText(name, 100, '…');
	                    },
	                    data: me.pieDate.key
	                },
	                series: [{
	                    name: '项目营收比例',
	                    type: 'pie',
	                    radius: '70%',
	                    center: ['30%', '50%'],
	                    data: me.pieDate.object,
	                    labelLine: {
	                        normal: {
	                            show: false
	                        }
	                    },
	                    label: {
	                        normal: {
	                            show: true,
	                            position: 'inside',
	                            formatter: '{d}%'
	                        }
	                    }
	                }]
	            };
	            myPieChart.setOption(pieoption);
	        }
	    }
	};
	
	// </script>
	/* generated by vue-loader */
	// <template>
	// <div class="page manage">
	//     <header id="header" class="mui-bar mui-bar-nav">
	//         <h1 class="mui-title">报表</h1>
	//         <!-- <a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a> -->
	//     </header>
	//
	//     <div class="mui-scroll-wrapper">
	//     <div class="mui-scroll">
	//     <div id="sliderSegmentedControl" class="mui-slider-indicator mui-segmented-control mui-segmented-control-inverted">
	//         <a class="winslow-control-item {{activeChart == 'today' ? 'mui-active' : ''}}" @click="activeChart = 'today'">日报</a>
	//         <a class="winslow-control-item {{activeChart == 'week' ? 'mui-active' : ''}}" @click="activeChart = 'week'">周报</a>
	//         <a class="winslow-control-item {{activeChart == 'month' ? 'mui-active' : ''}}" @click="activeChart = 'month'">月报</a>
	//     </div>
	//     <div class="title">{{chartInfo[activeChart].title}}：{{sohru}}元</div>
	//     <div class="winslow-container">
	//         <div id="lineChart" style="width: 100%;height: 200px;" v-show="lineDate.key.length > 0"></div>
	//         <div class="no-data" v-if="lineDate.key.length<=0">
	//             <h6>还没有数据~</h6>
	//         </div>
	//     </div>
	//     <div class="title">项目营收比例</div>
	//     <div class="winslow-container">
	//         <div id="pieChart" style="width: 100%;height: 200px;" v-show="pieDate.key.length > 0"></div>
	//         <div class="no-data" v-if="pieDate.key.length<=0">
	//             <h6>还没有数据~</h6>
	//         </div>
	//     </div>
	//     </div></div>
	//
	//     <nav class="mui-bar mui-bar-tab">
	//         <a class="winslow-tab-item" @click="local('manage-shop')">
	//             <span class="iconfont">&#xe67d;</span>
	//             <span class="mui-tab-label">管理</span>
	//         </a>
	//         <a class="winslow-tab-item mui-active">
	//             <span class="iconfont">&#xe671;</span>
	//             <span class="mui-tab-label">报表</span>
	//         </a>
	//         <a class="winslow-tab-item" @click="local('manage-shop-comment')">
	//             <span class="iconfont">&#xe706;</span>
	//             <span class="mui-tab-label">店铺评价</span>
	//         </a>
	//     </nav>
	//
	// </div>
	// </template>
	//
	// <script>

/***/ },
/* 194 */
/***/ function(module, exports, __webpack_require__) {

	var __WEBPACK_AMD_DEFINE_FACTORY__, __WEBPACK_AMD_DEFINE_ARRAY__, __WEBPACK_AMD_DEFINE_RESULT__;/* WEBPACK VAR INJECTION */(function(global) {"use strict";var _typeof2=__webpack_require__(92);var _typeof3=_interopRequireDefault(_typeof2);function _interopRequireDefault(obj){return obj&&obj.__esModule?obj:{default:obj};}!function(t,e){ true?!(__WEBPACK_AMD_DEFINE_ARRAY__ = [], __WEBPACK_AMD_DEFINE_FACTORY__ = (e

.exports = __WEBPACK_AMD_DEFINE_RESULT__)):"object"==(typeof module==="undefined"?"undefined":(0,_typeof3.default)(module))&&module.exports?module.exports=e():t.echarts=e();}(undefined,function(){var t,e;!function(){function i(t,e){if(!e)return t;if(0===t.indexOf(".")){var i=e.split("/"),n=t.split("/"),r=i.length-1,a=n.length,o=0,s=0;t:for(var l=0;a>l;l++){switch(n[l]){case"..":if(!(r>o))break t;o++,s++;break;case".":s++;break;default:break t;}}return i.length=r-o,n=n.slice(s),i.concat(n).join("/");}return t;}function n(t){function e(e,o){if("string"==typeof e){var s=n[e];return s||(s=a(i(e,t)),n[e]=s),s;}e instanceof Array&&(o=o||function(){},o.apply(this,r(e,o,t)));}var n={};return e;}function r(e,n,r){for(var s=[],l=o[r],u=0,c=Math.min(e.length,n.length);

}return e.exports;}var o={};e=function e(t,_e2,i){o[t]={id:t,deps:_e2,factory:i,defined:0,exports:{},require:n(t)};},t=n("");}();var i="undefined",n="moveTo",r="ecModel",a="ordinal",o="getItemModel",s="getName",l="dimensions",u="retrieve",c="option",h="concat",f="getContext",d="rotation",p="inside",v="getShallow",m="stroke",g="applyTransform",y="getExtent",_="inherits",x="number",b="function",w="isArray",T="replace",M="zlevel",S="getDataParams",C="mouseout",A="splice",P="series",L="colorStops",I="update",k="getItemVisual",z="dataIndex",D="getData",O="indexOf",E="ignore",R="isObject",B="animation",N="canvas",F="string",G="toLowerCase",q="zrender/core/env",V="create",H="normalize",U="contain",Z="length",W="_extent",X="extend",j="prototype",Y="opacity",$="setStyle",Q="position",K="bottom",J="center",te="middle",ee="getHeight",ie="getWidth",ne="silent",re="height",ae="getBoundingRect",oe="getTextColor",se="getFont",le="textAlign",ue="textStyle",ce="getModel",he="coordinateSystem",fe="removeAll",de="zrender/core/util",pe="require";e("echarts/component/grid",[pe,"../util/graphic",de,"../echarts","../coord/cartesian/Grid","./axis"],function(t){

../visual/dataColor"),"pie")),i.registerLayout(e.curry(t("./pie/pieLayout"),"pie")),i.registerProcessor(e.curry(t("../processor/dataFilter"),"pie"));}

),e("echarts/component/legend",[pe,"./legend/LegendModel","./legend/legendAction","./legend/LegendView","../echarts","./legend/legendFilter"],function(t){t("./legend/LegendModel"),t("./legend/legendAction"),t("./legend/LegendView");var e=t("../echarts");e.registerProcessor(t("./legend/legendFilter"));}),e("echarts/component/tooltip",[pe,"./tooltip/TooltipModel","./tooltip/TooltipView","../echarts"],function(t){t("./tooltip/TooltipModel"),t("./tooltip/TooltipView"),t("../echarts").registerAction({type:"showTip",event:"showTip",update:"tooltip:manuallyShowTip"},function(){}),t("../echarts").registerAction({type:"hideTip",event:"hideTip",update:"tooltip:manuallyHideTip"},function(){});}),e("echarts/scale/Time",[pe,de,"../util/number","../util/format","./Interval"],function(t){var e=t(de),i=t("../util/number"),n=t("../util/format"),r=t("./Interval"),a=r[j],o=Math.ceil,s=Math.floor,l=1e3,u=60*l,c=60*u,h=24*c,f=function f(t,e,i,n){for(;n>i;){var r=i+n>>>1;t[r][2]<e?i=r+1:n=r;}return i;},d=r[X]({type:"time",getLabel:function getLabel(t){var e=this._stepLvl,i=new Date(t);return n.formatTime(e[0],i);},niceExtent:function niceExtent(t,e,n){var r=this[W];if(r[0]===r[1]&&(r[0]-=h,r[1]+=h),r[1]===-1/0&&1/0===r[0]){var a=new Date();r[1]=new Date(a.getFullYear(),a.getMonth(),a.getDate()),r[0]=r[1]-h;}this.niceTicks(t);var l=this._interval;e||(r[0]=i.round(s(r[0]/l)*l)),n||(r[1]=i.round(o(r[1]/l)*l));},niceTicks:function niceTicks(t){t=t||10;var e=this[W],n=e[1]-e[0],r=n/t,a=p[Z],l=f(p,r,0,a),u=p[Math.min(l,a-1)],c=u[2];if("year"===u[0]){var h=n/c,d=i.nice(h/t,!0);c*=d;}var v=[o(e[0]/c)*c,s(e[1]/c)*c];this._stepLvl=u,this._interval=c,this._niceExtent=v;},parse:function parse(t){return+i.parseDate(t);}});e.each([U,H],function(t){d[j][t]=function(e){return a[t].call(this,this.parse(e));};});var p=[["hh:mm:ss",1,l],["hh:mm:ss",5,5*l],["hh:mm:ss",10,10*l],["hh:mm:ss",15,15*l],["hh:mm:ss",30,30*l],["hh:mm\nMM-dd",1,u],["hh:mm\nMM-dd",5,5*u],["hh:mm\nMM-dd",10,10*u],["hh:mm\nMM-dd",15,15*u],["hh:mm\nMM-dd",30,30*u],["hh:mm\nMM-dd",1,c],["hh:mm\nMM-dd",2,2*c],["hh:mm\nMM-dd",6,6*c],["hh:mm\nMM-dd",12,12*c],["MM-dd\nyyyy",1,h],["week",7,7*h],["month",1,31*h],["quarter",3,380*h/4],["half-year",6,380*h/2],["year",1,380*h]];

};}function i(){me.call(this);}function n(t,e,n){function r(t,e){return t.prio-e.prio;}n=n||{},(typeof e==="undefined"?"undefined":(0,_typeof3.default)(e))===F&&(e=Fe[e]),this.id,this.group,this._dom=t;

o<a[Z];o++){var s=a[o];if(s[t]&&null!=(n=s[t](r,e,i)))return n;}}function a(t,e,i,n,r){var a=t._model,o={};o[n+"Id"]=i[n+"Id"],o[n+"Index"]=i[n+"Index"],o[n+"Name"]=i[n+"Name"];var s={mainType:n,query:o};r&&(s.subType=r),a&&a.eachComponent(s,function(r){var o=t[n===P?"_chartsMap":"_componentsMap"][r.__viewId];o&&o.__alive&&o[e](r,a,t._api,i);},t);}function o(t,e){var i=t.type,n=Oe[i],r=n.actionInfo,o=(r[I]||I).split(":"),s=o.pop();o=o[0]&&_e(o[0]),this[Ae]=!0;var l=[t],u=!1;t.batch&&(u=!0,l=pe.map(t.batch,function(e){return e=pe.defaults(pe[X]({},e),t),e.batch=null,e;}));for(var c,h=[],f="highlight"===i||"downplay"===i,d=0;d<l[Z];d++){var p=l[d];c=n.action(p,this._model),c=c||pe[X]({},p),c.type=r.event||c.type,h.push(c),f?a(this,s,p,P):o&&a(this,s,p,o.main,o.sub);}"none"===s||f||o||(this[Le]?(ze.prepareAndUpdate.call(this,t),this[Le]=!1):ze[s].call(this,t)),c=u?{

}}function l(t){!t&&this.trigger("updated");}function u(t,e,i){var n=this._api;ye(this._componentsViews,function(r){var a=r.__model;r[t](a,e,n,i),y(a,r);},this),e.eachSeries(function(r){var a=this._chartsMap[r.__viewId];a[t](r,e,n,i),y(r,a),g(r,a);},this),m(this._zr,e);}function c(t,e){for(var i="component"===t,n=i?this._componentsViews:this._chartsViews,r=i?this._componentsMap:this._chartsMap,a=this._zr,o=0;o<n[Z];o++){n[o].__alive=!1;}e[i?"eachComponent":"eachSeries"](function(t,o){if(i){if(t===P)return;}else o=t;var s=o.id+"_"+o.type,l=r[s];if(!l){var u=_e(o.type),c=i?ae.getClass(u.main,u.sub):oe.getClass(u.sub);if(!c)return;l=new c(),l.init(e,this._api),r[s]=l,n.push(l),a.add(l.group);}o.__viewId=s,l.__alive=!0,l.__id=s,l.__model=o;},this);for(var o=0;o<n[Z];){var s=n[o];s.__alive?o++:(a.remove(s.group),s.dispose(e,this._api),n[A](o,1),delete r[s.__id]);}}function h(t,e){ye(Re,function(i){i.func(t,e);});}function f(t){var e={};t.eachSeries(function(t){var i=t.get("stack"),n=t[D]();if(i&&"list"===n.type){var r=e[i];r&&(n.stackedOn=r),e[i]=n;}});}function d(t,e){var i=this._api;ye(Ne,function(n){n.isLayout&&n.func(t,i,e);});}function p(t,e,i){var n=this._api;t.clearColorPalette(),t.eachSeries(function(t){t.clearColorPalette();}),ye(Ne,function(r){(!i||!r.isLayout)&&r.func(t,n,e);});}function v(t,e){var i=this._api;ye(this._componentsViews,function(n){var r=n.__model;n.render(r,t,i,e),y(r,n);},this),ye(this._chartsViews,function(t){t.__alive=!1;},this),t.eachSeries(function(n){var r=this._chartsMap[n.__viewId];r.__alive=!0,r.render(n,t,i,e),r.group[ne]=!!n.get(ne),y(n,r),g(n,r);},this),m(this._zr,t),ye(this._chartsViews,function(e){e.__alive||e.remove(t,i);},this);}function m(t,e){

});var a=t.get("blendMode")||null;e.group.traverse(function(t){t.isGroup||t[$]("blend",a);});}function y(t,e){var i=t.get("z"),n=t.get(M);e.group.traverse(function(t){"group"!==t.type&&(null!=i&&(t.z=i),null!=n&&(t[M]=n));});}function T(t){function e(t,e){for(var i=0;i<t[Z];i++){var n=t[i];n[a]=e;}}var i=0,n=1,r=2,a="__connectUpdateStatus";

},ke.setTheme=function(){console.log("ECharts#setTheme() is DEPRECATED in ECharts 3.0");},ke[ce]=function(){return this._model;},ke.getOption=function(){return this._model&&this._model.getOption();},ke[ie]=function(){return this._zr[ie]();},ke[ee]=function(){return this._zr[ee]();}

});p.add(e);}),p.refreshImmediately(),d.toDataURL("image/"+(t&&t.type||"png"));}return this.getDataURL(t);}},ke.convertToPixel=pe.curry(r,"convertToPixel"),ke.convertFromPixel=pe.curry(r,"convertFromPixel"),ke.containPixel=function(t,e){

}),p.call(this,e,t),u.call(this,"updateView",e,t));},updateVisual:function updateVisual(t){var e=this._model;e&&(e.eachSeries(function(t){t[D]().clearAllVisual();}),p.call(this,e,t,!0),u.call(this,"updateVisual",e,t));},updateLayout:function updateLayout(t){var e=this._model;e&&(d.call(this,e,t),u.call(this,"updateLayout",e,t));},prepareAndUpdate:function prepareAndUpdate(t){var e=this._model;c.call(this,"component",e),c.call(this,"chart",e),this.__lastOnlyGraphic?(ye(this._componentsViews,function(i){var n=i.__model;n&&"graphic"===n.mainType&&(i.render(n,e,this._api,t),y(n,i));},this),this.__lastOnlyGraphic=!1):ze[I].call(this,t);}}

,ke.showLoading=function(t,e){if(pe[R](t)&&(e=t,t=""),t=t||"default",this.hideLoading(),Ge[t]){var i=Ge[t](this._api,e),n=this._zr;this._loadingFX=i,n.add(i);}},ke.hideLoading=function(){this._loadingFX&&this._zr.remove(this._loadingFX),this._loadingFX=null;},ke.makeActionFromEvent=function(t){var e=pe[X]({},t);return e.type=Ee[t.type],e;},ke.dispatchAction=function(t,e){if(pe[R](e)||(e={

}},ke.on=e("on"),ke.off=e("off"),ke.one=e("one");var De=["click","dblclick","mouseover",C,"mousemove","mousedown","mouseup","globalout","contextmenu"];ke._initEvents=function(){ye(De,function(t){this._zr.on(t,function(e){var i,n=this[ce](),r=e.target;if("globalout"===t)i={};else if(r&&null!=r[z]){var a=r.dataModel||n.getSeriesByIndex(r.seriesIndex);i=a&&a[S](r[z],r.dataType)||{};}else r&&r.eventData&&(i=pe[X]({},r.eventData));i&&(i.event=e,i.type=t,this.trigger(t,i));},this);},this),ye(Ee,function(t,e){this._messageCenter.on(e,function(t){this.trigger(e,t);},this);},this);},ke.isDisposed=function(){return this._disposed;},ke.clear=function(){this.setOption({series:[]},!0);},ke.dispose=function(){if(!this._disposed){this._disposed=!0;var t=this._api,e=this._model;ye(this._componentsViews,function(i){i.dispose(e,t);}),ye(this._chartsViews,function(i){i.dispose(e,t);}),this._zr.dispose(),delete qe[this.id];}},pe.mixin(n,me);var Oe=[],Ee={},Re=[],Be=[],Ne=[],Fe={},Ge={},qe={},Ve={},He=new Date()-0,Ue=new Date()-0,Ze="_echarts_instance_",We={version:"3.4.0",dependencies:{zrender:"3.3.0"}};return We.init=function(t,e,i){var r=new n(t,e,i);return r.id="ec_"+He++,qe[r.id]=r,t.setAttribute&&t.setAttribute(Ze,r.id),T(r),r;},We.connect=function(t){if(pe[w](t)){var e=t;t=null,pe.each(e,function(e){null!=e.group&&(t=e.group);}),t=t||"g_"+Ue++,pe.each(e,function(e){e.group=t;});}return Ve[t]=!0,t;},We.disConnect=function(t){Ve[t]=!1;},We.dispose=function(t){pe.isDom(t)?t=We.getInstanceByDom(t):(typeof t==="undefined"?"undefined":(0,_typeof3.default)(t))===F&&(t=qe[t]),t instanceof n&&!t.isDisposed()&&t.dispose();},We.getInstanceByDom=function(t){var e=t.getAttribute(Ze);return qe[e];},We.getInstanceById=function(t){return qe[t];},We.registerTheme=function(t,e){Fe[t]=e;},We.registerPreprocessor=function(t){Be.push(t);},We.registerProcessor=function(t,e){(typeof t==="undefined"?"undefined":(0,_typeof3.default)(t))===b&&(e=t,t=xe),Re.push({prio:t,func:e});},We.registerAction=function(t,e,i){(typeof e==="undefined"?"undefined":(0,_typeof3.default)(e))===b&&(i=e,e="");var n=pe[R](t)?t.type:[t,t={event:e}][0];t.event=(t.event||n)[G](),e=t.event,pe.assert(Ie.test(n)&&Ie.test(e)),Oe[n]||(Oe[n]={action:i,actionInfo:t}),Ee[e]=n;},We.registerCoordinateSystem=function(t,e){Y.register(t,e);},We.registerLayout=function(t,e){(typeof t==="undefined"?"undefined":(0,_typeof3.default)(t))===b&&(e=t,t=we),Ne.push({prio:t,func:e,isLayout:!0});},We.registerVisual=function(t,e){(typeof t==="undefined"?"undefined":(0,_typeof3.default)(t))===b&&(e=t,t=Me),Ne.push({prio:t,func:e});},We.registerLoading=function(t,e){Ge[t]=e;},We.extendComponentModel=function(t){return J[X](t);},We.extendComponentView=function(t){return ae[X](t);},We.extendSeriesModel=function(t){return te[X](t);},We.extendChartView=function(t){return oe[X](t);},We.setCanvasCreator=function(t){pe.createCanvas=t;}

},ye(["map","each","filter",O,_,"reduce","filter","bind","curry",w,"isString",R,"isFunction",X,"defaults","clone"],function(t){We.util[t]=pe[t];}),We.PRIORITY={

n.apply(this,arguments),this._originalScale=new a();},getTicks:function getTicks(){var t=this._originalScale,n=this[W],a=t[y]();return i.map(s.getTicks.call(this),function(i){var o=r.round(f(this.base,i));return o=i===n[0]&&t.__fixMin?e(o,a[0]):o,o=i===n[1]&&t.__fixMax?e(o,a[1]):o;},this);},getLabel:s.getLabel,scale:function scale(t){return t=o.scale.call(this,t),f(this.base,t);},setExtent:function setExtent(t,e){var i=this.base;t=d(t)/d(i),e=d(e)/d(i),s.setExtent.call(this,t,e);},getExtent:function getExtent(){var t=this.base,i=o[y].call(this);i[0]=f(t,i[0]),i[1]=f(t,i[1]);var n=this._originalScale,r=n[y]();return n.__fixMin&&(i[0]=e(i[0],r[0])),n.__fixMax&&(i[1]=e(i[1],r[1])),i;},unionExtent:function unionExtent(t){this._originalScale.unionExtent(t);var e=this.base;t[0]=d(t[0])/d(e),t[1]=d(t[1])/d(e),o.unionExtent.call(this,t);},unionExtentFromData:function unionExtentFromData(t,e){this.unionExtent(t.getDataExtent(e,!0,function(t){return t>0;}));},niceTicks:function niceTicks(t){t=t||10;var e=this[W],i=e[1]-e[0];if(!(1/0===i||0>=i)){var n=r.quantity(i),a=t/i*n;for(.5>=a&&(n*=10);

}});return i.each([U,H],function(t){p[j][t]=function(e){return e=d(e)/d(this.base),o[t].call(this,e);};}),p[V]=function(){return new p();},p;}),e("echarts/util/graphic",[pe,de,"zrender/tool/path","zrender/graphic/Path","zrender/tool/color","zrender/core/matrix","zrender/core/vector","zrender

zrender/graphic/CompoundPath","zrender/graphic/LinearGradient","zrender/graphic/RadialGradient","zrender/core/BoundingRect"],function(t){function e(t){return null!=t&&"none"!=t;}function i(t){return(typeof t==="undefined"?"undefined":(0,_typeof3.default)(t))===F?M.lift(t,-.1):t;}function n(t){if(t.__hoverStlDirty){var n=t.style[m],r=t.style.fill,a=t.__hoverStl;a.fill=a.fill||(e(r)?i(r):null),a[m]=a[m]||(e(n)?i(n):null);var o={};for(var s in a){a.hasOwnProperty(s)&&(o[s]=t.style[s]);}t.__normalStl=o,t.__hoverStlDirty=!1;}}function r(t){t.__isHover||(n(t),t.useHoverLayer?t.__zr&&t.__zr.addHover(t,t.__hoverStl):(t[$](t.__hoverStl),t.z2+=1),t.__isHover=!0);}function a(t){if(t.__isHover){

}function u(t){this.__hoverSilentOnTouch&&t.zrByTouch||!this.__isEmphasis&&o(this);}function c(t){this.__hoverSilentOnTouch&&t.zrByTouch||!this.__isEmphasis&&s(this);}function h(){this.__isEmphasis=!0,o(this);}function f(){this.__isEmphasis=!1,s(this);}function y(t,e,i,n,r,a){(typeof r==="undefined"?"undefined":(0,_typeof3.default)(r))===b&&(a=r,r=null);var o=n&&n.isAnimationEnabled();if(o){var s=t?"Update":"",l=n[v]("animationDuration"+s),u=n[v]("animationEasing"+s),c=n[v]("animationDelay"+s);(typeof c==="undefined"?"undefined":(0,_typeof3.default)(c))===b&&(c=c(r,n.getAnimationDelayParams?n.getAnimationDelayParams(e,r):null)),(typeof l==="undefined"?"undefined":(0,_typeof3.default)(l))===b&&(l=l(r)),l>0?e.animateTo(i,l,c||0,u,a):(e.attr(i),a&&a());}else e.attr(i),a&&a();}var _=t(de),x=t("zrender/tool/path"),w=Math.round,T=t("zrender/graphic/Path"),M=t("zrender/tool/color"),S=t("zrender/core/matrix"),A=t("zrender/core/vector"),P={};return P.Group=t("zrender/container/Group"),P.Image=t("zrender/graphic/Image"),P.Text=t("zrender/graphic/Text"),P.Circle=t("zrender/graphic/shape/Circle"),P.Sector=t("zrender/graphic/shape/Sector"),P.Ring=t("zrender/graphic/shape/Ring"),P.Polygon=t("zrender/graphic/shape/

),P.CompoundPath=t("zrender/graphic/CompoundPath"),P.LinearGradient=t("zrender/graphic/LinearGradient"),P.RadialGradient=t("zrender/graphic/RadialGradient"),P.BoundingRect=t("zrender/core/BoundingRect"),P.extendShape=function(t){return T[X](t);},P.extendPath=function(t,e){return x.extendFromString(t,e);},P.makePath=function(t,e,i,n){var r=x.createFromString(t,e),a=r[ae]();if(i){var o=a.width/a[re];if(n===J){var s,l=i[re]*o;l<=i.width?s=i[re]:(l=i.width,s=l/o);var u=i.x+i.width/2,c=i.y+i[re]/2;i.x=u-l/2,i.y=c-s/2,i.width=l,i[re]=s;}P.resizePath(r,i);}return r;},P.mergePath=x.mergePath,P.resizePath=function(t,e){if(t[g]){var i=t[ae](),n=i.calculateTransform(e);t[g](n);}},P.subPixelOptimizeLine=function(t){var e=P.subPixelOptimize,i=t.shape,n=t.style.lineWidth;return w(2*i.x1)===w(2*i.x2)&&(i.x1=i.x2=e(i.x1,n,!0)),w(2*i.y1)===w(2*i.y2)&&(i.y1=i.y2=e(i.y1,n,!0)),t;},P.subPixelOptimizeRect=function(t){var e=P.subPixelOptimize,i=t.shape,n=t.style.lineWidth,r=i.x,a=i.y,o=i.width,s=i[re];return i.x=e(i.x,n,!0),i.y=e(i.y,n,!0),i.width=Math.max(e(r+o,n,!1)-i.x,0===o?0:1),i[re]=Math.max(e(a+s,n,!1)-i.y,0===s?0:1),t;},P.subPixelOptimize=function(t,e,i){var n=w(2*t);return(n+w(e))%2===0?n/2:(n+(i?1:-1))/2;},P.setHoverStyle=function(t,e,i){t.__hoverSilentOnTouch=i&&i.hoverSilentOnTouch,"group"===t.type?t.traverse(function(t){"group"!==t.type&&l(t,e);}):l(t,e),t.on("mouseover",u).on(C,c),t.on("emphasis",h).on("normal",f);},P.setText=function(t,e,i){var n=e[v](Q)||p,r=e[v]("offset"),a=n[O](p)>=0?"white":i,o=e[ce](ue);_[X](t,{textDistance:e[v]("distance")||5,textFont:o[se](),textPosition:n,textOffset:r,textFill:o[oe]()||a});},P.updateProps=function(t,e,i,n,r){y(!0,t,e,i,n,r);},P.initProps=function(t,e,i,n,r){y(!1,t,e,i,n,r);},P.getTransform=function(t,e){for(var i=S.identity([]);t&&t!==e;){S.mul(i,t.getLocalTransform(),i),t=t.parent;}return i;}

}}});}},P;}),e(de,[pe],function(){function t(e){if(null==e||"object"!=(typeof e==="undefined"?"undefined":(0,_typeof3.default)(e)))return e;var i=e,n=R.call(e);if("[object Array]"===n){i=[];for(var r=0,a=e[Z];a>r;r++){i[r]=t(e[r]);}}else if(E[n])i=e.constructor.from(e);else if(!D[n]&&!A(e)){i={};for(var o in e){e.hasOwnProperty(o)&&(i[o]=t(e[o]));}}return i;}function e(i,n,r){if(!S(n)||!S(i))return r?t(n):i;for(var a in n){if(n.hasOwnProperty(a)){var o=i[a],s=n[a];!S(s)||!S(o)||w(s)||w(o)||A(s)||A(o)||C(s)||C(o)?!r&&a in i||(i[a]=t(n[a],!0)):e(o,s,r);}}return i;}function i(t,i){for(var n=t[0],r=1,a=t[Z];a>r;r++){n=e(n,t[r],i);}return n;}function n(t,e){for(var i in e){e.hasOwnProperty(i)&&(t[i]=e[i]);}return t;}function r(t,e,i){for(var n in e){e.hasOwnProperty(n)&&(i?null!=e[n]:null==t[n])&&(t[n]=e[n]);}return t;}function a(){return document.createElement(N);}function o(){return z||(z=W.createCanvas()[f]("2d")),z;}function s(t,e){if(t){if(t[O])return t[O](e);for(var i=0,n=t[Z];n>i;i++){if(t[i]===e)return i;}}return-1;}function l(t,e){function i(){}var n=t[j];i[j]=e[j],t[j]=new i();for(var r in n){t[j][r]=n[r];}t[j].constructor=t,t.superClass=e;}function u(t,e,i){t=j in t?t[j]:t,e=j in e?e[j]:e,r(t,e,i);}function c(t){return t?(typeof t==="undefined"?"undefined":(0,_typeof3.default)(t))==F?!1:(0,_typeof3.default)(t[Z])==x:void 0;}function d(t,e,i){if(t&&e)if(t.forEach&&t.forEach===G)t.forEach(e,i);else if(t[Z]===+t[Z])for(var n=0,r=t[Z];r>n;n++){e.call(i,t[n],n,t);}else for(var a in t){t.hasOwnProperty(a)&&e.call(i,t[a],a,t);}}function p(t,e,i){if(t&&e){if(t.map&&t.map===H)return t.map(e,i);for(var n=[],r=0,a=t[Z];a>r;r++){n.push(e.call(i,t[r],r,t));}return n;}}function v(t,e,i,n){if(t&&e){if(t.reduce&&t.reduce===U)return t.reduce(e,i,n);for(var r=0,a=t[Z];a>r;r++){i=e.call(n,i,t[r],r,t);}return i;}}function m(t,e,i){if(t&&e){if(t.filter&&t.filter===q)return t.filter(e,i);for(var n=[],r=0,a=t[Z];a>r;r++){e.call(i,t[r],r,t)&&n.push(t[r]);}return n;}}function g(t,e,i){if(t&&e)for(var n=0,r=t[Z];r>n;n++){if(e.call(i,t[n],n,t))return t[n];}}function y(t,e){var i=V.call(arguments,2);return function(){return t.apply(e,i[h](V.call(arguments)));};}function _(t){var e=V.call(arguments,1);return function(){return t.apply(this,e[h](V.call(arguments)));};}function w(t){return"[object Array]"===R.call(t);}function T(t){return(typeof t==="undefined"?"undefined":(0,_typeof3.default)(t))===b;}function M(t){return"[object String]"===R.call(t);}function S(t){var e=typeof t==="undefined"?"undefined":(0,_typeof3.default)(t);return e===b||!!t&&"object"==e;}function C(t){return!!D[R.call(t)];}function A(t){return"object"==(typeof t==="undefined"?"undefined":(0,_typeof3.default)(t))&&(0,_typeof3.default)(t.nodeType)===x&&"object"==(0,_typeof3.default)(t.ownerDocument);}function P(t){return t!==t;}function L(){for(var t=0,e=arguments[Z];e>t;t++){if(null!=arguments[t])return arguments[t];}}function I(){return Function.call.apply(V,arguments);}function k(t,e){if(!t)throw new Error(e);}var z,D={

}};return W;}),e("echarts/chart/pie/PieSeries",[pe,"../../data/List",de,"../../util/model","../../data/helper/completeDimensions","../../component/helper/selectableMixin","../../echarts"],function(t){var e=t("../../data/List"),i=t(de),n=t("../../util/model"),r=t("../../data/helper/completeDimensions"),a=t("../../component/helper/selectableMixin"),o=t("../../echarts").extendSeriesModel({type:"series.pie",init:function init(t){o.superApply(this,"init",arguments),this.legendDataProvider=function(){return this.getRawData();},this.updateSelectedMap(t.data),this._defaultLabelLine(t);},mergeOption:function mergeOption(t){o.superCall(this,"mergeOption",t),this.updateSelectedMap(this[c].data);},getInitialData:function getInitialData(t){var i=r(["value"],t.data),n=new e(i,this);return n.initData(t.data),n;},getDataParams:function getDataParams(t){var e=this[D](),i=o.superCall(this,S,t),n=e.getSum("value");return i.percent=n?+(e.get("value",t)/n*100).toFixed(2):0,i.$vars.push("percent"),i;},_defaultLabelLine:function _defaultLabelLine(t){n.defaultEmphasis(t.labelLine,["show"]);var e=t.labelLine.normal,i=t.labelLine.emphasis;e.show=e.show&&t.label.normal.show,i.show=i.show&&t.label.emphasis.show;},defaultOption:{

}function i(t){var e,i=t.model,n=i.getFormattedLabels(),r=i[ce]("axisLabel.textStyle"),a=1,o=n[Z];o>40&&(a=Math.ceil(o/40));for(var s=0;o>s;s+=a){if(!t.isLabelIgnored(s)){var l=r.getTextRect(n[s]);e?e.union(l):e=l;}}return e;}function n(t,e,i){this._coordsMap={},this._coordsList=[],this._axesMap={},this._axesList=[],this._initCartesian(t,e,i),this._model=t;}function r(t,e){var i=t[y](),n=i[0]+i[1];t.toGlobalCoord="x"===t.dim?function(t){return t+e;}:function(t){return n-t+e;},t.toLocalCoord="x"===t.dim?function(t){return t-e;}:function(t){return n-t+e;};}function a(t){return c.map(g,function(e){var i=t.getReferringComponents(e)[0];return i;});}function o(t){return"cartesian2d"===t.get(he);}var s=t("../../util/layout"),u=t("../../coord/axisHelper"),c=t(de),h=t("./Cartesian2D"),f=t("./Axis2D"),d=c.each,p=u.ifAxisCrossZero,v=u.niceScaleExtent;t("./GridModel");var m=n[j];m.type="grid",m.getRect=function(){return this._rect;},m[I]=function(t,e){function i(t){var e=n[t];for(var i in e){if(e.hasOwnProperty(i)){var r=e[i];if(r&&("category"===r.type||!p(r)))return!0;}}return!1;}var n=this._axesMap;this._updateScale(t,this._model),d(n.x,function(t){v(t,t.model);}),d(n.y,function(t){v(t,t.model);}),d(n.x,function(t){i("y")&&(t.onZero=!1);}),d(n.y,function(t){i("x")&&(t.onZero=!1);}),this.resize(this._model,e);},m.resize=function(t,e){function n(){d(o,function(t){var e=t.isHorizontal(),i=e?[0,a.width]:[0,a[re]],n=t.inverse?1:0;t.setExtent(i[n],i[1-n]),r(t,e?a.x:a.y);});}var a=s.getLayoutRect(t.getBoxLayoutParams(),{

}}}),n());},m.getAxis=function(t,e){var i=this._axesMap[t];if(null!=i){if(null==e)for(var n in i){if(i.hasOwnProperty(n))return i[n];}return i[e];}},m.getCartesian=function(t,e){if(null!=t&&null!=e){var i="x"+t+"y"+e;return this._coordsMap[i];}for(var n=0,r=this._coordsList;n<r[Z];n++){if(r[n].getAxis("x").index===t||r[n].getAxis("y").index===e)return r[n];

,this);},this)):(this._axesMap={},void(this._axesList=[]));},m._updateScale=function(t,i){function n(t,e,i){d(i.coordDimToDataDim(e.dim),function(i){e.scale.unionExtentFromData(t,i);});}c.each(this._axesList,function(t){t.scale.setExtent(1/0,-1/0);}),t.eachSeries(function(r){if(o(r)){var s=a(r,t),l=s[0],u=s[1];if(!e(l,i,t)||!e(u,i,t))return;

}),t.eachSeries(function(e){if(o(e)){var i=a(e,t),n=i[0],r=i[1],s=n.getCoordSysModel(),l=s[he];e[he]=l.getCartesian(n.componentIndex,r.componentIndex);}}),i;}

}switch(t.top||t[K]){case te:case J:u=s/2-d/2-i[0];break;case K:u=s-d-p;}l=l||0,u=u||0,isNaN(f)&&(f=r-l-(c||0)),isNaN(d)&&(d=s-u-(h||0));var g=new n(l+i[3],u+i[0],f,d);return g.margin=i,g;},l.positionElement=function(t,e,r,a,o){var s=!o||!o.hv||o.hv[0],u=!o||!o.hv||o.hv[1],c=o&&o.boundingMode||"all";if(s||u){var h;if("raw"===c)h="group"===t.type?new n(0,0,+e.width||0,+e[re]||0):t[ae]();else if(h=t[ae](),t.needLocalTransform()){var f=t.getLocalTransform();h=h.clone(),h[g](f);}e=l.getLayoutRect(i.defaults({width:h.width,height:h[re]},e),r,a);var d=t[Q],p=s?e.x-h.x:0,v=u?e.y-h.y:0;t.attr(Q,"raw"===c?[p,v]:[d[0]+p,d[1]+v]);}},l.mergeLayoutParam=function(t,e,n){function r(i){var r={},l=0,u={},c=0,h=n.ignoreSize?1:2;if(s(i,function(e){u[e]=t[e];}),s(i,function(t){a(e,t)&&(r[t]=u[t]=e[t]),o(r,t)&&l++,o(u,t)&&c++;}),c!==h&&l){if(l>=h)return r;for(var f=0;f<i[Z];f++){var d=i[f];if(!a(r,d)&&a(t,d)){r[d]=t[d];break;}}return r;}return u;}function a(t,e){return t.hasOwnProperty(e);}function o(t,e){return null!=t[e]&&"auto"!==t[e];}function l(t,e,i){s(t,function(t){e[t]=i[t];});}!i[R](n)&&(n={});var u=["width","left","right"],c=[re,"top",K],h=r(u),f=r(c);l(u,t,h),l(c,t,f);},l.getLayoutParams=function(t){return l.copyLayoutParams({},t);},l.copyLayoutParams=function(t,e){return e&&t&&s(u,function(i){e.hasOwnProperty(i)&&(t[i]=e[i]);}),t;},l;}),e("echarts/chart/pie/PieView",[pe,"../../util/graphic",de,"../../view/Chart"],function(t){

});}function i(t,e,i,n,r){var a=(e.startAngle+e.endAngle)/2,o=Math.cos(a),s=Math.sin(a),l=i?n:0,u=[o*l,s*l];r?t.animate().when(200,{position:u}).start("bounceOut"):t.attr(Q,u);}function n(t,e){function i(){o[E]=o.hoverIgnore,s[E]=s.hoverIgnore;}function n(){o[E]=o.normalIgnore,s[E]=s.normalIgnore;}a.Group.call(this);var r=new a.Sector({z2:2}),o=new a.Polyline(),s=new a.Text();this.add(r),this.add(o),this.add(s),this.updateData(t,e,!0),this.on("emphasis",i).on("normal",n).on("mouseover",i).on(C,n);}function r(t,e,i,n,r){var a=n[ce](ue),o=r===p||"inner"===r;return{fill:a[oe]()||(o?"#fff":t[k](e,"color")),opacity:t[k](e,Y),textFont:a[se](),text:l[u](t.hostModel.getFormattedLabel(e,i),t[s](e))};}var a=t("../../util/graphic"),l=t(de),c=n[j];

u.useStyle(l.defaults({lineJoin:"bevel",fill:g},m[ce]("normal").getItemStyle())),u.hoverStyle=m[ce]("emphasis").getItemStyle(),i(this,t.getItemLayout(e),h.get("selected"),c.get("selectedOffset"),c.get(B)),u.off("mouseover").off(C

n[$](r(t,e,"normal",f,g)),n[E]=n.normalIgnore=!f.get("show"),n.hoverIgnore=!p.get("show"),i[E]=i.normalIgnore=!v.get("show"),i.hoverIgnore=!m.get("show"),i[$]({stroke:h,opacity:t[k](e,Y)}),i[$](v[ce]("lineStyle").getLineStyle()),n.hoverStyle=r(t,e,"emphasis",p,g),i.hoverStyle=m[ce]("lineStyle").getLineStyle();var y=v.get("smooth");y&&y===!0&&(y=.4),i.setShape({smooth:y});},l[_](n,a.Group);var h=t("../../view/Chart")[X]({type:"pie",init:function init(){var t=new a.Group();this._sectorGroup=t;},render:function render(t,i,r,a){

),p&&e.on("click",d),o.setItemGraphicEl(t,e),u.add(e);})[I](function(t,e){var i=s.getItemGraphicEl(e);i.updateData(o,t),i.off("click"),p&&i.on("click",d),u.add(i),o.setItemGraphicEl(t,i);}).remove(function(t){var e=s.getItemGraphicEl(t);u.remove(e);}

this._data=o;}},dispose:function dispose(){},_createClipPath:function _createClipPath(t,e,i,n,r,o,s){var l=new a.Sector({shape:{cx:t,cy:e,r0:0,r:i,startAngle:n,endAngle:n,clockwise:r}});return a.initProps(l,{shape:{endAngle:n+(r?1:-1)*Math.PI*2}},s,o),l;},containPoint:function containPoint(t,e){var i=e[D](),n=i.getItemLayout(0);if(n){var r=t[0]-n.cx,a=t[1]-n.cy,o=Math.sqrt(r*r+a*a);

});n(t,m,c,h);});};}),e("echarts/processor/dataFilter",[],function(){return function(t,e){var i=e.findComponents({mainType:"legend"});i&&i[Z]&&e.eachSeriesByType(t,function(t){var e=t[D]();e.filterSelf(function(t){for(var n=e[s](t),r=0;r<i[Z];r++){if(!i[r].isSelected(n))return!1;}return!0;},this);},this);};}),e("echarts/chart/line/LineView",[pe,de,"../helper/SymbolDraw","../helper/Symbol","./lineAnimationDiff","../../util/graphic","../../util/model","./poly","../../view/Chart"],function(t){function e(t,e){if(t[Z]===e[Z]){for(var i=0;i<t[Z];i++){var n=t[i],r=e[i];if(n[0]!==r[0]||n[1]!==r[1])return;}return!0;}}function i(t){return(typeof t==="undefined"?"undefined":(0,_typeof3.default)(t))===x?t:t?.3:0;}function n(t){var e=t.getGlobalExtent();if(t.onBand){var i=t.getBandWidth()/2-1,n=e[1]>e[0]?1:-1;e[0]+=n*i,e[1]-=n*i;}return e;}function r(t){return t>=0?1:-1;}function o(t,e){

}}return t[o]&&a.push(t[o]),a;}function f(t,e){var i=t.getVisual("visualMeta");if(i&&i[Z]&&t.count()){for(var n,r=i[Z]-1;r>=0;r--){if(i[r].dimension<2){n=i[r];break;}}

return w[X]({type:"line",init:function init(){var t=new g.Group(),e=new p();this.group.add(e.group),this._symbolDraw=e,this._lineGroup=t;},render:function render(t,n,r){var

);}this._data=l,this._coordSys=a,this._stackedOnPoints=M,this._points=v,this._step=P;},dispose:function dispose(){},highlight:function highlight(t,e,i,n){

}o.highlight();}else w[j].highlight.call(this,t,e,i,n);},downplay:function downplay(t,e,i,n){var r=t[D](),a=_.queryDataIndex(r,n);if(null!=a&&a>=0){var o=r.getItemGraphicEl(a);o&&(o.__temp?(r.setItemGraphicEl(a,null),this.group.remove(o)):o.downplay());}else w[j].downplay.call(this,t,e,i,n);},_newPolyline:function _newPolyline(t){var e=this._polyline;return e&&this._lineGroup.remove(e),e=new b.Polyline({shape:{points:t},silent:!0,z2:10}),this._lineGroup.add(e),this._polyline=e,e;},_newPolygon:function _newPolygon(t,e){var i=this._polygon;return i&&this._lineGroup.remove(i),i=new b.Polygon({shape:{points:t,stackedOnPoints:e},silent:!0}),this._lineGroup.add(i),this._polygon=i,i;},_getSymbolIgnoreFunc:function _getSymbolIgnoreFunc(t,e){var i=e.getAxesByScale(a)[0];return i&&i.isLabelIgnored?d.bind(i.isLabelIgnored,i):void 0;},_updateAnimation:function _updateAnimation(t,e,i,n,r){

}});},remove:function remove(){var t=this.group,e=this._data;this._lineGroup[fe](),this._symbolDraw.remove(!0),e&&e.eachItemGraphicEl(function(i,n){i.__temp&&(t.remove(i),e.setItemGraphicEl(n,null));}),this._polyline=this._polygon=this._coordSys=this._points=this._stackedOnPoints=this._data=null;}});}),e("echarts/chart/line/LineSeries",[pe,"../helper/createListFromArray","../../model/Series"],function(t){var e=t("../helper/createListFromArray"),i=t("../../model/Series");return i[X]({type:"series.line",dependencies:["grid","polar"],getInitialData:function getInitialData(t,i){return e(t.data,this,i);},defaultOption:{zlevel:0,z:2,coordinateSystem:"cartesian2d",legendHoverLink:!0,hoverAnimation:!0,clipOverflow:!0,label:{

var i=t.getRawValue(e),n=t[S](e);r.setItemVisual(e,"symbolSize",s(i,n));}),r.each(function(t){var e=r[o](t),i=e[v]("symbol",!0),n=e[v]("symbolSize",!0);

if(i){var n=i[l];"singleAxis"===i.type?e.each(n[0],function(t,n){e.setItemLayout(n,isNaN(t)?[0/0,0/0]:i.dataToPoint(t));}):e.each(n,function(t,n,r){e.setItemLayout(r,isNaN(t)||isNaN(n)?[0/0,0/0]:i.dataToPoint([t,n]));},!0);}});};}),e("echarts/component/legend/LegendModel",[pe,de,"../../model/Model","../../echarts"],function(t){var e=t(de),i=t("../../model/Model"),n=t("../../echarts").extendComponentModel({type:"legend",dependencies:[P],layoutMode:{type:"box",ignoreSize:!0},init:function init(t,e,i){this.mergeDefaultAndTheme(t,i),t.selected=t.selected||{};},mergeOption:function mergeOption(t){n.superCall(this,"mergeOption",t);},optionUpdated:function optionUpdated(){this._updateData(this[r]);var t=this._data;if(t[0]&&"single"===this.get("selectedMode")){for(var e=!1,i=0;i<t[Z];i++){var n=t[i].get("name");if(this.isSelected(n)){this.select(n),e=!0;break;}}!e&&this.select(t[0].get("name"));}},_updateData:function _updateData(t){var n=e.map(this.get("data")||[],function(t){return((typeof t==="undefined"?"undefined":(0,_typeof3.default)(t))===F||(typeof t==="undefined"?"undefined":(0,_typeof3.default)(t))===x)&&(t={name:t}),new i(t,this,this[r]);},this);this._data=n;var a=e.map(t.getSeries(),function(t){return t.name;});t.eachSeries(function(t){if(t.legendDataProvider){var e=t.legendDataProvider();a=a[h](e.mapArray(e[s]));}}),this._availableNames=a;},getData:function getData(){return this._data;},select:function select(t){var i=this[c].selected,n=this.get("selectedMode");if("single"===n){var r=this._data;e.each(r,function(t){i[t.get("name")]=!1;});}i[t]=!0;},unSelect:function unSelect(t){"single"!==this.get("selectedMode")&&(this[c].selected[t]=!1);},toggleSelected:function toggleSelected(t){var e=this[c].selected;e.hasOwnProperty(t)||(e[t]=!0),this[e[t]?"unSelect":"select"](t);},isSelected:function isSelected(t){var i=this[c].selected;return!(i.hasOwnProperty(t)&&!i[t])&&e[O](this._availableNames,t)>=0;},defaultOption:{

}),e("echarts/processor/dataSample",[],function(){var t={average:function average(t){for(var e=0,i=0,n=0;n<t[Z];n++){isNaN(t[n])||(e+=t[n],i++);}return 0===i?0/0:e/i;},sum:function sum(t){for(var e=0,i=0;i<t[Z];i++){e+=t[i]||0;}return e;},max:function max(t){for(var e=-1/0,i=0;i<t[Z];i++){t[i]>e&&(e=t[i]);}return e;},min:function min(t){for(var e=1/0,i=0;i<t[Z];i++){t[i]<e&&(e=t[i]);}return e;},nearest:function nearest(t){return t[0];}},e=function e(t){return Math.round(t[Z]/2);};return function(i,n){

(typeof r==="undefined"?"undefined":(0,_typeof3.default)(r))===F?h=t[r]:(typeof r==="undefined"?"undefined":(0,_typeof3.default)(r))===b&&(h=r),h&&(n=n.downSample(s.dim,1/c,h,e),i.setData(n));}}},this);};}),e("echarts/component/legend/legendAction",[pe,"../../echarts",de],function(t){function e(t,e,i){var r,a={},o="toggleSelected"===t;return i.eachComponent("legend",function(i){o&&null!=r?i[r?"select":"unSelect"](e.name):(i[t](e.name),r=i.isSelected(e.name));var s=i[D]();n.each(s,function(t){var e=t.get("name");if("\n"!==e&&""!==e){var n=i.isSelected(e);a[e]=e in a?a[e]&&n:n;}});}),{name:e.name,selected:a};}var i=t("../../echarts"),n=t(de);i.registerAction("legendToggleSelect","legendselectchanged",n.curry(e,"toggleSelected")),i.registerAction("legendSelect","legendselected",n.curry(e,"select")),i.registerAction("legendUnSelect","legendunselected",n.curry(e,"unSelect"));}),e("echarts/component/legend/LegendView",[pe,de,"../../util/symbol","../../util/graphic","../helper/listComponent","../../echarts"],function(t){function e(t,e){e.dispatchAction({type:"legendToggleSelect",name:t});}function i(t,e,i){

var v=p[D](),m=v.getVisual("color");(typeof m==="undefined"?"undefined":(0,_typeof3.default)(m))===b&&(m=m(p[S](0)));var g=v.getVisual("legendSymbol")||"roundRect",y=v.getVisual("symbol"),_=this._createItem(s,r,t,g,y,f,m,h);_.on("click",l(e,s,u)).on("mouseover",l(i,p,null,u)).on(C,l(n,p,null,u)),d[s]=!0;}else a.eachRawSeries(function(a){if(!d[s]&&a.legendDataProvider){var o=a.legendDataProvider(),c=o.indexOfName(s);if(0>c)return;var p=o[k](c,"color"),v="roundRect",m=this._createItem(s,r,t,v,null,f,p,h);m.on("click",l(e,s,u)).on("mouseover",l(i,a,s,u)).on(C,l(n,a,s,u)),d[s]=!0;}},this);},this),s.layout(c,t,u),s.addBackground(c,t);}},_createItem:function _createItem(t,e,i,n,s,l,u,h){var f=i

}var M="left"===l?f+5:-5,S=l,C=i.get("formatter"),A=t;(typeof C==="undefined"?"undefined":(0,_typeof3.default)(C))===F&&C?A=C[T]("{name}",null!=t?t:""):(typeof C==="undefined"?"undefined":(0,_typeof3.default)(C))===b&&(A=C(t));

},formatterParams:{componentType:"legend",legendIndex:i.componentIndex,name:t,$vars:["name"]}},_[c]):null});return m.add(L),m.eachChild(function(t){t[ne]=!0;}),L[ne]=!h,this.group.add(m),o.setHoverStyle(m),m;}});}),e("echarts/component/tooltip/TooltipModel",[pe,"../../echarts"],function(t){t("../../echarts").extendComponentModel({type:"tooltip",defaultOption:{

),e("echarts/component/legend/legendFilter",[],function(){return function(t){var e=t.findComponents({mainType:"legend"});e&&e[Z]&&t.filterSeries(function(t){for(var i=0;i<e[Z];i++){if(!e[i].isSelected(t.name))return!1;}return!0;});};}),e("echarts/util/number",[pe],function(){function t(t){return t[T](/^\s+/,"")[T](/\s+$/,"");}var e={},i=1e-4;return e.linearMap=function(t,e,i,n){var r=e[1]-e[0],a=i[1]-i[0];if(0===r)return 0===a?i[0]:(i[0]+i[1])/2;if(n){if(r>0){if(t<=e[0])return i[0];if(t>=e[1])return i[1];}else{if(t>=e[0])return i[0];if(t<=e[1])return i[1];}}else{if(t===e[0])return i[0];if(t===e[1])return i[1];}return(t-e[0])/r*a+i[0];},e.parsePercent=function(e,i){switch(e){case J:case te:e="50%";break;case"left":case"top":e="0%";break;case"right":case K:e="100%";}return(typeof e==="undefined"?"undefined":(0,_typeof3.default)(e))===F?t(e).match(/%$/)?parseFloat(e)/100*i:parseFloat(e):null==e?0/0:+e;},e.round=function(t,e){return null==e&&(e=10),e=Math.min(Math.max(0,e),20),+(+t).toFixed(e);},e.asc=function(t){return t.sort(function(t,e){return t-e;}),t;},e.getPrecision=function(t){if(t=+t,isNaN(t))return 0;for(var e=1,i=0;Math.round(t*e)/e!==t;){e*=10,i++;}return i;},e.getPrecisionSafe=function(t){var e=t.toString(),i=e[O](".");return 0>i?0:e[Z]-1-i;}

},e.isRadianAroundZero=function(t){return t>-i&&i>t;},e.parseDate=function(t){if(t instanceof Date)return t;if((typeof t==="undefined"?"undefined":(0,_typeof3.default)(t))===F){var e=new Date(t);return isNaN(+e)&&(e=new Date(new Date(t[T](/-/g,"/"))-new Date("1970/01/01"))),e;}return new Date(Math.round(t));},e.quantity=function(t){return Math.pow(10,Math.floor(Math.log(t)/Math.LN10));},e.nice=function(t,i){var n,r=e.quantity(t),a=t/r;return n=i?1.5>a?1:2.5>a?2:4>a?3:7>a?5:10:1>a?1:2>a?2:3>a?3:5>a?5:10,n*r;},e.reformIntervals=function(t){function e(t,i,n){return t.interval[n]<i.interval[n]||t.interval[n]===i.interval[n]&&(t.close[n]-i.close[n]===(n?-1:1)||!n&&e(t,i,1));}t.sort(function(t,i){return e(t,i,0)?-1:1;});for(var i=-1/0,n=1,r=0;r<t[Z];){for(var a=t[r].interval,o=t[r].close,s=0;2>s;s++){a[s]<=i&&(a[s]=i,o[s]=s?1:1-n),i=a[s],n=o[s];}a[0]===a[1]&&o[0]*o[1]!==1?t[A](r,1):r++;}return t;},e.isNumeric=function(t){return t-parseFloat(t)>=0;},e;}),e("echarts/component/tooltip/TooltipView",[pe,"./TooltipContent","../../util/graphic",de,"../../util/format","../../util/number","../../util/model",q,"../../model/Model","../../echarts"],function(t){function e(t,e){if(!t||!e)return!1;var i=L.round;return i(t[0])===i(e[0])&&i(t[1])===i(e[1]);}function i(t,e,i,n){return{x1:t,y1:e,x2:i,y2:n};}function a(t,e,i,n){return{x:t,y:e,width:i,height:n};}function u(t,e,i,n,r,a){return{cx:t,cy:e,r0:i,r:n,startAngle:r,endAngle:a,clockwise:!0};}function c(t,e,i,n,r){var a=i.clientWidth,o=i.clientHeight,s=20;return t+a+s>n?t-=a+s:t+=s,e+o+s>r?e-=o+s:e+=s,[t,e];}

}return[o,s];}function d(t,e,i,r,a,o,s,l){var u=l[ie](),d=l[ee](),p=s&&s[ae]().clone();if(s&&p[g](s.transform),(typeof t==="undefined"?"undefined":(0,_typeof3.default)(t))===b&&(t=t([e,i],o,a.el,p)),T[w](t))e=E(t[0],u),i=E(t[1],d);else if((typeof t==="undefined"?"undefined":(0,_typeof3.default)(t))===F&&s){var v=f(t,p,a.el);e=v[0],i=v[1];}else{var v=c(e,i,a.el,u,d);e=v[0],i=v[1];}if(r){var v=h(e,i,a.el,u,d);e=v[0],i=v[1];}a[n](e,i);}function v(t){var e=t[he],i=t.get("tooltip.trigger",!0);return!(!e||"cartesian2d"!==e.type&&"polar"!==e.type&&"singleAxis"!==e.type||"item"===i);}var _=t("./TooltipContent"),x=t("../../util/graphic"),T=t(de),A=t("../../util/format"),L=t("../../util/number"),k=t("../../util/model"),E=L.parsePercent,R=t(q),N=t("../../model/Model");t("../../echarts").extendComponentView({type:"tooltip",_axisPointers:{},init:function init(t,e){if(!R.node){var i=new _(e.getDom(),e);this._tooltipContent=i;}},render:function render(t,e,i){if(!R.node){

r&&this.group.add(r);var a=t.get("triggerOn");if(null!=this._lastX&&null!=this._lastY&&"none"!==a){var o=this;clearTimeout(this._refreshUpdateTimeout),this._refreshUpdateTimeout=setTimeout(function(){o.manuallyShowTip(t,e,i,{x:o._lastX,y:o._lastY});});}var s=this._api.getZr();s.off("click",this._tryShow),s.off("mousemove",this._mousemove),s.off(C,this._hide),s.off("globalout",this._hide),"click"===a?s.on("click",this._tryShow,this):"mousemove"===a&&(s.on("mousemove",this._mousemove,this),s.on(C,this._hide,this),s.on("globalout",this._hide,this));}},_mousemove:function _mousemove(t){var e=this._tooltipModel.get("showDelay"),i=this;clearTimeout(this._showTimeout),e>0?this._showTimeout=setTimeout(function(){i._tryShow(t);},e):this._tryShow(t);},manuallyShowTip:function manuallyShowTip(t,e,i,n){function r(t){var e=t[D](),i=k.queryDataIndex(e,n);return null!=i&&!T[w](i)&&e.hasValue(i)?!0:void 0;}if(n.from!==this.uid){var e=this._ecModel,a=n.seriesIndex,o=e.getSeriesByIndex(a),i=this._api,s="axis"===this._tooltipModel.get("trigger");if(null==n.x||null==n.y){if(s?(o&&!r(o)&&(o=null),o||e.eachSeries(function(t){v(t)&&!o&&r(t)&&(o=t);})):o=o||e.getSeriesByIndex(0),o){var u=o[D](),c=k.queryDataIndex(u,n);if(null==c||T[w](c))return;var h,f,d=u.getItemGraphicEl(c),p=o[he];if(o.getTooltipPosition){var m=o.getTooltipPosition(c)||[];h=m[0],f=m[1];}else if(p&&p.dataToPoint){var m=p.dataToPoint(u.getValues(T.map(p[l],function(t){return o.coordDimToDataDim(t)[0];}),c,!0));h=m&&m[0],f=m&&m[1];}else if(d){var y=d[ae]().clone();y[g](d.transform),h=y.x+y.width/2,f=y.y+y[re]/2;}null!=h&&null!=f&&this._tryShow({offsetX:h,offsetY:f,position:n[Q],target:d,event:{}});}}else{var d=i.getZr().handler.findHover(n.x,n.y);this._tryShow({offsetX:n.x,offsetY:n.y,position:n[Q],target:d,event:{}});}}},manuallyHideTip:function manuallyHideTip(t,e,i,n){n.from!==this.uid&&this._hide();},_prepareAxisTriggerData:function _prepareAxisTriggerData(t,e){var i={};return e.eachSeries(function(t){if(v(t)){var e,n,r=t[he];"cartesian2d"===r.type?(e=r.getBaseAxis(),n=e.dim+e.index):"singleAxis"===r.type?(e=r.getAxis(),n=e.dim+e.type):(e=r.getBaseAxis(),n=e.dim+r.name),i[n]=i[n]||{coordSys:[],series:[]},i[n].coordSys.push(r),i[n][P].push(t);}},this),i;},_tryShow:function _tryShow(t){var e=t.target,i=this._tooltipModel,n=i.get("trigger"),r=this._ecModel,a=this._api;if(i)if(this._lastX=t.offsetX,this._lastY=t.offsetY,e&&null!=e[z]){var s=e.dataModel||r.getSeriesByIndex(e.seriesIndex),l=e[z],u=s[D](),c=u[o](l);"axis"===(c.get("tooltip.trigger")||n)?this._showAxisTooltip(i,r,t):(this._ticket="",this._hideAxisPointer(),this._resetLastHover(),this._showItemTooltipContent(s,l,e.dataType,t)),a.dispatchAction({type:"showTip",from:this.uid,dataIndexInside:l,dataIndex:u.getRawIndex(l),seriesIndex:e.seriesIndex});}else if(e&&e.tooltip){var h=e.tooltip;if((typeof h==="undefined"?"undefined":(0,_typeof3.default)(h))===F){var f=h;h={content:f,formatter:f};}

,_showAxisTooltip:function _showAxisTooltip(t,i,n){var r=t[ce]("axisPointer"),a=r.get("type");if("cross"===a){var o=n.target;if(o&&null!=o[z]){var s=i.getSeriesByIndex(o.seriesIndex),u=o[z];this._showItemTooltipContent(s,u,o.dataType,n);}

var v=!1,m=this._lastHover;if("cross"===a)e(m.data,f)&&(v=!0),m.data=f;else{var g=T[O](h,p);m.data===f[g]&&(v=!0),m.data=f[g];}var y=t.get(B);"cartesian2d"!==s.type||v?"polar"!==s.type||v?"singleAxis"!==s.type||v||this._showSinglePointer(r,s,p,u,y):this._showPolarPointer(r,s,p,u,y):this._showCartesianPointer(r,s,p,u,y),"cross"!==a&&this._dispatchAndShowSeriesTooltipContent(s,i[P],u,f,v,n[Q]);},this),this._tooltipModel.get("show")||this._hideAxisPointer(),c&&this._hide();},_showCartesianPointer:function _showCartesianPointer(t,e,n,r,o){function s(n,r,a){var o="x"===n?i(r[0],a[0],r[0],a[1]):i(a[0],r[1],a[1],r[1]),s=u._getPointerElement(e,t,n,o);x.subPixelOptimizeLine({shape:o,style:s.style}),f?x.updateProps(s,{shape:o},t):s.attr({shape:o});}function l(i,n,r){var o=e.getAxis(i),s=o.getBandWidth(),l=r[1]-r[0],c="x"===i?a(n[0]-s/2,r[0],s,l):a(r[0],n[1]-s/2,l,s),h=u._getPointerElement(e,t,i,c);f?x.updateProps(h,{shape:c},t):h.attr({shape:c});}var u=this,c=t.get("type"),h=e.getBaseAxis(),f=o&&"cross"!==c&&"category"===h.type&&h.getBandWidth()>20;

);}var s=this,l=t.get("type"),u=a&&"cross"!==l&&"category"===e.getBaseAxis().type,c=e.getRect(),h=[c.y,c.y+c[re]];o(n,r,h);},_showPolarPointer:function _showPolarPointer(t,e,n,r,a){

);}var l=this,c=t.get("type"),h=e.getAngleAxis(),f=e.getRadiusAxis(),d=a&&"cross"!==c&&"category"===e.getBaseAxis().type;if("cross"===c)o("angle",r,f[y]()),o("radius",r,h[y]()),this._updateCrossText(e,r,t);else{var p=e.getAxis("radius"===n?"angle":"radius"),v=p[y]();("line"===c?o:s)(n,r,v);}},_updateCrossText:function _updateCrossText(t,e,i){var n=i[ce]("crossStyle"),r=n[ce](ue),a=this._tooltipModel,o=this._crossText;o||(o=this._crossText=new x.Text({style:{textAlign:"left",textVerticalAlign:"bottom"}}),this.group.add(o));var s=t.pointToData(e),u=t[l];s=T.map(s,function(e,i){var n=t.getAxis(u[i]);return e="category"===n.type||"time"===n.type?n.scale.getLabel(e):A.addCommas(e.toFixed(n.getPixelPrecision()));}),o[$]({fill:r[oe]()||n.get("color"),textFont:r[se](),text:s.join(", "),x:e[0]+5,y:e[1]-5}),o.z=a.get("z"),o[M]=a.get(M);},_getPointerElement:function _getPointerElement(t,e,i,n){

},_dispatchAndShowSeriesTooltipContent:function _dispatchAndShowSeriesTooltipContent(t,e,i,n,r,a){var o=this._tooltipModel,l=t.getBaseAxis(),u={x:1,radius:1,single:1}[l.dim]?0:1;if(e[Z]){var c,h=T.map(e,function(t){return{seriesIndex:t.seriesIndex,dataIndexInside:t.getAxisTooltipDataIndex?t.getAxisTooltipDataIndex(t.coordDimToDataDim(l.dim),n,l):t[D]().indexOfNearest(t.coordDimToDataDim(l.dim)[0],n[u],!1,"category"===l.type?.5:null)};});T.each(h,function(t,i){e[i][D]().hasValue(t.dataIndexInside)&&(c=i);}),c=c||0;var f=this._lastHover,p=this._api;f.payloadBatch&&!r&&p.dispatchAction({type:"downplay",batch:f.payloadBatch}),r||(p.dispatchAction({type:"highlight",batch:h}),f.payloadBatch=h);var v=h[c].dataIndexInside;if(p.dispatchAction({type:"showTip",dataIndexInside:v,dataIndex:e[c][D]().getRawIndex(v),seriesIndex:h[c].seriesIndex,from:this.uid}),l&&o.get("showContent")&&o.get("show")){var m=T.map(e,function(t,e){return t[S](h[e].dataIndexInside);});if(r)d(a||o.get(Q),i[0],i[1],o.get("confine"),this._tooltipContent,m,null,p);else{var g=h[c].dataIndexInside,y="time"===l.type?l.scale.getLabel(n[u]):e[c][D]()[s](g),_=(y?A.encodeHTML(y)+"<br />":"")+T.map(e,function(t,e){return t.formatTooltip(h[e].dataIndexInside,!0);}).join("<br />"),x="axis_"+t.name+"_"+g;this._showTooltipContent(o,_,m,x,i[0],i[1],a,null,p);}}}},_showItemTooltipContent:function _showItemTooltipContent(t,e,i,n){var a=this._api,s=t[D](i),l=s[o](e),u=l.get("tooltip",!0);if((typeof u==="undefined"?"undefined":(0,_typeof3.default)(u))===F){var c=u;u={formatter:c};}var h=this._tooltipModel,f=t[ce]("tooltip",h),d=new N(u,f,f[r]),p=t[S](e,i),v=t.formatTooltip(e,!1,i),m="item_"+t.name+"_"+e;this._showTooltipContent(d,v,p,m,n.offsetX,n.offsetY,n[Q],n.target,a);},_showTooltipContent:function _showTooltipContent(t,e,i,n,r,a,o,s,l){if(this._ticket="",t.get("showContent")&&t.get("show")){var u=this._tooltipContent,c=t.get("confine"),h=t.get("formatter");o=o||t.get(Q);var f=e;if(h)if((typeof h==="undefined"?"undefined":(0,_typeof3.default)(h))===F)f=A.formatTpl(h,i,!0);else if((typeof h==="undefined"?"undefined":(0,_typeof3.default)(h))===b){var p=this,v=n,m=function m(t,e){t===p._ticket&&(u.setContent(e),d(o,r,a,c,u,i,s,l));};p._ticket=v,f=h(i,v,m);}u.show(t),u.setContent(f),d(o,r,a,c,u,i,s,l);}},_showAxisPointer:function _showAxisPointer(t){if(t){var e=this._axisPointers[t];e&&T.each(e,function(t){t.show();});}else this.group.eachChild(function(t){t.show();}),this.group.show();},_resetLastHover:function _resetLastHover(){var t=this._lastHover;t.payloadBatch&&this._api.dispatchAction({type:"downplay",batch:t.payloadBatch}),this._lastHover={};},_hideAxisPointer:function _hideAxisPointer(t){if(t){var e=this._axisPointers[t];e&&T.each(e,function(t){t.hide();});}else this.group.children()[Z]&&this.group.hide();},_hide:function _hide(){clearTimeout(this._showTimeout),this._hideAxisPointer(),this._resetLastHover(),this._alwaysShowContent||this._tooltipContent.hideLater(this._tooltipModel.get("hideDelay")),this._api.dispatchAction({type:"hideTip",from:this.uid}),this._lastX=this._lastY=null;},dispose:function dispose(t,e){if(!R.node){var i=e.getZr();this._tooltipContent.hide(),i.off("click",this._tryShow),i.off("mousemove",this._mousemove),i.off(C,this._hide),i.off("globalout",this._hide);}}});}),e("echarts/util/format",[pe,de,"./number","zrender/contain/text"],function(t){var e=t(de),i=t("./number"),n=t("zrender/contain/text"),r={};r.addCommas=function(t){return isNaN(t)?"-":(t=(t+"").split("."),t[0][T](/(\d{1,3})(?=(?:\d{3})+(?!\d))/g,"$1,")+(t[Z]>1?"."+t[1]:""));},r.toCamelCase=function(t,e){return t=(t||"")[G]()[T](/-(.)/g,function(t,e){return e.toUpperCase();}),e&&t&&(t=t.charAt(0).toUpperCase()+t.slice(1)),t;},r.normalizeCssArray=function(t){var e=t[Z];return(typeof t==="undefined"?"undefined":(0,_typeof3.default)(t))===x?[t,t,t,t]:2===e?[t[0],t[1],t[0],t[1]]:3===e?[t[0],t[1],t[2],t[1]]:t;};var a=r.encodeHTML=function(t){return String(t)[T](/&/g,"&amp;")[T](/</g,"&lt;")[T](/>/g,"&gt;")[T](/"/g,"&quot;")[T](/'/g,"&#39;");},o=["a","b","c","d","e","f","g"],s=function s(t,e){return"{"+t+(null==e?"":e)+"}";};r.formatTpl=function(t,i,n){e[w](i)||(i=[i]);var r=i[Z];if(!r)return"";for(var l=i[0].$vars||[],u=0;u<l[Z];u++){var c=o[u],h=s(c,0);t=t[T](s(c),n?a(h):h);}for(var f=0;r>f;f++){for(var d=0;d<l[Z];d++){var h=i[f][l[d]];t=t[T](s(o[d],f),n?a(h):h);}}return t;};var l=function l(t){return 10>t?"0"+t:t;};return r.formatTime=function(t,e){

[G]()[T]("yyyy",r)[T]("yy",r%100)[T]("dd",l(o))[T]("d",o)[T]("hh",l(s))[T]("h",s)[T]("mm",l(u))[T]("m",u)[T]("ss",l(c))[T]("s",c);},r.capitalFirst=function(t){return t?t.charAt(0).toUpperCase()+t.substr(1):t;},r.truncateText=n.truncateText,r;}),e("echarts/scale/Interval",[pe,"../util/number","../util/format","./Scale"],function(t){var e=t("../util/number"),i=t("../util/format"),n=t("./Scale"),r=Math.floor,a=Math.ceil,o=e.getPrecisionSafe,s=e.round,l=n[X]({type:"interval",_interval:0,setExtent:function setExtent(t,e){var i=this[W];isNaN(t)||(i[0]=parseFloat(t)),isNaN(e)||(i[1]=parseFloat(e));},unionExtent:function unionExtent(t){var e=this[W];t[0]<e[0]&&(e[0]=t[0]),t[1]>e[1]&&(e[1]=t[1]),l[j].setExtent.call(this,e[0],e[1]);},getInterval:function getInterval(){return this._interval||this.niceTicks(),this._interval;},setInterval:function setInterval(t){this._interval=t,this._niceExtent=this[W].slice();},getTicks:function getTicks(){this._interval||this.niceTicks();var t=this._interval,e=this[W],i=[],n=1e4;if(t){var r=this._niceExtent,a=o(t)+2;e[0]<r[0]&&i.push(e[0]);for(var l=r[0];l<=r[1];){if(i.push(l),l=s(l+t,a),i[Z]>n)return[];}e[1]>(i[Z]?i[i[Z]-1]:r[1])&&i.push(e[1]);}return i;},getTicksLabels:function getTicksLabels(){for(var t=[],e=this.getTicks(),i=0;i<e[Z];i++){t.push(this.getLabel(e[i]));}return t;},getLabel:function getLabel(t){return i.addCommas(t);},niceTicks:function niceTicks(t){t=t||5;var i=this[W],n=i[1]-i[0];if(isFinite(n)){0>n&&(n=-n,i.reverse());var l=s(e.nice(n/t,!0),Math.max(o(i[0]),o(i[1]))+2),u=o(l)+2,c=[s(a(i[0]/l)*l,u),s(r(i[1]/l)*l,u)];this._interval=l,this._niceExtent=c;}},niceExtent:function niceExtent(t,e,i){var n=this[W];if(n[0]===n[1])if(0!==n[0]){var o=n[0];i?n[0]-=o/2:(n[1]+=o/2,n[0]-=o/2);}else n[1]=1;var l=n[1]-n[0];isFinite(l)||(n[0]=0,n[1]=1),this.niceTicks(t);var u=this._interval;e||(n[0]=s(r(n[0]/u)*u)),i||(n[1]=s(a(n[1]/u)*u));}});return l[V]=function(){return new l();},l;}),e(q,[],function(){

;}var e={};return e=(typeof navigator==="undefined"?"undefined":(0,_typeof3.default)(navigator))===i?{browser:{},os:{},node:!0,canvasSupported:!0}:t(navigator.userAgent);}),e("echarts/model/Global",[pe,de,"../util/model","./Model","./Component","./globalDefault","./mixin/colorPalette"],function(t){function e(t,e){l.each(e,function(e,i){y.hasClass(i)||("object"==(typeof e==="undefined"?"undefined":(0,_typeof3.default)(e))?t[i]=t[i]?l.merge(t[i],e,!1):l.clone(e):null==t[i]&&(t[i]=e));});}function i(t){t=t,this[c]={},this[c][x]=1,this._componentsMap={},this._seriesIndices=null,e(t,this._theme[c]),l.merge(t,_,!1),this.mergeOption(t);}function n(t,e){l[w](e)||(e=e?[e]:[]);var i={};return f(e,function(e){i[e]=(t[e]||[]).slice();}),i;}function r(t,e,i){var n=e.type?e.type:i?i.subType:y.determineSubType(t,e);return n;}function a(t){return p(t,function(t){return t.componentIndex;})||[];}function o(t,e){

},this);}return e;},mergeOption:function mergeOption(t){function e(e,s){var h=u.normalizeToArray(t[e]),d=u.mappingToExists(o[e],h);u.makeIdAndName(d),f(d,function(t){var i=t[c];g(i)&&(t.keyInfo.mainType=e,t.keyInfo.subType=r(e,i,t.exist));});var p=n(o,s);i[e]=[],o[e]=[],f(d,function(t,n){

},this),e===P&&(this._seriesIndices=a(o[P]));}var i=this[c],o=this._componentsMap,s=[];f(t,function(t,e){null!=t&&(y.hasClass(e)?s.push(e):i[e]=null==i[e]?l.clone(t):l.merge(i[e],t,!0));}),y.topologicalTravel(s,y.getAllClassMainTypes(),e,this),this._seriesIndices=this._seriesIndices||[];},getOption:function getOption(){var t=l.clone(this[c]);return f(t,function(e,i){if(y.hasClass(i)){for(var e=u.normalizeToArray(e),n=e[Z]-1;n>=0;n--){u.isIdInner(e[n])&&e[A](n,1);}t[i]=e;}}),delete t[x],t;},getTheme:function getTheme(){return this._theme;},getComponent:function getComponent(t,e){var i=this._componentsMap[t];return i?i[e||0]:void 0;},queryComponents:function queryComponents(t){var e=t.mainType;if(!e)return[];var i=t.index,n=t.id,r=t.name,a=this._componentsMap[e];if(!a||!a[Z])return[];var s;if(null!=i)v(i)||(i=[i]),s=d(p(i,function(t){return a[t];}),function(t){return!!t;});else if(null!=n){var l=v(n);s=d(a,function(t){return l&&m(n,t.id)>=0||!l&&t.id===n;});}else if(null!=r){var u=v(r);s=d(a,function(t){return u&&m(r,t.name)>=0||!u&&t.name===r;});}else s=a;return o(s,t);},findComponents:function findComponents(t){function e(t){var e=r+"Index",i=r+"Id",n=r+"Name";return!t||null==t[e]&&null==t[i]&&null==t[n]?null:{mainType:r,index:t[e],id:t[i],name:t[n]};}function i(e){return t.filter?d(e,t.filter):e;}var n=t.query,r=t.mainType,a=e(n),s=a?this.queryComponents(a):this._componentsMap[r];return i(o(s,t));},eachComponent:function eachComponent(t,e,i){var n=this._componentsMap;if((typeof t==="undefined"?"undefined":(0,_typeof3.default)(t))===b)i=e,e=t,f(n,function(t,n){f(t,function(t,r){e.call(i,n,t,r);});});else if(l.isString(t))f(n[t],e,i);else if(g(t)){var r=this.findComponents(t);f(r,e,i);}},getSeriesByName:function getSeriesByName(t){var e=this._componentsMap[P];return d(e,function(e){return e.name===t;});},getSeriesByIndex:function getSeriesByIndex(t){return this._componentsMap[P][t];},getSeriesByType:function getSeriesByType(t){var e=this._componentsMap[P];return d(e,function(e){return e.subType===t;});},getSeries:function getSeries(){return this._componentsMap[P].slice();},eachSeries:function eachSeries(t,e){s(this),f(this._seriesIndices,function(i){var n=this._componentsMap[P][i];t.call(e,n,i);},this);},eachRawSeries:function eachRawSeries(t,e){f(this._componentsMap[P],t,e);},eachSeriesByType:function eachSeriesByType(t,e,i){s(this),f(this._seriesIndices,function(n){var r=this._componentsMap[P][n];r.subType===t&&e.call(i,r,n);},this);},eachRawSeriesByType:function eachRawSeriesByType(t,e,i){return f(this.getSeriesByType(t),e,i);},isSeriesFiltered:function isSeriesFiltered(t){return s(this),l[O](this._seriesIndices,t.componentIndex)<0;},filterSeries:function filterSeries(t,e){

);});}});return l.mixin(T,t("./mixin/colorPalette")),T;}),e("echarts/ExtensionAPI",[pe,de],function(t){function e(t){i.each(n,function(e){this[e]=i.bind(t[e],t);},this);}var i=t(de),n=["getDom","getZr",ie,ee,"dispatchAction","isDisposed","on","off","getDataURL","getConnectedDataURL",ce,"getOption"];return e;}),e("echarts/CoordinateSystem",[pe,de],function(t){function e(){this._coordinateSystems=[];}var i=t(de),n={};return e[j]={constructor:e,create:function create(t,e){var r=[];i.each(n,function(i){var n=i[V](t,e);r=r[h](n||[]);}),this._coordinateSystems=r;},update:function update(t,e){i.each(this._coordinateSystems,function(i){i[I]&&i[I](t,e);});},getCoordinateSystems:function getCoordinateSystems(){return this._coordinateSystems.slice();}},e.register=function(t,e){n[t]=e;},e.get=function(t){return n[t];},e;}),e("echarts/model/OptionManager",[pe,de,"../util/model","./Component"],function(t){function e(t){this._api=t,this._timelineOptions=[],this._mediaList=[],this._mediaDefault,this._currentMediaIndices=[],this._optionBackup,this._newBaseOption;}function i(t,e,i){var n,r,a=[],o=[],l=t.timeline;if(t.baseOption&&(r=t.baseOption),(l||t.options)&&(r=r||{},a=(t.options||[]).slice()),t.media){r=r||{};var u=t.media;f(u,function(t){t&&t[c]&&(t.query?o.push(t):n||(n=t));});}return r||(r=t),r.timeline||(r.timeline=l),f([r][h](a)[h](s.map(o,function(t){return t[c];})),function(t){f(e,function(e){e(t,i);});}),{baseOption:r,timelineOptions:a,mediaDefault:n,mediaList:o};}function n(t,e,i){var n={width:e,height:i,aspectratio:e/i},a=!0;return s.each(t,function(t,e){var i=e.match(m);if(i&&i[1]&&i[2]){var o=i[1],s=i[2][G]();r(n[s],t,o)||(a=!1);}}),a;}function r(t,e,i){return"min"===i?t>=e:"max"===i?e>=t:t===e;}function a(t,e){return t.join(",")===e.join(",");}function o(t,e){

},getTimelineOption:function getTimelineOption(t){var e,i=this._timelineOptions;if(i[Z]){var n=t.getComponent("timeline");n&&(e=d(i[n.getCurrentIndex()],!0));}return e;},getMediaOption:function getMediaOption(){var t=this._api[ie](),e=this._api[ee](),i=this._mediaList,r=this._mediaDefault,o=[],s=[];if(!i[Z]&&!r)return s;for(var l=0,u=i[Z];u>l;l++){n(i[l].query,t,e)&&o.push(l);}return!o[Z]&&r&&(o=[-1]),o[Z]&&!a(o,this._currentMediaIndices)&&(s=p(o,function(t){return d(-1===t?r[c]:i[t][c]);})),this._currentMediaIndices=o,s;}},e;}),e("echarts/model/Component",[pe,"./Model",de,"../util/component","../util/clazz","../util/layout","./mixin/boxLayout"],function(t){function e(t){var e=[];return n.each(u.getClassesByMainType(t),function(t){a.apply(e,t[j].dependencies||[]);}),n.map(e,function(t){return s.parseClassType(t).main;});}

,init:function init(t,e,i){this.mergeDefaultAndTheme(t,i);},mergeDefaultAndTheme:function mergeDefaultAndTheme(t,e){var i=this.layoutMode,r=i?l.getLayoutParams(t):{},a=e.getTheme();n.merge(t,a.get(this.mainType)),n.merge(t,this.getDefaultOption()),i&&l.mergeLayoutParam(t,r,i);},mergeOption:function mergeOption(t){n.merge(this[c],t,!0);var e=this.layoutMode;e&&l.mergeLayoutParam(this[c],t,e);},optionUpdated:function optionUpdated(){},getDefaultOption:function getDefaultOption(){if(!s.hasOwn(this,"__defaultOption")){for(var t=[],e=this.constructor;e;){var i=e[j].defaultOption;i&&t.push(i),e=e.superClass;}for(var r={},a=t[Z]-1;a>=0;a--){r=n.merge(r,t[a],!0);}s.set(this,"__defaultOption",r);}return s.get(this,"__defaultOption");},getReferringComponents:function getReferringComponents(t){return this[r].queryComponents({

this.seriesIndex=this.componentIndex,this.mergeDefaultAndTheme(t,i),d(this,"dataBeforeProcessed",this.getInitialData(t,i)),this.restoreData();},mergeDefaultAndTheme:function mergeDefaultAndTheme(t,i){

},fillDataTextStyle:function fillDataTextStyle(t){if(t)for(var e=0;e<t[Z];e++){t[e]&&t[e].label&&o.defaultEmphasis(t[e].label,o.LABEL_OPTIONS);}},getInitialData:function getInitialData(){},getData:function getData(t){var e=p(this,"data");return null==t?e:e.getLinkedData(t);},setData:function setData(t){d(this,"data",t);},getRawData:function getRawData(){return p(this,"dataBeforeProcessed");},coordDimToDataDim:function coordDimToDataDim(t){return[t];},dataDimToCoordDim:function dataDimToCoordDim(t){return t;},getBaseAxis:function getBaseAxis(){var t=this[he];return t&&t.getBaseAxis&&t.getBaseAxis();},formatTooltip:function formatTooltip(t,n){function r(t){var r=[];return e.each(t,function(t,e){var s,l=o.getDimensionInfo(e),u=l&&l.type;s=u===a?t+"":"time"===u?n?"":i.formatTime("yyyy/MM/dd hh:mm:ss",t):g(t),s&&r.push(s);}),r.join(", ");}var o=p(this,"data"),l=this.getRawValue(t),u=m(e[w](l)?r(l):g(l)),c=o[s](t),h=o[k](t,"color");e[R](h)&&h[L]&&(h=(h[L][0]||{}).color),h=h||"transparent";var f='<span style="display:inline-block;margin-right:5px;border-radius:10px;width:9px;height:9px;background-color:'+m(h)+'"></span>',d=this.name;return"\x00-"===d&&(d=""),n?f+m(this.name)+" : "+u:(d&&m(d)+"<br />")+f+(c?m(c)+" : "+u:u);},isAnimationEnabled:function isAnimationEnabled(){if(h.node)return!1;var t=this[v](B);return t&&this[D]().count()>this[v]("animationThreshold")&&(t=!1),t;},restoreData:function restoreData(){d(this,"data",p(this,"dataBeforeProcessed").cloneShallow());},getColorFromPalette:function getColorFromPalette(t,e){var i=this[r],n=u.getColorFromPalette.call(this,t,e);return n||(n=i.getColorFromPalette(t,e)),n;},getAxisTooltipDataIndex:null,getTooltipPosition:null});return e.mixin(y,o.dataFormatMixin),e.mixin(y,u),y;}),e("echarts/view/Component",[pe,"zrender/container/Group","../util/component","../util/clazz"],function(t){var e=t("zrender/container/Group"),i=t("../util/component"),n=t("../util/clazz"),r=function r(){this.group=new e(),this.uid=i.getUID("viewComponent");};r[j]={constructor:r,init:function init(){},render:function render(){},dispose:function dispose(){}};var a=r[j];return a.updateView=a.updateLayout=a.updateVisual=function(){

};h(e,function(t){var e=l[u](i[t],n[t]);null!=e&&(i[t]=e);});}},d.LABEL_OPTIONS=[Q,"offset","show",ue,"distance","formatter"],d.getDataItemValue=function(t){return t&&(null==t.value?t:t.value);},d.isDataItemOption=function(t){return f(t)&&!(t instanceof Array);},d.converDataValue=function(t,e){var i=e&&e.type;return i===a?t:("time"!==i||isFinite(t)||null==t||"-"===t||(t=+n.parseDate(t)),null==t||""===t?0/0:+t);},d.createDataFormatModel=function(t,e){var i=new r();return l.mixin(i,d.dataFormatMixin),i.seriesIndex=e.seriesIndex,i.name=e.name||"",i.mainType=e.mainType,i.subType=e.subType,i[D]=function(){return t;},i;},d.dataFormatMixin={getDataParams:function getDataParams(t,e){var i=this[D](e),n=this.seriesIndex,r=this.name,a=this.getRawValue(t,e),o=i.getRawIndex(t),l=i[s](t,!0),u=i.getRawDataItem(t);

,getFormattedLabel:function getFormattedLabel(t,e,n,r){e=e||"normal";var a=this[D](n),s=a[o](t),l=this[S](t,n);null!=r&&l.value instanceof Array&&(l.value=l.value[r]);var u=s.get(["label",e,"formatter"]);return(typeof u==="undefined"?"undefined":(0,_typeof3.default)(u))===b?(l.status=e,u(l)):(typeof u==="undefined"?"undefined":(0,_typeof3.default)(u))===F?i.formatTpl(u,l):void 0;},getRawValue:function getRawValue(t,e){var i=this[D](e),n=i.getRawDataItem(t);return null!=n?!f(n)||n instanceof Array?n:n.value:void 0;},formatTooltip:l.noop},d.mappingToExists=function(t,e){e=(e||[]).slice();var i=l.map(t||[],function(t){return{exist:t};});return h(e,function(t,n){if(f(t)){for(var r=0;r<i[Z];r++){if(!i[r][c]&&null!=t.id&&i[r].exist.id===t.id+"")return i[r][c]=t,void(e[n]=null);

}}}),h(e,function(t){if(f(t)){for(var e=0;e<i[Z];e++){var n=i[e].exist;if(!i[e][c]&&!d.isIdInner(n)&&null==t.id){i[e][c]=t;break;}}e>=i[Z]&&i.push({option:t});}}),i;},d.makeIdAndName=function(t){var e={};h(t,function(t){var i=t.exist;i&&(e[i.id]=t);}),h(t,function(t){var i=t[c];l.assert(!i||null==i.id||!e[i.id]||e[i.id]===t,"id duplicates: "+(i&&i.id)),i&&null!=i.id&&(e[i.id]=t),!t.keyInfo&&(t.keyInfo={});}),h(t,function(t){var i=t.exist,n=t[c],r=t.keyInfo;if(f(n)){if(r.name=null!=n.name?n.name+"":i?i.name:"\x00-",i)r.id=i.id;else if(null!=n.id)r.id=n.id+"";else{var a=0;do{r.id="\x00"+r.name+"\x00"+a++;}while(e[r.id]);}e[r.id]=t;}});},d.isIdInner=function(t){return f(t)&&t.id&&0===(t.id+"")[O]("\x00_ec_\x00");},d.compressBatches=function(t,e){function i(t,e,i){for(var n=0,r=t[Z];r>n;n++){for(var a=t[n].seriesId,o=d.normalizeToArray(t[n][z]),s=i&&i[a],l=0,u=o[Z];u>l;l++){var c=o[l];s&&s[c]?s[c]=null:(e[a]||(e[a]={}))[c]=1;}}}function n(t,e){var i=[];for(var r in t){if(t.hasOwnProperty(r)&&null!=t[r])if(e)i.push(+r);else{var a=n(t[r],!0);a[Z]&&i.push({seriesId:r,dataIndex:a});}}return i;}var r={},a={};return i(t||[],r),i(e||[],a,r),[n(r),n(a)];},d.queryDataIndex=function(t,e){return null!=e.dataIndexInside?e.dataIndexInside:null!=e[z]?l[w](e[z])?l.map(e[z],function(e){return t.indexOfRawIndex(e);}):t.indexOfRawIndex(e[z]):null!=e.name?l[w](e.name)?l.map(e.name,function(e){return t.indexOfName(e);}):t.indexOfName(e.name):void 0;},d.parseFinder=function(t,i,n){if(l.isString(i)){var r={};r[i+"Index"]=0,i=r;}var a=n&&n.defaultMainType;!a||e(i,a+"Index")||e(i,a+"Id")||e(i,a+"Name")||(i[a+"Index"]=0);var o={};return h(i,function(e,n){var e=i[n];if(n===z||"dataIndexInside"===n)return void(o[n]=e);var r=n.match(/^(\w+)(Index|Id|Name)$/)||[],a=r[1],s=r[2];if(a&&s){var l={mainType:a};l[s[G]()]=e;var u=t.queryComponents(l);o[a+"Models"]=u,o[a+"Model"]=u[0];}}),o;},d;}),e("echarts/view/Chart",[pe,"zrender/container/Group","../util/component","../util/clazz","../util/model",de],function(t){function e(){this.group=new r(),this.uid=a.getUID("viewChart");}function i(t,e){if(t&&(t.trigger(e),"group"===t.type))for(var n=0;n<t.childCount();n++){i(t.childAt(n),e);}}function n(t,e,n){var r=s.queryDataIndex(t,e);null!=r?l.each(s.normalizeToArray(r),function(e){i(t.getItemGraphicEl(e),n);}):t.eachItemGraphicEl(function(t){i(t,n);});}var r=t("zrender/container/Group"),a=t("../util/component"),o=t("../util/clazz"),s=t("../util/model"),l=t(de);e[j]={type:"chart",init:function init(){},render:function render(){},highlight:function highlight(t,e,i,r){n(t[D](),r,"emphasis");},downplay:function downplay(t,e,i,r){n(t[D](),r,"normal");},remove:function remove(){this.group[fe]();},dispose:function dispose(){}};var u=e[j];return u.updateView=u.updateLayout=u.updateVisual=function(t,e,i,n){this.render(t,e,i,n);},o.enableClassExtend(e,["dispose"]),o.enableClassManagement(e,{registerWhenExtend:!0}),e;}),e("echarts/util/throttle",[],function(){var t={},e="\x00__throttleOriginMethod",i="\x00__throttleRate",n="\x00__throttleType";return t.throttle=function(t,e,i){function n(){u=new Date().getTime(),c=null,t.apply(o,s||[]);}var r,a,o,s,l=0,u=0,c=null;e=e||0;var h=function h(){r=new Date().getTime(),o=this,s=arguments,a=r-(i?l:u)-e,clearTimeout(c),i?c=setTimeout(n,e):a>=0?n():c=setTimeout(n,-a),l=r;};return h.clear=function(){c&&(clearTimeout(c),c=null);},h;},t.createOrUpdate=function(r,a,o,s){var l=r[a];if(l){var u=l[e]||l,c=l[n],h=l[i];if(h!==o||c!==s){if(null==o||!s)return r[a]=u;l=r[a]=t.throttle(u,o,"debounce"===s),l[e]=u,l[n]=s,l[i]=o;}return l;}},t.clear=function(t,i){var n=t[i];n&&n[e]&&(t[i]=n[e]);},t;}),e("zrender/zrender",[pe,"./core/guid","./core/env","./core/util","./Handler","./Storage","./animation/Animation","./dom/HandlerProxy","./Painter"],function(t){

},f.dispose=function(t){if(t)t.dispose();else{for(var e in h){h.hasOwnProperty(e)&&h[e].dispose();}h={};}return f;},f.getInstance=function(t){return h[t];},f.registerPainter=function(t,e){c[t]=e;};var d=function d(t,e,i){i=i||{},this.dom=e,this.id=t;var h=this,f=new o(),d=i.renderer;if(u){if(!c.vml)throw new Error("You need to require 'zrender/vml/vml' to support IE8");d="vml";}else d&&c[d]||(d=N);

},f.addToMap=function(t){g.call(f,t),t.addSelfToZr(h);};};return d[j]={constructor:d,getId:function getId(){return this.id;},add:function add(t){this.storage.addRoot(t),this._needsRefresh=!0;},remove:function remove(t){this.storage.delRoot(t),this._needsRefresh=!0;},configLayer:function configLayer(t,e){this.painter.configLayer(t,e),this._needsRefresh=!0;},refreshImmediately:function refreshImmediately(){this._needsRefresh=!1,this.painter.refresh(),this._needsRefresh=!1;},refresh:function refresh(){this._needsRefresh=!0;},flush:function flush(){this._needsRefresh&&this.refreshImmediately(),this._needsRefreshHover&&this.refreshHoverImmediately();},addHover:function addHover(t,e){this.painter.addHover&&(this.painter.addHover(t,e),this.refreshHover());},removeHover:function removeHover(t){this.painter.removeHover&&(this.painter.removeHover(t),this.refreshHover());},clearHover:function clearHover(){this.painter.clearHover&&(this.painter.clearHover(),this.refreshHover());},refreshHover:function refreshHover(){this._needsRefreshHover=!0;},refreshHoverImmediately:function refreshHoverImmediately(){this._needsRefreshHover=!1,this.painter.refreshHover&&this.painter.refreshHover();},resize:function resize(t){t=t||{},this.painter.resize(t.width,t[re]),this.handler.resize();},clearAnimation:function clearAnimation(){this[B].clear();},getWidth:function getWidth(){return this.painter[ie]();},getHeight:function getHeight(){return this.painter[ee]();},pathToImage:function pathToImage(t,e,n){var r=i();return this.painter.pathToImage(r,t,e,n);},setCursorStyle:function setCursorStyle(t){this.handler.setCursorStyle(t);},on:function on(t,e,i){this.handler.on(t,e,i);},off:function off(t,e){this.handler.off(t,e);},trigger:function trigger(t,e){this.handler.trigger(t,e);},clear:function clear(){this.storage.delRoot(),this.painter.clear();},dispose:function dispose(){this[B].stop(),this.clear(),this.storage.dispose(),this.painter.dispose(),this.handler.dispose(),this[B]=this.storage=this.painter=this.handler=null,e(this.id);}},f;}),e("zrender/tool/color",[pe],function(){function t(t){return t=Math.round(t),0>t?0:t>255?255:t;}function e(t){return t=Math.round(t),0>t?0:t>360?360:t;}function i(t){return 0>t?0:t>1?1:t;}function n(e){return t(e[Z]&&"%"===e.charAt(e[Z]-1)?parseFloat(e)/100*255:parseInt(e,10));}function r(t){return i(t[Z]&&"%"===t.charAt(t[Z]-1)?parseFloat(t)/100:parseFloat(t));}function a(t,e,i){return 0>i?i+=1:i>1&&(i-=1),1>6*i?t+(e-t)*i*6:1>2*i?e:2>3*i?t+(e-t)*(2/3-i)*6:t;}function o(t,e,i){return t+(e-t)*i;}function s(t){if(t){t+="";var e=t[T](/ /g,"")[G]();

}}}}function l(e){var i=(parseFloat(e[0])%360+360)%360/360,n=r(e[1]),o=r(e[2]),s=.5>=o?o*(n+1):o+n-o*n,l=2*o-s,u=[t(255*a(l,s,i+1/3)),t(255*a(l,s,i)),t(255*a(l,s,i-1/3))];return 4===e[Z]&&(u[3]=e[3]),u;}function u(t){if(t){var e,i,n=t[0]/255,r=t[1]/255,a=t[2]/255,o=Math.min(n,r,a),s=Math.max(n,r,a),l=s-o,u=(s+o)/2;if(0===l)e=0,i=0;else{i=.5>u?l/(s+o):l/(2-s-o);var c=((s-n)/6+l/2)/l,h=((s-r)/6+l/2)/l,f=((s-a)/6+l/2)/l;n===s?e=f-h:r===s?e=1/3+c-f:a===s&&(e=2/3+h-c),0>e&&(e+=1),e>1&&(e-=1);}var d=[360*e,i,u];return null!=t[3]&&d.push(t[3]),d;}}function c(t,e){var i=s(t);if(i){for(var n=0;3>n;n++){i[n]=0>e?i[n]*(1-e)|0:(255-i[n])*e+i[n]|0;}return m(i,4===i[Z]?"rgba":"rgb");}}function h(t){var e=s(t);return e?((1<<24)+(e[0]<<16)+(e[1]<<8)+ +e[2]).toString(16).slice(1):void 0;}function f(e,i,n){if(i&&i[Z]&&e>=0&&1>=e){n=n||[0,0,0,0];

}}function p(t,i,n,a){return t=s(t),t?(t=u(t),null!=i&&(t[0]=e(i)),null!=n&&(t[1]=r(n)),null!=a&&(t[2]=r(a)),m(l(t),"rgba")):void 0;}function v(t,e){return t=s(t),t&&null!=e?(t[3]=i(e),m(t,"rgba")):void 0;}function m(t,e){var i=t[0]+","+t[1]+","+t[2];return("rgba"===e||"hsva"===e||"hsla"===e)&&(i+=","+t[3]),e+"("+i+")";}var g={transparent:[0,

this._$handlers={};};return e[j]={constructor:e,one:function one(t,e,i){var n=this._$handlers;if(!e||!t)return this;n[t]||(n[t]=[]);for(var r=0;r<n[t][Z];r++){if(n[t][r].h===e)return this;}return n[t].push({h:e,one:!0,ctx:i||this}),this;},on:function on(t,e,i){var n=this._$handlers;if(!e||!t)return this;n[t]||(n[t]=[]);for(var r=0;r<n[t][Z];r++){if(n[t][r].h===e)return this;}return n[t].push({h:e,one:!1,ctx:i||this}),this;},isSilent:function isSilent(t){var e=this._$handlers;return e[t]&&e[t][Z];},off:function off(t,e){var i=this._$handlers;if(!t)return this._$handlers={},this;if(e){if(i[t]){for(var n=[],r=0,a=i[t][Z];a>r;r++){i[t][r].h!=e&&n.push(i[t][r]);}i[t]=n;}i[t]&&0===i[t][Z]&&delete i[t];}else delete i[t];return this;},trigger:function trigger(e){

isSeriesFiltered(i)||((typeof a==="undefined"?"undefined":(0,_typeof3.default)(a))!==b||a instanceof e||r.each(function(t){r.setItemVisual(t,"color",a(i[S](t)));}),r.each(function(t){var e=r[o](t),i=e.get(n,!0);null!=i&&r.setItemVisual(t,"color",i);}));}t.eachRawSeries(i);};}),e("zrender/core/timsort",[],function(){function t(t){for(var e=0;t>=l;){e|=1&t,t>>=1;}return t+e;}function e(t,e,n,r){var a=e+1;if(a===n)return 1;if(r(t[a++],t[e])<0){for(;n>a&&r(t[a],t[a-1])<0;){a++;}i(t,e,a);}else for(;n>a&&r(t[a],t[a-1])>=0;){a++;}return a-e;}function i(t,e,i){for(i--;i>e;){var n=t[e];t[e++]=t[i],t[i--]=n;}}function n(t,e,i,n,r){for(n===e&&n++;i>n;n++){for(var a,o=t[n],s=e,l=n;l>s;){a=s+l>>>1,r(o,t[a])<0?l=a:s=a+1;}var u=n-s;switch(u){case 3:t[s+3]=t[s+2];case 2:t[s+2]=t[s+1];case 1:t[s+1]=t[s];break;default:for(;u>0;){t[s+u]=t[s+u-1],u--;}}t[s]=o;}}function r(t,e,i,n,r,a){var o=0,s=0,l=1;if(a(t,e[i+r])>0){for(s=n-r;s>l&&a(t,e[i+r+l])>0;){o=l,l=(l<<1)+1,0>=l&&(l=s);}l>s&&(l=s),o+=r,l+=r;}else{for(s=r+1;s>l&&a(t,e[i+r-l])<=0;){o=l,l=(l<<1)+1,0>=l&&(l=s);}l>s&&(l=s);var u=o;o=r-l,l=r-u;}for(o++;l>o;){var c=o+(l-o>>>1);a(t,e[i+c])>0?o=c+1:l=c;}return l;}function a(t,e,i,n,r,a){var o=0,s=0,l=1;if(a(t,e[i+r])<0){for(s=r+1;s>l&&a(t,e[i+r-l])<0;){o=l,l=(l<<1)+1,0>=l&&(l=s);}l>s&&(l=s);var u=o;o=r-l,l=r-u;}else{for(s=n-r;s>l&&a(t,e[i+r+l])>=0;){o=l,l=(l<<1)+1,0>=l&&(l=s);}l>s&&(l=s),o+=r,l+=r;}for(o++;l>o;){var c=o+(l-o>>>1);a(t,e[i+c])<0?l=c:o=c+1;}return l;}function o(t,e){function i(t,e){f[y]=t,d[y]=e,y+=1;}function n(){for(;y>1;){var t=y-2;if(t>=1&&d[t-1]<=d[t]+d[t+1]||t>=2&&d[t-2]<=d[t]+d[t-1])d[t-1]<d[t+1]&&t--;else if(d[t]>d[t+1])break;s(t);}}function o(){for(;y>1;){var t=y-2;t>0&&d[t-1]<d[t+1]&&t--,s(t);}}function s(i){var n=f[i],o=d[i],s=f[i+1],u=d[i+1];d[i]=o+u,i===y-3&&(f[i+1]=f[i+2],d[i+1]=d[i+2]),y--;var c=a(t[s],t,n,o,0,e);n+=c,o-=c,0!==o&&(u=r(t[n+o-1],t,s,u,u-1,e),0!==u&&(u>=o?l(n,o,s,u):h(n,o,s,u)));}function l(i,n,o,s){var l=0;for(l=0;n>l;l++){_[l]=t[i+l];}var c=0,h=o,f=i;if(t[f++]=t[h++],0!==--s){if(1===n){for(l=0;s>l;l++){t[f+l]=t[h+l];}return void(t[f+s]=_[c]);}for(var d,v,m,g=p;;){d=0,v=0,m=!1;do{if(e(t[h],_[c])<0){if(t[f++]=t[h++],v++,d=0,0===--s){m=!0;break;}}else if(t[f++]=_[c++],d++,v=0,1===--n){m=!0;break;}}while(g>(d|v));if(m)break;do{if(d=a(t[h],_,c,n,0,e),0!==d){for(l=0;d>l;l++){t[f+l]=_[c+l];}if(f+=d,c+=d,n-=d,1>=n){m=!0;break;}}if(t[f++]=t[h++],0===--s){m=!0;break;}if(v=r(_[c],t,h,s,0,e),0!==v){for(l=0;v>l;l++){t[f+l]=t[h+l];}if(f+=v,h+=v,s-=v,0===s){m=!0;break;}}if(t[f++]=_[c++],1===--n){m=!0;break;}g--;}while(d>=u||v>=u);if(m)break;0>g&&(g=0),g+=2;}if(p=g,1>p&&(p=1),1===n){for(l=0;s>l;l++){t[f+l]=t[h+l];}t[f+s]=_[c];}else{if(0===n)throw new Error();for(l=0;n>l;l++){t[f+l]=_[c+l];}}}else for(l=0;n>l;l++){t[f+l]=_[c+l];}}function h(i,n,o,s){var l=0;for(l=0;s>l;l++){_[l]=t[o+l];}var c=i+n-1,h=s-1,f=o+s-1,d=0,v=0;if(t[f--]=t[c--],0!==--n){if(1===s){for(f-=n,c-=n,v=f+1,d=c+1,l=n-1;l>=0;l--){t[v+l]=t[d+l];}return void(t[f]=_[h]);}for(var m=p;;){var g=0,y=0,x=!1;do{if(e(_[h],t[c])<0){if(t[f--]=t[c--],g++,y=0,0===--n){x=!0;break;}}else if(t[f--]=_[h--],y++,g=0,1===--s){x=!0;break;}}while(m>(g|y));if(x)break;do{if(g=n-a(_[h],t,i,n,n-1,e),0!==g){for(f-=g,c-=g,n-=g,v=f+1,d=c+1,l=g-1;l>=0;l--){t[v+l]=t[d+l];}if(0===n){x=!0;break;}}if(t[f--]=_[h--],1===--s){x=!0;break;}if(y=s-r(t[c],_,0,s,s-1,e),0!==y){for(f-=y,h-=y,s-=y,v=f+1,d=h+1,l=0;y>l;l++){t[v+l]=_[d+l];}if(1>=s){x=!0;break;}}if(t[f--]=t[c--],0===--n){x=!0;break;}m--;}while(g>=u||y>=u);if(x)break;0>m&&(m=0),m+=2;}if(p=m,1>p&&(p=1),1===s){for(f-=n,c-=n,v=f+1,d=c+1,l=n-1;l>=0;l--){t[v+l]=t[d+l];}t[f]=_[h];}else{if(0===s)throw new Error();for(d=f-(s-1),l=0;s>l;l++){t[d+l]=_[l];}}}else for(d=f-(s-1),l=0;s>l;l++){t[d+l]=_[l];}}var f,d,p=u,v=0,m=c,g=0,y=0;v=t[Z],2*c>v&&(m=v>>>1);var _=[];g=120>v?5:1542>v?10:119151>v?19:40,f=[],d=[],this.mergeRuns=n,this.forceMergeRuns=o,this.pushRun=i;}function s(i,r,a,s){a||(a=0),s||(s=i[Z]);var u=s-a;if(!(2>u)){var c=0;if(l>u)return c=e(i,a,s,r),void n(i,a,s,a+c,r);var h=new o(i,r),f=t(u);do{if(c=e(i,a,s,r),f>c){var d=u;d>f&&(d=f),n(i,a,a+d,a+c,r),c=d;}h.pushRun(a,c),h.mergeRuns(),u-=c,a+=c;}while(0!==u);h.forceMergeRuns();}}var l=32,u=7,c=256;return s;}),e("echarts/preprocessor/backwardCompat",[pe,de,"./helper/compatStyle"],function(t){function e(t,e){e=e.split(",");for(var i=t,n=0;n<e[Z]&&(i=i&&i[e[n]],null!=i);n++){}return i;}function i(t,e,i,n){e=e.split(",");for(var r,a=t,o=0;o<e[Z]-1;o++){r=e[o],null==a[r]&&(a[r]={}),a=a[r];}(n||null==a[e[o]])&&(a[e[o]]=i);}function n(t){u(o,function(e){e[0]in t&&!(e[1]in t)&&(t[e[1]]=t[e[0]]);});}var r

}for(var u=0;u<l[Z];u++){if(l[u]===t.type){n(t);break;}}}}),t.dataRange&&(t.visualMap=t.dataRange),u(s,function(e){var i=t[e];i&&(r[w](i)||(i=[i]),u(i,function(t){n(t);}));});};}),e("echarts/data/List",[pe,"../model/Model","./DataDiffer",de,"../util/model"],function(t){

o++){var s=i[o],u=a[s];r[s]=g[O](e,s)>=0?new u.constructor(a[s][Z]):a[s];}return n;}var u=i,c=(typeof window==="undefined"?"undefined":(0,_typeof3.default)(window))===i?global:window,f=(0,_typeof3.default)(c.Float64Array)===u?Array:c.Float64Array,d=(0,_typeof3.default)(c.Int32Array)===u?Array:c.Int32Array,p={

}),t.__wrappedMethods=e.__wrappedMethods;},S=function S(t,e){t=t||["x","y"];for(var i={},n=[],r=0;r<t[Z];r++){var a,o={};(0,_typeof3.default)(t[r])===F?(a=t[r],o={

},C.getDimensionInfo=function(t){return g.clone(this._dimensionInfos[this.getDimension(t)]);},C.initData=function(t,e,i){t=t||[],this._rawData=t;var n=this._storage={},r=this.indices=[],a=this[l],o=t[Z],s=this._dimensionInfos,u=[],c={};e=e||[];for(var h=0;h<a[Z];h++){var f=s[a[h]],d=p[f.type];n[a[h]]=new d(o);}var v=this;

!T&&w&&(c[w]=c[w]||0,T=w,c[w]>0&&(T+="__ec__"+c[w]),c[w]++),T&&(u[h]=T);}this._nameList=e,this._idList=u;},C.count=function(){return this.indices[Z];},C.get=function(t,e,i){var n=this._storage,r=this.indices[e];if(null==r)return 0/0;var a=n[t]&&n[t][r];if(i){var o=this._dimensionInfos[t];if(o&&o.stackable)for(var s=this.stackedOn;s;){var l=s.get(t,e);(a>=0&&l>0||0>=a&&0>l)&&(a+=l),s=s.stackedOn;}}return a;},C.getValues=function(t,e,i){var n=[];g[w](t)||(i=e,e=t,t=this[l]);for(var r=0,a=t[Z];a>r;r++){n.push(this.get(t[r],e,i));}return n;},C.hasValue=function(t){for(var e=this[l],i=this._dimensionInfos,n=0,r=e[Z];r>n;n++){if(i[e[n]].type!==a&&isNaN(this.get(e[n],t)))return!1;}return!0;},C.getDataExtent=function(t,e,i){t=this.getDimension(t);var n=this._storage[t],r=this.getDimensionInfo(t);e=r&&r.stackable&&e;var a,o=(this[W]||(this[W]={}))[t+!!e];if(o)return o;if(n){for(var s=1/0,l=-1/0,u=0,c=this.count();c>u;u++){a=this.get(t,u,e),(!i||i(a,t,u))&&(s>a&&(s=a),a>l&&(l=a));}return this[W][t+!!e]=[s,l];}return[1/0,-1/0];}

o>a;a++){var s=r[a];if(n[s]===e)return a;}return-1;},C.indexOfName=function(t){for(var e=this.indices,i=this._nameList,n=0,r=e[Z];r>n;n++){var a=e[n];if(i[a]===t)return n;}return-1;},C.indexOfRawIndex=function(t){var e=this.indices,i=e[t];if(null!=i&&i===t)return t;for(var n=0,r=e[Z]-1;r>=n;){var a=(n+r)/2|0;if(e[a]<t)n=a+1;else{if(!(e[a]>t))return a;r=a-1;}}return-1;},C.indexOfNearest=function(t,e,i,n){var r=this._storage,a=r[t];null==n&&(n=1/0);var o=-1;if(a)for(var s=Number.MAX_VALUE,l=0,u=this.count();u>l;l++){var c=e-this.get(t,l,i),h=Math.abs(c);n>=c&&(s>h||h===s&&c>0)&&(s=h,o=l);}return o;},C.getRawIndex=function(t){var e=this.indices[t];return null==e?-1:e;},C.getRawDataItem=function(t){return this._rawData[this.getRawIndex(t)];},C[s]=function(t){return this._nameList[this.indices[t]]||"";},C.getId=function(t){return this._idList[this.indices[t]]||this.getRawIndex(t)+"";},C.each=function(t,i,n,r){(typeof t==="undefined"?"undefined":(0,_typeof3.default)(t))===b&&(r=n,n=i,i=t,t=[]),t=g.map(e(t),this.getDimension,this);

u++){a[u]=this.get(t[u],l,n);}a[u]=l,i.apply(r,a);}}},C.filterSelf=function(t,i,n,r){(typeof t==="undefined"?"undefined":(0,_typeof3.default)(t))===b&&(r=n,n=i,i=t,t=[]),t=g.map(e(t),this.getDimension,this);var a=[],o=[],s=t[Z],l=this.indices;r=r||this;for(var u=0;u<l[Z];u++){var c;if(1===s)c=i.call(r,this.get(t[0],u,n),u);else{for(var h=0;s>h;h++){o[h]=this.get(t[h],u,n);}o[h]=u,c=i.apply(r,o);}c&&a.push(l[u]);}return this.indices=a,this[W]={},this;},C.mapArray=function(t,e,i,n){(typeof t==="undefined"?"undefined":(0,_typeof3.default)(t))===b&&(n=i,i=e,e=t,t=[]);

if(null!=n){(typeof n==="undefined"?"undefined":(0,_typeof3.default)(n))===x&&(u[0]=n,n=u);for(var r=0;r<n[Z];r++){var a=t[r],o=l[a],c=s[e];o&&(o[c]=n[r]);}}},r,a),o;},C.downSample=function(t,e,i,r){for(var a=n(this,[t]),o=this._storage,s=a._storage,l=this.indices,u=a.indices=[],c=[],h=[],f=Math.floor(1/e),d=s[t],p=this.count(),v=0;v<o[t][Z];v++){s[t][v]=o[t][v];}for(var v=0;p>v;v+=f){f>p-v&&(f=p-v,c[Z]=f);for(var m=0;f>m;m++){var g=l[v+m];c[m]=d[g],h[m]=g;}var y=i(c),g=h[r(c,y)||0];d[g]=y,u.push(g);}return a;},C[o]=function(t){var e=this.hostModel;return t=this.indices[t],new v(this._rawData[t],e,e&&e[r]);},C.diff=function(t){var e,i=this._idList,n=t&&t._idList,r="e\x00\x00";return new m(t?t.indices:[],this.indices,function(t){return null!=(e=n[t])?e:r+t;},function(t){return null!=(e=i[t])?e:r+t;});},C.getVisual=function(t){var e=this._visual;return e&&e[t];},C.setVisual=function(t,e){if(_(t))for(var i in t){t.hasOwnProperty(i)&&this.setVisual(i,t[i]);}else this._visual=this._visual||{},this._visual[t]=e;},C.setLayout=function(t,e){if(_(t))for(var i in t){t.hasOwnProperty(i)&&this.setLayout(i,t[i]);}else this._layout[t]=e;},C.getLayout=function(t){return this._layout[t];},C.getItemLayout=function(t){return this._itemLayouts[t];},C.setItemLayout=function(t,e,i){this._itemLayouts[t]=i?g[X](this._itemLayouts[t]||{},e):e;},C.clearItemLayouts=function(){this._itemLayouts[Z]=0;},C[k]=function(t,e,i){var n=this._itemVisuals[t],r=n&&n[e];return null!=r||i?r:this.getVisual(e);},C.setItemVisual=function(t,e,i){var n=this._itemVisuals[t]||{};if(this._itemVisuals[t]=n,_(e))for(var r in e){e.hasOwnProperty(r)&&(n[r]=e[r]);}else n[e]=i;},C.clearAllVisual=function(){this._visual={},this._itemVisuals=[];};var A=function A(t){t.seriesIndex=this.seriesIndex,t[z]=this[z],t.dataType=this.dataType;};return C.setItemGraphicEl=function(t,e){var i=this.hostModel;e&&(e[z]=t,e.dataType=this.dataType,e.seriesIndex=i&&i.seriesIndex,"group"===e.type&&e.traverse(A,e)),this._graphicEls[t]=e;},C.getItemGraphicEl=function(t){return this._graphicEls[t];},C.eachItemGraphicEl=function(t,e){g.each(this._graphicEls,function(i,n){i&&t&&t.call(e,i,n);});},C.cloneShallow=function(){var t=g.map(this[l],this.getDimensionInfo,this),e=new S(t,this.hostModel);return e._storage=this._storage,M(e,this),e.indices=this.indices.slice(),this[W]&&(e[W]=g[X]({},this[W])),e;},C.wrapMethod=function(t,e){var i=this[t];(typeof i==="undefined"?"undefined":(0,_typeof3.default)(i))===b&&(this.__wrappedMethods=this.__wrappedMethods||[],this.__wrappedMethods.push(t),this[t]=function(){var t=i.apply(this,arguments);return e.apply(this,[t][h](g.slice(arguments)));});},C.TRANSFERABLE_METHODS=["cloneShallow","downSample","map"],C.CHANGABLE_METHODS=["filterSelf"],S;}),e("echarts/loading/default",[pe,"../util/graphic",de],function(t){

return l.add(o),l.add(s),l.add(a),l.resize=function(){var e=t[ie]()/2,i=t[ee]()/2;o.setShape({cx:e,cy:i});var n=o.shape.r;s.setShape({x:e-n,y:i-n,width:2*n,height:2*n}),a.setShape({x:0,y:0,width:t[ie](),height:t[ee]()});},l.resize(),l;};}),e("echarts/model/Model",[pe,de,"../util/clazz",q,"./mixin/lineStyle","./mixin/areaStyle","./mixin/textStyle","./mixin/itemStyle"],function(t){function e(t,e,i){this.parentModel=e,this[r]=i,this[c]=t;}function i(t,e,i){for(var n=0;n<e[Z]&&(!e[n]||(t=t&&"object"==(typeof t==="undefined"?"undefined":(0,_typeof3.default)(t))?t[e[n]]:null,null!=t));n++){}return null==t&&i&&(t=i.get(e)),t;}function n(t,e){var i=o.get(t,"getParent");return i?i.call(t,e):t.parentModel;}var a=t(de),o=t("../util/clazz"),s=t(q);e[j]={constructor:e,init:null,mergeOption:function mergeOption(t){a.merge(this[c],t,!0);},get:function get(t,e){

},isEmpty:function isEmpty(){return null==this[c];},restoreData:function restoreData(){},clone:function clone(){var t=this.constructor;return new t(a.clone(this[c]));},setReadOnly:function setReadOnly(t){o.setReadOnly(this,t);},parsePath:function parsePath(t){return(typeof t==="undefined"?"undefined":(0,_typeof3.default)(t))===F&&(t=t.split(".")),t;},customizeGetParent:function customizeGetParent(t){o.set(this,"getParent",t);},isAnimationEnabled:function isAnimationEnabled(){if(!s.node){if(null!=this[c][B])return!!this[c][B];if(this.parentModel)return this.parentModel.isAnimationEnabled();}}},o.enableClassExtend(e);var l=a.mixin;return l(e,t("./mixin/lineStyle")),l(e,t("./mixin/areaStyle")),l(e,t("./mixin/textStyle")),l(e,t("./mixin/itemStyle")),e;}),e("zrender/core/matrix",[],function(){var t=(typeof Float32Array==="undefined"?"undefined":(0,_typeof3.default)(Float32Array))===i?Array:Float32Array,e={create:function create(){var i=new t(6);return e.identity(i),i;},identity:function identity(t){return t[0]=1,t[1]=0,t[2]=0,t[3]=1,t[4]=0,t[5]=0,t;},copy:function copy(t,e){return t[0]=e[0],t[1]=e[1],t[2]=e[2],t[3]=e[3],t[4]=e[4],t[5]=e[5],t;},mul:function mul(t,e,i){var n=e[0]*i[0]+e[2]*i[1],r=e[1]*i[0]+e[3]*i[1],a=e[0]*i[2]+e[2]*i[3],o=e[1]*i[2]+e[3]*i[3],s=e[0]*i[4]+e[2]*i[5]+e[4],l=e[1]*i[4]+e[3]*i[5]+e[5];return t[0]=n,t[1]=r,t[2]=a,t[3]=o,t[4]=s,t[5]=l,t;},translate:function translate(t,e,i){return t[0]=e[0],t[1]=e[1],t[2]=e[2],t[3]=e[3],t[4]=e[4]+i[0],t[5]=e[5]+i[1],t;},rotate:function rotate(t,e,i){var n=e[0],r=e[2],a=e[4],o=e[1],s=e[3],l=e[5],u=Math.sin(i),c=Math.cos(i);return t[0]=n*c+o*u,t[1]=-n*u+o*c,t[2]=r*c+s*u,t[3]=-r*u+c*s,t[4]=c*a+u*l,t[5]=c*l-u*a,t;},scale:function scale(t,e,i){var n=i[0],r=i[1];return t[0]=e[0]*n,t[1]=e[1]*r,t[2]=e[2]*n,t[3]=e[3]*r,t[4]=e[4]*n,t[5]=e[5]*r,t;},invert:function invert(t,e){var i=e[0],n=e[2],r=e[4],a=e[1],o=e[3],s=e[5],l=i*o-a*n;return l?(l=1/l,t[0]=o*l,t[1]=-a*l,t[2]=-n*l,t[3]=i*l,t[4]=(n*s-o*r)*l,t[5]=(a*r-i*s)*l,t):null;}};return e;}),e("echarts/scale/Scale",[pe,"../util/clazz"],function(t){function e(){this[W]=[1/0,-1/0],this._interval=0,this.init&&this.init.apply(this,arguments);}var i=t("../util/clazz"),n=e[j];return n.parse=function(t){return t;},n[U]=function(t){var e=this[W];return t>=e[0]&&t<=e[1];},n[H]=function(t){var e=this[W];return e[1]===e[0]?.5:(t-e[0])/(e[1]-e[0]);},n.scale=function(t){var e=this[W];return t*(e[1]-e[0])+e[0];},n.unionExtent=function(t){var e=this[W];t[0]<e[0]&&(e[0]=t[0]),t[1]>e[1]&&(e[1]=t[1]);},n.unionExtentFromData=function(t,e){this.unionExtent(t.getDataExtent(e,!0));},n[y]=function(){return this[W].slice();},n.setExtent=function(t,e){var i=this[W];isNaN(t)||(i[0]=t),isNaN(e)||(i[1]=e);},n.getTicksLabels=function(){for(var t=[],e=this.getTicks(),i=0;i<e[Z];i++){t.push(this.getLabel(e[i]));}return t;},i.enableClassExtend(e),i.enableClassManagement(e,{registerWhenExtend:!0}),e;}),e("zrender/core/vector",[],function(){var t=(typeof Float32Array==="undefined"?"undefined":(0,_typeof3.default)(Float32Array))===i?Array:Float32Array,e={create:function create(e,i){var n=new t(2);return null==e&&(e=0),null==i&&(i=0),n[0]=e,n[1]=i,n;},copy:function copy(t,e){return t[0]=e[0],t[1]=e[1],t;},clone:function clone(e){var i=new t(2);return i[0]=e[0],i[1]=e[1],i;},set:function set(t,e,i){return t[0]=e,t[1]=i,t;},add:function add(t,e,i){return t[0]=e[0]+i[0],t[1]=e[1]+i[1],t;},scaleAndAdd:function scaleAndAdd(t,e,i,n){return t[0]=e[0]+i[0]*n,t[1]=e[1]+i[1]*n,t;},sub:function sub(t,e,i){return t[0]=e[0]-i[0],t[1]=e[1]-i[1],t;},len:function len(t){return Math.sqrt(this.lenSquare(t));},lenSquare:function lenSquare(t){return t[0]*t[0]+t[1]*t[1];},mul:function mul(t,e,i){return t[0]=e[0]*i[0],t[1]=e[1]*i[1],t;},div:function div(t,e,i){return t[0]=e[0]/i[0],t[1]=e[1]/i[1],t;},dot:function dot(t,e){return t[0]*e[0]+t[1]*e[1];},scale:function scale(t,e,i){return t[0]=e[0]*i,t[1]=e[1]*i,t;},normalize:function normalize(t,i){var n=e.len(i);return 0===n?(t[0]=0,t[1]=0):(t[0]=i[0]/n,t[1]=i[1]/n),t;},distance:function distance(t,e){return Math.sqrt((t[0]-e[0])*(t[0]-e[0])+(t[1]-e[1])*(t[1]-e[1]));},distanceSquare:function distanceSquare(t,e){return(t[0]-e[0])*(t[0]-e[0])+(t[1]-e[1])*(t[1]-e[1]);},negate:function negate(t,e){return t[0]=-e[0],t[1]=-e[1],t;},lerp:function lerp(t,e,i,n){return t[0]=e[0]+n*(i[0]-e[0]),t[1]=e[1]+n*(i[1]-e[1]),t;},applyTransform:function applyTransform(t,e,i){var n=e[0],r=e[1];return t[0]=i[0]*n+i[2]*r+i[4],t[1]=i[1]*n+i[3]*r+i[5],t;},min:function min(t,e,i){return t[0]=Math.min(e[0],i[0]),t[1]=Math.min(e[1],i[1]),t;},max:function max(t,e,i){

var e=t("../core/util"),i=t("../Element"),n=t("../core/BoundingRect"),r=function r(t){t=t||{},i.call(this,t);for(var e in t){t.hasOwnProperty(e)&&(this[e]=t[e]);}this._children=[],this.__storage=null,this.__dirty=!0;};return r[j]={constructor:r,isGroup:!0,type:"group",silent:!1,children:function children(){return this._children.slice();},childAt:function childAt(t){return this._children[t];},childOfName:function childOfName(t){for(var e=this._children,i=0;i<e[Z];i++){if(e[i].name===t)return e[i];}},childCount:function childCount(){return this._children[Z];},add:function add(t){return t&&t!==this&&t.parent!==this&&(this._children.push(t),this._doAdd(t)),this;},addBefore:function addBefore(t,e){if(t&&t!==this&&t.parent!==this&&e&&e.parent===this){var i=this._children,n=i[O](e);n>=0&&(i[A](n,0,t),this._doAdd(t));}return this;},_doAdd:function _doAdd(t){

}return i[Z]=0,this;},eachChild:function eachChild(t,e){for(var i=this._children,n=0;n<i[Z];n++){var r=i[n];t.call(e,r,n);}return this;},traverse:function traverse(t,e){for(var i=0;i<this._children[Z];i++){var n=this._children[i];t.call(e,n),"group"===n.type&&n.traverse(t,e);}return this;},addChildrenToStorage:function addChildrenToStorage(t){for(var e=0;e<this._children[Z];e++){var i=this._children[e];t.addToMap(i),i instanceof r&&i.addChildrenToStorage(t);}},delChildrenFromStorage:function delChildrenFromStorage(t){for(var e=0;e<this._children[Z];e++){var i=this._children[e];t.delFromMap(i.id),i instanceof r&&i.delChildrenFromStorage(t);}},dirty:function dirty(){return this.__dirty=!0,this.__zr&&this.__zr.refresh(),this;},getBoundingRect:function getBoundingRect(t){for(var e=null,i=new n(0,0,0,0),r=t||this._children,a=[],o=0;

break;case"a":x=m[v++],b=m[v++],w=m[v++],M=m[v++],S=m[v++],C=s,A=u,s+=m[v++],u+=m[v++],f=h.A,e(C,A,s,u,M,S,x,b,w,f,c);}}("z"===p||"Z"===p)&&(f=h.Z,c.addData(f)),r=f;}return c.toStatic(),c;}function n(t,e){var n,r=i(t);return e=e||{},e.buildPath=function(t){t.setData(r.data),n&&o(t,n);var e=t[f]();e&&t.rebuildPath(e);},e[g]=function(t){n||(n=s[V]()),s.mul(n,t,n),this.dirty(!0);},e;}var r=t("../graphic/Path"),a=t("../core/PathProxy"),o=t("./transformPath"),s=t("../core/matrix"),l=["m","M","l","L","v","V","h","H","z","Z","c","C","q","Q","t","T","s","S","a","A"],u=Math.sqrt,c=Math.sin,h=Math.cos,d=Math.PI,p=function p(t){return Math.sqrt(t[0]*t[0]+t[1]*t[1]);},v=function v(t,e){return(t[0]*e[0]+t[1]*e[1])/(p(t)*p(e));},m=function m(t,e){return(t[0]*e[1]<t[1]*e[0]?-1:1)*Math.acos(v(t,e));};return{createFromString:function createFromString(t,e){return new r(n(t,e));},extendFromString:function extendFromString(t,e){return r[X](n(t,e));},mergePath:function mergePath(t,e){for(var i=[],n=t[Z],a=0;n>a;a++){var o=t[a];o.__dirty&&o.buildPath(o.path,o.shape,!0),i.push(o.path);}var s=new r(e);return s.buildPath=function(t){t.appendPath(i);var e=t[f]();e&&t.rebuildPath(e);},s;}};}),e("zrender/graphic/Path",[pe,"./Displayable","../core/util","../core/PathProxy","../contain/path","./Pattern"],function(t){function e(t){i.call(this,t),this.path=new r();}

}if(r.hasFill())return a[U](o,t,e);}return!1;},dirty:function dirty(t){null==t&&(t=!0),t&&(this.__dirtyPath=t,this._rect=null),this.__dirty=!0,this.__zr&&this.__zr.refresh(),this.__clipTarget&&this.__clipTarget.dirty();},animateShape:function animateShape(t){return this.animate("shape",t);},attrKV:function attrKV(t,e){"shape"===t?(this.setShape(e),this.__dirtyPath=!0,this._rect=null):i[j].attrKV.call(this,t,e);},setShape:function setShape(t,e){var i=this.shape;if(i){if(n[R](t))for(var r in t){t.hasOwnProperty(r)&&(i[r]=t[r]);}else i[t]=e;this.dirty(!0);}return this;},getLineScale:function getLineScale(){var t=this.transform;return t&&l(t[0]-1)>1e-10&&l(t[3]-1)>1e-10?Math.sqrt(l(t[0]*t[3]-t[2]*t[1])):1;}},e[X]=function(t){var i=function i(_i){e.call(this,_i),t.style&&this.style.extendFrom(t.style,!1);var n=t.shape;if(n){this.shape=this.shape||{};var r=this.shape;for(var a in n){!r.hasOwnProperty(a)&&n.hasOwnProperty(a)&&(r[a]=n[a]);}}t.init&&t.init.call(this,_i);};n[_](i,e);for(var r in t){"style"!==r&&"shape"!==r&&(i[j][r]=t[r]);}return i;},n[_](e,i),e;}),e("zrender/graphic/Image",[pe,"./Displayable","../core/BoundingRect","../core/util","../core/LRU"],function(t){function e(t){i.call(this,t);}var i=t("./Displayable"),n=t("../core/BoundingRect"),r=t("../core/util"),a=t("../core/LRU"),o=new a(50);return e[j]={constructor:e,type:"image",brush:function brush(t,e){var i,n=this.style,r=n.image;if(n.bind(t,this,e),i=(typeof r==="undefined"?"undefined":(0,_typeof3.default)(r))===F?this._image:r,!i&&r){var a=o.get(r);if(!a)return i=new Image(),i.onload=function(){i.onload=null;for(var t=0;t<a.pending[Z];t++){a.pending[t].dirty();}},a={image:i,pending:[this]},i.src=r,o.put(r,a),void(this._image=i);if(i=a.image,this._image=i,!i.width||!i[re])return void a.pending.push(this);}if(i){var s=n.width||i.width,l=n[re]||i[re],u=n.x||0,c=n.y||0;if(!i.width||!i[re])return;if(this.setTransform(t),n.sWidth&&n.sHeight){var h=n.sx||0,f=n.sy||0;t.drawImage(i,h,f,n.sWidth,n.sHeight,u,c,s,l);}else if(n.sx&&n.sy){var h=n.sx,f=n.sy,d=s-h,p=l-f;t.drawImage(i,h,f,d,p,u,c,s,l);}else t.drawImage(i,u,c,s,l);null==n.width&&(n.width=s),null==n[re]&&(n[re]=l),this.restoreTransform(t),null!=n.text&&this.drawRectText(t,this[ae]());}},getBoundingRect:function getBoundingRect(){var t=this.style;return this._rect||(this._rect=new n(t.x||0,t.y||0,t.width||0,t[re]||0)),this._rect;}},r[_](e,i),e;}),e("zrender/graphic/Text",[pe,"./Displayable","../core/util","../contain/text"],function(t){var e=t("./Displayable"),i=t("../core/util"),n=t("../contain/text"),r=function r(t){e.call(this,t);};return r[j]={constructor:r,type:"text",brush:function brush(t,e){

}this.restoreTransform(t);}},getBoundingRect:function getBoundingRect(){if(!this._rect){var t=this.style,e=t.textVerticalAlign,i=n[ae](t.text+"",t.textFont||t.font,t[le],e?"top":t.textBaseline);switch(e){case te:i.y-=i[re]/2;break;case K:i.y-=i[re];}i.x+=t.x||0,i.y+=t.y||0,this._rect=i;}return this._rect;}},i[_](r,e),r;}),e("zrender/graphic/shape/Circle",[pe,"../Path"],function(t){return t("../Path")[X]({type:"circle",shape:{cx:0,cy:0,r:0},buildPath:function buildPath(t,e,i){i&&t[n](e.cx+e.r,e.cy),t.arc(e.cx,e.cy,e.r,0,2*Math.PI,!0);}});}),e("zrender/graphic/shape/Sector",[pe,"../../core/env","../Path"],function(t){var e=t("../../core/env"),i=t("../Path"),r=[["shadowBlur",0],["shadowColor","#000"],["shadowOffsetX",0],["shadowOffsetY",0]];

s++){r[s][2]=n[r[s][0]],n[r[s][0]]=r[s][1];}t=!0;break;}}if(i[j].brush.apply(this,arguments),t)for(var s=0;s<r[Z];s++){n[r[s][0]]=r[s][2];}}:i[j].brush,buildPath:function buildPath(t,e){var i=e.cx,r=e.cy,a=Math.max(e.r0||0,0),o=Math.max(e.r,0),s=e.startAngle,l=e.endAngle,u=e.clockwise,c=Math.cos(s),h=Math.sin(s);

}});}),e("zrender/graphic/shape/Ring",[pe,"../Path"],function(t){return t("../Path")[X]({type:"ring",shape:{cx:0,cy:0,r:0,r0:0},buildPath:function buildPath(t,e){var i=e.cx,r=e.cy,a=2*Math.PI;t[n](i+e.r,r),t.arc(i,r,e.r,0,a,!1),t[n](i+e.r0,r),t.arc(i,r,e.r0,0,a,!0);}});}),e("zrender/graphic/shape/Polygon",[pe,"../helper/poly","../Path"],function(t){var e=t("../helper/poly");return t("../Path")[X]({type:"polygon",shape:{points:null,smooth:!1,smoothConstraint:null},buildPath:function buildPath(t,i){e.buildPath(t,i,!0);}});}),e("zrender/graphic/shape/Polyline",[pe,"../helper/poly","../Path"],function(t){var e=t("../helper/poly");return t("../Path")[X]({type:"polyline",shape:{points:null,smooth:!1,smoothConstraint:null},style:{stroke:"#000",fill:null},buildPath:function buildPath(t,i){e.buildPath(t,i,!1);}});}),e("zrender/graphic/shape/Rect",[pe,"../helper/roundRect","../Path"],function(t){var e=t("../helper/roundRect");return t("../Path")[X]({type:"rect",shape:{r:0,x:0,y:0,width:0,height:0},buildPath:function buildPath(t,i){var n=i.x,r=i.y,a=i.width,o=i[re];i.r?e.buildPath(t,i):t.rect(n,r,a,o),t.closePath();}});}),e("zrender/graphic/shape/BezierCurve",[pe,"../../core/curve","../../core/vector","../Path"],function(t){function e(t,e,i){var n=t.cpx2,r=t.cpy2;return null===n||null===r?[(i?c:l)(t.x1,t.cpx1,t.cpx2,t.x2,e),(i?c:l)(t.y1,t.cpy1,t.cpy2,t.y2,e)]:[(i?u:s)(t.x1,t.cpx1,t.x2,e),(i?u:s)(t.y1,t.cpy1,t.y2,e)];}var i=t("../../core/curve"),r=t("../../core/vector"

var i=e.x1,r=e.y1,s=e.x2,l=e.y2,u=e.cpx1,c=e.cpy1,f=e.cpx2,d=e.cpy2,p=e.percent;0!==p&&(t[n](i,r),null==f||null==d?(1>p&&(a(i,u,s,p,h),u=h[1],s=h[2],a(r,c,l,p,h),c=h[1],l=h[2]),t.quadraticCurveTo(u,c,s,l)):(1>p&&(o(i,u,f,s,p,h),u=h[1],f=h[2],s=h[3],o(r,c,d,l,p,h),c=h[1],d=h[2],l=h[3]),t.bezierCurveTo(u,c,f,d,s,l)));},pointAt:function pointAt(t){return e(this.shape,t,!1);},tangentAt:function tangentAt(t){var i=e(this.shape,t,!0);return r[H](i,i);}});}),e("zrender/graphic/shape/Line",[pe,"../Path"],function(t){return t("../Path")[X]({type:"line",shape:{x1:0,y1:0,x2:0,y2:0,percent:1},style:{stroke:"#000",fill:null},buildPath:function buildPath(t,e){var i=e.x1,r=e.y1,a=e.x2,o=e.y2,s=e.percent;0!==s&&(t[n](i,r),1>s&&(a=i*(1-s)+a*s,o=r*(1-s)+o*s),t.lineTo(a,o));},pointAt:function pointAt(t){var e=this.shape;return[e.x1*(1-t)+e.x2*t,e.y1*(1-t)+e.y2*t];}});}),e("zrender/graphic/CompoundPath",[pe,"./Path"],function(t){var e=t("./Path");return e[X]({type:"compound",shape:{paths:null},_updatePathDirty:function _updatePathDirty(){for(var t=this.__dirtyPath,e=this.shape.paths,i=0;i<e[Z];i++){t=t||e[i].__dirtyPath;}this.__dirtyPath=t,this.__dirty=this.__dirty||t;},beforeBrush:function beforeBrush(){this._updatePathDirty();for(var t=this.shape.paths||[],e=this.getGlobalScale(),i=0;i<t[Z];i++){t[i].path.setScale(e[0],e[1]);}},buildPath:function buildPath(t,e){for(var i=e.paths||[],n=0;n<i[Z];n++){i[n].buildPath(t,i[n].shape,!0);}},afterBrush:function afterBrush(){for(var t=this.shape.paths,e=0;e<t[Z];e++){t[e].__dirtyPath=!1;}},getBoundingRect:function getBoundingRect(){return this._updatePathDirty(),e[j][ae].call(this);}});}),e("zrender/graphic/shape/Arc",[pe,"../Path"],function(t){return t("../Path")[X]({type:"arc",shape:{cx:0,cy:0,r:0,startAngle:0,endAngle:2*Math.PI,clockwise:!0},style:{stroke:"#000",fill:null},buildPath:function buildPath(t,e){var i=e.cx,r=e.cy,a=Math.max(e.r,0),o=e.startAngle,s=e.endAngle,l=e.clockwise,u=Math.cos(o),c=Math.sin(o);t[n](u*a+i,c*a+r),t.arc(i,r,a,o,s,!l);}});}

}};}(),calculateTransform:function calculateTransform(t){var e=this,i=t.width/e.width,r=t[re]/e[re],a=n[V]();return n.translate(a,a,[-e.x,-e.y]),n.scale(a,a,[i,r]),n.translate(a,a,[t.x,t.y]),a;},intersect:function intersect(t){if(!t)return!1;t instanceof e||(t=e[V](t));var i=this,n=i.x,r=i.x+i.width,a=i.y,o=i.y+i[re],s=t.x,l=t.x+t.width,u=t.y,c=t.y+t[re];return!(s>r||n>l||u>o||a>c);},contain:function contain(t,e){var i=this;return t>=i.x&&t<=i.x+i.width&&e>=i.y&&e<=i.y+i[re];},clone:function clone(){return new e(this.x,this.y,this.width,this[re]);},copy:function copy(t){this.x=t.x,this.y=t.y,this.width=t.width,this[re]=t[re];},plain:function plain(){return{x:this.x,y:this.y,width:this.width,height:this[re]};}},e[V]=function(t){return new e(t.x,t.y,t.width,t[re]);},e;}),e("zrender/graphic/RadialGradient",[pe,"../core/util","./Gradient"],function(t){var e=t("../core/util"),i=t("./Gradient"),n=function n(t,e,_n2,r,a){this.x=null==t?.5:t,this.y=null==e?.5:e,this.r=null==_n2?.5:_n2,this.type="radial",this.global=a||!1,i.call(this,r);};return n[j]={constructor:n},e[_](n,i),n;}),e("echarts/model/globalDefault",[],function(){var t="";return(typeof navigator==="undefined"?"undefined":(0,_typeof3.default)(navigator))!==i&&(t=navigator.platform||""),{

!0,animationDuration:1e3,animationDurationUpdate:300,animationEasing:"exponentialOut",animationEasingUpdate:"cubicOut",animationThreshold:2e3,progressiveThreshold:3e3,progressive:400,hoverLayerThreshold:3e3};}),e("zrender/contain/text",[pe,"../core/util","../core/BoundingRect"],function(t){function e(t,e){var i=t+":"+e;if(o[i])return o[i];for(var n=(t+"").split("\n"),r=0,a=0,u=n[Z];u>a;a++){r=Math.max(v.measureText(n[a],e).width,r);}return s>l&&(s=0,o={}),s++,o[i]=r,r;}function i(t,i,n,r){var a=((t||"")+"").split("\n")[Z],o=e(t,i),s=e("国",i),l=a*s,u=new h(0,0,o,l);

}return{x:r,y:a,textAlign:c,textBaseline:"top"};}function r(t,i,n,r,o){if(!i)return"";o=o||{},r=d(r,"...");for(var s=d(o.maxIterations,2),l=d(o.minChar,0),u=e("国",n),c=e("a",n),h=d(o.placeholder,""),f=i=Math.max(0,i-1),p=0;l>p&&f>=c;p++){f-=c;}var v=e(r);v>f&&(r="",v=0),f=i-v;for(var m=(t+"").split("\n"),p=0,g=m[Z];g>p;p++){var y=m[p],_=e(y,n);if(!(i>=_)){for(var x=0;;x++){if(f>=_||x>=s){y+=r;break;}var b=0===x?a(y,f,c,u):_>0?Math.floor(y[Z]*f/_):0;y=y.substr(0,b),_=e(y,n);}""===y&&(y=h),m[p]=y;}}return m.join("\n");}function a(t,e,i,n){for(var r=0,a=0,o=t[Z];o>a&&e>r;a++){var s=t.charCodeAt(a);r+=s>=0&&127>=s?i:n;}return a;}var o={},s=0,l=5e3,c=t("../core/util"),h=t("../core/BoundingRect"),d=c[u],v={getWidth:e,getBoundingRect:i,adjustTextPositionOnRect:n,truncateText:r,measureText:function measureText(t,e){var i=c[f]();return i.font=e||"12px sans-serif",i.measureText(t);}};return v;}),e("echarts/model/mixin/colorPalette",[pe,"../../util/clazz"],function(t){var e=t("../../util/clazz"),i=e.set,n=e.get;return{clearColorPalette:function clearColorPalette(){i(this,"colorIdx",0),i(this,"colorNameMap",{});},getColorFromPalette:function getColorFromPalette(t,e){e=e||this;var r=n(e,"colorIdx")||0,a=n(e,"colorNameMap")||i(e,"colorNameMap",{});if(a[t])return a[t];var o=this.get("color",!0)||[];if(o[Z]){var s=o[r];return t&&(a[t]=s),i(e,"colorIdx",(r+1)%o[Z]),s;}}};}),e("echarts/model/mixin/lineStyle",[pe,"./makeStyleMapper"],function(t){var e=t("./makeStyleMapper")([["lineWidth","width"],[m,"color"],[Y],["shadowBlur"],["shadowOffsetX"],["shadowOffsetY"],["shadowColor"]]);return{getLineStyle:function getLineStyle(t){var i=e.call(this,t),n=this.getLineDash(i.lineWidth);return n&&(i.lineDash=n),i;},getLineDash:function getLineDash(t){null==t&&(t=1);var e=this.get("type"),i=Math.max(t,2),n=4*t;return"solid"===e||null==e?null:"dashed"===e?[n,n]:[i,i];}};}),e("echarts/util/clazz",[pe,de],function(t){function e(t){r.assert(/^[a-zA-Z0-9_]+([.][a-zA-Z0-9_]+)?$/.test(t),'componentType "'+t+'" illegal');}function i(t,e){var i=r.slice(arguments,2);return this.superClass[j][e].apply(t,i);}function n(t,e,i){return this.superClass[j][e].apply(t,i);}var r=t(de),a={},o=".",s="___EC__COMPONENT__CONTAINER___",l="\x00ec_\x00";a.set=function(t,e,i){return t[l+e]=i;},a.get=function(t,e){return t[l+e];},a.hasOwn=function(t,e){return t.hasOwnProperty(l+e);};var u=a.parseClassType=function(t){var e={main:"",sub:""};return t&&(t=t.split(o),e.main=t[0]||"",e.sub=t[1]||""),e;};return a.enableClassExtend=function(t,e){t.$constructor=t,t[X]=function(t){var e=this,a=function a(){t.$constructor?t.$constructor.apply(this,arguments):e.apply(this,arguments);};return r[X](a[j],t),a[X]=this[X],a.superCall=i,a.superApply=n,r[_](a,this),a.superClass=e,a;};},a.enableClassManagement=function(t,i){function n(t){var e=a[t.main];return e&&e[s]||(e=a[t.main]={},e[s]=!0),e;}i=i||{};var a={};if(t.registerClass=function(t,i){if(i)if(e(i),i=u(i),i.sub){if(i.sub!==s){var r=n(i);r[i.sub]=t;}}else a[i.main]=t;return t;},t.getClass=function(t,e,i){var n=a[t];if(n&&n[s]&&(n=e?n[e]:null),i&&!n)throw new Error(e?"Component "+t+"."+(e||"")+" not exists. Load it first.":t+".type should be specified.");return n;},t.getClassesByMainType=function(t){t=u(t);var e=[],i=a[t.main];return i&&i[s]?r.each(i,function(t,i){i!==s&&e.push(t);}):e.push(i),e;},t.hasClass=function(t){return t=u(t),!!a[t.main];},t.getAllClassMainTypes=function(){var t=[];return r.each(a,function(e,i){t.push(i);}),t;},t.hasSubTypes=function(t){t=u(t);var e=a[t.main];return e&&e[s];},t.parseClassType=u,i.registerWhenExtend){var o=t[X];o&&(t[X]=function(e){var i=o.call(this,e);return t.registerClass(i,e.type);});}return t;},a.setReadOnly=function(){},a;}),e("echarts/model/mixin/areaStyle",[pe,"./makeStyleMapper"],function(t){

}var i=t("zrender/contain/text");return{getTextColor:function getTextColor(){var t=this[r];return this[v]("color")||t&&t.get("textStyle.color");},getFont:function getFont(){var t=this[r],i=t&&t[ce](ue);return[this[v]("fontStyle")||e(i,"fontStyle"),this[v]("fontWeight")||e(i,"fontWeight"),(this[v]("fontSize")||e(i,"fontSize")||12)+"px",this[v]("fontFamily")||e(i,"fontFamily")||"sans-serif"].join(" ");},getTextRect:function getTextRect(t){return i[ae](t,this[se](),this[v]("align"),this[v]("baseline"));},truncateText:function truncateText(t,e,n,r){return i.truncateText(t,e,this[se](),n,r);}};}),e("zrender/tool/transformPath",[pe,"../core/PathProxy","../core/vector"],function(t){

return e;}),e("zrender/graphic/Displayable",[pe,"../core/util","./Style","../Element","./mixin/RectText"],function(t){function e(t){t=t||{},r.call(this,t);for(var e in t){t.hasOwnProperty(e)&&"style"!==e&&(this[e]=t[e]);}this.style=new n(t.style),this._rect=null,this.__clipPaths=[];}var i=t("../core/util"),n=t("./Style"),r=t("../Element"),a=t("./mixin/RectText");

},afterBrush:function afterBrush(){},brush:function brush(){},getBoundingRect:function getBoundingRect(){},contain:function contain(t,e){return this.rectContain(t,e);},traverse:function traverse(t,e){t.call(e,this);},rectContain:function rectContain(t,e){var i=this.transformCoordToLocal(t,e),n=this[ae]();return n[U](i[0],i[1]);},dirty:function dirty(){this.__dirty=!0,this._rect=null,this.__zr&&this.__zr.refresh();},animateStyle:function animateStyle(t){return this.animate("style",t);},attrKV:function attrKV(t,e){"style"!==t?r[j].attrKV.call(this,t,e):this.style.set(e);},setStyle:function setStyle(t,e){return this.style.set(t,e),this.dirty(!1),this;},useStyle:function useStyle(t){return this.style=new n(t),this.dirty(!1),this;}},i[_](e,r),i.mixin(e,a),e;}

this.data=[],this._len=0,this._ctx=null,this._xi=0,this._yi=0,this._x0=0,this._y0=0,this._ux=0,this._uy=0;};return b[j]={constructor:b,_lineDash:null,_dashOffset:0,_dashIdx:0,_dashSum:0,setScale:function setScale(t,e){this._ux=_(1/s/t)||0,this._uy=_(1/s/e)||0;},getContext:function getContext(){return this._ctx;},beginPath:function beginPath(t){return this._ctx=t,t&&t.beginPath(),t&&(this.dpr=t.dpr),this._len=0,this._lineDash&&(this._lineDash=null,this._dashOffset=0),this;},moveTo:function moveTo(t,e){return this.addData(l.M,t,e),this._ctx&&this._ctx[n](t,e),this._x0=t,this._y0=e,this._xi=t,this._yi=e,this;},lineTo:function lineTo(t,e){var i=_(t-this._xi)>this._ux||_(e-this._yi)>this._uy||this._len<5;return this.addData(l.L,t,e),this._ctx&&i&&(this._needsDash()?this._dashedLineTo(t,e):this._ctx.lineTo(t,e)),i&&(this._xi=t,this._yi=e),this;},bezierCurveTo:function bezierCurveTo(t,e,i,n,r,a){return this.addData(l.C,t,e,i,n,r,a),this._ctx&&(this._needsDash()?this._dashedBezierTo(t,e,i,n,r,a):this._ctx.bezierCurveTo(t,e,i,n,r,a)),this._xi=r,this._yi=a,this;},quadraticCurveTo:function quadraticCurveTo(t,e,i,n){return this.addData(l.Q,t,e,i,n),this._ctx&&(this._needsDash()?this._dashedQuadraticTo(t,e,i,n):this._ctx.quadraticCurveTo(t,e,i,n)),this._xi=i,this._yi=n,this;},arc:function arc(t,e,i,n,r,a){return this.addData(l.A,t,e,i,i,n,r-n,0,a?0:1),this._ctx&&this._ctx.arc(t,e,i,n,r,a),this._xi=v(r)*i+t,this._yi=g(r)*i+t,this;},arcTo:function arcTo(t,e,i,n,r){return this._ctx&&this._ctx.arcTo(t,e,i,n,r),this;},rect:function rect(t,e,i,n){

},fill:function fill(t){t&&t.fill(),this.toStatic();},stroke:function stroke(t){t&&t[m](),this.toStatic();},setLineDash:function setLineDash(t){if(t instanceof Array){this._lineDash=t,this._dashIdx=0;for(var e=0,i=0;i<t[Z];i++){e+=t[i];}this._dashSum=e;}return this;},setLineDashOffset:function setLineDashOffset(t){return this._dashOffset=t,this;},len:function len(){return this._len;},setData:function setData(t){var e=t[Z];this.data&&this.data[Z]==e||!x||(this.data=new Float32Array(e));for(var i=0;e>i;i++){this.data[i]=t[i];}this._len=e;},appendPath:function appendPath(t){t instanceof Array||(t=[t]);for(var e=t[Z],i=0,n=this._len,r=0;e>r;r++){i+=t[r].len();}x&&this.data instanceof Float32Array&&(this.data=new Float32Array(n+i));for(var r=0;e>r;r++){for(var a=t[r].data,o=0;o<a[Z];o++){this.data[n++]=a[o];}}this._len=n;},addData:function addData(t){var e=this.data;this._len+arguments[Z]>e[Z]&&(this._expandData(),e=this.data);for(var i=0;i<arguments[Z];i++){e[this._len++]=arguments[i];}this._prevCmd=t;},_expandData:function _expandData(){if(!(this.data instanceof Array)){for(var t=[],e=0;e<this._len;e++){t[e]=this.data[e];}this.data=t;}},_needsDash:function _needsDash(){return this._lineDash;},_dashedLineTo:function _dashedLineTo(t,e){

}h=m-t,f=g-e,this._dashOffset=-y(h*h+f*f);},_dashedBezierTo:function _dashedBezierTo(t,i,r,a,o,s){

},_dashedQuadraticTo:function _dashedQuadraticTo(t,e,i,n){var r=i,a=n;i=(i+2*t)/3,n=(n+2*e)/3,t=(this._xi+2*t)/3,e=(this._yi+2*e)/3,this._dashedBezierTo(t,e,i,n,r,a);},toStatic:function toStatic(){var t=this.data;t instanceof Array&&(t[Z]=this._len,x&&(this.data=new Float32Array(t)));},getBoundingRect:function getBoundingRect(){u[0]=u[1]=h[0]=h[1]=Number.MAX_VALUE,c[0]=c[1]=f[0]=f[1]=-Number.MAX_VALUE;

}r.min(u,u,h),r.max(c,c,f);}return 0===d&&(u[0]=u[1]=c[0]=c[1]=0),new o(u[0],u[1],c[0]-u[0],c[1]-u[1]);},rebuildPath:function rebuildPath(t){for(var e,i,r,a,o,s,u=this.data,c=this._ux,h=this._uy,f=this._len,d=0;

return{getItemStyle:function getItemStyle(t){var i=e.call(this,t),n=this.getBorderLineDash();return n&&(i.lineDash=n),i;},getBorderLineDash:function getBorderLineDash(){

}function i(){var t=_[0];_[0]=_[1],_[1]=t;}function n(t,e,n,r,a,o,s,l,u,c){if(c>e&&c>r&&c>o&&c>l||e>c&&r>c&&o>c&&l>c)return 0;var h=d.cubicRootAt(e,r,o,l,c,y);if(0===h)return 0;for(var f,p,v=0,m=-1,g=0;h>g;g++){var x=y[g],b=0===x||1===x?.5:1,w=d.cubicAt(t,n,a,s,x);u>w||(0>m&&(m=d.cubicExtrema(e,r,o,l,_),_[1]<_[0]&&m>1&&i(),f=d.cubicAt(e,r,o,l,_[0]),m>1&&(p=d.cubicAt(e,r,o,l,_[1]))),v+=2==m?x<_[0]?e>f?b:-b:x<_[1]?f>p?b:-b:p>l?b:-b:x<_[0]?e>f?b:-b:f>l?b:-b);}return v;}function r(t,e,i,n,r,a,o,s){if(s>e&&s>n&&s>a||e>s&&n>s&&a>s)return 0;var l=d.quadraticRootAt(e,n,a,s,y);if(0===l)return 0;var u=d.quadraticExtremum(e,n,a);if(u>=0&&1>=u){for(var c=0,h=d.quadraticAt(e,n,a,u),f=0;l>f;f++){var p=0===y[f]||1===y[f]?.5:1,v=d.quadraticAt(t,i,r,y[f]);o>v||(c+=y[f]<u?e>h?p:-p:h>a?p:-p);}return c;}var p=0===y[0]||1===y[0]?.5:1,v=d.quadraticAt(t,i,r,y[0]);return o>v?0:e>a?p:-p;}function a(t,e,i,n,r,a,o,s){if(s-=e,s>i||-i>s)return 0;var l=Math.sqrt(i*i-s*s);y[0]=-l,y[1]=l;var u=Math.abs(n-r);if(1e-4>u)return 0;if(1e-4>u%m){n=0,r=m;var c=a?1:-1;return o>=y[0]+t&&o<=y[1]+t?c:0;}if(a){var l=n;n=f(r),r=f(l);}else n=f(n),r=f(r);n>r&&(r+=m);for(var h=0,d=0;2>d;d++){var p=y[d];if(p+t>o){var v=Math.atan2(s,p),c=a?1:-1;0>v&&(v=m+v),(v>=n&&r>=v||v+m>=n&&r>=v+m)&&(v>Math.PI/2&&v<1.5*Math.PI&&(c=-c),h+=c);}}return h;}function o(t,i,o,l,f){for(var d=0,m=0,g=0,y=0,_=0,x=0;x<t[Z];){var b=t[x++];switch(b===s.M&&x>1&&(o||(d+=p(m,g,y,_,l,f))),1==x&&(m=t[x],g=t[x+1],y=m,_=g),b){case s.M:y=t[x++],_=t[x++],m=y,g=_;break;case s.L:if(o){if(v(m,g,t[x],t[x+1],i,l,f))return!0;}else d+=p(m,g,t[x],t[x+1],l,f)||0;m=t[x++],g=t[x++];

}else d+=r(m,g,t[x++],t[x++],t[x],t[x+1],l,f)||0;m=t[x++],g=t[x++];break;case s.A:var w=t[x++],T=t[x++],M=t[x++],S=t[x++],C=t[x++],A=t[x++],P=(t[x++],1-t[x++]),L=Math.cos(C)*M+w,I=Math.sin(C)*S+T;x>1?d+=p(m,g,L,I,l,f):(y=L,_=I);var k=(l-w)*S/M+w;if(o){if(h.containStroke(w,T,S,C,C+A,P,i,k,f))return!0;}else d+=a(w,T,S,C,C+A,P,k,f);m=Math.cos(C+A)*M+w,g=Math.sin(C+A)*S+T;break;case s.R:y=m=t[x++],_=g=t[x++];var z=t[x++],D=t[x++],L=y+z,I=_+D;if(o){if(v(y,_,L,_,i,l,f)||v(L,_,L,I,i,l,f)||v(L,I,y,I,i,l,f)||v(y,I,y,_,i,l,f))return!0;}else d+=p(L,_,L,I,l,f),d+=p(y,I,y,_,l,f);break;case s.Z:if(o){if(v(m,g,y,_,i,l,f))return!0;}else d+=p(m,g,y,_,l,f);m=y,g=_;}}return o||e(g,_)||(d+=p(m,g,y,_,l,f)||0),0!==d;}

return o(t,0,!1,e,i);},containStroke:function containStroke(t,e,i,n){return o(t,e,!0,i,n);}};}),e("zrender/graphic/Pattern",[pe],function(){var t=function t(_t,e){this.image=_t,this.repeat=e,this.type="pattern";};return t[j].getCanvasPattern=function(t){return this._canvasPattern||(this._canvasPattern=t.createPattern(this.image,this.repeat));},t;}),e("echarts/model/mixin/makeStyleMapper",[pe,de],function(t){var e=t(de);return function(t){for(var i=0;i<t[Z];i++){t[i][1]||(t[i][1]=t[i][0]);}return function(i){for(var n={},r=0;r<t[Z];r++){var a=t[r][1];if(!(i&&e[O](i,a)>=0)){var o=this[v](a);null!=o&&(n[t[r][0]]=o);}}return n;};};}),e("zrender/graphic/mixin/RectText",[pe,"../../contain/text","../../core/BoundingRect"],function(t){function e(t,e){return(typeof t==="undefined"?"undefined":(0,_typeof3.default)(t))===F?t.lastIndexOf("%")>=0?parseFloat(t)/100*e:parseFloat(t):t;}var i=t("../../contain/text"),n=t("../../core/BoundingRect"),r=new n(),a=function a(){};return a[j]={constructor:a,drawRectText:function drawRectText(t,n,a){

}v=te;}}else{var _=i.adjustTextPositionOnRect(c,n,a,f);l=_.x,u=_.y,d=d||_[le],v=v||_.textBaseline;}h&&(l+=h[0],u+=h[1]),t[le]=d||"left",t.textBaseline=v||"alphabetic";

T<w[Z];T++){x&&t.fillText(w[T],l,u),b&&t.strokeText(w[T],l,u),u+=a.lineHeight;}t.restore();}}},a;}),e("zrender/graphic/Style",[pe],function(){function t(t,e,i){var n=e.x,r=e.x2,a=e.y,o=e.y2;e.global||(n=n*i.width+i.x,r=r*i.width+i.x,a=a*i[re]+i.y,o=o*i[re]+i.y);var s=t.createLinearGradient(n,a,r,o);return s;}function e(t,e,i){var n=i.width,r=i[re],a=Math.min(n,r),o=e.x,s=e.y,l=e.r;e.global||(o=o*n+i.x,s=s*r+i.y,l*=a);var u=t.createRadialGradient(o,s,0,o,s,l);return u;}var i=[["shadowBlur",0],["shadowOffsetX",0],["shadowOffsetY",0],["shadowColor","#000"],["lineCap","butt"],["lineJoin","miter"],["miterLimit",10]],n=function n(t){this.extendFrom(t);};n[j]={constructor:n,fill:"#000000",stroke:null,opacity:1,lineDash:null,lineDashOffset:0,shadowBlur:0,shadowOffsetX:0,shadowOffsetY:0,lineWidth:1,strokeNoScale:!1,text:null,textFill:"#000",textStroke:null,textPosition:"inside",textOffset:null,textBaseline:null,textAlign:null,textVerticalAlign:null,textDistance:5,textShadowBlur:0,textShadowOffsetX:0,textShadowOffsetY:0,textTransform:!1,textRotation:0,blend:null,bind:function bind(t,e,n){for(var r=this,a=n&&n.style,o=!a,s=0;s<i[Z];s++){var l=i[s],u=l[0];(o||r[u]!==a[u])&&(t[u]=r[u]||l[1]);}

}},hasFill:function hasFill(){var t=this.fill;return null!=t&&"none"!==t;},hasStroke:function hasStroke(){var t=this[m];return null!=t&&"none"!==t&&this.lineWidth>0;},extendFrom:function extendFrom(t,e){if(t){var i=this;for(var n in t){!t.hasOwnProperty(n)||!e&&i.hasOwnProperty(n)||(i[n]=t[n]);}}},set:function set(t,e){(typeof t==="undefined"?"undefined":(0,_typeof3.default)(t))===F?this[t]=e:this.extendFrom(t,!0);},clone:function clone(){var t=new this.constructor();return t.extendFrom(this,!0),t;},getGradient:function getGradient(i,n,r){for(var a="radial"===n.type?e:t,o=a(i,n,r),s=n[L],l=0;l<s[Z];l++){o.addColorStop(s[l].offset,s[l].color);}return o;}};for(var r=n[j],a=0;a<i[Z];a++){var o=i[a];o[0]in r||(r[o[0]]=o[1]);}return n.getGradient=r.getGradient,n;}),e

switch(this.draggable){case"horizontal":e=0;break;case"vertical":t=0;}var i=this.transform;i||(i=this.transform=[1,0,0,1,0,0]),i[4]+=t,i[5]+=e,this.decomposeTransform(),this.dirty(!1);},beforeUpdate:function beforeUpdate(){},afterUpdate:function afterUpdate(){},update:function update(){this.updateTransform();},traverse:function traverse(){},attrKV:function attrKV(t,e){if(t===Q||"scale"===t||"origin"===t){if(e){var i=this[t];i||(i=this[t]=[]),i[0]=e[0],i[1]=e[1];}}else this[t]=e;},hide:function hide(){this[E]=!0,this.__zr&&this.__zr.refresh();},show:function show(){this[E]=!1,this.__zr&&this.__zr.refresh();},attr:function attr(t,e){if((typeof t==="undefined"?"undefined":(0,_typeof3.default)(t))===F)this.attrKV(t,e);else if(a[R](t))for(var i in t){t.hasOwnProperty(i)&&this.attrKV(i,t[i]);}return this.dirty(!1),this;},setClipPath:function setClipPath(t){var e=this.__zr;e&&t.addSelfToZr(e),this.clipPath&&this.clipPath!==t&&this.removeClipPath(),this.clipPath=t,t.__zr=e,t.__clipTarget=this,this.dirty(!1);},removeClipPath:function removeClipPath(){var t=this.clipPath;t&&(t.__zr&&t.removeSelfFromZr(t.__zr),t.__zr=null,t.__clipTarget=null,this.clipPath=null,this.dirty(!1));},addSelfToZr:function addSelfToZr(t){this.__zr=t;var e=this.animators;if(e)for(var i=0;i<e[Z];i++){t[B].addAnimator(e[i]);}this.clipPath&&this.clipPath.addSelfToZr(t);},removeSelfFromZr:function removeSelfFromZr(t){this.__zr=null;var e=this.animators;if(e)for(var i=0;i<e[Z];i++){t[B].removeAnimator(e[i]);}this.clipPath&&this.clipPath.removeSelfFromZr(t);}},a.mixin(o,r),a.mixin(o,n),a.mixin(o,i),o;}),e("echarts/util/component",[pe,de,"./clazz"],function(t){

});}),{graph:n,noEntryList:o};}function r(t,e){return t[e]||(t[e]={predecessor:[],successor:[]}),t[e];}function a(t,i){var n=[];return e.each(t,function(t){e[O](i,t)>=0&&n.push(t);}),n;}t.topologicalTravel=function(t,i,r,a){function o(t){u[t].entryCount--,0===u[t].entryCount&&c.push(t);}function s(t){h[t]=!0,o(t);}if(t[Z]){var l=n(i),u=l.graph,c=l.noEntryList,h={};for(e.each(t,function(t){h[t]=!0;});c[Z];){var f=c.pop(),d=u[f],p=!!h[f];p&&(r.call(a,f,d.originalDeps.slice()),delete h[f]),e.each(d.successor,p?s:o);}e.each(h,function(){throw new Error("Circle dependency may exists");});}};},a;}),e("echarts/model/mixin/boxLayout",[pe],function(){return{getBoxLayoutParams:function getBoxLayoutParams(){

this.animators=[];};return s[j]={constructor:s,animate:function animate(t,n){var r,a=!1,s=this,l=this.__zr;if(t){var u=t.split("."),c=s;a="shape"===u[0];for(var h=0,f=u[Z];f>h;h++){c&&(c=c[u[h]]);}c&&(r=c);}else r=s;if(!r)return void o('Property "'+t+'" is not existed in element '+s.id);var d=s.animators,p=new e(r,n);return p.during(function(){s.dirty(a);}).done(function(){d[A](i[O](d,p),1);}),d.push(p),l&&l[B].addAnimator(p),p;},stopAnimation:function stopAnimation(t){for(var e=this.animators,i=e[Z],n=0;i>n;n++){e[n].stop(t);}return e[Z]=0,this;},animateTo:function animateTo(t,e,i,a,o){function s(){u--,u||o&&o();}n(i)?(o=a,a=i,i=0):r(a)?(o=a,a="linear",i=0):r(i)?(o=i,i=0):r(e)?(o=e,e=500):e||(e=500),this.stopAnimation(),this._animateToShallow("",this,t,e,i,a,o);var l=this.animators.slice(),u=l[Z];u||o&&o();for(var c=0;c<l[Z];c++){l[c].done(s).start(a);}},_animateToShallow:function _animateToShallow(t,e,n,r,o){var s={},l=0;for(var u in n){if(n.hasOwnProperty(u))if(null!=e[u])a(n[u])&&!i.isArrayLike(n[u])?this._animateToShallow(t?t+"."+u:u,e[u],n[u],r,o):(s[u]=n[u],l++);else if(null!=n[u])if(t){var c={};c[t]={},c[t][u]=n[u],this.attr(c);}else this.attr(u,n[u]);}return l>0&&this.animate(t,!1).when(null==r?500:r,s).delay(o||0),this;

},s.setTransform=function(t){var e=this.transform,i=t.dpr||1;e?t.setTransform(i*e[0],i*e[1],i*e[2],i*e[3],i*e[4],i*e[5]):t.setTransform(i,0,0,i,0,0);},s.restoreTransform=function(t){var e=(this.transform,t.dpr||1);t.setTransform(e,0,0,e,0,0);};var l=[];return s.decomposeTransform=function(){

}else if(e.debugMode>1)for(var t in arguments){console.log(arguments[t]);}};}),e("zrender/animation/Animator",[pe,"./Clip","../tool/color","../core/util"],function(t){function e(t,e){return t[e];}function i(t,e,i){t[e]=i;}function n(t,e,i){return(e-t)*i+t;}function r(t,e,i){return i>.5?e:t;}function a(t,e,i,r,a){var o=t[Z];if(1==a)for(var s=0;o>s;s++){r[s]=n(t[s],e[s],i);}else for(var l=t[0][Z],s=0;o>s;s++){for(var u=0;l>u;u++){r[s][u]=n(t[s][u],e[s][u],i);}}}function o(t,e,i){var n=t[Z],r=e[Z];if(n!==r){var a=n>r;if(a)t[Z]=r;else for(var o=n;r>o;o++){t.push(1===i?e[o]:g.call(e[o]));}}for(var s=t[0]&&t[0][Z],o=0;o<t[Z];o++){if(1===i)isNaN(t[o])&&(t[o]=e[o]);else for(var l=0;s>l;l++){isNaN(t[o][l])&&(t[o][l]=e[o][l]);}}}function s(t,e,i){if(t===e)return!0;var n=t[Z];if(n!==e[Z])return!1;if(1===i){for(var r=0;n>r;r++){if(t[r]!==e[r])return!1;}}else for(var a=t[0][Z],r=0;n>r;r++){for(var o=0;a>o;o++){if(t[r][o]!==e[r][o])return!1;}}return!0;}function l(t,e,i,n,r,a,o,s,l){var c=t[Z];if(1==l)for(var h=0;c>h;h++){s[h]=u(t[h],e[h],i[h],n[h],r,a,o);}else for(var f=t[0][Z],h=0;c>h;h++){for(var d=0;f>d;d++){s[h][d]=u(t[h][d],e[h][d],i[h][d],n[h][d],r,a,o);}}}function u(t,e,i,n,r,a,o){var s=.5*(i-t),l=.5*(n-e);return(2*(e-i)+s+l)*o+(-3*(e-i)-2*s-l)*a+s*r+e;}function c(t){if(m(t)){var e=t[Z];if(m(t[0])){for(var i=[],n=0;e>n;n++){i.push(g.call(t[n]));}return i;}return g.call(t);}return t;}function h(t){

}),x=c[_-1].time;for(var C=[],A=[],P=c[0].value,L=!0,I=0;_>I;I++){C.push(c[I].time/x);var k=c[I].value;if(w&&s(k,P,S)||!w&&k===P||(L=!1),P=k,(typeof k==="undefined"?"undefined":(0,_typeof3.default)(k))==F){var z=p.parse(k);z?(k=z,T=!0):M=!0;}A.push(k);}if(!L){for(var D=A[_-1],I=0;_-1>I;I++){w?o(A[I],D,S):!isNaN(A[I])||isNaN(D)||M||T||(A[I]=D);}w&&o(v(t._target,f),D,S);var O,E,R,B,N,G,q=0,V=0;if(T)var H=[0,0,0,0];var U=function U(t,e){var i;if(0>e)i=0;else if(V>e){for(O=Math.min(q+1,_-1),i=O;i>=0&&!(C[i]<=e);i--){}i=Math.min(i,_-2);}else{for(i=q;_>i&&!(C[i]>e);i++){}i=Math.min(i-1,_-2);}q=i,V=e;var o=C[i+1]-C[i];if(0!==o)if(E=(e-C[i])/o,y){if(B=A[i],R=A[0===i?i:i-1],N=A[i>_-2?_-1:i+1],G=A[i>_-3?_-1:i+2],w)l(R,B,N,G,E,E*E,E*E*E,v(t,f),S);else{var s;if(T)s=l(R,B,N,G,E,E*E,E*E*E,H,1),s=h(H);else{if(M)return r(B,N,E);s=u(R,B,N,G,E,E*E,E*E*E);}g(t,f,s);}}else if(w)a(A[i],A[i+1],E,v(t,f),S);else{var s;if(T)a(A[i],A[i+1],E,H,1),s=h(H);else{if(M)return r(A[i],A[i+1],E);s=n(A[i],A[i+1],E);}g(t,f,s);}},W=new d({

;return y[j]={when:function when(t,e){var i=this._tracks;for(var n in e){if(e.hasOwnProperty(n)){if(!i[n]){i[n]=[];var r=this._getter(this._target,n);if(null==r)continue;0!==t&&i[n].push({time:0,value:c(r)});}i[n].push({time:t,value:e[n]});}}return this;},during:function during(t){return this._onframeList.push(t),this;},_doneCallback:function _doneCallback(){this._tracks={},this._clipList[Z]=0;for(var t=this._doneList,e=t[Z],i=0;e>i;i++){t[i].call(this);}},start:function start(t){var e,i=this,n=0,r=function r(){n--,n||i._doneCallback();};for(var a in this._tracks){if(this._tracks.hasOwnProperty(a)){var o=f(this,t,r,this._tracks[a],a);o&&(this._clipList.push(o),n++,this[B]&&this[B].addClip(o),e=o);}}if(e){var s=e.onframe;e.onframe=function(t,e){s(t,e);for(var n=0;n<i._onframeList[Z];n++){i._onframeList[n](t,e);}};}return n||this._doneCallback(),this;},stop:function stop(t){for(var e=this._clipList,i=this[B],n=0;n<e[Z];n++){var r=e[n];t&&r.onframe(this._target,1),i&&i.removeClip(r);}e[Z]=0;},delay:function delay(t){return this._delay=t,this;},done:function done(t){return t&&this._doneList.push(t),this;},getClips:function getClips(){return this._clipList;}},y;}),e("zrender/animation/Clip",[pe,"./easing"],function(t){

}var i=t("./easing");return e[j]={constructor:e,step:function step(t){this._initialized||(this._startTime=t+this._delay,this._initialized=!0);var e=(t-this._startTime)/this._life;if(!(0>e)){e=Math.min(e,1);var n=this.easing,r=(typeof n==="undefined"?"undefined":(0,_typeof3.default)(n))==F?i[n]:n,a=(typeof r==="undefined"?"undefined":(0,_typeof3.default)(r))===b?r(e):e;return this.fire("frame",a),1==e?this.loop?(this.restart(t),"restart"):(this._needsRemove=!0,"destroy"):null;}},restart:function restart(t){var e=(t-this._startTime)%this._life;this._startTime=t-e+this.gap,this._needsRemove=!1;},fire:function fire(t,e){t="on"+t,this[t]&&this[t](this._target,e);}},e;}),e("zrender/animation/easing",[],function(){var t={linear:function linear(t){return t;},quadraticIn:function quadraticIn(t){return t*t;},quadraticOut:function quadraticOut(t){return t*(2-t);},quadraticInOut:function quadraticInOut(t){return(t*=2)<1?.5*t*t:-.5*(--t*(t-2)-1);},cubicIn:function cubicIn(t){return t*t*t;},cubicOut:function cubicOut(t){return--t*t*t+1;},cubicInOut:function cubicInOut(t){return(t*=2)<1?.5*t*t*t:.5*((t-=2)*t*t+2);},quarticIn:function quarticIn(t){return t*t*t*t;},quarticOut:function quarticOut(t){return 1- --t*t*t*t;},quarticInOut:function quarticInOut(t){return(t*=2)<1?.5*t*t*t*t:-.5*((t-=2)*t*t*t-2);},quinticIn:function quinticIn(t){return t*t*t*t*t;},quinticOut:function quinticOut(t){return--t*t*t*t*t+1;},quinticInOut:function quinticInOut(t){return(t*=2)<1?.5*t*t*t*t*t:.5*((t-=2)*t*t*t*t+2);},sinusoidalIn:function sinusoidalIn(t){return 1-Math.cos(t*Math.PI/2);},sinusoidalOut:function sinusoidalOut(t){return Math.sin(t*Math.PI/2);},sinusoidalInOut:function sinusoidalInOut(t){return .5*(1-Math.cos(Math.PI*t));},exponentialIn:function exponentialIn(t){return 0===t?0:Math.pow(1024,t-1);},exponentialOut:function exponentialOut(t){return 1===t?1:1-Math.pow(2,-10*t);},exponentialInOut:function exponentialInOut(t){return 0===t?0:1===t?1:(t*=2)<1?.5*Math.pow(1024,t-1):.5*(-Math.pow(2,-10*(t-1))+2);},circularIn:function circularIn(t){return 1-Math.sqrt(1-t*t);},circularOut:function circularOut(t){return Math.sqrt(1- --t*t);},circularInOut:function circularInOut(t){return(t*=2)<1?-.5*(Math.sqrt(1-t*t)-1):.5*(Math.sqrt(1-(t-=2)*t)+1);},elasticIn:function elasticIn(t){var e,i=.1,n=.4;return 0===t?0:1===t?1:(!i||1>i?(i=1,e=n/4):e=n*Math.asin(1/i)/(2*Math.PI),-(i*Math.pow(2,10*(t-=1))*Math.sin(2*(t-e)*Math.PI/n)));},elasticOut:function elasticOut(t){var e,i=.1,n=.4;return 0===t?0:1===t?1:(!i||1>i?(i=1,e=n/4):e=n*Math.asin(1/i)/(2*Math.PI),i*Math.pow(2,-10*t)*Math.sin(2*(t-e)*Math.PI/n)+1);},elasticInOut:function elasticInOut(t){var e,i=.1,n=.4;return 0===t?0:1===t?1:(!i||1>i?(i=1,e=n/4):e=n*Math.asin(1/i)/(2*Math.PI),(t*=2)<1?-.5*i*Math.pow(2,10*(t-=1))*Math.sin(2*(t-e)*Math.PI/n):i*Math.pow(2,-10*(t-=1))*Math.sin(2*(t-e)*Math.PI/n)*.5+1);},backIn:function backIn(t){var e=1.70158;return t*t*((e+1)*t-e);},backOut:function backOut(t){var e=1.70158;return--t*t*((e+1)*t+e)+1;},backInOut:function backInOut(t){var e=2.5949095;return(t*=2)<1?.5*t*t*((e+1)*t-e):.5*((t-=2)*t*((e+1)*t+e)+2);},bounceIn:function bounceIn(e){return 1-t.bounceOut(1-e);},bounceOut:function bounceOut(t){return 1/2.75>t?7.5625*t*t:2/2.75>t?7.5625*(t-=1.5/2.75)*t+.75:2.5/2.75>t?7.5625*(t-=2.25/2.75)*t+.9375:7.5625*(t-=2.625/2.75)*t+.984375;},bounceInOut:function bounceInOut(e){return .5>e?.5*t.bounceIn(2*e):.5*t.bounceOut(2*e-1)+.5;}};return t;}),e("zrender/config",[],function(){var t=1;(typeof window==="undefined"?"undefined":(0,_typeof3.default)(window))!==i&&(t=Math.max(window.devicePixelRatio||1,1));var e={debugMode:0,devicePixelRatio:t};return e;}),e("zrender/core/curve",[pe,"./vector"],function(t){function e(t){return t>-x&&x>t;}function i(t){return t>x||-x>t;}function n(t,e,i,n,r){var a=1-r;return a*a*(a*t+3*r*e)+r*r*(r*n+3*a*i);}function r(t,e,i,n,r){var a=1-r;return 3*(((e-t)*a+2*(i-e)*r)*a+(n-i)*r*r);}function a(t,i,n,r,a,o){var s=r+3*(i-n)-t,l=3*(n-2*i+t),u=3*(i-t),c=t-a,h=l*l-3*s*u,f=l*u-9*s*c,d=u*u-3*l*c,p=0;if(e(h)&&e(f)){if(e(l))o[0]=0;else{var v=-u/l;v>=0&&1>=v&&(o[p++]=v);}}else{var m=f*f-4*h*d;if(e(m)){var g=f/h,v=-l/s+g,x=-g/2;v>=0&&1>=v&&(o[p++]=v),x>=0&&1>=x&&(o[p++]=x);}else if(m>0){var b=_(m),M=h*l+1.5*s*(-f+b),S=h*l+1.5*s*(-f-b);M=0>M?-y(-M,T):y(M,T),S=0>S?-y(-S,T):y(S,T);var v=(-l-(M+S))/(3*s);v>=0&&1>=v&&(o[p++]=v);}else{var C=(2*h*l-3*s*f)/(2*_(h*h*h)),A=Math.acos(C)/3,P=_(h),L=Math.cos(A),v=(-l-2*P*L)/(3*s),x=(-l+P*(L+w*Math.sin(A)))/(3*s),I=(-l+P*(L-w*Math.sin(A)))/(3*s);v>=0&&1>=v&&(o[p++]=v),x>=0&&1>=x&&(o[p++]=x),I>=0&&1>=I&&(o[p++]=I);}}return p;}function o(t,n,r,a,o){var s=6*r-12*n+6*t,l=9*n+3*a-3*t-9*r,u=3*n-3*t,c=0;if(e(l)){if(i(s)){var h=-u/s;h>=0&&1>=h&&(o[c++]=h);}}else{var f=s*s-4*l*u;if(e(f))o[0]=-s/(2*l);else if(f>0){var d=_(f),h=(-s+d)/(2*l),p=(-s-d)/(2*l);h>=0&&1>=h&&(o[c++]=h),p>=0&&1>=p&&(o[c++]=p);}}return c;}function s(t,e,i,n,r,a){var o=(e-t)*r+t,s=(i-e)*r+e,l=(n-i)*r+i,u=(s-o)*r+o,c=(l-s)*r+s,h=(c-u)*r+u;a[0]=t,a[1]=o,a[2]=u,a[3]=h,a[4]=h,a[5]=c,a[6]=l,a[7]=n;}function l(t,e,i,r,a,o,s,l,u,c,h){var f,d,p,v,m,y=.005,x=1/0;M[0]=u,M[1]=c;for(var w=0;1>w;w+=.05){S[0]=n(t,i,a,s,w),S[1]=n(e,r,o,l,w),v=g(M,S),x>v&&(f=w,x=v);}x=1/0;for(var T=0;32>T&&!(b>y);T++){d=f-y,p=f+y,S[0]=n(t,i,a,s,d),S[1]=n(e,r,o,l,d),v=g(S,M),d>=0&&x>v?(f=d,x=v):(C[0]=n(t,i,a,s,p),C[1]=n(e,r,o,l,p),m=g(C,M),1>=p&&x>m?(f=p,x=m):y*=.5);}return h&&(h[0]=n(t,i,a,s,f),h[1]=n(e,r,o,l,f)),_(x);}function u(t,e,i,n){var r=1-n;return r*(r*t+2*n*e)+n*n*i;}function c(t,e,i,n){return 2*((1-n)*(e-t)+n*(i-e));}function h(t,n,r,a,o){var s=t-2*n+r,l=2*(n-t),u=t-a,c=0;if(e(s)){if(i(l)){var h=-u/l;h>=0&&1>=h&&(o[c++]=h);}}else{var f=l*l-4*s*u;if(e(f)){var h=-l/(2*s);h>=0&&1>=h&&(o[c++]=h);}else if(f>0){var d=_(f),h=(-l+d)/(2*s),p=(-l-d)/(2*s);h>=0&&1>=h&&(o[c++]=h),p>=0&&1>=p&&(o[c++]=p);}}return c;}function f(t,e,i){var n=t+i-2*e;return 0===n?.5:(t-e)/n;}function d(t,e,i,n,r){var a=(e-t)*n+t,o=(i-e)*n+e,s=(o-a)*n+a;r[0]=t,r[1]=a,r[2]=s,r[3]=s,r[4]=o,r[5]=i;}function p(t,e,i,n,r,a,o,s,l){var c,h=.005,f=1/0;M[0]=o,M[1]=s;for(var d=0;1>d;d+=.05){S[0]=u(t,i,r,d),S[1]=u(e,n,a,d);var p=g(M,S);f>p&&(c=d,f=p);}f=1/0;for(var v=0;32>v&&!(b>h);v++){var m=c-h,y=c+h;S[0]=u(t,i,r,m),S[1]=u(e,n,a,m);var p=g(S,M);if(m>=0&&f>p)c=m,f=p;else{C[0]=u(t,i,r,y),C[1]=u(e,n,a,y);var x=g(C,M);1>=y&&f>x?(c=y,f=x):h*=.5;}}return l&&(l[0]=u(t,i,r,c),l[1]=u(e,n,a,c)),_(f);}var v=t("./vector"),m=v[V],g=v.distSquare,y=Math.pow,_=Math.sqrt,x=1e-8,b=1e-4,w=_(3),T=1/3,M=m(),S=m(),C=m();

for(n=1;n<t[Z];n++){o=t[n],s=r(s,o[0]),l=a(l,o[0]),u=r(u,o[1]),c=a(c,o[1]);}e[0]=s,e[1]=u,i[0]=l,i[1]=c;}},n.fromLine=function(t,e,i,n,o,s){o[0]=r(t,i),o[1]=r(e,n),s[0]=a(t,i),s[1]=a(e,n);};var f=[],d=[];return n.fromCubic=function(t,e,n,o,s,l,u,c,h,p){var v,m=i.cubicExtrema,g=i.cubicAt,y=m(t,n,s,u,f);for(h[0]=1/0,h[1]=1/0,p[0]=-1/0,p[1]=-1/0,v=0;y>v;v++){var _=g(t,n,s,u,f[v]);h[0]=r(_,h[0]),p[0]=a(_,p[0]);}for(y=m(e,o,l,c,d),v=0;y>v;v++){var x=g(e,o,l,c,d[v]);h[1]=r(x,h[1]),p[1]=a(x,p[1]);}h[0]=r(t,h[0]),p[0]=a(t,p[0]),h[0]=r(u,h[0]),p[0]=a(u,p[0]),h[1]=r(e,h[1]),p[1]=a(e,p[1]),h[1]=r(c,h[1]),p[1]=a(c,p[1]);},n.fromQuadratic=function(t,e,n,o,s,l,u,c){var h=i.quadraticExtremum,f=i.quadraticAt,d=a(r(h(t,n,s),1),0),p=a(r(h(e,o,l),1),0),v=f(t,n,s,d),m=f(e,o,l,p);u[0]=r(t,s,v),u[1]=r(e,l,m),c[0]=a(t,s,v),c[1]=a(e,l,m);},n.fromArc=function(t,i,n,r,a,f,d,p,v){var m=e.min,g=e.max,y=Math.abs(a-f);if(1e-4>y%h&&y>1e-4)return p[0]=t-n,p[1]=i-r,v[0]=t+n,void(v[1]=i+r);if(l[0]=s(a)*n+t,l[1]=o(a)*r+i,u[0]=s(f)*n+t,u[1]=o(f)*r+i,m(p,l,u),g(v,l,u),a%=h,0>a&&(a+=h),f%=h,0>f&&(f+=h),a>f&&!d?f+=h:f>a&&d&&(a+=h),d){var _=f;f=a,a=_;}for(var x=0;f>x;x+=Math.PI/2){x>a&&(c[0]=s(x)*n+t,c[1]=o(x)*r+i,m(p,c,p),g(v,c,v));}},n;}),e("zrender/contain/util",[pe],function(){var t=2*Math.PI;return{normalizeRadian:function normalizeRadian(e){return e%=t,0>e&&(e+=t),e;}};}),e("zrender/contain/quadratic",[pe,"../core/curve"],function(t){var e=t("../core/curve");return{containStroke:function containStroke(t,i,n,r,a,o,s,l,u){if(0===s)return!1;var c=s;if(u>i+c&&u>r+c&&u>o+c||i-c>u&&r-c>u&&o-c>u||l>t+c&&l>n+c&&l>a+c||t-c>l&&n-c>l&&a-c>l)return!1;var h=e.quadraticProjectPoint(t,i,n,r,a,o,l,u,null);return c/2>=h;}};}),e("zrender/contain/windingLine",[],function(){return function(t,e,i,n,r,a){if(a>e&&a>n||e>a&&n>a)return 0;if(n===e)return 0;var o=e>n?1:-1,s=(a-e)/(n-e);(1===s||0===s)&&(o=e>n?.5:-.5);var l=s*(i-t)+t;return l>r?o:0;};}),e("zrender/contain/line",[],function(){return{containStroke:function containStroke(t,e,i,n,r,a,o){if(0===r)return!1;var s=r,l=0,u=t;if(o>e+s&&o>n+s||e-s>o&&n-s>o||a>t+s&&a>i+s||t-s>a&&i-s>a)return!1;if(t===i)return Math.abs(a-t)<=s/2;l=(e-n)/(t-i),u=(t*n-i*e)/(t-i);var c=l*a-o+u,h=c*c/(l*l+1);return s/2*s/2>=h;}};}),e("zrender/contain/cubic",[pe,"../core/curve"],function(t){var e=t("../core/curve");return{containStroke:function containStroke(t,i,n,r,a,o,s,l,u,c,h){if(0===u)return!1;var f=u;if(h>i+f&&h>r+f&&h>o+f&&h>l+f||i-f>h&&r-f>h&&o-f>h&&l-f>h||c>t+f&&c>n+f&&c>a+f&&c>s+f||t-f>c&&n-f>c&&a-f>c&&s-f>c)return!1;var d=e.cubicProjectPoint(t,i,n,r,a,o,s,l,c,h,null);return f/2>=d;}};}),e("zrender/contain/arc",[pe,"./util"],function(t){var e=t("./util").normalizeRadian,i=2*Math.PI;return{containStroke:function containStroke(t,n,r,a,o,s,l,u,c){if(0===l)return!1;

}};}),e("zrender/core/LRU",[pe],function(){var t=function t(){this.head=null,this.tail=null,this._len=0;}

},e.len=function(){return this._len;};var i=function i(t){this.value=t,this.next,this.prev;},n=function n(e){this._list=new t(),this._map={},this._maxSize=e||10;},r=n[j];return r.put=function(t,e){var i=this._list,n=this._map;if(null==n[t]){var r=i.len();if(r>=this._maxSize&&r>0){var a=i.head;i.remove(a),delete n[a.key];}var o=i.insert(e);o.key=t,n[t]=o;}},r.get=function(t){var e=this._map[t],i=this._list;return null!=e?(e!==i.tail&&(i.remove(e),i.insertEntry(e)),e.value):void 0;},r.clear=function(){this._list.clear(),this._map={};},n;}),e("zrender/graphic/helper/poly",[pe,"./smoothSpline","./smoothBezier"],function(t){var e=t("./smoothSpline"),i=t("./smoothBezier");return{buildPath:function buildPath(t,r,a){var o=r.points,s=r.smooth;if(o&&o[Z]>=2){if(s&&"spline"!==s){var l=i(o,s,a,r.smoothConstraint);t[n](o[0][0],o[0][1]);for(var u=o[Z],c=0;(a?u:u-1)>c;c++){var h=l[2*c],f=l[2*c+1],d=o[(c+1)%u];t.bezierCurveTo(h[0],h[1],f[0],f[1],d[0],d[1]);}}else{"spline"===s&&(o=e(o,a)),t[n](o[0][0],o[0][1]);for(var c=1,p=o[Z];p>c;c++){t.lineTo(o[c][0],o[c][1]);}}a&&t.closePath();}}};}),e("zrender/graphic/helper/smoothSpline",[pe,"../../core/vector"],function(t){function e(t,e,i,n,r,a,o){var s=.5*(i-t),l=.5*(n-e);return(2*(e-i)+s+l)*o+(-3*(e-i)-2*s-l)*a+s*r+e;}var i=t("../../core/vector");return function(t,n){for(var r=t[Z],a=[],o=0,s=1;r>s;s++){o+=i.distance(t[s-1],t[s]);}var l=o/2;l=r>l?r:l;for(var s=0;l>s;s++){var u,c,h,f=s/(l-1)*(n?r:r-1),d=Math.floor(f),p=f-d,v=t[d%r];n?(u=t[(d-1+r)%r],c=t[(d+1)%r],h=t[(d+2)%r]):(u=t[0===d?d:d-1],c=t[d>r-2?r-1:d+1],h=t[d>r-3?r-1:d+2]);var m=p*p,g=p*m;a.push([e(u[0],v[0],c[0],h[0],p,m,g),e(u[1],v[1],c[1],h[1],p,m,g)]);}return a;};}

continue;}c=t[y-1],h=t[y+1];}e.sub(v,h,c),r(v,v,s);var b=a(x,c),w=a(x,h),T=b+w;0!==T&&(b/=T,w/=T),r(m,v,-b),r(g,v,w);var M=o([],x,m),S=o([],x,g);u&&(n(M,M,f),i(M,M,d),n(S,S,f),i(S,S,d)),p.push(M),p.push(S);}return l&&p.push(p.shift()),p;};}),e("zrender/graphic/helper/roundRect",[pe],function(){return{buildPath:function buildPath(t,e){var i,r,a,o,s=e.x,l=e.y,u=e.width,c=e[re],h=e.r;0>u&&(s+=u,u=-u),0>c&&(l+=c,c=-c),(typeof h==="undefined"?"undefined":(0,_typeof3.default)(h))===x?i=r=a=o=h:h instanceof Array?1===h[Z]?i=r=a=o=h[0]:2===h[Z]?(i=a=h[0],r=o=h[1]):3===h[Z]?(i=h[0],r=o=h[1],a=h[2]):(i=h[0],r=h[1],a=h[2],o=h[3]):i=r=a=o=0;var f;i+r>u&&(f=i+r,i*=u/f,r*=u/f),a+o>u&&(f=a+o,a*=u/f,o*=u/f),r+a>c&&(f=r+a,r*=c/f,a*=c/f),i+o>c&&(f=i+o,i*=c/f,o*=c/f),t[n](s+i,l),t.lineTo(s+u-r,l),0!==r&&t.quadraticCurveTo(s+u,l,s+u,l+r),t.lineTo(s+u,l+c-a),0!==a&&t.quadraticCurveTo(s+u,l+c,s+u-a,l+c),t.lineTo(s+o,l+c),0!==o&&t.quadraticCurveTo(s,l+c,s,l+c-o),t.lineTo(s,l+i),0!==i&&t.quadraticCurveTo(s,l,s+i,l);}};}),e("zrender/graphic/Gradient",[pe],function(){var t=function t(_t2){this[L]=_t2||[];};return t[j]={constructor:t,addColorStop:function addColorStop(t,e){this[L].push({offset:t,color:e});}},t;}),e("zrender/Painter",[pe,"./config","./core/util","./core/log","./core/BoundingRect","./core/timsort","./Layer","./animation/requestAnimationFrame","./graphic/Image"],function(t){function e(t){return parseInt(t,10);}function i(t){return t?t.isBuildin?!0:(0,_typeof3.default)(t.resize)!==b||(0,_typeof3.default)(t.refresh)!==b?!1:!0:!1;}function n(t){t.__unusedCount++;}function r(t){1==t.__unusedCount&&t.clear();}function a(t,e,i){return x.copy(t[ae]()),t.transform&&x[g](t.transform),w.width=e,w[re]=i,!x.intersect(w);}function o(t,e){if(t==e)return!1;if(!t||!e||t[Z]!==e[Z])return!0;for(var i=0;i<t[Z];i++){if(t[i]!==e[i])return!0;}}function s(t,e){for(var i=0;i<t[Z];i++){var n=t[i],r=n.path;n.setTransform(e),r.beginPath(e),n.buildPath(r,n.shape),e.clip(),n.restoreTransform(e);}

t.appendChild(d);}this.pathToImage=this._createPathToImage(),this._progressiveLayers=[],this._hoverlayer,this._hoverElements=[];};return T[j]={constructor:T,isSingleCanvas:function isSingleCanvas(){return this._singleCanvas;},getViewportRoot:function getViewportRoot(){return this._domRoot;},refresh:function refresh(t){var e=this.storage.getDisplayList(!0),i=this._zlevelList;this._paintList(e,t);for(var n=0;n<i[Z];n++){var r=i[n],a=this._layers[r];!a.isBuildin&&a.refresh&&a.refresh();}return this.refreshHover(),this._progressiveLayers[Z]&&this._startProgessive(),this;},addHover:function addHover(t,e){if(!t.__hoverMir){var i=new t.constructor({style:t.style,shape:t.shape});i.__from=t,t.__hoverMir=i,i[$](e),this._hoverElements.push(i);}},removeHover:function removeHover(t){var e=t.__hoverMir,i=this._hoverElements,n=c[O](i,e);n>=0&&i[A](n,1),t.__hoverMir=null;},clearHover:function clearHover(){for(var t=this._hoverElements,e=0;e<t[Z];e++){var i=t[e].__from;i&&(i.__hoverMir=null);}t[Z]=0;},refreshHover:function refreshHover(){var t=this._hoverElements,e=t[Z],i=this._hoverlayer;

}i.ctx.restore();}},_startProgessive:function _startProgessive(){function t(){i===e._progressiveToken&&e.storage&&(e._doPaintList(e.storage.getDisplayList()),e._furtherProgressive?(e._progress++,y(t)):e._progressiveToken=-1);}var e=this;if(e._furtherProgressive){var i=e._progressiveToken=+new Date();e._progress++,y(t);}},_clearProgressive:function _clearProgressive(){this._progressiveToken=-1,this._progress=0,c.each(this._progressiveLayers,function(t){t.__dirty&&t.clear();});},_paintList:function _paintList(t,e){null==e&&(e=!1),this._updateLayerStatus(t),this._clearProgressive(),this.eachBuildinLayer(n),this._doPaintList(t,e),this.eachBuildinLayer(r);},_doPaintList:function _doPaintList(t,e){function i(t){var e=a.dpr||1;a.save(),a.globalAlpha=1,a.shadowBlur=0,n.__dirty=!0,a.setTransform(1,0,0,1,0,0),a.drawImage(t.dom,0,0,f*e,d*e),a.restore();}

continue;}l=s.__progress,s.__dirty||(p=l),s.__progress=p+1;}x===p&&this._doPaintEl(g,s,!0,s.renderScope);}else this._doPaintEl(g,n,e,o);g.__dirty=!1;}}s&&i(s),a&&a.restore(),this._furtherProgressive=!1,c.each(this._progressiveLayers,function(t){t.__maxProgress>=t.__progress&&(this._furtherProgressive=!0);},this);},_doPaintEl:function _doPaintEl(t,e,i,n){var r=e.ctx,l=t.transform;if(!(!e.__dirty&&!i||t.invisible||0===t.style[Y]||l&&!l[0]&&!l[3]||t.culling&&a(t,this._width,this._height))){

}},getLayer:function getLayer(t){if(this._singleCanvas)return this._layers[0];var e=this._layers[t];return e||(e=new m("zr_"+t,this,this.dpr),e.isBuildin=!0,this._layerConfig[t]&&c.merge(e,this._layerConfig[t],!0),this.insertLayer(t,e),e.initContext()),e;},insertLayer:function insertLayer(t,e){

},eachLayer:function eachLayer(t,e){var i,n,r=this._zlevelList;for(n=0;n<r[Z];n++){i=r[n],t.call(e,this._layers[i],i);}},eachBuildinLayer:function eachBuildinLayer(t,e){var i,n,r,a=this._zlevelList;for(r=0;r<a[Z];r++){n=a[r],i=this._layers[n],i.isBuildin&&t.call(e,i,n);}},eachOtherLayer:function eachOtherLayer(t,e){var i,n,r,a=this._zlevelList;for(r=0;r<a[Z];r++){n=a[r],i=this._layers[n],i.isBuildin||t.call(e,i,n);}},getLayers:function getLayers(){return this._layers;},_updateLayerStatus:function _updateLayerStatus(t){var e=this._layers,i=this._progressiveLayers,n={

});},clear:function clear(){return this.eachBuildinLayer(this._clearLayer),this;},_clearLayer:function _clearLayer(t){t.clear();},configLayer:function configLayer(t,e){if(e){var i=this._layerConfig;i[t]?c.merge(i[t],e,!0):i[t]=e;var n=this._layers[t];n&&c.merge(n,i[t],!0);}},delLayer:function delLayer(t){

}c.each(this._progressiveLayers,function(i){i.resize(t,e);}),this.refresh(!0);}return this._width=t,this._height=e,this;},clearLayer:function clearLayer(t){var e=this._layers[t];e&&e.clear();},dispose:function dispose(){this.root.innerHTML="",this.root=this.storage=this._domRoot=this._layers=null;},getRenderedCanvas:function getRenderedCanvas(t){if(t=t||{},this._singleCanvas)return this._layers[0].dom;var e=new m("image",this,t.pixelRatio||this.dpr);e.initContext(),e.clearColor=t.backgroundColor,e.clear();for(var i=this.storage.getDisplayList(!0),n={},r=0;r<i[Z];r++){var a=i[r];this._doPaintEl(a,e,!0,n);}return e.dom;},getWidth:function getWidth(){return this._width;},getHeight:function getHeight(){return this._height;},_getSize:function _getSize(t){var i=this._opts,n=["width",re][t],r=["clientWidth","clientHeight"][t],a=["paddingLeft","paddingTop"][t],o=["paddingRight","paddingBottom"][t];

},_pathToImage:function _pathToImage(e,i,n,r,a){var o=document.createElement(N),s=o[f]("2d");o.width=n*a,o[re]=r*a,s.clearRect(0,0,n*a,r*a);

},_createPathToImage:function _createPathToImage(){var t=this;return function(e,i,n,r){return t._pathToImage(e,i,n,r,t.dpr);};}},T;}

};return a[j]={constructor:a,addClip:function addClip(t){this._clips.push(t);},addAnimator:function addAnimator(t){t[B]=this;for(var e=t.getClips(),i=0;i<e[Z];i++){this.addClip(e[i]);}},removeClip:function removeClip(t){var i=e[O](this._clips,t);i>=0&&this._clips[A](i,1);},removeAnimator:function removeAnimator(t){for(var e=t.getClips(),i=0;i<e[Z];i++){this.removeClip(e[i]);}t[B]=null;},_update:function _update(){for(var t=new Date().getTime()-this._pausedTime,e=t-this._time,i=this._clips,n=i[Z],r=[],a=[],o=0;n>o;o++){var s=i[o],l=s.step(t);l&&(r.push(l),a.push(s));}for(var o=0;n>o;){i[o]._needsRemove?(i[o]=i[n-1],i.pop(),n--):o++;}n=r[Z];for(var o=0;n>o;o++){a[o].fire(r[o]);}this._time=t,this.onframe(e),this.trigger("frame",e),this.stage[I]&&this.stage[I]();},_startLoop:function _startLoop(){function t(){e._running&&(n(t),!e._paused&&e._update());}var e=this;this._running=!0,n(t);},start:function start(){this._time=new Date().getTime(),this._pausedTime=0,this._startLoop();},stop:function stop(){this._running=!1;},pause:function pause(){this._paused||(this._pauseStart=new Date().getTime(),this._paused=!0);},resume:function resume(){this._paused&&(this._pausedTime+=new Date().getTime()-this._pauseStart,this._paused=!1);},clear:function clear(){this._clips=[];},animate:function animate(t,e){e=e||{};var i=new r(t,e.loop,e.getter,e.setter);return this.addAnimator(i),i;}},e.mixin(a,i),a;}),e("zrender/dom/HandlerProxy",[pe,"../core/event","../core/util","../mixin/Eventful","../core/env","../core/GestureMgr"],function(t){function e(t){return"mousewheel"===t&&c.browser.firefox?"DOMMouseScroll":t;}function i(t,e,i){

},700);}function r(t){var e=t.pointerType;return"pen"===e||"touch"===e;}function a(t){function e(t,e){return function(){return e._touching?void 0:t.apply(e,arguments);};}l.each(y,function(e){t._handlers[e]=l.bind(b[e],t);}),l.each(x,function(e){t._handlers[e]=l.bind(b[e],t);}),l.each(g,function(i){t._handlers[i]=e(b[i],t);});}function o(t){function i(i,n){l.each(i,function(i){d(t,e(i),n._handlers[i]);},n);}u.call(this),this.dom=t,this._touching=!1,this._touchTimer,this._gestureMgr=new f(),this._handlers={},a(this),c.pointerEventsSupported?i(x,this):(c.touchEventsSupported&&i(y,this),i(g,this));}var s=t("../core/event"),l=t("../core/util"

}),b={mousemove:function mousemove(t){t=v(this.dom,t),this.trigger("mousemove",t);},mouseout:function mouseout(t){t=v(this.dom,t);var e=t.toElement||t.relatedTarget;if(e!=this.dom)for(;e&&9!=e.nodeType;){if(e===this.dom)return;e=e.parentNode;}this.trigger(C,t);},touchstart:function touchstart(t){t=v(this.dom,t),t.zrByTouch=!0,this._lastTouchMoment=new Date(),i(this,t,"start"),b.mousemove.call(this,t),b.mousedown.call(this,t),n(this);},touchmove:function touchmove(t){t=v(this.dom,t),t.zrByTouch=!0,i(this,t,"change"),b.mousemove.call(this,t),n(this);},touchend:function touchend(t){t=v(this.dom,t),t.zrByTouch=!0,i(this,t,"end"),b.mouseup.call(this,t),+new Date()-this._lastTouchMoment<m&&b.click.call(this,t),n(this);},pointerdown:function pointerdown(t){b.mousedown.call(this,t);},pointermove:function pointermove(t){r(t)||b.mousemove.call(this,t);},pointerup:function pointerup(t){b.mouseup.call(this,t);},pointerout:function pointerout(t){r(t)||b[C].call(this,t);}};l.each(["click","mousedown","mouseup","mousewheel","dblclick","contextmenu"],function(t){b[t]=function(e){e=v(this.dom,e),this.trigger(t,e);};});var w=o[j];return w.dispose=function(){for(var t=g[h](y),i=0;i<t[Z];i++){var n=t[i];p(this.dom,e(n),this._handlers[n]);}},w.setCursor=function(t){this.dom.style.cursor=t||"default";},l.mixin(o,u),o;}),e("zrender/Handler",[pe,"./core/util","./mixin/Draggable","./mixin/Eventful"],function(t){

;}function i(){}function n(t,e,i){if(t[t.rectHover?"rectContain":U](e,i)){for(var n=t;n;){if(n[ne]||n.clipPath&&!n.clipPath[U](e,i))return!1;n=n.parent;}return!0;}return!1;}var r=t("./core/util"),a=t("./mixin/Draggable"),o=t("./mixin/Eventful");i[j].dispose=function(){

);},resize:function resize(){this._hovered=null;},dispatch:function dispatch(t,e){var i=this[t];i&&i.call(this,e);},dispose:function dispose(){this.proxy.dispose(),this.storage=this.proxy=this.painter=null;},setCursorStyle:function setCursorStyle(t){var e=this.proxy;e.setCursor&&e.setCursor(t);},dispatchToElement:function dispatchToElement(t,i,n){for(var r="on"+i,a=e(i,t,n),o=t;o&&(o[r]&&(a.cancelBubble=o[r].call(o,a)),o.trigger(i,a),o=o.parent,!a.cancelBubble);){}a.cancelBubble||(this.trigger(i,a),this.painter&&this.painter.eachOtherLayer(function(t){(0,_typeof3.default)(t[r])==b&&t[r].call(t,a),t.trigger&&t.trigger(i,a);}));},findHover:function findHover(t,e,i){for(var r=this.storage.getDisplayList(),a=r[Z]-1;a>=0;a--){if(!r[a][ne]&&r[a]!==i&&!r[a][E]&&n(r[a],t,e))return r[a];}

;}),r.mixin(l,o),r.mixin(l,a),l;}),e("zrender/Storage",[pe,"./core/util","./core/env","./container/Group","./core/timsort"],function(t){function e(t,e){return t[M]===e[M]?t.z===e.z?t.z2-e.z2:t.z-e.z:t[M]-e[M];}var i=t("./core/util"),n=t("./core/env"),r=t("./container/Group"),a=t("./core/timsort"),o=function o(){this._elements={},this._roots=[],this._displayList=[],this._displayListLen=0;};return o[j]={constructor:o,traverse:function traverse(t,e){for(var i=0;i<this._roots[Z];i++){this._roots[i].traverse(t,e);}},getDisplayList:function getDisplayList(t,e){return e=e||!1,t&&this.updateDisplayList(e),this._displayList;},updateDisplayList:function updateDisplayList(t){this._displayListLen=0;for(var i=this._roots,r=this._displayList,o=0,s=i[Z];s>o;o++){this._updateAndAddDisplayable(i[o],null,t);}r[Z]=this._displayListLen,n.canvasSupported&&a(r,e);},_updateAndAddDisplayable:function _updateAndAddDisplayable(t,e,i){if(!t[E]||i){t.beforeUpdate(),t.__dirty&&t[I](),t.afterUpdate();var n=t.clipPath;if(n){e=e?e.slice():[];for(var r=n,a=t;r;){r.parent=a,r.updateTransform(),e.push(r),a=r,r=r.clipPath;}}if(t.isGroup){for(var o=t._children,s=0;s<o[Z];s++){var l=o[s];t.__dirty&&(l.__dirty=!0),this._updateAndAddDisplayable(l,e,i);}t.__dirty=!1;}else t.__clipPaths=e,this._displayList[this._displayListLen++]=t;}},addRoot:function addRoot(t){this._elements[t.id]||(t instanceof r&&t.addChildrenToStorage(this),this.addToMap(t),this._roots.push(t));},delRoot:function delRoot(t){if(null==t){for(var e=0;e<this._roots[Z];e++){var n=this._roots[e];n instanceof r&&n.delChildrenFromStorage(this);}return this._elements={},this._roots=[],this._displayList=[],void(this._displayListLen=0);}if(t instanceof Array)for(var e=0,a=t[Z];a>e;e++){this.delRoot(t[e]);}else{var o;o=(typeof t==="undefined"?"undefined":(0,_typeof3.default)(t))==F?this._elements[t]:t;var s=i[O](this._roots,o);s>=0&&(this.delFromMap(o.id),this._roots[A](s,1),o instanceof r&&o.delChildrenFromStorage(this));}},addToMap:function addToMap(t){return t instanceof r&&(t.__storage=this),t.dirty(!1),this._elements[t.id]=t,this;},get:function get(t){return this._elements[t];},delFromMap:function delFromMap(t){var e=this._elements,i=e[t];return i&&(delete e[t],i instanceof r&&(i.__storage=null)),this;},dispose:function dispose(){this._elements=this._renderList=this._roots=null;},displayableSortFunc:e},o;}),e("zrender/mixin/Draggable",[pe],function(){function t(){this.on("mousedown",this._dragStart,this),this.on("mousemove",this._drag,this),this.on("mouseup",this._dragEnd,this),this.on("globalout",this._dragEnd,this);}return t[j]={constructor:t,_dragStart:function _dragStart(t){var e=t.target;e&&e.draggable&&(this._draggingTarget=e,e.dragging=!0,this._x=t.offsetX,this._y=t.offsetY,this.dispatchToElement(e,"dragstart",t.event));},_drag:function _drag(t){var e=this._draggingTarget;

}},_dragEnd:function _dragEnd(t){var e=this._draggingTarget;e&&(e.dragging=!1),this.dispatchToElement(e,"dragend",t.event),this._dropTarget&&this.dispatchToElement(this._dropTarget,"drop",t.event),this._draggingTarget=null,this._dropTarget=null;}},t;}),e("zrender/animation/requestAnimationFrame",[pe],function(){return(typeof window==="undefined"?"undefined":(0,_typeof3.default)(window))!==i&&(window.requestAnimationFrame||window.msRequestAnimationFrame||window.mozRequestAnimationFrame||window.webkitRequestAnimationFrame)||function(t){setTimeout(t,16);};}),e("zrender/core/event",[pe,"../mixin/Eventful","./env"],function(t){function e(t){return t.getBoundingClientRect?t.getBoundingClientRect():{left:0,top:0};}function n(t,e,i,n){

return e;}function o(t,e,i){c?t.addEventListener(e,i):t.attachEvent("on"+e,i);}function s(t,e,i){c?t.removeEventListener(e,i):t.detachEvent("on"+e,i);}var l=t("../mixin/Eventful"),u=t("./env"),c=(typeof window==="undefined"?"undefined":(0,_typeof3.default)(window))!==i&&!!window.addEventListener,h=c?function(t){t.preventDefault(),t.stopPropagation(),t.cancelBubble=!0;}:function(t){t.returnValue=!1,t.cancelBubble=!0;};return{clientToLocal:n,normalizeEvent:a,addEventListener:o,removeEventListener:s,stop:h,Dispatcher:l};}),e("zrender/core/GestureMgr",[pe,"./event"],function(t){function e(t){var e=t[1][0]-t[0][0],i=t[1][1]-t[0][1];return Math.sqrt(e*e+i*i);}function i(t){return[(t[0][0]+t[1][0])/2,(t[0][1]+t[1][1])/2];}var n=t("./event"),r=function r(){this._track=[];};r[j]={constructor:r,recognize:function recognize(t,e,i){return this._doTrack(t,e,i),this._recognize(t);},clear:function clear(){return this._track[Z]=0,this;},_doTrack:function _doTrack(t,e,i){var r=t.touches;if(r){for(var a={points:[],touches:[],target:e,event:t},o=0,s=r[Z];s>o;o++){var l=r[o],u=n.clientToLocal(i,l,{});a.points.push([u.zrX,u.zrY]),a.touches.push(l);}this._track.push(a);}},_recognize:function _recognize(t){for(var e in a){if(a.hasOwnProperty(e)){var i=a[e](this._track,t);if(i)return i;}}}};var a={pinch:function pinch(t,n){var r=t[Z];if(r){var a=(t[r-1]||{}).points,o=(t[r-2]||{}).points||a;if(o&&o[Z]>1&&a&&a[Z]>1){var s=e(a)/e(o);

var s;o=o||r.devicePixelRatio,(typeof t==="undefined"?"undefined":(0,_typeof3.default)(t))===F?s=i(t,N,a,o):n[R](t)&&(s=t,t=s.id),this.id=t,this.dom=s;

};return s[j]={constructor:s,elCount:0,__dirty:!0,initContext:function initContext(){this.ctx=this.dom[f]("2d"),this.ctx.dpr=this.dpr;},createBackBuffer:function createBackBuffer(){var t=this.dpr;this.domBack=i("back-"+this.id,N,this.painter,t),this.ctxBack=this.domBack[f]("2d"),1!=t&&this.ctxBack.scale(t,t);},resize:function resize(t,e){

r++){e(n[r]);}var a=t.markPoint;if(a&&a.data)for(var o=a.data,r=0;r<o[Z];r++){e(o[r]);}var s=t.markLine;if(s&&s.data)for(var l=s.data,r=0;r<l[Z];r++){i[w](l[r])?(e(l[r][0]),e(l[r][1])):e(l[r]);}}}};}),e("echarts/data/DataDiffer",[pe],function(){function t(t){return t;}function e(e,i,n,r){this._old=e,this._new=i,this._oldKeyGetter=n||t,this._newKeyGetter=r||t;}function i(t,e,i,n){for(var r=0;r<t[Z];r++){var a=n(t[r],r),o=e[a];null==o?(i.push(a),e[a]=r):(o[Z]||(e[a]=o=[o]),o.push(r));}}return e[j]={constructor:e,add:function add(t){return this._add=t,this;},update:function update(t){return this._update=t,this;},remove:function remove(t){return this._remove=t,this;},execute:function execute(){

}else this._remove&&this._remove(t);}for(var t=0;t<u[Z];t++){var c=u[t];if(s.hasOwnProperty(c)){var h=s[c];if(null==h)continue;if(h[Z])for(var d=0,f=h[Z];f>d;d++){this._add&&this._add(h[d]);}else this._add&&this._add(h);}}}},e;}),e("echarts/coord/cartesian/Axis2D",[pe,de,"../Axis","./axisLabelInterval"],function(t){var e=t(de),i=t("../Axis"),n=t("./axisLabelInterval"),r=function r(t,e,n,_r,a){i.call(this,t,e,n),this.type=_r||"value",this[Q]=a||K;};return r[j]={constructor:r,index:0,onZero:!1,model:null,isHorizontal:function isHorizontal(){var t=this[Q];return"top"===t||t===K;},getGlobalExtent:function getGlobalExtent(){var t=this[y]();return t[0]=this.toGlobalCoord(t[0]),t[1]=this.toGlobalCoord(t[1]),t;},getLabelInterval:function getLabelInterval(){var t=this._labelInterval;return t||(t=this._labelInterval=n(this)),t;},isLabelIgnored:function isLabelIgnored(t){if("category"===this.type){var e=this.getLabelInterval();return(typeof e==="undefined"?"undefined":(0,_typeof3.default)(e))===b&&!e(t,this.scale.getLabel(t))||t%(e+1);}},toLocalCoord:null,toGlobalCoord:null},e[_](r,i),r;}

:c;}}return t;}function i(t){return n[w](t)?t:n[R](t)?t.value:t;}var n=t(de),r=e.guessOrdinal=function(t,e){for(var r=0,a=t[Z];a>r;r++){var o=i(t[r]);if(!n[w](o))return!1;

},l.niceScaleExtent=function(t,e){var i=t.scale,n=l.getScaleExtent(t,e),r=null!=e.getMin(),a=null!=e.getMax(),o=e.get("splitNumber");

var h=(n[1]+n[0])/2;i.setExtent(c*(n[0]-h)+h,c*(n[1]-h)+h),i.niceExtent(o);}var u=e.get("interval");null!=u&&i.setInterval&&i.setInterval(u);},l.createScaleByModel=function(t,r){if(r=r||t.get("type"))switch(r){case"category":return new e(t.getCategories(),[1/0,-1/0]);case"value":return new i();default:return(n.getClass(r)||i)[V](t);}},l.ifAxisCrossZero=function(t){

return 0===a&&l>1?l:(a+1)*l-1;},l.getFormattedLabels=function(t,e){var i=t.scale,n=i.getTicksLabels(),r=i.getTicks();return(typeof e==="undefined"?"undefined":(0,_typeof3.default)(e))===F?(e=function(t){return function(e){return t[T]("{value}",null!=e?e:"");};}(e),o.map(n,e)):(typeof e==="undefined"?"undefined":(0,_typeof3.default)(e))===b?o.map(r,function(n,r){

},dataToPoints:function dataToPoints(t,e){return t.mapArray(["x","y"],function(t,e){return this.dataToPoint([t,e]);},e,this);},dataToPoint:function dataToPoint(t,e){

},getOtherAxis:function getOtherAxis(t){return this.getAxis("x"===t.dim?"y":"x");}},i[_](e,n),e;}),e("echarts/component/helper/selectableMixin",[pe,de],function(t){var e=t(de);return{updateSelectedMap:function updateSelectedMap(t){this._selectTargetMap=e.reduce(t||[],function(t,e){return t[e.name]=e,t;},{});},select:function select(t){var i=this._selectTargetMap,n=i[t],r=this.get("selectedMode");"single"===r&&e.each(i,function(t){t.selected=!1;}),n&&(n.selected=!0);},unSelect:function unSelect(t){var e=this._selectTargetMap[t];e&&(e.selected=!1);},toggleSelected:function toggleSelected(t){var e=this._selectTargetMap[t];return null!=e?(this[e.selected?"unSelect":"select"](t),e.selected):void 0;},isSelected:function isSelected(t){var e=this._selectTargetMap[t];return e&&e.selected;}};}),e("echarts/chart/helper/createListFromArray",[pe,"../../data/List","../../data/helper/completeDimensions",de,"../../util/model","../../CoordinateSystem"],function(t){function e(t){for(var e=0;e<t[Z]&&null==t[e];){e++;}return t[e];}function i(t){var i=e(t);return null!=i&&!f[w](v(i));}function n(t,e,n){t=t||[];var r=e.get(he),a=g[r],o=p.get(r),y=a&&a(t,e,n),_=y&&y[l];_||(_=o&&o[l]||["x","y"],_=c(_,t,_[h](["value"])));var x=y?y.categoryIndex:-1,b=new u(_,e),w=s(y,t),T={},M=x>=0&&i(t)?function(t,e,i,n){

(typeof a==="undefined"?"undefined":(0,_typeof3.default)(a))===F&&(T[e]=T[e]||o[e].getCategories(),a=f[O](T[e],a),0>a&&!isNaN(a)&&(a=+a)),a;};return b.hasItemOption=!1,b.initData(t,w,M),b;}function r(t){return"category"!==t&&"time"!==t;}function o(t){return"category"===t?a:"time"===t?"time":"float";}function s(t,e){var i,n=[],r=t&&t[l][t.categoryIndex];if(r&&(i=t.categoryAxesModels[r.name]),i){var a=i.getCategories();if(a){var o=e[Z];if(f[w](e[0])&&e[0][Z]>1){n=[];for(var s=0;o>s;s++){n[s]=a[e[s][t.categoryIndex||0]];}}else n=a.slice(0);}}return n;}var u=t("

var i=e.cx,r=e.cy,a=e.width/2,o=e[re]/2;t[n](i,r-o),t.lineTo(i+a,r+o),t.lineTo(i-a,r+o),t.closePath();}}),a=e.extendShape({type:"diamond",shape:{cx:0,cy:0,width:0,height:0},buildPath:function buildPath(t,e){var i=e.cx,r=e.cy,a=e.width/2,o=e[re]/2;t[n](i,r-o),t.lineTo(i+a,r),t.lineTo(i,r+o),t.lineTo(i-a,r),t.closePath();}}),o=e.extendShape({type:"pin",shape:{x:0,y:0,width:0,height:0},buildPath:function buildPath(t,e){var i=e.x,n=e.y,r=e.width/5*3,a=Math.max(r,e[re]),o=r/2,s=o*o/(a-o),l=n-a+o+s,u=Math.asin(s/o),c=Math.cos(u)*o,h=Math.sin(u),f=Math.cos(u);t.arc(i,l,o,Math.PI-u,2*Math.PI+u);var d=.6*o,p=.7*o;t.bezierCurveTo(i+c-h*d,l+s+f*d,i,n-p,i,n),t.bezierCurveTo(i,n-p,i-c+h*d,l+s+f*d,i-c,l+s),t.closePath();}}),s=e.extendShape({type:"arrow",shape:{x:0,y:0,width:0,height:0},buildPath:function buildPath(t,e){var i=e[re],r=e.width,a=e.x,o=e.y,s=r/3*2;t[n](a,o),t.lineTo(a+s,o+i),t.lineTo(a,o+i/4*3),t.lineTo(a-s,o+i),t.lineTo(a,o),t.closePath();

r.x1=t,r.y1=e+n/2,r.x2=t+i,r.y2=e+n/2;},rect:function rect(t,e,i,n,r){r.x=t,r.y=e,r.width=i,r[re]=n;},roundRect:function roundRect(t,e,i,n,r){r.x=t,r.y=e,r.width=i,r[re]=n,r.r=Math.min(i,n)/4;},square:function square(t,e,i,n,r){var a=Math.min(i,n);r.x=t,r.y=e,r.width=a,r[re]=a;},circle:function circle(t,e,i,n,r){r.cx=t+i/2,r.cy=e+n/2,r.r=Math.min(i,n)/2;},diamond:function diamond(t,e,i,n,r){r.cx=t+i/2,r.cy=e+n/2,r.width=i,r[re]=n;},pin:function pin(t,e,i,n,r){r.x=t+i/2,r.y=e+n/2,r.width=i,r[re]=n;},arrow:function arrow(t,e,i,n,r){r.x=t+i/2,r.y=e+n/2,r.width=i,r[re]=n;},triangle:function triangle(t,e,i,n,r){r.cx=t+i/2,r.cy=e+n/2,r.width=i,r[re]=n;}},c={};for(var h in l){l.hasOwnProperty(h)&&(c[h]=new l[h]());}var f=e.extendShape({type:"symbol",shape:{symbolType:"",x:0,y:0,width:0,height:0},beforeBrush:function beforeBrush(){var t=this.style,e=this.shape;"pin"===e.symbolType&&t.textPosition===p&&(t.textPosition=["50%","40%"],t[le]=J,t.textVerticalAlign=te);},buildPath:function buildPath(t,e,i){

}).join(";");}function i(t){var e=[],i=t.get("fontSize"),n=t[oe]();return n&&e.push("color:"+n),e.push("font:"+t[se]()),i&&e.push("line-height:"+Math.round(3*i/2)+"px"),u(["decoration","align"],function(i){var n=t.get(i);n&&e.push("text-"+i+":"+n);}),e.join(";");}function n(t){t=t;var n=[],r=t.get("transitionDuration"),a=t.get("backgroundColor"),s=t[ce](ue),f=t.get("padding");

},i.onmousemove=function(e){if(e=e||window.event,!r.enterable){var i=n.handler;s.normalizeEvent(t,e,!0),i.dispatch("mousemove",e);}}

},setContent:function setContent(t){var e=this.el;e.innerHTML=t,e.style.display=t?"block":"none";},moveTo:function moveTo(t,e){var i=this.el.style;i.left=t+"px",i.top=e+"px",this._x=t,this._y=e;},hide:function hide(){this.el.style.display="none",this._show=!1;},hideLater:function hideLater(t){!this._show||this._inContent&&this.enterable||(t?(this._hideDelay=t,this._show=!1,this._hideTimeout=setTimeout(a.bind(this.hide,this),t)):this.hide());},isShow:function isShow(){return this._show;}},r;}),e("echarts/scale/Ordinal",[pe,de,"./Scale"],function(t){var e=t(de),i=t("./Scale"),n=i[j],r=i[X]({type:"ordinal",init:function init(t,e){this._data=t,this[W]=e||[0,t[Z]-1];},parse:function parse(t){return(typeof t==="undefined"?"undefined":(0,_typeof3.default)(t))===F?e[O](this._data,t):Math.round(t);},contain:function contain(t){return t=this.parse(t),n[U].call(this,t)&&null!=this._data[t];},normalize:function normalize(t){return n[H].call(this,this.parse(t));},scale:function scale(t){return Math.round(n.scale.call(this,t));},getTicks:function getTicks(){for(var t=[],e=this[W],i=e[0];i<=e[1];){t.push(i),i++;}return t;},getLabel:function getLabel(t){return this._data[t];},count:function count(){return this[W][1]-this[W][0]+1;},unionExtentFromData:function unionExtentFromData(t,e){this.unionExtent(t.getDataExtent(e,!1));},niceTicks:e.noop,niceExtent:e.noop});return r[V]=function(){return new r();},r;}),e("echarts/chart/pie/labelLayout",[pe,"zrender/contain/text"],function(t){

e&&f>=o&&(f=o-10),!e&&o>=f&&(f=o+10),t[s].x=i+f*a,o=f;}}}t.sort(function(t,e){return t.y-e.y;});for(var c,h=0,f=t[Z],d=[],p=[],v=0;f>v;v++){c=t[v].y-h,0>c&&s(v,f,-c,r),h=t[v].y+t[v][re];}0>o-h&&l(f-1,h-o);for(var v=0;f>v;v++){t[v].y>=i?p.push(t[v]):d.push(t[v]);}u(d,!1,e,i,n,r),u(p,!0,e,i,n,r);}function i(t,i,n,r,a,o){for(var s=[],l=[],u=0;u<t[Z];u++){t[u].x<i?s.push(t[u]):l.push(t[u]);}e(l,i,n,r,1,a,o),e(s,i,n,r,-1,a,o);for(var u=0;u<t[Z];u++){var c=t[u].linePoints;if(c){var h=c[1][0]-c[2][0];c[2][0]=t[u].x<i?t[u].x+3:t[u].x-3,c[1][1]=c[2][1]=t[u].y,c[1][0]=c[2][0]+h;}

}),!f&&t.get("avoidLabelOverlap")&&i(h,l,u,e,r,a);};}),e("echarts/chart/helper/SymbolDraw",[pe,"../../util/graphic","./Symbol"],function(t){function e(t){this.group=new n.Group(),this._symbolCtor=t||r;}function i(t,e,i){var n=t.getItemLayout(e);return!(!n||isNaN(n[0])||isNaN(n[1])||i&&i(e)||"none"===t[k](e,"symbol"));}var n=t("../../util/graphic"),r=t("./Symbol"),a=e[j];return a.updateData=function(t,e){var r=this.group,a=t.hostModel,o=this._data,s=this._symbolCtor,l={itemStyle:a[ce]("itemStyle.normal").getItemStyle(["color"]),hoverItemStyle:a[ce]("itemStyle.emphasis").getItemStyle(),symbolRotate:a.get("symbolRotate"),symbolOffset:a.get("symbolOffset"),hoverAnimation:a.get("hoverAnimation"),labelModel:a[ce]("label.normal"),hoverLabelModel:a[ce]("label.emphasis")};t.diff(o).add(function(n){var a=t.getItemLayout(n);if(i(t,n,e)){var o=new s(t,n,l);o.attr(Q,a),t.setItemGraphicEl(n,o),r.add(o);}})[I](function(u,c){var h=o.getItemGraphicEl(c),f=t.getItemLayout(u);return i(t,u,e)?(h?(h.updateData(t,u,l),n.updateProps(h,{position:f},a)):(h=new s(t,u),h.attr(Q,f)),r.add(h),void t.setItemGraphicEl(u,h)):void r.remove(h);}).remove(function(t){var e=o.getItemGraphicEl(t);e&&e.fadeOut(function(){r.remove(e);});}).execute(),this._data=t;},a.updateLayout=function(){var t=this._data;t&&t.eachItemGraphicEl(function(e,i){var n=t.getItemLayout(i);e.attr(Q,n);});},a.remove=function(t){var e=this.group,i=this._data;i&&(t?i.eachItemGraphicEl(function(t){t.fadeOut(function(){e.remove(t);});}):e[fe]());},e;}),e("echarts/chart/helper/Symbol",[pe,de,"../../util/symbol","../../util/graphic","../../util/number"],function(t){function e(t,e){var i=t[k](e,"symbolSize");return i instanceof Array?i.slice():[+i,+i];}function i(t){return[t[0]/2,t[1]/2];}function n(t,e,i){h.Group.call(this),this.updateData(t,e,i);}function r(t,e){this.parent.drift(t,e);}var s=t(de),c=t("../../util/symbol"),h=t("../../util/graphic"),f=t("../../util/number"),p=n[j];

},p.stopSymbolAnimation=function(t){this.childAt(0).stopAnimation(t);},p.getSymbolPath=function(){return this.childAt(0);},p.getScale=function(){return this.childAt(0).scale;},p.highlight=function(){this.childAt(0).trigger("emphasis");},p.downplay=function(){this.childAt(0).trigger("normal");},p.setZ=function(t,e){var i=this.childAt(0);i[M]=t,i.z=e;},p.setDraggable=function(t){var e=this.childAt(0);e.draggable=t,e.cursor=t?"move":"pointer";},p.updateData=function(t,n,r){this[ne]=!1;var a=t[k](n,"symbol")||"circle",o=t.hostModel,s=e(t,n);if(a!==this._symbolType)this._createSymbol(a,t,n,s);else{var l=this.childAt(0);h.updateProps(l,{scale:i(s)},o,n);}this._updateCommon(t,n,s,r),this._seriesModel=o;};var m=["itemStyle","normal"],g=["itemStyle","emphasis"],y=["label","normal"],x=["label","emphasis"];

}else w=s[X]({},w);var I=c.style;c.attr(d,(T||0)*Math.PI/180||0),M&&c.attr(Q,[f.parsePercent(M[0],n[0]),f.parsePercent(M[1],n[1])]),c.setColor(_),c[$](b);var z=t[k](e,Y);null!=z&&(I[Y]=z);for(var D,O,E=t[l].slice();

,"emphasis"),t.get(D,e))):w.text="",c.off("mouseover").off(C).off("emphasis").off("normal"),c.hoverStyle=w,h.setHoverStyle(c);var R=i(n);if(P&&p.isAnimationEnabled()){var B=function B(){var t=R[1]/R[0];this.animateTo({scale:[Math.max(1.1*R[0],R[0]+3),Math.max(1.1*R[1],R[1]+3*t)]},400,"elasticOut");},N=function N(){this.animateTo({scale:R},400,"elasticOut");};c.on("mouseover",B).on(C,N).on("emphasis",B).on("normal",N);}},p.fadeOut=function(t){var e=this.childAt(0);this[ne]=!0,e.style.text="",h.updateProps(e,{

c&&t(c.get(l,n))===t(h);){r=c;break;}var f=[];return f[u]=i.get(a.dim,n),f[1-u]=r?r.get(l,n,!0):s,e.dataToPoint(f);}function i(t,e){var i=[];return e.diff(t).add(function(t){i.push({cmd:"+",idx:t});})[I](function(t,e){i.push({cmd:"=",idx:e,idx1:t});}).remove(function(t){i.push({cmd:"-",idx:t});}).execute(),i;}return function(t,n,r,a,o,s){for(var u=i(t,n),c=[],h=[],f=[],d=[],p=[],v=[],m=[],g=s[l],y=0;y<u[Z];y++){var _=u[y],x=!0;

}x&&(p.push(_),v.push(v[Z]));}v.sort(function(t,e){return m[t]-m[e];});for(var S=[],C=[],A=[],P=[],L=[],y=0;y<v[Z];y++){var T=v[y];S[y]=c[T],C[y]=h[T],A[y]=f[T],P[y]=d[T],L[y]=p[T];}return{current:S,next:C,stackedOnCurrent:A,stackedOnNext:P,status:L};};}),e("echarts/chart/line/poly",[pe,"zrender/graphic/Path","zrender/core/vector"],function(t){function e(t){return isNaN(t[0])||isNaN(t[1]);}function i(t,i,r,a,p,v,m,g,y,_,x){for(var b=0,w=r,T=0;a>T;T++){var M=i[w];if(w>=p||0>w)break;if(e(M)){if(x){w+=v;continue;}break;}if(w===r)t[v>0?n:"lineTo"](M[0],M[1]),c(f,M);else if(y>0){var S=w+v,C=i[S];if(x)for(;C&&e(i[S]);){S+=v,C=i[S];}var A=.5,P=i[b],C=i[S];if(!C||e(C))c(d,M);else{e(C)&&!x&&(C=M),o.sub(h,C,P);var L,I;if("x"===_||"y"===_){var k="x"===_?0:1;L=Math.abs(M[k]-P[k]),I=Math.abs(M[k]-C[k]);}else L=o.dist(M,P),I=o.dist(M,C);A=I/(I+L),u(d,M,h,-y*(1-A));}s(f,f,g),l(f,f,m),s(d,d,g),l(d,d,m),t.bezierCurveTo(f[0],f[1],d[0],d[1],M[0],M[1]),u(f,M,h,y*A);}else t.lineTo(M[0],M[1]);b=w,w+=v;}return T;}function r(t,e){var i=[1/0,1/0],n=[-1/0,-1/0];if(e)for(var r=0;r<t[Z];r++){var a=t[r];a[0]<i[0]&&(i[0]=a[0]),a[1]<i[1]&&(i[1]=a[1]),a[0]>n[0]&&(n[0]=a[0]),a[1]>n[1]&&(n[1]=a[1]);}return{min:e?i:n,max:e?n:i};}var a=t("zrender/graphic/Path"),o=t("zrender/core/vector"),s=o.min,l=o.max,u=o.scaleAndAdd,c=o.copy,h=[],f=[],d=[];return{Polyline:a[X]({type:"ec-polyline",shape:{points:[],smooth:0,smoothConstraint:!0,smoothMonotone:null,connectNulls:!1},style:{fill:null,stroke:"#000"},buildPath:function buildPath(t,n){var a=n.points,o=0,s=a[Z],l=r(a,n.smoothConstraint);if(n.connectNulls){for(;s>0&&e(a[s-1]);s--){}for(;s>o&&e(a[o]);o++){}}for(;s>o;){o+=i(t,a,o,s,s,1,l.min,l.max,n.smooth,n.smoothMonotone,n.connectNulls)+1;}}

l--){}for(;l>s&&e(a[s]);s++){}}for(;l>s;){var f=i(t,a,s,l,l,1,c.min,c.max,n.smooth,u,n.connectNulls);i(t,o,s+f-1,f,l,-1,h.min,h.max,n.stackedOnSmooth,u,n.connectNulls),s+=f+1,t.closePath();}}})};}),e("echarts/coord/cartesian/Cartesian",[pe,de],function(t){function e(t){return this._axes[t];}var i=t(de),n=function n(t){this._axes={},this._dimList=[],this.name=t||"";};return n[j]={constructor:n,type:"cartesian",getAxis:function getAxis(t){return this._axes[t];},getAxes:function getAxes(){return i.map(this._dimList,e,this);},getAxesByScale:function getAxesByScale(t){return t=t[G](),i.filter(this.getAxes(),function(e){return e.scale.type===t;});},addAxis:function addAxis(t){var e=t.dim;this._axes[e]=t,this._dimList.push(e);},dataToCoord:function dataToCoord(t){return this._dataCoordConvert(t,"dataToCoord");},coordToData:function coordToData(t){return this._dataCoordConvert(t,"coordToData");},_dataCoordConvert:function _dataCoordConvert(t,e){for(var i=this._dimList,n=t instanceof Array?[]:{},r=0;r<i[Z];r++){var a=i[r],o=this._axes[a];n[a]=o[e](t[a]);}return n;}},n;}),e("echarts/coord/Axis",[pe,"../util/number",de],function(t){function e(t,e){var i=t[1]-t[0],n=e,r=i/n/2;t[0]+=r,t[1]-=r;}var i=t("../util/number"),n=i.linearMap,r=t(de),o=[0,1],s=function s(t,e,i){this.dim=t,this.scale=e,this[W]=i||[0,0],this.inverse=!1,this.onBand=!1;};return s[j]={constructor:s,contain:function contain(t){var e=this[W],i=Math.min(e[0],e[1]),n=Math.max(e[0],e[1]);return t>=i&&n>=t;},containData:function containData(t){return this[U](this.dataToCoord(t));},getExtent:function getExtent(){var t=this[W].slice();return t;},getPixelPrecision:function getPixelPrecision(t){return i.getPixelPrecision(t||this.scale[y](),this[W]);},setExtent:function setExtent(t,e){var i=this[W];i[0]=t,i[1]=e;},dataToCoord:function dataToCoord(t,i){var r=this[W],s=this.scale;return t=s[H](t),this.onBand&&s.type===a&&(r=r.slice(),e(r,s.count())),n(t,o,r,i);},coordToData:function coordToData(t,i){var r=this[W],s=this.scale;this.onBand&&s.type===a&&(r=r.slice(),e(r,s.count()));var l=n(t,r,o,i);return this.scale.scale(l);},getTicksCoords:function getTicksCoords(t){if(this.onBand&&!t){for(var e=this.getBands(),i=[],n=0;n<e[Z];n++){i.push(e[n][0]);}return e[n-1]&&i.push(e[n-1][1]),i;}return r.map(this.scale.getTicks(),this.dataToCoord,this);},getLabelsCoords:function getLabelsCoords(){return r.map(this.scale.getTicks(),this.dataToCoord,this);},getBands:function getBands(){for(var t=this[y](),e=[],i=this.scale.count(),n=t[0],r=t[1],a=r-n,o=0;i>o;o++){e.push([a*o/i+n,a*(o+1)/i+n]);}return e;},getBandWidth:function getBandWidth(){var t=this[W],e=this.scale[y](),i=e[1]-e[0]+(this.onBand?1:0);0===i&&(i=1);var n=Math.abs(t[1]-t[0]);return Math.abs(n)/i;},isBlank:function isBlank(){return this._isBlank;},setBlank:function setBlank(t){this._isBlank=t;}},s;}

};}),e("echarts/coord/cartesian/AxisModel",[pe,"../../model/Component",de,"../axisModelCreator","../axisModelCommonMixin"],function(t){function e(t,e){return e.type||(e.data?"category":"value");}var i=t("../../model/Component"),n=t(de),a=t("../axisModelCreator"),o=i[X]({type:"cartesian2dAxis",axis:null,init:function init(){o.superApply(this,"init",arguments),this.resetRange();},mergeOption:function mergeOption(){o.superApply(this,"mergeOption",arguments),this.resetRange();},restoreData:function restoreData(){o.superApply(this,"restoreData",arguments),this.resetRange();},getCoordSysModel:function getCoordSysModel(){return this[r].queryComponents({mainType:"grid",index:this[c].gridIndex,id:this[c].gridId})[0];}});n.merge(o[j],t("../axisModelCommonMixin"));var s={offset:0};return a("x",o,e,s),a("y",o,e,s),o;}),e("echarts/coord/axisModelCommonMixin",[pe,de,"./axisHelper"],function(t){function e(t){return i[R](t)&&null!=t.value?t.value:t;}var i=t(de),n=t("./axisHelper");return{getFormattedLabels:function getFormattedLabels(){return n.getFormattedLabels(this.axis,this.get("axisLabel.formatter"));},getCategories:function getCategories(){return"category"===this.get("type")&&i.map(this.get("data"),e);},getMin:function getMin(t){var e=this[c],n=t||null==e.rangeStart?e.min:e.rangeStart;return null==n||"dataMin"===n||i.eqNaN(n)||(n=this.axis.scale.parse(n)),n;},getMax:function getMax(t){var e=this[c],n=t||null==e.rangeEnd?e.max:e.rangeEnd;return null==n||"dataMax"===n||i.eqNaN(n)||(n=this.axis.scale.parse(n)),n;},getNeedCrossZero:function getNeedCrossZero(){var t=this[c];return null!=t.rangeStart||null!=t.rangeEnd?!1:!t.scale;},getCoordSysModel:i.noop,setRange:function setRange(t,e){this[c].rangeStart=t,this[c].rangeEnd=e;},resetRange:function resetRange(){this[c].rangeStart=this[c].rangeEnd=null;}};}),e("echarts/coord/axisModelCreator",[pe,"./axisDefault",de,"../model/Component","../util/layout"],function(t){

var o=this.layoutMode,l=o?r.getLayoutParams(e):{},u=a.getTheme();i.merge(e,u.get(n+"Axis")),i.merge(e,this.getDefaultOption()),e.type=s(t,e),o&&r.mergeLayoutParam(e,l,o);},defaultOption:i.mergeAll([{},e[n+"Axis"],l],!0)});}),n.registerSubTypeDefaulter(t+"Axis",i.curry(s,t));};}),e("echarts/coord/axisDefault",[pe,de],function(t){

scale:!0,min:"dataMin",max:"dataMax"},r),o=e.defaults({logBase:10},r);return o.scale=!0,{categoryAxis:n,valueAxis:r,timeAxis:a,logAxis:o};}),e("echarts/component/axis/AxisView",[pe,de,"../../util/graphic","./AxisBuilder","../../echarts"],function(t){function e(t,e){function i(t){var e=n.getAxis(t);return e.toGlobalCoord(e.dataToCoord(0));}var n=t[he],r=e.axis,a={

var m=e[ce]("axisLabel").get("rotate");return a.labelRotation="top"===s?-m:m,a.labelInterval=r.getLabelInterval(),a.z2=1,a;}var i=t(de),n=t("../../util/graphic"),r=t("./AxisBuilder"),a=r.ifIgnoreOnTick,o=r.getInterval,s=["axisLine","axisLabel","axisTick","axisName"],l=["splitArea","splitLine"],u=t("../../echarts").extendComponentView({type:"axis",render:function render(t){this.group[fe]();var a=this._axisGroup;if(this._axisGroup=new n.Group(),this.group.add(this._axisGroup),t.get("show")){var o=t.getCoordSysModel(),u=e(o,t),c=new r(t,u);i.each(s,c.add,c),this._axisGroup.add(c.getGroup()),i.each(l,function(e){t.get(e+".show")&&this["_"+e](t,o,u.labelInterval);},this),n.groupTransition(a,this._axisGroup,t);}},_splitLine:function _splitLine(t,e,r){var s=t.axis;if(!s.isBlank()){var l=t[ce]("splitLine"),u=l[ce]("lineStyle"),c=u.get("color"),h=o(l,r);

this.opt=e,this.axisModel=t,s.defaults(e,{labelOffset:0,nameDirection:1,tickDirection:1,labelDirection:1,silent:!0}),this.group=new c.Group();var i=new c.Group({position:e[Q].slice(),rotation:e[d]});i.updateTransform(),this._transform=i.transform,this._dumbGroup=i;};T[j]={constructor:T,hasBuilder:function hasBuilder(t){return!!M[t];},add:function add(t){M[t].call(this);},getGroup:function getGroup(){return this.group;}};var M={axisLine:function axisLine(){

)));}}},axisLabel:function axisLabel(){function t(t,e){var i=t&&t[ae]().clone(),n=e&&e[ae]().clone();return i&&n?(i[g](t.getLocalTransform()),n[g](e.getLocalTransform()),i.intersect(n)):void 0;}var n=this.opt,a=this.axisModel,l=a.axis,u=x(n.axisLabelShow,a.get("axisLabel.show"));

,position:f,rotation:T[d],silent:A,z2:10});P&&(g.eventData=e(a),g.eventData.targetType="axisLabel",g.eventData.value=m),this._dumbGroup.add(g),g.updateTransform(),C.push(g),this.group.add(g),g.decomposeTransform();}},this),null!=a.getMin()){var L=C[0],I=C[1];t(L,I)&&(L[E]=!0);}if(null!=a.getMax()){var k=C[C[Z]-1],z=C[C[Z]-2];t(z,k)&&(k[E]=!0);}}},axisName:function axisName(){

},C=T.getInterval=function(t,e){var i=t.get("interval");return(null==i||"auto"==i)&&(i=e),i;};return T;}),e("zrender",["zrender/zrender"],function(t){return t;}),e("echarts",["echarts/echarts"],function(t){return t;});var ve=t("echarts");return ve.graphic=t("echarts/util/graphic"),ve.number=t("echarts/util/number"),ve.format=t("echarts/util/format"),t("echarts/chart/line"),t("echarts/chart/pie"),t("echarts/component/grid"),t("echarts/component/title"),t("echarts/component/legend"),t("echarts/component/tooltip"),ve;});
	/* WEBPACK VAR INJECTION */}.call(exports, (function() { return this; }())))

/***/ },
/* 195 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page manage\">\n    <header id=\"header\" class=\"mui-bar mui-bar-nav\">\n        <h1 class=\"mui-title\">报表</h1>\n        <!-- <a class=\"mui-action-back mui-icon mui-icon-left-nav mui-pull-left\"></a> -->\n    </header>\n    \n    <div class=\"mui-scroll-wrapper\">\n    <div class=\"mui-scroll\">\n    <div id=\"sliderSegmentedControl\" class=\"mui-slider-indicator mui-segmented-control mui-segmented-control-inverted\">\n        <a class=\"winslow-control-item {{activeChart == 'today' ? 'mui-active' : ''}}\" @click=\"activeChart = 'today'\">日报</a>\n        <a class=\"winslow-control-item {{activeChart == 'week' ? 'mui-active' : ''}}\" @click=\"activeChart = 'week'\">周报</a>\n        <a class=\"winslow-control-item {{activeChart == 'month' ? 'mui-active' : ''}}\" @click=\"activeChart = 'month'\">月报</a>\n    </div>\n    <div class=\"title\">{{chartInfo[activeChart].title}}：{{sohru}}元</div>\n    <div class=\"winslow-container\">\n        <div id=\"lineChart\" style=\"width: 100%;height: 200px;\" v-show=\"lineDate.key.length > 0\"></div>\n        <div class=\"no-data\" v-if=\"lineDate.key.length<=0\">\n            <h6>还没有数据~</h6>\n        </div>\n    </div>\n    <div class=\"title\">项目营收比例</div>\n    <div class=\"winslow-container\">\n        <div id=\"pieChart\" style=\"width: 100%;height: 200px;\" v-show=\"pieDate.key.length > 0\"></div>\n        <div class=\"no-data\" v-if=\"pieDate.key.length<=0\">\n            <h6>还没有数据~</h6>\n        </div>\n    </div>\n    </div></div>\n\n    <nav class=\"mui-bar mui-bar-tab\">\n        <a class=\"winslow-tab-item\" @click=\"local('manage-shop')\">\n            <span class=\"iconfont\">&#xe67d;</span>\n            <span class=\"mui-tab-label\">管理</span>\n        </a>\n        <a class=\"winslow-tab-item mui-active\">\n            <span class=\"iconfont\">&#xe671;</span>\n            <span class=\"mui-tab-label\">报表</span>\n        </a>\n        <a class=\"winslow-tab-item\" @click=\"local('manage-shop-comment')\">\n            <span class=\"iconfont\">&#xe706;</span>\n            <span class=\"mui-tab-label\">店铺评价</span>\n        </a>\n    </nav>\n    \n</div>\n";

/***/ },
/* 196 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(197)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/manage/tech.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(198)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-b257a096/tech.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 197 */
/***/ function(module, exports) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	// <template>
	// <div class="page manage">
	//     <header id="header" class="mui-bar mui-bar-nav">
	//         <h1 class="mui-title">技师管理</h1>
	//         <!-- <a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a> -->
	//     </header>
	//
	//     <div class="mui-scroll-wrapper">
	//     <div class="mui-scroll">
	//
	//     <div id="sliderSegmentedControl" class="mui-slider-indicator mui-segmented-control mui-segmented-control-inverted">
	//         <a class="winslow-control-item" @click="local('manage-shop')" >店铺信息</a>
	//         <a class="winslow-control-item" @click="local('manage-project')">项目管理</a>
	//         <a class="winslow-control-item mui-active" @click="local('manage-tech')">技师管理</a>
	//     </div>
	//
	//     <div class="title" @click="showCheck=!showCheck"><i class="ico-down {{!showCheck?'up':''}}"></i>待审核({{wait}})</div>
	//     <ul class="mui-table-view item-container" v-show="showCheck">
	//         <li class="mui-table-view-cell mui-media" v-show="showHave" v-for="item in techList" v-if="item.Unread">
	//             <a v-link="{path: '/manage-tech-info/'+$route.params.token+'/'+item.massagist_id}">
	//                 <img class="mui-media-object mui-pull-left" :src="item.massagist_photo">
	//                 <div class="mui-media-body">
	//                     工号:{{item.massagist_no}}&nbsp;&nbsp;&nbsp;&nbsp;昵称:{{item.massagist_name}}
	//                     <p class="mui-ellipsis">申请类别: 项目调整</p>
	//                 </div>
	//                 <i class="status-text">{{item.Unread | format 'yyyy-MM-dd'}}</i>
	//                 <i class="status wait">未审核</i>
	//             </a>
	//         </li>
	//     </ul>
	//
	//     <div class="title" @click="showHave=!showHave"><i class="ico-down {{!showHave?'up':''}}"></i>技师列表({{techList.length}})</div>
	//     <ul class="mui-table-view item-container" v-show="showHave">
	//         <li class="mui-table-view-cell mui-media" v-for="item in techList">
	//             <a v-link="{path: '/manage-tech-info/'+$route.params.token+'/'+item.massagist_id}">
	//                 <img class="mui-media-object mui-pull-left" :src="item.massagist_photo">
	//                 <div class="mui-media-body center">
	//                     工号:{{item.massagist_no}}&nbsp;&nbsp;&nbsp;&nbsp;昵称:{{item.massagist_name}}
	//                 </div>
	//             </a>
	//         </li>
	//     </ul>
	//
	//     </div></div>
	//
	//     <nav class="mui-bar mui-bar-tab">
	//         <a class="winslow-tab-item mui-active">
	//             <span class="iconfont">&#xe67d;</span>
	//             <span class="mui-tab-label">管理</span>
	//         </a>
	//         <a class="winslow-tab-item" @click="local('manage-shop-chart')">
	//             <span class="iconfont">&#xe671;</span>
	//             <span class="mui-tab-label">报表</span>
	//         </a>
	//         <a class="winslow-tab-item" @click="local('manage-shop-comment')">
	//             <span class="iconfont">&#xe706;</span>
	//             <span class="mui-tab-label">店铺评价</span>
	//         </a>
	//     </nav>
	//
	//
	// </div>
	// </template>
	//
	// <script>
	exports.default = {
	    name: 'manageTech',
	    data: function data() {
	        return {
	            showCheck: true,
	            showHave: true,
	            techList: [],
	            wait: 0
	        };
	    },
	    ready: function ready() {
	        var _this = this;
	
	        document.title = '技师管理';
	        mui('.mui-scroll-wrapper').scroll({ bounce: false });
	        this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	        this.$http.post(API.getTList, {}).then(function (data) {
	            if (data.data.errorInfo) return;
	            _this.$store.state.hideTip();
	            _this.techList = data.data.list;
	            for (var i = 0; i < _this.techList.length; i++) {
	                if (_this.techList[i].Unread) _this.wait++;
	                if (_this.techList[i].massagist_photo.indexOf('http') < 0) {
	                    _this.techList[i].massagist_photo = _this.$store.state.URL + _this.techList[i].massagist_photo;
	                }
	            }
	        });
	    },
	    methods: {
	        local: function local(name) {
	            this.$router.go('/' + name + '/' + this.$route.params.token);
	        }
	    }
	};
	
	// </script>
	/* generated by vue-loader */

/***/ },
/* 198 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page manage\">\n    <header id=\"header\" class=\"mui-bar mui-bar-nav\">\n        <h1 class=\"mui-title\">技师管理</h1>\n        <!-- <a class=\"mui-action-back mui-icon mui-icon-left-nav mui-pull-left\"></a> -->\n    </header>\n\n    <div class=\"mui-scroll-wrapper\">\n    <div class=\"mui-scroll\">\n    \n    <div id=\"sliderSegmentedControl\" class=\"mui-slider-indicator mui-segmented-control mui-segmented-control-inverted\">\n        <a class=\"winslow-control-item\" @click=\"local('manage-shop')\" >店铺信息</a>\n        <a class=\"winslow-control-item\" @click=\"local('manage-project')\">项目管理</a>\n        <a class=\"winslow-control-item mui-active\" @click=\"local('manage-tech')\">技师管理</a>\n    </div>\n\n    <div class=\"title\" @click=\"showCheck=!showCheck\"><i class=\"ico-down {{!showCheck?'up':''}}\"></i>待审核({{wait}})</div>\n    <ul class=\"mui-table-view item-container\" v-show=\"showCheck\">\n        <li class=\"mui-table-view-cell mui-media\" v-show=\"showHave\" v-for=\"item in techList\" v-if=\"item.Unread\">\n            <a v-link=\"{path: '/manage-tech-info/'+$route.params.token+'/'+item.massagist_id}\">\n                <img class=\"mui-media-object mui-pull-left\" :src=\"item.massagist_photo\">\n                <div class=\"mui-media-body\">\n                    工号:{{item.massagist_no}}&nbsp;&nbsp;&nbsp;&nbsp;昵称:{{item.massagist_name}}\n                    <p class=\"mui-ellipsis\">申请类别: 项目调整</p>\n                </div>\n                <i class=\"status-text\">{{item.Unread | format 'yyyy-MM-dd'}}</i>\n                <i class=\"status wait\">未审核</i>\n            </a>\n        </li>\n    </ul>\n\n    <div class=\"title\" @click=\"showHave=!showHave\"><i class=\"ico-down {{!showHave?'up':''}}\"></i>技师列表({{techList.length}})</div>\n    <ul class=\"mui-table-view item-container\" v-show=\"showHave\">\n        <li class=\"mui-table-view-cell mui-media\" v-for=\"item in techList\">\n            <a v-link=\"{path: '/manage-tech-info/'+$route.params.token+'/'+item.massagist_id}\">\n                <img class=\"mui-media-object mui-pull-left\" :src=\"item.massagist_photo\">\n                <div class=\"mui-media-body center\">\n                    工号:{{item.massagist_no}}&nbsp;&nbsp;&nbsp;&nbsp;昵称:{{item.massagist_name}}\n                </div>\n            </a>\n        </li>\n    </ul>\n\n    </div></div>\n\n    <nav class=\"mui-bar mui-bar-tab\">\n        <a class=\"winslow-tab-item mui-active\">\n            <span class=\"iconfont\">&#xe67d;</span>\n            <span class=\"mui-tab-label\">管理</span>\n        </a>\n        <a class=\"winslow-tab-item\" @click=\"local('manage-shop-chart')\">\n            <span class=\"iconfont\">&#xe671;</span>\n            <span class=\"mui-tab-label\">报表</span>\n        </a>\n        <a class=\"winslow-tab-item\" @click=\"local('manage-shop-comment')\">\n            <span class=\"iconfont\">&#xe706;</span>\n            <span class=\"mui-tab-label\">店铺评价</span>\n        </a>\n    </nav>\n    \n    \n</div>\n";

/***/ },
/* 199 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(200)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/manage/tech-info.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(201)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-bd927f14/tech-info.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 200 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	
	var _start = __webpack_require__(44);
	
	var _start2 = _interopRequireDefault(_start);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	exports.default = {
	    name: 'manageTechInfo',
	    data: function data() {
	        return {
	            getHaveProject: [],
	            getHandleProject: [],
	            getAddProject: [],
	            showChange: true,
	            showHave: true,
	            showProject: true,
	            techInfo: {}
	        };
	    },
	    ready: function ready() {
	        var _this = this;
	
	        document.title = '技师管理';
	        this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	        mui('.mui-scroll-wrapper').scroll({ bounce: false });
	        this.$http.post(API.getMassagistInfo, { tid: this.$route.params.id }).then(function (data) {
	            if (data.data.errorInfo) return;
	            _this.$store.state.hideTip();
	            _this.techInfo = data.data.object;
	            if (_this.techInfo.massagist_photo.indexOf('http') < 0) {
	                _this.techInfo.massagist_photo = _this.$store.state.URL + _this.techInfo.massagist_photo;
	            }
	        });
	        this.getProject();
	    },
	    methods: {
	        local: function local(name) {
	            this.$router.go('/' + name + '/' + this.$route.params.token);
	        },
	        getProject: function getProject() {
	            var _this2 = this;
	
	            // 获取已有项目
	            this.$http.post(API.getHaveProject, { tid: this.$route.params.id }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this2.$store.state.hideTip();
	                _this2.getHaveProject = data.data.list;
	            });
	            // 获取已申请项目
	            this.$http.post(API.getHandleProject, { tid: this.$route.params.id }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this2.$store.state.hideTip();
	                _this2.getHandleProject = data.data.list;
	            });
	            // 获取可申请项目
	            this.$http.post(API.getAddProject, { tid: this.$route.params.id }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this2.$store.state.hideTip();
	                _this2.getAddProject = data.data.list;
	            });
	        },
	        operate: function operate(pid, action) {
	            var alertText = '';
	            var me = this;
	            if (action == 'add') alertText = '添加';
	            if (action == 'delete') alertText = '移除';
	            if (action == 'agree') alertText = '同意审核';
	            if (action == 'refuse') alertText = '退回';
	            mui.confirm('确定' + alertText + '该项目吗？', '提醒', ['取消', '确定'], function (e) {
	                if (e.index == 1) {
	                    me.$store.state.showTip({ type: 'loading', content: alertText + '中...' });
	                    me.$http.post(API.applyProject, {
	                        pid: pid,
	                        tid: me.$route.params.id,
	                        action: action
	                    }).then(function (data) {
	                        if (data.data.errorInfo) return;
	                        me.$store.state.hideTip();
	                        me.$store.state.showTip({ type: 'success', content: '处理成功' });
	                        me.getProject();
	                    });
	                }
	            });
	        }
	    },
	    components: {
	        Start: _start2.default
	    }
	};
	
	// </script>
	/* generated by vue-loader */
	// <template>
	// <div class="page manage">
	//     <header id="header" class="mui-bar mui-bar-nav haed2 ">
	//         <h1 class="mui-title">技师管理</h1>
	//         <a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a>
	//     </header>
	//     <div class="mui-scroll-wrapper" style="bottom: px;">
	//     <div class="mui-scroll">
	//     <div class="my-info-container no-border">
	//         <div class="my-info">
	//             <img :src="techInfo.massagist_photo" class="head">
	//             <div class="info mui-ellipsis">
	//                 <h1>工号：{{techInfo.massagist_no}}</h1>
	//                 <h2>年龄：{{techInfo.massagist_birth || '未设置' | years}}</h2>
	//                 <h3><start :value='parseInt(techInfo.massagist_store)'></start> {{parseInt(techInfo.massagist_store)}}分</h3>
	//             </div>
	//             <div class="info">
	//                 <h1>昵称：{{techInfo.massagist_name}}</h1>
	//                 <h2>籍贯：{{techInfo.c_name}}</h2>
	//             </div>
	//         </div>
	//     </div>
	//
	//     <div class="title" @click="showChange=!showChange"><i class="ico-down {{!showChange?'up':''}}"></i>项目调整申请({{getHandleProject.length}})</div>
	//         <ul class="mui-table-view item-container" v-show="showChange">
	//             <li class="mui-table-view-cell mui-media" v-for="item in getHandleProject">
	//                 <a href="javascript:;">
	//                     <img class="mui-media-object mui-pull-left" :src="$store.state.URL + item.item_image">
	//                     <div class="mui-media-body">
	//                         {{item.item_name}}
	//                         <p class="mui-ellipsis">申请时间：{{(item.apply_time || '前台添加') | format 'yyyy-MM-dd hh:mm'}}</p>
	//                     </div>
	//                     <i class="status-text">申请{{item.handle == 'add' ? '添加' : '移除'}}</i>
	//                     <i class="status agree" v-if="item.apply_state == 1">已同意</i>
	//                     <i class="status reject" v-if="item.apply_state == 2">已拒绝</i>
	//                     <i class="status true" v-if="item.apply_state == 0" @click="operate(item.item_id, 'agree')">同意</i>
	//                     <i class="status false" v-if="item.apply_state == 0" @click="operate(item.item_id, 'refuse')">退回</i>
	//                 </a>
	//             </li>
	//             <div class="no-data" v-if="getHandleProject.length<=0">
	//                 <h6>还没有该项目~</h6>
	//             </div>
	//         </ul>
	//
	//         <div class="title" @click="showHave=!showHave"><i class="ico-down {{!showHave?'up':''}}"></i>已添加项目({{getHaveProject.length}})</div>
	//         <ul class="mui-table-view item-container" v-show="showHave">
	//             <li class="mui-table-view-cell mui-media" v-for="item in getHaveProject" >
	//                 <a href="javascript:;">
	//                     <img class="mui-media-object mui-pull-left" :src="$store.state.URL + item.item_image">
	//                     <div class="mui-media-body center">
	//                         {{item.item_name}}
	//                     </div>
	//                     <i class="status center remove" v-if="item.apply_state!=0" @click="operate(item.item_id, 'delete')">移除</i>
	//                     <i class="status center fail" v-if="item.apply_state==0">已申请</i>
	//                 </a>
	//             </li>
	//             <div class="no-data" v-if="getHaveProject.length<=0">
	//                 <h6>还没有该项目~</h6>
	//             </div>
	//         </ul>
	//
	//         <div class="title" @click="showProject=!showProject"><i class="ico-down {{!showProject?'up':''}}"></i>可申请项目({{getAddProject.length}})</div>
	//         <ul class="mui-table-view item-container" v-show="showProject">
	//             <li class="mui-table-view-cell mui-media" v-for="item in getAddProject" >
	//                 <a href="javascript:;">
	//                     <img class="mui-media-object mui-pull-left" :src="$store.state.URL + item.item_image">
	//                     <div class="mui-media-body center">
	//                         {{item.item_name}}
	//                     </div>
	//                     <i class="status center add" v-if="item.apply_state!=0" @click="operate(item.item_id, 'add')">添加</i>
	//                     <i class="status center fail" v-if="item.apply_state==0">已申请</i>
	//                 </a>
	//             </li>
	//             <div class="no-data" v-if="getAddProject.length<=0">
	//                 <h6>还没有该项目~</h6>
	//             </div>
	//         </ul>
	//
	//         </div></div>
	//
	// </div>
	// </template>
	//
	// <script>

/***/ },
/* 201 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page manage\">\n    <header id=\"header\" class=\"mui-bar mui-bar-nav haed2 \">\n        <h1 class=\"mui-title\">技师管理</h1>\n        <a class=\"mui-action-back mui-icon mui-icon-left-nav mui-pull-left\"></a>\n    </header>\n    <div class=\"mui-scroll-wrapper\" style=\"bottom: px;\">\n    <div class=\"mui-scroll\">\n    <div class=\"my-info-container no-border\">\n        <div class=\"my-info\">\n            <img :src=\"techInfo.massagist_photo\" class=\"head\">\n            <div class=\"info mui-ellipsis\">\n                <h1>工号：{{techInfo.massagist_no}}</h1>\n                <h2>年龄：{{techInfo.massagist_birth || '未设置' | years}}</h2>\n                <h3><start :value='parseInt(techInfo.massagist_store)'></start> {{parseInt(techInfo.massagist_store)}}分</h3>\n            </div>\n            <div class=\"info\">\n                <h1>昵称：{{techInfo.massagist_name}}</h1>\n                <h2>籍贯：{{techInfo.c_name}}</h2>\n            </div>\n        </div>\n    </div>\n\n    <div class=\"title\" @click=\"showChange=!showChange\"><i class=\"ico-down {{!showChange?'up':''}}\"></i>项目调整申请({{getHandleProject.length}})</div>\n        <ul class=\"mui-table-view item-container\" v-show=\"showChange\">\n            <li class=\"mui-table-view-cell mui-media\" v-for=\"item in getHandleProject\">\n                <a href=\"javascript:;\">\n                    <img class=\"mui-media-object mui-pull-left\" :src=\"$store.state.URL + item.item_image\">\n                    <div class=\"mui-media-body\">\n                        {{item.item_name}}\n                        <p class=\"mui-ellipsis\">申请时间：{{(item.apply_time || '前台添加') | format 'yyyy-MM-dd hh:mm'}}</p>\n                    </div>\n                    <i class=\"status-text\">申请{{item.handle == 'add' ? '添加' : '移除'}}</i>\n                    <i class=\"status agree\" v-if=\"item.apply_state == 1\">已同意</i>\n                    <i class=\"status reject\" v-if=\"item.apply_state == 2\">已拒绝</i>\n                    <i class=\"status true\" v-if=\"item.apply_state == 0\" @click=\"operate(item.item_id, 'agree')\">同意</i>\n                    <i class=\"status false\" v-if=\"item.apply_state == 0\" @click=\"operate(item.item_id, 'refuse')\">退回</i>\n                </a>\n            </li>\n            <div class=\"no-data\" v-if=\"getHandleProject.length<=0\">\n                <h6>还没有该项目~</h6>\n            </div>\n        </ul>\n\n        <div class=\"title\" @click=\"showHave=!showHave\"><i class=\"ico-down {{!showHave?'up':''}}\"></i>已添加项目({{getHaveProject.length}})</div>\n        <ul class=\"mui-table-view item-container\" v-show=\"showHave\">\n            <li class=\"mui-table-view-cell mui-media\" v-for=\"item in getHaveProject\" >\n                <a href=\"javascript:;\">\n                    <img class=\"mui-media-object mui-pull-left\" :src=\"$store.state.URL + item.item_image\">\n                    <div class=\"mui-media-body center\">\n                        {{item.item_name}}\n                    </div>\n                    <i class=\"status center remove\" v-if=\"item.apply_state!=0\" @click=\"operate(item.item_id, 'delete')\">移除</i>\n                    <i class=\"status center fail\" v-if=\"item.apply_state==0\">已申请</i>\n                </a>\n            </li>\n            <div class=\"no-data\" v-if=\"getHaveProject.length<=0\">\n                <h6>还没有该项目~</h6>\n            </div>\n        </ul>\n\n        <div class=\"title\" @click=\"showProject=!showProject\"><i class=\"ico-down {{!showProject?'up':''}}\"></i>可申请项目({{getAddProject.length}})</div>\n        <ul class=\"mui-table-view item-container\" v-show=\"showProject\">\n            <li class=\"mui-table-view-cell mui-media\" v-for=\"item in getAddProject\" >\n                <a href=\"javascript:;\">\n                    <img class=\"mui-media-object mui-pull-left\" :src=\"$store.state.URL + item.item_image\">\n                    <div class=\"mui-media-body center\">\n                        {{item.item_name}}\n                    </div>\n                    <i class=\"status center add\" v-if=\"item.apply_state!=0\" @click=\"operate(item.item_id, 'add')\">添加</i>\n                    <i class=\"status center fail\" v-if=\"item.apply_state==0\">已申请</i>\n                </a>\n            </li>\n            <div class=\"no-data\" v-if=\"getAddProject.length<=0\">\n                <h6>还没有该项目~</h6>\n            </div>\n        </ul>\n\n        </div></div>\n    \n</div>\n";

/***/ },
/* 202 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(203)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/manage/project.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(204)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-2260924a/project.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 203 */
/***/ function(module, exports) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	// <template>
	// <div class="page manage">
	//     <header id="header" class="mui-bar mui-bar-nav">
	//         <h1 class="mui-title">项目管理</h1>
	//         <a class="mui-icon mui-icon-gear mui-pull-right" style="color: #fff;" @click="showSet=!showSet"></a>
	//     </header>
	//
	//     <div class="mui-scroll-wrapper">
	//     <div class="mui-scroll">
	//     <div id="sliderSegmentedControl" class="mui-slider-indicator mui-segmented-control mui-segmented-control-inverted">
	//         <a class="winslow-control-item" @click="local('manage-shop')" >店铺信息</a>
	//         <a class="winslow-control-item mui-active" @click="local('manage-project')">项目管理</a>
	//         <a class="winslow-control-item" @click="local('manage-tech')">技师管理</a>
	//     </div>
	//
	//     <div class="set" v-show="showSet" transition>
	//         <h1>项目审核</h1>
	//         <p>
	//             开启项目审核后，店铺每发布一个项目都需经理对该项目进行审核后才可发布；<br>
	//             关闭项目审核后，店铺每发布一个项目无需经理审核，可直接发布。<br>
	//             请根据店铺实际需求设置该项功能使用与否。</p>
	//         <a class="{{storeCheck==1 ? 'on' : 'off'}}" @click="proSwitch('on')">开启</a>
	//         <a class="{{storeCheck==0 ? 'on' : 'off'}}" @click="proSwitch('off')">关闭</a>
	//     </div>
	//     <div class="mask-bg" v-show="showSet" @click="showSet=!showSet"></div>
	//
	//     <ul class="mui-table-view item-container project">
	//         <li class="mui-table-view-cell mui-media" v-for="item in projectList">
	//             <a href="javascript:;" v-link="{path: '/manage-project-info/' + $route.params.token + '/' + item.item_id + '/' + storeCheck}">
	//                 <img class="mui-media-object mui-pull-left" :src="$store.state.URL + item.item_image">
	//                 <div class="mui-media-body">
	//                     {{item.item_name}}
	//                     <p class="mui-ellipsis">套餐详情:{{item.item_intro}}</p>
	//                     <p><em class="price">￥{{item.item_vipprice}}</em> <em class="disguise">非会员：￥{{item.item_price}}</em></p>
	//                 </div>
	//             </a>
	//             <div class="prostatus" v-if="item.item_status == 1 || item.item_status == 2">
	//                 {{item.item_date_verify | format 'yyyy-MM-dd'}}
	//                 <a class="btn true" v-if="storeCheck==1">已审核</a>
	//             </div>
	//             <div class="prostatus" v-if="item.item_status == 3">
	//                 {{item.item_date_sub | format 'yyyy-MM-dd'}}
	//                 <a class="btn" v-if="storeCheck==1">未审核</a>
	//             </div>
	//             <div class="prostatus" v-if="item.item_status == 4">
	//                 {{item.item_date_verify | format 'yyyy-MM-dd'}}
	//                 <a class="btn false" v-if="storeCheck==1">已退回</a>
	//             </div>
	//         </li>
	//
	//     </ul>
	//     <div class="no-data" v-if="projectList.length<=0">
	//         <h6>还没有项目~</h6>
	//     </div>
	//     </div>
	//     </div>
	//
	//     <nav class="mui-bar mui-bar-tab">
	//         <a class="winslow-tab-item mui-active">
	//             <span class="iconfont">&#xe67d;</span>
	//             <span class="mui-tab-label">管理</span>
	//         </a>
	//         <a class="winslow-tab-item" @click="local('manage-shop-chart')">
	//             <span class="iconfont">&#xe671;</span>
	//             <span class="mui-tab-label">报表</span>
	//         </a>
	//         <a class="winslow-tab-item" @click="local('manage-shop-comment')">
	//             <span class="iconfont">&#xe706;</span>
	//             <span class="mui-tab-label">店铺评价</span>
	//         </a>
	//     </nav>
	//
	//
	// </div>
	// </template>
	//
	// <script>
	exports.default = {
	    name: 'manageProject',
	    data: function data() {
	        return {
	            showSet: false,
	            projectList: [],
	            storeCheck: 0
	        };
	    },
	    ready: function ready() {
	        var _this = this;
	
	        document.title = '项目管理';
	        mui('.mui-scroll-wrapper').scroll({ bounce: false });
	        this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	        this.$http.post(API.getProjectList, {}).then(function (data) {
	            if (data.data.errorInfo) return;
	            _this.$store.state.hideTip();
	            _this.projectList = data.data.list;
	            _this.storeCheck = data.data.object;
	        });
	    },
	    methods: {
	        local: function local(name) {
	            this.$router.go('/' + name + '/' + this.$route.params.token);
	        },
	        proSwitch: function proSwitch(val) {
	            var _this2 = this;
	
	            var action = '';
	            if (val == 'on') {
	                if (this.storeCheck == 1) {
	                    this.$store.state.showTip({ type: 'error', content: '您已经是开启状态了' });
	                    return;
	                }
	                action = '开启';
	            } else {
	                if (this.storeCheck == 0) {
	                    this.$store.state.showTip({ type: 'error', content: '您已经是关闭状态了' });
	                    return;
	                }
	                action = '关闭';
	            }
	            this.$store.state.showTip({ type: 'loading', content: action + '中...' });
	            this.$http.post(API.projectSwitch, {
	                action: val
	            }).then(function (data) {
	                if (data.data.errorInfo) return;
	                _this2.$store.state.hideTip();
	                _this2.$store.state.showTip({ type: 'success', content: action + '成功!' });
	                _this2.showSet = false;
	                location.reload();
	            });
	        }
	    }
	};
	
	// </script>
	/* generated by vue-loader */

/***/ },
/* 204 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page manage\">\n    <header id=\"header\" class=\"mui-bar mui-bar-nav\">\n        <h1 class=\"mui-title\">项目管理</h1>\n        <a class=\"mui-icon mui-icon-gear mui-pull-right\" style=\"color: #fff;\" @click=\"showSet=!showSet\"></a>\n    </header>\n    \n    <div class=\"mui-scroll-wrapper\">\n    <div class=\"mui-scroll\">\n    <div id=\"sliderSegmentedControl\" class=\"mui-slider-indicator mui-segmented-control mui-segmented-control-inverted\">\n        <a class=\"winslow-control-item\" @click=\"local('manage-shop')\" >店铺信息</a>\n        <a class=\"winslow-control-item mui-active\" @click=\"local('manage-project')\">项目管理</a>\n        <a class=\"winslow-control-item\" @click=\"local('manage-tech')\">技师管理</a>\n    </div>\n\n    <div class=\"set\" v-show=\"showSet\" transition>\n        <h1>项目审核</h1>\n        <p>\n            开启项目审核后，店铺每发布一个项目都需经理对该项目进行审核后才可发布；<br>\n            关闭项目审核后，店铺每发布一个项目无需经理审核，可直接发布。<br>\n            请根据店铺实际需求设置该项功能使用与否。</p>\n        <a class=\"{{storeCheck==1 ? 'on' : 'off'}}\" @click=\"proSwitch('on')\">开启</a>\n        <a class=\"{{storeCheck==0 ? 'on' : 'off'}}\" @click=\"proSwitch('off')\">关闭</a>\n    </div>\n    <div class=\"mask-bg\" v-show=\"showSet\" @click=\"showSet=!showSet\"></div>\n\n    <ul class=\"mui-table-view item-container project\">\n        <li class=\"mui-table-view-cell mui-media\" v-for=\"item in projectList\">\n            <a href=\"javascript:;\" v-link=\"{path: '/manage-project-info/' + $route.params.token + '/' + item.item_id + '/' + storeCheck}\">\n                <img class=\"mui-media-object mui-pull-left\" :src=\"$store.state.URL + item.item_image\">\n                <div class=\"mui-media-body\">\n                    {{item.item_name}}\n                    <p class=\"mui-ellipsis\">套餐详情:{{item.item_intro}}</p>\n                    <p><em class=\"price\">￥{{item.item_vipprice}}</em> <em class=\"disguise\">非会员：￥{{item.item_price}}</em></p>\n                </div>\n            </a>\n            <div class=\"prostatus\" v-if=\"item.item_status == 1 || item.item_status == 2\">\n                {{item.item_date_verify | format 'yyyy-MM-dd'}}\n                <a class=\"btn true\" v-if=\"storeCheck==1\">已审核</a>\n            </div>\n            <div class=\"prostatus\" v-if=\"item.item_status == 3\">\n                {{item.item_date_sub | format 'yyyy-MM-dd'}}\n                <a class=\"btn\" v-if=\"storeCheck==1\">未审核</a>\n            </div>\n            <div class=\"prostatus\" v-if=\"item.item_status == 4\">\n                {{item.item_date_verify | format 'yyyy-MM-dd'}}\n                <a class=\"btn false\" v-if=\"storeCheck==1\">已退回</a>\n            </div>\n        </li>\n        \n    </ul>\n    <div class=\"no-data\" v-if=\"projectList.length<=0\">\n        <h6>还没有项目~</h6>\n    </div>\n    </div>\n    </div>\n\n    <nav class=\"mui-bar mui-bar-tab\">\n        <a class=\"winslow-tab-item mui-active\">\n            <span class=\"iconfont\">&#xe67d;</span>\n            <span class=\"mui-tab-label\">管理</span>\n        </a>\n        <a class=\"winslow-tab-item\" @click=\"local('manage-shop-chart')\">\n            <span class=\"iconfont\">&#xe671;</span>\n            <span class=\"mui-tab-label\">报表</span>\n        </a>\n        <a class=\"winslow-tab-item\" @click=\"local('manage-shop-comment')\">\n            <span class=\"iconfont\">&#xe706;</span>\n            <span class=\"mui-tab-label\">店铺评价</span>\n        </a>\n    </nav>\n    \n    \n</div>\n";

/***/ },
/* 205 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(206)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/views/manage/project-info.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(207)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-934c99fe/project-info.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 206 */
/***/ function(module, exports) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	// <template>
	// <div class="page manage" style="background: #fff;">
	//     <header id="header" class="mui-bar mui-bar-nav">
	//         <h1 class="mui-title">项目详情</h1>
	//         <a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a>
	//     </header>
	//
	//     <div class="mui-scroll-wrapper" style="bottom: 0px;">
	//     <div class="mui-scroll">
	//     <div class="banner-main" style="background-image: url({{$store.state.URL + projectInfo.item_image}})"></div>
	//
	//     <div class="shop-title-container" style="padding-right: 95px;">
	//         <h1>{{projectInfo.item_name}}</h1>
	//         <h5>套餐详情:{{projectInfo.item_intro}}</h5>
	//         <h4>申请时间：{{projectInfo.item_date_sub || '' | format 'yyyy-MM-dd hh:mm:ss'}}</h4>
	//         <div class="price">
	//             <em>￥{{projectInfo.item_vipprice}}</em>
	//             <span>非会员:￥{{projectInfo.item_price}}</span>
	//         </div>
	//     </div>
	//
	//     <div class="pro-btns" v-if="(projectInfo.item_status == 1 || projectInfo.item_status == 2) && $route.params.switch==1">
	//         <a class="true-ok">已通过</a>
	//     </div>
	//     <div class="pro-btns" v-if="projectInfo.item_status == 3 && $route.params.switch==1">
	//         <a class="true" @click="review('pass')">通过</a>
	//         <a class="false" @click="review('back')">退回</a>
	//     </div>
	//     <div class="pro-btns" v-if="projectInfo.item_status == 4 && $route.params.switch==1">
	//         <a class="false-ok">已退回</a>
	//     </div>
	//     </div>
	//     </div>
	// </div>
	// </template>
	//
	// <script>
	exports.default = {
	    name: 'manageProjectInfo',
	    data: function data() {
	        return {
	            projectInfo: {}
	        };
	    },
	    ready: function ready() {
	        var _this = this;
	
	        document.title = '项目详情';
	        mui('.mui-scroll-wrapper').scroll({ bounce: false });
	        // mui("#slider").slider()
	        this.$store.state.showTip({ type: 'loading', content: '加载中...' });
	        this.$http.post(API.getProjectInfo, {
	            pid: this.$route.params.id
	        }).then(function (data) {
	            if (data.data.errorInfo) return;
	            _this.$store.state.hideTip();
	            _this.projectInfo = data.data.list;
	        });
	    },
	    methods: {
	        local: function local(name) {
	            this.$router.go('/' + name + '/' + this.$route.params.token);
	        },
	        review: function review(action) {
	            var text = '';
	            var me = this;
	            var aid = void 0;
	            if (action == 'pass') {
	                text = '通过审核';
	                aid = 2;
	            } else {
	                text = '退回审核';
	                aid = 4;
	            }
	            mui.confirm('确定' + text + '吗？', '提醒', ['取消', '确定'], function (e) {
	                if (e.index == 1) {
	                    me.$store.state.showTip({ type: 'loading', content: '处理中...' });
	                    me.$http.post(API.handleProject, {
	                        pid: me.$route.params.id,
	                        action: aid
	                    }).then(function (data) {
	                        if (data.data.errorInfo) return;
	                        me.$store.state.hideTip();
	                        me.$store.state.showTip({ type: 'success', content: text + '成功' });
	                        location.reload();
	                    });
	                }
	            });
	        }
	    }
	};
	
	// </script>
	/* generated by vue-loader */

/***/ },
/* 207 */
/***/ function(module, exports) {

	module.exports = "\n<div class=\"page manage\" style=\"background: #fff;\">\n    <header id=\"header\" class=\"mui-bar mui-bar-nav\">\n        <h1 class=\"mui-title\">项目详情</h1>\n        <a class=\"mui-action-back mui-icon mui-icon-left-nav mui-pull-left\"></a>\n    </header>\n\n    <div class=\"mui-scroll-wrapper\" style=\"bottom: 0px;\">\n    <div class=\"mui-scroll\">\n    <div class=\"banner-main\" style=\"background-image: url({{$store.state.URL + projectInfo.item_image}})\"></div>\n    \n    <div class=\"shop-title-container\" style=\"padding-right: 95px;\">\n        <h1>{{projectInfo.item_name}}</h1>\n        <h5>套餐详情:{{projectInfo.item_intro}}</h5>\n        <h4>申请时间：{{projectInfo.item_date_sub || '' | format 'yyyy-MM-dd hh:mm:ss'}}</h4>\n        <div class=\"price\">\n            <em>￥{{projectInfo.item_vipprice}}</em>\n            <span>非会员:￥{{projectInfo.item_price}}</span>\n        </div>\n    </div>\n    \n    <div class=\"pro-btns\" v-if=\"(projectInfo.item_status == 1 || projectInfo.item_status == 2) && $route.params.switch==1\">\n        <a class=\"true-ok\">已通过</a>\n    </div>\n    <div class=\"pro-btns\" v-if=\"projectInfo.item_status == 3 && $route.params.switch==1\">\n        <a class=\"true\" @click=\"review('pass')\">通过</a>\n        <a class=\"false\" @click=\"review('back')\">退回</a>\n    </div>\n    <div class=\"pro-btns\" v-if=\"projectInfo.item_status == 4 && $route.params.switch==1\">\n        <a class=\"false-ok\">已退回</a>\n    </div>\n    </div>\n    </div>\n</div>\n";

/***/ },
/* 208 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(209)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/App.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(221)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-2103c657/App.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 209 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	
	var _store = __webpack_require__(210);
	
	var _store2 = _interopRequireDefault(_store);
	
	var _tips = __webpack_require__(212);
	
	var _tips2 = _interopRequireDefault(_tips);
	
	var _wechat = __webpack_require__(219);
	
	var _wechat2 = _interopRequireDefault(_wechat);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	exports.default = {
	    data: function data() {
	        return {
	            tips: this.$store.state.tip
	        };
	    },
	    ready: function ready() {
	        document.body.addEventListener('touchmove', function (event) {
	            event.preventDefault();
	        }, true);
	        var screenWidth = window.screen.width;
	        window.onresize = function () {
	            if (Math.abs(screenWidth - window.screen.width) > 20) location.reload();
	        };
	    },
	    components: {
	        Tip: _tips2.default, Wechat: _wechat2.default
	    },
	    store: _store2.default
	};
	// </script>
	/* generated by vue-loader */
	// <template>
	//     <router-view></router-view>
	//     <tip :show.sync='tips.show' :options.sync='tips.options'></tip>
	//     <wechat></wechat>
	// </template>
	// <script>

/***/ },
/* 210 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	
	var _vue = __webpack_require__(2);
	
	var _vue2 = _interopRequireDefault(_vue);
	
	var _vuex = __webpack_require__(211);
	
	var _vuex2 = _interopRequireDefault(_vuex);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	_vue2.default.use(_vuex2.default);
	
	var state = {
	    URL: 'http://www.yangshenghui.net/',
	    tip: {
	        show: false,
	        options: {}
	    },
	    showTip: function showTip(opt) {
	        this.tip.show = true;
	        this.tip.options = opt;
	    },
	    hideTip: function hideTip() {
	        this.tip.show = false;
	    },
	    storeLabel: ['环境优雅', '设备完善', '价格合理', '下次还来', '环境一般', '设施一般', '性价比低', '有待提高', '环境恶劣', '设施陈旧', '价格坑爹', '绝不再来'],
	    tecLabel: ['服务热情', '手法专业', '态度很好', '颜值爆表', '服务一般', '手法一般', '态度一般', '看的过去', '服务太差', '手法太差', '态度恶劣', '惨不忍睹'],
	    __getImgData: function __getImgData(img, dir, next) {
	        var image = new Image();
	        image.onload = function () {
	            var degree = 0,
	                drawWidth = void 0,
	                drawHeight = void 0,
	                width = void 0,
	                height = void 0;
	            drawWidth = this.naturalWidth;
	            drawHeight = this.naturalHeight;
	            if (drawWidth > 2000 || drawHeight > 2000) {
	                drawWidth = drawWidth / 3;
	                drawHeight = drawHeight / 3;
	            }
	            var canvas = document.createElement('canvas');
	            canvas.width = width = drawWidth;
	            canvas.height = height = drawHeight;
	            var context = canvas.getContext('2d');
	            var isRouter = true;
	            //判断图片方向，重置canvas大小，确定旋转角度，iphone默认的是home键在右方的横屏拍摄方式
	            switch (dir) {
	                //iphone横屏拍摄，此时home键在左侧
	                case 3:
	                    degree = 180;
	                    drawWidth = -width;
	                    drawHeight = -height;
	                    break;
	                //iphone竖屏拍摄，此时home键在下方(正常拿手机的方向)
	                case 6:
	                    canvas.width = height;
	                    canvas.height = width;
	                    degree = 90;
	                    drawWidth = width;
	                    drawHeight = -height;
	                    break;
	                //iphone竖屏拍摄，此时home键在上方
	                case 8:
	                    canvas.width = height;
	                    canvas.height = width;
	                    degree = 270;
	                    drawWidth = -width;
	                    drawHeight = height;
	                    break;
	            }
	            //使用canvas旋转校正
	            context.rotate(degree * Math.PI / 180);
	            context.drawImage(this, 0, 0, drawWidth, drawHeight);
	            next(canvas.toDataURL(0.7));
	        };
	        image.src = img;
	    },
	    locationReload: function locationReload() {
	        location.reload();
	    },
	    pageSize: 10
	};
	
	exports.default = new _vuex2.default.Store({ state: state });

/***/ },
/* 211 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * vuex v2.1.2
	 * (c) 2017 Evan You
	 * @license MIT
	 */
	(function (global, factory) {
		 true ? module.exports = factory() :
		typeof define === 'function' && define.amd ? define(factory) :
		(global.Vuex = factory());
	}(this, (function () { 'use strict';
	
	var devtoolHook =
	  typeof window !== 'undefined' &&
	  window.__VUE_DEVTOOLS_GLOBAL_HOOK__;
	
	function devtoolPlugin (store) {
	  if (!devtoolHook) { return }
	
	  store._devtoolHook = devtoolHook;
	
	  devtoolHook.emit('vuex:init', store);
	
	  devtoolHook.on('vuex:travel-to-state', function (targetState) {
	    store.replaceState(targetState);
	  });
	
	  store.subscribe(function (mutation, state) {
	    devtoolHook.emit('vuex:mutation', mutation, state);
	  });
	}
	
	var applyMixin = function (Vue) {
	  var version = Number(Vue.version.split('.')[0]);
	
	  if (version >= 2) {
	    var usesInit = Vue.config._lifecycleHooks.indexOf('init') > -1;
	    Vue.mixin(usesInit ? { init: vuexInit } : { beforeCreate: vuexInit });
	  } else {
	    // override init and inject vuex init procedure
	    // for 1.x backwards compatibility.
	    var _init = Vue.prototype._init;
	    Vue.prototype._init = function (options) {
	      if ( options === void 0 ) options = {};
	
	      options.init = options.init
	        ? [vuexInit].concat(options.init)
	        : vuexInit;
	      _init.call(this, options);
	    };
	  }
	
	  /**
	   * Vuex init hook, injected into each instances init hooks list.
	   */
	
	  function vuexInit () {
	    var options = this.$options;
	    // store injection
	    if (options.store) {
	      this.$store = options.store;
	    } else if (options.parent && options.parent.$store) {
	      this.$store = options.parent.$store;
	    }
	  }
	};
	
	var mapState = normalizeNamespace(function (namespace, states) {
	  var res = {};
	  normalizeMap(states).forEach(function (ref) {
	    var key = ref.key;
	    var val = ref.val;
	
	    res[key] = function mappedState () {
	      var state = this.$store.state;
	      var getters = this.$store.getters;
	      if (namespace) {
	        var module = getModuleByNamespace(this.$store, 'mapState', namespace);
	        if (!module) {
	          return
	        }
	        state = module.context.state;
	        getters = module.context.getters;
	      }
	      return typeof val === 'function'
	        ? val.call(this, state, getters)
	        : state[val]
	    };
	  });
	  return res
	});
	
	var mapMutations = normalizeNamespace(function (namespace, mutations) {
	  var res = {};
	  normalizeMap(mutations).forEach(function (ref) {
	    var key = ref.key;
	    var val = ref.val;
	
	    val = namespace + val;
	    res[key] = function mappedMutation () {
	      var args = [], len = arguments.length;
	      while ( len-- ) args[ len ] = arguments[ len ];
	
	      if (namespace && !getModuleByNamespace(this.$store, 'mapMutations', namespace)) {
	        return
	      }
	      return this.$store.commit.apply(this.$store, [val].concat(args))
	    };
	  });
	  return res
	});
	
	var mapGetters = normalizeNamespace(function (namespace, getters) {
	  var res = {};
	  normalizeMap(getters).forEach(function (ref) {
	    var key = ref.key;
	    var val = ref.val;
	
	    val = namespace + val;
	    res[key] = function mappedGetter () {
	      if (namespace && !getModuleByNamespace(this.$store, 'mapGetters', namespace)) {
	        return
	      }
	      if (!(val in this.$store.getters)) {
	        console.error(("[vuex] unknown getter: " + val));
	        return
	      }
	      return this.$store.getters[val]
	    };
	  });
	  return res
	});
	
	var mapActions = normalizeNamespace(function (namespace, actions) {
	  var res = {};
	  normalizeMap(actions).forEach(function (ref) {
	    var key = ref.key;
	    var val = ref.val;
	
	    val = namespace + val;
	    res[key] = function mappedAction () {
	      var args = [], len = arguments.length;
	      while ( len-- ) args[ len ] = arguments[ len ];
	
	      if (namespace && !getModuleByNamespace(this.$store, 'mapActions', namespace)) {
	        return
	      }
	      return this.$store.dispatch.apply(this.$store, [val].concat(args))
	    };
	  });
	  return res
	});
	
	function normalizeMap (map) {
	  return Array.isArray(map)
	    ? map.map(function (key) { return ({ key: key, val: key }); })
	    : Object.keys(map).map(function (key) { return ({ key: key, val: map[key] }); })
	}
	
	function normalizeNamespace (fn) {
	  return function (namespace, map) {
	    if (typeof namespace !== 'string') {
	      map = namespace;
	      namespace = '';
	    } else if (namespace.charAt(namespace.length - 1) !== '/') {
	      namespace += '/';
	    }
	    return fn(namespace, map)
	  }
	}
	
	function getModuleByNamespace (store, helper, namespace) {
	  var module = store._modulesNamespaceMap[namespace];
	  if (!module) {
	    console.error(("[vuex] module namespace not found in " + helper + "(): " + namespace));
	  }
	  return module
	}
	
	/**
	 * Get the first item that pass the test
	 * by second argument function
	 *
	 * @param {Array} list
	 * @param {Function} f
	 * @return {*}
	 */
	/**
	 * Deep copy the given object considering circular structure.
	 * This function caches all nested objects and its copies.
	 * If it detects circular structure, use cached copy to avoid infinite loop.
	 *
	 * @param {*} obj
	 * @param {Array<Object>} cache
	 * @return {*}
	 */
	
	
	/**
	 * forEach for object
	 */
	function forEachValue (obj, fn) {
	  Object.keys(obj).forEach(function (key) { return fn(obj[key], key); });
	}
	
	function isObject (obj) {
	  return obj !== null && typeof obj === 'object'
	}
	
	function isPromise (val) {
	  return val && typeof val.then === 'function'
	}
	
	function assert (condition, msg) {
	  if (!condition) { throw new Error(("[vuex] " + msg)) }
	}
	
	var Module = function Module (rawModule, runtime) {
	  this.runtime = runtime;
	  this._children = Object.create(null);
	  this._rawModule = rawModule;
	};
	
	var prototypeAccessors$1 = { state: {},namespaced: {} };
	
	prototypeAccessors$1.state.get = function () {
	  return this._rawModule.state || {}
	};
	
	prototypeAccessors$1.namespaced.get = function () {
	  return !!this._rawModule.namespaced
	};
	
	Module.prototype.addChild = function addChild (key, module) {
	  this._children[key] = module;
	};
	
	Module.prototype.removeChild = function removeChild (key) {
	  delete this._children[key];
	};
	
	Module.prototype.getChild = function getChild (key) {
	  return this._children[key]
	};
	
	Module.prototype.update = function update (rawModule) {
	  this._rawModule.namespaced = rawModule.namespaced;
	  if (rawModule.actions) {
	    this._rawModule.actions = rawModule.actions;
	  }
	  if (rawModule.mutations) {
	    this._rawModule.mutations = rawModule.mutations;
	  }
	  if (rawModule.getters) {
	    this._rawModule.getters = rawModule.getters;
	  }
	};
	
	Module.prototype.forEachChild = function forEachChild (fn) {
	  forEachValue(this._children, fn);
	};
	
	Module.prototype.forEachGetter = function forEachGetter (fn) {
	  if (this._rawModule.getters) {
	    forEachValue(this._rawModule.getters, fn);
	  }
	};
	
	Module.prototype.forEachAction = function forEachAction (fn) {
	  if (this._rawModule.actions) {
	    forEachValue(this._rawModule.actions, fn);
	  }
	};
	
	Module.prototype.forEachMutation = function forEachMutation (fn) {
	  if (this._rawModule.mutations) {
	    forEachValue(this._rawModule.mutations, fn);
	  }
	};
	
	Object.defineProperties( Module.prototype, prototypeAccessors$1 );
	
	var ModuleCollection = function ModuleCollection (rawRootModule) {
	  var this$1 = this;
	
	  // register root module (Vuex.Store options)
	  this.root = new Module(rawRootModule, false);
	
	  // register all nested modules
	  if (rawRootModule.modules) {
	    forEachValue(rawRootModule.modules, function (rawModule, key) {
	      this$1.register([key], rawModule, false);
	    });
	  }
	};
	
	ModuleCollection.prototype.get = function get (path) {
	  return path.reduce(function (module, key) {
	    return module.getChild(key)
	  }, this.root)
	};
	
	ModuleCollection.prototype.getNamespace = function getNamespace (path) {
	  var module = this.root;
	  return path.reduce(function (namespace, key) {
	    module = module.getChild(key);
	    return namespace + (module.namespaced ? key + '/' : '')
	  }, '')
	};
	
	ModuleCollection.prototype.update = function update$1 (rawRootModule) {
	  update(this.root, rawRootModule);
	};
	
	ModuleCollection.prototype.register = function register (path, rawModule, runtime) {
	    var this$1 = this;
	    if ( runtime === void 0 ) runtime = true;
	
	  var parent = this.get(path.slice(0, -1));
	  var newModule = new Module(rawModule, runtime);
	  parent.addChild(path[path.length - 1], newModule);
	
	  // register nested modules
	  if (rawModule.modules) {
	    forEachValue(rawModule.modules, function (rawChildModule, key) {
	      this$1.register(path.concat(key), rawChildModule, runtime);
	    });
	  }
	};
	
	ModuleCollection.prototype.unregister = function unregister (path) {
	  var parent = this.get(path.slice(0, -1));
	  var key = path[path.length - 1];
	  if (!parent.getChild(key).runtime) { return }
	
	  parent.removeChild(key);
	};
	
	function update (targetModule, newModule) {
	  // update target module
	  targetModule.update(newModule);
	
	  // update nested modules
	  if (newModule.modules) {
	    for (var key in newModule.modules) {
	      if (!targetModule.getChild(key)) {
	        console.warn(
	          "[vuex] trying to add a new module '" + key + "' on hot reloading, " +
	          'manual reload is needed'
	        );
	        return
	      }
	      update(targetModule.getChild(key), newModule.modules[key]);
	    }
	  }
	}
	
	var Vue; // bind on install
	
	var Store = function Store (options) {
	  var this$1 = this;
	  if ( options === void 0 ) options = {};
	
	  assert(Vue, "must call Vue.use(Vuex) before creating a store instance.");
	  assert(typeof Promise !== 'undefined', "vuex requires a Promise polyfill in this browser.");
	
	  var state = options.state; if ( state === void 0 ) state = {};
	  var plugins = options.plugins; if ( plugins === void 0 ) plugins = [];
	  var strict = options.strict; if ( strict === void 0 ) strict = false;
	
	  // store internal state
	  this._committing = false;
	  this._actions = Object.create(null);
	  this._mutations = Object.create(null);
	  this._wrappedGetters = Object.create(null);
	  this._modules = new ModuleCollection(options);
	  this._modulesNamespaceMap = Object.create(null);
	  this._subscribers = [];
	  this._watcherVM = new Vue();
	
	  // bind commit and dispatch to self
	  var store = this;
	  var ref = this;
	  var dispatch = ref.dispatch;
	  var commit = ref.commit;
	  this.dispatch = function boundDispatch (type, payload) {
	    return dispatch.call(store, type, payload)
	  };
	  this.commit = function boundCommit (type, payload, options) {
	    return commit.call(store, type, payload, options)
	  };
	
	  // strict mode
	  this.strict = strict;
	
	  // init root module.
	  // this also recursively registers all sub-modules
	  // and collects all module getters inside this._wrappedGetters
	  installModule(this, state, [], this._modules.root);
	
	  // initialize the store vm, which is responsible for the reactivity
	  // (also registers _wrappedGetters as computed properties)
	  resetStoreVM(this, state);
	
	  // apply plugins
	  plugins.concat(devtoolPlugin).forEach(function (plugin) { return plugin(this$1); });
	};
	
	var prototypeAccessors = { state: {} };
	
	prototypeAccessors.state.get = function () {
	  return this._vm.$data.state
	};
	
	prototypeAccessors.state.set = function (v) {
	  assert(false, "Use store.replaceState() to explicit replace store state.");
	};
	
	Store.prototype.commit = function commit (_type, _payload, _options) {
	    var this$1 = this;
	
	  // check object-style commit
	  var ref = unifyObjectStyle(_type, _payload, _options);
	    var type = ref.type;
	    var payload = ref.payload;
	    var options = ref.options;
	
	  var mutation = { type: type, payload: payload };
	  var entry = this._mutations[type];
	  if (!entry) {
	    console.error(("[vuex] unknown mutation type: " + type));
	    return
	  }
	  this._withCommit(function () {
	    entry.forEach(function commitIterator (handler) {
	      handler(payload);
	    });
	  });
	  this._subscribers.forEach(function (sub) { return sub(mutation, this$1.state); });
	
	  if (options && options.silent) {
	    console.warn(
	      "[vuex] mutation type: " + type + ". Silent option has been removed. " +
	      'Use the filter functionality in the vue-devtools'
	    );
	  }
	};
	
	Store.prototype.dispatch = function dispatch (_type, _payload) {
	  // check object-style dispatch
	  var ref = unifyObjectStyle(_type, _payload);
	    var type = ref.type;
	    var payload = ref.payload;
	
	  var entry = this._actions[type];
	  if (!entry) {
	    console.error(("[vuex] unknown action type: " + type));
	    return
	  }
	  return entry.length > 1
	    ? Promise.all(entry.map(function (handler) { return handler(payload); }))
	    : entry[0](payload)
	};
	
	Store.prototype.subscribe = function subscribe (fn) {
	  var subs = this._subscribers;
	  if (subs.indexOf(fn) < 0) {
	    subs.push(fn);
	  }
	  return function () {
	    var i = subs.indexOf(fn);
	    if (i > -1) {
	      subs.splice(i, 1);
	    }
	  }
	};
	
	Store.prototype.watch = function watch (getter, cb, options) {
	    var this$1 = this;
	
	  assert(typeof getter === 'function', "store.watch only accepts a function.");
	  return this._watcherVM.$watch(function () { return getter(this$1.state, this$1.getters); }, cb, options)
	};
	
	Store.prototype.replaceState = function replaceState (state) {
	    var this$1 = this;
	
	  this._withCommit(function () {
	    this$1._vm.state = state;
	  });
	};
	
	Store.prototype.registerModule = function registerModule (path, rawModule) {
	  if (typeof path === 'string') { path = [path]; }
	  assert(Array.isArray(path), "module path must be a string or an Array.");
	  this._modules.register(path, rawModule);
	  installModule(this, this.state, path, this._modules.get(path));
	  // reset store to update getters...
	  resetStoreVM(this, this.state);
	};
	
	Store.prototype.unregisterModule = function unregisterModule (path) {
	    var this$1 = this;
	
	  if (typeof path === 'string') { path = [path]; }
	  assert(Array.isArray(path), "module path must be a string or an Array.");
	  this._modules.unregister(path);
	  this._withCommit(function () {
	    var parentState = getNestedState(this$1.state, path.slice(0, -1));
	    Vue.delete(parentState, path[path.length - 1]);
	  });
	  resetStore(this);
	};
	
	Store.prototype.hotUpdate = function hotUpdate (newOptions) {
	  this._modules.update(newOptions);
	  resetStore(this, true);
	};
	
	Store.prototype._withCommit = function _withCommit (fn) {
	  var committing = this._committing;
	  this._committing = true;
	  fn();
	  this._committing = committing;
	};
	
	Object.defineProperties( Store.prototype, prototypeAccessors );
	
	function resetStore (store, hot) {
	  store._actions = Object.create(null);
	  store._mutations = Object.create(null);
	  store._wrappedGetters = Object.create(null);
	  store._modulesNamespaceMap = Object.create(null);
	  var state = store.state;
	  // init all modules
	  installModule(store, state, [], store._modules.root, true);
	  // reset vm
	  resetStoreVM(store, state, hot);
	}
	
	function resetStoreVM (store, state, hot) {
	  var oldVm = store._vm;
	
	  // bind store public getters
	  store.getters = {};
	  var wrappedGetters = store._wrappedGetters;
	  var computed = {};
	  forEachValue(wrappedGetters, function (fn, key) {
	    // use computed to leverage its lazy-caching mechanism
	    computed[key] = function () { return fn(store); };
	    Object.defineProperty(store.getters, key, {
	      get: function () { return store._vm[key]; },
	      enumerable: true // for local getters
	    });
	  });
	
	  // use a Vue instance to store the state tree
	  // suppress warnings just in case the user has added
	  // some funky global mixins
	  var silent = Vue.config.silent;
	  Vue.config.silent = true;
	  store._vm = new Vue({
	    data: { state: state },
	    computed: computed
	  });
	  Vue.config.silent = silent;
	
	  // enable strict mode for new vm
	  if (store.strict) {
	    enableStrictMode(store);
	  }
	
	  if (oldVm) {
	    if (hot) {
	      // dispatch changes in all subscribed watchers
	      // to force getter re-evaluation for hot reloading.
	      store._withCommit(function () {
	        oldVm.state = null;
	      });
	    }
	    Vue.nextTick(function () { return oldVm.$destroy(); });
	  }
	}
	
	function installModule (store, rootState, path, module, hot) {
	  var isRoot = !path.length;
	  var namespace = store._modules.getNamespace(path);
	
	  // register in namespace map
	  if (namespace) {
	    store._modulesNamespaceMap[namespace] = module;
	  }
	
	  // set state
	  if (!isRoot && !hot) {
	    var parentState = getNestedState(rootState, path.slice(0, -1));
	    var moduleName = path[path.length - 1];
	    store._withCommit(function () {
	      Vue.set(parentState, moduleName, module.state);
	    });
	  }
	
	  var local = module.context = makeLocalContext(store, namespace, path);
	
	  module.forEachMutation(function (mutation, key) {
	    var namespacedType = namespace + key;
	    registerMutation(store, namespacedType, mutation, local);
	  });
	
	  module.forEachAction(function (action, key) {
	    var namespacedType = namespace + key;
	    registerAction(store, namespacedType, action, local);
	  });
	
	  module.forEachGetter(function (getter, key) {
	    var namespacedType = namespace + key;
	    registerGetter(store, namespacedType, getter, local);
	  });
	
	  module.forEachChild(function (child, key) {
	    installModule(store, rootState, path.concat(key), child, hot);
	  });
	}
	
	/**
	 * make localized dispatch, commit, getters and state
	 * if there is no namespace, just use root ones
	 */
	function makeLocalContext (store, namespace, path) {
	  var noNamespace = namespace === '';
	
	  var local = {
	    dispatch: noNamespace ? store.dispatch : function (_type, _payload, _options) {
	      var args = unifyObjectStyle(_type, _payload, _options);
	      var payload = args.payload;
	      var options = args.options;
	      var type = args.type;
	
	      if (!options || !options.root) {
	        type = namespace + type;
	        if (!store._actions[type]) {
	          console.error(("[vuex] unknown local action type: " + (args.type) + ", global type: " + type));
	          return
	        }
	      }
	
	      return store.dispatch(type, payload)
	    },
	
	    commit: noNamespace ? store.commit : function (_type, _payload, _options) {
	      var args = unifyObjectStyle(_type, _payload, _options);
	      var payload = args.payload;
	      var options = args.options;
	      var type = args.type;
	
	      if (!options || !options.root) {
	        type = namespace + type;
	        if (!store._mutations[type]) {
	          console.error(("[vuex] unknown local mutation type: " + (args.type) + ", global type: " + type));
	          return
	        }
	      }
	
	      store.commit(type, payload, options);
	    }
	  };
	
	  // getters and state object must be gotten lazily
	  // because they will be changed by vm update
	  Object.defineProperties(local, {
	    getters: {
	      get: noNamespace
	        ? function () { return store.getters; }
	        : function () { return makeLocalGetters(store, namespace); }
	    },
	    state: {
	      get: function () { return getNestedState(store.state, path); }
	    }
	  });
	
	  return local
	}
	
	function makeLocalGetters (store, namespace) {
	  var gettersProxy = {};
	
	  var splitPos = namespace.length;
	  Object.keys(store.getters).forEach(function (type) {
	    // skip if the target getter is not match this namespace
	    if (type.slice(0, splitPos) !== namespace) { return }
	
	    // extract local getter type
	    var localType = type.slice(splitPos);
	
	    // Add a port to the getters proxy.
	    // Define as getter property because
	    // we do not want to evaluate the getters in this time.
	    Object.defineProperty(gettersProxy, localType, {
	      get: function () { return store.getters[type]; },
	      enumerable: true
	    });
	  });
	
	  return gettersProxy
	}
	
	function registerMutation (store, type, handler, local) {
	  var entry = store._mutations[type] || (store._mutations[type] = []);
	  entry.push(function wrappedMutationHandler (payload) {
	    handler(local.state, payload);
	  });
	}
	
	function registerAction (store, type, handler, local) {
	  var entry = store._actions[type] || (store._actions[type] = []);
	  entry.push(function wrappedActionHandler (payload, cb) {
	    var res = handler({
	      dispatch: local.dispatch,
	      commit: local.commit,
	      getters: local.getters,
	      state: local.state,
	      rootGetters: store.getters,
	      rootState: store.state
	    }, payload, cb);
	    if (!isPromise(res)) {
	      res = Promise.resolve(res);
	    }
	    if (store._devtoolHook) {
	      return res.catch(function (err) {
	        store._devtoolHook.emit('vuex:error', err);
	        throw err
	      })
	    } else {
	      return res
	    }
	  });
	}
	
	function registerGetter (store, type, rawGetter, local) {
	  if (store._wrappedGetters[type]) {
	    console.error(("[vuex] duplicate getter key: " + type));
	    return
	  }
	  store._wrappedGetters[type] = function wrappedGetter (store) {
	    return rawGetter(
	      local.state, // local state
	      local.getters, // local getters
	      store.state, // root state
	      store.getters // root getters
	    )
	  };
	}
	
	function enableStrictMode (store) {
	  store._vm.$watch('state', function () {
	    assert(store._committing, "Do not mutate vuex store state outside mutation handlers.");
	  }, { deep: true, sync: true });
	}
	
	function getNestedState (state, path) {
	  return path.length
	    ? path.reduce(function (state, key) { return state[key]; }, state)
	    : state
	}
	
	function unifyObjectStyle (type, payload, options) {
	  if (isObject(type) && type.type) {
	    options = payload;
	    payload = type;
	    type = type.type;
	  }
	
	  assert(typeof type === 'string', ("Expects string as the type, but found " + (typeof type) + "."));
	
	  return { type: type, payload: payload, options: options }
	}
	
	function install (_Vue) {
	  if (Vue) {
	    console.error(
	      '[vuex] already installed. Vue.use(Vuex) should be called only once.'
	    );
	    return
	  }
	  Vue = _Vue;
	  applyMixin(Vue);
	}
	
	// auto install in dist mode
	if (typeof window !== 'undefined' && window.Vue) {
	  install(window.Vue);
	}
	
	var index = {
	  Store: Store,
	  install: install,
	  version: '2.1.2',
	  mapState: mapState,
	  mapMutations: mapMutations,
	  mapGetters: mapGetters,
	  mapActions: mapActions
	};
	
	return index;
	
	})));


/***/ },
/* 212 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__webpack_require__(213)
	__vue_script__ = __webpack_require__(217)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/components/tips.vue: named exports in *.vue files are ignored.")}
	__vue_template__ = __webpack_require__(218)
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-7f332de7/tips.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 213 */
/***/ function(module, exports, __webpack_require__) {

	// style-loader: Adds some css to the DOM by adding a <style> tag
	
	// load the styles
	var content = __webpack_require__(214);
	if(typeof content === 'string') content = [[module.id, content, '']];
	// add the styles to the DOM
	var update = __webpack_require__(216)(content, {});
	if(content.locals) module.exports = content.locals;
	// Hot Module Replacement
	if(false) {
		// When the styles change, update the <style> tags
		if(!content.locals) {
			module.hot.accept("!!./../../node_modules/.npminstall/css-loader/0.23.1/css-loader/index.js?sourceMap!./../../node_modules/vue-loader/lib/style-rewriter.js?id=_v-7f332de7&scoped=true!./../../node_modules/.npminstall/sass-loader/4.0.0/sass-loader/index.js!./../../node_modules/vue-loader/lib/selector.js?type=style&index=0!./tips.vue", function() {
				var newContent = require("!!./../../node_modules/.npminstall/css-loader/0.23.1/css-loader/index.js?sourceMap!./../../node_modules/vue-loader/lib/style-rewriter.js?id=_v-7f332de7&scoped=true!./../../node_modules/.npminstall/sass-loader/4.0.0/sass-loader/index.js!./../../node_modules/vue-loader/lib/selector.js?type=style&index=0!./tips.vue");
				if(typeof newContent === 'string') newContent = [[module.id, newContent, '']];
				update(newContent);
			});
		}
		// When the module is disposed, remove the <style> tags
		module.hot.dispose(function() { update(); });
	}

/***/ },
/* 214 */
/***/ function(module, exports, __webpack_require__) {

	exports = module.exports = __webpack_require__(215)();
	// imports
	
	
	// module
	exports.push([module.id, ".tips[_v-7f332de7] {\n  width: 100%;\n  height: 100%;\n  position: fixed;\n  z-index: 100000;\n  left: 0px;\n  top: 0px; }\n  .tips.slide-transition[_v-7f332de7] {\n    -webkit-transition: all .3s ease;\n    transition: all .3s ease;\n    opacity: 1; }\n  .tips.slide-enter[_v-7f332de7], .tips.slide-leave[_v-7f332de7] {\n    opacity: 0; }\n  .tips .container[_v-7f332de7] {\n    min-width: 120px;\n    min-height: 120px;\n    padding: 10px;\n    position: fixed;\n    left: 50%;\n    top: 50%;\n    -webkit-transform: translate(-50%, -50%) !important;\n            transform: translate(-50%, -50%) !important;\n    border-radius: 4px;\n    opacity: 1;\n    color: #FFF; }\n    .tips .container i[_v-7f332de7] {\n      font-size: 80px;\n      display: block;\n      margin: 0px auto 0px auto;\n      height: 70px; }\n    .tips .container b[_v-7f332de7] {\n      font-weight: 300;\n      font-size: 16px; }\n  .tips.loading .container[_v-7f332de7] {\n    background: #333; }\n  .tips.error .container[_v-7f332de7] {\n    background: #a50d0d; }\n  .tips.success .container[_v-7f332de7] {\n    background: #0da514; }\n", "", {"version":3,"sources":["/./src/components/tips.vue"],"names":[],"mappings":"AAAA;EACE,YAAY;EACZ,aAAa;EACb,gBAAgB;EAChB,gBAAgB;EAChB,UAAU;EACV,SAAS,EAAE;EACX;IACE,iCAAyB;IAAzB,yBAAyB;IACzB,WAAW,EAAE;EACf;IACE,WAAW,EAAE;EACf;IACE,iBAAiB;IACjB,kBAAkB;IAClB,cAAc;IACd,gBAAgB;IAChB,UAAU;IACV,SAAS;IACT,oDAA4C;YAA5C,4CAA4C;IAC5C,mBAAmB;IACnB,WAAW;IACX,YAAY,EAAE;IACd;MACE,gBAAgB;MAChB,eAAe;MACf,0BAA0B;MAC1B,aAAa,EAAE;IACjB;MACE,iBAAiB;MACjB,gBAAgB,EAAE;EACtB;IACE,iBAAiB,EAAE;EACrB;IACE,oBAAoB,EAAE;EACxB;IACE,oBAAoB,EAAE","file":"tips.vue","sourcesContent":[".tips {\n  width: 100%;\n  height: 100%;\n  position: fixed;\n  z-index: 100000;\n  left: 0px;\n  top: 0px; }\n  .tips.slide-transition {\n    transition: all .3s ease;\n    opacity: 1; }\n  .tips.slide-enter, .tips.slide-leave {\n    opacity: 0; }\n  .tips .container {\n    min-width: 120px;\n    min-height: 120px;\n    padding: 10px;\n    position: fixed;\n    left: 50%;\n    top: 50%;\n    transform: translate(-50%, -50%) !important;\n    border-radius: 4px;\n    opacity: 1;\n    color: #FFF; }\n    .tips .container i {\n      font-size: 80px;\n      display: block;\n      margin: 0px auto 0px auto;\n      height: 70px; }\n    .tips .container b {\n      font-weight: 300;\n      font-size: 16px; }\n  .tips.loading .container {\n    background: #333; }\n  .tips.error .container {\n    background: #a50d0d; }\n  .tips.success .container {\n    background: #0da514; }\n"],"sourceRoot":"webpack://"}]);
	
	// exports


/***/ },
/* 215 */
/***/ function(module, exports) {

	/*
		MIT License http://www.opensource.org/licenses/mit-license.php
		Author Tobias Koppers @sokra
	*/
	// css base code, injected by the css-loader
	module.exports = function() {
		var list = [];
	
		// return the list of modules as css string
		list.toString = function toString() {
			var result = [];
			for(var i = 0; i < this.length; i++) {
				var item = this[i];
				if(item[2]) {
					result.push("@media " + item[2] + "{" + item[1] + "}");
				} else {
					result.push(item[1]);
				}
			}
			return result.join("");
		};
	
		// import a list of modules into the list
		list.i = function(modules, mediaQuery) {
			if(typeof modules === "string")
				modules = [[null, modules, ""]];
			var alreadyImportedModules = {};
			for(var i = 0; i < this.length; i++) {
				var id = this[i][0];
				if(typeof id === "number")
					alreadyImportedModules[id] = true;
			}
			for(i = 0; i < modules.length; i++) {
				var item = modules[i];
				// skip already imported module
				// this implementation is not 100% perfect for weird media query combinations
				//  when a module is imported multiple times with different media queries.
				//  I hope this will never occur (Hey this way we have smaller bundles)
				if(typeof item[0] !== "number" || !alreadyImportedModules[item[0]]) {
					if(mediaQuery && !item[2]) {
						item[2] = mediaQuery;
					} else if(mediaQuery) {
						item[2] = "(" + item[2] + ") and (" + mediaQuery + ")";
					}
					list.push(item);
				}
			}
		};
		return list;
	};


/***/ },
/* 216 */
/***/ function(module, exports, __webpack_require__) {

	/*
		MIT License http://www.opensource.org/licenses/mit-license.php
		Author Tobias Koppers @sokra
	*/
	var stylesInDom = {},
		memoize = function(fn) {
			var memo;
			return function () {
				if (typeof memo === "undefined") memo = fn.apply(this, arguments);
				return memo;
			};
		},
		isOldIE = memoize(function() {
			return /msie [6-9]\b/.test(window.navigator.userAgent.toLowerCase());
		}),
		getHeadElement = memoize(function () {
			return document.head || document.getElementsByTagName("head")[0];
		}),
		singletonElement = null,
		singletonCounter = 0,
		styleElementsInsertedAtTop = [];
	
	module.exports = function(list, options) {
		if(false) {
			if(typeof document !== "object") throw new Error("The style-loader cannot be used in a non-browser environment");
		}
	
		options = options || {};
		// Force single-tag solution on IE6-9, which has a hard limit on the # of <style>
		// tags it will allow on a page
		if (typeof options.singleton === "undefined") options.singleton = isOldIE();
	
		// By default, add <style> tags to the bottom of <head>.
		if (typeof options.insertAt === "undefined") options.insertAt = "bottom";
	
		var styles = listToStyles(list);
		addStylesToDom(styles, options);
	
		return function update(newList) {
			var mayRemove = [];
			for(var i = 0; i < styles.length; i++) {
				var item = styles[i];
				var domStyle = stylesInDom[item.id];
				domStyle.refs--;
				mayRemove.push(domStyle);
			}
			if(newList) {
				var newStyles = listToStyles(newList);
				addStylesToDom(newStyles, options);
			}
			for(var i = 0; i < mayRemove.length; i++) {
				var domStyle = mayRemove[i];
				if(domStyle.refs === 0) {
					for(var j = 0; j < domStyle.parts.length; j++)
						domStyle.parts[j]();
					delete stylesInDom[domStyle.id];
				}
			}
		};
	}
	
	function addStylesToDom(styles, options) {
		for(var i = 0; i < styles.length; i++) {
			var item = styles[i];
			var domStyle = stylesInDom[item.id];
			if(domStyle) {
				domStyle.refs++;
				for(var j = 0; j < domStyle.parts.length; j++) {
					domStyle.parts[j](item.parts[j]);
				}
				for(; j < item.parts.length; j++) {
					domStyle.parts.push(addStyle(item.parts[j], options));
				}
			} else {
				var parts = [];
				for(var j = 0; j < item.parts.length; j++) {
					parts.push(addStyle(item.parts[j], options));
				}
				stylesInDom[item.id] = {id: item.id, refs: 1, parts: parts};
			}
		}
	}
	
	function listToStyles(list) {
		var styles = [];
		var newStyles = {};
		for(var i = 0; i < list.length; i++) {
			var item = list[i];
			var id = item[0];
			var css = item[1];
			var media = item[2];
			var sourceMap = item[3];
			var part = {css: css, media: media, sourceMap: sourceMap};
			if(!newStyles[id])
				styles.push(newStyles[id] = {id: id, parts: [part]});
			else
				newStyles[id].parts.push(part);
		}
		return styles;
	}
	
	function insertStyleElement(options, styleElement) {
		var head = getHeadElement();
		var lastStyleElementInsertedAtTop = styleElementsInsertedAtTop[styleElementsInsertedAtTop.length - 1];
		if (options.insertAt === "top") {
			if(!lastStyleElementInsertedAtTop) {
				head.insertBefore(styleElement, head.firstChild);
			} else if(lastStyleElementInsertedAtTop.nextSibling) {
				head.insertBefore(styleElement, lastStyleElementInsertedAtTop.nextSibling);
			} else {
				head.appendChild(styleElement);
			}
			styleElementsInsertedAtTop.push(styleElement);
		} else if (options.insertAt === "bottom") {
			head.appendChild(styleElement);
		} else {
			throw new Error("Invalid value for parameter 'insertAt'. Must be 'top' or 'bottom'.");
		}
	}
	
	function removeStyleElement(styleElement) {
		styleElement.parentNode.removeChild(styleElement);
		var idx = styleElementsInsertedAtTop.indexOf(styleElement);
		if(idx >= 0) {
			styleElementsInsertedAtTop.splice(idx, 1);
		}
	}
	
	function createStyleElement(options) {
		var styleElement = document.createElement("style");
		styleElement.type = "text/css";
		insertStyleElement(options, styleElement);
		return styleElement;
	}
	
	function addStyle(obj, options) {
		var styleElement, update, remove;
	
		if (options.singleton) {
			var styleIndex = singletonCounter++;
			styleElement = singletonElement || (singletonElement = createStyleElement(options));
			update = applyToSingletonTag.bind(null, styleElement, styleIndex, false);
			remove = applyToSingletonTag.bind(null, styleElement, styleIndex, true);
		} else {
			styleElement = createStyleElement(options);
			update = applyToTag.bind(null, styleElement);
			remove = function() {
				removeStyleElement(styleElement);
			};
		}
	
		update(obj);
	
		return function updateStyle(newObj) {
			if(newObj) {
				if(newObj.css === obj.css && newObj.media === obj.media && newObj.sourceMap === obj.sourceMap)
					return;
				update(obj = newObj);
			} else {
				remove();
			}
		};
	}
	
	var replaceText = (function () {
		var textStore = [];
	
		return function (index, replacement) {
			textStore[index] = replacement;
			return textStore.filter(Boolean).join('\n');
		};
	})();
	
	function applyToSingletonTag(styleElement, index, remove, obj) {
		var css = remove ? "" : obj.css;
	
		if (styleElement.styleSheet) {
			styleElement.styleSheet.cssText = replaceText(index, css);
		} else {
			var cssNode = document.createTextNode(css);
			var childNodes = styleElement.childNodes;
			if (childNodes[index]) styleElement.removeChild(childNodes[index]);
			if (childNodes.length) {
				styleElement.insertBefore(cssNode, childNodes[index]);
			} else {
				styleElement.appendChild(cssNode);
			}
		}
	}
	
	function applyToTag(styleElement, obj) {
		var css = obj.css;
		var media = obj.media;
		var sourceMap = obj.sourceMap;
	
		if (media) {
			styleElement.setAttribute("media", media);
		}
	
		if (sourceMap) {
			// https://developer.chrome.com/devtools/docs/javascript-debugging
			// this makes source maps inside style tags work properly in Chrome
			css += '\n/*# sourceURL=' + sourceMap.sources[0] + ' */';
			// http://stackoverflow.com/a/26603875
			css += "\n/*# sourceMappingURL=data:application/json;base64," + btoa(unescape(encodeURIComponent(JSON.stringify(sourceMap)))) + " */";
		}
	
		if (styleElement.styleSheet) {
			styleElement.styleSheet.cssText = css;
		} else {
			while(styleElement.firstChild) {
				styleElement.removeChild(styleElement.firstChild);
			}
			styleElement.appendChild(document.createTextNode(css));
		}
	}


/***/ },
/* 217 */
/***/ function(module, exports) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	// <style lang='sass' scoped>
	// .tips{
	//     width: 100%;
	//     height: 100%;
	//     position: fixed;
	//     z-index: 100000;
	//     left: 0px;
	//     top: 0px;
	//     &.slide-transition{
	//         transition: all .3s ease;
	//         opacity: 1;
	//     }
	//     &.slide-enter,&.slide-leave{opacity: 0;}
	//     .container{
	//         min-width: 120px;
	//         min-height: 120px;
	//         padding: 10px;
	//         position: fixed;
	//         left: 50%;
	//         top: 50%;
	//         transform: translate(-50%,-50%)!important;
	//         border-radius: 4px;
	//         opacity: 1;
	//         color: #FFF;
	//         i{
	//             font-size: 80px;
	//             display: block;
	//             margin: 0px auto 0px auto;
	//             height: 70px;
	//         }
	//         b{
	//             font-weight: 300;
	//             font-size: 16px;
	//         }
	//     }
	//     &.loading{
	//         .container{
	//             background: #333;
	//         }
	//     }
	//     &.error{
	//         .container{
	//             background: #a50d0d;
	//         }
	//     }
	//     &.success{
	//         .container{
	//             background: #0da514;
	//         }
	//     }
	// }
	// </style>
	// <template>
	//     <div class="tips" v-show="show" :class="options.type">
	//         <div class="container">
	//             <i v-if="options.type=='loading'" class="mui-spinner" style="width: 50px;"></i>
	//             <i v-if="options.type=='error'" class="mui-icon mui-icon-closeempty"></i>
	//             <i v-if="options.type=='success'" class="mui-icon mui-icon-checkmarkempty"></i>
	//             <b>{{ options.content }}</b>
	//         </div>
	//     </div>
	// </template>
	// <script>
	exports.default = {
	    props: {
	        options: {
	            type: Object,
	            default: {}
	        },
	        show: {
	            type: Boolean,
	            default: false
	        }
	    },
	    watch: {
	        options: function options(val) {
	            var _this = this;
	
	            if (this.options.type != 'loading') {
	                clearTimeout(this.timeout);
	                this.timeout = setTimeout(function () {
	                    _this.show = false;
	                }, 2000);
	            }
	        }
	    }
	};
	// </script>
	/* generated by vue-loader */

/***/ },
/* 218 */
/***/ function(module, exports) {

	module.exports = "\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<div class=\"tips\" v-show=\"show\" :class=\"options.type\" _v-7f332de7=\"\">\n    <div class=\"container\" _v-7f332de7=\"\">\n        <i v-if=\"options.type=='loading'\" class=\"mui-spinner\" style=\"width: 50px;\" _v-7f332de7=\"\"></i>\n        <i v-if=\"options.type=='error'\" class=\"mui-icon mui-icon-closeempty\" _v-7f332de7=\"\"></i>\n        <i v-if=\"options.type=='success'\" class=\"mui-icon mui-icon-checkmarkempty\" _v-7f332de7=\"\"></i>\n        <b _v-7f332de7=\"\">{{ options.content }}</b>\n    </div>\n</div>\n";

/***/ },
/* 219 */
/***/ function(module, exports, __webpack_require__) {

	var __vue_script__, __vue_template__
	__vue_script__ = __webpack_require__(220)
	if (__vue_script__ &&
	    __vue_script__.__esModule &&
	    Object.keys(__vue_script__).length > 1) {
	  console.warn("[vue-loader] src/service/wechat.vue: named exports in *.vue files are ignored.")}
	module.exports = __vue_script__ || {}
	if (module.exports.__esModule) module.exports = module.exports.default
	if (__vue_template__) {
	(typeof module.exports === "function" ? (module.exports.options || (module.exports.options = {})) : module.exports).template = __vue_template__
	}
	if (false) {(function () {  module.hot.accept()
	  var hotAPI = require("vue-hot-reload-api")
	  hotAPI.install(require("vue"), false)
	  if (!hotAPI.compatible) return
	  var id = "_v-b89d754c/wechat.vue"
	  if (!module.hot.data) {
	    hotAPI.createRecord(id, module.exports)
	  } else {
	    hotAPI.update(id, module.exports, __vue_template__)
	  }
	})()}

/***/ },
/* 220 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	
	var _stringify = __webpack_require__(52);
	
	var _stringify2 = _interopRequireDefault(_stringify);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	// <script>
	exports.default = {
	    ready: function ready() {
	        this.$http.post(API.getWxconfig, {}).then(function (data) {
	            var config = data.data;
	            wx.config({
	                debug: false,
	                appId: config.appId,
	                timestamp: config.timestamp,
	                nonceStr: config.nonceStr,
	                signature: config.signature,
	                jsApiList: ['checkJsApi', 'onMenuShareTimeline', 'onMenuShareAppMessage', 'onMenuShareQQ', 'onMenuShareWeibo', 'onMenuShareQZone', 'hideMenuItems', 'showMenuItems', 'hideAllNonBaseMenuItem', 'showAllNonBaseMenuItem', 'translateVoice', 'startRecord', 'stopRecord', 'onVoiceRecordEnd', 'playVoice', 'onVoicePlayEnd', 'pauseVoice', 'stopVoice', 'uploadVoice', 'downloadVoice', 'chooseImage', 'previewImage', 'uploadImage', 'downloadImage', 'getNetworkType', 'openLocation', 'getLocation', 'hideOptionMenu', 'showOptionMenu', 'closeWindow', 'scanQRCode', 'chooseWXPay', 'openProductSpecificView', 'addCard', 'chooseCard', 'openCard']
	            });
	
	            wx.ready(function () {
	                // 隐藏其他按钮
	                wx.hideAllNonBaseMenuItem();
	                wx.hideMenuItems({
	                    menuList: ['copyUrl']
	                });
	            });
	
	            wx.error(function (res) {
	                alert((0, _stringify2.default)(res));
	            });
	        });
	    }
	};
	// </script>
	/* generated by vue-loader */

/***/ },
/* 221 */
/***/ function(module, exports) {

	module.exports = "\n<router-view></router-view>\n<tip :show.sync='tips.show' :options.sync='tips.options'></tip>\n<wechat></wechat>\n";

/***/ },
/* 222 */
/***/ function(module, exports, __webpack_require__) {

	"use strict";
	
	var _vue = __webpack_require__(2);
	
	var _vue2 = _interopRequireDefault(_vue);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	Date.prototype.format = function (format) {
	       var date = {
	              "M+": this.getMonth() + 1,
	              "d+": this.getDate(),
	              "h+": this.getHours(),
	              "m+": this.getMinutes(),
	              "s+": this.getSeconds(),
	              "q+": Math.floor((this.getMonth() + 3) / 3),
	              "S+": this.getMilliseconds()
	       };
	       if (/(y+)/i.test(format)) {
	              format = format.replace(RegExp.$1, (this.getFullYear() + '').substr(4 - RegExp.$1.length));
	       }
	       for (var k in date) {
	              if (new RegExp("(" + k + ")").test(format)) {
	                     format = format.replace(RegExp.$1, RegExp.$1.length == 1 ? date[k] : ("00" + date[k]).substr(("" + date[k]).length));
	              }
	       }
	       return format;
	};
	
	_vue2.default.filter('format', function (value, key) {
	       if (!value || value.length < 8) return value;
	       // yyyy年 MM月 dd日 hh:mm:ss
	       return new Date(value * 1000).format(key);
	});
	// 性别
	_vue2.default.filter('sex', function (value) {
	       if (value == 1) return '男';else return '女';
	});
	// 年龄
	_vue2.default.filter('years', function (value) {
	       if (value.length < 8) return value;
	       return new Date().format('yyyy') - new Date(value * 1000).format('yyyy') + '岁';
	});

/***/ },
/* 223 */
/***/ function(module, exports, __webpack_require__) {

	"use strict";
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	
	var _stringify = __webpack_require__(52);
	
	var _stringify2 = _interopRequireDefault(_stringify);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	exports.default = {
	    set: function set(key, value) {
	        localStorage.setItem(key, (0, _stringify2.default)(value));
	    },
	    get: function get(key) {
	        var _strinify = localStorage.getItem(key);
	        if (_strinify) {
	            return JSON.parse(_strinify);
	        }
	        return null;
	    },
	    remove: function remove(key) {
	        localStorage.removeItem(key);
	    }
	};

/***/ },
/* 224 */
/***/ function(module, exports, __webpack_require__) {

	var __WEBPACK_AMD_DEFINE_RESULT__;'use strict';
	
	var _typeof2 = __webpack_require__(92);
	
	var _typeof3 = _interopRequireDefault(_typeof2);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	;(function () {
		'use strict';
	
		/**
	  * @preserve FastClick: polyfill to remove click delays on browsers with touch UIs.
	  *
	  * @codingstandard ftlabs-jsv2
	  * @copyright The Financial Times Limited [All Rights Reserved]
	  * @license MIT License (see LICENSE.txt)
	  */
	
		/*jslint browser:true, node:true*/
		/*global define, Event, Node*/
	
		/**
	  * Instantiate fast-clicking listeners on the specified layer.
	  *
	  * @constructor
	  * @param {Element} layer The layer to listen on
	  * @param {Object} [options={}] The options to override the defaults
	  */
	
		function FastClick(layer, options) {
			var oldOnClick;
	
			options = options || {};
	
			/**
	   * Whether a click is currently being tracked.
	   *
	   * @type boolean
	   */
			this.trackingClick = false;
	
			/**
	   * Timestamp for when click tracking started.
	   *
	   * @type number
	   */
			this.trackingClickStart = 0;
	
			/**
	   * The element being tracked for a click.
	   *
	   * @type EventTarget
	   */
			this.targetElement = null;
	
			/**
	   * X-coordinate of touch start event.
	   *
	   * @type number
	   */
			this.touchStartX = 0;
	
			/**
	   * Y-coordinate of touch start event.
	   *
	   * @type number
	   */
			this.touchStartY = 0;
	
			/**
	   * ID of the last touch, retrieved from Touch.identifier.
	   *
	   * @type number
	   */
			this.lastTouchIdentifier = 0;
	
			/**
	   * Touchmove boundary, beyond which a click will be cancelled.
	   *
	   * @type number
	   */
			this.touchBoundary = options.touchBoundary || 10;
	
			/**
	   * The FastClick layer.
	   *
	   * @type Element
	   */
			this.layer = layer;
	
			/**
	   * The minimum time between tap(touchstart and touchend) events
	   *
	   * @type number
	   */
			this.tapDelay = options.tapDelay || 200;
	
			/**
	   * The maximum time for a tap
	   *
	   * @type number
	   */
			this.tapTimeout = options.tapTimeout || 700;
	
			if (FastClick.notNeeded(layer)) {
				return;
			}
	
			// Some old versions of Android don't have Function.prototype.bind
			function bind(method, context) {
				return function () {
					return method.apply(context, arguments);
				};
			}
	
			var methods = ['onMouse', 'onClick', 'onTouchStart', 'onTouchMove', 'onTouchEnd', 'onTouchCancel'];
			var context = this;
			for (var i = 0, l = methods.length; i < l; i++) {
				context[methods[i]] = bind(context[methods[i]], context);
			}
	
			// Set up event handlers as required
			if (deviceIsAndroid) {
				layer.addEventListener('mouseover', this.onMouse, true);
				layer.addEventListener('mousedown', this.onMouse, true);
				layer.addEventListener('mouseup', this.onMouse, true);
			}
	
			layer.addEventListener('click', this.onClick, true);
			layer.addEventListener('touchstart', this.onTouchStart, false);
			layer.addEventListener('touchmove', this.onTouchMove, false);
			layer.addEventListener('touchend', this.onTouchEnd, false);
			layer.addEventListener('touchcancel', this.onTouchCancel, false);
	
			// Hack is required for browsers that don't support Event#stopImmediatePropagation (e.g. Android 2)
			// which is how FastClick normally stops click events bubbling to callbacks registered on the FastClick
			// layer when they are cancelled.
			if (!Event.prototype.stopImmediatePropagation) {
				layer.removeEventListener = function (type, callback, capture) {
					var rmv = Node.prototype.removeEventListener;
					if (type === 'click') {
						rmv.call(layer, type, callback.hijacked || callback, capture);
					} else {
						rmv.call(layer, type, callback, capture);
					}
				};
	
				layer.addEventListener = function (type, callback, capture) {
					var adv = Node.prototype.addEventListener;
					if (type === 'click') {
						adv.call(layer, type, callback.hijacked || (callback.hijacked = function (event) {
							if (!event.propagationStopped) {
								callback(event);
							}
						}), capture);
					} else {
						adv.call(layer, type, callback, capture);
					}
				};
			}
	
			// If a handler is already declared in the element's onclick attribute, it will be fired before
			// FastClick's onClick handler. Fix this by pulling out the user-defined handler function and
			// adding it as listener.
			if (typeof layer.onclick === 'function') {
	
				// Android browser on at least 3.2 requires a new reference to the function in layer.onclick
				// - the old one won't work if passed to addEventListener directly.
				oldOnClick = layer.onclick;
				layer.addEventListener('click', function (event) {
					oldOnClick(event);
				}, false);
				layer.onclick = null;
			}
		}
	
		/**
	 * Windows Phone 8.1 fakes user agent string to look like Android and iPhone.
	 *
	 * @type boolean
	 */
		var deviceIsWindowsPhone = navigator.userAgent.indexOf("Windows Phone") >= 0;
	
		/**
	  * Android requires exceptions.
	  *
	  * @type boolean
	  */
		var deviceIsAndroid = navigator.userAgent.indexOf('Android') > 0 && !deviceIsWindowsPhone;
	
		/**
	  * iOS requires exceptions.
	  *
	  * @type boolean
	  */
		var deviceIsIOS = /iP(ad|hone|od)/.test(navigator.userAgent) && !deviceIsWindowsPhone;
	
		/**
	  * iOS 4 requires an exception for select elements.
	  *
	  * @type boolean
	  */
		var deviceIsIOS4 = deviceIsIOS && /OS 4_\d(_\d)?/.test(navigator.userAgent);
	
		/**
	  * iOS 6.0-7.* requires the target element to be manually derived
	  *
	  * @type boolean
	  */
		var deviceIsIOSWithBadTarget = deviceIsIOS && /OS [6-7]_\d/.test(navigator.userAgent);
	
		/**
	  * BlackBerry requires exceptions.
	  *
	  * @type boolean
	  */
		var deviceIsBlackBerry10 = navigator.userAgent.indexOf('BB10') > 0;
	
		/**
	  * Determine whether a given element requires a native click.
	  *
	  * @param {EventTarget|Element} target Target DOM element
	  * @returns {boolean} Returns true if the element needs a native click
	  */
		FastClick.prototype.needsClick = function (target) {
			switch (target.nodeName.toLowerCase()) {
	
				// Don't send a synthetic click to disabled inputs (issue #62)
				case 'button':
				case 'select':
				case 'textarea':
					if (target.disabled) {
						return true;
					}
	
					break;
				case 'input':
	
					// File inputs need real clicks on iOS 6 due to a browser bug (issue #68)
					if (deviceIsIOS && target.type === 'file' || target.disabled) {
						return true;
					}
	
					break;
				case 'label':
				case 'iframe': // iOS8 homescreen apps can prevent events bubbling into frames
				case 'video':
					return true;
			}
	
			return (/\bneedsclick\b/.test(target.className)
			);
		};
	
		/**
	  * Determine whether a given element requires a call to focus to simulate click into element.
	  *
	  * @param {EventTarget|Element} target Target DOM element
	  * @returns {boolean} Returns true if the element requires a call to focus to simulate native click.
	  */
		FastClick.prototype.needsFocus = function (target) {
			switch (target.nodeName.toLowerCase()) {
				case 'textarea':
					return true;
				case 'select':
					return !deviceIsAndroid;
				case 'input':
					switch (target.type) {
						case 'button':
						case 'checkbox':
						case 'file':
						case 'image':
						case 'radio':
						case 'submit':
							return false;
					}
	
					// No point in attempting to focus disabled inputs
					return !target.disabled && !target.readOnly;
				default:
					return (/\bneedsfocus\b/.test(target.className)
					);
			}
		};
	
		/**
	  * Send a click event to the specified element.
	  *
	  * @param {EventTarget|Element} targetElement
	  * @param {Event} event
	  */
		FastClick.prototype.sendClick = function (targetElement, event) {
			var clickEvent, touch;
	
			// On some Android devices activeElement needs to be blurred otherwise the synthetic click will have no effect (#24)
			if (document.activeElement && document.activeElement !== targetElement) {
				document.activeElement.blur();
			}
	
			touch = event.changedTouches[0];
	
			// Synthesise a click event, with an extra attribute so it can be tracked
			clickEvent = document.createEvent('MouseEvents');
			clickEvent.initMouseEvent(this.determineEventType(targetElement), true, true, window, 1, touch.screenX, touch.screenY, touch.clientX, touch.clientY, false, false, false, false, 0, null);
			clickEvent.forwardedTouchEvent = true;
			targetElement.dispatchEvent(clickEvent);
		};
	
		FastClick.prototype.determineEventType = function (targetElement) {
	
			//Issue #159: Android Chrome Select Box does not open with a synthetic click event
			if (deviceIsAndroid && targetElement.tagName.toLowerCase() === 'select') {
				return 'mousedown';
			}
	
			return 'click';
		};
	
		/**
	  * @param {EventTarget|Element} targetElement
	  */
		FastClick.prototype.focus = function (targetElement) {
			var length;
	
			// Issue #160: on iOS 7, some input elements (e.g. date datetime month) throw a vague TypeError on setSelectionRange. These elements don't have an integer value for the selectionStart and selectionEnd properties, but unfortunately that can't be used for detection because accessing the properties also throws a TypeError. Just check the type instead. Filed as Apple bug #15122724.
			if (deviceIsIOS && targetElement.setSelectionRange && targetElement.type.indexOf('date') !== 0 && targetElement.type !== 'time' && targetElement.type !== 'month') {
				length = targetElement.value.length;
				targetElement.setSelectionRange(length, length);
			} else {
				targetElement.focus();
			}
		};
	
		/**
	  * Check whether the given target element is a child of a scrollable layer and if so, set a flag on it.
	  *
	  * @param {EventTarget|Element} targetElement
	  */
		FastClick.prototype.updateScrollParent = function (targetElement) {
			var scrollParent, parentElement;
	
			scrollParent = targetElement.fastClickScrollParent;
	
			// Attempt to discover whether the target element is contained within a scrollable layer. Re-check if the
			// target element was moved to another parent.
			if (!scrollParent || !scrollParent.contains(targetElement)) {
				parentElement = targetElement;
				do {
					if (parentElement.scrollHeight > parentElement.offsetHeight) {
						scrollParent = parentElement;
						targetElement.fastClickScrollParent = parentElement;
						break;
					}
	
					parentElement = parentElement.parentElement;
				} while (parentElement);
			}
	
			// Always update the scroll top tracker if possible.
			if (scrollParent) {
				scrollParent.fastClickLastScrollTop = scrollParent.scrollTop;
			}
		};
	
		/**
	  * @param {EventTarget} targetElement
	  * @returns {Element|EventTarget}
	  */
		FastClick.prototype.getTargetElementFromEventTarget = function (eventTarget) {
	
			// On some older browsers (notably Safari on iOS 4.1 - see issue #56) the event target may be a text node.
			if (eventTarget.nodeType === Node.TEXT_NODE) {
				return eventTarget.parentNode;
			}
	
			return eventTarget;
		};
	
		/**
	  * On touch start, record the position and scroll offset.
	  *
	  * @param {Event} event
	  * @returns {boolean}
	  */
		FastClick.prototype.onTouchStart = function (event) {
			var targetElement, touch, selection;
	
			// Ignore multiple touches, otherwise pinch-to-zoom is prevented if both fingers are on the FastClick element (issue #111).
			if (event.targetTouches.length > 1) {
				return true;
			}
	
			targetElement = this.getTargetElementFromEventTarget(event.target);
			touch = event.targetTouches[0];
	
			if (deviceIsIOS) {
	
				// Only trusted events will deselect text on iOS (issue #49)
				selection = window.getSelection();
				if (selection.rangeCount && !selection.isCollapsed) {
					return true;
				}
	
				if (!deviceIsIOS4) {
	
					// Weird things happen on iOS when an alert or confirm dialog is opened from a click event callback (issue #23):
					// when the user next taps anywhere else on the page, new touchstart and touchend events are dispatched
					// with the same identifier as the touch event that previously triggered the click that triggered the alert.
					// Sadly, there is an issue on iOS 4 that causes some normal touch events to have the same identifier as an
					// immediately preceeding touch event (issue #52), so this fix is unavailable on that platform.
					// Issue 120: touch.identifier is 0 when Chrome dev tools 'Emulate touch events' is set with an iOS device UA string,
					// which causes all touch events to be ignored. As this block only applies to iOS, and iOS identifiers are always long,
					// random integers, it's safe to to continue if the identifier is 0 here.
					if (touch.identifier && touch.identifier === this.lastTouchIdentifier) {
						event.preventDefault();
						return false;
					}
	
					this.lastTouchIdentifier = touch.identifier;
	
					// If the target element is a child of a scrollable layer (using -webkit-overflow-scrolling: touch) and:
					// 1) the user does a fling scroll on the scrollable layer
					// 2) the user stops the fling scroll with another tap
					// then the event.target of the last 'touchend' event will be the element that was under the user's finger
					// when the fling scroll was started, causing FastClick to send a click event to that layer - unless a check
					// is made to ensure that a parent layer was not scrolled before sending a synthetic click (issue #42).
					this.updateScrollParent(targetElement);
				}
			}
	
			this.trackingClick = true;
			this.trackingClickStart = event.timeStamp;
			this.targetElement = targetElement;
	
			this.touchStartX = touch.pageX;
			this.touchStartY = touch.pageY;
	
			// Prevent phantom clicks on fast double-tap (issue #36)
			if (event.timeStamp - this.lastClickTime < this.tapDelay) {
				event.preventDefault();
			}
	
			return true;
		};
	
		/**
	  * Based on a touchmove event object, check whether the touch has moved past a boundary since it started.
	  *
	  * @param {Event} event
	  * @returns {boolean}
	  */
		FastClick.prototype.touchHasMoved = function (event) {
			var touch = event.changedTouches[0],
			    boundary = this.touchBoundary;
	
			if (Math.abs(touch.pageX - this.touchStartX) > boundary || Math.abs(touch.pageY - this.touchStartY) > boundary) {
				return true;
			}
	
			return false;
		};
	
		/**
	  * Update the last position.
	  *
	  * @param {Event} event
	  * @returns {boolean}
	  */
		FastClick.prototype.onTouchMove = function (event) {
			if (!this.trackingClick) {
				return true;
			}
	
			// If the touch has moved, cancel the click tracking
			if (this.targetElement !== this.getTargetElementFromEventTarget(event.target) || this.touchHasMoved(event)) {
				this.trackingClick = false;
				this.targetElement = null;
			}
	
			return true;
		};
	
		/**
	  * Attempt to find the labelled control for the given label element.
	  *
	  * @param {EventTarget|HTMLLabelElement} labelElement
	  * @returns {Element|null}
	  */
		FastClick.prototype.findControl = function (labelElement) {
	
			// Fast path for newer browsers supporting the HTML5 control attribute
			if (labelElement.control !== undefined) {
				return labelElement.control;
			}
	
			// All browsers under test that support touch events also support the HTML5 htmlFor attribute
			if (labelElement.htmlFor) {
				return document.getElementById(labelElement.htmlFor);
			}
	
			// If no for attribute exists, attempt to retrieve the first labellable descendant element
			// the list of which is defined here: http://www.w3.org/TR/html5/forms.html#category-label
			return labelElement.querySelector('button, input:not([type=hidden]), keygen, meter, output, progress, select, textarea');
		};
	
		/**
	  * On touch end, determine whether to send a click event at once.
	  *
	  * @param {Event} event
	  * @returns {boolean}
	  */
		FastClick.prototype.onTouchEnd = function (event) {
			var forElement,
			    trackingClickStart,
			    targetTagName,
			    scrollParent,
			    touch,
			    targetElement = this.targetElement;
	
			if (!this.trackingClick) {
				return true;
			}
	
			// Prevent phantom clicks on fast double-tap (issue #36)
			if (event.timeStamp - this.lastClickTime < this.tapDelay) {
				this.cancelNextClick = true;
				return true;
			}
	
			if (event.timeStamp - this.trackingClickStart > this.tapTimeout) {
				return true;
			}
	
			// Reset to prevent wrong click cancel on input (issue #156).
			this.cancelNextClick = false;
	
			this.lastClickTime = event.timeStamp;
	
			trackingClickStart = this.trackingClickStart;
			this.trackingClick = false;
			this.trackingClickStart = 0;
	
			// On some iOS devices, the targetElement supplied with the event is invalid if the layer
			// is performing a transition or scroll, and has to be re-detected manually. Note that
			// for this to function correctly, it must be called *after* the event target is checked!
			// See issue #57; also filed as rdar://13048589 .
			if (deviceIsIOSWithBadTarget) {
				touch = event.changedTouches[0];
	
				// In certain cases arguments of elementFromPoint can be negative, so prevent setting targetElement to null
				targetElement = document.elementFromPoint(touch.pageX - window.pageXOffset, touch.pageY - window.pageYOffset) || targetElement;
				targetElement.fastClickScrollParent = this.targetElement.fastClickScrollParent;
			}
	
			targetTagName = targetElement.tagName.toLowerCase();
			if (targetTagName === 'label') {
				forElement = this.findControl(targetElement);
				if (forElement) {
					this.focus(targetElement);
					if (deviceIsAndroid) {
						return false;
					}
	
					targetElement = forElement;
				}
			} else if (this.needsFocus(targetElement)) {
	
				// Case 1: If the touch started a while ago (best guess is 100ms based on tests for issue #36) then focus will be triggered anyway. Return early and unset the target element reference so that the subsequent click will be allowed through.
				// Case 2: Without this exception for input elements tapped when the document is contained in an iframe, then any inputted text won't be visible even though the value attribute is updated as the user types (issue #37).
				if (event.timeStamp - trackingClickStart > 100 || deviceIsIOS && window.top !== window && targetTagName === 'input') {
					this.targetElement = null;
					return false;
				}
	
				this.focus(targetElement);
				this.sendClick(targetElement, event);
	
				// Select elements need the event to go through on iOS 4, otherwise the selector menu won't open.
				// Also this breaks opening selects when VoiceOver is active on iOS6, iOS7 (and possibly others)
				if (!deviceIsIOS || targetTagName !== 'select') {
					this.targetElement = null;
					event.preventDefault();
				}
	
				return false;
			}
	
			if (deviceIsIOS && !deviceIsIOS4) {
	
				// Don't send a synthetic click event if the target element is contained within a parent layer that was scrolled
				// and this tap is being used to stop the scrolling (usually initiated by a fling - issue #42).
				scrollParent = targetElement.fastClickScrollParent;
				if (scrollParent && scrollParent.fastClickLastScrollTop !== scrollParent.scrollTop) {
					return true;
				}
			}
	
			// Prevent the actual click from going though - unless the target node is marked as requiring
			// real clicks or if it is in the whitelist in which case only non-programmatic clicks are permitted.
			if (!this.needsClick(targetElement)) {
				event.preventDefault();
				this.sendClick(targetElement, event);
			}
	
			return false;
		};
	
		/**
	  * On touch cancel, stop tracking the click.
	  *
	  * @returns {void}
	  */
		FastClick.prototype.onTouchCancel = function () {
			this.trackingClick = false;
			this.targetElement = null;
		};
	
		/**
	  * Determine mouse events which should be permitted.
	  *
	  * @param {Event} event
	  * @returns {boolean}
	  */
		FastClick.prototype.onMouse = function (event) {
	
			// If a target element was never set (because a touch event was never fired) allow the event
			if (!this.targetElement) {
				return true;
			}
	
			if (event.forwardedTouchEvent) {
				return true;
			}
	
			// Programmatically generated events targeting a specific element should be permitted
			if (!event.cancelable) {
				return true;
			}
	
			// Derive and check the target element to see whether the mouse event needs to be permitted;
			// unless explicitly enabled, prevent non-touch click events from triggering actions,
			// to prevent ghost/doubleclicks.
			if (!this.needsClick(this.targetElement) || this.cancelNextClick) {
	
				// Prevent any user-added listeners declared on FastClick element from being fired.
				if (event.stopImmediatePropagation) {
					event.stopImmediatePropagation();
				} else {
	
					// Part of the hack for browsers that don't support Event#stopImmediatePropagation (e.g. Android 2)
					event.propagationStopped = true;
				}
	
				// Cancel the event
				event.stopPropagation();
				event.preventDefault();
	
				return false;
			}
	
			// If the mouse event is permitted, return true for the action to go through.
			return true;
		};
	
		/**
	  * On actual clicks, determine whether this is a touch-generated click, a click action occurring
	  * naturally after a delay after a touch (which needs to be cancelled to avoid duplication), or
	  * an actual click which should be permitted.
	  *
	  * @param {Event} event
	  * @returns {boolean}
	  */
		FastClick.prototype.onClick = function (event) {
			var permitted;
	
			// It's possible for another FastClick-like library delivered with third-party code to fire a click event before FastClick does (issue #44). In that case, set the click-tracking flag back to false and return early. This will cause onTouchEnd to return early.
			if (this.trackingClick) {
				this.targetElement = null;
				this.trackingClick = false;
				return true;
			}
	
			// Very odd behaviour on iOS (issue #18): if a submit element is present inside a form and the user hits enter in the iOS simulator or clicks the Go button on the pop-up OS keyboard the a kind of 'fake' click event will be triggered with the submit-type input element as the target.
			if (event.target.type === 'submit' && event.detail === 0) {
				return true;
			}
	
			permitted = this.onMouse(event);
	
			// Only unset targetElement if the click is not permitted. This will ensure that the check for !targetElement in onMouse fails and the browser's click doesn't go through.
			if (!permitted) {
				this.targetElement = null;
			}
	
			// If clicks are permitted, return true for the action to go through.
			return permitted;
		};
	
		/**
	  * Remove all FastClick's event listeners.
	  *
	  * @returns {void}
	  */
		FastClick.prototype.destroy = function () {
			var layer = this.layer;
	
			if (deviceIsAndroid) {
				layer.removeEventListener('mouseover', this.onMouse, true);
				layer.removeEventListener('mousedown', this.onMouse, true);
				layer.removeEventListener('mouseup', this.onMouse, true);
			}
	
			layer.removeEventListener('click', this.onClick, true);
			layer.removeEventListener('touchstart', this.onTouchStart, false);
			layer.removeEventListener('touchmove', this.onTouchMove, false);
			layer.removeEventListener('touchend', this.onTouchEnd, false);
			layer.removeEventListener('touchcancel', this.onTouchCancel, false);
		};
	
		/**
	  * Check whether FastClick is needed.
	  *
	  * @param {Element} layer The layer to listen on
	  */
		FastClick.notNeeded = function (layer) {
			var metaViewport;
			var chromeVersion;
			var blackberryVersion;
			var firefoxVersion;
	
			// Devices that don't support touch don't need FastClick
			if (typeof window.ontouchstart === 'undefined') {
				return true;
			}
	
			// Chrome version - zero for other browsers
			chromeVersion = +(/Chrome\/([0-9]+)/.exec(navigator.userAgent) || [, 0])[1];
	
			if (chromeVersion) {
	
				if (deviceIsAndroid) {
					metaViewport = document.querySelector('meta[name=viewport]');
	
					if (metaViewport) {
						// Chrome on Android with user-scalable="no" doesn't need FastClick (issue #89)
						if (metaViewport.content.indexOf('user-scalable=no') !== -1) {
							return true;
						}
						// Chrome 32 and above with width=device-width or less don't need FastClick
						if (chromeVersion > 31 && document.documentElement.scrollWidth <= window.outerWidth) {
							return true;
						}
					}
	
					// Chrome desktop doesn't need FastClick (issue #15)
				} else {
					return true;
				}
			}
	
			if (deviceIsBlackBerry10) {
				blackberryVersion = navigator.userAgent.match(/Version\/([0-9]*)\.([0-9]*)/);
	
				// BlackBerry 10.3+ does not require Fastclick library.
				// https://github.com/ftlabs/fastclick/issues/251
				if (blackberryVersion[1] >= 10 && blackberryVersion[2] >= 3) {
					metaViewport = document.querySelector('meta[name=viewport]');
	
					if (metaViewport) {
						// user-scalable=no eliminates click delay.
						if (metaViewport.content.indexOf('user-scalable=no') !== -1) {
							return true;
						}
						// width=device-width (or less than device-width) eliminates click delay.
						if (document.documentElement.scrollWidth <= window.outerWidth) {
							return true;
						}
					}
				}
			}
	
			// IE10 with -ms-touch-action: none or manipulation, which disables double-tap-to-zoom (issue #97)
			if (layer.style.msTouchAction === 'none' || layer.style.touchAction === 'manipulation') {
				return true;
			}
	
			// Firefox version - zero for other browsers
			firefoxVersion = +(/Firefox\/([0-9]+)/.exec(navigator.userAgent) || [, 0])[1];
	
			if (firefoxVersion >= 27) {
				// Firefox 27+ does not have tap delay if the content is not zoomable - https://bugzilla.mozilla.org/show_bug.cgi?id=922896
	
				metaViewport = document.querySelector('meta[name=viewport]');
				if (metaViewport && (metaViewport.content.indexOf('user-scalable=no') !== -1 || document.documentElement.scrollWidth <= window.outerWidth)) {
					return true;
				}
			}
	
			// IE11: prefixed -ms-touch-action is no longer supported and it's recomended to use non-prefixed version
			// http://msdn.microsoft.com/en-us/library/windows/apps/Hh767313.aspx
			if (layer.style.touchAction === 'none' || layer.style.touchAction === 'manipulation') {
				return true;
			}
	
			return false;
		};
	
		/**
	  * Factory method for creating a FastClick object
	  *
	  * @param {Element} layer The layer to listen on
	  * @param {Object} [options={}] The options to override the defaults
	  */
		FastClick.attach = function (layer, options) {
			return new FastClick(layer, options);
		};
	
		if ("function" === 'function' && (0, _typeof3.default)(__webpack_require__(133)) === 'object' && __webpack_require__(133)) {
	
			// AMD. Register as an anonymous module.
			!(__WEBPACK_AMD_DEFINE_RESULT__ = function () {
				return FastClick;
			}.call(exports, __webpack_require__, exports, module), __WEBPACK_AMD_DEFINE_RESULT__ !== undefined && (module.exports = __WEBPACK_AMD_DEFINE_RESULT__));
		} else if (typeof module !== 'undefined' && module.exports) {
			module.exports = FastClick.attach;
			module.exports.FastClick = FastClick;
		} else {
			window.FastClick = FastClick;
		}
	})();

/***/ },
/* 225 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	
	var _defineProperty2 = __webpack_require__(226);
	
	var _defineProperty3 = _interopRequireDefault(_defineProperty2);
	
	var _getUserInfo$getWxcon;
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	var api = 'http://www.yangshenghui.net/admin/api/';
	var basic = 'http://www.yangshenghui.net/admin/basic/';
	var pay = 'http://www.yangshenghui.net/admin/order/';
	exports.default = (_getUserInfo$getWxcon = {
	    getUserInfo: basic + 'get_user_info',
	    getWxconfig: api + 'get_wxconfig',
	    choiceIdentity: api + 'Choice_identity',
	    getMassagistInfo: api + 'get_massagist_info',
	    getHaveProject: api + 'get_have_project',
	    getHandleProject: api + 'get_handle_project',
	    getAddProject: api + 'get_add_project',
	    getWorkOrder: api + 'get_work_order',
	    applyProject: api + 'apply_project',
	    updateTechnicianState: api + 'update_technician_state',
	    deleteTechnicianImg: api + 'delete_technician_img',
	    getTechnicianEvaluate: api + 'get_technician_evaluate',
	    updateUserInfo: api + 'update_user_info',
	    getProvince: api + 'get_province',
	    getStoreContents: api + 'get_store_contents',
	    getProjectList: api + 'get_project_list',
	    getProjectInfo: api + 'get_project_info',
	    uploadImg: api + 'upload_img',
	    projectSwitch: api + 'project_switch',
	    handleProject: api + 'handle_project',
	    getTList: api + 'get_t_list',
	    getDataTable: api + 'get_data_table',
	    getProjectProportion: api + 'get_project_proportion',
	    getUserOrderlist: api + 'get_user_orderlist',
	    getUserOrderInfo: api + 'get_user_order_info',
	    getOnlineProject: api + 'get_online_project',
	    getProjectTechnician: api + 'get_project_technician'
	}, (0, _defineProperty3.default)(_getUserInfo$getWxcon, 'getMassagistInfo', api + 'get_massagist_info'), (0, _defineProperty3.default)(_getUserInfo$getWxcon, 'generateOrderHtml', api + 'generate_order_html'), (0, _defineProperty3.default)(_getUserInfo$getWxcon, 'getRoomNum', api + 'get_room_num'), (0, _defineProperty3.default)(_getUserInfo$getWxcon, 'userSubmitOrder', api + 'user_submit_order'), (0, _defineProperty3.default)(_getUserInfo$getWxcon, 'updateOrderStae', api + 'update_order_stae'), (0, _defineProperty3.default)(_getUserInfo$getWxcon, 'getCommentHtml', api + 'get_comment_html'), (0, _defineProperty3.default)(_getUserInfo$getWxcon, 'releaseComment', api + 'Release_comment'), (0, _defineProperty3.default)(_getUserInfo$getWxcon, 'getStoreWx', api + 'get_store_wx'), (0, _defineProperty3.default)(_getUserInfo$getWxcon, 'deleteOrderList', api + 'delete_order_list'), (0, _defineProperty3.default)(_getUserInfo$getWxcon, 'wxPayIndex', pay + 'res_order'), (0, _defineProperty3.default)(_getUserInfo$getWxcon, 'wxPayOrder', pay + 'order_num'), (0, _defineProperty3.default)(_getUserInfo$getWxcon, 'userGetHandSelect', api + 'user_get_hand_select'), (0, _defineProperty3.default)(_getUserInfo$getWxcon, 'user_get_goods_select', api + 'user_get_goods_select'), (0, _defineProperty3.default)(_getUserInfo$getWxcon, 'user_add_goods_this_order', api + 'user_add_goods_this_order'), (0, _defineProperty3.default)(_getUserInfo$getWxcon, 'user_get_this_order_goods', api + 'user_get_this_order_goods'), (0, _defineProperty3.default)(_getUserInfo$getWxcon, 'Binding_member', api + 'Binding_member'), (0, _defineProperty3.default)(_getUserInfo$getWxcon, 'user_get_member_info', api + 'user_get_member_info'), _getUserInfo$getWxcon);

/***/ },
/* 226 */
/***/ function(module, exports, __webpack_require__) {

	"use strict";
	
	exports.__esModule = true;
	
	var _defineProperty = __webpack_require__(55);
	
	var _defineProperty2 = _interopRequireDefault(_defineProperty);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	exports.default = function (obj, key, value) {
	  if (key in obj) {
	    (0, _defineProperty2.default)(obj, key, {
	      value: value,
	      enumerable: true,
	      configurable: true,
	      writable: true
	    });
	  } else {
	    obj[key] = value;
	  }
	
	  return obj;
	};

/***/ }
/******/ ]);
//# sourceMappingURL=app.js.map